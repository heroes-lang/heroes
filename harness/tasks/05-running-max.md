# UNVERIFIED — author must confirm

Write `running_max = function: (@best: int, x: int)` that updates a mutable
parameter to the max of itself and x, and a `main` that folds it over an
array using a `for x in xs` loop, then prints the result. Include one test.
