# UNVERIFIED — author must confirm

Define a record `FileMove` with two fields, `from_path` and `to_path`, both
strings. Write a function `describe` that takes a `FileMove` and returns the
string "<from> -> <to>" (using string concatenation), and a `main` that
prints the description of one example move. Include one test.
