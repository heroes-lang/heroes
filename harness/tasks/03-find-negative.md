# UNVERIFIED — author must confirm

Write `first_negative = function: (xs: [int]) -> int?` returning the first
negative element or an error with code "none_negative". In `main`, call it
on two arrays (one with, one without a negative) and print either the value
or the error code, using match.
