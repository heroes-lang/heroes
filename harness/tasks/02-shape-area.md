# UNVERIFIED — author must confirm

Define a variant `Shape` with cases `square` (side: int) and `rect`
(w: int, h: int). Write `area = function: (s: Shape) -> int` using match,
and a `main` that prints the areas of one square and one rectangle.
Include a test asserting both areas.
