# UNVERIFIED — author must confirm

Write a function that counts, in a map `{str: int}`, how many times each
single-character string appears in an input string (use s.chars()). Print
the count for a chosen character, handling the missing-key case explicitly.
