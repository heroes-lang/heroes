- [ ] **M-deferral-ledger 2** | design.md deferred doctests *"until the first mechanism is proven"*. That condition has been met for months — 641 `test` blocks in the compiler, 545 in the examples, one of the three suites made of them — and nothing happened. Say what a condition needs in order to be able to wake somebody, and name the one thing every return condition panel 135 and 136 wrote has that this one did not.

    **Where to look:** design.md §4.18's doctest paragraph and its correction
    beneath; Part 7 item 6; the four return conditions in
    `docs/panel/136-the-item-named-three-ancestors-and-the-two-it-needed-were-elsewhere.md`
    § The resolution adopted; and `docs/work/milestones/M-deferral-ledger.md`'s
    own opening sentence about what a promise with no date is.

    **Why it matters:** this is the milestone's whole thesis arriving as a
    measurement rather than as an argument. The condition was not vague and it
    was not wrong; it simply named no instrument and no reader, so nothing in the
    repository could ever notice it had come true. Every condition the two
    sittings since have written names a command — a `grep -c` with a number, a
    `heroes mutate` kill rate, a suite that must stay green — and that difference
    is the entire lesson.
