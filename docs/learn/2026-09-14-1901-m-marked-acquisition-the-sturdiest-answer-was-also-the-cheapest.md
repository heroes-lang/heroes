- [ ] **M-marked-acquisition exit quiz** | Panel 148 priced two spellings of the same mark: a bare `acquires`, and one that NAMES the function taking the handle back. **Before looking: say which you would expect to cost more spec tokens, and why.** Then read the two measured numbers and the two repairs that separate them.

    **Where to look:** `docs/measurements/010-spec-budget-ledger.md` rows 74 and
    75; `docs/panel/148-the-mark-is-written-and-never-inferred-and-it-names-what-ends-the-life.md`
    § What the critic changed, points 3 and 4;
    `docs/panel/148-reports/spec-warden.md` Findings 1 and 2.

    **Why it matters:** the naming version says strictly more, so it should cost
    more, and the historian who proposed it estimated exactly that — *"more than
    A's +64"*. It measured **+66 against the bare word's +70**, because the bare
    word needed two repairs the warden found and the named one already carried
    them: its grammar did not derive its own example (only `CParam` had been
    amended, so `-> Curl acquires f` was underivable), and it wrote a second copy
    of a clause the spec already carries for `lease`, where merging beats
    appending.

    **So the sturdier answer and the cheaper text were the same text**, and
    CLAUDE.md § 4's rule — the most robust resolution, never the cheapest — and
    the budget pointed the same way for once. **The question to carry away is
    what that coincidence rests on**: it is not luck. Ask why a form that says
    more can cost less in a document whose rule is *one rule, one home*, and what
    that implies about comparing two drafts neither of which has been through the
    repairs the other has.
