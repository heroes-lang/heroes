# Measurements

One file per run: docs/measurements/NNN.md with provenance stamps
(spec sha, compiler sha, model id, prompt sha, suite sha). Never diff
runs with different compiler shas unless flagged. See harness/.
