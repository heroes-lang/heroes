2026-09-03 | **A type parameter takes the type the context asks for** (`docs/panel/105`, full panel, five seats, **provisional — author ratification pending**; the defect is `docs/defects/006`, found the same day by M-corpus-depth step 5). The spec has said *"always inferred"* since the beginning and never said from WHERE; design.md §4.12 said *"look at argument types, deduce `T`"*, and a type parameter that appears only in a function's result — `function wanted<A>(what: str) -> A?` — passed `check` at exit 0 and died in `build` at exit 134 with `assert failed: false`, because `substitute` turned the unbound parameter into the silent error id and nothing reported it. Measured by the pragmatist over 21 adjacent shapes: 0 refused by `check`, 3 caught by the verifier, 12 aborting at build, **3 building clean and panicking at run time**. Two rulings were on the table and the seats split 3–1–1 for A: infer from the context (the annotation, the declared result reaching a `return` or an arm, a non-generic callee's parameter), refuse a call with no context AT the call, refuse a parameter that appears nowhere in the signature at the declaration. The ergonomist's argument carried it — with no constraints a result-only parameter can only produce a failure, an empty container or a hole, so whatever type the context picks the value is the same, and the spec's own `fail`, `[]`, `{}` and `nullptr` are already context-typed; the engineer measured that B's premise (*"no call could ever bind it"*) is false because `check(expected)` binds it at every ⇐ position, so a Part 6 refusal would have no falsifier; the historian found that every language that refuses ships an outlet Heroes lacks by design, and that Go's objection is to the NESTED case this checker already refuses. The warden objected on cost (+32 tokens against a burden no program in the repository meets: 52 of 52 signatures bind from arguments) and found the shape nobody had named — a parameter appearing NOWHERE compiles today. The author's instruction the same evening, robust over cheap, decided the tie. Two repairs land under any ruling: the verifier's `no_error_survives` (mono and owned, under *no holes*), and the emitter's bare asserts given words. Spec 3718 → 3750 when the sentence lands, paid by six registered predictions.
