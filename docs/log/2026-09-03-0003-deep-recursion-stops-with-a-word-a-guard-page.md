2026-09-03 | **Deep recursion stops with a word: a guard-page handler that names the function, and no depth counter** (`docs/panel/104`, soundness lane, compiler-engineer and ffi-pragmatist, **provisional — author ratification pending**; M-robustness-guards step 4). Measured before the sitting: the five-line recursion is exit 139 with silence on the Mac at 100,000 frames and on Linux, and **exit 127 with silence on Windows** at 1,000,000 — the number `hero_run_go` also uses for *program not found*; `runtime/` has no signal handling at all. **Both seats approve (a) with conditions and reject (b) for the same reason from two sides**: the engineer measured the depth counter at +55–60% per call at `-O0` and at 0.81 s against 0.000 s at `-O2`, because a counter with a side effect forbids the transformation that turns the recursion into a loop; the pragmatist showed that where the handler is blind — a library's own thread, a library overwriting the handler after `main` — the counter is blind too, since those frames are C. **The engineer's prototype works and moves nothing in the compiler**: `runtime/parts/stack.c`, 156 essential lines, installed from `hero_args_set` so the emitted `main` and the ABI stamp do not change; `panic: stack exhausted in d100000.down`, exit 134, on the Mac and in the Linux image; a frame walk names the function even when the fault lands inside `__vfprintf`. **Three findings corrected the brief**: *restore the default and return* is a compiled defect — a library that installed its own handler in a constructor loses its write-barrier at exit 138 — so the previous disposition is saved and **chained** (measured: 134 with the library recovering); on `si_addr` alone a wild store 8 KiB under the stack was reported *stack exhausted*, a false abort, so the handler needs **two witnesses**, the address in the guard region AND `sp` within 4 KiB of the bound; and `SIGSTKSZ` is 8,192 on glibc 2.41, so the alternate stack is sized in bytes, `mmap`ed with a `PROT_NONE` page below. Under `--sanitize` nothing is installed — ASan's report names the `.hero` line and is the better one. Linux needs `_GNU_SOURCE` and `-rdynamic` for the name; Windows gets `AddVectoredExceptionHandler` and is measured on the box before the commit or the step does not close. **Threads are a recorded hole, not a fix**: a C library's own thread that overflows dies 138 today and 132 with the handler; M-isolated-threads owns the per-thread stack. Real SDL2 and SDL3 install no SIGSEGV/SIGBUS handler after `SDL_Init`, which is the pragmatist's registered prediction | CLAUDE.md §1, §4, §12, §13 · design.md §1.11, §1.12, §4.19 | 104, 097, 070
