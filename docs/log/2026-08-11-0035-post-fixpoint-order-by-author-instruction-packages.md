2026-08-11 | **Post-fixpoint order, by author instruction: packages (M10) before concurrency (M11) before QBE (M12), lsp last (M13) — and QBE's warrant is restated.** It was recorded in 2026-08-03 as "a second backend, not maybe"; the honest form is that **as long as exactly one backend exists, "the IR is target-agnostic" is an assertion no artifact tests**, and the IR could be a C pre-processor wearing an abstraction's name without anything in this repo noticing. Packages need M9 rather than M8a — without separate compilation, installing one means recompiling the world — and they are also where "a standard library that wraps C" legitimately goes: §1.11 refuses a standard library permanently, but **distributable bindings** are ordinary modules over real headers, each verified by clang against the header it names | design.md:637 already fixed the shape (`heroes add`/`heroes fetch`, inside the same binary), so this is an ordering decision rather than a design one — no panel, and design.md Part 7 is deliberately left untouched so that a milestone number does not convert a deferral into a commitment | Part 7 items 13–14, §1.11, §3.5 | — |
