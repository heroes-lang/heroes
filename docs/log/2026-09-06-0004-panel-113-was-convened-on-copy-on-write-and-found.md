2026-09-06 | **Panel 113 was convened on copy-on-write and found the door somewhere else: a `[T]` crossed the FFI boundary inside a callback signature, at exit 0** (M-isolated-threads step 5; defect 014; `docs/panel/113-the-door-was-the-callback-not-the-copy.md`). `selfhost/check/ffi.hero`'s `crosses_the_boundary` is the one function that decides what a C header may spell. It refuses `.array | .fixed | .map | .fallible` and answered `.function_ty => return true` **without recursing into that function type's own parameters and result**, so the vocabulary rule was applied to the callback and never to what the callback carries. The bar was one forward declaration and no runtime include. **`spec:224` had already refused it** — *"a callback is a parameter, never a result; its parameters follow the same rule"* — so this was the compiler disagreeing with the specification (CLAUDE.md §12) and it cost no spec token to settle. The repair recurses, on the termination precedent at `check/table.hero:278-285`, and it **edits nothing that exists**: enumerated twice, by two seats, from the tree, there are **7 callback declarations inside `extern` groups and 0 of them name a container**. **And the sitting's real finding is the negative one**: both compiling seats, in separate checkouts and with the guard patched down, could not race `cow.c`'s `refcount == 1` — 8 threads and 240,000 mutations, ThreadSanitizer silent — while the same instrument named a hand-made race at `cow.c:80` in the same session. A callback parameter arrives borrowed and the emitted body increfs before it can store. Panel 111 measured the corruption correctly and attributed it to the wrong noun | §1.12, §4.19, Part 7.13 | 049, 053, 111, 113
