2026-08-12 | **An out-of-range `int` literal is the checker's, and `heroes check` had been exiting 0 on a program `build` refused.** The class landed in lowering, where the value is decoded — so `check`, whose whole job is *does this program have diagnostics*, never got that far and returned **0** on `print(99999999999999999999)` while `build` returned 1 on the same bytes. That is what an editor and `--apply` trust, and §10's exit-code contract says it cannot happen. The literal's text and *`int` is the only integer type* are both frontend facts. Lowering keeps its copy as a net and a test asserts it is unreachable rather than absent — a net that has never fired is indistinguishable from no net. And the code is now `int_out_of_range`: `int-literal-out-of-range` was the only hyphenated one among fifty | a diagnostic in the wrong pass is not merely late, it is invisible to the command whose contract is that it reports everything | §4.3, CLAUDE.md §10, §8 | 019 |
