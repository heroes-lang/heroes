2026-08-19 | **THE THIRD LANGUAGE IS GONE — `crates/` is `archive/bootstrap-rs/`, and design.md:82 is a fact rather than a plan** (M-bootstrap-archive close, tag `m-bootstrap-archive`). The way in is one clang line over `seed/heroes.c` (3.4 s), what runs the tests is the one command, and CI holds four steps of which none is Rust. **Five successors landed before the move and each was measured against the thing it replaced**: `tests/emission/` — 142 programs, 5.4 MB, both compilers green against the same bytes, which is the differential's claim recorded while a second implementation still existed to make it; `heroes measure` in Heroes at 3440 / 3512 / spread 72, **identical to the bootstrap's**, plus the three published BPE vectors; `tests/harness/suite_spec.hero`, 8 checks, replacing 435 lines of Rust in which every check was `cfg(test)`; `heroes mutate` at 538 mutants and 512 (96%) / 414 (78%), **byte-identical report including `--survivors`**; and the seed regenerated with both verbs inside it, fixpoint re-verified byte for byte (22,025,792 bytes, 15m41s). The port is **37,137 lines of Heroes across 153 files** against 25,482 non-test lines of Rust | **What the milestone found is worth more than what it moved.** The net was still holding the bootstrap's hand in nine self-tests that named `./target/debug/heroes` — in the harness built to survive that path's deletion; both record walks read a **gitignored TLS private key** and reported it as a milestone identifier, so they now ask `git ls-files` (a list of exclusions is a premise about which junk exists, git's answer is a fact about the project); the Heroes `numbered_in` **aborted** where its Rust twin reaches for `from_utf8_lossy`, which is the language refusing to guess; and panel 086 R7's dead-citation check went green on arrival — its own defect, because anchoring on existing roots stops recognising a path the moment its root moves — then found **nine dead citations that predate the archive** in the two documents this project is steered by. Three process texts and one README were edited rather than queued (`DECIDE.md` carries the revert list); CLAUDE.md §4's trigger paths and the Windows CI leg, which the archive left covering the toolchain and nothing else, are the author's | docs/journal/024-bootstrap-archive.md · archive/bootstrap-rs/README.md · docs/ROADMAP.md § Status · .github/workflows/ci.yml · docs/debrief/{DECIDE,SCHEDULED,LEARN}.md | 085, 086
