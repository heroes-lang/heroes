2026-08-13 | **Panel 046 ratified, and the ratification's whole content is *where the date lives*** (author decision (a), `/decide`). R1–R5 stand unchanged — a registered prediction pays only if it names an instrument that exists on the day of registration, an outstanding one is re-decided and never renewed, (b) refused at its stated price of a spec frozen at 2974 until M-selfhost-fixpoint with §1.0 items included. Three things the provisional default left implicit and the yes settles. **The rule is not retroactive**: R1 governs what may be *offered* as payment, so none of the six prediction-bought rows is un-bought and no clause leaves the spec — what R2 reaches is the obligation to look. **The expiry is M-program-corpus, not M-selfhost-fixpoint**, though five of the six named metric 2: the spec-warden's condition on its own yes was that the expiry not be the instrument's arrival date, *"otherwise the expiry is the tenancy"*. **And the date is written once**, in `measure/gate.rs`'s module doc beside the rows, rather than into each of the six — six copies of one date is six places for it to rot, which is the failure this sitting was convened about, restated as a filing rule | **the sixth row is the argument, and it happened while nobody was watching**: `2675` (panel 040's bitwise set) was scored *"at the next FFI rung"*, and M-ffi-ladder closed on 2026-08-12 — the same day the row was written — unlooked-at. That is a milestone-named expiry lapsing inside three days, in a repository whose author had just been shown two others lapsing, and it is why the ledger and not the panel file is where an expiry belongs: `/step`'s close bullet greps the ledger | design.md §1.6 · CLAUDE.md §12, §14 · .claude/skills/step/SKILL.md § 5 | 046, 040, 042, 038 |
