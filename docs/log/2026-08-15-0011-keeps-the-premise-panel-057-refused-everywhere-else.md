2026-08-15 | **`directory_text` keeps the `\` premise panel 057 refused everywhere else, written as a claim that can die** (author decision, `/decide`). The premise is real and the counterexample was created on this machine before the decision: `a\b.hero` is **one legal file** on Darwin and Linux, and this function reports its directory as `a\`, which does not exist. It is kept because a path reaching it was **typed by the author** on the command line and may use the Windows separator whatever the platform, and spelling a sibling module the way the author spelled the root is worth more than that case | **what makes it survivable is scope, not luck, and scope is what the test pins**: nothing opens a file with this result — `load_text` reaches the filesystem through `PathBuf::parent`, which is separator-correct, and this text reaches only `InputFile::name`, what a diagnostic prints. `a_backslash_name_is_wrong_here_and_right_where_files_are_opened` asserts both halves *and* that they disagree, so it fires the day the wrong answer reaches a real path — which is the only way the cost stops being cosmetic. The third option this item offered, *record an exemption because the display-name case is harmless*, was **struck by the measurement**: the printed name is wrong when the file exists, so there is nothing harmless to record | CLAUDE.md §11 · crates/heroes/src/modules/mod.rs | 057 |
