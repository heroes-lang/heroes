2026-09-10 | **The sitemap says when each page last changed, and the date is read from git rather than from the clock; `robots.txt` states the site's position on the crawlers that feed models instead of leaving it to `Allow: /`** (out of the session that asked why what a search engine shows does not match the site's new form). **The premise was half wrong, and the correction is the finding**: the SEO tags were written the same day as the redesign, on the author's own instruction, and the live site serves them, so nothing on the site described the old one. What was missing was any signal to come back and look. `<lastmod>` was absent on all **184** advertised addresses, and after a redesign that took 46 pages to 186, retired five addresses per edition and moved a sixth, the one thing a sitemap is uniquely able to say was the one thing it did not say. **Build time was refused as the source**: a date stamped at deploy claims 184 simultaneous edits on every push, which is a field carrying no information, and a date that is sometimes wrong is worse than one that is absent, because the absent one can still be added. The source is the last commit touching the page's OWN sources, its prose fragment and its wrapper, and never the shell, since `site/src/layouts/BaseLayout.astro`, the nav and the footer touch every page and folding them in would move all 184 dates together, which is the build-time stamp wearing a different hat. Measured after: **15 distinct dates over 184 addresses**, three of them verified by hand against `git log`. **The trap is silent and cost a workflow line**: `actions/checkout` is shallow by default and `git log` in a one-commit clone answers one date for every path, so `fetch-depth: 0` is now on the checkout step in `.github/workflows/deploy-site.yml`. **And the first audit passed on the exact run it existed to catch**, which is the sharper half: measured in a `--depth 1` clone, the refusal fired, `@astrojs/sitemap` **swallowed every throw from `serialize`**, all 184 entries were dropped, and the build wrote a sitemap holding **zero URLs and exited 0**; the audit was reading an array the config filled while building, a side channel, and an empty array cleared its own test. Both moved: the history check to `astro:build:start`, where a throw is not caught, and the audit onto the built `sitemap-index.xml` and its chunks, the files that ship. `site/CLAUDE.md` § No dates on the page is **not** relaxed, and the rule was read rather than stretched: it governs what a reader is shown, a sitemap is not a page, and no date became visible anywhere (**author decision**, put with three routes and the machine-only one chosen; the routes that would have put `dateModified` in the structured data and a date beside a search result were refused) | a sitemap that cannot say what moved leaves a redesign to be found by chance, and a check that cannot see the failure it exists for is worse than none, because it is also a reason not to look | — | —
