2026-08-16 | **Panel 077: the sitting refuted its own brief, and the flagship union was still printing a false number three hours after the panel that repaired it** (provisional — author ratification pending). The coordinator's brief asserted that C cannot detect a union naming one member, having checked `__is_union` (C++ only), `sizeof` equality and the positional probe. **Two seats that compile found the answer independently, in a builtin this emitter already calls on line 10 of every generated FFI unit**: `__builtin_classify_type(*(T *)0)` is **13 for a union and 12 for a struct**, in a `_Static_assert`, on a one-member union. So panel 073's `sizeof(T) >= Σ sizeof(field)` is replaced: it is **incomplete for a padded union** — `union { int32_t i; float f; char pad[128]; }` passes `128 >= 8` and prints **1065353216**, the same false number panel 073's own commit body cites as the defect it killed — and **`SDL_Event` is a padded union**. Four more defects land with it, three of them introduced or left by that morning's own commit: the trigger gains the **map key** (`hash` was reachable where `==` was refused, which is worse than refusing neither); a **tagged** union was **exit 2** because `typedefs.rs` spells `struct` unconditionally, which C11 6.7.2.3#1a (DR 251) makes a constraint violation clang already diagnoses and even names the right word for; `extern_union.rs`'s marker carried `c_type` where panel 072 rider 1 established it must carry the **declaration's** name, one file over from where that was fixed the same morning; and `extern_complete.rs` emits `T v = {0};`, which **clang exempts from `-Wmissing-field-initializers`** — so a one-field record over any multi-field struct silently claimed completeness, and `record FileStat tag stat` naming `st_size` said **two different files, different inodes, different mtimes, were equal at exit 0**. That last one has **no union in it**, is panel 061's transparent-colour defect reborn in the one-field case, and the repair is **two characters**: `(0)`. A **premise-death control** ships with the predicate (CLAUDE.md §11): two assertions stating this clang still answers 13 and 12, because clang answered 12 for unions before 2019 and a regression would turn every union check into a silent pass. The spec says **the size and not the construction** (+12…+14 measured): construction's wrong guesses are all exit 1 with the rule in the message (panel 035 R4), while the size has no diagnostic **because there is no mistake** — the reader who guesses wrong gets a correct program, and the cost is a binding **never written**, which two blind seats demonstrated by refusing to write one. The historian supplied what pays for it: **every FFI that let the field list decide the size shipped a wrong number** — Nim's `dirent` 272 vs 1, Rust's `libc` `struct stat` 136 vs 120 — and every one that got it right documents the rule. Blast radius measured over five real libraries: **12 of 382 named structs (3.1 %)**, SQLite **0**, raylib **0**. And the contradiction stays on the record: **Go has shipped `==` over C-union bytes for fifteen years with no public bug report**. `docs/panel/077`
