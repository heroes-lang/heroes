2026-08-15 | **Panel 061: the partial field list is adopted, and the sitting found it was already shipping** (author instruction *"ok rendili legabili"*, adopting what panel 060 queued; provisional — ratification pending). Spelling **B**, `record Font partial`; `==`, `hash` and a map key refused **transitively**; **construction permitted**; `_eq`/`_hash` still emitted for a partial record as `hero_panic` bodies so no `HeroDesc` holds a null. Q3 splits — construction **per-declaration**, comparison **transitive** — because the transitive default would make raylib's `RenderTexture { Texture texture; }` unbuildable, which is the milestone's own use case | **`spec:211` says *"all its fields"* and nothing enforces it**: `SDL_Event` binds with 1 member of 41 and runs at exit 0 today, so panel 060's *"Q2 = complete"* ratified a rule that was never implemented. The cost is a **silent wrong answer shipped this morning** — three of raylib's four `Color` fields, every `_Static_assert` green, zero warnings, exit 0, `0xFF000000` where C gives `0xFF0000FF`. So Q1 never enabled partial; its real job is making **complete** checkable, and the answer is a mechanism no ballot option contained and two judges compiled independently: a never-called **positional** probe, `Font f = {0,0};`, which `-Wmissing-field-initializers` names by field at **zero spec tokens**, while the designated form the emitter writes warns nothing even under `-Wextra`. **E was refused on evidence rather than by vote**: the llm-ergonomist wanted Nim's polarity because the *unmarked* record is the dangerous one — correct, and its own programs proved it — but the historian read `sizealignoffsetimpl.nim` and found Nim's default-incomplete governs **`typ.size` only** and refuses no construction, and the compiler-engineer found that under E a default program carries **fewer** `_Static_assert`s and the author opts in to being checked, which is the wrong direction for a robustness rule and is invisible from the spec alone. **A is vetoed** (spec-warden): its own sentence declares the spec's `Point(x: 3, y: 4)` a compile error, and it costs a lexer token | spec § FFI · design.md §1.0, §1.7, §1.11, §1.12, §4.19 · CLAUDE.md §7, §11, §12 | 060, 061
