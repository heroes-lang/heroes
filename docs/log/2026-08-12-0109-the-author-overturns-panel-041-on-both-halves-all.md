2026-08-12 | **The author overturns panel 041 on both halves: all four bases land, and sized integers are built after all** (author decision, in conversation the same day the panel sat; the verdict is appended to `docs/panel/041` and CLAUDE.md §4 makes that the panel's closing act rather than a new sitting). Notation is adopted **wider than proposed** — `0x` `0o` `0b` and the `_` separator, not hexadecimal alone — on the author's ground that *"se sono interi sono interi"*: a base is how an existing value of an existing type is written, and nine languages have all four with none regretting it. The spec-warden's consistency argument from panel 035 R6 is answered rather than set aside, and the answer is the disanalogy the sitting accepted too fast: **an exponent creates values that do not otherwise exist and a base creates none**, so R6's "new syntactic form" conjunct does not reach it. The **value** reading survives the ratification, which means the one veto that changed nothing is the one that was right. Octal earns its place twice: panel 041's own D1 defect — `print(0700)` printing `700` — has two repairs and the sitting offered only the narrow one, where giving octal a real spelling closes it *and* delivers the notation. That is Python 3's answer and Rust's and Go's `0o`, arrived at because the bare leading zero is a trap | on the second half the ask is **bigger than the menu the panel judged**: `i8 i16 i32 i64` and `u8 u16 u32 u64`, first-class, the Rust shape — not option (b), which every judge who could see the repository rejected, and not option (c) as priced, which the panel described in one line and refused in one line. Half of what design.md:833 calls "the most expensive spec item that exists" is already paid, because §4.3 forbids implicit conversions outright. **Part 7 item 10 is retired, not reinterpreted** — this overrides *"never in v1"* exactly as panel 040 overrode §4.14's own sentence, and the milestone that retires it must say so. Three judge objections survive as work rather than as doubt, all measured: `Ty::Cstr` appears 11 times across 9 files with **9 of them silent `_` arms**, so every new `Ty` variant inherits nine defects under CLAUDE.md §11; `_Generic` **cannot be spelled with typedefs** (`size_t` and `uintptr_t` in one association is a hard clang error), so the FFI half must use fundamental types plus a `sizeof` companion assertion or a width-named type cannot verify its own name; and the historian's Kotlin precedent now points the other way, since the KEEP's motivation **#1** for unsigned types was hexadecimal literals overflowing the signed type — this ratification's two halves, in the order they are being built. Two milestones and deliberately not one (`M-literal-bases`, then `M-sized-integers`): decision 1a made the cheap question hostage to the expensive one that morning and said so in its own words, and folding the notation into the type system would be the same trap a third time. Four of the sitting's ten predictions become checkable early and are scored at `M-literal-bases` close; four were written about option (b), are not what is being built, and are **retired unscored** with the reason recorded rather than the numbers quietly dropped | §4.3, §4.15, §1.6, Part 7 item 10, CLAUDE.md §4, §9, §11 | 035, 040, 041 |
