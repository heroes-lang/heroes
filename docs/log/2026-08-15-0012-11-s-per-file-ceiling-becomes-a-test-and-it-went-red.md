2026-08-15 | **§11's per-file ceiling becomes a test, and it went red on four files the moment it existed** (author decision, `/decide`). The 2026-08-12 sweep took the tree to zero breaches in one run; ordinary work put **four** files back over it within three days, unnoticed, because nothing was watching. `crates/heroes-cli/tests/layout.rs` walks `crates/`, excludes tests **by where they sit** rather than by a name list, and carries a `DECIDED` table whose load-bearing column is the **reason** — a bare exception list decays into the breach list it replaces | it is a **test** and not a `/step` checklist line because a checklist is discipline and discipline is what already failed twice, and not a `heroes` subcommand because §10's stopping rule says *nothing if two existing invocations already compose to it* and `find crates -name '*.rs' \| xargs wc -l \| sort -rn` is that composition — what a subcommand would add is not the measurement but the **obligation**, and an obligation is what a test already is. Same shape as `emit/tests/gate.rs` and `clippy.toml`. **An entry in `DECIDED` is a ceiling, not a licence**, which is how `toolchain.rs` at **425** against a 400 settled by name became visible at all. Four cuts, each along a seam that names a concern: `emit/unread.rs` (what the emitted C **reads** — `inst.rs`'s per-op half and `body.rs`'s per-function half, one question in two places), `syntax/extern_members.rs` (what a group **declares**, against `externs.rs`'s what it **names**), `commands/flags.rs` (what clang **enforces**, which knows no path — deliberately *not* the cache-key seam the author's 2026-08-14 decision refused). Emitted C **byte-identical over 66 goldens** at every step (`f990db898b5a`) | CLAUDE.md §11, §10 · design.md §1.6 | — |
