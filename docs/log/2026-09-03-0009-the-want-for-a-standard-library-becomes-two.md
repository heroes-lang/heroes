2026-09-03 | **The want for a standard library becomes two milestones: small packages organised as Go's tree, and the web framework that composes them** (`M-core-packages`, chain row 36, and `M-web-framework`, row 37, both **author instruction**, out of a reasoning session the same evening — `docs/reasoning/005-packages-in-place-of-a-standard-library.md`). The author's words: *"vorrei avere tutti gli strumenti necessari a costruire un web framework alla Rails o Django o anche più sottile, tipo Go, Echo o FastAPI"*, then, when one toolkit was proposed, *"mi immagino più pacchetti che si combinano e poi quello web che li usa tutti; il modello è Go come organizzazione"*. **What the record already said, read forward before anything was proposed**: §1.11 refuses a standard library permanently (`design.md:443-445`) and panel 097 condition 5 keeps `selfhost/library_source.hero` shut; `DESIGN-LOG:178` and the ROADMAP's M-package-manager entry route *"a standard library that wraps C"* to distributable bindings; Part 9 says of the web that JSON *"is a library you would write yourself, like everything else"*; and a package written in Heroes alone is ruled on by nothing — no Part 6 row, no Part 7 item, deliberately (`DESIGN-LOG:178`). **Three decisions**: after M-package-manager, because `heroes fetch` is what makes a package a thing you distribute; Go's tree as the organisation, which `use net/http` already spells since M-package-layout (`spec:10-13`); Go's and Echo's level, everything explicit, because the Rails and Django shape rests on Part 6's own rows (`design.md:2382-2404`, `:2395`). **The sitting sits at the opening, not now** (M-interpolation-verdict's precedent), with six questions — the §1.11 boundary for a pure-Heroes package; where packages live (measured: a module that `use`s a sibling package compiles only from the program's root, so `heroes fetch` places a tree under the root); the byte buffer's three routes; `net` as a runtime part; Part 7 item 10 widened to *width or sign*; and, added by the author the same night, **conditional compilation** — five shapes surveyed (C's `#if`; Nim's `when`, D's `version`, Odin's `when`, Swift's `#if`; Rust's `cfg`; Go's one file per platform; nothing, Ada and Oberon, which is where Heroes stands), against panel 049's veto of the platform axis, panel 097's arm in the runtime, panel 039's unplaced comptime, §4.15, and the CI leg that `cmp`s the seed against what the compiler emits on every platform. **Measured before scheduling, on the Mac and in the Linux image**: a single-threaded HTTP server written in Heroes answers `curl` today with no language change — `socket`/`bind`/`listen`/`accept` as `extern`s, the address as `record SockAddr tag sockaddr` with the port bytes computed in Heroes, `fdopen`+`fgets` in, `write` out, about 100 lines, three record lines different on Linux; `hero_str_from_bytes` brings a `ptr` of known length back as a `str` (10 MB in 0.32 s); SHA-256 and HMAC over `package "libcrypto"` run on both; a libcurl client with no callback fetched from that server. The callback boundary — `selfhost/check/ffi.hero:45` refuses `.function_ty` while `selfhost/emit/ctype.hero:375-380` already emits a function value as a C function pointer — is needed by **no package on the list** and by threads, so it is filed under M-isolated-threads with the loopback server as that sitting's program. **What blocks a distributable `net`**: `sockaddr` differs between Darwin and glibc (`ffi_field_type` on the other platform, both ways) and Windows is winsock; `clockid_t` is `u32` on Darwin and `i32` on glibc. **Two compiler defects found by the probes and filed, not fixed**: `xs @ xs.push(c.to_u8().must())` copies the array on every push — 100,000 pushes in 14.78 s against 0.00 s with the value bound first, `selfhost/ir/place_store.hero:100` — and `@value: i32` against a `const void *` pointee is `internal error` at exit 2 on both platforms, panel 103's pointee assertion writing `sizeof(const void)`. **The corpus, measured**: 78 files, 10,356 lines, 35 programs and the gallery, 6 files with an `extern`, zero socket calls, zero thread creation; `split_lines` copied in 8 files, `is_space` 8, `trimmed` 7, `index_of` 4, and `tests/harness/strings.hero` a further `trimmed`. **Records moved**: rows 36–37 inserted and 38–44 renumbered, `ROWS` 46 → 48, five items in `SCHEDULED.md`, the ROADMAP's `design.md:637` corrected to `:772`, and panel 056's deliverable D — the three return conditions in the M-package-manager entry — discharged nineteen days after its ratification; deliverable B, the two stale inventories in design.md and Part 7 item 4's *"no aliases"* wait for the sitting, because they are Parts 1–11 | design.md §1.11, §4.15, §4.19, Part 6, Part 7 items 4, 10, 13, Part 9 · CLAUDE.md §1, §4, §9, §12, §14 · spec:10-13 | — (013, 028, 032, 035, 036, 039, 049, 056, 057, 087, 092, 094, 097, 099, 103 are what it inherits)
