2026-08-18 | **A caret that swallowed a comment: the 2026-08-12 repair had reached statements and not declarations** (defect, no panel — same class as the three the fixpoint found, `docs/journal/021`). `tests/golden/unsupported/ffi-writable-parameter.expected` underlined **71** columns of `    function strtok(s1: cstr, s2: cstr) -> cstr   #~ ffi_writable_parameter` — the declaration **and** the case's own annotation — and had been pinning that as correct since the case was written | **The span of a declaration depended on whether somebody had written a comment beside it.** `decl.rs:207` closed an `extern` member's extent with `previous_span()`, which is the last token *consumed*: `body_check` skips terminators looking for an indented block, and a trailing comment is a token, so the extent reached past it. `previous_significant_span()` — written 2026-08-12 for this exact defect on statements, with `f() # a note` underlined 23 columns wide for a three-column statement — was never applied here. One-line repair, and the measurement is the evidence: **71 columns with the comment, 43 without**, on two files differing by that comment alone; **43 in both** after. Nine other `#~`-carrying goldens all stop at their construct (measured), so this declaration was the only one. **What exposed it was a second implementation disagreeing** — the port never had the defect — which is the argument for keeping a differential past the archive, and the reason `M-bootstrap-archive`'s entry already asks for one that survives its own oracle. `cargo test` 576 green; the one expectation changed by a **single line**, read and quoted | crates/heroes/src/syntax/decl.rs:207 · crates/heroes/src/syntax/cursor.rs:133 · tests/golden/unsupported/ffi-writable-parameter.expected · CLAUDE.md §9 | —
