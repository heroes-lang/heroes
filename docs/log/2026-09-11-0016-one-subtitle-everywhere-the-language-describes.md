2026-09-11 · **One subtitle, everywhere the language describes itself: *A small
compiled language*, and *Un piccolo linguaggio compilato* in the Italian
edition** (**author decision**, taken 2026-09-10 out of the session that asked
which of five wordings to keep, applied and pushed 2026-09-11). Measured before
the choice: four adjectives were in service across about thirty places. The
specification's third line, the README's first paragraph, CLAUDE.md, the title
of design.md and both home meta descriptions said *small compiled*; the home
kicker in both editions, the README banner and its alt said *simple
programming*; the GitHub description said *simple compiled*; the social card alt
said *compiled programming*; the two home share cards had *A COMPILED
PROGRAMMING LANGUAGE* baked into their pixels; and the page title said neither.
The choice is the specification's own line, and the reasons are on record.
*Small* is the adjective an instrument counts, the 6144-token ceiling with its
red test and the line count, where *simple* is an opinion every language claims
of itself. *Compiled* is the fact that separates Heroes from Python, which it
reads like, and it states the category by itself, so *programming* adds a word
and no information. It reverses the kicker decision of 2026-09-09, *A simple
programming language*, which was the author's word and is the author's to
reverse. The page title followed, by the author's choice against the
recommendation to keep it for search. Two consequences on the home page: the
claim under the name no longer opens with *A compiled language*, so the word is
said once, and its close reads *small enough to fit in a prompt*, the measured
half of the old promise line. The home share cards were redrawn from the
pixels, and their template is in the repository for the first time,
`site/cards/home.html`, English only, because `records/english` reads every
`.html` outside the Italian edition. Left as they are, on purpose: the frozen
measurement prompt `harness/prompts/first-try.md`, which says *a small
programming language* and whose change would open a new series; the quotations
of the original brief in design.md §1.11 and journal 012; and the README's h3
hook, which is not the subtitle. No panel sat: a subtitle is copy and not
language, and the specification did not change.
