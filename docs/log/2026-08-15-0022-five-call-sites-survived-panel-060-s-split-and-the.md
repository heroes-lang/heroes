2026-08-15 | **Five call sites survived panel 060's `of`/`satellite` split, and the order of work is now part of panel 061's resolution.** `emit/aggregate.rs:92,240,251` and `emit/descriptors.rs:229,262` read the C **type** table where they need the generated-function **prefix**, so on any group `record` — complete or not — `a == b` is `call to undeclared function 'Color_eq'` at exit 2, and `[Color]` emits `static const HeroDesc Color_desc`, a **global unmangled name beside the library** (CLAUDE.md §7). Found independently by the spec-warden and the ffi-pragmatist, three sites and two, on their first programs | **The consequence outranks the bug**: the two operations panel 061 spends spec tokens forbidding cannot be performed on any group record at all, so the refusal's control arm is unwritable and a golden asserting it would pass **vacuously** — gate row `2675`'s finding for the fourth time. Both compiling judges made the sweep a condition of their verdicts, so the resolution lands it **before** partial rather than with it. Two further items ride the same commit: a group whose members are only `record`s emits **no `#include`**, and `heroes fmt` **deletes the word `partial`** while `printer/dump.rs` drops it too — so `assert_canonical`'s *"fmt changed the tree"* check is blind in the same place and **two blind spots cancel into a green test**. That is panel 060's `fmt_extern.rs` hoisting one milestone later, in the same file family, with the round-trip guard complicit | crates/heroes/src/emit/aggregate.rs · crates/heroes/src/emit/descriptors.rs · crates/heroes/src/printer/dump.rs · CLAUDE.md §7, §9 | 060, 061
