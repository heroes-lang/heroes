2026-08-31 | **M-argv-execution closes: the shell is out of the compiler, and Windows is a first-class leg** (26 steps, journal `docs/journal/026-argv-execution.md`). The decisions that outlive the day, each provisional under §4 with its item in `docs/work/DECIDE.md`: the watchdog is a runtime call (`hero_run_limit`, 124 on a kill — three probes could not tell coreutils' `timeout` from Windows' pause utility, so no probe decides any more), the runtime ABI sits at 18 and a bump propagates over THREE seed generations, and a Windows binary links with `-Wl,/STACK:67108864 -Wl,/INCREMENTAL:NO` because 1 MB of main-thread stack kills a recursive-descent compiler on its own modules, measured with two seeds one flag apart. Close: the net 1166/0 and the harness self-tests 92/92 on Windows, Darwin and Linux green in CI on the same commit.
