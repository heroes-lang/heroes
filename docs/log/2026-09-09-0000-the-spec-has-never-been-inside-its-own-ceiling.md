2026-09-09 | **The spec has never been inside its own ceiling, because the binding instrument was OpenAI's** (author's finding, author's key, author's question; `docs/measurements/023-the-instrument-was-not-the-readers.md`). `heroes measure` takes the larger of two VENDORED tokenisers, and the larger is `cl100k_base`, which is **OpenAI's** and which Anthropic's own documentation says undercounts Claude tokens by 15-20% on typical text and much more on code. Measured against `POST /v1/messages/count_tokens`, the request structure accounted for at **6** tokens by two independent probes: the spec is **4128** on Haiku 4.5 and Opus 4.6, which is **32 over** the hard 4096, and **5094** on Opus 5 and Sonnet 5, which is **998 over**. The two pairs agree exactly inside themselves, so this is two tokeniser GENERATIONS and not noise, and the gap between them is **966** tokens against the 78-token `spread` this project has used as its error bar all along. `CLAUDE.md` is **7190** against a `CONTRACT_CEILING` of 5500. So the 40 free tokens never existed, every ceiling number computed on 2026-09-08 is void as arithmetic — the 4224 confirmed, the 4237 recommended, panel 121's 4253 — and the clause deltas were understated by the same factor: the `f"…"` clause is **+151** where 022 read +118 and the `\{e}` clause **+126** where it read +99, so the ranking survives and the cheaper spelling is still cheaper, by 25 rather than 19 | §1.6 already said the reader's real tokeniser was unpublished and that the spread therefore applied to the ceiling; the sentence was right and the spread it warned about was an order of magnitude short, which is why the coordinator spent an evening computing a ceiling to the token on a number that was 27.5% low | §1.2, §1.6, §2 | 012, 024, 030, 086, 121 |
