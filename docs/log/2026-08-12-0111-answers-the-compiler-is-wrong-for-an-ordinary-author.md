2026-08-12 | **`heroes` answers *"the compiler is wrong"* for an ordinary author mistake, whenever a bound C function returns a pointer — and the `+` that causes it was never doing the job its comment claims** (panel 042, ffi-pragmatist; reproduced twice, independently). `extern function getenv(name: cstr) -> int` gives `internal error: compiling the generated C failed … invalid argument type 'char *' to unary expression`, exit 2. Unary `+` on a pointer is a **hard clang error**, so `_Generic(+(c), …)` never reaches the point of producing the marker string `emit/ffi.rs` matches, and CLAUDE.md §7's named exception — the one place a clang failure is the *author's* error and not the compiler's — cannot fire. The mechanism panel 036 built to say *"your declaration disagrees with the header"* fails on exactly the return type most likely to be declared wrong | the premise justifying the `+` is **false today**, measured on Apple clang 21 against a synthetic enum *and* against the real `CURLcode`: `emit/externs.rs:117-123` records that without it the macro *"refused every enum-returning C function in existence"*, and a bare `_Generic` accepts both. The repair at M-ffi-ladder was two changes made together — the `+`, and widening the accepted set to `unsigned int` and its narrower siblings — and **the second alone was sufficient**, because `_Generic` on an enum selects its compatible integer type, which the widened set now contains. So the `+` has been carrying a justification for work it does not do while breaking pointer returns. It is also what the narrow widths need removed, since `+` promotes away every distinction below `int` and makes an `i8` assertion either reject the correct binding or check nothing at all. Both point the same way, which is the only reason this is a repair and not a second question. Third instance in one sitting of CLAUDE.md §11's named failure mode, after `HERO_RET_INT`'s *"exactly what fits in Heroes' `int64_t`"* and `printer/types.rs:3`'s *"this renderer has no options and no choices to make"* | §4.19, CLAUDE.md §7, §11 | 036, 042 |
