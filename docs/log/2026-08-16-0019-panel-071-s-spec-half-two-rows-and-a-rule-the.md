2026-08-16 | **Panel 071's spec half: two rows, and a rule the document had never stated** (provisional — author ratification pending). **3405 → 3440**, +35 against a **−32 named removal**, net +3. `ptr` and `cstr` enter § Types **and `spec:222`'s definition clause leaves** — the warden showed the table is a **glossary, not a predicate** (nothing in the document reads off it; `spec:214`'s field list names both types itself), so rows close no hole and what was wrong was **placement**: `p: ptr @ nullptr` compiles at exit 0 **with no `extern` group in the file**, so ordinary types were filed under FFI. And `spec:222` gave `nullptr` to `ptr` alone while `c: cstr @ nullptr` compiles — repaired free. The second amendment states the rule that **a line computing a value must use it**, which this document had never carried: measured, six shapes all exit 1 `discarded_value`, `design.md:1718` calls it *"the highest-frequency LLM habit error under value semantics"*, and grep for *"discard"* returned only the escape hatch. Panel 068's row again — design.md carries it, the compiler **is** the decision, the spec never caught up | **Two ballot wordings were measured FALSE, and pricing them is what caught it**: both said *"`ptr` and `cstr` are § FFI's"*, at +31 and +15, and both are false because neither type needs an `extern` group. That would have been the **ninth** silent-direction sentence here. **Adopted at zero tokens with them**: no sentence in this spec may refer to *"this document's types"* — the phrasing that nearly cost panel 060 130 of 192 raylib entry points, and grep-enforceable. `push`'s shape is **folded into the existing cost sentence** rather than added (+25 unpaid as its own), and the same day proved that sentence load-bearing: `discarded_value`'s `certain` fix wrote `_ = xs.push(4)`, exit 0, element lost. The warden predicted E alone at **3338 / 3409** and it landed **exact to the token**; the full package it put at 3438 against 3440 measured, the 2 from line wrapping | spec § Types, § Bindings, § Strings · design.md §1.6 | 071
