2026-08-12 | **Metric 3's instrument stops flattering itself, and starts being attributable** (author decisions, `/decide`). Two decisions on one harness. **The corpus must compile**: `heroes mutate` checks every program before mutating anything and refuses a corpus it cannot use with exit 2, naming every offending file — the flattering direction was the live one, since `crates/heroes/src/library/` reported 77 sites and 100% killed while `heroes check` refused that same corpus, every mutant killed by a diagnostic that was already there. The check reuses `fate`, so the verdict on a base program and the verdict on its mutants are literally the same function. **And a rate is now attributable**: `--survivors` prints each accepted mutant as its site and its two lines, `--operator` narrows to one of the twelve, and the site is derived from the two texts rather than threaded out of the edits | measurement 002 recorded two per-operator rates that moved between runs and **declined to explain either**, because attributing a delta needs the mutants behind it — CLAUDE.md §12 says measurement beats opinion, and a number nobody can attribute is an opinion with a decimal point. The flag paid for itself on its first run: `swap-args` survives mostly on `fail("code", "msg")`, two `str` arguments in a position where the language's own same-type rule would demand names if `fail` were an ordinary function | Part 11, CLAUDE.md §9, §10, §12 | 011, 038 |
