2026-09-09 | **String interpolation lands: three tokens, one node, a desugar in the lowering, and four decided ceilings moved by exactly what the knots took** (M-interpolated-strings step 1; panel 121 R1 to R5 as ratified). `f"line {n}: {word}"` lexes as `str_piece ident str_piece`: the pieces are tokens whose text says which piece they are, and the hole's own tokens stand between them lexed as anywhere else — which is what makes R1's skipping scan FREE, because `state.emit` already counts brackets and a `}` closes the hole only when the depth is back to what it was when the hole opened. `{{` is one brace, and a hole whose expression begins with `{` needs a space after the opener for the same reason Python's does. The parser makes ONE node, `interp { pieces, holes }`, with the holes as children (R4); the checker types each hole in the walk and hands the type to `check/interp.hero`, which refuses what `to_str` cannot render with `to_str`'s own words — so a map index, which answers `V?`, is refused until `.must()`; the lowering hands the hole values to `ir/interp.hero`, which chains `to_str` calls and `add`s and lets a `str` hole through untouched; every printer re-prints the literal verbatim, and `fmt` is a fixpoint on it. **The blast radius was the sitting's argument for the node and it was paid**: 68 exhaustive matches in 22 files gained an arm, seven of them real and the rest a literal group joined. **Four DECIDED rows moved by measured lines** — `grammar_expr.hero` 1002 to 1041, which confirms the compiler seat's registered prediction; `check/walk.hero` 1700 to 1708; `ir/flatten.hero` 1110 to 1114; `ast.hero` 475 to 484 — each because a helper that calls back into the knot is a `use` cycle (R5), and each with the lines named. The three helpers that needed no call back are modules of their own. **The spec grew +138 real tokens** for the clause with R1's scan stated, one definition of *abort* covering eight sites, and the printed float's round-trip; the staleness gate fired on the old digest first, as designed | the coordinator's two test programs were both wrong and the compiler was right both times — `ok` is a reserved name, and `m["k"]` in a hole is an `i64?` that the new diagnostic sent to `.must()` — and the second lexer test was wrong for Python's reason, `{{1: 2}` at a hole's edge being text | §1.6, §4.3, §4.15, Part 7 item 7 | 008, 121, 123 |
