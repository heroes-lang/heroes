2026-08-16 | **The seventh shape of the container defect, and the convener's third wrong brief** (`docs/panel/068`). `ptr`/`cstr` reached **through a record field** where the record is a **map key** passes `heroes check` at exit 0 and aborts at 134 — panel 067's fix covered six shapes with the pointer as the *direct* element, and `emit/gate_types.rs::check_element` does not descend into a record's fields. Only the key position fires, because only there is `hash` called. **The project already owns the correct transitive walk** (`types/partial.rs::reaches`, written for `partial` records) and it was not reused | and the brief for this sitting asserted *"sort.c already solved NaN ordering once (totalOrder)"* — **false**: sort.c rejects totalOrder in writing and **aborts**, and `DESIGN-LOG:205` had superseded `:148` four days after it was written. Three sittings, three wrong premises from the convening seat, three catches by the judges (066's dead test, 067's line-count-against-a-token-formula, 068's dead NaN premise). The instrument is working; the seat that convenes it is the one that keeps needing correction | crates/heroes/src/emit/gate_types.rs · crates/heroes/src/types/partial.rs · runtime/parts/sort.c | 029, 067, 068 |
