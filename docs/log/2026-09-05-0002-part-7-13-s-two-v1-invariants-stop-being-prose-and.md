2026-09-05 | **Part 7.13's two v1 invariants stop being prose and become `tests/harness/suite_runtime.hero`, and one of them was broken** (M-isolated-threads step 0; no panel — the shape is `DESIGN-LOG:422`'s, where the `ORDER:` mark stopped being a grep, *"the convention's shape is design.md's own sentence, obeyed rather than amended"*). The refcount boundary held: **0** `inline` in `runtime/heroes_runtime.h`, **6** `;`-terminated refcount prototypes. The single allocation point did not: `runtime/parts/array.c:203` grew `hero_eq_queue` with **`realloc`**, a second allocation site, which §4.20 (`design.md:2210`) and Part 7.13 (`:2582`) both forbid because a per-thread heap needs one place to change. **The promised grep could not have found it** — `alloc.c` said *"checkable with one grep"* and the sentence names `malloc` and `free` — so the suite reads the whole allocation family and reads code rather than text, `parts/run.c:484` having the English words *"the first check is free ("* in a comment. Repaired with `hero_grow_kept`, a third shape in `alloc.c` and deliberately not a pair, weighed by neither counter: the leak gate would report a false leak on a buffer kept on purpose, and `hero_live_scratch` means borrowed-and-not-returned, which it is not; §4.20's normative length guard added, which the `realloc` never carried. Measured over 300 nested arrays (queue 64->512): correct at `-O0`, `-O2` and `--sanitize`, exit 0, leak gate clean. **And the open half of `docs/work/DONE.md:157` is answered no**: the two rules stay in design.md and are NOT copied into CLAUDE.md §7, whose own flag-list paragraph records what a second home costs. | §4.20, Part 7.13 | —
