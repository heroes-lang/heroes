2026-08-11 | **Monomorphisation runs BEFORE the ownership pass, and the reason is not the one the ROADMAP gave** (panel 029 R1). Not "ownership cannot decide with generic types" — it **has already decided, wrongly**: `is_refcounted` answers `false` for `Ty::Generic` (`types/counted.rs:89`), so `own.rs` emits `store total <- $t1` with no incref and no decref into a body that will be instantiated at `str`, while the `[A]` beside it is counted because `Ty::Array(_)` is unconditionally counted. Dumped by a judge and reproduced by the convener. `ir/phases.rs` cannot catch it, because `released_on_return` asks the same lying predicate | a pass that has committed a wrong answer is worse than one that declines to answer, and the ordering follows from the defect rather than from a preference | §4.12, Part 5 | 029 |
