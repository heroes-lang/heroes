2026-08-16 | **Panel 081 R4 lands: `char[N]` binds, and it is panel 064's own argument arriving one type late** (§1.11/§4.19 completeness defect, ruled by panel 081). A C `char[N]` struct member could be bound at **none** of the eight integer widths, so `uname()` was unreachable at any price — and its `partial` escape hatch is refused as `empty_record`, because `struct utsname` is nothing but `char` arrays. Scale, from a clang AST walk over eleven POSIX headers plus raylib, SDL3, sqlite3, zlib and curl: **26 of 81 fixed-array struct fields (32%) are `char[N]`** | Cause: `emit/extern_field.rs` asked **class, width and sign** of a scalar integer (panel 064, whose own doc says *"C's integer ABI is width and sign, and C's type identity is finer than its ABI"*) and **`_Generic` type identity** of an array element — and C's `char` is a **third type**, distinct from `signed char` and `unsigned char`, so `&t.name` is `char (*)[4]` and matches `int8_t (*)[4]` never. Nobody carried the argument across to the element. The repair asks panel 064's rule of `{place}[0]` and keeps the length **exact and sign-agnostic** by asking it of the field's own element type — `__builtin_types_compatible_p(__typeof__(place), __typeof__(place[0])[n])` decides the count and never re-litigates the element. **Four refusals verified to survive** before the golden was written: `u8[4]` (wrong sign — `char` is signed here), `i16[4]` (wrong width), `i8[8]` and `i8[2]` (wrong length both ways); and a flexible array member keeps panel 079's own `ffi_flexible_array_member` rather than being told to correct a length that does not exist, because the tail measurement is still asked first. Golden `run/ffi-a-char-array-member` prints **68**. **`ffi_decls.rs`'s `_` arm repaired in the same commit**: *"no C header can declare a field of this type"* is CLAUDE.md §11's claim about the world and is false — `tiffio.h` declares `float d_mat[3][3]` — so it now names what *this group form* has no spelling for | docs/panel/081 R4 · CLAUDE.md §11, §12 · design.md §1.11, §4.19 · panel 064 | 081
