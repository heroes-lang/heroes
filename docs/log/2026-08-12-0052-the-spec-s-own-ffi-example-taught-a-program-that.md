2026-08-12 | **The spec's own FFI example taught a program that silently does nothing.** `function sqlite3_open(path: cstr, out: ptr)` contradicted the rule four lines below it (*"a C out-parameter is an `@` parameter"*), on the most-copied line in the document. Copied verbatim it compiles, links, runs and **exits 0** printing `rc: 21` — `SQLITE_MISUSE`, no database opened, nothing said. Found by the llm-ergonomist, which reads only the spec, while it was being asked about something else; repaired at **+1 token** | the redundancy between an example and the rule beside it is only worth its tokens if something checks that they agree, and nothing did | spec § FFI | 038 |
