# DECIDE — the decisions the compiler is waiting on

Read by **`/decide`**. Every item here asks **what should be true**, and until it
is answered the compiler goes on behaving some way by default. That default is
the cost of leaving an item open, so each item names it.

**Only open items live here.** The moment one is answered it is ticked with the
verdict written into it and moved to `docs/work/DONE.md`. A file that keeps its
own closed items stops being a list of what is owed — this one held **138 ticked
items and zero open ones** on 2026-08-26, under eighteen headings still titled
`## Open`, which is why the rule now lives in `/decide` itself instead of in
prose about `/decide`.

**One notation: `- [ ]`.** No section headings, no prose sections, no
strikethrough. A finding written as a bare bullet is invisible to every count in
this project, and nine of them sat in exactly that shape under two panel
headings here until 2026-08-26 — **five already closed** and two of those
measurably stale, while the file reported itself empty.

Two rules bind this file, both learned the hard way:

- **Verify before asking.** An entry is a claim from the day it was written, and
  entries outlive their causes. Asking the author about a settled question is the
  one cost this list cannot pay. Of the nine findings recovered on 2026-08-26,
  five were closed and were ticked with what closed them rather than asked.
- **Rank by what it blocks**, never by age, and say the blocker in the question.

Format: `- [ ] <origin> | <what> | <where to look> | <why it matters>`

- [ ] **panel 096 — Q2's mechanism was built after the sitting and does not work, so what is left of Q2 is the message** | **RATIFIED 2026-08-28 and then partly OVERTURNED by a measurement on the code it asked for** (`docs/panel/096` § Q2's mechanism was built and does not work, appended after ratification). `HERO_VARIADIC` was implemented in full — runtime macro, ABI 16 -> 17, the one-entry promotion table, `probe_line`, `argument_columns` and all five consumers, 148 emission traces and 6 emit goldens re-blessed, 516 selfhost tests green — and then the net named `examples/raylib/main.hero:80`: `function ColorAlpha(color: Color, alpha: f32) -> Color` REFUSED against raylib's own `float alpha`. A correct binding that had been accepted since it was written. **Cause, in nine lines of C: `__builtin_choose_expr` forms and diagnoses BOTH arms**, because clang's conversion warnings are emitted at expression formation, so the `(double)` cast the variadic case needs is itself a `double`-to-`float` error against every non-variadic callee whose header says `float`. Moving the cast inside the argument gives the same error. The construction gets **two of three** shapes, which is the worst score available: it looks like it works. **Neither seat attacked `f32` against a `float` header** — both attacked the variadic slot and the `double` header, and the shape that was ALREADY CORRECT is the only one the promotion can break. **Reverted in full**, nothing committed, and the control case is now permanent at `tests/golden/fixedbugs/ffi-f32-against-a-float-header-is-correct.hero`. **WHAT SURVIVES AND WHAT IS OPEN**: removal is still refused on its own measurement (clang's variadic and fixed messages are byte-identical, so dropping the flag loses `pow(x: f32, y: f32)`), and `HERO_VARIADIC` is sound as a PREDICATE — 9 of 9, re-verified. What is left of Q2 is the **message**: the diagnostic says *"the header's `double`"* where the header says `...`, which is a false premise inside a diagnostic (§8), repairable without touching the refusal, and the only sound half. **The author decides whether the message repair is worth taking alone**, and whether a second sitting is owed for the position problem or whether the refusal simply stands with an honest message. **Also still open from the sitting**: Q1's fact-versus-provenance question, which a blanket ratification could not settle | docs/panel/096 · selfhost/emit_extern_probe.hero · selfhost/emit_ffi_narrowed.hero · examples/raylib/main.hero:80 · tests/golden/fixedbugs/ffi-f32-against-a-float-header-is-correct.hero | a ratified sitting that describes a mechanism which refuses a correct program is the worst kind of stale record: somebody implements it, and here that somebody was the coordinator, the same day

- [ ] **`to_i64` on an `i32`: the question is NOT whether a widening should be infallible — the author already ruled on that, and this item did not know** | **REWRITTEN 2026-08-28 after the grep CLAUDE.md §1 asks for, which found the ruling the brief was about to overturn.** `DESIGN-LOG:266`, 2026-08-13, author ratification closing panel 043's Q1 in the author's own words *"fai la soluzione più solida e sicura"*: **the uniform `T?` is restored — a width conversion is fallible at every pair, including the ones that cannot fail.** The bent rule this item proposes was BUILT, was measured CORRECT (panel 043's compiler-engineer generated all 64 width pairs from an independent reimplementation and found no fault), and was reverted anyway for three recorded reasons: **one rule**, so a reader knows the result's shape from the name without resolving the argument's declaration to learn its width — §1.3's locality test, scored by that judge as *"worse by exactly one write-side hop"*; **it was an invention**, the historian having searched Zig, Rust, Swift, D and Ada for one conversion name yielding a plain value for some argument types and an optional for others and found NONE, while Zig's `math.cast` returns `?T` unconditionally; and **reversibility**, taken at 8 sites when it was cheap. **The brief cited panel 051 and never mentioned 043.** And its one new argument does not survive measurement: it claimed **251 call sites**, and `grep -rhoE '\.to_(i8|i16|i32|i64|u8|u16|u32|u64)\(\)'` over `selfhost/`, `tests/harness/` and `examples/` counts **38** — 20 `.to_i64()`, 13 `.to_u64()`, 4 `.to_u32()`, 1 `.to_i8()`. The record says 8 at the time of the ruling, so the growth is 8 to 38, not 8 to 251. **SO THE QUESTION IS SMALLER AND IT IS ABOUT REOPENING, NOT DECIDING**: has anything changed since 2026-08-13 that reopens a ratified decision? The three reasons still hold as written; the count is a third of what was claimed and the same order of magnitude the ruling contemplated. **The default while it waits**: the uniform rule stands, `.must()` appears on 38 conversions that cannot fail, and §1.4 calls that redundancy spent where errors cannot occur — which the ruling already priced and accepted | DESIGN-LOG:266 · docs/panel/043 § Q1 · docs/panel/051 · spec:61-64 · selfhost/check_builtins.hero | asking the author to re-decide something they ratified, without citing the ratification, is the one cost `/decide` cannot pay — and this item was three lines from doing it

- [ ] **The spec never says what `print` guarantees a float, and the question is real even though this item's witness is dead** | **REWRITTEN 2026-08-28: the premise was stale by three weeks and the Java example proves the opposite of what it was cited for.** What the item claimed: the runtime renders `5e-324` as `4.94065645841247e-324`, so a reader who infers *shortest* from the spec is misled. **Measured against the live runtime** (`hero_f64_to_str`, compiled directly): `5e-324` renders **`5e-324`**, `1e-323` renders **`1e-323`** — both matching Python. `DESIGN-LOG:100`, 2026-08-05, says exactly this and says why: gnulib's `ftoastr` subnormal branch landed, *"and the word shortest is struck"*, because the spec-warden's counterexample had dissolved while the wording correction stood. The item was reading `runtime/parts/f64.c:26`'s comment, which still carries the pre-branch example. **AND THE JAVA CASE CUTS THE OTHER WAY**: `1.9999999999999998e23`, the value Java shipped for eighteen years, Heroes prints **`2e+23`** — the shortest form that round-trips, i.e. the right answer. The item cited it as evidence of a gap. **A THIRD CORRECTION, found while verifying**: the witness is **not writable in Heroes at all** — `5.0e-324` is `error[exponent_literal]: there are no exponents`, so it is a fact about the C runtime and not a program a judge can compile. Any brief must say so or a seat will try. **WHAT IS ACTUALLY OPEN, and it is smaller and cleaner**: `runtime/parts/f64.c:26` guarantees ROUND-TRIP-EXACTNESS through a `%.*g` ladder, and `grep -i 'round.trip|shortest'` over the spec returns **nothing**. The spec says only *"A float prints a point or exponent (`1.0`, `1e-06`)"*. A ladder is not a shortest algorithm even when it usually agrees with one, so the guarantee a program may rely on is unstated. Does the spec owe one sentence naming round-trip? Headroom is **504** of a hard 4096. **The default while it waits**: the runtime guarantees round-trip, the document implies nothing, and a reader who assumes *shortest* is assuming something no version of this runtime ever promised | spec:180-181 · runtime/parts/f64.c:26 · DESIGN-LOG:92, :100 · docs/panel/021, 027 | a spec that is silent where the runtime is specific is §12's own case, and the item that noticed it had three wrong facts in it

- [ ] **Two live holes at the C boundary, found by panel 096 on its way to something else, and filed separately on that seat's own condition 8** | **(1) A `size_t` out-parameter declared `i32` is ACCEPTED at exit 0.** Measured 2026-08-28 by the ffi-pragmatist on a live compiler: `extern function getline(@line: ptr, @n: i32, stream: ptr) -> i64` builds and runs, printing `warning: incompatible pointer types passing 'int32_t *' to parameter of type 'size_t *'` and **exiting 0**, because `-Wincompatible-pointer-types` is not among `cli_flags.hero`'s thirteen as `-Werror`. By C's rules the callee writes 8 bytes into a 4-byte slot. **The seat could not demonstrate corruption and said so** — the write landed in stack padding and inside malloc granularity, exit 0 under ASan and UBSan both ways — so the claim is only what was measured: accepted at exit 0, out of bounds by the standard, corruption at the allocator's discretion. **This also corrects a claim panel 091 is quoted for**: the probe is NOT *"the only thing that sees"* this — it sees it and the build does not stop. **(2) An `extern` arity mismatch is exit 2, the compiler blaming itself.** `extern function strtok(target: cstr, sep: cstr, pad: str)` gives `too many arguments to function call, expected 2, have 3` from the return `_Static_assert`, at exit 2 — panel 096's Q3 family, a different member, and it will ride whatever sitting settles Q3's attribution question | selfhost/cli_flags.hero:32 · selfhost/emit_extern_probe.hero · selfhost/emit_ffi.hero:48-122 · docs/panel/096 § Two live holes · docs/panel/091 | §1.12 makes memory safety a goal that outranks everything else in the contract, and (1) is a write out of bounds that the compiler currently waves through

- [ ] **Two harness runs at once CORRUPT the blessed emission traces, and one of them was writing them** | **Found by doing it, 2026-08-28, and it is the most serious thing found that day.** `UPDATE_EMISSION=1 heroes run tests/harness/main.hero -- <compiler> emission` was started while an ordinary net run was still finishing in the background. Both use `build/harness` as their scratch (`tests/harness/main.hero`'s `SCRATCH`), so their captured streams collided — and the blessing wrote the collision to disk. Measured damage: **three traces rewritten with a program's stdout spliced into the C**, including a mangled first line — `tests/emission/run-ffi-cstr.c` began `s 0.0.1 from tests/golden/run/ffi-cstr.hero. Do not edit. */` with `20 30`, `2 5` and `cd abcdef!` inserted above it. Reverted with `git checkout`; the same command run alone afterwards was clean, **299 passed 0 failed, one trace added and no other touched**, so this is a RACE and not a systematic defect. **Why it matters more than an ordinary race**: `suite_emission.hero`'s own doc says a blessed file *"is not one compiler's opinion: it is what two independent implementations agreed on, byte for byte"* — the bootstrap is archived, so those bytes **cannot be regenerated from their original source of truth**. A corrupted trace that got committed would replace a two-compiler agreement with garbage, and the suite would then be green about it. **Three ways out**: (a) the scratch directory carries the process id, so two runs cannot share it — smallest and it removes the class; (b) a lock file under `build/`, and the second run refuses with a diagnostic naming the first; (c) `UPDATE_EMISSION=1` refuses to write when the scratch already holds another run's files. **The default while it waits**: the corruption is silent, `git status` is the only thing that showed it, and nothing in the harness would have caught a committed one | tests/harness/main.hero (`SCRATCH`) · tests/harness/suite_emission.hero · tests/harness/shell.hero (`run`) | the emission traces are the one instrument in this repository whose source of truth no longer exists, and a race can overwrite them while reporting success

- [ ] **A Heroes program that recurses deeply enough dies at exit 139 with no message, and §1.12 makes that a goal's failure rather than a limitation** | **Found 2026-08-28 while checking the site's own claims before publication, and the site was claiming the opposite.** `zen.html`'s line 17 says *"Nothing crashes and nothing leaks"*, `index.html` headed a section *"A small surface, no collector, and no crash"*, and `why.html` said *"one hole remains, and it is declared"* naming only the C boundary. **Measured on the live compiler**, an ordinary recursive function (`function down(n: i64) -> i64` returning `1 + down(n - 1)`): 500,000 frames is **exit 0**, 1,000,000 frames is **exit 139**, SIGSEGV, with no output and nothing on stderr. Nothing in the language, the runtime or the emitted C sees it. **THE DIAGNOSIS ALREADY EXISTS AND IS GOOD** — under `--sanitize` the same program is **exit 134** with `AddressSanitizer: stack-overflow` and, decisively, `#0 h_d_down d.hero:1` plus `SUMMARY: ... stack-overflow d.hero:1 in h_d_down`: the sanitizer names the **`.hero` line**, so the information a diagnostic would need is already reachable. What is missing is the default build. **Why this is a decision and not just work**: §1.12 makes not crashing a GOAL of the language and CLAUDE.md §12 gives it the tie-break over elegance, tokens, ergonomics and speed, so the question is which of three shapes the goal buys, and they cost differently. (a) A **guard page plus a handler** that turns the fault into `panic: stack exhausted` at the abort path every other runtime failure uses, which is the only option that keeps the message on the author's own program without touching what is emitted. (b) A **depth counter** in the prologue of every emitted function, which is honest, portable and a per-call cost on every program including those that never recurse, so it is the one option §13's performance line has an opinion about. (c) **Declare it**, as the FFI hole is declared, and let the guarantee read *"two holes remain"* — which is what the SITE now does, because a published promise that a five-line program falsifies is worse than a published limit. (c) is already taken for the copy and does not settle the language. **The default while it waits**: the site declares the hole, the compiler stays silent about it, and the only way a program's author learns which function ran out is to re-run under `--sanitize`. **Not measured and worth measuring before the sitting**: whether the same program is 139 on Linux, where the stack and the guard page are the kernel's rather than Darwin's | spec:1 (`§1.12`, the goal) · CLAUDE.md §12, §13 · runtime/runtime.c (`hero_panic`, the abort path a message would take) · site/src/html/why.html, zen.html (where the claim was, and what it says now) | a language whose stated goal is that programs do not crash, crashing without a word on a five-line program, is the one defect its own showcase site cannot survive being asked about
