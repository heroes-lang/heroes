# DECIDE — the decisions the compiler is waiting on

Read by **`/decide`**. Every item here asks **what should be true**, and until it
is answered the compiler goes on behaving some way by default. That default is
the cost of leaving an item open, so each item names it.

**Only open items live here.** The moment one is answered it is ticked with the
verdict written into it and moved to `docs/work/DONE.md`. A file that keeps its
own closed items stops being a list of what is owed — this one held **138 ticked
items and zero open ones** on 2026-08-26, under eighteen headings still titled
`## Open`, which is why the rule now lives in `/decide` itself instead of in
prose about `/decide`.

**One notation: `- [ ]`.** No section headings, no prose sections, no
strikethrough. A finding written as a bare bullet is invisible to every count in
this project, and nine of them sat in exactly that shape under two panel
headings here until 2026-08-26 — **five already closed** and two of those
measurably stale, while the file reported itself empty.

Two rules bind this file, both learned the hard way:

- **Verify before asking.** An entry is a claim from the day it was written, and
  entries outlive their causes. Asking the author about a settled question is the
  one cost this list cannot pay. Of the nine findings recovered on 2026-08-26,
  five were closed and were ticked with what closed them rather than asked.
- **Rank by what it blocks**, never by age, and say the blocker in the question.

Format: `- [ ] <origin> | <what> | <where to look> | <why it matters>`

- [ ] panel 052, and the ffi-pragmatist's blocking condition | **One C name cannot carry two arities, so a variadic function can be bound exactly once.** Measured 2026-08-26: `function printf_i(format: cstr, value: i32) -> i32 = "printf"` — any spelling that keeps the C symbol on the declaration line — is `error[expected_extern_signature]: expected a `function`, a `constant` or a `record`, found `=`` at the `=`. Without a rename the second shape of `curl_easy_setopt` is `declared_twice`, so a program binds one arity of a variadic and no more. This is the ergonomist's blocking condition on the alternative to `...`, and the ABI is already correct — what is missing is a way to spell the C name apart from the Heroes name. **The default while it waits**: one arity per C symbol, chosen by whoever writes the binding first | selfhost/parse_decl.hero (`expected_extern_signature`) · design.md §4.19 · docs/panel/052 | the FFI is complete or it is not (§1.11), and a library whose calls differ only in arity is unbindable past its first shape

- [ ] panel 051 and panel 052 | **A C variadic bound at a fixed arity compiles, runs, and its width is checked by nothing.** Measured 2026-08-26 by running it: `extern "stdio.h" function printf(format: cstr, value: i32) -> i32` against a header declaring `int printf(const char *, ...)` builds clean and prints `n=42`. Nothing sees the variadic slot — not a flag, not `_Generic`, not the function-pointer probe — because the probe compares against the declared prototype and C's own promotion rules apply past the last named parameter. The consequence the sitting measured: an `int -1` in a variadic slot reads `-1` on arm64 and `4294967295` on x86-64, the same program, two answers, no diagnostic. `...` itself does not parse (`error[expected_parameter]` at the `.`), so this is the only reachable shape, and §4.19 rung 4 already depends on it. **The default while it waits**: silent, and platform-dependent | selfhost/emit_extern_probe.hero · design.md §4.19 · docs/panel/051, docs/panel/052 | §1.12 says a Heroes program must not corrupt memory, and this is the one FFI hole where the compiler cannot see the value at all

- [ ] panel 051, found while landing it | **A widening that cannot fail is fallible, so every narrow binding composes through a `.must()` that asserts nothing.** Measured 2026-08-26: `n: i32 @ 7` then `n.to_i64()` is typed `i64?` — `error[bad_operand]: `print` takes any integer, a float, `bool` or `str`, found `i64?``. An `i32` always fits an `i64`; the result cannot be an error, and the type says it can. The ffi-pragmatist booked this as half the ergonomic cost of the widths at **251 call sites**. **The default while it waits**: every widening call site carries a `.must()`, which trains the reader to write `.must()` where it is *not* free — the habit §1.3 exists to prevent | selfhost/check_builtins.hero (`to_i64`) · docs/panel/051 | a fallible type on an infallible operation teaches the author to ignore fallibility, which is the one thing the language asks them to read

- [ ] M-selfhost-port, `emit_c_spellings` | **The compiler hardcodes the word width to 64, which is the one option the design forbids.** **Why there is no cheap way out**: the bootstrap derived C `long`'s width from `size_of::<c_ulong>()` and named the host from `env::consts::OS`, and Heroes has neither — and an `extern constant` cannot carry a platform-varying width by construction, because declaring `ULONG_MAX` at either width fails the width assertion on the other platform, which is this very table's lesson applied to itself. The port therefore takes the width as a parameter (`spelling(c_type, word_bits)`) — derived, never tabled — and then measured 2026-08-26 **every live call site passes the literal `64`**: `selfhost/cli_verbs.hero:94` and `:149`, plus the tests at `emit_ffi.hero:119,126`. There is no `hero_word_bits` anywhere in `runtime/`. So the answer taken by default is (c) below, the one the bootstrap's own doc calls out as forbidden (*"derived, never tabled"*), and it ships without the premise-death test that was (c)'s whole condition. The three ways out: **(a)** a runtime entry point (`hero_word_bits()` in `hero_os.h`) — one C line, an ABI addition, cheapest, and it keeps the derivation; **(b)** a builtin the language grows — heavier, §10's stopping rule applies; **(c)** hardcode 64 **with** a premise-death test, which is what is shipping minus the test. The bootstrap's test contract — `the_word_width_this_compiler_claims_is_the_one_clang_uses` — should hold whichever way is taken, and it is what a premise-death test would restore. A second question rides along: the port's caveat drops the `(macOS)` host name the bootstrap printed — accept the divergence, or add the host to the same entry point? | selfhost/emit_c_spellings.hero:44 · selfhost/cli_verbs.hero:94,149 · runtime/hero_os.h | the compiler tells an author `u64` is right *for this target* while believing a number nobody derived, and Windows is the target where it is wrong

- [ ] panel 019, moved here from `SCHEDULED.md` 2026-08-26 because it names no milestone | **`heroes measure` vendors two of panel 011's three tokenisers, and the missing one was the binding one.** o200k was the tightest by 2 tokens on v0, so the instrument's error bar is wider than some verdicts it decides. The item's own trigger is a condition rather than a date — *vendor it before any verdict lands within 10 tokens of a ceiling* — which is precisely why it has no home and belongs here: work scheduled at nothing is a decision nobody has made (`SCHEDULED.md`'s own rule). **The citation had rotted**: it named `crates/heroes/src/measure/mod.rs`, archived since 2026-08-19; the live port is `selfhost/measure*.hero`. **The default while it waits**: the spec budget is enforced against the looser of two tokenisers | docs/panel/011 · selfhost/measure_bpe.hero | the budget is the project's only forcing function and it is measured with an instrument known to be short one arm

- [ ] M-strings-ownership, moved here from `SCHEDULED.md` 2026-08-26 because its milestone closed 2026-08-05 | **The `f64` ladder's guarantee is round-trip-exact, not shortest, and the spec has never said which.** The distinction is worth one paragraph and the two are not the same promise: `5e-324` renders shortest only because of gnulib's subnormal branch, and Java shipped `1.9999999999999998E23` for eighteen years while satisfying its own *"round-trips"* spec. A spec sentence that said *shortest* would be false, and CLAUDE.md §12 makes a false spec the compiler's bug. This is a spec amendment, so it is a panel path (CLAUDE.md §4) and the decision here is whether to convene one or leave the guarantee unstated. **The default while it waits**: unstated, and a reader infers *shortest* from the examples | runtime/runtime.c (`hero_f64_render`) · design.md §4.9 · spec:155 | the milestone that was going to write it closed twenty-one days ago and nothing opened the item

- [ ] M-separate-compilation step 10, found while annotating acceptance row 4's fixture | **19 `#~` annotations are decorations: nothing checks them.** `suite_annotations` is §9's one unforgeable check — a regenerator can rewrite an `.expected` but cannot invent an annotation — and it sweeps exactly two directories, because its mechanism compares a case's `.hero` against its `.expected`. Measured 2026-08-26: **15 annotations across 11 files in `tests/golden/fixedbugs/` and 4 across 3 in `tests/golden/surface-fixtures/` are outside that sweep**, against 181 inside it. `fixedbugs/` has **zero** `.expected` files for 16 `.hero`, and `surface-fixtures/` has none by design: both are driven by `suite_surface` rows that run the compiler and match stderr, so there is no expectation file to compare against. The suite's own words for this state are in its failure message — *"annotations that stop being checked are the shape §9 calls a decoration"* — and it says them about a floor it can only measure inside its own two directories. **Three ways out and the choice is the author's**: (a) the sweep learns to RUN the compiler for cases with no `.expected`, which is new machinery and needs the argv each case wants (some are `build`, some `check`, some need `--include`); (b) `fixedbugs/` and `surface-fixtures/` grow `.expected` files, which makes them ordinary collected cases and costs the reason they were kept out of `cases.collect` (a neighbour module breaks it, and `suite_emission` requires every collected program to emit); (c) the annotations come OUT of those files, which is honest bookkeeping and gives up §9's invariant exactly where the FFI's own regression cases live. **The default while it waits**: 19 annotations that read as pinned and are not, in the directory §9 created for fixed defects | tests/harness/suite_annotations.hero:42-46 (`DIRECTORIES`, `LEAST: 181`) · tests/golden/fixedbugs/ · tests/golden/surface-fixtures/externsignature/ | an invariant that covers some of its own subject is a convention wearing an invariant's name, and the files it misses are the ones that exist because a defect already shipped

- [ ] M-separate-compilation close, scoring the ledger's registered predictions | **`args_checked` bought 11 spec tokens with a prediction whose instrument was never built, and the prediction is now FALSE.** The ledger row at 3541 spent **+11** on the `args_checked() -> [str?]` clause under panel 012 branch 2 — an addition paid by a registered falsifiable prediction rather than by a removal — and the prediction reads: *"at M-separate-compilation close, a `run/` golden handed one ill-formed argument among three prints the other two and exits 0"*. Measured at the close, 2026-08-26: **no file in `tests/golden/` or `examples/` uses `args_checked` at all** (grep, zero hits), and `suite_run` has **no mechanism to pass argv** to a golden, so the promised instrument could not have existed in the shape the row names. What does exist is the opposite witness: `tests/golden/run/edges-file-args-exit.hero` uses `args()` and, handed `good.txt $'\xff\xfe' more.txt`, dies at **exit 134** with `panic: hero_str_from_bytes: not well-formed UTF-8` — which is correct, because `args()` is specified to abort. Panel 046 R2 says a falsified prediction sends its clause back here to be re-argued under the removal branch, never renewed under a new milestone name. **Two ways out, and the cheap one is cheap**: (a) build the instrument now — the right home is `suite_special` or a `suite_surface` row, not a `run/` golden, because the shell can spell an ill-formed byte with `$(printf '\377\376')` and the harness already runs arbitrary command lines; or (b) argue the clause on its merits without a prediction behind it, which is the removal branch. **The default while it waits**: `spec:186` carries a sentence whose registered evidence does not exist | docs/measurements/010-spec-budget-ledger.md (row 3541) · tests/harness/suite_run.hero · tests/harness/suite_special.hero · spec:186 | a budget whose additions are paid by promises is only as honest as the promises that get collected, and this is the first one measured uncollected

- [ ] M-separate-compilation close, scoring panel 092 | **Three of panel 092's seven predictions lapse, and one of them was a payment.** Scored at the close: the landed ones are on the step-3 record; **two name options the sitting did not adopt** (`-Werror=incompatible-pointer-types` against sqlite, and P2's claim that §4.19 rung 3 needs a shim) and cannot be scored without building what was refused; **one is the llm-ergonomist's** — *"with the mapping named, generated callers convert at a size argument in a majority of samples, against near zero today"* — which needs a sampling experiment nobody has run, and it is the falsifier attached to the **+12 tokens** the `size_t` mapping row cost. Panel 046 R2: a lapsed prediction's clause returns here rather than being renewed. The question is whether the mapping row stands on the removal branch without its experiment, or whether the experiment gets run — it is one prompt set against `spec/heroes-spec.md` alone, which is the llm-ergonomist seat's whole protocol. **The default while it waits**: `spec:204-205` carries a row whose registered evidence is unrun | docs/panel/092 § Predictions to score · docs/measurements/010-spec-budget-ledger.md (row 3560) | the same shape as the item above, one sitting over: the budget's forcing function is only real if somebody comes back for the receipts
