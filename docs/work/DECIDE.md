# DECIDE — the decisions the compiler is waiting on

Read by **`/decide`**. Every item here asks **what should be true**, and until it
is answered the compiler goes on behaving some way by default. That default is
the cost of leaving an item open, so each item names it.

**Only open items live here.** The moment one is answered it is ticked with the
verdict written into it and moved to `docs/work/DONE.md`. A file that keeps its
own closed items stops being a list of what is owed — this one held **138 ticked
items and zero open ones** on 2026-08-26, under eighteen headings still titled
`## Open`, which is why the rule now lives in `/decide` itself instead of in
prose about `/decide`.

**One notation: `- [ ]`.** No section headings, no prose sections, no
strikethrough. A finding written as a bare bullet is invisible to every count in
this project, and nine of them sat in exactly that shape under two panel
headings here until 2026-08-26 — **five already closed** and two of those
measurably stale, while the file reported itself empty.

Two rules bind this file, both learned the hard way:

- **Verify before asking.** An entry is a claim from the day it was written, and
  entries outlive their causes. Asking the author about a settled question is the
  one cost this list cannot pay. Of the nine findings recovered on 2026-08-26,
  five were closed and were ticked with what closed them rather than asked.
- **Rank by what it blocks**, never by age, and say the blocker in the question.

Format: `- [ ] <origin> | <what> | <where to look> | <why it matters>`

- [ ] **Does the spec owe a sentence saying `T??` is refused, and does the refusal owe a golden?** | taken as a conservative parser reading 2026-08-04 and **queued rather than decided** (`DESIGN-LOG.md:48`); carried on the panel watch list until it was retired 2026-09-04 | **The default the compiler runs on**: `T??` written in a program is `error[nested_fallible]`, and that diagnostic exists in exactly one place — `selfhost/parse/type.hero:55`, with one in-module test at `:390`. Measured 2026-09-04: **no sentence in `spec/heroes-spec.md`** (the only `??` there is `???`, the typed hole), **no panel**, **no golden anywhere under `tests/golden/`**. §4.6 refuses the *level ambiguity* under monomorphisation with `A := Expr?` (`design.md:1152`) but never says whether the **written** form is rejected, so a reader with the spec alone cannot know. The reading was deliberately conservative and the reason is on the record: *"a rejection can be relaxed later, an acceptance cannot be withdrawn."* **Two things are owed and only one of them is a decision.** The golden is owed whatever the answer is — CLAUDE.md §9 says every verifier check has a test that makes it fire, and this one has none, so a refactor could delete the refusal in silence. The sentence is the decision, and it is a spec-budget question: headroom is **346** tokens today. **Recommended: keep the refusal, write the golden now, and put the sentence into the one sitting proposed below** rather than spending a panel on it alone — it is one of four spec silences that share a shape and three of the four were queued the same day | selfhost/parse/type.hero:55, :390 · spec/heroes-spec.md · design.md §4.6, `:1152` · DESIGN-LOG.md:48 · CLAUDE.md §9 | a diagnostic class with no spec text and no golden is a rule that exists only in the parser, and §12 says the spec is what beats the compiler

- [ ] **Does the type table owe a `()` row — now, on a §1 argument, or never?** | panel 003's own watch item, 2026-08-03: *"if the baseline shows confusion about `()`, the spec type table owes a `()` row"*; carried on the panel watch list until 2026-09-04 | **The default**: the spec's Types table has ten rows and none of them is `()`, while the reader meets the form three times — `function main()` (`spec:6`), `(function() -> C)` (`spec:114`) and `write_file(path: str, text: str) -> ()?` (`spec:189`), which is a signature nobody can write without knowing what `()` is. **The condition has fired and cannot be read.** Panel 003 keyed it to the baseline; the baseline run happened (`DESIGN-LOG.md:63`, `docs/measurements/001`) and is **metric 3**, a mutation run, while the condition asks about **completions**, which is metric 2 and has never run. Panel 071's spec-warden said so out loud and asked this to be written down: the item is *"dead as payment and dormant as observation"*, because a condition on metric 2 *"cannot authorise a row for the whole life of v0"* — and the watch list never recorded it, which is one of the reasons that file is gone. **The spend was budgeted and never made**: `DESIGN-LOG.md:37` names *"the deferred escape/layout/`()` sentences"* as a pre-allocated purse; the escapes landed at panel 008 and the layout sentence at panel 007. **Recommended: write the row now, on §1.4 rather than on metric 2** — a type used in the spec's own signatures and absent from the spec's own table is redundancy the document is missing rather than redundancy it is paying for, and one row is a handful of tokens against **346** of headroom. The alternative, honest and cheap, is to mark it `lapsed` under panel 046 R1 and let M-thesis-harness decide | docs/panel/003-statement-position.md:37-39 · docs/panel/071-six-holes-and-the-one-nobody-listed.md:154-157 · spec:6, :49-60, :114, :189 · DESIGN-LOG.md:37, :63 | the one thing a spec of 3750 tokens must not do is use a type it never defines

- [ ] **Does the spec owe a sentence on what a `constant`'s body may contain?** | panel 039 R4, 2026-08-12: the defect was queued and *"the spec sentence stays unbought pending the warden's Part 11 condition"*; carried on the panel watch list until 2026-09-04 | **The default**: the compiler is right and the spec is silent. `constant_body` (`selfhost/resolve/cycles.hero:193`) refuses a call, a UFCS call, a `?` and a `???` in a `constant`'s body, syntactically on purpose — the rule landed the day a `constant` was found being **re-evaluated on every read**, with `constant ARGC: int` / `len(args())` printing `3` and `0` from one binary. The spec shows one literal (`spec:22-28`, `constant MAX_DEPTH: i64` / `64`) and says nothing else, so a reader cannot tell that arithmetic over other constants is legal and a call is not — and panel 041 later leaned on exactly that generality, since `~0` *"already is the all-bits mask, because a `constant`'s body may compute"*. **What has changed since the item was written**: everything factual in it. It said the compiler accepts an arbitrary block and that no golden pins the difference; both were true on 2026-08-12 and both are false now (`tests/golden/check/fixedbugs-constant-cycle.hero` carries the code). **Recommended: buy the sentence, in the same sitting as the two above** — it is the cheapest of the four, because what it must say is what the compiler already enforces, and the spec-only reader is the one person the spec is for | DESIGN-LOG.md:255 · docs/work/DONE.md:275 · docs/panel/039-comptime-and-part-6.md R4 · selfhost/resolve/cycles.hero:193 · spec:22-28 | Principle 0 aside, a `constant` whose legal contents nobody wrote down is a form a model has to guess at, which is the one thing this language is built to prevent

- [ ] **May a parameter list break across lines the way a multi-line literal does, and what shape does `heroes fmt` print for a long signature?** | taken as a conservative parser reading 2026-08-04 and **queued rather than decided** (`DESIGN-LOG.md:48`); carried on the panel watch list until 2026-09-04 | **The default, in three parts, all measured 2026-09-04.** The spec gives newline separation to multi-line **literals** only (`spec:174`) and says nothing about a signature. The parser requires `,` — `selfhost/parse/members.hero:92-99`, and it reports the newline form through `expected_params_close`, *"`)`, or `,` and another parameter"*. And `heroes fmt` has **no long-signature shape at all**: `selfhost/print/fmt.hero` joins parameters with `", "` unconditionally and emits the line with no width test, deliberately — its own comment says panel 007 *"leaves nowhere else to break it, and a formatter that invents a continuation invents one the language does not have"*. So a signature past the formatter's width simply stays long. **Why it is a decision and not work**: it is surface syntax, so CLAUDE.md §4 makes it a panel path, and it is the one of these four that would **add** a form rather than write down a refusal. Panel 095 sat on the formatter's vertical air and bracket-depth continuation and never touched signature width (grepped: zero hits for `signature` in that file). **Recommended: leave the default and take the question to the same sitting**, with one number measured at its opening — how many signatures in `selfhost/` exceed the formatter's width today. If that number is small the honest answer is that the language does not owe a form; if it is large, the sitting is arguing from a count instead of a feeling | DESIGN-LOG.md:48 · spec:174 · selfhost/parse/members.hero:92-99 · selfhost/print/fmt.hero · docs/panel/095 · CLAUDE.md §4 | the formatter is the one tool whose silence makes every diff untrustworthy (§9), and a shape it cannot print is a shape it will not warn you about
