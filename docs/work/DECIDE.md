# DECIDE — the decisions the compiler is waiting on

Read by **`/decide`**. Every item asks **what should be true**, and until it is
answered the compiler goes on behaving some way by default — so the item names
that default, because it is the cost of leaving the item open.

**Only open items live here.** The moment one is answered it is ticked with the
verdict written into it and moved to `docs/work/DONE.md`, the record. Rank by
what an item blocks, never by age, and verify it against the repository before
putting it to the author: asking a settled question is the one cost this list
cannot pay.

**The shape.** One line per item, and an optional body indented four spaces
under it. The first field is the item's **origin**, and where that origin is a
sitting it is spelled `panel NNN`, padded — because
`tests/harness/suite_records.hero`'s `queued` check reads the `- [ ] ` lines
alone and scans them for exactly that, so a citation that slides into the body
makes every pending sitting report as unqueued, and it fails silently. Nothing
lives outside the two banners: `records/lists` is the executor of that.

Format: `- [ ] **<origin>** | <the question, in one line> | <where to look>`

*******************************************************************************
**OPEN: 1**

- [ ] **panel 135** | Part 7 item 5, `alias`: ratify DEFERRED AGAIN with a dated return condition (a 12-token spelling repeated 10 times in one file, a commit changing one spelling at 8 sites, or item 10's sitting naming it), the three struck claims and the settled shape of the form — or rule ENTERS or REFUSED with what each compels | `docs/panel/135-the-form-was-cheap-and-the-reasons-under-it-were-borrowed.md` · design.md Part 7 item 5

    **Origin:** 2026-09-13, M-deferral-ledger step 1. **The default the compiler
    runs on while this is open**: no `alias`, `alias` unreserved, and design.md
    Part 7 item 5 carrying the dated deferral and the corrections beneath its
    original text. Five seats, no veto: the two that judged the FORM (ergonomist,
    FFI) found nothing to refuse, the three that judged the WARRANT (warden,
    engineer, historian) found Principle 0 unmet — the compiler compiles itself
    without it, and the strongest measured saving is 48 tokens, 1.4% of one file.
    Conservative was recorded rather than adopted: the engineer's later return
    threshold (a 30-character spelling at 20 sites). What a yes to ENTERS compels:
    its own milestone, route R2a, `alias` reserved in the same commit as the spec
    text, the cycle walk before any `type_uses` write, `T??` re-refused in the
    checker, `fmt.hero` split first, W1 at +26 net paid by R1 and R2. What a yes
    to REFUSED compels: a Part 6 row on §1.3 whose falsifier is the return
    condition, since no seat found a shipped language that removed its synonym.
    **Recommendation: ratify as adopted.** It is the one verdict every seat's
    measurement supports, and it ends the promise without a date that the
    ledger exists to end.

*******************************************************************************
