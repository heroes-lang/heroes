# Debrief queue — comprehension owed, never blocking

Development appends; `/debrief` sessions tick off. Checked items stay (the
queue is also the record). Format:

`- [ ] <origin> | <question / task> | <where to look> | <why it matters>`

## Open

- [ ] M4.1 | First IR. `x = 2 + 3 * 4` becomes how many instructions — 3, 4, or 5? And which of them proves the precedence table, given the tree is gone by then? | crates/heroes/src/ir/tests/scalars.rs (an_expression_becomes_a_line_per_operation) | flattening is the whole job of the pass, and it is what makes the C emitter a printer
- [ ] M4.1 | A `for` loop is four blocks and a `while` is three. Which block does `continue` jump to in each, and what exactly happens to the program if a `for`'s `continue` lands on the test? | crates/heroes/src/ir/control.rs (for_loop) · tests/golden/ir/adversarial-continue-steps.expected | the wrong answer compiles, type-checks, passes every other test, and hangs
- [ ] M4.1 | `a && b` produces two blocks and no `and` instruction. Say why in one sentence — and then say which spec line forces it | spec/heroes-spec.md § Operators · ir/control.rs (short_circuit) | a construct that evaluates one side conditionally IS control flow, and calling it an operator hides that
- [ ] M4.1 | `function step(@r: Reader, ...) -> int?` with a `?`, an early `return` and a tail: how many `copyout` instructions does the dump show, and in which blocks? | tests/golden/ir/adversarial-try-copies-out.expected · ir/verify.rs (check_copy_out) | §4.8's "copy-out happens always" is the sentence panel 000 called the place this project would stall
- [ ] M4.1 | Four instructions: `add`, `add!`, `lt`, `index!`. Which can stop the program, and what makes `add` and `add!` different — the operation or the operands? | ir/print_inst.rs (aborts) · spec lines 126–128 | the reader who misses this writes a pass that assumes one exit per block
- [ ] M5a | Name the pass in the verifier's failure, and index the invariants by phase — the practice the record converges on that this project has not taken. LLVM's `-verify-each` exists "for cases where it is suspected that a pass is creating an invalid module but it is not clear which pass is doing it"; rustc bakes the attribution in (`validate_body(tcx, body, format!("after pass {pass_name}"))`), gives MIR a `phase`, panics on an illegal transition, and validates unconditionally at the last phase; Go runs `checkFunc` "between each phase" under `-d=ssa/check/on` and it caught a real ARM bug in `runtime/malloc.go` (golang/go#22499). **Cost: zero payoff today with one pass, total at pass two** — so the moment to take it is when the ownership pass lands, not before | crates/heroes/src/ir/verify.rs · docs/measurements/002-metric-3.md | retrofitting phase-indexed invariants later means auditing every existing check
- [ ] M5a | Run one corpus in more than one configuration — GHC re-runs its whole suite across "ways" (and enables `-dcore-lint -dstg-lint -dcmm-lint` for **every** test, not in a dedicated directory); Zig runs `test/behavior` in up to four optimisation modes. For a C backend the honest analogue is clang `-O0`/`-O2` plus a sanitiser build of the emitted C, which the ROADMAP already schedules for `run/` goldens at M5b | docs/ROADMAP.md M5b · CLAUDE.md §7 | the same corpus in N configurations is the cheapest multiplier in the record, and it needs the backend to exist first
- [ ] harness | `heroes mutate` prints rates, not mutants — so a per-operator rate that MOVES between runs cannot be read, only guessed at (measurement 002 records two that moved and declines to explain them). It wants a way to print the surviving mutants of one operator | docs/measurements/002-metric-3.md § What cannot · crates/heroes/src/mutate/ | CLAUDE.md §12 says measurement beats opinion; a number nobody can attribute is an opinion with a decimal point
- [ ] M4 robustness | DEFECT, open: a `match` written as an **inline** arm body of another `match`, with its own arms indented below, is rejected — and the message points at the *next* outer arm ("expected the end of the line"). The same program in a block arm body is accepted. Reproducer in the queue entry below; §4.7 appears to allow it | tests/golden/ir/nested-match.hero (the comment) · syntax/control.rs arm() | a program that looks legal and is refused with a message about the wrong line is the failure mode this project spends tokens to avoid — and the parser is where it lives
- [ ] M4 robustness | A `{K: V}` cannot be added to. The inventory (§4.20) has `has`, `len` and `m[k]`, and `push` is array-only — so a map is written as a literal and never grows. Is that intended for v1, or is the closure list short a `set`/`insert`? | examples/gallery/10-maps.hero (the comment) · types/builtins.rs · design.md §4.20 | the self-hosted compiler will want a symbol table, and a map that cannot grow is not one
- [ ] M4 robustness | Undecided: the error `code` a map miss produces. §4.9 gives `m[k]` a `V?` and §4.6 says codes are "stable snake_case strings", but nothing names this one — so `examples/gallery/10-maps.hero` deliberately asserts nothing about it. Every `T?` the *runtime* produces needs the same answer (an out-of-bounds read aborts rather than failing, so the map is the first case) | examples/gallery/10-maps.hero (the comment) · design.md §4.6, §4.9 | a code that gets invented by whoever writes the runtime first is a code that changes under somebody's test
- [ ] M4.2 | `g.rows[r].cells[c] @ v` printed as `store g.rows[$t1].0[$t2]` until the fix. Say what the printer had lost, and why only the *second* field name was affected — not the first | ir/print_names.rs (field_type) · tests/golden/ir/place-paths.expected | a path is a fold, and dropping the accumulator only shows up from the second step on
- [ ] M4.2 | The verifier had 257 lines of checks and no test that any of them fired. Fifteen tests now break one invariant each. Pick two and say what the *wrong* IR would have done at M5a, in C | ir/tests/verify.rs · docs/journal/005-lowering.md §3 | LLVM keeps a directory for exactly this; a safety net nobody has fallen into is indistinguishable from no net
- [ ] M4 close | Milestone debrief offers, all optional: walkthrough (one function from source to dump, following the four blocks of a `for` and the copy-out chain) · ratify the 5 adversarial IR goldens (continue-steps, try-copies-out, diverging-arms, nested-shortcircuit, for-evaluates-once) · mutation drill (change `continue_to: step` to `continue_to: test` in control.rs and predict which tests fail — the answer is interesting) · exit-quiz (hand-lower `for x in xs` or `e?` on paper, then diff against `--dump-ir`) | /debrief | the author's call, when and how much

- [ ] panel 019 | Ratify the seven points: one pass (no desugared tree, `ir::verify` instead of GHC's Core tree) · slots and no phi · the amended three-address invariant with the five omissions the compiled C found · `heroes build` that speaks · polymorphic IR with monomorphisation as an IR→IR pass at M6 · `test`/`assert` lowered at M4 with both sides carried · the dump's notation. Two vetoes were lifted by amending design.md §4.12 and Part 5 rather than by argument — worth reading which, since CLAUDE.md §12 is what settled it | docs/panel/019-the-ir-and-where-sugar-dies.md § Resolution | the representation every later pass reads, decided once
- [ ] panel 019 | Decide: `???` in a file that reaches `build`. §4.16 says such a file "type-checks everything else but produces no binary"; what `--dump-ir` prints for a hole is undecided and M4 is deciding it by default | design.md §4.16 · docs/panel/019 § Watch list | the hole is the thesis's own construct and the IR is the first pass that cannot represent it
- [ ] panel 019 | An out-of-range `int` literal is a **new diagnostic class landing in M4** — nothing in this compiler decodes a literal yet. CLAUDE.md §4 makes a diagnostic class its own panel trigger | crates/heroes/src/ir/ (the literal decoder) · lexer/escape.rs unescape | the class was invisible until the pass that needs the value existed
- [ ] panel 019 | `heroes measure` vendors two of panel 011's three tokenisers, and o200k was the binding one on v0 by 2 tokens. Vendor it before any verdict lands within 10 tokens of a ceiling | crates/heroes/src/measure/mod.rs · docs/panel/011 | the instrument's own error bar, found by the warden while measuring something else
- [ ] pre-M4 | Hand-desugar `for x in xs`, `?`, and one UFCS chain from the appendix calculator — **against the IR text** (panel 019 re-specified this: there is no desugared tree to compare against, so the exercise's answer key is `heroes build --dump-ir`) | design.md Part 5 · tests/golden/ir/ | the desugarer landed in M4 and the dump is the only evidence the table was honoured

- [ ] panel 018 impl | The pre-018 shape `MAX = constant: int` now costs exactly ONE diagnostic and the body below it vanishes with it. Which code fires, and what makes the recovery stop before the next declaration? | syntax/tests/recovery.rs (the_old_shape_costs_one_diagnostic) · syntax/decl.rs file() | the migration path is itself a diagnostic, and the closed keyword set is the anchor
- [ ] panel 018 impl | `record Point` now ends in a token that can end a statement. What did that single fact let recovery DELETE, and what replaced the span arithmetic? | syntax/recover.rs recover_to_next_decl · docs/glossary/002 § coda | panel 007's patch died of the inversion — the lesson demonstrated on itself
- [ ] panel 018 impl | `for i < 3` gets the `while` fix as a Guess; `for !done` gets it Certain. What distinguishes the two parse states, given the message is identical? | syntax/stmt.rs for_stmt | certainty is a property of the parse state, not of the message
- [ ] panel 018 impl | `function f():` costs one diagnostic with a certain fix and the block still parses. Where is the colon eaten, and on which constructs does the same helper fire? | syntax/stmt.rs eat_python_colon · tests/golden/check/trailing-colon.hero | the blind experiment's top pre-registered slip became machine-repairable
- [x] panel 018 | RATIFIED 2026-08-04 (option 2: core + F default, colon question to metric 2) — was: Ratify: A stands, the inversion and named tests rejected — then decide the candidate core (keyword-first, no `=`, attached parens: `record Point`, `function dist2(a: Point, b: Point) -> int`, plus the `while`/`for` split): ratify now (M3 closed — the engineer's churn window is open, >100 files past M4) or wait for metric 2? And the one open sub-decision: trailing `:` on every block header (E) vs bare headers (F) — pre-registered on both sides, first metric-2 run is the tiebreaker, conservative default F | docs/panel/018-top-level-declaration-shape.md § Resolution | the only §1.7-qualifying simplification on the table, and its cost compounds per milestone
- [x] M0 | Explain-it-back: what does spike 04 decide, and why does it exist before any compiler code? | tools/spike/04-variant.c · docs/journal/000-setup.md §5 | it fixed the runtime container + descriptor ABI that M5c will implement
- [x] M0 | Milestone offers: superseded by M1's drill (same lexer, live symptom); exit-quiz declined for now | docs/journal/000-setup.md | optional, on request — see /debrief
- [x] M0 | Decide: retro-tag `m0`? → tagged 2026-08-04 at 02c4ca1 (M0 closed before the tagging habit) | git log 02c4ca1 | tags are the chronology the book and /where rely on
- [ ] harness | Metric 2 minimum viable protocol (panel 011): local open-weights model, or 2 pre-registered tasks per milestone. NOT an API-key question | docs/panel/011-measurement-without-an-api-key.md | ~16 samples by v1 beats zero; the pre-amendment number is still unrecoverable later
- [ ] harness | Held-out tasks must be author-written (assistant-written tasks would measure the assistant's priors); n now scales with the paced protocol, not 15 at once | harness/tasks/README.md | metric 2 validity
- [x] panel 011 | RATIFIED 2026-08-04 — budget as hard measured ceiling (3000, panel 012) (max over two vendored tokenisers), metric 3 primary-but-never-pooled with three arms, metric 2 via local model or paced sampling | docs/panel/011-measurement-without-an-api-key.md § Resolution | the repo is IN BREACH today at 2050 — this decides what happens next
- [x] tooling | LANDED at M1 (early, commit fe33560): `heroes measure`, two vendored BPE tables, zero deps. Stale item ticked in the 2026-08-04 debrief | panel 011 | the rule 'measured-only' is unenforceable until this exists
- [ ] pre-M3b | Paper exercise: hand-check five expressions in two columns (⇐ checking / ⇒ synthesis) | glossary entry to be born from this session | M3b is the conceptual cliff; the exercise should precede it
- [x] M1.1 | First tokens. How many tokens does the line `print((2 + 3) * 4)` produce, counting neither layout tokens nor the terminator — 8, 10, or 12? | crates/heroes/src/lexer/tests.rs (first_hero_end_to_end) | reading a token dump is reading what the parser will see
- [x] M1.1 | `for` and `print`: one is a keyword token, one a plain identifier. Which is which, and why does the parser care about the difference? | spec/reserved-words.md § keywords | keywords carry structure; identifiers are just names
- [x] M1.1 | A line ends with `+`; the next starts a new expression. Does the lexer emit a terminator after the `+` — yes or no? And after a `)`? | crates/heroes/src/lexer/layout.rs (is_line_ender, design.md §4.15) | this one rule is how Heroes lives without semicolons
- [x] M1.1 | An 8-space indent follows a 0-space line: one Indent token, two Indent tokens, or an error? | lexer/tests/adversarial.rs | rigid indentation is the layout bet — errors, never interpretation
- [x] M1.1 | Ratify the 5 adversarial lexer cases (tab in margin, 5-space indent, 2-level jump, tab mid-line, single `&`) — say what each guards against | crates/heroes/src/lexer/tests/adversarial.rs, marked UNVERIFIED | they become check/ goldens when the runner lands
- [x] M1.1 | Design gap found: §4.15's terminator ender-list omits `break`/`continue`/`???` — panel session needed before M2 | resolved: docs/panel/007-terminator-enders.md, author ratified live 2026-08-03 | the parser cannot land on an ambiguous line-ending rule
- [x] M1.3 | How would a Heroes program express a real newline character? (When asked: it couldn't) | resolved by panel 008 → the M1.5 items below | the self-hosted lexer needs '\t' without magic numbers
- [ ] M1.4 | Write `let x = 5` in a .hero file and run `heroes lex` on it: what comes out, and what travels attached to the diagnostic? | spec/reserved-words.md · lexer tests (certain_fix_travels_with_the_diagnostic) | the thesis made executable: the likeliest LLM mistake fails loudly with the repair pre-written
- [x] M1 close | Milestone debrief offers (walkthrough + adversarial ratification + mutation drill done; exit-quiz declined): lexer walkthrough (16 token kinds end to end) · ratify the 5 adversarial cases · mutation drill · exit-quiz (re-implement `is_line_ender` or `char_lit` on a throwaway branch) | /debrief | all optional, author's call; doing it by hand first is the lesson
- [x] M1.5 | `"a\nb"` used to compile printing four characters. Now it prints two lines. Which half of panel 008 did that — the five escapes, or reserving the backslash? | docs/panel/008-escape-sequences.md · lexer/escape.rs | the reserved backslash is the half that does the thesis work
- [x] M1.5 | `"C:\temp"` still compiles silently as `C:<TAB>emp`. Why can no escape set fix that, and what would? | lexer/tests/literals.rs (the_residual_windows_path_trap_is_on_the_record) | knowing the limits of a fix is part of owning it
- [x] panel 009 | Ratify the budget governance adopted provisionally: measured-only, soft 1500/hard 2000, the +500 as a pre-allocated purse, spend only where a wrong guess is silent | docs/panel/009-spec-budget-2000.md § Resolution | it is the rule that keeps 2000 from becoming 2500
- [x] panel 010 | RATIFIED 2026-08-04 — the repeated-`@` rule: two `@` arguments sharing a root binding are a compile error, over-rejecting distinct-index pairs by design | docs/panel/010-repeated-mutable-arguments.md § Resolution | three judges converged; Ada reached the same rule in 2012 after 33 years of the alternative

- [ ] panel 013 | Ratify the function type's marker: `(function(A, B) -> C)`, `fn` stays a reserved-word error everywhere. The recommendation put to the judges (`fn` as a type-position-only keyword) was overturned — worth reading why before ratifying | docs/panel/013-function-type-marker.md § Resolution | it is the first panel whose verdict reversed the proposal on evidence, and the registry's uniformity was what was at stake
- [ ] panel 013 | Decide §1.6's scope for `spec/reserved-words.md` (831 measured tokens, uncounted). True headroom is 952 or 121 | docs/panel/013-function-type-marker.md § Watch list | the answer is worth more than anything panel 013 itself decided
- [x] M2.1 | First tree. `{str: [int]}?` — 5 nodes (str · int · [int] · the map · the fallible wrapper); now pinned by `the_arena_holds_one_node_per_written_constructor` | crates/heroes/src/syntax/types.rs, tests/types.rs | the arena is why a recursive type needs no pointers, which is why it ports to Heroes
- [x] M2.1 | Two of the five declaration headers get NO terminator from the lexer. Which two, and why does the body parser skip terminators instead of requiring one? | crates/heroes/src/syntax/decl.rs (module doc), lexer/layout.rs is_line_ender | panel 007's rule, seen from the other side of the fence
- [ ] M2.1 | `function advance(@l: Lex)` dumps with a result type. Which one, and where did it come from, given the source never wrote it? | syntax/decl.rs (the arrow branch), printer/dump.rs | a synthesised node is how later passes stop asking "was it written?"
- [ ] M2.1 | A `#` comment, then a blank line, then a declaration: does the comment become documentation — and which two comparisons decide it? | syntax/cursor.rs take_docs (§4.1) | Go's adjacency rule is two integer comparisons, not a parser mode

- [ ] M3c | A `match` used as a VALUE requires every arm to produce a value. Found by the llm-ergonomist in panel 014: the rule is orthogonal to the arm-body spelling, and today `tag = match t` with an arm whose body is `break` (or `v @ v + 1`, which does not even diverge) is silent under **both** readings — `=> assert false` inline and the same statement inside a block arm are equally unguarded | docs/panel/014-*.md, crates/heroes/src/syntax/control.rs | it is the only sentence in that panel that converts a silent wrong program into a loud one, and it belongs to the checker, not the grammar
- [x] panel? M2.2 | RESOLVED by panel 014 (author instruction, debrief 2026-08-04): an arm's body is one statement, inline, or an indented block. Adopted on §1.7 — it deleted the `ArmBody::Expr` bypass and with it a `heroes fmt` that deleted code, a `heroes parse` that hung, and a doubled diagnostic. R vetoed; the appendix's 8 inline `assert` arms are restored | docs/panel/014-match-arm-body.md | the rationale I attached to the proposal was false and the record says so: `?` never needed statement arms
- [ ] panel? M2.2 | The foreign-word registry reserves words that are plausible *identifiers*: the appendix had a variant case `.var` and could not lex. Found the same class as panel 013, Heroes-side | crates/heroes/src/lexer/keywords.rs (foreign_word), design.md appendix (`.var` → `.variable`) | `var case union use include class try const` are all unusable as names today; the registry's price is now measurable
- [x] M2.2 | First bodies. `x = 2 + 3 * 4` dumps as `(2 + (3 * 4))`. Which single line of `expr.rs` decides that, and what would `x = 2 * 3 + 4` dump as? | crates/heroes/src/syntax/expr.rs (binary_op), tests/bodies.rs | the dump is fully parenthesised so that it *proves* the precedence table
- [x] M2.2 | Three line shapes start with a name: `x = 5`, `v: int @ 0`, `v @ v + 1`. How many tokens of lookahead tell them apart, and which token is it in each case? | crates/heroes/src/syntax/stmt.rs (module doc, statement) | §4.4 designed the surface so this is decidable without scanning the block
- [ ] M2.2 | `if` is an expression (§4.7). In the dump of `state = if t.done …`, what appears on the `bind` line, and where do the branches go? | crates/heroes/src/printer/bodies.rs (write_valued), tests/bodies.rs | one rule seen in two places is the claim; the printer is where it either holds or does not

- [x] M2.3 | `heroes fmt` is idempotent AND tree-preserving. Which of the two would a formatter that deletes every comment still pass? | crates/heroes/src/printer/tests.rs (assert_canonical) | the two properties are not the same property, and only one of them is about the reader
- [x] M2.3 | `x = 2 + (3 * 4)` formats to `x = 2 + 3 * 4`, but `x = a - (b - c)` keeps its parentheses → distilled into docs/glossary/002-binding-power-and-position.md (power says how tight, position says which side) | crates/heroes/src/printer/fmt_expr.rs (wrapped) | associativity is the half of the precedence table nobody writes down
- [ ] M2.3 | `# Just a remark.` + blank line + a declaration. Why is the formatter forbidden from closing that gap? | crates/heroes/src/printer/tests.rs (a_blank_line_between_comment_and_declaration_is_preserved), §4.1 | in this language whitespace carries meaning twice: indentation, and this

- [x] panel? M2.4 | RESOLVED by author instruction (debrief 2026-08-04, "layout → migliora"): better lexer recovery instead of gating — round the margin to the nearest level, clamp a jump to one level. 5 diagnostics → 2, and gating would have hidden real parse errors elsewhere in the file. Original question: A wrong margin makes the block structure untrustworthy: `indentation.hero` shows 2 mistakes producing 5 diagnostics, 3 of them consequences | tests/golden/check/indentation.expected, docs/journal/002-parser.md §4 | the cascade is measured now, so the question has a number instead of an opinion
- [ ] M2 close | Milestone debrief offers, all optional: walkthrough (tokens → tree → text, the two renderings and why both exist) · ratify the 5 adversarial cases (`fn` both positions, depth-zero continuation, braces as a block, signature without `:`, trailing comma) · mutation drill (break one line of `expr.rs`'s precedence table and predict which test fails) · exit-quiz (re-implement `wrapped` in fmt_expr.rs, or the three-line-shapes lookahead, on a throwaway branch) | /debrief | the author's call, when and how much

- [ ] M3b | Classify `break`/`continue`/`return`: they must NOT be typed `()`. RFC 1216 records that exact wrong turn ("some code in the compiler assigns type `()` to diverging expressions because it doesn't have a sensible type to assign to them"); under `()` an `int`-valued `match` with a `return` arm becomes a type error and panel 014 reconvenes | design.md §4.7 (the new bullets), docs/panel/014-match-arm-body.md | Rust types them `!`, Kotlin `Nothing` — the historian's decisive row
- [ ] M3a/M3c | Keep *declarations* out of inline arm bodies (historian, panel 014 prediction 6): Kotlin's grammar admits them and a name bound in an arm is a name nothing can read | crates/heroes/src/syntax/stmt.rs (statement), docs/panel/014-match-arm-body.md | Rust keeps declarations out of expression position deliberately
- [ ] panel? | A `()` value in *binding* position is unrejected: `x = print("a")` today. Pre-existing, shared by every option panel 014 considered, and it wants its own panel priced on its own | docs/panel/014-match-arm-body.md § Watch list | panel 003 checks non-`()` in statement position, not `()` in value position

- [x] panel 015 | RATIFIED 2026-08-04 (author instruction, `/goal`) — the resolver's rejection set: unused reaches parameters (`_` is the valve), only a read is a use (with the `@` copy-out carve-out), **one tier** of built-in names reserved everywhere, the UFCS unknown narrowed to "not a field anywhere in the file", and sequential shadowing against Java/C#'s order-insensitive rule | docs/panel/015-resolver-rejection-set.md § Resolution | three of the five defaults were changed by the judges' evidence, and one of them (C) was killed by two judges who could not see each other's documents
- [ ] panel 015 | Decide whether `print` stays reserved. The historian's objection is on the record and unresolved: PEP 3105 made `print` a function *so that* one module could replace it, which is how Python captures output in tests — and Heroes has `test` blocks with no capture mechanism | docs/panel/015-* § Watch list | it is the only reserved name with a documented counter-precedent
- [ ] panel? M3b | `+` on `str` is unwritable per the spec's operator table, while §4.20 gives the runtime string concatenation and the appendix writes `"…" + name`. Found by the judge that had only the spec — it restructured a whole program to avoid a construct it could not find | docs/panel/015-* § Watch list, design.md §4.20 | the same class as the missing `\"` (panel 008): the language is INCOMPLETE without it, and nothing says so
- [ ] M3a | Order-free top level. `factor` and `group` call each other, and neither is declared first. How many passes does the resolver make over the declarations, and what exactly is in the table before the first body is walked? | crates/heroes/src/resolve/top.rs (module doc) | this is why the language has no forward declarations — not "does not need", cannot express a use for
- [ ] M3a | Three names in one function: `MAX` (a top-level constant), `x` (a local), `print` (a built-in). Each records a different `Ref` variant. Which one is looked up FIRST, and what would break if the order were reversed? | crates/heroes/src/resolve/exprs.rs (`name`), tests/names.rs | the lookup order is what makes "shadowing is an error" and "a built-in's name is taken" consistent instead of contradictory
- [ ] M3a | `total: int @ 0` then `total @ 1` and nothing else: one diagnostic. Now add `print(total)` at the end: zero. Which counter changed, and why is the *initialiser* not counted as a write? | crates/heroes/src/resolve/scope.rs (`report_unused`), tests/unused.rs | if the initialiser counted, the unused rule would be a no-op for every cell in the language
- [ ] M3a | A file whose last function is `function simplify(e: Expr) -> Expr` / `???` reports no unused bindings anywhere — not even in the function at the top. Which field decides it, and where is it computed (hint: not in the walk)? | crates/heroes/src/resolve/mod.rs (`has_hole`), §4.16 | a hole at the bottom of the file suspends a rule at the top of it, so the decision cannot be made while walking
- [ ] M3a | `print(totl)` next to `total = 1` used to produce **two** errors. Which one was the consequence, and what does the resolver now remember in order to stay quiet about it? | crates/heroes/src/resolve/mod.rs (`near_names`, `suggested`), journal 003 §3 | the compiler was reporting a mistake it had itself proposed the repair for
- [ ] M3a close | Milestone debrief offers, all optional: walkthrough (`heroes check --dump-scopes` on the appendix: the alphabetical table, the nesting, the read/write counts) · ratify the 5 adversarial cases (one tier · the `@`-typo · the four unused shapes · the hole's reach · shadowing with sibling loops as the control) · mutation drill (make `report_unused` count writes as reads and predict which of the 164 tests fail) · exit-quiz (re-implement the `nearest` candidate rule, or the sequential shadow check, on a throwaway branch) | /debrief | the author's call, when and how much

- [ ] panel 016/017 | Both RATIFIED as they landed (author instruction, `/goal`). Worth reading anyway for the two reversals: 017's A was decided by *executing* three options, and its historian's objection was answered by building the rule once instead of per construct — the judge's own withdrawal condition | docs/panel/016-command-surface.md, docs/panel/017-type-system-edges.md | the panel changed the design twice more, and both times the change came from a judge's evidence rather than from the proposal
- [ ] panel? M3b | A `match` over `bool` is treated as countable, so `_` is banned over it — but `true`/`false` are literals, not `.cases`, so an exhaustive `bool` match cannot be written at all today. Found while writing `patterns.rs` | crates/heroes/src/types/patterns.rs (`wildcard`, `exhaustive`) | the two rules that hold each other up (§4.7) do not fit the one type that is a two-case variant by construction
- [ ] panel? M3c | `()` inside a container — `[()]`, `{str: ()}`, `()?` — resolves and checks clean. Ten lines to reject, and the engineer's condition 4 in panel 017 says C1 is *not* credited with closing it | docs/panel/017-* § Resolution, crates/heroes/src/types/lower.rs | M5c's descriptor pass would otherwise be asked for `h_unit_copy` on a zero-size element
- [ ] panel? M3c | §4.9's same-typed-argument rule lands hardest on FFI, where C's numeric APIs cluster same-typed parameters: `pow(base:, exponent:)` and `hypotenuse(a:, b:)` now need labels at every call site. Visible in `examples/gallery/08-ffi.hero`. Does the rule cross the `extern` boundary, or does a bound C signature get an exemption? | examples/gallery/08-ffi.hero, design.md §4.9, §4.19 | the rule is one of the thesis's headline mechanisms and this is the first place its price is concrete
- [ ] panel? M3d | `to_int` on a `str` is not offered (parsing can fail and the spec does not say what it returns), and ordering on `str` is not offered either (`a < b` on strings is an error, because a collation is a language decision). Both are rejections, so both are relaxable | crates/heroes/src/types/builtins.rs, ops.rs | two absences a model will reach for, chosen rather than overlooked
- [ ] M3b | Bidirectional checking, the two modes. `x = []` is an error and `xs: [int] = []` is not — which mode is each, and why can exactly four forms not synthesise? | crates/heroes/src/types/mod.rs (the table), expect.rs | §4.5's promise that errors stay local is this table and nothing else
- [ ] M3b | `tag = match t` with one arm `=> return 0`: legal, and `tag = match t` with *every* arm a `return`: an error. Which file decides that, and how many places in the compiler know what a diverging branch is? | crates/heroes/src/types/join.rs, stmts.rs (`Flow`) | the answer is 1, and a judge's objection was withdrawn because of it
- [ ] M3c | `t == .plus` type-checks and `t = .plus` does not. What does the equality operator hand to the case, and why does every *other* binary operator do the same thing? | crates/heroes/src/types/exprs.rs (the Binary branch, `contextual`) | one rule where two would have been the obvious shape
- [ ] M3d | Run `heroes check` on a file with one wrong field label. Which four pieces does the message carry, and which one of them is the file you would otherwise have opened? | crates/heroes/src/diagnostics/render.rs, §4.17 | the whole §4.17 argument is that a model has no project open
- [ ] M3d | `heroes mutate` prints two columns. Which one is the measurement, and what would a compiler that rejected every program score in each? | crates/heroes/src/mutate/mod.rs, docs/measurements/001-metric-3.md | panel 011 made the control arm mandatory before any number could be quoted
- [ ] M3 close | Milestone debrief offers, all optional: walkthrough (a mismatch from `heroes check` end to end: which pass produced it, what `Want` was in play, why the message names the *other* end) · ratify the 5 adversarial cases (mixed arithmetic · non-exhaustive and the `_` ban · no implicit `T?` · swapped arguments · the jump-and-unit pair) · mutation drill (make `join.rs` treat a diverging branch as a value and predict which of the 259 tests fail) · exit-quiz (re-implement the `Want` enum's three cases, or the same-typed-argument rule, on a throwaway branch) | /debrief | the author's call, when and how much

## Covered

- 2026-08-04 — Panel 014 convened and resolved from the debrief (author: `arm → panel`): an arm's body is one statement or a block. Two shipped defects found by the judge that *built* the option — `heroes fmt` deleting an arm's `if` branches, `heroes parse` never terminating — and both fixed. R vetoed, the false `?`/M4 rationale struck, the appendix's inline `assert` arms restored. Layout cascade improved on the author's instruction: 5 diagnostics → 2. Panel 013 and M2's five adversarial cases ratified.

- 2026-08-04 — M2 concepts: the type arena (one node per written constructor — now a test), the terminator rule seen from the parser, the fully-parenthesised dump, the three line shapes of §4.4, the formatter's minimal parentheses, and the two properties `assert_canonical` checks. Two divergences pointed the same way and were distilled into `docs/glossary/002-binding-power-and-position.md`: binding power says how tight, position says which side, and a rule stated over *constructs* (which headers get a terminator, when parentheses survive) is wrong in exactly one case. Stale queue item ticked: `heroes measure` landed at M1.

- 2026-08-04 — Ratified panels 010 (repeated `@` arguments are a compile error), 011 (offline measurement, metric 3 primary but never pooled, metric 2 via local model or paced sampling) and 012's carry-forward governance.

- 2026-08-04 — M0/M1 closed out: spike 04 walkthrough written into journal 000 §5, mutation drill run and recorded in journal 001 §4 (the diagnostic lied — the bug was in the bookkeeping it reads), `m0` retro-tagged. Exit-quiz declined; it stays available on request.

- 2026-08-04 — Ratified: the 5 adversarial lexer cases (rationale now on each case; the UNVERIFIED marker is gone) and panel 009's budget governance in full.
- 2026-08-04 — M1 concepts (first tokens, keyword vs identifier, terminator insertion, indentation recovery) and the two escape questions. One divergence, distilled into `docs/glossary/001-expressible-vs-loud.md`: making something *expressible* and making a mistake *loud* are separate jobs with separate prices, and the panel voted them separately.

- [ ] panel 020 | Ratify the ten points: the unsupported report as a `Diagnostic` **kind** exiting 1 (not a parallel type exiting 2) · the completed gate table with `extern` held to M7 · the `@` pointer ABI · the ten things clang decided (unit temporaries, unreachable blocks, `INT64_C`, the `%` guard, the stdout flush, the destination-type overflow rule, the effective-line `#line`, `missing_return`, `main`'s contract, block-locality in the verifier) · the injective mangler · `--no-line` not born · both `-W` flags now · the stamped runtime · print's spec sentence · two-configuration `run/` goldens. **Two vetoes were lifted by a change, not an argument** — one table row and one exit code | docs/panel/020-the-c-emitter.md § Resolution | it is the milestone that turns the compiler into a compiler, and every rule here is inherited by M5b/M5c/M6 |
- [ ] panel 020 | The exit-code question is the panel's most interesting disagreement: the historian defended **2** from GCC's `sorry()` and the ergonomist measured **1** by watching its own first action under both. Read GCC's `toplev.c` line the historian quoted (`if (sorrycount) exit (FATAL_EXIT_CODE)`) and say which judge the source actually supports | docs/panel/020 § Where the judges disagreed | the same evidence pointed both ways until somebody read it to the end |
- [ ] panel 020 | Decide the spec gap the emitter walked into: nothing in the spec says what `INT64_MIN % -1` is. Heroes aborts it as overflow, on the grounds that silence is a gap rather than permission. Measured: unguarded it returns 0 on arm64 at -O0 and raises SIGFPE on x86 — the same program, two behaviours, no diagnostic | design.md §4.14 · spec lines 126-128 · docs/panel/020 § Watch list | the abort list is short enough to be complete, and it is not |
- [ ] panel 020 | `print`'s separator is 2-1 against Heroes in the record: Pascal's `WriteLn` inserts nothing (the cited precedent, verified against ISO 7185), but Go's `println` adds spaces *and* writes to stderr, and Python 3 defaults `sep=' '`. The trailing newline is unanimous. Panel 006's historian and panel 020's both asked for a Go-style hedge in the spec by v1 | docs/panel/020 § Watch list · docs/panel/006 | a model's prior is the thing the spec is buying, and here the prior is split |
- [ ] M5a | The compiler has no line budget and `crates/heroes/src` is already 15,145 non-test lines — about 4x Pascal-P4. "Does it fit one person" is no longer answerable globally; the only live ceiling is CLAUDE.md §11's per-file one. Recorded by the engineer, unanswered by design.md: decide whether a global number is wanted before the port makes it a Heroes number | docs/panel/020 § Watch list · design.md Part 0 | the port's size is the self-hosting schedule, and nothing is watching it |

- [ ] M5a.1 | First C. `print((2 + 3) * 4)` becomes how many lines of C, counting neither declarations nor `#line` directives — and which of them can stop the program? | tests/golden/emit/scalars.expected · crates/heroes/src/emit/inst.rs | the emitter is a printer, and reading its output is reading what clang will see
- [ ] M5a.1 | Why is `#line 8 "f.hero"` repeated before every instruction on line 8 rather than emitted once? One sentence, and it is about C rather than about Heroes | crates/heroes/src/emit/writer.rs (the module doc) · tools/spike/01-first.c:19 | the frozen target had this wrong and nobody noticed for two milestones
- [ ] M5a.1 | An `if` whose both arms `return` produces a block the emitter does not print at all. Say what would happen in C if it printed the label but not the block, and what would happen if it printed neither the label nor the `goto` | crates/heroes/src/emit/decls.rs (reachable) | C is physical, and this is the one place where that matters more than the CFG
- [ ] M5a.1 | `bump(@v)` becomes three lines of C in two functions. Name them, and say which one runs on the error side of a `?` | emit/decls.rs (prologue) · emit/inst.rs (CopyOut) · design.md §3.1's `@` bullet | §4.8's "copy-out happens always" is now a calling convention, and M5b/M5c/M6 inherit it
- [ ] M5a.2 | The dominance invariant's first wording was rejected by an existing test within the hour. Read both wordings and say which construct falsified the first — then say why that construct is *correct* | crates/heroes/src/ir/values.rs (the module doc) · ir/tests/verify.rs | an invariant that is too strong is as wrong as one that is too weak, and only one of the two is loud
- [ ] M5a.2 | `heroes build` on a file with a hole exits 1; `heroes check` on the same file exits 0. Both are right. Say what question each command is answering | crates/heroes-cli/src/commands/compile.rs · §4.16 | the ergonomist measured exit 0 producing a `build && ./artifact` loop that runs yesterday's binary
- [ ] M5a close | Milestone debrief offers, all optional: walkthrough (one function from `.hero` through the IR to the C to the binary, following the `#line` directives and the copy-out) · ratify the 5 adversarial `run/` cases (overflow-aborts, division-edges, short-circuit, copy-out-edges, join-slot) · mutation drill (delete the `preds.is_empty()` test in `emit/decls.rs::reachable` and predict which of the 55 goldens fail — the answer is interesting) · exit-quiz (hand-emit the C for a `while` loop, then diff against `--emit-c`) | /debrief | the author's call, when and how much
- [ ] M5a | DEFECT, open: a binding statement's span runs to the end of its line, so a trailing `#~ <code>` annotation lands under the diagnostic's caret (`greeting = "hello"      #~ str` underlines the comment). Two `unsupported/` goldens use the next-line form to work around it | tests/golden/unsupported/strings.hero (the comment) · syntax/stmt.rs | the caret is the part of §4.17 that says *where*, and a caret over a comment is a caret over nothing

- [ ] panel 021 | Ratify the ten points: two ops not three (`cow_check` struck for having zero call sites) · the five ownership rules, each with its compiled counterexample · zero-init with `ptr == NULL` as the one non-value · `HeroStr` by value **because of the FFI** · `hero_str_from_bytes` · the f64 ladder with no "shortest" and `1.0` not `1` · `uselocale` instead of a promise · `slice`'s `to` excluded · the phase on the `Program` · the leak counter instead of ASan. **Two vetoes, both lifted by a written paragraph and one primitive** | docs/panel/021-strings-ownership-and-how-a-float-prints.md § Resolution | the memory model every later milestone inherits, decided once
- [ ] panel 021 | Decide: is `inf`/`-inf`/`nan` **reachable**? §4.14 says "division by zero aborts" without qualifying the type, so `0.0/0.0` may abort — and nothing says f64 overflow does not. The three spellings are implemented and deliberately **not** in the spec (+10 measured for states the reader may not be able to reach) | design.md §4.14 · spec lines 126-128 · docs/panel/021 § Watch list | naming a spelling nobody can reach is redundancy spent where no error occurs (§1.4)
- [ ] panel 021 | Decide `len`'s unit in the spec, or leave it derivable. The discriminating probe is `print(len("è"))` — 2 under bytes, 1 under characters — and the panel's own test vector was all-ASCII, so it could not have told the difference. +8 measured | spec line 47, 146 · docs/panel/021 R7 | the corpus decides: pre-registered for M5c
- [ ] panel 021 | `assert s == "x"` will be a **use-after-free** under the ownership rules: `ir/asserts.rs` computes both operands in the test block and reads them in the abort block. Gated to M6 with `Abort::Assert`; the fix is to store refcounted operands into synthetic slots | crates/heroes/src/ir/asserts.rs · docs/panel/021 R2 | queued so it arrives as a scheduled repair rather than as a crash
- [ ] panel 021 | Reference counting leaks cycles, and Nim needed a whole second collector (ORC) for exactly this. Safe for `str`; false the first time a refcounted type can cycle, which is M5c's recursive `variant` | docs/panel/021 § Watch list · examples/gallery/11-trees.hero | the milestone that makes it false is the next one
- [ ] panel 021 | Optimisation level is chosen by **verb** (`build` = -O0, `run` = -O2) and sanitising by **flag**. Unifying them is a larger surface nothing must type (§10), so it waits — but the asymmetry is now visible in `--help` | crates/heroes-cli/src/cli/table.rs · CLAUDE.md §10 | a surface that grew by accident is the thing the stopping rule exists to prevent

- [ ] M5b.1 | First refcount. `s: str @ "a"` then `s @ s + "b"` produces how many refcount instructions, and which of them is the one that would double-free if the pair were reversed? | tests/golden/emit/strings.expected · crates/heroes/src/own.rs (rule 3) | the order of two operations around a store is the whole rule
- [ ] M5b.1 | The pass **moves** an owning temporary into a slot instead of releasing it at the end of its block. Read the two refuted wordings in `ir/phases.rs`'s doc and say what `.must()` does to an expression that made the second one false | crates/heroes/src/own.rs (rule 5) · ir/phases.rs | this is panel 019's slots-over-phi decision being cashed, one milestone later
- [ ] M5b.1 | A plain `str` parameter is never decrefed and an `@ str` parameter is never swept. Both are correct and for different reasons. Give each reason in one sentence | crates/heroes/src/own.rs (rules 1 and 2) · design.md §3.1's `@` bullet | the convention a judge got wrong on its first compiled program, with ASan as the referee
- [ ] M5b.2 | `print(0.1)` prints `0.1` and `print(1.0)` prints `1.0`. Say what breaks in the golden harness if the second printed `1` — the answer is not about readability | tests/golden/run/f64-rendering.hero (the comment) · runtime/runtime.c (hero_f64_render) | it is the one place a type confusion becomes observable in the artifact the harness compares
- [ ] M5b.2 | Three forms of the "every owned slot is released" check failed before one worked, and the third failure was invisible: the check stayed silent on a hand-broken sweep. Say what a store to a counted slot has in common with a sweep, and what separates them | ir/phases.rs (released_on_return) | an invariant that cannot fail on a hand-made violation is not an invariant
- [ ] M5b close | Milestone debrief offers, all optional: walkthrough (one `str` from literal to heap block to sweep, following `--dump-ir` then `--emit-c`) · ratify the 2 new adversarial cases (str-copy-out, str-self-assign) · mutation drill (swap the incref and decref around a store in `own.rs` and predict which of the 68 goldens fail — and which configuration catches it) · exit-quiz (hand-write the refcount instructions for `out @ out + to_str(i)` inside a loop) | /debrief | the author's call, when and how much
- [ ] M5b | The `f64` ladder's guarantee is **round-trip-exact, not shortest**, and the distinction is worth one paragraph: `5e-324` renders shortest only because of gnulib's subnormal branch, and Java shipped `1.9999999999999998E23` for eighteen years while satisfying its own "round-trips" spec | runtime/runtime.c (hero_f64_render) · design.md §4.9 | a spec sentence that says "shortest" would be false, and §12 makes a false spec the compiler's bug

- [ ] panel 022 | Ratify the six points: COW **per step of the place path** with the incref-before-unshare order (the veto: a measured aliasing leak that every instrument in this project reported as success) · `Op::CowCheck` staying out of the IR because a judge built a cycle with the hoistable form · records and variants by value, **confirming** spike 04 · `HeroDesc.copy` shallow, and the spike's own `.expected` changing with it · no `memcmp`, `hash` never null · cycles impossible for the *published* reason · the map's mutation half struck for having no call sites · three spec sentences at +24 | docs/panel/022-aggregates-descriptors-and-cycles.md § Resolution | the memory model's last open question, and the one place where a green harness was measured defending a wrong program
- [ ] panel 022 | DECIDE by M6's closure audit: fund `set(m, k, v)` + `for k in m` (**+29 measured**) or delete `{K: V}` from the language (**−57 measured**). The map cannot be added to, so the self-hosted compiler cannot build a symbol table by insertion; the bootstrap has 14 `.insert(` sites and `resolve/scope.rs` already ships the association-vector alternative. Queued since M4, now priced | design.md §1.0's closure list · spec line 146 · docs/panel/022 R4 | 57 tokens for a container that cannot hold a symbol table is the only indefensible option
- [ ] panel 022 | `T?` is **40 bytes for every `T`**, because `Failure` is two `str`s — so every `m[k]` returns through memory rather than in registers, measured on AAPCS64. design.md fixes `T?`'s C representation **nowhere**; M5c states it rather than inheriting it. Rust polices the same largest-case tax with two lints, `result_large_err`'s threshold being 128 bytes | docs/panel/022 § Watch list · design.md §4.6, §4.20 | the cost lands on the return type of every fallible function in the language
- [ ] panel 022 | A new diagnostic class M5c walks into: **no infinite-size rule exists**. `record Node { child: Node }` reaches clang as `error: field has incomplete type` — design.md §8's wart 13 happening to a *program* error. CLAUDE.md §4 makes a diagnostic class its own panel trigger | crates/heroes/src/types/decls.rs · docs/panel/022 § Watch list | the class is invisible until aggregates are emitted, which is now
- [ ] panel 022 | `for x in xs` over a counted element type is undecided: borrowing is a use-after-free if the body mutates `xs`, and +1 per element is the safe answer at the cost of a descriptor copy per iteration. `own.rs` currently declares `Op::Index` non-allocating, "listed rather than defaulted so that switching one on is a decision here" — this is that decision | crates/heroes/src/own.rs (allocates) · docs/panel/022 § Watch list | the comment invited the decision and the panel did not make it
- [ ] panel 022 | Two free corrections the warden found while measuring something else: `docs/ROADMAP.md` asserted "reference counting **leaks cycles**", contradicting design.md §4.10 gift 2 (fixed with this panel); and `resolve/exprs.rs` walks a `BTreeMap` into diagnostic text with **no `// ORDER:` marker**, which §4.9 requires — there are zero such markers in the tree | docs/panel/022 § Watch list · §4.9 | a rule with no instances is a rule nobody is following

- [ ] author draft 2026-08-10 | Three claims from the value-semantics draft were **not** adopted into §4.10, and each is a small lesson in over-claiming: `null`'s absence belongs to §4.6 (absence as a variant), not to value semantics; "no data races by construction" is true only once Part 7.13's isolated heaps exist, so §4.10 says *inexpressible once concurrency arrives* rather than *bought now*; and "nothing is ever genuinely shared" contradicts copy-on-write itself, whose premise is that data **is** physically shared until someone writes — the true form adds *across threads*. Confirm or overturn each | design.md §4.10 "What it buys" · DESIGN-LOG 2026-08-10 | a design document that over-claims teaches the reader to discount it, which is worse than claiming less
- [ ] author draft 2026-08-10 | **Green threads** were placed in Part 7 (deferred), not Part 6 (rejected permanently), against the draft's "not a goal" wording. The reason is the document's own rule: Part 6 is for what loses on *locality*, Part 7 for what loses on *simplicity*, and a scheduler is a simplicity cost. Coroutines stay in Part 6 because stack switching is a step change in kind. Ratify the split, or move green threads to Part 6 and say which vertex refused them | design.md Part 6 preamble · Part 7 preamble · Part 7.13 | the two lists mean different things, and one wrong row makes both lists advisory
- [ ] author draft 2026-08-10 | The two Part 7.13 invariants — allocation through a **single point**, refcount ops behind a **narrow never-inlined boundary** — are recorded in design.md §4.20 but **not** in CLAUDE.md §7, where the generated-C rules that get enforced live. Both are true today (verified: one `malloc`, one `free`, both in the `str` primitives; `incref`/`decref` in a separately compiled TU). Decide whether they become §7 rules with M5c's array and map allocation, which is the first thing that can break the first one | CLAUDE.md §7 · design.md §4.20, Part 7.13 · runtime/runtime.c | an invariant nothing checks is a comment, and this one is checkable by grep

- [ ] panel 023 | Ratify the ten resolutions: `no_size` in the **checker** · the edge set as the **by-value property** rather than a list (wrong in both directions, both compiled) · `T?` **transparent** · one walk published in `Checked`, `emit/` with no graph code · the walk **iterative** (four compilers shipped a crash inside this check) · Swift's diagnostic shape, error on the **field** · the fix a **`guess`** (rustc's is `HasPlaceholders`, Swift emits none) · the spec at **+6**, buying the licence and not the prohibition · design.md §3.1:461 amended · the accepted `[T]`-mediated cycle landing before the check | docs/panel/023-a-type-with-no-size.md § Resolution | the class that decides whether a program error blames the author or the compiler
- [ ] panel 023 | **Spec line 53 is a defect this panel did not fix and must not be credited with.** "Absence is a different type (`T?`)" invites `T?` as an option type; line 50 defines it as "a `T`, or an error". The ergonomist predicts ≥25% of tree-shaped programs reach for `next: T?` under **either** spec — a `no_size` error where the author wanted an optional child. Decide whether the spec names the distinction or `T?` genuinely doubles as an option type | spec/heroes-spec.md lines 50 and 53 · docs/panel/023 § Watch list | the second-most-likely first draft of every recursive type in the language
- [ ] panel 023 | A payload-free variant emits `union { } as;`: `variant Color { red \| green \| blue }` compiles at `-Wall` and is `error: empty union is a GNU extension` under `-pedantic-errors`, which §4.19 schedules. Omitting the union entirely is clean under both, size 4 either way. M5c work, found by a judge compiling something else | docs/panel/023 R10.3 · design.md §4.20 | it passes CI today and fails on the day a flag lands

- [ ] M5c step 1 | The first **graph algorithm** in this compiler. `types/sized.rs` colours nodes white/grey/black instead of carrying a `visited` flag, and the difference IS the algorithm: grey means "on the current path", which is what makes a back edge a cycle rather than a diamond. Question: for `record Point/record Rect { a: Point, b: Point }`, how many times does the walk enter `Point`, and what would a `visited` flag get wrong that colours get right — and vice versa? | crates/heroes/src/types/sized.rs (walk) · docs/panel/023 R9 | the historian predicted the first defect here would be a false positive, and this is the mechanism that avoids it
- [ ] M5c step 1 | `Checked::type_order` is empty for a cyclic file, and the first version published a partial order instead. Task: say what a *partial* topological order of a cyclic graph would have done downstream, and why "the emitter never runs on a program with diagnostics" is a weaker guarantee than an empty list | crates/heroes/src/types/sized.rs (the tail of `walk`) · the test `a_cycle_leaves_no_order_to_mistake_for_one` | it was a test that found this, one line after the code was written
- [ ] M5c step 1 | Count: the corpus invariant runs over **66** accepted programs and only **14** aggregate declarations. Question: why so few, and which of `heroes mutate`'s ten operators could ever produce a containment cycle? (The answer decides whether `no_size` can appear in Metric 3 at all.) | crates/heroes/src/types/tests/sizes.rs · harness/mutations/operators.md | an invariant over a corpus with no instances is a rule nobody is following
- [ ] panel 023 | **Prediction 1 is falsified**: the compiler-engineer predicted `wc -l types/sized.rs` ≤ 150 and it is **298** (175 code, 123 prose). The calibration was `missing_return`'s 66 lines, which is a diagnostic with no algorithm in it. Decide whether the file splits (the graph walk from the edge definition) or whether ~300 with a 40% comment ratio is what this house style costs — `lexer/scan.rs` is 294/243 and was never split | crates/heroes/src/types/sized.rs · docs/panel/023 § Predictions to score · CLAUDE.md §11 | the prediction was mechanical on purpose, and it is the first one this project has scored as false

- [ ] M5c coverage | `print(match s ...)` does **not** parse: `error[missing_match_arms]: a match needs its arms indented one level below it`, plus a cascade from the argument list. Spec line 139 says "there is no ternary; `if` is an expression, and so is `match`", so a reader expects it to work — and panel 023's ergonomist independently reported binding `s = match t` first "purely to avoid an unspecified case". Decide: a layout rule the spec states, a parser fix, or a diagnostic that names the workaround | crates/heroes/src/syntax/ (match arms) · spec line 139 · docs/panel/023 § hesitation_points | every `match` used as an argument, which is the natural shape for `print`
- [ ] M5c coverage | Two rules bit while writing the coverage cases, and both are the language working rather than failing: `_` as a whole ARM over a variant is `wildcard_on_variant` ("name every case, so that adding one breaks this `match`"), and an unread parameter is `unused_binding`. Task: for a variant with five cases where three share a body, write the arm that is legal — and say what `|` costs against what `_` would have cost | spec lines 99-103 · tests/golden/run/variant-arms.hero | the rule exists so that adding a case is a compile error, and it is only worth its cost if the legal form is writable

- [ ] M5c step 5 | The first **type-erased dispatch** in this project: the runtime works on an array through `HeroDesc`'s five function pointers, and the emitter's own calls stay typed. Count: for `[Point]` where `Point` holds a `str`, how many functions does one `hero_array_decref` reach, and which of them is the one the compiler generated versus the one it wrote by hand? | crates/heroes/src/emit/perfn.rs (descriptors) · runtime/runtime.c (hero_array_decref) · tests/golden/emit/aggregates.expected | it is the one place the "clang type-checks every call" property is deliberately given up, once per type
- [ ] M5c step 5 | `push` **copies, always** — building an array by successive push is O(n²), the same shape as `s + t`. Question: what would have to be true of `xs` for an in-place append to be legal, and why can the IR never know it? (The answer is in what a refcount of 1 means for a value held by a slot.) | runtime/heroes_runtime.h (hero_array_push) · design.md §4.10's "known performance consequence" | the choice looks like a performance decision and is a correctness one
- [ ] M5c step 5 | Three defects in one step, each a *missing row* rather than wrong logic: `Ty::Array` absent from the field walk in `perfn.rs` (aborted at `hero_unreachable`), a variant's `hash` prototyped but never defined (undefined symbol at link), and a descriptor emitted after the function that names it (undeclared identifier). Task: say which of the three the "list every arm, never a catch-all" rule caught, and which two it did not — and what would have caught them | commits after ce2276a · crates/heroes/src/emit/perfn.rs | the rule has a shape, and knowing its edge is knowing when to add another instrument

- [ ] M5c step 6 | **Copy-on-write, per step.** Count: for `g.rows[0].cells[0] @ 7` where `h = g` shares every level, how many heap blocks exist before the write and how many after — and which of them does `h` still point at? Then say what a single unshare at the primitive would have left `h` pointing at | tests/golden/run/adversarial-cow-per-step.hero · runtime/runtime.c (hero_array_unshare, hero_array_set) · docs/panel/022 R2 | panel 022 measured the wrong version passing every instrument this project has
- [ ] M5c step 6 | The order is the rule: the value is increfed **before** the outermost unshare. Task: trace `n.children[0] @ n` with the incref moved to *after* the unshare, and say what the stored value would then refer to. (The answer is why `Op::CowCheck` is not an IR instruction — a judge built a three-block cycle out of the hoistable form.) | crates/heroes/src/own.rs (the indexed-store arm) · docs/panel/022 R3 | it is the one place in the pass where instruction order carries the correctness argument

- [ ] M5c close | **Walkthrough offer** (optional, author's call): the path of one `g.rows[0].cells[0] @ 7` from source to C, through `sized.rs`'s order, `counted.rs`'s answer, `own.rs`'s incref, `aggregate.rs`'s lvalue walk, and the three runtime primitives | docs/journal/008-aggregates.md · tests/golden/run/adversarial-cow-per-step.hero | it is the one path in the compiler where four passes each contribute one line to the same statement
- [ ] M5c close | **Golden ratification offer**: five adversarial cases marked UNVERIFIED — `no-size-best-friend`, `records-own-strings`, `adversarial-aggregate-overwrite`, `adversarial-recursive-tree`, `adversarial-cow-per-step` | tests/golden/check/, tests/golden/run/ | each was written to fail before it was written to pass; the ratification is checking that claim
- [ ] M5c close | **Mutation drill offer**: delete one line from `own.rs` rule 6, or one array arm from `perfn.rs`'s field walk, and predict which instrument fires — the leak counter, ASan, clang, or nothing | crates/heroes/src/own.rs · crates/heroes/src/emit/perfn.rs | the milestone's own record says the answer differs per line, and that difference is the lesson
- [ ] M5c close | **Exit-quiz offer**: six defects, all of them a missing row. For each, say which table was exhaustive (so the compiler caught it) and which walk had a catch-all (so it did not) | docs/journal/008-aggregates.md § What broke and why | the rule "list every arm" has an edge, and knowing the edge is knowing when to add an instrument instead

- [ ] M5d | **`int?` owns memory**, which is the least obvious consequence in the language: its error side is two `str`s, so every `T?` is reference-counted whatever `T` is. Question: how many heap blocks does `half(8)` allocate when it returns `ok(4)`, and how many does `half(7)` allocate returning `fail("odd", "not divisible")`? (The second answer depends on something about literals.) | crates/heroes/src/emit/perfn.rs (option_bodies) · tests/golden/run/fallible.hero | it is where "a scalar is free" stops being true, and the reason is a field nobody looks at
- [ ] M5d | The map's seed is **fixed**, and the reason is not security or speed. Task: say what would break, and at which milestone, if the seed were taken from the clock — and why the answer is about `diff` rather than about the map | runtime/runtime.c (HERO_MAP_SEED) · docs/panel/006 · ROADMAP M8c | it is the one place where a hash table's usual defaults are wrong for this project specifically
- [ ] M5d | The leak that only existed with a computed value: `{1: "one" + "!"}` then `.default(…)` lost one block, `{1: "one"}` lost nothing. Explain the difference in one sentence, then say what it implies for how every counting test in this repo has to be written | DESIGN-LOG 2026-08-10 (Op::MapGet) · runtime/runtime.c (HERO_STR_STATIC) | the cheaper spelling of every one of these tests would have passed
- [ ] M5d close | **The decision the author owes M6's audit**: `{K: V}` stays (fund `set` + `for k in m` at +29) or goes (−57). M5d built the read-only half on instruction, with the risk recorded — the audit should read a decision made with the cost known rather than a sunk cost arguing for itself | docs/panel/022 R4 · docs/ROADMAP.md M5d · DESIGN-LOG 2026-08-10 | 57 tokens for a container that cannot hold a symbol table is the option panel 022 called indefensible

- [ ] panel 024 | Ratify the five resolutions: the ceiling at **4096** · the soft line **held at 2000** (a thermometer vs a thermostat, and Linux's two numbers) · the **delta gate** and the **mortgage ledger** as the checks that replace a veto which now fires after ~61 amendments · that **915 of the 1865 new tokens have no named claimant** · and that panel 023's +6-token row is **falsified** | docs/panel/024-spec-budget-4096.md § Resolution | the budget is the project's only forcing function, and this raise left it measuring nothing until something else lands
- [ ] panel 024 | **The repair panel panel 023 now owes.** Its +6-token form turned silence into confident ambiguity: the ergonomist wrote `args: [Expr]` and `lhs: Expr` as two plausible evaluators and nothing in the spec forces either. Decide the wording that makes the recursion rule *decidable from the line*, and price it — it is one of the four gaps that together capture ~80% of the available gain at ~250–300 tokens | docs/panel/024 R4 and R5 · spec line 48 · docs/panel/023 R7 | every AST in the language is this choice, and the language exists to write compilers
- [ ] panel 024 | The other three costed gaps, none of which had been reported before: a map's **iteration order** (and whether it yields keys or pairs), the **sign of `%`** on negatives (`-7 % 3` is `-1` or `2`?), and **`slice` out of range** (clamp, abort, or `T?`). All three are silent when guessed wrong. Plus two the ergonomist reframed: **`T` → `T?` widening** is unspecified and used constantly, and **the spec never shows how to construct a variant case** | docs/panel/024 R5 | these are the whole silent-error surface a reader with only the spec could find
- [ ] panel 024 | **Part 11 would be measuring something nobody has measured** — the historian found no paper, benchmark or report varying a language specification's length in context against correctness of programs written in it. Decide whether that is an opportunity to state as one, and whether Aycock et al. (ICLR 2025: examples carried the signal, grammatical explanations did not) makes worked examples a Part 11 arm rather than a spec-budget question | docs/panel/024 § Watch list · arXiv 2409.19151 | if it holds for a programming language, the spec budget is measuring the wrong half of the document

- [ ] panel 025 | Ratify: the recursion rule promoted (**−6 removal, +47**, the first measured removal in eight amendments) · `/` and `%` stated (**+26**) · the slice sentence **held** on three teeth · **nothing** on map order · and the correction that panel 024 R4 was factually false | docs/panel/025-the-four-costed-gaps.md § Resolution | the spec is the deliverable, and this is the first time a panel repaired one of its own
- [ ] panel 025 | **A claim of mine was false and I repeated it to you unverified.** Panel 024's ergonomist said `args: [Expr]` and `lhs: Expr` both compile; I reported that. `lhs: Expr` is `no_size` at exit 1 with two notes and a fix. Task: run it, then say what the difference is between a gap that costs a *retry* and one that costs a *wrong answer* — and which of the two the project's instruments can see | crates/heroes/src/types/sized.rs · docs/panel/025 § The reversal | the whole ranking of what to spend spec tokens on depends on that distinction
- [ ] panel 025 | The gap no candidate closed, now costed at **+35** (not the ~15 the judge guessed): **the spec never says how a variant case is constructed.** Declaring is there, matching is there, building is not — and it hits every AST program, which is the language's reason to exist. Decide whether it lands, and whether it goes in the record-construction rule or the `variant` example | docs/panel/025 R5 · scratchpad/cand-d.md · spec line 78 | a reader can declare the type the language exists for and not build a value of it
- [ ] panel 025 | Two gaps both judges named and neither candidate touched, both loud: **`T` → `T?` widening** (unspecified, used constantly — `return 5` from `-> int?` is never sanctioned) and **`if`-as-an-expression's layout** (the spec says it is one and shows only the statement form; the judge routed around it with `@` cells in every program) | docs/panel/025 § Watch list · spec lines 116, 141 | they cost retries rather than wrong answers, which is exactly the rank this panel established

- [ ] M6 step 1 | `abort must` was lowered with an **empty argument list**, so the only message available was "a `.must()` failed" — the one thing the reader already knows. The failure now travels with it. Question: why does the read of the failure need no synthetic slot, when panel 021 had to route `assert`'s counted operands through one? (The answer is one word about *where* the read happens.) | crates/heroes/src/ir/fallible.rs (the `must` arm) · crates/heroes/src/ir/asserts.rs · docs/panel/021 | it is the same hazard in two constructs, and only one of them has it
- [ ] M6 step 1 | **A gate row that cannot fire, kept deliberately.** `Abort::Assert` is refused by a row no program can reach: an `assert` lives inside a `test`, ordinary builds skip test functions entirely, and `heroes test` does not exist. It is the `cow_check` shape, struck at M5b for exactly this. Decide: keep it because the step that lands `heroes test` needs it written, or strike it and re-add it then | crates/heroes/src/emit/gate.rs (the Abort arm) · DESIGN-LOG 2026-08-05 (cow_check) | a row nobody can make fire is a row nobody is checking

- [ ] panel 026 | Ratify the surface: **`m[k] @ v`** rather than `set(m, k, v)` (vetoed as core by §1.7's test) · **`keys(m)`** rather than `for k in m` (zero new IR ops, composes with `for`/`sort`) · the insertion-order guarantee **withdrawn as false** · `has` **struck** at −17 · net **+43** | docs/panel/026-the-map-funded.md § Resolution | the decision was yours; the surface it buys was not the one you were shown
- [ ] panel 026 | **I sold you the decision on an inverted reason.** I said a compiler needs incremental insertion for a symbol table. True, but `{K: V}` + insertion gives neither shadowing nor O(1) scope pop and the map has no delete — so it is the *wrong* structure for one, and `resolve/scope.rs` already chose a vector. Task: read `scope.rs` lines 41, 96, 107 and say what a map would have to gain to serve there | crates/heroes/src/resolve/scope.rs · docs/panel/026 R5 | the funding still stands on other grounds, but not on the one I gave
- [ ] panel 026 | Measured: `{"a":1,"b":2,"c":3}` iterates `b a c` at cap 8, and the same map with a duplicated first key iterates `b c a` at cap 16 — yet `hero_map_eq` says they are equal. Question: which line of `emit/aggregate.rs` puts the *source text's* duplicate count into the data structure, and what else does that leak? | crates/heroes/src/emit/aggregate.rs (build_map) · runtime/runtime.c (hero_map_new) · docs/panel/026 | a shipped guarantee was false for a reason nobody had looked at
- [ ] panel 026 | **The delta gate, now formally demanded** by the warden on the trigger it named: the spec grew **+73 in one cycle** against **+183 across the preceding eight** amendments. Proposed: any single amendment above +25 measured requires a named measured removal, no exceptions, enforced by `heroes measure` in CI against the previous commit. Decide whether it lands as architecture | docs/panel/026 § Watch list · docs/panel/024 R2 | §1.6's discipline was promised to survive the raise to 4096, and measured it did not

- [ ] M6 step 2 | **A defect I wrote and the leak counter's magic word caught.** `hero_map_grown` first did `memcpy` of the live entries plus `free` of the old block *without* dropping — the reasoning being that the references pass to the new block unchanged, so no descriptor should run. Every printed answer was correct and the program panicked at exit with `not a Heroes string block`. Task: say what that reasoning got right, and name the second place it put refcount arithmetic | runtime/runtime.c (hero_map_grown) · DESIGN-LOG 2026-08-11 | the fix is shorter than the bug and does no arithmetic at all
- [ ] M6 step 2 | `m[k] @ v` needed **zero new IR forms** — `ir/places.rs` already built the step. Count: how many files changed to make a map assignable, and how many would `set(m, k, v)` have needed? (The panel costed the second at three passes plus `Arg::InOut` on a builtin.) | docs/panel/026 R2 · crates/heroes/src/emit/aggregate.rs (the map arm of write_element) | the cheap spelling was already in the IR and the expensive one was in the proposal

- [ ] M6 spec repair | **The one document nothing in this project reads.** Symptom: a program written from the committed spec answered `error[unknown_name]: nothing named `has` is in scope`, exit 1, four days after `has` was struck. Task: say which of the three artifacts (spec, design.md, the compiler) was wrong under CLAUDE.md §12's precedence rule — and why the answer is not the one the rule's first sentence gives | spec line 128 · docs/panel/026 R7 · crates/heroes/src/resolve/tests/spec.rs | the removal measured −17 and shipped −4, and the missing 13 were a sentence in a different section
- [ ] M6 spec repair | Count: 23 built-ins, three spellings the spec uses for a name (`` `join` ``, `range(`, `` `.must()` ``), and one English word that must not count as a mention. Question: which built-in is named in the spec by *only* the third form, and what would a bare word-boundary search have wrongly reported struck? | crates/heroes/src/resolve/tests/spec.rs (`mentions`) | a test over prose is only as good as the form it matches, and the loose version passes while meaning nothing

- [ ] panel 027 | Ratify the seven resolutions: **S-desc vetoed** (a sixth `HeroDesc` field is `NULL` in seven descriptors, silently, under this project's own flags) · `sort` on `[int]`/`[f64]`/`[str]`, refused **by the gate** and not by the checker · `[f64]` by **IEEE 754 totalOrder** · array `slice` **aborts** · the UTF-8 error moved to **the slice that splits a character** · a **stable merge sort**, never `qsort` · spec **W7, +11** | docs/panel/027-the-five-builtins-that-reach-c.md § Resolution | it is the first panel where the deciding evidence was compiled rather than argued
- [ ] panel 027 | **Two seats vetoed the same thing and neither could see the other's reason.** One compiled it: C11 6.7.9p21 zero-fills a short initialiser list, so the field is `NULL` everywhere with no diagnostic, and the call is `SEGV` with no type name. The other read the tree: the order it installs is one `<` refuses to compute. Task: say which of the two arguments would still stand if the other were withdrawn — and which one panel 022 had already made | docs/panel/027 § Where they converged · docs/panel/022 (null `hash`) | the project's rule is that judges are differentiated by *input*, and this is what it buys when it works
- [ ] panel 027 | `_Static_assert(HERO_RUNTIME_ABI == N)` was believed to catch a stale runtime. Measured: it catches a **function** change (undefined symbol at link) and **not** a struct-field change — new header, old `runtime.o`, link exit 0, a read four bytes past a 40-byte object, silence. Question: what actually protects the build today, and what would have to be true for the `_Static_assert` to earn the sentence the header's comment gives it? | runtime/heroes_runtime.h:10-12 · crates/heroes-cli/src/commands/toolchain.rs (the cache key) | a guard credited with a job it cannot do is worse than no guard
- [ ] panel 027 | The order the port actually needs is **`sort_by_key` over `span.start`** at four sites (`types/mod.rs:197`, `resolve/mod.rs:195`, `ir/mod.rs:258`, `emit/gate.rs:108`), and **neither** branch of the proposal supplies it — a structural `cmp` would sort `Diagnostic` by *kind*. Task: write the Heroes signature that does serve those four sites, and say which M6 step has to land before it can be written | crates/heroes/src/diagnostics/mod.rs:71-83 · design.md §4.12 | the closure list has no comparator form, and this is the M6 audit's real question
- [ ] panel 027 | `to_int`'s range check, and why reasoning about it fails. Two checks a reviewer would sign off are wrong: `v <= (double)INT64_MAX` **accepts** 2^63 and `v > -9223372036854775809.0` **rejects** `INT64_MIN`. Question: what does `(double)INT64_MAX` round to, and why does the correct check use a hex float and a half-open interval? Then: on arm64 an unchecked cast does not trap — say what `inf` and `NaN` silently become | runtime/runtime.c (hero_f64_to_int) · docs/panel/027 R7 | 8 of 13 probe values are UB under the raw cast and the hardware hides all of them
- [ ] panel 027 | **A mandate the compiler never implemented, found by pricing a sentence.** design.md:809 says "Slicing that lands mid-sequence is an error"; `slice("caffè", from: 0, to: 5)` exits **0** with a corrupt byte. Task: say why the error belongs at the slice rather than at `chars`, and what law `chars` can assert over `heroes mutate`'s corpus once it is total | design.md:809 · docs/panel/027 R4 · runtime/runtime.c (hero_str_slice) | the ergonomist found the symptom, the warden found the mandate, and neither had the other's input

- [ ] M6 step 3 | **Two defects the map shipped with, and neither golden could see them.** `n = m` then `m["b"] @ 2` printed `2 2 2` where the spec requires `1 2 -1` — exit 0, ASan clean, leak counter zero. And `m["a"] @ "x" + "y"` leaked one block. Task: say why `run/maps.hero` passed through both, then say what one line in that file would have to change to catch each | tests/golden/run/fixedbugs-map-store-aliased.hero · fixedbugs-map-store-leaked.hero · runtime/runtime.c (hero_map_set) | the cheaper spelling of a container test — int values, never aliased — passes while testing neither rule
- [ ] M6 step 3 | The aliasing defect was found by **rewriting a stale comment**, not by a test: the header still said "READ-ONLY at M5d … there is no `hero_map_unshare` here", which was true one milestone after it stopped being safe. Question: which instrument in this project *should* have caught it, and why did none — given that panel 022 had already measured this exact failure for arrays | docs/panel/022 R2 · runtime/heroes_runtime.h (the map paragraph) | a comment that describes the past is a claim nobody re-checks
- [ ] M6 step 3 | The value is MOVED into a map and the key is COPIED, in the same call. Task: say which one `own.rs` increfed and why, then say what `hero_array_set`'s comment means by "the caller increfed before this call, which is what lets the value be something that lived inside the container the unshare just copied" | crates/heroes/src/own.rs (the indexed arm) · runtime/runtime.c (hero_map_set) | two arguments to one function with opposite ownership rules, and the asymmetry is the correct one
- [ ] M6 step 3 | **A row retiring rewrites its own tests, and that is the design.** Three unit tests used `sort` and `join` to demonstrate "not emitted"; both acquired entry points, so all three went red. Count: how many golden and unit cases changed in this step *because a refusal became a capability* rather than because behaviour changed? | crates/heroes/src/emit/tests/gate.rs · tests/golden/unsupported/three-capabilities.hero (rewritten a seventh time) | "a row nobody can make fire is a row nobody can retire" — the cost of that rule, paid
- [ ] M6 step 3 | `1e300` does not lex. The spec shows `1e-06` and `1e+23` as `print` OUTPUT, and a program cannot read back what `print` writes. Decide whether exponent notation enters the float literal (a lexer change, so a panel) or whether the spec should stop showing a form it cannot parse | crates/heroes/src/lexer/ · spec line 155 | found while writing an abort golden, by trying the most natural way to spell a large float
- [ ] M6 step 3 | `print(0.0 / 0.0)` prints `nan`, which has neither a point nor an exponent — and spec line 155 says an `f64` always has one. NaN appears nowhere in the spec or design.md, yet it is reachable, because "division by zero aborts" is implemented for integers only. Decide: does f64 division by zero abort too, or does the spec gain NaN? | runtime/runtime.c (hero_f64_render) · crates/heroes/src/emit/ops.rs (BinOp::Div) · spec lines 130, 155 | `sort` had to decide NaN's fate this step, and the language still has not
