# Decisions the compiler is waiting on

Read by **`/decide`**. Every item asks **what should be true**, and until it is
answered the compiler goes on behaving some way by default — which is the whole
difference from `LEARN.md`.

Two rules bind this file, both learned the hard way:

- **Verify before asking.** An entry is a claim from the day it was written, and
  entries outlive their causes: eight were found already settled in the first two
  sessions. Asking the author about a settled question is the one cost this list
  cannot pay.
- **Rank by what it blocks**, never by age, and say the blocker in the question.

Format: `- [ ] <origin> | <what> | <where to look> | <why it matters>`

**Split from `docs/debrief/QUEUE.md` on 2026-08-12, by author instruction.** One
file held 192 open items of three different kinds, and a list you have to filter
before you can read it is a list nobody opens. The closed items — the record —
stayed in `QUEUE.md`, because every commit subject in this repository cites that
path and CLAUDE.md §14 does not rewrite a record.

An answer is applied in the same session it is given — spec, design.md or
CLAUDE.md amendment, DESIGN-LOG line, the item ticked with the verdict written
into it. A decision recorded and not applied is the same open question with more
paperwork.

**The order is the ranking**: the headings are what each group blocks, and inside
a group the first item is the one the next milestone reads first.

**Sixteen items were answered on 2026-08-12 in one `/decide` session** and are in
`QUEUE.md` with their verdicts written into them. Before a single question was
put, nine items left for `LEARN.md` (seven milestone-close offers and two
questions the code had already answered) and one was ticked because the
diagnostic it asked for already existed and fired. Of the sixteen, four changed
the compiler, one changed the spec, four were ratifications, and one — the
`emit/decls.rs` split — is **back here**, because the ratified cut was made and
the file is still over the limit.

## Open — blocks M-selfhost-probe and the port

- [x] panel 022 — **ANSWERED 2026-08-12 (3d): deferred to M-selfhost-probe, and moved on to `SCHEDULED.md` with that milestone named.** Chosen over marking the sites now and over turning design.md's sentence into a tested claim, on the ground the item itself supplies: the marker's purpose is to tell the port where an explicit `sort` is owed, so it is worth exactly what M-selfhost-probe reads from it, and marking sites today against a compiler that is about to be rewritten in Heroes marks the wrong tree. **What the deferral costs, written here so it is not rediscovered as an oversight**: design.md:1233's port note keeps naming `types/holes.rs` as the bootstrap's one map iteration, and that is **false today** — the live site is `resolve/mod.rs:198`. A design.md sentence that misidentifies the file stays in the document until M-selfhost-probe, which is the same class as CLAUDE.md §11's expired premise, and the item now carries the correction so the probe does not re-derive it. Was | **`// ORDER:` is a rule with zero instances, and the sentence that states it also states its own zero.** design.md:1233 reads *"Every ordering-sensitive walk is still marked `// ORDER:` at the write site, and there are **zero** such marks in the tree today"* — so the document is honest, but the marker is a convention nothing follows and nothing tests. Two facts found while verifying it (2026-08-12): the tree still has **zero** `// ORDER:` marks, and the § 4.9 port note that names **`types/holes.rs`** as the bootstrap's one map iteration is **obsolete** — `holes.rs` today walks `resolved.locals`, `fields` and `cases`, all vectors. The live site is elsewhere and it is user-visible: `resolve/resolved.rs:147` `names_in` (was `resolve/mod.rs:198`) iterates the `BTreeMap top` (`resolved.rs:116`) straight into the *did-you-mean* candidate list and `--dump-scopes`, via `resolve/exprs.rs:297-312`. Decide which of the three §11 shapes this takes: mark the sites now, or turn design.md's sentence into a falsifiable claim with a test that fires when it dies, or withdraw the marker | design.md:1225-1236 · crates/heroes/src/resolve/resolved.rs:116,147 (was `resolve/mod.rs:167,198` until the §11 sweep moved the result types out on 2026-08-13) · crates/heroes/src/resolve/exprs.rs:297 | the note's own last line says it "becomes an explicit `sort` in the Heroes port, or the M-selfhost-fixpoint diff breaks", and the marker was the only thing that was going to say where

## Open — panel 049

- [x] panel 049, 2026-08-14 | **The platform axis is refused, and what you asked for does not unblock what you asked it for.** Five judges; one **veto** (spec-warden, Principle 0 — the payment does not exist), three objections, and the single approval came from the judge that was asked whether the *syntax reads*, not whether the feature works. Measured by four independent routes: raylib's dylib links on Darwin with `-I`/`-L` and **zero frameworks**, one judge **opened a window from `.hero` source** with the head line unchanged, and `heroes build` dies at `'raylib.h' file not found` — the header path, before the linker. **design.md:2058 said so a milestone ago** in ratified text. Landed instead at zero spec tokens: `ffi_missing_header`, exit 2 → exit 1 on the author's line. **What is yours, ranked by the measurements**: (1) **search paths** at +28 — but the design question is real and must not be settled fast, because an absolute path in a source file is *more* machine-dependent than a platform token; (2) the **pkg-config refusal** written with its falsifier, §12 — raylib ships a `.pc` and Go made it a first-class cgo verb, so it needs an answer rather than silence; (3) **struct-by-value at the FFI boundary**, which is **383 of raylib's 600 entry points** and the actual wall at rung 5; (4) `framework` untagged must be an error (Rust E0455); (5) `iconv` is the axis's one genuine witness and the real axis is the **libc**, not the OS; (6) `windows` struck until the runtime builds there and CI has a third leg | docs/panel/049 · design.md:2056-2061 · crates/heroes-cli/src/commands/toolchain.rs | the FFI is this language's whole library chapter, and the sitting convened to widen it found the widening pointed away from the wall. **Three of the six sub-items are overtaken and are struck rather than asked** (verified 2026-08-14): (2) the pkg-config refusal is moot — panel 050 **adopted** it; (4) `framework` untagged is moot — there is no `framework` keyword, the axis stayed refused; (6) `windows` is **done** — the runtime cross-compiles to PE32+ and `windows-latest` is the third CI leg. What survives is **(1) search paths** and **(3) struct-by-value**, and (3) has grown into the headline **ANSWERED 2026-08-14, and panel 055 inverted the question twice.** (1) **Search paths**: the answer is *no clause*, and the reason is that one **already existed** — `extern "/opt/foo/include/foo.h"` compiled and ran, inherited from Nim's `header` pragma. It is now refused, `machine_locked_path`, absolute forms only. The measured route is the machine's own `CPATH`/`LIBRARY_PATH`/`PKG_CONFIG_PATH`, all three live because `Command` inherits the environment; §10's composition test yields *nothing*. A clause was refused on four grounds, the sharpest being that it can **skew** — `-I` at v1 and `-L` at v2 compiles clean and prints the wrong number, while `package` asks one question of one `.pc` and cannot. **`--include`/`--library` on the CLI is queued, not refused**: one judge built it (+105/−6, zero spec tokens) and three answered that §10 says nothing; that split is the author's to settle. (3) **Struct-by-value** stays open and is the next milestone's question, not this one's.

## Open — panel 048

- [x] panel 048, 2026-08-14 | **Ratify what the sitting refused, and decide the one clause it queued.** Five judges, full panel, the first sitting in this repository to run all five briefs. **Both candidate clauses were refused** and the ffi-pragmatist showed why they had to be: X is **false on glibc** (`strptime`, `wcswidth`, `wcwidth`, `swab` are declared on Darwin, musl and FreeBSD and hidden on glibc under the ratified `gnu11`, because glibc's default is `_DEFAULT_SOURCE` and musl's `features.h` sets `_XOPEN_SOURCE 700` under the same flag), and Y's rule breaks on three rows it compiled (`-lrt` errors on Darwin; `-liconv` is required there and does not exist on glibc; `pthread` moved into libc at glibc 2.34). What landed instead: the spec's **already-false** sentence repaired at +9 as §1.0 compiler-need, and `ffi_missing_link` — a linker failure the author caused, now exit 1 on their own line, at **zero spec tokens**. **What is yours**: (a) ~~the yes on both~~ — **given 2026-08-14, ratified as it stands**; (b) whether X returns at the ffi-pragmatist's honest wording, *"A header shows more than ISO C's names… How much more is the platform's answer, not this language's"* (+42 measured, and the only version that is true 4/4); and (c) the blind reader's counter-proposal, which it said it would fund **ahead of either clause** — the **inbound `cstr`**. The spec documents one direction only, `s.cstr()`, and a reader following it exactly cannot write `getenv`. Verified after the sitting: `to_str` on an inbound `cstr` **works** and `ptr == nullptr` **works**, and the spec's `==` list omits `ptr`. The language is wider than its specification in two places at the FFI boundary | docs/panel/048 · spec/heroes-spec.md § FFI · crates/heroes/src/emit/ffi.rs | the founding constraint is that everything comes from C, and the section describing that boundary is the one place the spec is narrower than the language **ANSWERED 2026-08-14, all three.** (a) ratified. (b) **X returns**, at the ffi-pragmatist's wording — *"A header shows more than ISO C's names… How much more is the platform's answer, not this language's"* — at +45 against panel 052's R2 removal of −12, net +33, ledger row `3191`. (c) the **inbound `cstr`** went to panel 053 and landed as `nullptr` taking its context's type plus a call-site null guard, and the sitting's warden found the third thing nobody had asked about: `==` on a `cstr` compares **addresses** while the spec said *"structural equality on everything"*, repaired at +17.

- [x] panel 048, ffi-pragmatist, 2026-08-14 | **§4.19's ladder rung 5 is unbindable on Darwin today, and it is `link`'s shape that stops it.** `toolchain.rs` builds `format!("-l{library}")` by design, so no `.hero` can hand clang an arbitrary flag — and macOS frameworks cannot be spelled that way at all. raylib (design.md:2056) needs `-framework OpenGL -framework Cocoa -framework IOKit` on Darwin against `-lGL -lX11 -lm -lpthread -ldl -lrt` on Linux, measured 4/4 on the framework half. So the rung the ROADMAP schedules cannot be reached without deciding what `link` may spell — and the same question governs whether one `.hero` file can bind one library on both platforms at all. Not urgent, and it should be decided **before** rung 5 is attempted rather than during it | design.md:2056 · crates/heroes-cli/src/commands/toolchain.rs · docs/panel/048 | a ladder rung that cannot be climbed on the developer's own machine is a rung that will be discovered at the worst moment **CLOSED 2026-08-14 by panel 050.** `package "<name>"` is in the spec at line 202 and `examples/raylib/` opens a window: `pkg-config` answers with `-framework` where that is the answer and `-lGL -lX11 -lm -ldl` where that is, from one spelling, and it says `-I` and `-L`, which four measurements had found to be the actual blocker rather than the frameworks. The question this item asked — *what may `link` spell* — was answered by not extending `link` at all.

## Open — found by the CI's first run

- [x] **RATIFIED 2026-08-14 as it stands** — `-std=gnu11`, all four conditions, both refused options refused; the provisional default was already green on two platforms when the yes arrived, and panel 047's own falsified prediction stays in the file rather than being tidied away. What remains open is the second half, now panel 048: whether the *spec* should say which C the FFI reaches. Was | **Ratify the C dialect: the build is `-std=gnu11` and the provisional default is already landed.** Three judges, no veto — the ffi-pragmatist holds it on ABI breakage, cross-compiled the real artifacts against real glibc/musl/FreeBSD headers, and reported *"I came to break this and could not"*: identical LLVM IR, 16 struct layouts unchanged, 63/63 `run/` goldens compiling for Linux under `gnu11` against 62/63 under `c11`. The two dialects differ by **one predefined macro**, `__STRICT_ANSI__`, measured three times independently. What the emitter writes is unchanged. Refused: feature-test macros in the generated C (127 hand-edited golden lines where `UPDATE_GOLDEN` is forbidden, the same rule in two translation units, and `_POSIX_C_SOURCE` measurably *hides* five identifiers on Darwin), and passing no `-std=` at all — which is what 3 of 3 sourceable C-emitting compilers do, declined because a byte-identical fixpoint cannot inherit clang's drifting default. **What is yours**: the yes, and one question the lane did not ask — whether the *spec* should say which C the FFI reaches. It says *"anything beyond this document comes from C libraries"*, which after this change is true on both platforms and was true on one; making it say *which* C is a different sitting with an llm-ergonomist in it | docs/panel/047 · crates/heroes-cli/src/commands/toolchain.rs:72 · CLAUDE.md §7 | the language's founding constraint is that everything comes from C, and until today half of what a program binds was invisible on half the platforms

- [x] CI run 4, 2026-08-13 | **Does the FFI see the platform's headers as a POSIX system sees them, or strictly as C11? The two platforms disagree, and today the language's own promise is what breaks.** `tests/golden/run/ffi-constant.hero` binds `constant M_PI: f64` from `math.h`. On Darwin it compiles; on Linux the compiler says, correctly, `error[ffi_unknown_name]: `math.h` declares no `M_PI` — clang read the header and could not find it`. Neither side is wrong: **`M_PI` is not in C11** — it is POSIX/XSI — and glibc hides it under `-std=c11`, which defines `__STRICT_ANSI__`, while Darwin's `math.h` defines it unconditionally. CLAUDE.md §7 says the generated C is C11, and §4.19's whole promise is *"clang checks every signature and constant against that header, so a wrong FFI type is a compile error"*. Under strict C11 on glibc, a large part of what anybody actually binds — `M_PI`, `strdup`, `fileno`, `strtod_l` — is simply **invisible**, so the same `.hero` file compiles on one platform and not the other. That is the failure §4.19 exists to prevent, arriving through the flags rather than through the types. Three answers, and it is an architecture question (CLAUDE.md §4 — a panel path, and the panel does not block): (a) compile generated C as `-std=gnu11`, which is what every real C build does and what the headers are written for, at the cost of §7's sentence; (b) keep `-std=c11` and have the emitter define `_DEFAULT_SOURCE`, which is narrower and still a deliberate departure from the standard the project names; (c) keep strict C11 and accept that the FFI can only reach ISO C, which makes §1.11's *"everything comes from C libraries"* false in a way the SQLite acceptance test happens not to notice. **Meanwhile the golden is red on Linux and green on Darwin, and that is left visible rather than papered over** | tests/golden/run/ffi-constant.hero · crates/heroes-cli/src/commands/toolchain.rs (FLAGS) · CLAUDE.md §7 · design.md §4.19, §1.11 | the one promise the C backend exists to keep is the one that does not survive a second platform **CLOSED 2026-08-14 by panel 047, ratified.** Answer (a): `toolchain.rs` names `-std=gnu11`, and the ratification's content was that the dialect is *named* rather than which one won. `gnu11` over `c11` followed from the measurement — one predefined macro, `__STRICT_ANSI__`, identical IR. Two more platforms have since reproduced the class from their own direction: MSVC hides the same constants behind `_USE_MATH_DEFINES`, which is now named in `FLAGS` beside it.

- [x] CI runs 3 and 4, 2026-08-13 | **`tests/golden/run/abort-slice-splits-a-character.hero` failed once on Linux at `-O2` and passed on the next run with nothing relevant changed.** Its stdout was empty where `caff\n` was expected — a program that prints, then aborts, having printed nothing. `hero_panic` calls `fflush(stdout)` before `abort()` and `heroes run` inherits stdout rather than capturing it, so neither of the two obvious explanations fits. It has not reproduced since, and the harness now reports the exit status and stderr on a mismatch, so the next occurrence will say more than this one could. Decide whether to chase it now or wait for it: a flake in the abort path is the class this language cannot afford to have — the whole of §4.14 is that a program which must stop, stops visibly | tests/golden/run/abort-slice-splits-a-character.hero · runtime/parts/panic.c · crates/heroes-cli/tests/expectation/mod.rs | an intermittent failure in the mechanism that makes aborts observable, seen once and not since **ANSWERED 2026-08-14 (`b` — chased, and it has a cause).** Three things were checked. (1) **Every abort path flushes**: `panic.c`, `failure.c` ×3 and `alloc.c` all `fflush(stdout)` before writing to stderr and calling `abort()`, so the buffering hypothesis is out. (2) **Not reproducible here**: 1000 runs of the `-O2` binary on Darwin — 400 through a pipe, 300 through a file, 300 across eight parallel workers — gave an empty stdout **zero** times. (3) **The timestamps explain it.** Compile-beside-and-rename for the *binary* landed in `5ecbc98` at **08-13 23:32**; the seven CI runs of 2026-08-13 all ran between **22:54 and 23:28**, so runs 3 and 4 executed on a tree that still wrote the binary **in place** — while `cargo test` runs its binaries in parallel. One process truncating a binary another is about to exec produces a program that starts and prints nothing, which is the symptom exactly, and the `-O2` leg has its own cache path and so its own race window. It has not recurred in any run since 23:32, which is consistent. **Not marked as a fixed defect with a case of its own**, because a test for two processes racing on one path is not deterministic and the fix it would pin is already asserted by the `publish` helper's own doc — but if it recurs after 23:32 this diagnosis is wrong and the item reopens with that fact.

- [x] CI run 1, 2026-08-13 | **The Cyclone rule's own enforcement now fires on `vec!`, and the toolchain is pinned while you decide which way to fix it.** The first CI run ever to compare two machines found clippy **1.97** rejecting **forty** lines that clippy **1.90** accepts, on both platforms identically — so it is the toolchain and not the platform. Every one is `vec![…]`, every message is *use of a disallowed type `std::boxed::Box`*, and this project has **zero** `Box` in its own Rust: `vec!` expands to `Box::new([…])` inside the standard library and the newer lint reads into that expansion. The rule `clippy.toml` states is *"Heroes' only indirection is the array — use Vec or indices"*, so the lint is now refusing the macro that builds the sanctioned indirection. **Landed meanwhile**: `rust-toolchain.toml` pins 1.90.0, written once and honoured by `rustup` for local and CI alike, which is a fix a compiler with a byte-identical fixpoint as its acceptance criterion wanted anyway. **What is open is which way it comes off the pin**: (a) exempt `vec!` — but `disallowed-types` has no macro knob, so this means `#[allow]` at forty sites or dropping `Box` from the list, and dropping it stops catching a real `Box` the day somebody writes one; (b) stay pinned and move the version deliberately, which is defensible for this project and makes the pin a thing somebody must remember; (c) check first whether 1.97's reach is a clippy **regression** — most lints skip external macro expansions — in which case it is reported upstream and (b) holds until it is fixed | clippy.toml · rust-toolchain.toml · `.github/workflows/ci.yml` · CLAUDE.md §5, §10 | the tool that enforces the Cyclone rule is now wrong about the Cyclone rule, and a pinned toolchain is a decision nobody took **ANSWERED 2026-08-14 (`c`, then `a`).** The upstream search was run and found **no report of this class**, but it found the reason it may not be one: current `alloc` builds `vec!` through `Box` on purpose (`write_box_via_move`, to keep unoptimised builds off the stack), so the lint is reading a real `Box` rather than misreading a macro. That makes it **the project's rule that is too broad**, not clippy that is wrong — `disallowed-types` is meant to catch the *author's* `Box`, and it cannot tell one from `alloc`'s. So (b) is not available cheaply and (c) returns nothing: the pin stays, and the version moves deliberately. What is left open, and is smaller: whether `Box` should come off `disallowed-types` altogether, since it is now catching a thing the Cyclone rule sanctions and cannot catch the thing it was written for without also catching that.

## Open — the four clauses whose predictions lapsed (panel 046 R2, M-program-corpus)

- [x] panel 046 R2, scored 2026-08-13 | **Four spec clauses are now unfunded, and the decision is one decision taken four times: keep each at its price, or take the removal.** `2588` (+28, a group's `constant`), `2745` (+70, the four bases and `_`), `2768` (+23, `int` deleted for `i64`), `2959` (+191, the eight widths) were all bought with a **registered prediction naming metric 2** — first-try rates, first-try failures, the first metric-2 harness run — and metric 2 waits for M-selfhost-fixpoint by the author's own decision of 2026-08-12. They are marked `lapsed` in `measure/gate.rs` and **not renewed with a new milestone name**, which is the one thing R2 forbids. Nothing has left the spec: R1 governs what may be *offered* as payment and is not retroactive. What is owed is the re-argument under the removal branch, per clause, and the honest starting position is that **three of the four are §1.0 compiler-need** — the widths, `i64` and the bases are all on the road to the port — so the likely answer is *keep, unfunded, and say so*. The one that is not is `2588`. **312 tokens are involved**, which is a quarter of the headroom | crates/heroes/src/measure/gate.rs (the four rows) · docs/panel/038, 041, 042 · design.md §1.6 | four clauses are in the spec on a promise nobody can collect, and pretending otherwise is what panel 046 was convened to stop **ANSWERED 2026-08-14 (`c`, applied as R2 permits it to be).** Not renewed — **re-registered**, and the distinction is the one R2 exists to draw: a renewal differs only in a date or a milestone name, and each of these differs in what is claimed and in what will answer it. All four now name an instrument that exists **and has an arm** (R1 as amended the same day), and all four are countable with `grep` or `heroes check` rather than the metric-2 harness. Three are kept as §1.0 compiler-need with a falsifier attached; `2588`, the one that is not, has the prediction written to be able to fail. The table is in `measure/gate.rs` § The four, re-decided. `2959`'s is the strongest: its original prediction named metric 2 because nothing else could see the widths, and `ffi_parameter_type` — built at M-binding-fidelity, *because* of them — now can.

- [x] `docs/measurements/007`, 2026-08-13 | **A reader told a loop is quadratic, and given no non-loop, does the loop.** Panel 037's +39 bought a spec sentence naming both `+`'s copy and `push`'s. A blind reader avoided `+` *"for one reason — that sentence … is the only sentence in the document with a cost claim in it, so it is loud"* and then wrote `push`-in-a-loop in two programs of three, noticing only afterwards: *"the warning redirected me from one quadratic accumulation to the other."* The cause is not the wording. It is that **`map` cannot capture a local** (no lambda, no partial application) and there is **no `repeat`**, so the two programs that needed a captured separator and N copies of one character had no other route — while the third, whose transform captures nothing, is the one line with no accumulator in it. Decide which of the three this is: a Part 7 lambda question, a `repeat` built-in (one name, no new type, and §1.0 will want it for the port's own error rendering), or a spec that stops claiming a cost it leaves no way to avoid | docs/measurements/007 · docs/panel/037 § Ratification · spec § Strings, arrays, maps | the clause's first half held under a real reader and its second half is falsified, and the falsifier is a language gap rather than a sentence **ANSWERED 2026-08-14 (`a`), and panel 054 settled the shape.** `repeat(s: str, n: u64)` lands at +14 against a −5 removal the sitting found, net +9; the runtime primitive is 13 lines. The count is a **`u64`** because design.md §1.12 chose among three answers to the negative case — a defensive check must **surface** a defect, not hide it — so a negative count does not compile and a computed one goes through `to_u64().must()`, which aborts at the subtraction. The multiply is guarded by a division: `4 * (2^62+2)` wraps *positive* to 8 and was **exit 138, SIGBUS, silent** before the guard, the class Rust shipped as CVE-2018-1000810. And the clause itself is repaired rather than deleted, on the rule the sitting adopted: a cost claim is admissible only where the same paragraph names a construct the reader can reach for. **`repeat` is recorded as *not* compiler-need** — the port's own renderer builds its padding as a tab-preserving per-character map, not `repeat(" ", col)`. The lambda half stays refused (CLAUDE.md §13, Part 7).

- [x] `docs/measurements/007`, 2026-08-13 | **`heroes mutate` has no operator for the mistake panel 040 called "the falsifier for this whole change".** Prediction 2 of `2675` asks that no survivor list gain an entry where a bitwise operator was misread as its boolean twin (`&` for `&&`, `|` for `||`). It is scored **held** — 892 survivor lines over 5798 mutants, none of them that — and it holds **vacuously**: none of the twelve operators makes that substitution. The prediction named an instrument that exists, which is what R1 asks; what R1 does not ask is that the instrument have an arm for the question. Decide whether a `boolean-twin` operator is added (it is the same shape as `mix-int-float`, which catches 100%, and the corpus has carried bitwise code since M-program-corpus), and whether R1 gains the second half — *and the instrument can produce the observation* | docs/measurements/007 · docs/panel/040 § Predictions to score · crates/heroes/src/mutate/operators.rs | a prediction that cannot be falsified by the instrument it names is the thing panel 046 was convened about, wearing the other face **ANSWERED 2026-08-14 (`a`).** `boolean-twin` is the thirteenth operator: 132 mutants over `examples/`, 132 killed, and panel 040's prediction 2 is re-scored **held and measured** rather than held and vacuous. R1 gained its second half in the same decision — *and the instrument can produce the observation*.

## Open

- [x] **RATIFIED 2026-08-13 (a) — R1–R5 as they stand, (b) refused at its stated price.** The yes changed no clause and settled the three things the provisional default had left implicit (`docs/panel/046` § Ratification). **Not retroactive**: R1 is a rule about what may be offered as payment on the day of registration, so none of the six rows is un-bought and no clause leaves the spec. **What R2 reaches is the looking**: each of the six is *re-decided* at **M-program-corpus** — scored, or `lapsed` with its clause re-argued under the removal branch — and that date is written **once**, in `measure/gate.rs`'s module doc beside the rows, because six copies of one date is six places for it to rot. The milestone is deliberately **not** M-selfhost-fixpoint even though five of the six named metric 2: the spec-warden's condition on its own yes was that the expiry not be the instrument's arrival date, *"otherwise the expiry is the tenancy"*. **The sixth row is the finding**: `2675` named *"the next FFI rung"*, which closed 2026-08-12 — three days after the row was written, unlooked-at — the compiler-engineer's evidence happening a third time here, and the reason the date now sits beside the rows rather than in a panel file nobody greps. The warden's veto stays live and unmechanised: it fires if the first three `SPEC_TOKENS` rows after today name an instrument in name only, and (b) returns as the fallback at R3's freeze cost. Was | **ANSWERED by `docs/panel/046` 2026-08-13 — provisional, ratification pending. The panel's default is (c) plus (a) rebuilt, and (b) refused.** A registered prediction pays only if it names the instrument that will score it **and that instrument exists on the day of registration**; an unscorable one is still registered as an observation and pays nothing. An outstanding prediction is **re-decided, never renewed**: scored at the milestone it names, or marked `lapsed` in the ledger row that spent it with the clause re-argued under the removal branch — nothing is ever deleted from the record. Landed: design.md §1.6 amended, `/step`'s close checklist gains the scoring bullet, **zero compiler lines**. **Three things the sitting corrected in this item's own text**: the delta gate is `measure/gate.rs` at **121 lines**, not the 372-line `emit/gate.rs`; the gate **reads no commit body at all** (no git call, no CI, no hook), so today none of the three options is enforceable by anything but discipline; and *26* counts judges' predictions across sittings, where the number that carries the ruling is **6 of the 14 ledger rows bought with a prediction, none ever collected**. **What the author still owns**: whether to take (b) instead — the cost of it is the spec frozen at 2974 until M-selfhost-fixpoint, §1.0 items included. Two expired premises repaired alongside, neither restated as a number: design.md:314 and :289 (the real mean is **+57.2**, so the ceiling binds in **~20** amendments, not 61) | docs/panel/046 · design.md §1.6 · crates/heroes/src/measure/gate.rs | the project's own objective has never been measured, and the rule that was supposed to force it is the one branch that costs nothing. Was | **Panel 012's two branches are not equal, and one of them is free.** The named-removal branch has paid **three times in eleven amendments** (panels 035, 036, 042) — it works. The registered-prediction branch has **26 rows across panels 031–042 parked on a harness that has never run**, and `docs/measurements/` holds five files of which **none is metric-2**. §1.1 makes comprehension *the objective* and operationalises it as first-try compile rate; there is **not one measurement of it in this repository**. Panel 041 had to write *"NOT SCORABLE, and the reason is the instrument"* twice in one table, and panel 037's two predictions protected a spec paragraph for a month without ever being testable. Registering costs nothing and nothing comes back. **Decide the expiry**: an unscored prediction protects its clause until a **named milestone**, after which it stops protecting it — or the branch is closed to new registrations until one metric-2 run exists. Without a date it is a permanent tenancy, which is what happened here | docs/panel/043 § The finding that outlives both questions · crates/heroes/src/measure/gate.rs (every row since 2588 cites this branch) · docs/measurements/ | the project's own objective has never been measured, and the rule that was supposed to force it is the one branch that costs nothing

- [x] **RATIFIED 2026-08-13: the uniform `T?` is restored and step 7's bend is reverted** (*"fai la soluzione più solida e sicura"*). One rule instead of two, no invention — the historian found the shipped encoding search-negative across five languages while Zig's `math.cast`, `fit`'s exact shape, returns `?T` unconditionally — and taken at the cheap moment: 8 sites today against ≥25 at M-selfhost-probe, which scores the compiler-engineer's prediction 4 inside its own "still cheaply reversible" branch. The cost is `.must()` back on the calculator's lexer, written into the code rather than hidden. The revert also made `emit/ops.rs`'s empty-test branch reachable again — it had been `hero_unreachable()`, correct under the bent rule — and a golden caught it in a minute. Was | **RATIFICATION STILL PENDING, and panel 043 sharpened it rather than closing it** (2026-08-12). The implementation survived a real attack — all 64 pairs generated from an *independent reimplementation*, zero errors; C's usual-arithmetic conversions shown structurally unable to bite; reverting measured at **9 lines and 8 sites**. **The proposal's central claim was wrong and the compiler-engineer corrected it**: panel 042's Q2 chose among *names* and **never ruled on a widening**, so this bends the **author's** ruling and nobody else's. Its load-bearing condition is now met — a `certain` fix removing a `.must()` that cannot fail, and a `guess` adding one where a `T?` met a `T` — which is what turns the extra locality hop into a machine-applied repair instead of a round-trip. **And the historian found the option that was never on your menu**, the only one with a shipped precedent (Rust's): two names, both always available — `fit_i64(aU8) -> i64` a *compile error* on a lossy pair with a certain fix naming the other form, and `try_fit_u8(anI64) -> u8?` for every pair. It kills the lexer's `.must()` too, keeps the return type a function of the **name**, and costs eight more names. Heroes' rule itself is D's `to!T` and Rust's `From`/`TryFrom` partition; only the *encoding* — one name, two shapes — was search-negative across five languages. So the choice is: keep what shipped, or take the third option. Was | **A widening conversion returns `T`, not `T?` — which bends your ruling 3, and the reason is measured rather than argued.** You ruled that conversions return `T?`; applied uniformly, `s[i] -> u8` put this on the calculator's lexer: `v @ v * 10 + fit_i64(l.here() - '0').must()` — a `.must()` on a conversion that **cannot fail**, at the hottest line of the shape the closure list is made of, and `is_digit`/`digit_of` each carried one too. So `fit_<w>` now returns `T` where `IntKind::contains` says the source width fits the target and `T?` where it does not. **What this does NOT bend is panel 042's Q2**, which refused one name with two result types on the ground of *unpredictability*: this is a function of the argument's width, the compiler knows it, and it is fallible exactly when it can fail — which is also what the llm-ergonomist asked for and was overruled on, for uniformity, three steps before the measurement went its way. Reverting is one `if` in `types/builtins.rs` plus one early return in `emit/ops.rs`, and it costs a `.must()` at every byte-to-number site in every lexer this language will ever compile | crates/heroes/src/types/builtins.rs (the `fit_` arm) · crates/heroes/src/types/table.rs (`IntKind::contains`) · examples/calculator/lex.hero | the closure list contains a lexer, so the cost of getting this wrong is paid on the road to v1 rather than in a corner

- [x] **CLOSED 2026-08-12 by `docs/panel/043`: the paragraph stays, restated, and the spec-warden withdrew its own proposal on the merits.** Part 2:497 governs *choosing*, not *describing*, and design.md §4.10:1327 already pairs both in one sentence (*"Price, declared: mutating a shared array copies it, O(n)"*) — so the spec **may** name a price. Three judges converged on the same cut from three unshared routes: keep the **cost property**, delete the **imperative**. And a claim in this very item was wrong: *"their prediction is still unscored"* — two of panel 037's five are already observed, and the two unscored are the llm-ergonomist's, which this sitting tested blind. The array half turned out to be worse than stray: `[i64] + [i64]` is `bad_operand`, so *"build a long array in chunks"* **advised the impossible** for a month. Net **+7**, and the rule it establishes is now CLAUDE.md §13's second sentence. Was | **The spec-warden's −39 removal is refused for now, and the reason is a fact it could not see.** Convening panel 041, its spec-warden named spec lines 152–153 — *"Build a long string with `join`, not repeated `+`… each `+` copies both sides and each `push` copies the array"* — as the one paragraph in 191 lines that is neither syntax, semantics nor built-in library, therefore stray performance advice under CLAUDE.md §13, therefore **−39 measured** and the funding for the notation clause. It is not stray. Those two lines are `measure::gate`'s **2627** row: panel 037 added them on the llm-ergonomist's ask, `+39` exactly, and **their registered prediction is still unscored** — deleting them would have spent one sitting's live measurement to fund another's addition, and neither panel would have known. The judge was reading the spec, which is its brief and is why it could not see the ledger. Two things follow and only the author can pick. First, whether the removal is refused permanently, or taken **after** panel 037's prediction is scored at M-program-corpus, when the paragraph has either earned its keep or has not. Second, whether §13's amendment (*"le prestazioni… non devono essere nemmeno un limite"*) means the spec may carry cost advice at all — because if it may, the row is safe and this question closes; if it may not, then panel 037's addition was wrong when it was made and the −39 is owed regardless of what the prediction says | spec/heroes-spec.md:152-153 · crates/heroes/src/measure/gate.rs (the 2627 and 2745 rows) · docs/panel/037 § Ratification · docs/panel/041 | the spec budget is the project's one indicator, and funding an addition by deleting another panel's unscored deliverable would corrupt it in the direction nobody audits

- [x] found while verifying the unsigned item — **ANSWERED 2026-08-12 (1a): it rides in the unsigned panel's sitting.** So it is not a separate panel and not a wait: the sitting owed below now has **two** questions in its charge, and its brief must carry both — the type vocabulary (`u64`, `size_t`) and the literal notation (`0x`), with the second costing one spec clause and no new type. The reason the pairing is right is the reason they were nearly filed apart: a judge asked only about unsigned would reach for `0xFFFF…` in its own example programme and discover the notation gap inside the sitting, which is how panels 031 and 023 both found their best evidence. One consequence to state plainly: **1a makes the cheap question wait for the expensive one**, and if the unsigned panel is not convened for weeks, a mask stays decimal for weeks. Was | **Heroes has no hexadecimal literal, and the bitwise set just made that visible.** Measured, not assumed: `x = 0xFFFFFFFFFFFFFFFF` fails as `expected_end_of_line` on `xFFFFFFFFFFFFFFFF` (a *name*), the lexer contains no hex scanning, and the spec's 191 lines never write `0x`. Panel 040 landed `& | ^ ~ << >>` on the argument that a flag union spelled `+` is a silent wrong answer — and a mask must now be written in decimal, which is the notation every C header does **not** use, so a reader comparing `curl.h` to a `.hero` file converts by hand. This is a **separate and much cheaper** question than sized integers (a literal notation, no new type, no conversion rules — spec cost is one clause), and it is only worth asking now because the operators exist. Decide whether it rides in the unsigned panel's sitting, gets its own, or waits | crates/heroes/src/lexer/scan.rs · spec § Types · docs/panel/040 | a language whose bitwise operators cannot read the constants they exist for has the operators and not the use case


## Closed by panel 039


- [x] reasoning 003 — ANSWERED, see `docs/panel/039-comptime-and-part-6.md` and QUEUE.md. Was | **Does Part 6's `Macros` row cover compile-time *evaluation*?** The row's reason — *"the code you read is not the code that runs"* — reads as covering it, and Part 6's preamble gives that reading force, but `comptime`/`const-eval`/`constexpr`/`CTFE` appear in the whole record exactly **once**, at design.md:1821, and there as a remark about Zig getting an answer right by accident (re-verified 2026-08-12: still exactly one occurrence; the row is design.md:2124). A permanent-rejection list that names one spelling of a mechanism and is silent on the other is how a refusal gets rediscovered as an idea. Part 6 is design.md Parts 1–11, so amending it is a **panel** path (CLAUDE.md §4) — this item decides only whether that panel is convened and when. **It must be numbered 039**: `docs/panel/` now ends at 038, and the skill's "next NNN" is right for once, but the number is worth stating because the last sitting was numbered from a worktree | design.md:2114-2132 · design.md:1821 · docs/reasoning/003-comptime-and-macros.md § What stayed open | the cheapest possible outcome is one Part 6 row at zero spec tokens, and the expensive one is re-litigating this every time a new language ships comptime


## Open — the §11 sweep, running

- [x] **CLOSED 2026-08-13 — zero non-test files over 300.** | author decision 2026-08-12 (2a) | **The standing §11 sweep: 22 non-test files over ~300 lines, taken to zero, one commit per file.** The list at the decision, largest first — `emit/perfn.rs` 628 · `emit/aggregate.rs` 511 · `printer/fmt_stmt.rs` 457 · `emit/inst.rs` 445 · `emit/ops.rs` 433 · `printer/fmt.rs` 417 · `resolve/mod.rs` 393 · `ir/mono.rs` 393 · `types/calls.rs` 378 · `emit/gate.rs` 372 · `lexer/scan.rs` 360 · `mutate/edits.rs` 342 · `source/mod.rs` 340 · `emit/ctype.rs` 338 · `types/stmts.rs` 323 · `heroes-cli/src/commands/compile.rs` 317 · `resolve/exprs.rs` 313 · `types/sized.rs` 312 · `own.rs` 309 · `syntax/stmt.rs` 306 · `types/mod.rs` 303 · `ir/inst.rs` 303. Two rules the two earlier splits earned: **a cut is along a seam that names a concern**, never at a line count — `emit/decls.rs`'s residual turned out to be three concerns where the item had guessed two, and a guessed single cut would have landed at ~320 and stayed in breach — and **the emitted C is byte-identical**, which is what makes "mechanical" a measurement rather than a claim. **Landed so far**: `emit/perfn.rs` **628 → 223**, along four seams — `counted.rs` 182 (the ownership question and the two reference walks it guards), `structural.rs` 211 (`eq` and `hash`, together because they are one invariant), and the descriptor emitter moved into `descriptors.rs` 175 → 258, which already owned every other question about descriptors. Emitted C byte-identical over 60 `run/` goldens, 529 tests green, clippy clean. **2 of 22**: `emit/aggregate.rs` **511 → 240**, cut at the file's own section separators — `container.rs` 251 (`[T]` and `{K: V}`, one mechanism under two names: a runtime pointer, every element crossing by address through its descriptor) and `fallible.rs` 71 (`T?`, the one aggregate whose members are *alternatives*). C byte-identical again, 529 green, clippy clean. **3 of 22**: `printer/fmt_stmt.rs` **457 → 122**, into four — `fmt_break.rs` 180 (the too-long line, panel 007's no-continuation rule), `fmt_arms.rs` 108 (one alignment column per *run* of arms), `fmt_fits.rs` 104 (the three extent measurements all three printers ask). C byte-identical, `fmt` clean over 60 goldens plus `examples/`, 529 green. **4 of 23**: `emit/ops.rs` **593 → 173** into three named concerns — `literal.rs` 77 (a constant as C text; nothing here aborts, branches or allocates), `operator.rs` 263 (one operator and the guard that keeps it out of UB, the aggregate comparisons with it because they answer the same question by a call), and `convert.rs` 134 (`to_<width>`, the one built-in that builds its own `T?` inline). A doubled `name`/`to`/union computation, shadowed and identical, came out with it. C byte-identical over 62 goldens, 533 green, clippy clean. **The list is 23, not 22** (re-measured 2026-08-13, `modules/tests.rs` 583 excluded as a test file): M-sized-integers pushed `emit/ops.rs` 433→593 and `emit/inst.rs` 445→475, the `decls.rs` split left `emit/externs.rs` at 306, and four files entered the list unremarked — `types/table.rs` 380, `types/builtins.rs` 322, `types/exprs.rs` 301, `mutate/edits.rs` 342→367. `lexer/scan.rs` left it (360→304 is still a breach, so it did not). **5 of 23**: `emit/inst.rs` **475 → 293**, and the constraint held — the exhaustive `match` over `Op` still has every arm, no `_ =>` was introduced, and only arm BODIES moved, which is what the item recorded in advance. Three concerns, and they are the three the item named: `abort.rs` 109 (what the author sees when the program stops — everything here runs after the failure, so a wrong entry point is a wrong message at exit 1 with nothing to catch it), `construct.rs` 122 (a composite value coming into existence: one question asked five times, and the five answers divide into compound literals and statement sequences, which is why two of the five return lines where three return text), `access.rs` 126 (reading one part of a composite — five ops united by being a read that cannot allocate). C byte-identical, 533 green, clippy clean. **6 of 23**: `printer/fmt.rs` **417 → 190**, into three — `fmt_extern.rs` 93 (putting a flattened `extern` group back together: a run of consecutive members sharing a head line, the only rule that survives `fmt(fmt(x)) == fmt(x)`), `fmt_comments.rs` 70 (the formatter's most dangerous concern — a mistake here produces *missing* output, which the round-trip test cannot see), `fmt_decl.rs` 123 (what one declaration looks like once it is its turn). A doc comment that had come adrift is repaired: `signature`'s two paragraphs were sitting on `extern_head`, describing the wrong function. `fmt` idempotent over 81 files, 533 green, clippy clean. **7 of 23**: `resolve/mod.rs` **393 → 220**, one cut along the pass's own standing rule — `resolved.rs` 194 is what the pass *leaves behind*, the table every later pass reads and none recomputes, and `mod.rs` is how the answer is *computed*. Nothing in the new file walks a tree; everything in it is `pub`. **The move invalidated three citations and they were repaired in the same commit**, which is the point: `resolve/mod.rs:167,198` is the `// ORDER:` item's own address for the live map walk, and leaving it would have reproduced exactly the defect that item exists to record — a note naming the wrong file. C byte-identical, 533 green, clippy clean. **8 of 23**: `ir/mono.rs` **393 → 198**, into the three questions the pass actually answers — which instances exist (`mono.rs`), what one instance *is* (`mono_subst.rs` 117), and why the walk terminates (`mono_recursion.rs` 103, which is the half with a literature behind it: MLton total for 25 years because SML bans polymorphic recursion, Rust accepting it and blowing up at codegen). **Two parameters came out that the code discarded and the prose described as live**: `calls_in` took a `subst` it never read, under a doc line saying calls *"come back already substituted"* — they do not, the caller substitutes them — and `run` bound a `parent` it immediately dropped, because `recursive` re-reads it from the queue. Both are CLAUDE.md §11's class caught in passing. C byte-identical, 533 green, clippy clean. **9 of 23**: `types/table.rs` **380 → 246**, one cut — `widths.rs` 143 holds the eight integer widths and every fact about *one* width (bits, name, range, C spelling), none of which knows the type table exists, while `table.rs` is how types are stored and compared. The panel-042 argument for the shape moves with it and stops being repeated twice, and its example is repaired in the move: it cited `emit/ops.rs`'s `operands == Ty::Int`, which as of this morning's first sweep commit is `emit/operator.rs`'s `matches!(operands, Ty::Int(_))`. C byte-identical, 533 green, clippy clean. **10 of 23**: `types/calls.rs` **378 → 174**, into the dispatch and its two long answers — `ufcs.rs` 97 (the three tokens `x.f(y)` with two meanings, which is the question the resolver deliberately left open because only a type can settle it) and `apply.rs` 145 (a call to a declared function, and §4.12's *generics are inferred here and nowhere else* — a paragraph that had been sitting in a module doc two files away from the loop it describes). C byte-identical, 533 green, clippy clean. **11 of 23**: `emit/gate.rs` **372 → 146**, into the two tables its own doc had already named — `gate_types.rs` 142 (what the runtime can represent: the table that is *empty of refusals* today, and whose descent into element types is a filed defect rather than tidiness) and `gate_ops.rs` 133 (what the emitter can write, gating by name as well as by op because `xs.len()` and `for` lower differently). What stayed is neither table: the **list** — one entry per capability, deduplicated on the message, sorted by first appearance. C byte-identical, 533 green, clippy clean. **12 of 23**: `mutate/edits.rs` **367 → 214**, cut by *what mistake is being imitated* rather than by size — `edits_typo.rs` 180 holds the three operators where the mutation is **identical** and only the target changes, which is what turns three operators into one measurement: an identifier (the resolver catches it, 100%), an error code (**nothing** catches it, 0% — the one operator of eleven that points at no rule which kills it), a constant's digit. Verified by the instrument itself: `heroes mutate` produces **1255 mutants, 93% / 78%**, the same corpus and the same rates as before the cut. **Found while verifying, and it is a real gap**: `typo-digit` has **0 sites** on the whole corpus — an operator that measures nothing, which is §9's *"every verifier check has a test that makes it fire"* in mutation form. Not fixed here; it is not a sweep's business. **13 of 23**: `source/mod.rs` **340 → 182**, cut on *when* the code runs — `build.rs` 95 is everything that happens **once, before any offset exists** (`InputFile`, and the three ways several files become one text), `locate.rs` 92 is from an offset to a place a person reads (line, column, the text a span covers — every caret and every `#line`), and `mod.rs` keeps the questions *about* an offset that already exists: which file, which module, is it the library, is it the root. C byte-identical, 533 green, clippy clean. **14 of 23**: `emit/ctype.rs` **338 → 131**, one cut between two jobs with different failure modes — `ctype.rs` says what a type *is* in C (a wrong answer is a clang error), `typedefs.rs` 237 says what it is *named* (a wrong answer is a **collision**, which clang reports about a line nobody wrote). The leading-digit rule that keeps the emitter's invented names apart from the author's moves with the table it protects. C byte-identical, 533 green, clippy clean. **15 of 23**: `types/stmts.rs` **323 → 260**, and this one moved *arms* rather than a section — `jumps.rs` 122 takes the four whose answer to `Flow` is the file's own reason to exist: `return` and `break`/`continue` **jump**, `while` and `for` **fall through always**, because a loop may run zero times. That last line is why a function whose only `return` sits inside a loop is still `missing_return`, and it was one comment inside a 323-line file. The match keeps every arm named. C byte-identical, 533 green, clippy clean. **16 of 23**: `types/builtins.rs` **322 → 260** — `fallible_ops.rs` 98 takes `must`, `default` and `is_err`, the three whose whole rule is the same one (*the argument must be fallible*), together with the diagnostic for when it is not, because that is the answer all three give to the same mistake. **And it caught a doc that had outlived its rule by one day**: `unwrapping_nothing`'s comment said *"a widening returns `T` rather than `T?`, so a reader carrying the uniform habit writes a `.must()` that has nothing to unwrap"* — the 2026-08-13 ratification restored the uniform `T?` and deleted that source of the mistake, while the `certain` fix it justifies is worth **more** now, not less. Rewritten to say what is true. C byte-identical, 533 green, clippy clean. **17 of 23**: `heroes-cli/src/commands/compile.rs` **317 → 262** — `contract.rs` 80 is not pipeline at all, it is **how the command talks**: CLAUDE.md §10's one sentence (artifact on stdout, diagnostics on stderr, 0/1/2) and the two consequences that are easy to lose among the compiler passes — a flag that asked two questions is an error rather than an answer to one of them (`-O0 -O2`, the same reading `build` gives `--dump-ir --emit-c`), and `build`/`run` take no diagnostic-shaping flags because `check` already answers that. The backend half of the pipeline was the other candidate and was **rejected**: extracting it needed eight parameters, which is a worse file than the one it fixes. C byte-identical, 533 green, clippy clean. **18 of 23**: `resolve/exprs.rs` **313 → 263** — `places.rs` 68 is the left of `@`, and the concern is that **resolving a name there is not reading it**: in `v @ v + 1` the root is *written* and the `i` of `xs[i] @ …` is *read*. That distinction is what makes spec line 87's *"a read is a use and a write is not"* implementable at all, and it was one comment inside the file that treats every other occurrence as a read. C byte-identical, 533 green, clippy clean. **19 of 23**: `types/sized.rs` **312 → 243** — `sized_cycle.rs` 91 is what the reader is *shown* about a cycle, and it separates from finding one because it is bound by rules the walk is not: the caret lands on the **field** (Swift's shape, chosen over rustc's), the cycle is **rotated to the lowest declaration index** so reordering two unrelated declarations does not rewrite a golden, and the fix is a **`guess`** because `[T]` and `{K: V}` both break the cycle and the compiler cannot know which was meant. This is the file whose ≤150-line prediction panel 023 scored as **falsified** at 298; it is now 243 + 91. **AND THE LIST IS 8, NOT 5** — re-measured: the sweep has been pushing `types/mod.rs` **up** (303 → 309), because every split adds a `mod` line to it. An index file grows by the act of tidying, and that has to be said rather than discovered at the end. C byte-identical, 533 green, clippy clean. **20 of 23**: `types/mod.rs` **310 → 224** — `checked.rs` 112 is what the checker leaves behind, **the same seam `resolve/resolved.rs` was cut on an hour earlier**, and that is the finding: two sibling passes had the same shape, each holding *how the answer is computed* and *what the answer is*, with only the second read by anyone else. Which also means the cut that fixes an index file is not to shorten the index — it is to move what is not index out from under it. C byte-identical, 533 green, clippy clean. **21 of 23**: `own.rs` **309 → 242** — `owning.rs` 87 answers *what needs a reference at all*, where `own.rs` answers *where the incref and the release go*. It earns a file because it is the question this pass gets wrong **silently**: every other mistake in the ownership pass is a crash or a sanitiser report, and a wrong answer here is a **leak**, which nothing in an ordinary build reports — it took `hero_runtime_check_leaks()` saying *3 heap blocks still live at exit* to find the first one. C byte-identical, 533 green, clippy clean. **22 of 23**: `ir/inst.rs` **308 → 254** — `ids.rs` 75 is how the IR *names* things, and it separates from what an instruction *is* because everything in it follows from a rule that has nothing to do with instructions: **no struct may hold a reference** (the Cyclone rule), so an id is a `u32` index and anything variable-length is a **run**. That is what keeps `Op` `Copy` even though `Call` and `Construct` are n-ary. C byte-identical, 533 green, clippy clean. **23 of 23**: `syntax/stmt.rs` **306 → 243** — `name_stmt.rs` 94 is the difficulty the file's own doc opens with: **three line shapes begin with a name**, and one token of lookahead tells them apart. The reason the type is mandatory on `v: i64 @ 0` lives there now, next to the code that depends on it — without it `v @ 0` and `v @ v + 1` are the same shape, and whether a name is being declared or written would depend on a declaration elsewhere in the file, which is §1.3's locality test failing. C byte-identical, 533 green, clippy clean. **The original 23 are done.** Then, on the re-measured list: **`emit/externs.rs` 306 → 90**, with `extern_assert.rs` 250 taking the half that **checks** what the head line says against the real header. It separates on more than size: it is the only place in the backend where a clang failure is the **author's** mistake rather than the compiler's — exit 1 and a `ffi_return_type` diagnostic on the `.hero` line, not exit 2 and *"the compiler is wrong"* (CLAUDE.md §7's one named exception). Then **`lexer/scan.rs` 304 → 197**, with `literals.rs` 130 taking the two scanners whose content is a value the author wrote out. They belong together because they share the rule panel 008 spent a sitting on — **the backslash is reserved**, five escapes, any other escape a compile error — and because both hold the recovery rule that a bad escape does **not** poison the token: one mistake inside a string costs one diagnostic instead of derailing the line. And last, **`types/exprs.rs` 301 → 241**, with `contextual.rs` 75 holding §4.5's ⇐-only list — *is this form one with no type of its own?* — in one place, so that two readings of it cannot become two languages. **THE SWEEP IS CLOSED**: `find crates -name '*.rs' -not -path '*/tests/*' | xargs wc -l | sort -rn` has **no non-test file over 300** (`modules/tests.rs` 583 is a test file and outside §11's words, as this item said when it opened). Twenty-two files cut in one session on top of the three before it, 195 non-test files now, C emitted **byte-identical on every single one** (`adce2f13` unchanged from the first cut to the last), `heroes mutate` still 1255 / 93% / 78%, PORT-DEBT still 4, spec untouched at 2974: `emit/inst.rs` (445) is a **single exhaustive `match` over `Op`** whose whole purpose is that a new IR instruction cannot be added without this file and `ir/print_inst.rs` both failing to compile. So the cut must move arm *bodies* out and leave every arm in place — the candidates are the two `Abort` arms (what the author sees when a program stops), the five `Construct` shapes, and the access arms (`Tag`, `Payload`, `Field`, `Index`, `Len`). A cut that collapses arms into a `_ =>` would trade §11 for the property §11 exists to protect, and CLAUDE.md §11's own loud-fallback rule forbids it. Tick each file here as it lands, with its before → after | CLAUDE.md §11 · `find crates -name '*.rs' -not -path '*/tests/*' \| xargs wc -l \| sort -rn` | the sweep's own risk is that it rewrites the compiler while nothing else is being watched, so one file per commit is the whole safety mechanism

## Open — the panel owed

- [x] **RATIFIED 2026-08-12 — the author overturned both refusals. All four bases land as `M-literal-bases`; the Rust-shaped integer widths land as `M-sized-integers`, which retires Part 7 item 10. The verdict, its argument and what it costs are the last section of `docs/panel/041`; the DESIGN-LOG line is the same date.** Two of the sitting's answers survive the ratification and are worth keeping visible: the **value** reading of a based literal (so the bit-pattern veto held), and the fact that no judge disputed the gap the llm-ergonomist measured — the spec's FFI promise is false at the integer boundary, and `M-sized-integers` is what repairs the sentence. Was | **The panel's provisional default is: refuse both, ship the honest diagnostic, and repair what is false.** Seven parts, and the author may take them apart. (i) Q1's **bit-pattern** hex reading is **vetoed** — `0xFFFFFFFF00000000` stops being `int_out_of_range` and silently becomes `-4294967296`; the ffi-pragmatist's counter-argument is not answered by the veto, only outweighed, and the author should know the language would then have two rules for the sign bit (reachable by `<<`, unreachable by a literal). (ii) Q1's **value** reading is **held, not adopted** — 4 of 5 judges would take it, but the spec-warden's consistency argument is unanswered: panel 035's R6 refused exponents for *"a new syntactic form for a type not on the closure list"*, and **the closure list is a list of forms, not of types**, so `int`'s presence on it carries hex no more than `f64`'s carried exponents. Adoption costs a tightened **+42** measured and **is conditional on the −39 removal landing in the same commit** (spec lines 152–153, the string-building performance advice, which CLAUDE.md §13 makes a non-justification). (iii) `u64`-at-the-boundary is **vetoed on a measured inversion**: `extern constant SIZE_MAX: u64` + `to_i64` is a binary that **traps** where today it is a compile error. (iv) full sized integers and first-class `u64` carry **two vetoes each**. (v) `e6-cheapest.c` — one extra pair of generic associations in the existing `_Generic`, binding `strlen`, `SIZE_MAX` and `CURLAUTH_ANY` with **no new type, no spec token, no ABI bump** and 6/6 wrong signatures still errors — is **recorded and not adopted**, because the historian pre-refuted it without knowing it existed: `dart:ffi` is Heroes' exact integer model, shipped the marker shape ~7 years ago, and its users filed *"nobody told us the value went negative"*; the widened assertion has **no marker at all**. Taking it compels a written **LP64-only premise with the test that fires when it dies** (`-target i386-linux-gnu`) plus a `sizeof(...)==8` companion assertion, and an amendment to design.md §4.19's two sentences, which currently book the refusal as an achievement. (vi) What ships at **zero spec tokens with no judge against it**: a `hex_literal` diagnostic, because `x = 0xFFFF` says *"expected the end of the line, found a name (`xFFFF`)"* — a message about a name, for a number. (vii) The **spec's FFI promise is false** and no option on the menu repairs it: *"clang checks every signature and constant against that header, so a wrong FFI type is a compile error"* is silently untrue at the integer boundary, and the llm-ergonomist wrote `strlen(s: cstr) -> int` in two variants **knowing it was wrong, because nothing else could be written and nothing would complain** | docs/panel/041-notation-and-the-unsigned-boundary.md · design.md Part 7 item 10, §4.19 · spec § Types, § FFI | the four judges who can see the repository rejected the mechanism; not one of them disputed the gap the judge reading only the spec measured

- [x] **FIXED 2026-08-12 at `M-literal-bases` step 1, and the author's answer was the third option neither the panel nor this item listed: give octal a real spelling.** `0o700` is 448, and the bare leading zero is now `leading_zero` — a diagnostic whose note names both readings and whose two fixes are **both `guess`**, because the compiler does not know whether 448 or 700 was meant and a `certain` fix would silently pick one of the wrong answers. `tests/golden/check/leading-zero.hero` carries symptom, cause and date. Was | **`print(0700)` prints `700`, and this is a live silent defect with or without hex.** A leading zero is decimal in Heroes: the lexer's `number` (`lexer/scan.rs:206`) scans digits and knows nothing of a prefix. Every file-permission example a model has ever read writes `0700`, so it writes a wrong program that compiles, runs, and tests green against itself — the thesis inverted, in the exact class this language exists to kill. The judge predicts **≥20% of first tries at a permissions task carry a leading-zero literal, in every spec variant**, which makes this independent of Q1 and larger than it. Two answers are defensible and the author picks: **refuse it** — `0700` is a compile error whose diagnostic names the two readings and carries a `certain` fix to `700` (the shape panel 035 gave the exponent, and CLAUDE.md §11's loud direction) — or **support octal**, which is a second notation and a second panel. **It is filed apart from the ratification above on purpose**: decision 1a made the cheap question hostage to the expensive one this morning and said so in its own words, and this is the same trap one step further out | crates/heroes/src/lexer/scan.rs:206 · docs/panel/041 § D1 · tests/golden/fixedbugs/ | a silent wrong value from the most-copied constant form in Unix is worse than anything either panel question was about

- [x] **CONVENED 2026-08-12 — `docs/panel/041-notation-and-the-unsigned-boundary.md`, full panel, five judges. The ratification item is below; this one is closed as a panel debt.** The sitting answered both halves and moved the question: the ABI was never the gap (`int64_t`↔`size_t` round-trips losslessly at all four edges on arm64 and x86-64, compiled), so `u64` buys type-checking only — and buys it badly, failing three ways off LP64. Two of this item's own claims were corrected inside the sitting: `SIZE_MAX` **does** produce `ffi_constant_type` today (transcript in the panel file, so the record now holds a diagnostic and not a judge's reading), and the case that actually indicts the vocabulary is not `SIZE_MAX` at all but **`strlen`**, which cannot be bound in Heroes by any spelling. Was | **Are the base types wrong — does Heroes need sized or unsigned integers (`u64`, `i64`, …)?** Asked by the author while reading panel 039's findings, and it is the largest open language question. **The evidence already exists and is compiled**: an ffi-pragmatist found two real header constants Heroes cannot express — `CURLAUTH_ANY` = `(~((unsigned long)1<<4))` and `SIZE_MAX` = `((size_t)-1)` — and classified them itself as *"a type-vocabulary gap (Part 7 item 10), not an evaluation gap"*. **Two citations in this item were wrong and are corrected here** (verified 2026-08-12, third `/decide` session): the two constants are in **`docs/panel/040-the-bitwise-set.md` § finding 4 and prediction 3**, not in panel 039 — 039 contains neither name — and they are not `ffi_constant_type` diagnostics: that code exists and fires (`types/decls.rs:244`), but nothing has been written that makes these two produce it, so what the record holds is a judge's compiled reading, not a diagnostic transcript. **Part 7 item 10 mis-locates the pressure**, because the notation gap and the type gap are one reading of `curl.h` apart and a judge asked about `u64` alone will write `0x` in its own example. The brief carries both, and the ticked item above states what the pairing costs | **Are the base types wrong — does Heroes need sized or unsigned integers (`u64`, `i64`, …)?** Asked by the author while reading panel 039's findings, and it is the largest open language question. **The evidence already exists and is compiled**: an ffi-pragmatist found two real header constants Heroes cannot express — `CURLAUTH_ANY` = `(~((unsigned long)1<<4))` and `SIZE_MAX` = `((size_t)-1)` — and classified them itself as *"a type-vocabulary gap (Part 7 item 10), not an evaluation gap"*. **Two citations in this item were wrong and are corrected here** (verified 2026-08-12, third `/decide` session): the two constants are in **`docs/panel/040-the-bitwise-set.md` § finding 4 and prediction 3**, not in panel 039 — 039 contains neither name — and they are not `ffi_constant_type` diagnostics: that code exists and fires (`types/decls.rs:244`), but nothing has been written that makes these two produce it, so what the record holds is a judge's compiled reading, not a diagnostic transcript. **Part 7 item 10 mis-locates the pressure**, which is the finding to open with: it defers sized integers as *"needed for any binary format"*, when the live case is the **FFI**, which is §1.11's founding constraint and is inside v1 — the same shape as the `Macros` row panel 039 repaired, a correct decision filed under a reason that does not describe the real case. Against: one signed `int` with abort-on-overflow removes the silent-wraparound class by construction, and conversion rules are the most expensive spec item that exists (item 10's own words); after the mangler fix the closure list needs no unsigned. Note that **the bitwise set does not close this**, and the reason is not the one panel 040 wrote: `0xFFFFFFFFFFFFFFFF` is **not** `int_out_of_range`, because **Heroes has no hexadecimal literal at all** — the lexer scans `0`, then `xFFFFFFFFFFFFFFFF` as a name, and the program dies with `expected_end_of_line` at column 10 (measured 2026-08-12; zero occurrences of hex scanning in `lexer/`, zero mentions of `0x` in the spec). The decimal form is the one that reaches the type checker: `18446744073709551615` → `int_out_of_range`, with the note naming the signed range. So the unsigned gap and the **notation** gap are two gaps, and only the second is cheap | design.md Part 7 item 10 (line 2287) · docs/panel/040 § finding 4, prediction 3 · spec § Types | §1.11 makes the FFI the standard library, and a standard library that cannot say `size_t` has a hole in it
- [x] §11, standing — **ANSWERED 2026-08-12 (2a): a standing sweep, with its own commit, run to zero violations.** Not a per-milestone obligation in `/step` and not a restated number: the rule keeps saying ~300 and the tree is brought to it. Re-measured at the moment of the decision — **22 non-test files over 300**, from `emit/perfn.rs` 628 down to `ir/inst.rs` 303 (`modules/tests.rs` at 583 is a test file and outside §11's words). Each file is its own commit, each one asserts byte-identical emitted C where it touches `emit/`, and the sweep's progress is tracked in the §11 sweep item below rather than here. The rule is what §1.6's refused global budget left as the only watch on the compiler's size, so its credibility is the whole point: a rule 22 files break is a preference. Was | **Twenty-two non-test files are over ~300 lines, and three of them were pushed there on 2026-08-12.** The two the author's decisions named are closed — `emit/decls.rs` 768 → 171 across `externs.rs`, `main.rs`, `signature.rs` and `body.rs`, and `resolve/cycles.rs` 343 → 241 across `constbody.rs` — with the emitted C byte-identical both times. **What this session added to the list, and it is owed rather than incidental**: `emit/ops.rs` 378 → 433 (the bitwise emission), `crates/heroes-cli/src/commands/compile.rs` ~300 → 317 (`level_from`), `ir/inst.rs` → 303. The largest standing breaches are `emit/perfn.rs` (628), `emit/aggregate.rs` (511) and `printer/fmt_stmt.rs` (457), none of which any decision has touched. Decide whether this is a **standing sweep** with its own commit, a per-milestone obligation in `/step`'s checklist, or a number §11 should restate — because "split a module before it passes ~300 lines" is currently a rule twenty-two files are in breach of, and a rule with twenty-two exceptions is a preference | CLAUDE.md §11 · `find crates -name '*.rs' | xargs wc -l | sort -rn` | the per-file ceiling is the only live one after the global number was refused (design.md §1.6), so it is now the whole of what watches the compiler's size

## From panel 051 (2026-08-14) — what the FFI's parameter side still owes

- ~~**`-Werror=shorten-64-to-32`, and the gate it needs.**~~ **CLOSED at
  M-binding-fidelity** (verified 2026-08-15): the flag is in `toolchain.rs::FLAGS`
  with `-Werror=sign-conversion` beside it, and the veto was answered by building
  the gate rather than by arguing it — `emit/extern_probe.rs` emits a never-called
  probe per `extern`, and `ffi_parameter_type` is exit 1 on the author's own line
  with the parameter's name in it. The three legs are green.
- **design.md Part 7 item 10 is unsupported.** Its own falsifier names `strlen`,
  and `extern function strlen(s: cstr) -> u64` compiles and prints `5` on this
  machine — closed by panel 042's widths, with the text left behind. Under §12 as
  amended by panel 039 a refusal carries a feature's burden of proof, so the row
  either names a new falsifier or goes.
- **Variadic arguments are unchecked by anything, and reachable today.**
  `curl_easy_setopt(handle, CURLOPT_URL, …)` takes its value through `...`, where
  no flag, no `_Generic` and no function-pointer probe can see it. §4.19 rung 4
  already depends on this.
- **`float` parameters have no answer at all.** `f32` does not exist
  (`error[unknown_type]`), so `SetMasterVolume(volume: f64)` builds clean and
  delivers `0.10000000149011612`. **19 raylib entry points are bindable and
  silently wrong**, and stay that way under every option this panel considered.
- **`size_t` signedness.** `-Wshorten-64-to-32` is blind to it — an `i64` of `-1`
  arrives as `18446744073709551615` — and the widths fix it, which is one more
  place the warden's objection and the pragmatist's measurement disagree.
- **A widening that cannot fail is fallible.** `n.to_i64()` on an `i32` yields
  `i64?`, so every narrow-width binding composes through a `.must()` that asserts
  nothing. Found while landing panel 051; it is half of the ergonomic cost the
  pragmatist booked at 251 call sites.
- **The warden's `−8` removal, unspent.** Dropping *"`push` copies the array"* from
  the cost paragraph is warranted on its own merits — ledger row `2627`'s
  prediction was scored **half falsified** at M-program-corpus, because the reader
  reached for `push` anyway, the document leaving no other way to build the array
  `join` needs. It was **not** spent to fund panel 051, because that clause belongs
  to panel 037 and panel 041 refused exactly this kind of cross-funding. It needs
  its own decision.

## From panel 052 (2026-08-14) — what the FFI still owes after the parameter check

- ~~**Struct-by-value is the whole remaining wall: 356 of 1159 entry points, 31%.**~~
  **SCHEDULED 2026-08-14** as `M-struct-passing`, ROADMAP order row **1**, ahead of
  `M-selfhost-probe` — design.md §1.12's completeness clause over Principle 0's
  silence. The ffi-pragmatist's *"not past 254 of 600"* prediction is unscored and
  the milestone's own close scores it.
- **A rename, so one C name can carry two arities.** This is what variadics
  actually need — the ABI is already correct — and it is the ergonomist's blocking
  condition on the alternative to `...`: `function printf_i(format: cstr, value:
  i32) -> i64 = "printf"`, or any spelling that keeps the C symbol on the
  declaration line. Without it the second shape of `curl_easy_setopt` is
  `declared_twice`.
- **A variadic argument's width is checked by nothing** — not a flag, not a
  `_Generic`, not the probe. `int -1` in a variadic slot reads `-1` on arm64 and
  `4294967295` on x86-64. Reachable today.
- **`f32`, and the shape is not settled.** The case that motivated it does not need
  it (a C `float` parameter bound `f64` is bit-exact). The case that does is a
  `float *` out-parameter — 2 entry points — and the compiler-engineer measured
  that `@f32` is **core**, not a boundary type: an `@` argument is the address of
  the author's own slot, so it needs an `f32` local. That is A1's cost, for two
  entry points.
- ~~**`emit/ffi.rs` is 550 lines against CLAUDE.md §11's ~300.**~~ **CLOSED at
  M-binding-fidelity** (verified 2026-08-15): split four ways on the engineer's own
  seam — `ffi.rs` 163, `ffi_build.rs` 181, `ffi_declared.rs` 157,
  `ffi_narrowed.rs` 190. Nothing in the tree is over 300 outside tests, which
  CLAUDE.md §11 as amended 2026-08-14 puts outside the rule's words.
- ~~**The warden's R2, −12.**~~ **SPENT 2026-08-14** at ledger row `3191`, funding
  panel 048's X clause (+45, net +33). Panel 051's R1 (−8) is **still unspent and
  still refused**, on the ground measurement 007 supplied: a blind reader quotes
  that sentence, `push` half included, as the document's only cost claim. It is the
  live half of this pair and it appears once, in panel 051's list above.

## Open — the §11 sweep, after M-binding-fidelity

- [x] §11, 2026-08-14 | **`toolchain.rs` sits at 400 lines and I did not split it further — say whether that is right.** The sweep took two files past ~300 this milestone. `emit/ffi.rs` reached **573** and was split four ways on a real seam (what a verdict is *about*: the declaration, a narrowing, the machine), leaving 160/157/188/131. `toolchain.rs` reached **517**; `libraries.rs` came out of it at 137 on the same kind of seam (*where a library is* versus *how clang is driven*), and the remaining **400** is where I stopped. The reason is that the rest reads as one concern — find the toolchain, cache the runtime, invoke it — and the obvious next cut, separating `link` from `runtime_object`, would run straight through the **cache key** and the `publish` helper that both use. §11's number is `~300` and its actual rule is *"the author must be able to open any file and read it without drowning"*, so this is a judgement about readability that is yours rather than arithmetic that is mine. **The other open half of the same question**: the sweep has never been applied to test files, and four are now larger than anything in `src/` — `surface.rs` 1164, `golden.rs` 785, `emit/tests/gate.rs` 612, `modules/tests.rs` 583. Two of those are inside `src/`, so the rule reaches them by its own words | CLAUDE.md §11 · crates/heroes-cli/src/commands/toolchain.rs · crates/heroes-cli/tests/surface.rs | a rule with a number in it stops being a rule the first time somebody rounds it in silence **ANSWERED 2026-08-14 (`a`).** §11 now says the ~300 governs what a reader must hold in their head, so it binds the compiler's code and not its tests — a test file is read one case at a time — and `toolchain.rs` stays at 400 because the cut that would take it under runs through the cache key `link` and `runtime_object` share. Both halves are in CLAUDE.md §11.

## Open — carried out of panel 055, because a ticked item does not track a live question

- [x] **ANSWERED 2026-08-14 — the flags landed; ticked 2026-08-15 without being asked** (verify-before-asking). `--include` and `--library` are on the argv table at `cli/table.rs:103-131`, on all three commands that reach clang, and `DESIGN-LOG.md`:298 records the decision with its argument. The item stayed unticked for a day, which is the inverse of the failure this list's first rule guards against and costs exactly the same: a ticked item that tracks nothing wastes a verification, an unticked item that tracks nothing wastes a *question to the author*. Was | panel 055, 2026-08-14 | **`--include` and `--library` on the argv table: the judges split, and the split is real.** Three hold that CLAUDE.md §10's stopping rule yields **nothing** — `CPATH`, `C_INCLUDE_PATH`, `LIBRARY_PATH` and `PKG_CONFIG_PATH` are all live because `Command` inherits the environment, measured four ways, and *"nothing if two existing invocations already compose to it"* is the rule's own third clause. One **built it** — `+105/−6`, 75 code lines, all in `heroes-cli`, **zero spec tokens**, 549 tests green — and found and fixed a defect in its own prototype on the way (search paths absent from the cache key, so two configurations shared one artifact path). Its argument is not about cost: an environment variable is **not an artifact a program can carry**, so asked for *"a program that binds SDL2"* a model produces something that cannot be built and the missing half is an unversioned shell line. Against it, and this is §1.12's ground: `-I` and `-L` named **independently** is the only way to produce the skew the engineer measured — header v1, library v2, compiles clean, links clean, exit 0, **wrong number** — while `package` asks one question of one `.pc` and cannot. **What is yours**: land the flags, or write the refusal with its falsifier per §12. If landed, three conditions are already measured and owed — the paths go in the cache key, no allow-list (a `.pc` is a whitespace-split string read as flags, a search path is one argv word clang consumes as a path, and that distinction gets the test §11 asks for), and `ffi_missing_header`'s note names the flag as it now names `CPATH` | docs/panel/055 · crates/heroes-cli/src/cli/table.rs · crates/heroes-cli/src/commands/toolchain.rs | the one question of the sitting where the measurements point two ways, and neither judge is wrong about what they measured

- [x] **ANSWERED 2026-08-14 — `M-struct-passing` is order row 1, ahead of `M-selfhost-probe`; ticked 2026-08-15 without being asked.** Both halves the item put to the author are decided and recorded at `DESIGN-LOG.md`:299: it is a **milestone of its own**, and it comes **before** the probe — design.md §1.12's completeness clause over Principle 0's silence, which is the one place §1.12 was written to be able to outrank Principle 0's *neither → it waits*. It is in `docs/ROADMAP.md` § The order row 1 and § Done's name table. What the yes did **not** decide, and stays live inside `M-struct-passing` rather than here: the ffi-pragmatist's prediction that the milestone will not raise raylib past **254 of 600** without it is unscored, and it is the milestone's own close that scores it. Was | panel 049 (3), still open after 055 | **Struct-by-value at the FFI boundary — the measured wall, and the next milestone's question rather than this one's.** **356 of 1159 entry points (31%)** across `sqlite3.h`, `curl/curl.h` and `raylib.h` pass or return a struct by value; raylib alone is **346 of 600**, and §4.19's ladder rung 5 promises *"real struct passing"* in ratified text. No scalar vocabulary touches it: panel 052 struck `f32` and variadics from its own agenda and this is what remained. Principle 0 does **not** fund it — the closure list binds only `hero_os.h`, which has no structs — so it enters, if it enters, on §1.12's completeness clause: *any C library must be bindable, because one Heroes cannot reach is C code the author keeps writing by hand*. The ffi-pragmatist predicts `M-binding-fidelity` will not raise raylib past 254 of 600 without it. **What is yours**: whether it is a milestone of its own, and whether it comes before or after `M-selfhost-probe` — which Principle 0 would put first | docs/panel/049, 052 · design.md §4.19's ladder, §1.12 | the FFI is this language's whole library chapter and a third of it is unreachable

## Open — found after panel 055 closed, by running it

- [x] **ANSWERED 2026-08-15 (`c`), and written as a refusal with its falsifier rather than as silence.** The allow-list gets no second half. `-lSDL2main` names no file, loads nothing and writes nothing **at build time**, so every question the list asks returns *safe* — correctly. The list is not weak on this class, it is **blind** to it: what a library does after linking is not a property of the flag string, and no allow-list reading flags can recover it. That is the same shape as CLAUDE.md §7's argument about the ABI stamp and a decoy runtime — a guard is not widened to cover a class it cannot see, because the widening buys nothing and makes the guard read as though it did. **Refusing `*main` by name is refused on §11**: a rule keyed on a naming convention is a premise about the world and expires in silence while its comment goes on reading as correct. The falsifier, per §12: a library whose linked-in behaviour breaks a corpus program **and** that the corpus timeout does not catch — the timeout being the instrument that answers this class, because it asks about the program's behaviour rather than the flag's spelling. The argument now lives beside `ALLOWED` in `libraries.rs`, where the next person to consider widening the list will read it. Was | panel 055, found 2026-08-14 by running the corpus | **`package "sdl2"` builds, and the program it builds is a GUI application that can hang forever.** The sitting measured that `-D`/`-U` and `-Wl,-framework,<name>` let SDL2 through the allow-list, and nobody asked what the resulting binary *is*. Two facts, both measured on this machine after the fact. (1) `pkg-config --libs sdl2` answers `-lSDL2main -lSDL2 -Wl,-framework,Cocoa`, and **`SDL2main` replaces the program's `main`** with SDL's own, which initialises Cocoa — so any Heroes program that links it becomes a windowed application. Eleven corpus programs in three configurations each filled the author's dock with app icons. (2) This machine's `libSDL2` is **`sdl2-compat 2.32.70`**, a shim that loads **SDL3 at runtime** and, when it cannot, opens a **modal dialog** — *"Fatal error! Cannot continue! Failed loading SDL3 library."* — and waits for a click. On an unattended test run that is not a failure, it is a **hang**: two processes sat at 0% CPU for ninety minutes until they were killed by hand. `examples/sdl/` was removed for this reason; the binding itself is correct and `heroes run` on it prints what it should. **What is yours**: whether the allow-list's property test needs a second half. Today it asks *does this word name a file, load anything, or write anything* — and `-lSDL2main` does none of those and still **replaces the program's entry point**, which no other library on the list can do. It is not build-time code execution, which is what CVE-2018-6574 was about, so panel 050's argument does not reach it; it is a runtime shape change that the corpus cannot survive. Options as they look from here: leave it (the author of a program that links SDL knows what SDL is), refuse `*main` by name (narrow, and a premise about naming conventions), or say nothing in the list and instead give the **corpus** a timeout, which catches this and every future hang | docs/panel/055 · crates/heroes-cli/src/commands/libraries.rs · crates/heroes-cli/tests/corpus.rs | the allow-list was measured against what a flag can do at build time, and this is what a library can do at run time. **Two things landed the same evening and neither closes this question.** (a) The corpus has a **timeout** — two minutes, chosen against the slowest thing it actually does, so it is the line between *slow* and *never* rather than a performance budget (§13). It is the answer that catches every future hang instead of this one. (b) `-Wl,-rpath,<dir>` joined the allow-list, because SDL3's own `.pc` answers with it and `package "sdl3"` was refused for a flag that names a directory — **and it is the second instance of this very question in one hour**: an rpath names no file, loads nothing and writes nothing at build time, and changes what the loader picks at run time. Its escalation is written in the code: a hostile `.pc` saying `-L/tmp/x -lfoo` already chooses the library, so the rpath is not a new capability, but it lets the choice be **swapped after the build** by anyone who can write to that directory. The example itself is now **SDL3**, which has no `SDL2main` and no compat shim, and the corpus runs it in 30 seconds

## Owed a panel — the path pair (author decision 2026-08-15: `b`, the pair, no normalisation)

- [x] **CLOSED 2026-08-15 by `docs/panel/057` — refused, three vetoes, and the
  proposal that lost was the one written in this file.** `path_join` fails §1.0 on
  its own concession; `path_parent`'s §1.0 claim was `directory_text` and **it does
  not compute it**, measured twice. Nothing lands: the port writes the scan once in
  `modules/` at 0 spec tokens, which was option (c) all along. The `\`-as-separator
  clause is refused outright with a falsifier that already fired. The Tier-2 route
  is measured at zero compiler lines and left available. **The ratification item is
  below.**

**This is the one item of the eight `/decide` took on 2026-08-15 that `/decide`
could not close, and the reason is jurisdictional rather than a hesitation.** Two
new built-in names plus spec tokens is surface, so CLAUDE.md §4 makes it a `/panel`
path, and the soundness lane does not fit because the change costs spec tokens. The
skill's own rule is to say so and stop. **The proposal is already instructed, so the
sitting can be convened whenever there is time to wait for it:**

> **Adopt `path_join(dir: str, name: str) -> str` and `path_parent(p: str) -> str?`,
> neither of which normalises.**
>
> - **`path_parent` is the one with the §1.0 argument.** The port's module loader
>   must take a path the author typed and find its directory part;
>   `modules/mod.rs`'s `directory_text` does `path.rfind(['/', '\\'])`, looking for
>   **either** separator because the author may have typed a Windows one. In Heroes
>   that is a backwards loop over a string looking for two characters — the shape a
>   built-in exists to remove.
> - **`path_join` has no §1.0 argument and comes along anyway.** Verified: no corpus
>   program builds a path, and `dir + "/" + name` is already correct on all three
>   platforms, so it buys **discoverability, not correctness**. It enters as the
>   inverse of a function that does have the argument, or not at all.
> - **`path_parent` returns `str?`.** Absent when there is no separator. Returning
>   `"."` would be inventing a meaning; `str?` makes the caller handle it and is the
>   language's own idiom (§4.6, absence as a variant).
> - **`path_join` inserts one `/` unless `dir` already ends in `/` or `\`** — a fact
>   about the value in hand (§11), not a decision about a path's meaning.
> - **Neither normalises, and that is the load-bearing half.** The runtime is
>   portable today *because it does nothing to a path*: `runtime/parts/os.c` has no
>   `strcat`, no separator literal, no normalisation, and the string reaches `fopen`
>   verbatim. A `path_join` that rewrote `\` to `/` or collapsed `..` would be the
>   first place this project decides what a path **means**, and that is a larger
>   step than the name suggests — it is also the step that has produced security
>   defects in every language that took it eagerly.
>
> **What the sitting must price**: the spec tokens for two names against §1.6's
> headroom (888 today), and whether `path_parent` alone is the honest §1.0 purchase
> with `path_join` refused under Principle 0's *neither → it waits*.

Was | author instruction, 2026-08-14 | **A runtime function that joins two path pieces — and the honest question is whether joining is the half that is missing.** Verified before writing this: **no program in the corpus builds a path today**, `runtime/parts/os.c` does nothing to one (no `strcat`, no separator literal, no normalisation — the string reaches `fopen` verbatim), and `/` works on all three platforms because Windows accepts it in every filesystem API. `examples/adventure/main.hero` proves it on the Windows leg with a literal `examples/adventure/walkthrough.txt`. So a program that wants to join **can** today: `dir + "/" + name` is correct everywhere, and a doubled separator from a trailing `/` is harmless on all three. **Which means `path_join` buys discoverability, not correctness — and that is not a Principle 0 argument.** What *is* one is the other half. The port's module loader must take a path the **author typed** on the command line and find its directory part: `crates/heroes/src/modules/mod.rs`'s `directory_text` does `path.rfind(['/', '\\'])`, looking for **either** separator, because the author may have typed a Windows one. In Heroes today that is writable — `chars`, `slice`, `len` — and it is a loop over the string backwards looking for two characters, which is the shape a built-in exists to remove. **So the three options are not join-or-nothing**: (a) `path_join(dir, name)` alone, which is the asked-for thing and the half the port does not need; (b) **the pair** — `path_join` and `path_parent` — where the second is the one with the §1.0 argument and the first comes along because a program that can take a path apart will want to put one together; (c) neither, and the port writes the backwards loop once, in the file that already knows why (`modules/`), with `directory_text`'s own comment carried over. **A fourth thing to decide with it**: whether either function normalises. The runtime's portability today is that it does **nothing**, written down as a claim that can die — a `path_join` that rewrote `\` to `/`, or collapsed `..`, would be the first place this project makes a decision about a path's *meaning*, and that is a larger step than the name suggests | runtime/parts/os.c · crates/heroes/src/modules/mod.rs (`directory_text`) · crates/heroes/src/library/source.hero · spec § Strings, arrays, maps | the runtime is portable because it does nothing to a path, and the first function that does something is where that stops being true

## Open — §11 drift, found 2026-08-15 while verifying the `/decide` session's own work

- [x] **ANSWERED 2026-08-15 — a test, and it was four files rather than two.** None of the three options this item offered was the most complete: a `/step` checklist line is **discipline**, and discipline is exactly what had already failed twice; a subcommand is refused by §10's own stopping rule (`find crates -name '*.rs' | xargs wc -l | sort -rn` composes, and what a subcommand would add is not the measurement but the **obligation** — which is what a test already is); *nothing* leaves the next breach to luck. `crates/heroes-cli/tests/layout.rs` walks the tree, excludes tests **by where they sit** rather than by a name list, and carries a `DECIDED` table whose load-bearing column is the **reason** — a bare exception list decays into the breach list it replaces. **It went red immediately on four, not two**: `toolchain.rs` 425 past its DECIDED 400, `emit/inst.rs` 348, `syntax/externs.rs` 307, `emit/body.rs` 303. All four cut along seams that name a concern — `emit/unread.rs` (what the emitted C **reads**; `inst.rs`'s per-op half and `body.rs`'s per-function half were one question in two places, and neither emits anything), `syntax/extern_members.rs` (what a group **declares** against what it **names**), `commands/flags.rs` (what clang **enforces**, which knows no path — deliberately **not** the cache-key seam your 2026-08-14 decision refused). Emitted C **byte-identical over 66 goldens** at every step, `f990db898b5a`. **An entry in `DECIDED` is a ceiling and not a licence**, which is the half that made the 425 visible at all. Was | The sweep closed 2026-08-13 with *"no non-test file over 300"*, and `find crates -name '*.rs' -not -path '*/tests/*' | xargs wc -l | sort -rn` now heads with **`toolchain.rs` 425** and **`emit/inst.rs` 348** (`modules/tests.rs` 583 is a test file and outside §11's words as amended 2026-08-14). The two are different cases and only one is a surprise. **`toolchain.rs` is the surprise**: the author's decision of 2026-08-14 named **400** explicitly and said why — the cut that would take it under runs through the cache key that `link` and `runtime_object` share, and a file split against its own seam is harder to read than a long one. It is 425 now, so a number settled by name has drifted by 25 without a decision, which is the exact failure mode §11's own credibility argument describes (*a rule twenty-two files break is a preference*). **`emit/inst.rs` is the ordinary case**: 293 at the sweep's close, 348 today, grown by milestones since. Its constraint is recorded and still binds — it is a single exhaustive `match` over `Op` whose purpose is that a new IR instruction cannot be added without this file failing to compile, so a cut moves arm *bodies* and never collapses arms into a `_ =>`. **Reported rather than repaired**, because neither is part of what this session was asked, and because the honest question is not *which two files* but whether §11 wants a **standing** re-measure — the 2026-08-12 decision made the sweep a one-off run to zero, and a rule that is checked once goes back into breach by ordinary work, which is what these two are. Decide: a re-measure in `/step`'s close checklist, a `heroes` subcommand that reports the breach list (§10's stopping rule says *nothing* — `find | xargs wc -l | sort -rn` already composes), or nothing and the next sweep is convened when somebody notices | CLAUDE.md §11 · crates/heroes-cli/src/commands/toolchain.rs · crates/heroes/src/emit/inst.rs | the per-file ceiling is the only live watch on the compiler's size after §1.6's global budget was refused, and it has now been silently exceeded twice since the sweep that existed to prove it was real

## From panel 056 (2026-08-15) — the project file, and what the sitting found on the way

- [x] **RATIFIED 2026-08-15 as it stands** (blanket author instruction, *"le ratifiche ratifica tutto per me"*). B stands adopted with D as its companion, A refused, C refused on its two vetoes, and the three return conditions are unchanged. **Panels 014 and 039 were ratified in the same instruction**, and 039 is the one that matters to this file: B *is* 039's precedent applied to a second shape of question, so the two were a decision leaning on a provisional one for as long as they were split. **The one thing the yes does not silently answer** is written into the panel file rather than left here: if C is ever overturned, the shape the judges would accept — one key — was **not on the ballot** and must be priced by its own sitting, not inherited from C's measurement. Four of the five predictions name instruments that do not exist; R2's *re-decided, never renewed* governs them exactly as before. Was | panel 056, 2026-08-15 | **Ratify the sitting, or overturn it.** Full lane, five judges, all building in copies. **A refused** (one veto, four objections), **C refused** (two vetoes), **B adopted** at zero spec tokens with **D** as its companion — compile-time project configuration is *examined and deliberately unplaced*, neither Part 6 nor Part 7, with the return conditions in the ROADMAP's M-package-manager entry rather than Part 7 (panel 039's precedent, and its reason: Part 7's preamble claims its items lose *only* on simplicity, which is false of this shape). The three conditions that would return the question are the judges' own convergence stated as tests: **one key, not two** (a `prefix <dir>` deriving both `-I` and `-L`, so the skew is unrepresentable); **no string in the file may also appear in a `.hero`** (mechanical, greppable, and deleting the file must never change which symbols are linked); and **one named binding that `package` and one command line cannot build** — a set that is empty today, giflib included. **What is yours**: the verdict. If you overturn C, note that the shape the judges would accept — one key — was not on the ballot and is six lines dearer than the one that was | docs/panel/056 · docs/reasoning/004-a-project-file.md · design.md §3.5, §1.12 | the resolution is provisional and the compiler behaves as if B holds until you say otherwise

- [x] **ANSWERED 2026-08-15 (`b`) — the check is in the argv layer, and the flag is refused with its reason written down.** `--include` and `--library` are built by a constructor now named `search_path` rather than `repeated`, and **the rename is half the fix**: a search path is repeatable *because* a list has more than one entry and must name a **directory** *because* that is what a search path is, and naming the constructor after the first property made the second invisible. A value that names no directory is now exit **2** — §10's *a bad flag*, `Flag::directory` in `cli.rs` — before clang is invoked. `-Wmissing-include-dirs` is **examined and refused**, with the measurement kept: added to `FLAGS` it exits 2 through §7's *"the compiler is wrong"* path on a program with no `extern` at all. The exit-1 route is refused because §7's narrowing is that every exit-1 class recovers a name *this program declared* `extern`, and a directory is not a declaration — the narrowing stays intact rather than being widened for one case. **What this deliberately does not reach is `CPATH`**, and that is not a gap: it is the same line panel 055 drew when these flags were adopted — an environment variable is the machine's configuration, a flag is the artifact a program carries. Falsifier, per §12: a wrong header reaching a build through a missing directory this check does not see. `a_search_path_that_names_nothing_is_refused_before_clang_sees_it` makes it fire on both flags; the filler in `a_search_path_reaches_clang_as_one_argv_word` moved from `/no/such/directory` to an existing empty directory, which does the same job because it holds no header. Was | panel 056, measured 2026-08-15 | **A missing search directory is ignored in silence, and the one-line repair is not available.** `clang -Wall -I/no/such/dir` says nothing, so a committed invocation pinning Homebrew's prefix builds against the SDK's headers on another machine — clean, exit 0, **against a different header than the one the declarations were checked against**, which is §4.19's oracle swapped rather than weakened. `-Wmissing-include-dirs` is the only warning that sees it and is in neither `-Wall` nor `FLAGS`. **Three things were measured before this was queued rather than landed.** (1) Added to `FLAGS` it exits **2** — *"internal error: compiling the generated C failed"* — which says the *compiler* is wrong when the author's flag is; a judge saw a program with **no `extern` at all** fail that way. (2) It turns `a_search_path_reaches_clang_as_one_argv_word` red, the test CLAUDE.md §11 requires for the no-allow-list exemption, because step 3 uses `/no/such/directory` as filler to prove both values reach clang in order — repairable, an existing empty directory does the same job. (3) Classifying it **exit 1** would break §7's stated narrowing: every exit-1 class recovers a name *this program declared `extern`*, and a directory is not a declaration. **What is yours**: the clang flag with exit 2 accepted, a check in the argv layer where `--include` already refuses a `-`-leading value (cheap, no new diagnostic class, catches what travels between machines but not `CPATH`), or a fifth exit-1 class with a new narrowing, which is §7 amended | crates/heroes-cli/src/commands/toolchain.rs (`FLAGS`) · crates/heroes-cli/src/cli.rs · crates/heroes-cli/tests/surface.rs:1188 · docs/panel/056 § Findings | this is the silent half of the skew the flags were approved with, and it is the half that survives being committed

- [x] **ANSWERED 2026-08-15 (`a`, and the answer was to stop tabling the width).** `malloc` is exit **1** on the author's own line with the parameter named, the header's `unsigned long` quoted, and `u64` proposed. The table was the defect, not the missing row: `long` and `unsigned long` were left out because no *tabled* string is right on three legs — true, and the correct conclusion was that a width is **not a constant**. `word_width_bits()` reads `size_of::<c_ulong>()`, Rust's name for the same C type clang is about to compile, on the same machine for the same target — a fact about the value in hand rather than a premise about which platforms exist (§11). `long long`/`unsigned long long` joined as fixed widths (64 everywhere, by C99's own floor); `size_t` and `ssize_t` need no rows because clang reports the **canonical** type, which is how `malloc`'s parameter arrives as `unsigned long`. **A second note carries the portability half** — which width this target uses and which the other one does — because without it the diagnostic would tell a reader on Windows to write `u64` for a 32-bit parameter, i.e. produce the silent widening the class exists to prevent, through the fix. The fix stays `guess` for two reasons now, both written in the code. **The premise has a test that fires when it dies** (§11): `the_word_width_this_compiler_claims_is_the_one_clang_uses` builds a real binding at the claimed width through the real toolchain, and goes red in the milestone that adds a `--target` flag rather than in a diagnostic somebody reads later. `tests/golden/fixedbugs/ffi-word-width.hero` carries symptom, cause and date (§9). Was | panel 056, found 2026-08-15 by binding it | **`function malloc(size: i64) -> ptr` exits 2 with raw clang output, and it should not.** `unsigned long` is deliberately absent from `emit/ffi_narrowed.rs::heroes_spelling`, so the parameter check cannot recover a spelling and the failure falls through to *"the compiler is wrong"*. `u64` binds `malloc` and `strlen` correctly, so the language can express it and only the diagnostic cannot. That is a fifth exit-1 class CLAUDE.md §7 does not have, and unlike the missing-directory case it **does** fit §7's `declaration()` narrowing — the name is declared, in an `extern` group, by this program | crates/heroes/src/emit/ffi_narrowed.rs · CLAUDE.md §7 · docs/panel/056 § Findings | `malloc` is the first thing anyone binds, and today it blames the compiler

- [x] **ANSWERED 2026-08-15 (`b`): there is no fourth slot, because there is no fourth input class.** §10 gains the rule rather than the slot, and it is written as a **recording** of what panel 056 adopted rather than as a new architectural decision — which is why it needed no second sitting. The three shapes all answer *what does this capability do to the inputs the tool already has* (a `.hero` file, argv, the environment); a per-project file is a new class, and §10 as written does not admit one. Two things are the point and both are in the amendment: the refusal is **not permanent** — 056's three return conditions are its only amendment path — and **§9's `#~` gap is evidence for the rule, not a second job**, because a new input class would arrive with its diagnostics exempt from the one invariant a regenerator cannot forge, which panel 020's historian already refused for `unsupported`. Was | panel 056, 2026-08-15 | **CLAUDE.md §10's shape table has three slots and a project file is a fourth.** The rule says a capability's shape is mechanical — a **subcommand** if it answers a different question, a **flag** if it changes how one question is answered about the same input, **nothing** if two invocations compose. A per-project file is none of the three: it is a new *input class*, and §10 does not admit one without being amended. Related and smaller: **CLAUDE.md §9's `#~` invariant cannot reach a non-`.hero` file** (`golden.rs:344-372` collects `.hero` only), so any future project file's diagnostics would be the one test class in the repository exempt from §9 — which panel 020's historian refused for `unsupported`. **What is yours**: whether §10 gains a fourth slot, or whether "no fourth input class" becomes the rule that refuses a project file more cleanly than Part 6 could | CLAUDE.md §9, §10 · docs/panel/056 § Findings | B was adopted without this, and it is the argument that would make B permanent rather than provisional

- [x] **ANSWERED 2026-08-15 (`b`): the clause is retired and §10 carries the weight, with the correction kept.** It was a **duplicate of §10** (*never a second binary*) stated with a false premise, and the duplication is precisely what let the false example survive — nobody rereads a rule that appears confirmed elsewhere. §6 now ends at `style-insensitive identifiers` and says why the clause went, naming the sourced fact (nimble ships *with* Nim, so the contrast was never real) and the second failure the historian found: it is the rule a reader reaches for first to refuse a project file, and it does not reach one — **a file is not a binary**. §10 gains the corrected sentence with `heroes add`/`heroes fetch` named as subcommands (design.md:637). The record is corrected, not deleted (§14). Was | panel 056's historian, sourced 2026-08-15 | **CLAUDE.md §6's *"a separate package binary: never"* is a bare rule, and it describes nimble incorrectly.** No argument, no panel, one citation (`docs/ROADMAP.md`:171). Sourced: **nimble is bundled with Nim**, so the thing §6 names by way of contrast is not separate. Nothing in the repository mentions `nim.cfg`, `config.nims` or `nimble` anywhere else — which matters because §6 is the rule a reader would reach for first to refuse a project file, and it does not reach one: a file is not a binary. **What is yours**: give the clause a reason, or retire it and let §3.5 and §10 carry the weight | CLAUDE.md §6 · docs/ROADMAP.md:171 · docs/panel/056 § Precedent | a bare rule that is also inaccurate is the kind of thing that gets cited once and then load-bears

- [x] **ANSWERED 2026-08-15 (`a`, legalised narrowly).** The README gains one exception, bounded by the reason for the rule rather than by the shape of the session: a session that reasons its way to a panel may leave a note **when the note holds what the panel file does not** — the path to the question, the inventory, the tension that produced the proposal — and it must hold **neither the proposal nor the verdict**. The test is mechanical and reads the two artifacts rather than the session's intent: if the note can be deleted without losing anything the panel file does not already say, it should not have been written. The rule's reason was never *one session, one artifact* — it was *do not record the same thing twice* — and 004 is the case where the two differ. Was | docs/reasoning/004, 2026-08-15 | **May a session that reasons its way to a panel also leave a note?** `docs/reasoning/README.md` § What is and is not a reasoning session says a `/panel` session does **not** qualify, *"because each already has its artifact… and recording it twice is noise"*. This session produced both, and they hold different things: the note holds the nine-job inventory and the two-decisions-in-one-day tension that *produced* the proposal, the panel file holds the proposal and the verdict. Neither restates the other. **What is yours**: legalise the pair with a sentence, or rule that the note should have been folded into the panel's § What the artifacts say | docs/reasoning/README.md · docs/reasoning/004-a-project-file.md · docs/panel/056 | the README is the only home for the rule, and the pair now exists whether or not it is legal

- [x] **ANSWERED 2026-08-15 (`a`, both rules).** `/panel` step 3 now says `rm -rf target build` after the copy, with the reason (`CARGO_MANIFEST_DIR` in a copied `target/` points at the real repository, so a judge's golden run silently measures the tree it was told not to touch) and with the fact that this is the **second** payment for the same lesson — panel 054 bought the copy rule, and the copy alone turned out insufficient. **And the freeze binds the coordinator**: the working tree is frozen from the moment the briefs go out until the synthesis is written. A rule that binds judges and not the seat that convened them has its hole exactly where the most privileged actor stands, which is what panel 056 demonstrated when `-Werror=missing-include-dirs` sat in `FLAGS` for eight minutes and a judge observed the effect from inside its own measurement. Was | panel 056, process, 2026-08-15 | **`/panel` step 3 should say `rm -rf target build` after the copy.** Two judges copied the tree as instructed; a copied `target/` leaves `env!("CARGO_MANIFEST_DIR")` pointing at the **real** repository, so a judge's golden run silently measures the tree it was told not to touch. The compiler-engineer found this in its own first run, discarded it and rebuilt from scratch — which is the only reason it is known. Separately and for the same rule: **the coordinator modified the real working tree mid-sitting** (`-Werror=missing-include-dirs` in `FLAGS`, 23:37 to 23:45), a judge observed the effect from inside its own measurement, and it was reverted before any commit. The rule that binds judges should bind the coordinator during a sitting | .claude/skills/panel/SKILL.md step 3 · docs/panel/056 § Process notes | panel 054 paid for this rule once already, and the copy is not sufficient on its own

- [x] **ALL FOUR REPAIRED 2026-08-15**, in the `/decide` session that read this item — and a **fifth** was found by running the same check one file further. (1) and (2) are ticked above with their verdicts written in. (3) `docs/ROADMAP.md` § Status now carries **M-binding-fidelity** and names `M-struct-passing` as next; § Done's name table already had both rows, so only the status paragraph had drifted. (4) `docs/panel/020`:5 now reads `RATIFIED 2026-08-12`, with the drift named in the line itself. **The fifth**: `CLAUDE.md` §7 and `toolchain.rs`:74 both still said *"panel 047, ratification pending"* against a ratification given 2026-08-14 — **and §7's flag list was a stale copy of `FLAGS`**, missing `-D_USE_MATH_DEFINES`, `-D_CRT_SECURE_NO_WARNINGS`, `-Werror=shorten-64-to-32` and `-Werror=sign-conversion`, all four landed in code by panels 047, 051 and 052 without reaching the contract that claims to state them. §7 now says it is a copy and of what, which is the only honest form for a duplicated list. Was | found 2026-08-15 while verifying, three at once | **Three record entries are stale, verified against what supersedes each.** (1) `DECIDE.md`:182 (the `--include` split) and (2) `:184` (struct-by-value) are unticked, but `DESIGN-LOG.md`:298-299 record both as decided and the flags are in `cli/table.rs`. (3) `docs/ROADMAP.md` § Status and § Done stop at `M-program-corpus` while tag `m-binding-fidelity` and `docs/journal/017-binding-fidelity.md` exist, and order row 1 is now `M-struct-passing`. Also `docs/panel/020`:5 still reads `provisional — author ratification pending` against its own line 461, which records RATIFIED 2026-08-12. Reported rather than repaired, because none is part of the question this session was asked | docs/debrief/DECIDE.md · docs/ROADMAP.md · docs/panel/020 | this list's first rule is *verify before asking*, and a ticked item does not track a live question — the inverse costs the same

## From panel 057 (2026-08-15) — the path pair, and what the sitting found on the way

- [x] **RATIFIED 2026-08-15 as it stands** (blanket author instruction, *"io ratifico tutto"*). Neither built-in enters, the port writes the scan once in `modules/` at 0 spec tokens, the `\`-as-separator clause is refused outright, and the Tier-2 route stays measured and available. **Three things the yes does not close, written into the panel file rather than left implicit**: `path_dir` was never on the ballot and is **not** refused by this ratification — should path work return, it starts there at +41 and not from the pair; the `.cstr()` write hazard is about the FFI boundary rather than about paths and is its own queued decision; and `directory_text` still carries the refused premise, which is now the tree's one instance of a premise a panel has ruled against. **The prediction to watch** is the ergonomist's, because it carries a counter-intuitive clause its own author offered as the falsifier — that the language with no built-in beats both proposed variants. A harness contradicting it is what reopens `path_join`. Was | panel 057, 2026-08-15 | **Ratify the sitting, or overturn it.** Full lane, five judges, all building in copies, working tree frozen throughout — the first observance of both halves of the `/panel` amendment taken the same day. **Three vetoes from three sections** (compiler-engineer §1.11, spec-warden §1.0, ffi-pragmatist §1.12) and **no seat in favour of the pair as proposed**; the two non-vetoing seats both refuse `path_join` independently. Resolution: **neither built-in enters**, the port writes the scan once in `modules/` at 0 spec tokens, the `\`-as-separator clause is refused outright, and the Tier-2 route stays measured and available at zero compiler cost. **What is yours**: the verdict. If you overturn, three things are owed and each seat named its own — the engineer wants a written answer to why §1.11's settled class (a `str?`-returning edge is Tier 2, panel 036) is reopened for a helper not on the closure list; the pragmatist requires `runtime/parts/os.c:70`'s portability claim be **struck rather than amended**, plus a `tests/fixedbugs` case named after the two-databases experiment; the warden requires the pair not land as a unit. **And note what the sitting built that no ballot listed**: `path_dir(p)` — the text up to *and including* the last separator, `""` on absence, measured **+41** — which dissolves the ergonomist's defect without needing `.` and makes `path_join` unnecessary, because the join becomes `+`. It is not adopted, because the seats that would build it vetoed the placement rather than the shape, and a shape with no agreed home is not a decision | docs/panel/057 · design.md §1.0, §1.11, §1.12 | the compiler behaves as if the refusal holds until you say otherwise, which costs nothing because nothing was landing either way

- [x] **CLOSED 2026-08-15 by `docs/panel/058`.** The flag plus a fifth exit-1 class is adopted under **five conditions**; unshare-or-copy and a mutable-buffer spelling are **vetoed twice each**; doing nothing is refused by every seat. The spec gains a **+2** rewrite rather than the +32 or +55 clause this item's own framing implied — and the expensive one turned out to *license* the use-after-free it looked like it closed. **The ratification item and the four findings that are not about the ballot are below.** Was | panel 057, measured 2026-08-15 | **`.cstr()` handed to a C function that *writes* is a live hazard, and §4.19 does not address it.** Panel 053 settled the **inbound** null `cstr`; this is the **outbound mutable** one, and it was found while pricing `dirname(3)` rather than by looking for it. Two measurements, both on this machine. (1) A **literal's** `.cstr()` is `HERO_STR_STATIC`, i.e. read-only, and a C function that writes through it **faults** — `SIGSEGV/SIGBUS`, which is design.md §1.12's *"a Heroes program must not segfault"* reachable from ordinary-looking source. (2) A **heap** `str`'s `.cstr()` **accepts** the write and leaves the `str` desynced from its own length — `len=17` over 9 bytes of content, which is worse than the fault because nothing reports it and §4.3 measures a `str` in bytes. The C functions that do this are not exotic: `dirname`, `basename`, `strtok`, `realpath`'s buffer, `getcwd`, every `_s` variant on MSVC. **What is open**: whether the spec's FFI section must say that a `cstr` handed out is not writable, whether the emitter can refuse or copy for a parameter clang reports as non-`const char *`, or whether this is §4.19's ladder rung that nobody has climbed. The parameter-side machinery from M-binding-fidelity already reads clang's view of every parameter, so the third option may be the cheapest | docs/panel/057 § Findings · crates/heroes/src/emit/ffi_narrowed.rs · design.md §4.19, §1.12 · docs/panel/053 | the FFI is this language's whole library chapter, and a `str` that disagrees with its own length is the silent half of the boundary panel 053 opened

- [x] **ANSWERED 2026-08-15 — the falsifier, and the measurement struck one of the three options before it was put.** The counterexample was built rather than argued: `a\b.hero` is **one legal file** on Darwin, and `directory_text` reports its directory as `a\`, which does not exist. That kills option three — *record that the display-name case is exempt* — because the printed name is **wrong when the file exists**, so there is nothing harmless to record. Option two (drop `\`) degrades the diagnostic for the case the clause was written for. What landed is option one, §11's own prescription: the premise is now a **claim that can die**, written as one — *a path reaching this function was typed by the author and may use the Windows separator whatever the platform, and spelling a sibling the way the author spelled the root is worth more than a POSIX file whose name contains `\`* — with the cost stated rather than hidden. **What makes it survivable is scope, and the test pins scope**: `a_backslash_name_is_wrong_here_and_right_where_files_are_opened` asserts the naming half is wrong, the filesystem half (`PathBuf::parent`) is right, **and that the two disagree** — so it fires the day this result reaches a real path, which is the only way the cost stops being cosmetic. Was | `crates/heroes/src/modules/mod.rs:91` does `path.rfind(['/', '\\'])` on **every** platform, and panel 057 refused exactly that premise — `\` is a legal filename byte on Linux and Darwin, and the pragmatist opened two different SQLite databases through it with no diagnostic. **The blast radius here is genuinely small and the function's own comment says why**: it decides the *display name* of a sibling module, and the filesystem is still asked through a real `Path`, which accepts either separator everywhere. So this is not the two-databases defect. But it is the same premise, and it is now the only instance of it in the tree after a panel refused it — which is the shape CLAUDE.md §11 says expires in silence while the comment goes on reading as correct. **Three options**: carry a falsifier and a test that fires when it dies (`dir/a\b.hero` on Darwin, asserting the resolved name matches the filesystem — the historian says this is settleable *today*, before anything lands); drop `\` and accept that a Windows author's typed backslash produces a worse diagnostic name; or record that the display-name case is exempt and say why, in the function's own comment. **Not urgent and not a defect today** — filed so the next reader does not have to re-derive that a panel already ruled on the premise | crates/heroes/src/modules/mod.rs:79-96 · docs/panel/057 § What the `\` clause actually costs | a premise a panel refused, surviving in one place because its consequences there are mild, is how a refused premise comes back

## From panel 058 (2026-08-15) — the outbound mutable `cstr`

- [x] **RATIFIED 2026-08-15 as it stands** (blanket author instruction, *"rattifica tutto"*). (i) adopted with **all five conditions binding**; (ii) and (iii) stay refused on their two vetoes each; (iv) refused; the spec takes the **+2** rewrite. **This ratification converts a resolution into a work order, which is what makes it different from the day's other three** — 057 ratified a refusal and nothing was owed afterwards, while here the yes authorises five conditions and, until they are met, **the silent corruption is reachable in a ratified language**. That is stated rather than left for the tick to imply. **Condition 1 must land first and separately** — the assertion zero becomes the bare `0` — because it is a repair on its own merits today and because the obvious alternative was built and reproduced a regression on a correct `const char **` binding. **The prediction to watch** is the ffi-pragmatist's: checkable at any time, offered by its own author as a withdrawal condition — all 28 of ncurses' plain-`char *` entry points run under the flag with zero shims, and one failure means the refusal is wrong. **On the historian's dissent**: ratifying the refusal of (iii) does **not** ratify the claim that a separate mutable-buffer type is wrong — §13 forbids it *now*, and the recorded disagreement is about the clock. Was | panel 058, 2026-08-15 | **Ratify the sitting, or overturn it — and note that nothing has landed yet, deliberately.** Full lane, five judges. **(i)** the flag plus a fifth exit-1 class is **adopted under five conditions**, and three seats set them independently with one stating that without them its verdict is `veto`; the warden's is the same rule from the spec's side — *landing the flag without the diagnostic class trades a silent corruption for an unactionable exit 2, a net §1.2 loss*. **(ii)** unshare-or-copy: **two vetoes**, on the measurement that the ownership pass's `$own` slot already holds a second reference, so the copy is unconditional (§4.10's own refutation of in-place `push`) and does not fix the unique case. **(iii)** a mutable-buffer spelling: **two vetoes**, Part 7 item 10 verbatim under §13, 400–600 lines — with the historian's dissent recorded, because four ecosystems converged on exactly that answer and OCaml's took three years, so the disagreement is about the **clock**. **(iv)** refused by every seat. **The spec takes a +2 rewrite** — *"`s.cstr()` **lends** a `str` to C to read"* — and not the +32/+55 clause, whose *"may read and never write"* **licenses** the pointer C keeps. **What is yours**: the verdict, and then whether the five conditions are worked in one step or spread. Condition 1 (the assertion zero becomes the bare `0`) is a fix on its own merits **today**, with or without the flag, because the emitter currently warns about its own generated `_Static_assert` | docs/panel/058 · crates/heroes/src/emit/extern_assert.rs · crates/heroes-cli/src/commands/flags.rs | the compiler behaves as it did before until you say otherwise, which means the silent corruption is still reachable — that is the cost of the conservative default here, and it is stated rather than hidden

- [x] **FIXED 2026-08-15** in `33ebee7`, three lines: the narrowing now excludes `SlotKind::Param { mutable: true }`, and panel 053's guard got the **first test it has ever had** — pinning both directions, since without the second assertion the repair is invisible and without the first, deleting the guard entirely would pass. `tests/golden/fixedbugs/ffi-out-parameter-guard.hero` carries symptom, cause and date. Ticked here because a ticked item that tracks nothing and an unticked one that tracks nothing cost the same. Was | panel 058, found 2026-08-15 by measuring something else | **`guard_cstr_arguments` is vacuous on an `@cstr` out-parameter, and it compiles only because a warning is not an error.** Panel 053's null guard (`emit/ops.rs:205-232`) narrows on the declared type `Ty::Cstr` and ignores `SlotKind::Param { mutable: true }`, so an `@cstr` is emitted as `hero_cstr_nonnull(&h0_e)` — **the address of a local, which is never null**. The guard fires and proves nothing. Reproduced by the compiler-engineer on unmodified `main` at exit 0, with two `-Wincompatible-pointer-types` warnings that are not errors because that flag is not in `FLAGS` as `-Werror=`. **~3 lines**, and it is CLAUDE.md §11's rule in miniature: the narrowing asks the declared type instead of the slot in hand. Independent of panel 058's ballot and worth doing whatever is decided there | crates/heroes/src/emit/ops.rs:205-232 · docs/panel/053 · crates/heroes-cli/src/commands/flags.rs | a guard that cannot fail is worse than no guard, because the next reader believes it

- [x] **CLOSED 2026-08-15 by `docs/panel/059`**, ratified. The clause is in the spec at +25 and states three facts the reader needed and one the item did not ask for: `to_str` **copies**, a null **aborts**, and the guard is `c == nullptr` **first**. Both questions this item put are answered — the spec names `to_str`, and the ergonomist's ranking was **confirmed and sharpened**: the read direction does cost more first-try failures, and the cheap way of documenting it is worse than silence. **One claim in this item was false and is corrected rather than carried**: `print(got)` on a `cstr` does not print an address, it is `bad_operand` at exit 1 — measured by 059's warden. Was | panel 058's blind seat, 2026-08-15 | **The spec documents no `cstr` → `str`, and a reader following it cannot write `getcwd` — this is panel 048's finding with half still unrepaired.** The llm-ergonomist, reading only the spec, was blocked on the same task in **all three** variants, and not on the write rule: it holds C's answer and has no documented way to turn it into a `str`. `print(got)` may print an **address**, since the spec's `==` line says a `cstr` compares as one — a silent wrong output. Verified after the sitting: **`got.to_str()` works and prints the directory.** Panel 048 recorded exactly this class — *"the language is wider than its specification in two places at the FFI boundary"* — and the inbound `cstr` went to panel 053, which repaired `nullptr` and `==` but left `to_str` undocumented. **Two things to decide**: whether the spec names `to_str` on a `cstr` (measure it — the FFI section is where the decision is made, §1.3), and whether the ergonomist's second prediction is accepted as the ranking it implies, that the **read** direction costs more first-try failures than the write direction this panel was convened about | spec/heroes-spec.md § FFI · docs/panel/048, 053, 058 | a spec that is narrower than the language teaches a reader to guess, and guessing at the C boundary is what §1.12 is about

- [x] **CLOSED AS AN OPEN CONDITION 2026-08-16 — it is still standing, and it now FIRES.** It stays a condition and is off this list, because what it was waiting for was never an answer: it was waiting for someone to re-read a list. CLAUDE.md §11 says a premise that can expire is owed *a test that fires when it dies, whose failure message names what depends on it*, and the item was prose. It is now `no_binding_needs_a_length_back_from_c` in `crates/heroes-cli/tests/corpus.rs`, written as the falsifiable claim it always was — **no program in this repository binds a C function that reports a written length** — over ten named contracts (`snprintf`, `readlink`, `strlcpy`, `recv`, …). **Verified to fire**: a probe binding `readlink` turned it red naming the file and the line, and its message sends the reader to `docs/panel/058` § Findings and design.md Part 7 item 10; removing the probe turned it green. It is deliberately a **proxy** — it fires on the binding rather than on the need, so it can fire early, and early is the right direction: a sitting convened one binding too soon costs an hour, one convened too late costs a shim that outlives the milestone. Was | **STANDING REOPEN CONDITION, not a question** (clarified 2026-08-15) — it needs no answer and is kept because something must fire when it comes true. panel 058, ffi-pragmatist | **A buffer whose length Heroes knows has no spelling, and that is the condition under which the vetoed option returns.** `malloc` + `ptr` is the route the sitting adopted for every C function that writes into a caller buffer, and it works — `getcwd`, `strtok`, `putenv`, ncurses `tigetnum` all run under the flag with zero shims. **What it gives back is no length.** So `snprintf`'s truncation return and `readlink`'s no-NUL result have no correct spelling in this language today: the program cannot learn how many bytes C wrote. This is filed as the falsifiable condition on panel 058's refusal of option (iii) — a mutable buffer type returns to the panel the moment a binding the closure list or §4.19's ladder needs requires a length back, and it is **not** a wish list item until then | docs/panel/058 § Findings · design.md §4.19, §1.12 · Part 7 item 10 | the sitting closed the corruption and named the hole it did not close, which is the half a synthesis usually loses

## From panel 059 (2026-08-15) — the read direction

- [x] **RATIFIED 2026-08-15 as it stands** (blanket author instruction, which also delegated the judges' recommendations — so the two things owed with the clause are work rather than questions and land without returning here). A stays refused on the measurement that condemned it; C stays vetoed, and ratifying that **does not rule a fallible conversion wrong** — it rules that `to_str` may not be the name carrying it, so the historian's second-name form survives unconsumed. Was | panel 059, 2026-08-15 | **Ratify the sitting, or overturn it.** Full lane, five judges. The clause lands at **+25 measured**, folded into the FFI sentence already there: *"`s.cstr()` lends a `str` to C to read and `c.to_str()` copies one back — test `c == nullptr` first, because converting one aborts."* **A (+11, name the conversion alone) is refused on a measurement** — it is worse than saying nothing, because spec:58's naming law then asserts that a null converts. **C (fallible `to_str`) took a veto** plus an objection no wording answers: `tigetstr` returns `(char *)-1`, so a `str?` promises what the boundary cannot deliver. Its reconciling form — a **second name**, Swift SE-0405's shipped shape — is recorded rather than struck. **What is yours**: the verdict, and the two things owed with the clause, which are conditions and not decoration: a **run-golden** for the inbound abort and for a guarded `sqlite3_column_text` on a SQL NULL (`tests/golden/run/` has zero coverage of the read direction today), and a **route on `bad_operand`** for a `cstr` — measured, `print(v)` stops with no `Fix` naming `to_str`, and that hard-stop-with-no-route is what produced panel 058's `static char[4096]` | docs/panel/059 · spec/heroes-spec.md § FFI · crates/heroes/src/measure/gate.rs | the clause is in the spec as of this commit, so the compiler and the document agree for the first time at this boundary

- [x] **ANSWERED 2026-08-15, and the experts' own position is what it took** (author instruction delegating them). Two halves, decided differently. **The message is repaired now**: it said *"C would write through it"*, which is **false on the first function anyone tries** — `free` deallocates, `putenv` retains — so it now says *"C does not promise to leave it alone"*, which is what a header actually declares. Only the header knows which permission is exercised, and asserting a mechanism where only a permission is known is CLAUDE.md §11 pointed at prose. **The leak is recorded as design.md Part 8 wart 18 with the §12 falsifier**, not fixed: §4.19 already names an ownership vocabulary as future work, and the judges' position was that this is the program making it concrete rather than a defect to patch now. What is new and recorded is that the refusal is **new** and the missing `cstr → ptr` conversion is **old** — closing half a door is how it became reachable. Falsifier: a binding on §4.19's ladder or the closure list that must free what C allocated; measured today, there is none. Was | panel 059, reproduced 2026-08-15 | **A `cstr` that C malloc'd cannot be freed in any spelling, and panel 058 closed half the door.** Both reproduced on the tree as it stands: `free(p: cstr)` is now `error[ffi_writable_parameter]`, and `free(p: ptr)` is `error[type_mismatch]: expected `ptr`, found `cstr``. So `readline`, `strdup` and `sqlite3_expanded_sql` **leak on every call** — and the ffi-pragmatist measured that `--sanitize` **and** `hero_runtime_check_leaks()` both report nothing, exit 0, because the block belongs to C and neither instrument looks there. §4.19:2062 already names the ownership vocabulary as future work; what is new is that it is **reachable today** and that a diagnostic landed yesterday is half the reason. **Two smaller things ride with it**: `ffi_writable_parameter`'s message says *"C would write through it"* for `free`, which **deallocates** rather than writes — the refusal may be right and the sentence is not; and a `cstr` cannot become a `ptr`, which is the conversion that would make the correct spelling available. **What is open**: whether the repair is a `cstr → ptr` conversion, an ownership marker on the declaration (§4.19:2062's own direction), or a refusal with a falsifier | crates/heroes/src/emit/ffi_mutable.rs · design.md §4.19:2062 · docs/panel/058, 059 | the FFI is this language's whole library chapter, and a binding that leaks silently is the half §1.12 was written for

- [x] **RECORDED 2026-08-15 — it asks nothing, and that is why it is ticked rather than carried.** The finding is sourced and lives in `docs/panel/059` § Findings, which is where a future sitting on the C boundary will look. It is **not a proposal**: Principle 0 does not fund it, no program needs it, and this list is for decisions the compiler is waiting on. An item that poses no question is the shape this list's own first rule warns about from the other side — it costs a read on every pass and yields nothing. The record keeps the four precedents (Clang 2015, Swift 3, Zig, Ada 1995) and the fact that they were **seen and not taken**. Was | panel 059's historian, sourced 2026-08-15 | **Nullability belongs to the declaration, and four systems say so independently — it was on nobody's ballot.** Clang's `_Nonnull`/`_Nullable` with audited regions (r240156, 2015); Swift SE-0055 making `UnsafePointer<Int>?` the nullable form and mapping `_Nullable` → `Optional` at import (Swift 3); Zig spelling it in the **return type**, `std.posix.getenv(key) ?[:0]const u8`; Ada's `Null_Ptr` as a *value of the pointer type* since 1995, with `Value` raising precisely because the programmer could have tested first. Every abort-camp precedent pairs the abort with a testable null — which Heroes has, verified during the sitting. **Not a proposal**: Principle 0 does not fund it and no program needs it today. Filed so a future sitting on the C boundary starts from this shape instead of rediscovering it, and so the record shows it was seen and not taken | docs/panel/059 § Findings · design.md §4.19 | the best findings in this project have been the option that was never on the ballot, and this one arrived after the vote rather than before it

## From panel 060 (2026-08-15) — how a struct crosses, and what the sitting found on the way

- [x] **RATIFIED 2026-08-16** (author instruction *"ratifica e sistema tutto"*, the fifth blanket of its kind; the recap of what had already landed was verified against the tree first, per CLAUDE.md §1). All three parts stand: the mechanism (**a group's `record` IS the header's struct** — no typedef, name unmangled, `_Generic` on each field's **address**), the **complete** field list, and the **−22 §12 repair** that funded the clause. **Two things the yes does not confirm, because the author already overturned them in the other direction, and a ratification that quietly re-asserted them would rewrite the record**: (a) *"no `f32`"* is **dead** — `f32` entered 2026-08-15 by author instruction (*"e aggiungi il tipo f32 basta storie"*, DESIGN-LOG:322), as `Ty::Float(FloatKind)` rather than a bare variant, on the panel's own 2-errors-19-silent-sites measurement, with `HERO_RUNTIME_ABI` 13 → 14; (b) the **`f64`-over-`float` field clause** — the very sentence 060 called *"the one clause that makes the last two survivable"* — was **removed the same day under §12** (DESIGN-LOG:324), because once `f32` exists the field assertion asks type identity and the spec was permitting what the compiler refuses. So what this ratifies is the sitting minus the deferral its own author overturned, and the record says which half died rather than leaving a reader to reconcile four documents. Was | panel 060 | **Ratify the sitting: the header's type, a complete field list, no `f32`, and the one clause that makes the last two survivable.** Five judges, one **veto** (ffi-pragmatist, on option B, and the veto binds nothing anyone adopted), two **object**s that were both about *how* rather than *whether*, and no seat defending the status quo. The provisional default is in `docs/panel/060` § The provisional resolution and work proceeds on it. **What is yours, and it is one yes with three parts.** (1) The mechanism: a `record` inside an `extern` group *is* the header's struct — no typedef emitted, the name unmangled, each field checked by `_Generic` on the field's **address**. B was vetoed on a measurement rather than a principle: `{float; float}` against `{int32_t; int32_t}` **is** `Vector2`, identical `sizeof` and identical `offsetof`, every assertion B can emit green, and it arrives as `(0.0, -1.13e35)` on arm64 because the two travel in different register files. C is Haskell's answer, held sixteen years, and unaffordable here because §10 forbids `hsc2hs`. (2) The field list is **complete**, with partial queued rather than refused — and the record says the reason is **Heroes-side, not ABI-side**, because partial-plus-by-value was *measured safe to read* (`SDL_Event`, 1 member of ~40, real `SDL_PollEvent` round trip) and SEGVs only on construction. (3) `f32` stays struck **and the milestone still delivers**, because a field declared `f64` over a header `float` is ABI-safe under this mechanism — real `CheckCollisionPointRec` and `GetSplinePointLinear`, exact. raylib goes 252 → **589 of 601**. Spec **+56** (the warden's wording, not the proposal's +137), paid for by a **−22 removal that is also a §12 repair** | docs/panel/060 · design.md §1.12, §4.19 · spec § FFI | the FFI is this language's whole library chapter, and the sitting convened to open a third of it found its own proposal's clause would have opened a ninth
- [x] **ANSWERED — it landed, and the answer was `now`.** Verified in the tree rather than recalled: `partial` is a marker parsed in one position (`syntax/data.rs:74 partial_marker`, so `partial` stays an ordinary identifier everywhere else — a `record partial` with a field named `partial` compiles), the refusal is transitive through `types/partial.rs`, and `spec:216` carries it. Panel 061 adopted it on 2026-08-15 and its whole work order was ratified the same day (this file's item at :294). So the disagreement resolved to the ffi-pragmatist's timing, not the compiler-engineer's — and it resolved by the sitting that measured the thing rather than by this question being answered. **What made the yes affordable is the part worth keeping**: 061 found `spec:211`'s *"all its fields"* was enforced by nothing (`SDL_Event`, 1 member of 41, exit 0), so Q1's real job turned out to be making **complete** checkable, through a never-called positional probe `Font f = {0,0};` that `-Wmissing-field-initializers` names by field at zero spec tokens. Was | panel 060, ffi-pragmatist vs compiler-engineer | **The partial field list: adopt it in this milestone, or after it?** This is the sitting's one real disagreement and both seats measured, so it is a question about **timing** and not about facts. Both agree partial is safe to **read** and fatal to **construct**: `Font h = {.baseSize=20, .glyphCount=95}` is well-defined C that zero-fills the pointers, and real `MeasureTextEx` gives exit 139 — `AddressSanitizer: SEGV on unknown address 0xc` — which is §1.12 violated starting from legal C. The ffi-pragmatist wants it **now**, because without it SDL3's real surface (`SDL_Event` is a 128-byte `union`) is unreachable and raylib stops at 337 of 349. The compiler-engineer priced it as **a second kind of type** rather than a flag on a declaration: `emit/perfn.rs:60-61` emits `_eq`/`_hash` for every aggregate unconditionally and `descriptors.rs:270` puts `_hash` in the `HeroDesc`, so a partial record needs a second arm in `perfn::every_generated_type`, a second table in `descriptors::generated`, and a refusal at every container site — and a dropped `hash` is the null descriptor panel 022 measured as `SEGV on unknown address 0x0, pc 0x0`, no type name and no line. The provisional default takes **complete first because it is the reversible order**. What a yes to partial buys is written and does not need re-deriving: permitted to read, copy, pass and return; refused at exit 1 as a construction target, as an `==`/`hash` operand, and as a map key | docs/panel/060 § Q2 · crates/heroes/src/emit/perfn.rs:60 · crates/heroes/src/emit/descriptors.rs:270 | the ballot offered two options and every seat that looked found the same two facts, which cut across both
- [x] **ANSWERED 2026-08-15 by author instruction, which overtook the question.** `f32` did **not** get its own sitting; it entered by decision (*"e aggiungi il tipo f32 basta storie"*), and the shape it entered in is this item's own measurement rather than the instruction's: no `Ty::F32`, but **`Ty::Float(FloatKind)`**, because a bare variant beside `Ty::F64` measured **2 rustc errors and 19 silent sites across 15 files** — and on landing the rename produced exactly 2 exhaustiveness errors, so nineteen sites had been sitting inside a `_`, two of them silently wrong at exit 0 (`crosses_the_boundary` answering *"an `f32` cannot cross"*, `structural.rs` generating an `==` that skips the field). `HERO_RUNTIME_ABI` 13 → 14 as priced. Spec +56. The item's own diagnosis was right and is why the instruction was correct: **inside a struct there is no prototype**, so the width that is a convenience at a signature is the layout at a field. Was | panel 060, Q3 | **`f32` returns as a precision question, and it needs its own sitting rather than a re-vote.** Panel 052 struck it because a C `float` **parameter** bound `f64` is bit-exact — true, because clang converts against the real prototype. **Inside a struct there is no prototype**, and the warden measured it: `record Vector2 { x: f64, y: f64 }` over the header's `Vector2` compiles at exit 0 with zero diagnostics and `_Static_assert(sizeof(((Vector2*)0)->x) == sizeof(int32_t))` **passes**. So 052's funding measurement does not reach fields, and its own condition — *"A1 returns the day a program needs a `float` value Heroes must hold"* — is met: of 157 blocked entry points **116 only take** a float-struct, so the program must construct one. What removes the *urgency* is the `f64`-over-`float` field rule, which is ABI-safe under this mechanism and takes raylib to 337 of 349 without a new `Ty` variant. What stays open is **precision**, and the price is known: `Ty::F32` gives **2 rustc errors and 19 silent sites** across 15 files — the same numbers `types/widths.rs`'s own module doc records for the integer widths — with shape (i) at 400–550 lines over 20+ files in two languages and **`HERO_RUNTIME_ABI` 13 → 14**, invalidating every cache key and every golden; shape (ii), boundary-only, is *narrower and not cheaper*, ~20% saved, and the historian found it shipped three times (Dart's `Float`, `ctypes.c_float`, Erlang's bit syntax) | docs/panel/060 § Q3 · docs/panel/052 · crates/heroes/src/types/widths.rs · crates/heroes/src/emit/extern_assert.rs:139 | the milestone that would have been crippled by the deferral is no longer crippled by it, which is what makes deferring honest instead of convenient
- [x] **DECIDED 2026-08-16 (author, blanket): the second option — §7 stops enumerating.** Verified in the tree today: `FLAGS` is `[&str; 13]` (`-g` joined it at M-selfhost-port step 1), and CLAUDE.md §7 no longer states a count at all — it names `-std=gnu11` as the one flag it argues about and deletes the copy, saying so in its own words: *"the copy that used to stand here is deleted rather than corrected a third time"*, with the 3h16m the copy survived written into the paragraph. So the drift this item recorded cannot recur in that direction: there is one list, in `flags.rs::FLAGS`, and the contract cites it. **The count moved again after the item was written** — eleven, twelve, now thirteen — which is the item's own argument arriving a third time and is why the enumerate-again option was refused. Was | panel 060, found twice independently (spec-warden and ffi-pragmatist) | **`FLAGS` has twelve entries and `CLAUDE.md` §7 says eleven.** `-Werror=incompatible-pointer-types-discards-qualifiers` landed with panel 058 on 2026-08-15 and did not reach the contract that claims to state the list. This is the **second** time the same drift has been repaired in the same list — `DECIDE.md`:255 records the first, on 2026-08-15, when §7 was missing four — and the file that was created to stop it carries the note in its own module doc. The ffi-pragmatist's sentence is the one to keep: *"the drift resumed the day the file was created."* Decide whether the copy is repaired again or the sentence in §7 stops enumerating and cites `flags.rs::FLAGS` alone | crates/heroes-cli/src/commands/flags.rs:28 · CLAUDE.md §7 | a list two documents claim to state is a list that will disagree, and this one has now disagreed twice in one day
- [x] **DONE 2026-08-16 (panel 071 E), and the answer was not the rows.** The warden showed § Types is a **glossary, not a predicate** — grepped, nothing in the document reads off it, and `spec:214`'s field list names both types itself — so two rows closed no hole and would have left `()`, nominal types, function types and `i32[4]` out, which is panel 069's instrument. **What was actually wrong is placement**: `p: ptr @ nullptr` and `record Handle { p: ptr }` compile at exit 0 **with no `extern` group in the file**. So the rows landed *with* the deletion of `spec:222`'s definition clause — **+4 net against a −23 removal** — and repaired a second thing free: that clause gave `nullptr` to `ptr` alone while `c: cstr @ nullptr` compiles. **Two ballot wordings were measured FALSE** (both *"they are § FFI's"*, +31 and +15) and pricing them is what caught it. **Adopted at zero tokens**: no sentence in this spec may refer to *"this document's types"* — grep-enforceable, and the phrasing that nearly cost panel 060 130 of 192 raylib entry points. Was | panel 060, llm-ergonomist + spec-warden | **`ptr` and `cstr` are not in § Types, and the record clause sends the reader straight there.** The table ends at `T?`; both types are used in the `==` bullet 160 lines before § FFI defines them. Measured consequence, and it is not cosmetic: read literally, *"fields are this document's types"* **forbids a pointer field**, and **130 of the 192** no-`f32` raylib entry points involve a struct with one — so the clause ships **62** instead of 191. The warden's rewrite names `ptr` and `cstr` in the field list, which repairs the clause; what stays open is whether the **table** gains the two rows, which is a separate and larger question about what § Types is for | spec/heroes-spec.md:44-55 · docs/panel/060 § The clause the brief offered | the blind reader wrote `data: ptr` anyway and logged it as a guess, which is the shape of a rule nobody can follow being followed
- [x] **BOTH HALVES CLOSED 2026-08-16.** Friction (1) was found **stale** while briefing panel 071 — `take_codepoint('A')` against an `i32` parameter checks clean at exit 0, because a character literal takes its context's type (`spec:62`), almost certainly M-sized-integers' doing after the item was written. Friction (2) went to that sitting and **the framing turned out to be wrong**: the question was where `_ = f(x)` is *documented*, and the warden's answer is that **the rule was never documented at all** — six shapes measured, all exit 1 `discarded_value`, and grep for *"discard"* returning only the escape hatch. `design.md:1718` carried it and called it *"the highest-frequency LLM habit error under value semantics"*. The rule is in § Bindings now. Was | **HALF STALE, measured 2026-08-16 — friction (1) is gone and nobody ticked it.** `take_codepoint(cp: i32)` called as `take_codepoint('A')` **checks clean at exit 0** today, and so does `d: i32 = 'A'`: a character literal takes its context's type like every other integer literal (`spec:62`), so the `'A'.to_i32().must()` the item describes is no longer needed. Almost certainly M-sized-integers' doing, and the item was written against the state before it. **Friction (2) stands, verified**: `_ = f(x)` is documented at `spec:121`, inside § Control flow, in a bullet about `_` as a **pattern** — *"`_` names anything you do not use: a payload, a parameter, a binding (`_ = f(x)`)"* — which is where a reader looking for *how do I discard a result* will not look. That half is a placement question worth its own answer and is what remains of this item. Was | panel 060, llm-ergonomist | **Two frictions every FFI program meets and § FFI never mentions.** (1) **Character literals are `i64` and C codepoint APIs are `int`**: `GetGlyphIndex(font, 'A')` is a compile error and wants `'A'.to_i32().must()`. It is **loud**, so it is a win by this project's standard — but it will hit essentially every binding a reader writes, and one sentence in § FFI with that exact spelling saves a round trip each time. (2) **`_ = f(x)` is documented under `match`** — the only way to discard a result, filed in the control-flow section's bullet about `_` *patterns*, where a reader looking for it will not look. Decide whether either is worth spec tokens, or whether both are the guide-book's job | spec § Tests and holes, § Control flow · docs/panel/060 § Free findings | the seat that only reads the spec found both while doing tasks, which is the only way either could have been found

## From panel 061 (2026-08-15) — the fields nobody named

- [x] **RATIFIED 2026-08-15 (*"RATIFICO"*), and the whole work order is done: the sweep (`9354cd8`), the completeness probe (`d22ffe9`), `partial` plus `spec:64` (`495982c`), and the two no-escape refusals the re-measurement found (`9d8dcda`). Spec 3329 → 3360, +35 against a −4 §12 dedup.** Was | **Ratify the sitting, and note that it is a work order rather than a yes.** Five judges, three **object**, one **veto** (spec-warden, on spelling A — whose own sentence declares the spec's `Point(x: 3, y: 4)` a compile error), one **veto threatened** (ffi-pragmatist, if construction is refused). The provisional resolution is in `docs/panel/061` § The provisional resolution and **its ORDER is the main instruction**: five swept sites and the completeness probe land *before* partial, because without them the new refusal has no control arm and its golden would pass vacuously. What is yours: spelling **B**; construction **permitted** with completeness enforced by a positional probe instead; `==`/`hash`/map key refused **transitively** while construction stays **per-declaration** (the transitive default would make raylib's own `RenderTexture` unbuildable); `_eq`/`_hash` still emitted as `hero_panic` bodies so no descriptor holds a null | docs/panel/061 · spec § FFI | the sitting was called to pick a spelling and found the feature already shipping, unmarked and unchecked
- [x] **CLOSED 2026-08-15 (`d22ffe9`)** — `error[ffi_incomplete_record]` at exit 1 on the author's line, at **zero spec tokens**, through a *positional* probe in a local `#pragma` region (a flag in `FLAGS` would fire on the emitter's own `= {0}`, panel 021's rule). Then it had to be repaired twice more the same afternoon: it mis-counted a **nested** record through C's brace elision, refusing a `Camera2D` that named all four fields (`9d8dcda`). Was | **A silent wrong answer is shipping right now, and it is `M-struct-passing`'s.** `record Color` with three of raylib's four fields: every `_Static_assert` green, zero warnings, **exit 0**, and `ColorToInt` returns `0xFF000000` where C returns `0xFF0000FF` — a transparent colour where the program asked for an opaque one. `spec:211` says *"all its fields"* and **nothing enforces it**. Reproduced on today's tree. The repair is the positional probe — `Font f = {0,0};` under `-Wmissing-field-initializers` names the missing field, at **zero spec tokens**, while the designated form the emitter writes warns nothing even under `-Wextra`. This is not waiting on the partial-list decision: it is wrong today, for a program that does not use partial at all | docs/panel/061 § The finding that reframes the ballot | the class this language exists to make impossible, shipped by the milestone that closed this morning
- [x] **REPAIRED 2026-08-15 at exactly 0 tokens (`495982c`)**: `- A character literal is an integer: 'a', '0', ' '.` The candidate eighth — `add == add` running while functions are in neither list at `spec:71` — is **reported and not spent**, on the warden's own advice. Was | **The seventh false sentence, and it is about the next milestone.** `spec:64` reads *"Character literals are `i64`"*. Measured: `b: u8 @ 'a'` prints **97**, `c: i32 @ 'z'` prints **122** — a character literal is context-typed like every other literal (`spec:62`). The direction is what makes it urgent: a reader who believes the spec thinks `s[i] == 'a'` is a type error, and that is **the most common idiom in a lexer, which is `M-selfhost-probe`'s whole deliverable**. Repair measured at **0 tokens** — `- A character literal is an integer: 'a', '0', ' '.` And a candidate eighth, reported rather than spent: `add == add` runs and prints `true` while functions appear in neither list at `spec:71` — panel 053's `cstr` class, one type later | spec/heroes-spec.md:64, :71 · docs/panel/061 § Three false sentences | six have been repaired and five were false in the silent direction; this one is false in the direction the next milestone reads first
- [x] **BOTH HALVES CLOSED — the second was fixed at panel 062 and verified in the tree today.** `printer/dump.rs`'s `Record` arm destructures `header`, `library` and `partial` and prints the group head through `extern_prefix` (`dump.rs:93-94`), so `assert_canonical` can no longer be green about a hoist. The arm's own comment now carries the measurement that proves it fires — with the `..` restored, `dump` of `extern "raylib.h" package "raylib"` + `record Font` and of a top-level `record Font` were **the same string**. Confirmed by hand: `heroes fmt` on a program with an `extern` group containing a `record` prints the group head, and the dump agrees. Was | **HALF CLOSED 2026-08-15 (`495982c`)**: `fmt_decl.rs` and `dump.rs` both grew the `partial` arm in the same commit as the word, so that field is safe. **The other half is open and was found by the audit**: `dump.rs`'s `Record` arm still elides `header`/`library`, while `fmt_decl.rs` prints a record's group head — so `assert_canonical` is still green about a **hoist**, which is the exact blindness panel 060 repaired `fmt_extern.rs` for. Repairing `fmt_extern.rs` did not restore the guard. What is still to decide is the engineer's own question: whether the round-trip guard needs a **structural** rule — every `DeclKind` field a formatter can print must appear in `dump` — rather than an arm at a time. Was | **`heroes fmt` deletes `partial`, and the test that exists to catch that is blind in the same place.** `printer/tests/mod.rs::assert_canonical` asserts `dump(text) == dump(fmt(text))` — *"fmt changed the tree"* — but `printer/dump.rs:69` drops the word too, so **two blind spots cancel into a green test**. Proved both ways: unchanged the probe passes; with one arm added to `dump.rs` it fails. This is panel 060's `fmt_extern.rs` hoisting one milestone later, in the same file family, with the round-trip guard complicit — and it is the second time this class has been found by a judge rather than by a test. Decide whether the round-trip guard needs a structural rule (every `DeclKind` field that a formatter can print must appear in `dump`) rather than another arm | crates/heroes/src/printer/dump.rs:69 · crates/heroes/src/printer/tests/mod.rs · docs/panel/060 § condition 1 | a formatter that silently deletes a safety marker is the worst shape this defect class takes
- [x] **CORRECTED 2026-08-15 (`9d8dcda`), and the re-measurement found more than it was sent for.** raylib declares **600** entry points, not 601 — `grep -c "^RLAPI"` and clang's own `FunctionDecl` count agree, so every entry-point figure in panels 060, 061 and journal 018 was one too high. The typed-pointer question **was** worth its own answer and it landed: `ptr` now asks *is this a pointer* rather than *does the header spell it `void *`*, swept across all 139 fields of all 35 raylib structs — 28 typed pointers move to PASS, and all 14 arrays, 96 scalars and 26 nested records stay refused, the arrays by `_Generic`'s own decay rather than by `sizeof`, which matters because six of them are `float[2]`, exactly `sizeof(void *)`. **What is still open is a third blocker class neither sitting named**: an SDL3 `enum` field. `_Generic(&((SDL_Surface *)0)->format, int32_t *: 1, default: 0)` **fails**, because a C enumerated type is a distinct type from its compatible integer — 35 such fields across the 57 structs SDL3's entry points name, 9 structs blocked by enums alone. raylib spells every enum-valued member `int`, which is why no measurement taken on raylib could have found it: row `2675`'s finding again, in the shape of a corpus with no arm for the question. Was | **Panel 060's numbers were wrong and the record needs them corrected rather than quietly superseded.** raylib as *actually shipped* is **226 of 349** by-value crossings, not 337: `_Generic(&((Font*)0)->recs, void **: …)` **fails**, because `Rectangle *` is not `void *`, so `Font.recs: ptr` is `error[ffi_field_type]` with no type to correct it to — 21 of raylib's 40 unbindable fields are typed pointers, blocking `Font`, `Mesh`, `Shader`, `Model`, `Sound`, `Music`, `AudioStream`, `FilePathList`, `ModelAnimation`. SDL3 has **8** by-value crossings, not 15, and **7 need `SDL_GUID`, so partial buys 0 of them** — what it buys is the addressable local, 178 → 235 entry points. The array residue does not dissolve, it concentrates: 3, all `VrStereoConfig`. Decide whether a **typed pointer field** (`ptr` that binds any object pointer, not just `void *`) is its own question — it is worth 111 crossings, more than partial | docs/panel/061 § The numbers, corrected · docs/journal/018 | the milestone's headline number was measured against a mechanism that had not shipped

## Open — the last three of raylib, measured 2026-08-15

- [x] **CLOSED — it landed at panel 062 (author instruction 2026-08-15, *"è fondamentale chiamare il C, altrimenti non andiamo da nessuna parte"*), and this box was never ticked.** Verified in the tree today rather than recalled: `Ty::Fixed(TyId, u32)` is a **separate variant** from `Ty::Array` — they cannot share a representation any more than `str` and `cstr` can — the spelling is C's with the bracket after a complete type (`leftLensCenter: f32[2]`, which a reader turns into the header line by deletion), and `tests/golden/run/ffi-a-c-array-member.hero` records the outcome: **all 35 raylib structs and all 349 by-value crossings, complete field lists, zero `partial`**. `VrStereoConfig` is what proves this item's own number was right — every one of its eight members is an array, so it had no nameable field at all and gave `empty_record` rather than a binding. Three diagnostics came with it (`fixed_outside_a_group`, `fixed_array_length`, `fixed_index_out_of_range`), the elementwise `eq` replaced C's `a->arr == b->arr` (legal C, zero warnings, and a **pointer** comparison — two byte-identical structs answered `false`), and the bound is the **compiler's**, because an inline array carries no length header and AddressSanitizer is documented blind to an intra-object overflow in a C struct. `[i32; 4]` was refused on a measured cost: the `;` appears nowhere else in this language. Was | measured 2026-08-15, `/loop fino a sistemare tutto raylib` | **A C **array member** has no Heroes spelling, and it is the last thing between this language and all of raylib.** The number, re-taken three times in one day because the first two were wrong: raylib declares **600** entry points (`grep -c "^RLAPI"` and clang's own `FunctionDecl` count agree — panels 060 and 061 both said 601), **349** cross a struct by value, **328** bind with every field named and **346** with `partial`. The residue is **3**, and it is **one struct**: `VrStereoConfig`, whose eight members are *all* arrays — `Matrix projection[2]`, `float leftLensCenter[2]` and six more — so it has no nameable field at all and `partial` gives `error[empty_record]` rather than a binding. The three are `BeginVrStereoMode`, `LoadVrStereoConfig`, `UnloadVrStereoConfig`. **What is yours is whether a fixed-size array field is worth a sitting**, and the honest case against is Principle 0's: the closure list needs none, so §1.0's first branch is empty, and 3 of 349 crossings is not a §1.12 completeness failure the way *"a third of the boundary"* was. **The case for is that the refusal is not stable**: `Material`, `Model`, `ModelAnimation`, `VrDeviceInfo` and `AutomationEvent` are bindable **only** through `partial` today, which costs them `==`, `hash` and map-key use for a reason that has nothing to do with those operations — the author gave up structural equality to work around a missing field type. **And the falsifier is named, per §12**: a program the corpus wants that needs an array member *outside* raylib's VR surface, or a second library whose common structs carry one. SDL3's `SDL_GUID` (`Uint8 data[16]`) is already one, and it is 7 of SDL3's 8 by-value crossings | docs/panel/060, 061 · examples/raylib/main.hero · /opt/homebrew/include/raylib.h | the milestone reached 346 of 349 and the last three have one name, which is what a refusal owes
- [x] **STRUCK 2026-08-16 — the field binds, and the item measured a mechanism panel 064 had replaced.** `kind: u32` over a C `enum` runs at exit 0, verified by the ffi-pragmatist on real SDL3 and reproduced by the coordinator on a header of its own. **97 enum fields, 95 bindable, 0 unbindable** (2 unreachable inside anonymous unions). `kind: i32` fails because SDL3's enums are **unsigned**, and refusing `i32` for an unsigned 32-bit member is the check doing its job. Panel 071 **vetoed** widening the field rule: it would loosen a sign check correct on 95 of 95 in exchange for nothing. **A false premise died with the item**: `extern_record.rs` listed *"an enum (3)"* among what the branch refuses, and clang promotes `__builtin_classify_type`'s operand so an enum answers **1** — deleted, with the measurement in its place. What survives is a **diagnostic** gap, filed separately: `ffi_field_type` says *"is not `i32`"* and never says `u32`. Was | measured 2026-08-15, same run | **An SDL3 `enum` field is unbindable, and no measurement taken on raylib could have found it.** `_Generic(&((SDL_Surface *)0)->format, int32_t *: 1, default: 0)` **fails** — a C enumerated type is a distinct type from its compatible integer type, and the address-of form sees the difference. 35 enum fields across the 57 structs SDL3's entry points name; **9 structs blocked by enums alone** and 7 more by enums together with typed pointers. raylib spells every enum-valued member `int`, which is why three sittings and four measurements missed it: gate row `2675`'s finding again, wearing a corpus with no arm for the question. Decide whether a `ptr`-style widening applies — accept any enumerated type for an integer field of the same size — or whether an enum field wants its own spelling, which is the same question the array member asks one row up | /opt/homebrew/include/SDL3/SDL.h · crates/heroes/src/emit/extern_record.rs | the corpus binds raylib and SDL3, and only one of them could have shown this

## From panel 062 (2026-08-15) — the C array member, and what the sitting found on the way

- [x] **ANSWERED 2026-08-16 by panel 069, and the sitting it convened was not about what this item said.** The spec sentence is repaired (`spec:70`, 3399 → 3405, the enumeration **deleted** rather than extended because it was the mechanism that manufactured the hole — and floats were not the only omission: **function values** compare by address too). The nan map key is now a **compile error** (`float_map_key`), not the runtime abort this item described; Rust and Zig refuse it statically and Heroes was the only language the historian could source that did it at run time. **What the item could not have known** is that the same measurement, pushed one step further, found `==` giving **two answers for the same contents** — `a == b` true and `a == c` false, decided by whether copy-on-write had shared a header — which was an aliasing leak with a float as its trigger rather than a float rule at all, proved by the compiler-engineer with a program containing no float. Two runtime lines deleted, zero compiler lines, 566 tests green. Three residues are filed below rather than closed here, and one of them is a **silent SIGSEGV** that predates everything | docs/panel/069-the-shortcut-that-is-not-about-floats.md | Was | panel 062, spec-warden | **`nan == nan` is `false`, and `spec:71` says `==` is *"structural equality on every value"*.** Measured: `P(a: nan) == P(a: nan)` is `false` at exit 0, and the runtime aborts on a nan map key with `panic: a map key that is not equal to itself (nan)` — a rule the spec never states. Floats are simply absent from `spec:71`'s list. This is panel 053's `cstr` class, one type later, and it got worse rather than better yesterday: `f32` landed, and **every raylib struct is floats**. Three shapes, and the choice is not obvious: state IEEE-754's rule in the spec (tokens, and it makes the document carry a fact about a standard rather than about this language); make `==` on a float total by collapsing nan (a lie, and one C does not tell); or leave the abort and document *it*, which is what the runtime already does | spec/heroes-spec.md:71 · runtime/parts/map.c · docs/panel/053 | the operator this language checks nullability with was described wrongly for a milestone; this is the same sentence, one type on
- [x] **FIXED 2026-08-16 (panel 071 C).** The field assertion's `sizeof({place}) == sizeof({elem}[{n}])` conjunct was a **hard error** on a flexible array member — *"invalid application of `sizeof` to an incomplete type"* — so clang could not evaluate the assertion, nothing carried `heroes-ffi-field`, and §7's named exception could not reach it. It is a **tail measurement** now: `sizeof(T) − __builtin_offsetof(T, m) >= n * sizeof(elem)`, defined for both shapes, **0** for a flexible member and at least `n * sizeof(elem)` for every sized one — compiled by the ffi-pragmatist across the padded `char[1]` and over-aligned cases. The assertion **fails** instead of failing to compile, so exit 1 on the author's line. `__builtin_types_compatible_p` was tried first and cannot do it (clang answers **1** for `char[4]` against `char[]`). Verified: sized arrays still bind, a wrong length is still refused. **The message's wording was a condition and is not met** — filed as its own item. Was | panel 062, ffi-pragmatist | **A flexible array member is exit 2, and §7's named exception does not reach it.** `char data[]` at the end of a C struct: `_Generic` **accepts** `char (*)[4]` for it, because a sized and an unsized array type are compatible — so only the `sizeof` conjunct refuses it, and that clang error carries no `heroes-ffi-field` marker. The result is *"the compiler is wrong"* on the author's own declaration. Decide whether it gets a fifth `emit/ffi.rs` class or a frontend refusal; the frontend cannot see it today, because the header is clang's to read | crates/heroes/src/emit/extern_record.rs · CLAUDE.md §7's named exception | every other way to get a field wrong is exit 1 on the author's line; this one is not
- [x] **FIXED — same defect as this file's item at :297, and closed with it.** Verified in the tree today, not recalled: `dump.rs:93` is `DeclKind::Record { fields, header, library, partial }` with no `..`, and line 94 builds the group prefix. The doc above the arm records that this is where the rule was broken **twice** — `partial` at panel 061, `header`/`library` until panel 062's audit — which is the pattern worth keeping: a `..` in a printer arm is invisible in review and silent in the test that exists to catch it. Was | panel 062, compiler-engineer + the audit | **`printer/dump.rs` still elides a record's `header`/`library`, so half of panel 060's cancelling pair survives.** `fmt_decl.rs` prints a record's group head; `dump.rs` does not; and `assert_canonical` asserts `dump(text) == dump(fmt(text))`, so **`fmt` hoisting a record out of its group, folding it into a neighbouring one, or turning `link` into `package` all produce identical dumps and a green test**. Repairing `fmt_extern.rs` did not restore the guard. The engineer's own question is the one to settle: whether this wants a **structural** rule — every `DeclKind` field a formatter can print must appear in `dump` — rather than an arm at a time, which is how it has now been repaired twice | crates/heroes/src/printer/dump.rs · crates/heroes/src/printer/tests/mod.rs | two blind spots that cancel into a green test is the worst shape a guard can take, and this is the second instance in two days

- [x] found 2026-08-15 by diagnosing the Windows leg | **PANEL 064 SAT 2026-08-15 — and the answer was none of the three options on the ballot.** The row is **Part 7 item 10, not Part 6** (the spec-warden caught the misfiling). At **parameter** position nothing is produced and there is no hole: `-Werror=shorten-64-to-32` already makes `fseek(offset: i64)` exit 1 on Windows and i686. At **field** position the row is falsified outright — `time_t`, `clock_t`, `ldiv_t.quot`, `timespec`, `timeval`, `rusage` and `z_stream.total_in` have no Heroes spelling on Darwin, because `int64_t` is `long long` there and panel 060's assertion asks for type identity. The vocabulary (`clong`/`culong`/`csize`) is **vetoed** by the compiler-engineer: core under §1.7, not on the closure list, and 3 loud sites plus **20 silent allow-list gates**. What lands is **9 lines** relaxing the *integer* field assertion to class+size+sign — monotone, 559/559 green, floats/pointers/enums still on identity. The row narrows to parameter position with its falsifier restated over the **canonical** C type. Was | design.md:2444-2449 · docs/panel/064-the-position-the-row-does-not-name.md | this project wrote the falsifier itself and then produced it in a position its own text does not mention

- [x] **SUPERSEDED, ticked 2026-08-16 rather than left open.** It is kept for the record, as its own text says, and an unticked box in a decision list reads as work outstanding — which is the one thing this file's items must never say falsely. Nothing here needs the author. Was | SUPERSEDED, kept for the record | **design.md:2446's own falsifier is produced, and it half-kills its Part 6 row.** The row deferring a C-width type vocabulary names what would make it wrong: *"a program … binding a C `long`, `size_t` or `unsigned long` parameter, whose correct spelling is the same on all three CI legs. Produce one and this row is wrong."* The **`size_t` clause is produced and compiled**: `extern function malloc(size: u64) -> ptr` is silent on all three CI targets, because `size_t` is 64 bits on LP64 twice and on LLP64 once. The **`long` clause survives** — there is still no `.hero` spelling for a C `long` that is right on three legs, which is why `ftell`, `strtol`, `sysconf` and curl's `long` options are not source-portable. §12 holds a refusal to a feature's standard, so a row whose falsifier is half-produced owes an answer rather than silence: amend the row to name `long` alone, or reopen it. **It is design.md Part 6, so it is a panel path** (CLAUDE.md §4) | design.md:2435-2449 · crates/heroes/src/emit/ffi_narrowed.rs | this project wrote the falsifier itself and then produced it without noticing

- [x] panel 062 follow-up, compiler-engineer | **PANEL 063 SAT 2026-08-15, soundness lane, both judges `approve`.** `HERO_RET_RECORD(c, T) = __builtin_types_compatible_p(__typeof__(c), T)`, in an **unevaluated operand with no probe**, reusing `ffi_return_type` and the existing `ASSERTION` marker — **no fifth `emit/ffi.rs` class**, which is what let the soundness lane decide it. `_Generic` refused (an association type must be complete; an incomplete one is a C2y extension Apple clang takes silently — the `HERO_RET_UNIT` defect); `sizeof` refused (accepts panel 060's veto case). Two corrections to this item's own text: the number is **113 of 600**, not 349 — 349 was the union of both directions — and a wrong struct result is **never a silent wrong answer**, because C struct assignment is nominal, so it is misattributed exit 2, which is worse. Cost +109/−45, zero golden churn, conditional macro. Was | crates/heroes/src/emit/assert_spelling.rs · docs/panel/063-the-direction-nobody-checked.md | panel 036's original defect, alive for the majority of a real library

- [x] **SUPERSEDED, ticked 2026-08-16 rather than left open**, for the reason the item above it now carries: a record kept for the record is not a decision waiting on anybody, and leaving the box empty makes the open count lie. Was | SUPERSEDED, kept for the record | **A struct RESULT gets no `_Static_assert`, so a wrong struct return is exit 0 or exit 2 and never exit 1.** `assert_spelling.rs::return_check` answers `None` for `Ty::Named`, and its comment claimed no such type crosses the boundary — false since panel 060. Measured with raylib: `ColorAlpha` and `GetSplinePointLinear` get **no** `heroes-ffi-return` line; declaring `-> Vector2` for a `Color`-returning function **builds at exit 0** while nothing calls it, and called is `assigning to 'Vector2' from incompatible type 'Color'` at exit 2 with no marker, so §7's named exception cannot fire. **349 of raylib's 600 entry points return a struct.** Needs a `HERO_RET_<struct>` shape — `__builtin_types_compatible_p` takes a struct where `_Generic` cannot — and a fifth `emit/ffi.rs` class, so it is a diagnostic class and a panel path | crates/heroes/src/emit/assert_spelling.rs · CLAUDE.md §7 | panel 036's original defect, alive for the majority of a real library, and the milestone that bound every struct did not check what one returns
- [x] **FIXED 2026-08-16 — and the class was four types wide, not one.** `()` is now `ffi_parameter_type` at **exit 1** on the author's line, refused in the frontend by `types/ffi_position.rs` (new file: the seam is *where may a boundary type stand*, which is a different question from `ffi_decls.rs`'s *can C spell it*, and the shared answer is exactly what hid this). **The test for whether a refusal belongs in the frontend at all** is written into that file and is what keeps it one function long: is the fact about C's **grammar** or about some **header**? A parameter list has no spelling for an argument of type `void`, so no header anywhere can take one. **Three more types were found by probing the class rather than the case** — `str`, `ptr` and `cstr` as parameters are still exit 2 — and they are NOT the same defect: each is a legitimate C parameter type that merely disagrees with *this* header, so only clang can judge them. Their repair is widening `emit/ffi_narrowed.rs`, which today matches only *implicit conversion loses integer precision* and *changes signedness*; measured, the probe **already errors on the author's own line** (`./p.hero:2:85: incompatible pointer to integer conversion`), so the location gate needs nothing — only the phrase family does. **Queued as its own item below rather than folded in here**, because it is a different mechanism. And one refusal was written and withdrawn inside the same session: `str` looked impossible by the same argument `ffi_constant` makes, and `runtime/hero_os.h:53` takes a `HeroStr` — the compiler's own library, reported broken on the first run. Was | panel 062 follow-up, compiler-engineer | **`()` as an `extern` PARAMETER is accepted and is exit 2 on the author's line.** `crosses_the_boundary` is shared by results and parameters and `()` is a legal *result*, so `extern function abs(n: ()) -> i32` checks clean; `zero_of` then writes `abs((void *)0)` — `incompatible pointer to integer conversion`, *"internal error: compiling the generated C failed"*. No C spelling of a `()` argument exists, so the repair is a frontend refusal, which is a diagnostic class | crates/heroes/src/types/ffi_decls.rs:122 · crates/heroes/src/emit/assert_spelling.rs | every other way to get a signature wrong is exit 1 on the `.hero` line

- [x] panel 065 | **RATIFIED IN FULL 2026-08-15** (author, "ratifica tutto" — blanket, the fourth of its kind on this date; the dated-count disagreement resolves to the adopted resolution). Was | **Ratify the sitting: the marks are the inventory, the note stops asserting a count, and the two break classes are stated separately.** Soundness lane, two approve-with-conditions, no veto; all seven conditions adopted and landed provisionally. What ratification settles: (a) the rewritten §4.9 port note — discipline, sort-key-bearing `// ORDER:` marks at 13 write sites, `grep -rn "// ORDER:" crates/` as the only live inventory, the count kept **only as a dated snapshot** (the sitting's one disagreement: the compiler-engineer holds a dated count admissible, the ffi-pragmatist wants none without a test — siding with the pragmatist strikes one parenthetical); (b) one marker at `names_in` covering its six consumers rather than six consumer-side marks; (c) the two new goldens — the nested-fixed-array tie-break in `check/`, and the emit case that pins the per-option function order and `fn{N}` numbering **for the first time** (the double-emit test pins stability, not order — the sitting's finding) | docs/panel/065 · design.md §4.9 · tests/golden/emit/order-options-and-fn-typedefs.hero | the port's map-walk obligations are the probe's own subject, and an unratified rewrite of a §4.9 sentence should not outlive the milestone that needed it

- [x] **RATIFIED 2026-08-16** (author instruction *"ratifica e sistema tutto"*). All ten items of the resolution stand as landed: the spec edit ("Five" → "Six", 3374 → 3377), the `SPEC_TOKENS` ledger row with the warden's registered prediction, the `lexer/escape.rs` table arm and both `legal()` strings, `ir/print.rs::escape` so dumps stop carrying raw bytes, **the coherence condition** — `raw_carriage_return` with a certain fix `\r`, which is the half that made the sitting more than a table row — the tests, the hand-edited `escapes.expected` with `UPDATE_GOLDEN` unused (forbidden in `check/`), the design.md amendments keeping `\0`/`\xNN`/`\u{…}`/octal frozen on the NUL ground, the port's four mirrored files, and the appended records. **The process note is ratified with it and is the durable part**: the spec-warden's veto adjudicated only the coordinator's own false parity premise, and that seat lifted it on its second ruling after verifying the other seats' grounds itself, including executing the raw-CR trap live. A veto that investigates and withdraws is the mechanism working. Was | panel 066 | **Ratify the sitting: `\r` is the sixth escape, a raw CR inside a literal becomes a diagnostic, and the veto that lifted itself.** Full lane; 2 approve, 3 approve-with-conditions; the spec-warden's veto adjudicated only the coordinator's (false) parity premise and was lifted on its second ruling after verifying the other seats' grounds itself — including executing the raw-CR trap live. What ratification settles: (a) the +3 spec edit ("Five"→"Six", the measured wording); (b) the coherence condition — `raw_carriage_return` diagnostic with certain fix `\r`; (c) the warden's payment prediction registered in the gate ledger, lapse-with-refund terms included; (d) the process note that a coordinator-briefed false premise reached three files in one day. A veto reverts items 1–9 of the resolution wholesale; the false-premise corrections stand regardless, on the first ruling no seat disputed | docs/panel/066 · spec § escapes · docs/panel/008 | the escape set is the language's most-copied sentence, and this sitting is the record of why its sixth member entered while the frozen four stayed frozen

- [x] **FIXED 2026-08-16, and it was panel 068's own owed half rather than a new question.** The sitting ratified R2 — *the refusal moves from the emitter to the checker* — and only the transitive walk had landed; the relocation had not. Now `types/ordering.rs` (new) holds what may be ordered, `unordered_element` fires at exit 1 on the author's line, the message names all **twelve** ordered types (the emitter's *"`i64`, `f64` or `str`"* had been stale since panel 042), never says *"has no `<`"* (false of `str`, which has no `<` and sorts), and leaves `sort_by` addable. `bool` joined the set with one comparator and one dispatch row in `sort.c` — no ABI change, `hero_cmp_for` is `static`. The golden moved `unsupported/` → `check/`. **The spec half was already repaired in the sitting**, so §12's disagreement is closed from both ends. Was | M-selfhost-port, the second file (measured 2026-08-16) | **`sort` on a record type-checks and cannot be built, and `spec:85` is the sentence that sends you there.** The spec says *"`keys(m) -> [K]` gives the keys, so `for k in sort(keys(m))` walks in order"* and lists `sort` with no element restriction; `<` on a record is `bad_operand`, so `sort` on `[Record]` reaches `emit/builtins.rs:164` and dies **`unsupported[builtin]`, exit 1, "no change to this file will fix this"**. A compound map key must be a record (no tuples), so the idiom is unavailable exactly where panel 065's rule requires a sort. **CLAUDE.md §12's class** — spec and compiler disagree — so this is a defect to settle rather than a feature to want, and it needs a panel (a diagnostic class at minimum). Three shapes: **(a)** a lexicographic order on records, field by field, which costs a spec sentence and gives `<` a meaning it does not have; **(b)** the **checker** refuses `sort` on an unorderable element with a type error naming why, and `spec:85` gains a caveat — zero new semantics, and it converts an emitter surprise into a frontend answer; **(c)** `sort_by(xs, less)` taking a comparison, which is generic and needs no order on records. **Not blocking**: the port keys by module then name (two levels of `str`, both sortable) and that shape is arguably better anyway, so this waits for a sitting rather than holding the port | selfhost/resolved.hero (the finding's own test) · docs/measurements/009 § the second file · emit/builtins.rs:164 · spec:85 | a program the spec tells you to write, that type-checks and cannot be built, is the exact shape §12 exists to arbitrate

- [x] M-selfhost-port, measured 2026-08-16 | **ANSWERED 2026-08-16 by author instruction — *"supera la regola per heroes"*: §11's threshold yields, the language rule stands.** The amendment is in CLAUDE.md §11 with the measurement and the reason: `toolchain.rs`'s argument at its limit — there a cut ran *against* a seam, here **no cut exists**, and a rule that cannot be obeyed is not a standard but a lie. The exchange the port owes is written into the rule: each knot carries a module doc naming its cycle, its entry points and its internal call graph. Was | **The grammar cannot be split the way Rust splits it, and CLAUDE.md §11 has to say what happens.** Measured both ways: Heroes' `module_cycle` fires on the **`use` edge**, whatever it carries — two modules that call each other are refused, and so are two that only name each other's types. Recursive descent has two irreducible knots in `crates/heroes/src/syntax/`: **A** (`expr`, `primary`, `control`, `stmt`, `name_stmt`) at **1025 Rust lines**, and **B** (`decl`, `data`, `externs`, `extern_members`) at **736** — 3.4× and 2.5× §11's ~300. Every other file in the directory is cycle-free and ports as its own module. **Nothing is blocked**: mutual recursion inside one module is free, so each knot ports as one long Heroes module and the grammar works. **What is owed is a rule.** The port proposes: the language rule stands (panel 031 R6 chose refusal as the reversible direction, and relaxing it spends the topological order M-separate-compilation needs — the trade 031 already priced), and **§11 yields for a grammar knot with its seam named**, exactly as it yielded for `toolchain.rs` at 400 on 2026-08-14 — where the accepted reason was that the cut ran through a seam, and here the seam is *forbidden by the language*, which is stronger. In exchange each knot carries a module doc naming its cycle, its entry points and its internal call graph, because a reader not drowning is what §11 actually protects. **Alternatives, both worse**: relax the cycle rule (spends M-separate-compilation's ordering, and 031's argument was explicitly about not doing this), or restructure the grammar to break the cycle (changing the compiler's architecture to satisfy a file-layout rule) | docs/measurements/009 § the structural finding · docs/panel/031 R6 · CLAUDE.md §11 · crates/heroes/src/syntax/ | it decides the shape of ~1,800 lines of the self-hosted compiler, and the port stops at the first knot until it is answered

- [x] panel 067 | **RATIFIED 2026-08-16** (author, *"ratifica il panel zero sessantasette"*, after the plain-language recap of all five seats). §4.7 and spec:117 unchanged; the three return conditions stand; the `???`-arms diagnostic stays queued rather than landed. Was | **Ratify the sitting: nothing enters the language, and the coordinator's number was wrong four times.** Full lane, five seats, no seat in favour of any form. What ratification settles: (a) §4.7 and spec:117 unchanged — no catch-all, no `rest`, no lookup-with-default, no case-groups; (b) the refusal's three return conditions (a closure-list construct that *cannot* be written without a default · a measured metric-2 effect · a named removal of ≥94 tokens or an admissible prediction); (c) that the **llm-ergonomist's `???`-arms diagnostic is queued rather than landed** — it is the sitting's one disagreement, and the reason is discipline rather than doubt: the warden priced B and C and never priced that sentence, so landing +30 spec tokens on one seat's estimate is the very thing this sitting caught its own brief doing. The diagnostic is worth having and needs its own pricing | docs/panel/067 · spec:117 · design.md §4.7 | the author asked whether a rule is too expensive and the honest answer is that the price was mis-measured and the worst site was the porter's own mistake
- [x] panel 067, ffi-pragmatist F7 | **FIXED 2026-08-16, same session.** All six shapes are now refused at **exit 1** with `pointer_element` and a reason, where they aborted at 134. The gate refuses them; the dead premise in its doc (*"an `[[ptr]]` is refused for its `ptr` and says so once"*) is replaced by what is true and why. **A refusal, not a feature**: the runtime could grow the two descriptor rows, and that is a panel question with an ABI bump in it — queued as such rather than smuggled in at 2am. F8 is fixed with it: the old case's `[ptr]` lines are gone (its comment claimed they were legal and they never were, and they were never *reached* either), the six shapes have their own file with no `()` to mask them, and the nested `[[ptr]]` that stayed is annotated now that it fires. Was | **`ptr` and `cstr` in a container: exit 0, then abort.** Six shapes measured — `[ptr]`, `[cstr]`, `{i64: ptr}`, `{i64: cstr}`, `{ptr: i64}`, `{cstr: i64}` — all pass `heroes check` and die at exit 134 with *"entered unreachable code — this is a compiler bug"*. Cause: `emit/descriptors.rs`'s `_ => None` under a doc that says the gate already refused these; the gate refuses only `Ty::Unit`. **§1.12's class** (a program that type-checks must not crash) and §11's shape (a dead premise whose comment still reads as correct). The pragmatist's conditions: the fix covers all six shapes, and the case that proves it lives in `tests/golden/run/` rather than `unsupported/`, because the existing case's `[()]` line fires first and the live half is never reached | crates/heroes/src/emit/descriptors.rs · tests/golden/unsupported/fixedbugs-container-element-reached-clang.hero · docs/panel/029 | a program that compiles clean and aborts is the one thing §1.12 puts above every other criterion

- [x] panel 068 | **RATIFIED 2026-08-16** (author, *"Ratifico il panel 68"*, after the recap of all five seats). `sort` keeps its scalar-and-`str` domain; A returns only by amending design.md §4.20 by name; the spec repair stands at 3399 with both ledger rows; C is refused-and-available at +47…+55. **Still owed**: B's *move* — the refusal from the emitter to the checker (the transitive walk landed with the seventh-shape fix; the relocation did not). Was | **Ratify the sitting: `sort` keeps its domain, the spec is repaired, `sort_by` is priced and waits.** Full lane, five seats; **A vetoed by all five**, B approved by all five, C split 3–2 on timing and resolved conservatively. What ratification settles: (a) the spec repair as the warden measured and wrote it — `spec:85`'s false derivation removed, `spec:168`'s entry gains its restriction, the operator row gains `(< <= > >=: a number only)`, **3377 → 3399, +22 net against a −12 named removal** (panel 012 branch 1, a payment rather than a promise); (b) that **A does not return without amending design.md §4.20's *"gains no sixth"* by name**, in a sitting that re-runs the `0x4` measurement; (c) that **C is refused today and left available** at +47…+55, with the warden's registered prediction as its return condition; (d) B's shape — the refusal moves to the checker, is **transitive through record fields** (reusing `types/partial.rs::reaches`), admits `bool`, and is worded so C stays addable and so it never says *"has no `<`"* (false of `str`, which sorts) | docs/panel/068 · spec:85, spec:168 · design.md §4.20 | the spec tells a reader to write a program that cannot be built, and it has done so since the map shipped

- [x] **ANSWERED 2026-08-16, and the question split in two while it was being answered.** The item asks about *`#line` on Windows* and treats the debugger as the only instrument. It is not, and separating them is the whole answer. **The mapping is now measured on all three legs**, by `no_generated_line_points_past_or_between_the_authors_lines` — it reads the emitted C and walks the `#line` claims the way C does, so it needs no debugger and no platform. It was written for a different reason and it covers this one: it found **550 drifted C lines across 81 of 81 `run/` goldens** on the tree that was standing when this item was open, so the thing this item wanted measured was not merely unmeasured on one leg, it was **broken on all three**. **What Windows does not cover is the debugger**, and that is now recorded as option (c) — honest and free: `lldb.exe` is there and exits `0xC0000135` before running a command, a Python DLL and LLVM's installer's known gap, and the debug info itself is fine (a 7.7 MB `.pdb`, which is where Windows keeps CodeView). **The route back in is named and is deliberately not asserted**: `llvm-pdbutil` reads CodeView and, unlike `lldb.exe`, has no reason to link Python — but whether the runner carries it is a claim about the world nobody here can check, so it goes to the machine as a **question** (CLAUDE.md §1). The `Report the toolchain` step now prints whether `llvm-pdbutil` and `lldb` are on PATH, so the next CI run answers it instead of a document guessing. Was | the third CI leg, 2026-08-16 | **Is `#line` on Windows worth measuring, and with what?** `lldb_breaks_on_a_hero_line` executes design.md §2's claim on Darwin and Linux, and on Windows it executes **nothing**: `lldb.exe` is there (LLVM's, not Xcode's) but exits `0xC0000135` (`STATUS_DLL_NOT_FOUND`) with both streams empty before running a command, so the test now skips there with the reason stated. **The debug info itself is not the problem and must not be recorded as one**: `-g` writes a **7.7 MB `.pdb`** beside the binary, which is where Windows keeps CodeView — the COFF file carrying no `.debug*` section is normal. So §2's "lldb breaks on and steps through `.hero` source lines" is **unmeasured on one of three legs**, not refuted. Three ways to close it, and they cost differently: install a working lldb on the runner (a CI step, and the DLL is Python's — the LLVM installer's known gap); assert against Microsoft's debugger instead (`cdb` ships with the Windows SDK and is scriptable, but nothing in this repo speaks CodeView); or record Windows as a leg that does not cover debugging, which is honest, free, and leaves a §2 claim with two thirds of a test | crates/heroes-cli/tests/golden.rs (`lldb_starts_here`) · docs/debrief/SCHEDULED.md § the `-g` defect, fixed the same night · run 31921731797 | design.md §2 is a promise to the author about their own debugger, and the platform where it is unverified is the one platform the author cannot check by hand

- [x] **FIXED 2026-08-16 (panel 079), and it was half of a larger defect.** `types/construct.rs::field_of_function_type` matched `Ty::Named` only, while `ir/layout.rs` has carried the `Ty::Case` arm since panel 068 and design.md **§4.2:845** says a variant case **is** a small record — so the checker was the half that had not caught up, and the message was §4.17's forbidden shape: it named a repair that does not exist, and a reader acting on it edits a correct declaration. `a.g(50)` now prints **100**. **The larger half, found by composing two seats' verdicts with a measurement**: `ir/calls.rs` asked the RESOLVER where `types/ufcs.rs` asks the field first, so the two passes disagreed — `s.g(50)` printed **50007** (the free function) while the checker had typed the field's **100**, at exit 0; and with differing result types the checker typed from the field while the lowering called the function, which **clang** caught and the type system did not (*"assigning to `int64_t` from incompatible type `HeroStr`"*, exit 2). Both halves land together, the dot form moves into `ir/ufcs.rs` so they cannot drift again, and the sweep says **0 of 268** corpus files change meaning. Golden: `run/fixedbugs-a-field-wins-the-dot-over-a-free-function.hero`. Was | panel 068 F8's neighbour, measured 2026-08-16 while fixing it | **`a.g(50)` on a variant case's payload is `unknown_function`, while `h.f(21)` on a record field works.** Both hold a function value; both are a dot followed by a name and an argument list. On a record the resolver finds the field; on a case payload it reads `x.g(…)` as UFCS, looks for a top-level `g`, and reports *"`Step.apply` has no field `g`, and no function is named `g`"* — a message that says the field does not exist while the payload declares it. The two-step form works (`call = a.g` then `call(50)`, measured: prints 100), so nothing is blocked and the lowering is shared. **Three shapes**: (a) the resolver descends into the matched case's payload for a method call, making the two consistent; (b) the refusal stands and the message stops saying the field is absent, naming the two-step form instead; (c) it is left alone and this item records why. §4.11's UFCS rule and §4.13's function values meet here and the spec says nothing about the intersection | tests/golden/run/fixedbugs-function-in-a-record-field.hero (the note in its body) · crates/heroes/src/resolve/ · docs/panel/068 F8 | a diagnostic that says a declared field does not exist is the shape §4.17 exists to prevent, whichever way the question is answered
- [x] **MOVED to `SCHEDULED.md` 2026-08-16, which is where it belongs now that its direction is ratified.** `DECIDE` is for what should be true; option (b) is no longer a question but **work with a home**, and the three-list rule sends it across. Building it today would contradict the ratification it came from: Principle 0 puts it behind the port and `selfhost/` declares zero generics, measured by three seats. What goes with it is the **trigger** rather than a date — `grep -rnE '^function [a-z_]+<' selfhost/` returning a generic whose body contains `sort(` makes it §1.0 compiler-need that day, at any price. **What does NOT move and is not closed** is the llm-ergonomist's veto: (b) puts the error on the call the author can edit and still leaves the generic body's line undecidable from the line plus its signature, so lifting it needs constraints on generics — the author's trade, priced from outside by the historian, and not spent by the ratification. Was | **ANSWERED 2026-08-16 by `docs/panel/082`, and this item's own premise was false.** It says *"the reason is structural, not effort"* and *"there is no third place to put it that `check` reaches"*. There is: `types/apply.rs:139-143` writes `checked.instantiations` **inside `types::check`**, keyed by the **call-site** span — the one line the author can edit — with both edges present when a generic calls a generic. The compiler-engineer found it by running parse → resolve → check with no IR anywhere and printing the table, which is CLAUDE.md §1's lesson from the other side: the pipeline was read and the conclusion was not run. **(a) is refused** — 57 lines, `check.rs` past §11's ceiling, 1.3–2.2× on every keystroke, `--permissive` contaminating Part 11's control arm, a `heroes mutate`/`heroes check` split scoring two languages, and a §4.16 hole both compiling seats hit independently; and as briefed it could not even catch its own example, because `mono::run` pushes only polymorphic recursion. **(c) is unavailable** — the blind seat vetoed the status quo on locality (*"'fixing' the reported line means deleting the function"*). **(b) is the direction**, ~90–110 lines in `types/`, queued behind the port on Principle 0: `selfhost/` declares zero generics, measured by three seats. **(d) landed** — see the row below. **R4 corrected two shipping module docs** that carried this item's false claim. Still open, and this is what stays open: **(b) itself**, plus the llm-ergonomist's veto, which (b) does not lift — it moves the error to the call site without making the generic body's line decidable from the line, and answering that needs constraints on generics, which the historian priced from outside (Swift and Go bought it with a closed vocabulary; C++ took to C++20 and names legacy migration as the blocker, which Heroes does not have; Nim's RFC is open since 2019) | docs/panel/082 · crates/heroes/src/types/apply.rs:139 · crates/heroes/src/types/sortable.rs (unwritten) | Was | panel 068 R2's residue, measured 2026-08-16 while landing it | **`sort` through a generic is the emitter's, and moving it means deciding whether `heroes check` runs the IR.** R2 put the element rule in the checker and it is there (`types/ordering.rs`, `unordered_element`, exit 1 on the author's line). **One shape cannot follow it and the reason is structural, not effort**: inside `function first<A>(xs: [A])`, `sort(xs)` has an element that is not yet a type — `first([3, 1, 2])` runs and prints 1, measured before a line was written, so refusing the generic body would delete a working program. The instantiation that picks an unordered `A` is visible only after **monomorphisation**, which runs in `ir/mono.rs`, and `heroes check` is parse → resolve → `types::check` and stops (`commands/check.rs`, verified). So there is no third place to put it that `check` reaches. What stands today: the gate refuses it at exit 1 with a message naming the **route** rather than the type (the element on that line is spelled `A`, so telling the reader to change it sends them nowhere), and `emit/tests/gate.rs` plus `unsupported/three-capabilities.hero` keep it alive as the gate's last remaining row. Three shapes, and the choice is architecture: (a) `heroes check` runs `mono` and reports its diagnostics, which makes *check accepts ⇒ build succeeds* true for every program and is the complete answer — its price is that `check` stops being frontend-only and the IR's cost enters the editor loop; (b) an interprocedural pass in the checker that propagates a *sortable* obligation from a generic's body to its call sites, which is a new kind of analysis this compiler has none of; (c) it stays where it is and this item is the record. **Panel path either way** (CLAUDE.md §4: architecture), and it is not urgent — nothing is unsafe today, the diagnostic is exit 1 on a real line | crates/heroes/src/emit/builtins.rs (`unsupported_operand`) · crates/heroes/src/types/ordering.rs (`is_refusable`) · crates/heroes-cli/src/commands/check.rs | panel 068 asked that a program the checker accepts must build, and this is the one shape where that is still false
- [x] **FIXED 2026-08-16 (panel 071 A).** All four parameter types are exit 1 now. `ffi_narrowed.rs` matched two clang strings, both **width** diagnostics; a **class** mismatch phrases itself two other ways and both contain `" to parameter of "`, which is the whole matcher. **The gate is unchanged** — `extern_at_line` still asks whether an `extern` is declared at exactly this file and line. **The condition that made it a repair rather than a regression**: the diagnostic fires whether or not the C type has a Heroes name, and only the `Fix` consults the table — declining would return `Vec2` to exit 2, **356 of 1159** entry points. **The widening broke two things and the corpus caught both within the hour**: it swallowed panel 058's `ffi_writable_parameter` class and told an author to *"declare it `cstr`"* when they already had (repaired by naming `ffi_mutable.rs`'s own `discards qualifiers` string as that class's property — a fact about the text, not a guess about precedence), and it said C *"would convert in silence"* where C refuses. **And it found a real mistake in the corpus**: `ffi-incomplete-record.hero` declared `CheckCollisionPointRec(point: Rectangle, …)` where raylib says `Vector2`, silent for as long as a class mismatch was blamed on the compiler. Was | measured 2026-08-16, probing panel 062's follow-up as a class | **Three `extern` parameter types are exit 2 when they disagree with the header, and the repair is one phrase family in `emit/ffi_narrowed.rs`.** Measured against a real header declaring `int32_t take(int32_t)`: a parameter declared `ptr`, `cstr` or `str` gives *"internal error: compiling the generated C failed"* — the compiler blaming itself for the author's declaration, which is the exact failure §4.19's guarantee exists to prevent. A parameter declared `i64` against the same header gives the correct `ffi_parameter_type` at exit 1. **The difference is not the location gate and not the machinery** — it is that `ffi_narrowed.rs` matches exactly two clang strings, `implicit conversion loses integer precision` and `implicit conversion changes signedness`, both of which are *width* diagnostics. A **class** mismatch produces a different family: `incompatible pointer to integer conversion passing 'void *' to parameter of type 'int32_t'` and `passing 'HeroStr' to parameter of incompatible type 'int32_t'`. **The probe already errors on the author's own line** — verified, `./p.hero:2:85` with the probe's text echoed — so `extern_at_line`'s gate, which is what panel 052 made the class safe with, needs no change at all. What is owed: the new strings, the header type recovered the same way (the last quoted name, which is `int` in both families, so `spelling()` is unchanged), and a message that does **not** say *"wider than"* or *"a different sign from"* — neither is true of a pointer against an integer. Also owed is a decision the width path never had to take: whether a `Fix` is offered, since for a class mismatch the header's type may have no Heroes spelling at all. **Panel path** — it widens a diagnostic class's admission rule, which is CLAUDE.md §4, and §7's narrowing rule (*"the narrowing is `declaration()`, not whose text it is"*) is the clause that governs it | crates/heroes/src/emit/ffi_narrowed.rs:32-34 (`NARROWED`, `RESIGNED`) · crates/heroes/src/emit/extern_probe.rs · docs/panel/052 | every other way to get an `extern` signature wrong is exit 1 on the `.hero` line, and this is three ways it is not
- [x] **CLOSED 2026-08-16 by panel 076** (`2ad3497`), and the fix cost **zero lines in the compiler**. `==` is a conjunction and `true` is the identity of `&&`, so a nested `hero_array_eq` pushes its elementwise work, answers `true` provisionally and the outermost call drains the queue and ANDs the real answer in — the generated `_eq` never learns it was deferred, so `emit/structural.rs` is untouched and the port reproduces nothing, which is exactly what the compiler-engineer's standing veto was about. **Two seats prototyped it independently and measured the same result**: depth **1 000 000 exits 0** where the unpatched control exits 139. `hash` takes the opposite fix — a depth cap — because it may lose information where `eq` may not, and that number is a fact about the value rather than about the machine. **The depth bound was refused on a measurement**: the same binary at depth 52 200 exits 0 in a plain environment and **139 with one 900 KB environment variable set**. Verified today: depth 60000 exits 0. Was | **HALF CLOSED 2026-08-16 by panel 070 (`56af8df`), and the open half crashes LOWER than the closed one.** The **release** path is iterative now (`runtime/parts/drop.c`): the worklist threads through a doomed block's dead `refcount`, so zero allocation, layout unchanged, `HERO_RUNTIME_ABI` still 14. Measured after: depth **5,000,000** at -O0 exit 0 against 74642 before, the alternating array/map chain at 300000 exit 0, `--sanitize` clean, 566 tests green, zero goldens changed. **What is still open is `==` and `hash`, and they crash at 52043** — *below* the path that was repaired, so the §1.12 hole is narrower and not shallower. They cannot take the same fix: a generated `_eq` compares a field and then descends, so deferring the descent means resuming mid-function, which is a continuation. **Both remaining shapes took a veto from a seat that had compiled a prototype**, and the vetoes are the item now: the compiler-engineer vetoed the continuation on §1.7's ceiling — it lands in `emit/structural.rs` and **the Heroes port must reproduce it**, for a closure list it measured at depth **3** across all 20 `selfhost/` modules (margin ~17000x); the ffi-pragmatist vetoed the stack-remaining probe on portability, with a compiled disqualifier — Darwin's `pthread_get_stackaddr_np` returns the stack's **HIGH** end and Linux's `pthread_attr_getstack` the **LOW** end, and treating one as the other yields **−6805** remaining bytes while passing `-fsyntax-only`. **The two exit conditions are the only amendment path**: the probe returns the day it is **compiled and run** on `ubuntu-latest` and `windows-latest` showing the figure correct on both (not a syntax check — the inversion passes one); the continuation returns the day a closure-list module measures release depth **> 1000**. A third shape is queued below and may dominate both. Was | panel 069, compiler-engineer — **§1.12, and it predates the sitting entirely** | **A deep-enough value SEGFAULTS on `==`, on `drop`, and on the generated `_eq`, silently.** Measured 2026-08-16 on the **unmodified** compiler, the binary run directly rather than through `heroes run` (which reports 134 for the same run — that discrepancy is itself worth a look): a `record Node { label: i64, children: [Node] }` chain built to depth **150000** at `-O0`, two distinct chains compared, gives **exit 139 with no output at all**. No diagnostic, no `hero_panic` line, nothing. `hero_array_eq`, the generated `_eq` and the drop chain each recurse once per level with no bound, so the C stack runs out. **This is design.md §1.12 violated by a program the author is entitled to write**, and CLAUDE.md §12 makes robustness the tie-break that outranks elegance, token cost and speed. Panel 069 found it while pricing something else and **none of its four options closed it** — the identity fast path only *hid* it when two operands happened to share a pointer, which is that sitting's sentence one more time. The shapes, and each has a real cost: (a) a **depth bound** in the runtime with an abort, which turns a SEGV into the loud direction and needs a number nobody has a principled source for; (b) an **explicit worklist** in `hero_array_eq`, the generated `_eq` and `desc.c`'s drop, which is unbounded-correct and is three rewrites in two languages, one of them generated code the port must reproduce; (c) a **compile-time depth limit on the type**, which is wrong — the type is finite, the *value* is deep. The historian's Erlang note is the nearest precedent worth reading. **Panel path** (§1.12 and the runtime's shape), and the reproducer should be a `run/` golden the day the answer lands | runtime/parts/array.c (`hero_array_eq`) · runtime/parts/desc.c (the drop chain) · crates/heroes/src/emit/structural.rs | a silent SIGSEGV is the one outcome design.md §1.12 names as forbidden, and it is reachable today in nine lines
- [x] **REFUSED AS SCOPED 2026-08-16 (panel 071 D), and the argument is not the one the item makes.** Reproduced against real POSIX: `inet_ntoa` returns the same pointer twice, and after the second call both records read `192.168.1.1`. **`a == b` is `true` CORRECTLY** — both hold the same address, which is what `spec:71` says — and the defect is that `a.name` no longer holds what the author put in it. **Refusing `==` leaves that untouched**: `a.name.to_str()` still returns the wrong string silently, which is worse than a surprising `true` because nothing signals it. Cost measured over real headers: **43 structs** lose `==` (raylib 2/35, SDL3 19/136, sqlite3 9/23, curl 13/49). **A conditional veto is on the record and binds any future attempt**: if the rule is ever written as `partial::reaches`, that function answers `Some` at depth 0, so `cstr == nullptr` — `spec:223`'s documented null test, 9 sites in this tree — becomes a compile error. It must be *a `cstr` reached **through a field***, never *a type that reaches a `cstr`*. The objection withdraws only if the **read** side is addressed with it. **Brief correction on the record**: the bound cited as `types/map_keys.rs` is `emit/gate_types.rs`, an unsupported **gate**, so `heroes check` exits 0 on `[a]` and `{a: 1}` and only `build` refuses. Was | panel 069, ffi-pragmatist — **found by compiling a real libc binding** | **Two values pointing at different bytes compare `true`, and it reaches containers.** A `cstr` compares as an address, which `spec:72` states — so a record holding one compares equal to a record holding a *different* string, and `[a] == [b]` does too. The seat measured it against real `<time.h>`: `a == b` is `true` while `a.text` is `"first"` and `b.text` is `"second"`. **This is not the identity fast path** and panel 069's repair does not touch it; it is `spec:73`'s *"No aliasing exists anywhere"* being false at the C boundary by design rather than by accident. What bounds it today: `cstr` is already refused as a map key and as an array *element* (`unsupported[pointer_element]`, panel 067), so the blast radius is a `cstr` **inside a record or a variant payload** — which is exactly what an FFI binding writes. What is owed is a decision, not obviously a fix: (a) refuse a `cstr`-reaching type as an `==` operand, mirroring `float_map_key`'s shape and `types/map_keys.rs`'s walk — but `v == nullptr` is the documented null test and must survive, so the refusal is on *containment*, not on the type; (b) state it in the spec, which is +tokens for a sentence saying a container's equality is not what it looks like; (c) leave it, and record that §4.19's boundary is where the language's guarantees stop, which design.md §1.11 already says. **Panel path** | crates/heroes/src/emit/structural.rs · spec:72-74 · docs/panel/069 | the FFI is this language's whole library chapter, and this is the one place `==` gives a confidently wrong answer with no float and no nan in sight
- [x] **CLOSED 2026-08-16 by panel 075** (`docs/panel/075`), five seats, no veto against the repair. **The wording is not the ergonomist's and not the warden's**: `<` `<=` `>` `>=` with an `f32`/`f64` operand **abort** when either side is a `nan` — the answer this language already gives at `sort` and at a map key — while `==` and `!=` stay exactly as they are, so every `== 0.0` divide guard survives. **`is_nan` was vetoed twice** (four modules, two mandatory spec tests, a name reserved in every scope forever) and what its two approving seats wanted is delivered at zero new names: the spec points at **`x != x`**. Spec **+27, 3440 → 3467**, in three places rather than one, because an enumeration that names the operators and omits `sort` claims `sort` is fine. **The item's own price is corrected twice over**: it said eleven tokens, the swap measures **+5**, and the wording it priced is one candidate 2 makes false. **The item's premise held and grew** — the coordinator ran it and found the same program answering **nan** or **1.0** depending on which line the nan is on, and the ffi-pragmatist compiled two De Morgan-equivalent validators that disagree at exit 0. **Implementation owed**: +23 lines in `emit/operator.rs`, the three spec edits, a golden that exercises **both** guard sites, and the test that fires if `sort.c`'s two guards are ever deleted — because the claim that this rule subsumes them is **false**, measured three times. Was | panel 069, llm-ergonomist — **found from the spec alone, before it knew the question** | **With a `nan`, `<` and `>=` are BOTH false, so `if/else` takes the else branch in silence.** Confirmed by running it: `n < a` → `false`, `n >= a` → `false`, and the `else` arm executes with no diagnostic. IEEE requires this and every language agrees, so the compiler is not wrong — but the spec says nothing, and the seat that reads only the spec found it while doing something else, which is the strongest evidence this project's instruments produce. **The cost is bounded and worth stating**: this is a *silent branch*, not a wrong value, and the reader's model — *"one of `<` and `>=` must be true"* — is one every programmer holds. The ergonomist's own repair was **eleven tokens inside the sentence panel 069 already landed**: *"Every comparison with `nan` is false, `!=` true"*, which it argued was the best trade on the page because it closes a second bug class for the price of a clause. That wording was **not** adopted at 069 — the sitting took the warden's shorter form — so this is a live amendment with a measured price and a named alternative, not a discovery | spec § Values (the sentence panel 069 rewrote) · docs/panel/069 § Three findings nobody asked for | the seat that only reads the spec found it while doing tasks, which is the only way it could have been found
- [x] **ANSWERED 2026-08-16 by panel 076: refused as that sitting's answer, kept alive with its conditions written down.** Five seats. It is **defeasible by any C library that installs its own `SIGSEGV` handler** — SDL, a GC, any JIT — measured at exit 139 in silence with no compile error, and §1.11 makes that the normal case rather than a corner; it **blinds ASan**, turning a 262-line symbolised report into 2 lines unless guarded by `__has_feature(address_sanitizer)`; a second fault inside it **hangs at 100% CPU**; its message **lies** when the fault is inside C; `SIGSTKSZ` and `_SC_SIGSTKSZ` compile on disjoint platforms; and Windows is a second implementation. **It returns as an unconditional REPORTER** — claiming a memory fault, never a cause — the day the three-leg CI matrix proves the classifier on Linux and Windows, which `.github/workflows/ci.yml` already runs on `workflow_dispatch`; the golden must assert **the printed line**, not exit 139, which the inverted build also produces. The historian's contradiction stays on the record: **OCaml ran this experiment for twenty years and abandoned it**, moving the check into the compiler's own prologue at OCaml 5. Was | panel 070, historian — **the shape nobody tabled, and four systems ship it** | **Turn a stack overflow into a printed message instead of a silent SEGV, at zero cost on the fast path.** Rust does exactly this in a native binary with no scheduler — `sigaltstack` plus a SIGSEGV/**and SIGBUS** handler that checks whether the faulting address is in the thread's guard-page range, then prints *"thread 'main' has overflowed its stack"* and aborts ([`library/std/src/sys/pal/unix/stack_overflow.rs`](https://raw.githubusercontent.com/rust-lang/rust/master/library/std/src/sys/pal/unix/stack_overflow.rs), sourced). The JVM, OCaml and AddressSanitizer use the same mechanism; Windows has a different one (`SetThreadStackGuarantee` + a vectored handler) printing the identical message. **It reports rather than prevents**, which is why it is not a substitute for the item above — but it is the only shape that also catches deep **user** recursion, which this language will have and today has nothing for at all. Three potholes are on the record and each is a real cost: since **glibc 2.34** `SIGSTKSZ` is no longer a compile-time constant, so `static char alt[SIGSTKSZ];` **does not compile** on modern Linux and the size must come from `sysconf`; a signal stack has **no guard page of its own**, so a handler that overflows it is back to a silent SEGV (rust-lang/rust#69533, open); and **ASan installs the same handler**, so `--sanitize` and this must be reconciled rather than assumed compatible. Also owed: a decision on what the message says — Go prints the stack bounds and the pointer, which is more than Rust does, and this compiler has `#line` information Rust does not. **Panel path** (it is runtime architecture and it touches every platform), and it should be decided together with the item above rather than after it, because if it lands, the argument for an expensive prevention weakens | docs/panel/070 § the historian's row · runtime/parts/panic.c | design.md §1.12 forbids a silent crash, and this is the shape that makes *every* crash loud rather than one class of it
- [x] **CLOSED 2026-08-16 by panel 074** (`docs/panel/074`, `930e66b`), which seated the two judges panel 072 refused to rule without. **The word is `tag`** — `record FileStat tag stat`, in `partial`'s modifier slot, the Heroes name free and the C tag exact. The free-form string `is "struct stat"` took **two vetoes** from opposite directions: the blind seat because whether the declared thing is a value or a pointer would be decided by characters inside a string Heroes never parses, the warden on §1.0's smallest self-consistent surface at +39. `as` was objected to because every language spends it on introducing the **new** name and `spec:9` already says *"No aliases"*; the warden measured **no token difference** between `tag` and `as` (both 3462) and declined to break the tie. **The spec sentence folds rather than appends** — `spec:217`'s *"the same name"* is what the marker falsifies, so it is rewritten, **+26 against a −4 named removal, net +22**, twelve tokens cheaper than the cheapest appended form. **Both seats condition their approval on the same repair**: `emit/ffi.rs:127`'s `declaration()` matches Function and Constant and drops `record` through `_ => return None` (verified by reading it), so a `record` this program declared `extern` never reaches exit 1 — CLAUDE.md §7's own rule, unimplemented for records. Re-measured this session over six real header sets: **323 struct tags, 109 tag-only**; **6 of 328 tags are also a function or an object** (`flock`, `sigaction`, `sigvec`, `stat`, `timezone`, `wait`), two more than panel 072 found, which is why the marker carries a C name rather than being a boolean. **Implementation owed**, and it lands with `DECIDE.md:344`'s rider and an `emit/typedefs.rs` split. Was | panel 071, ffi-pragmatist — **found while running item D's experiment, and it is larger than any of the six** | **A tag-only C `struct` is unbindable, at exit 2.** `struct Plain { int32_t a; int32_t b; };` — declared with a tag and no `typedef`, which is how most of C is written — gives *"internal error: compiling the generated C failed: **use of undeclared identifier 'Plain'**"*. The emitter writes the bare record name where C requires `struct Plain`. Verified by the coordinator on a header of its own. **Measured over real headers: POSIX 35 of 46, curl 35 of 49**, sqlite3 3 of 23, SDL3 5 of 134 — and the POSIX set is `struct stat`, `struct timeval`, `struct timespec`, `struct sockaddr_in`, `struct in_addr`. design.md §1.11 says everything comes from C, and this is most of the platform. **It cost the judge a shim header to run the sitting's own experiment**: a one-line `typedef` whose entire content is a name, which is precisely the shim §1.11 exists to abolish. What is owed is a decision on **where the tag comes from**: the emitter cannot guess, so either the group's `record` carries the spelling (a surface change, therefore a panel), or the emitter probes with `_Generic` / `__builtin_types_compatible_p` and picks, or `extern_record.rs` emits `struct <name>` whenever a bare `<name>` does not compile — which is prose-matching and `ffi_narrowed.rs` quotes cgo's reason for never doing that. **Panel path**, and it should be decided before `M-complete-structs` rather than inside it | crates/heroes/src/emit/extern_record.rs · docs/panel/071 § the seventh hole | §1.11 makes the platform the library, and `struct stat` is not a corner of it
- [x] **ANSWERED 2026-08-16 by panel 079's spec-warden: message only, +0 spec tokens, and a spec sentence is VETOED.** A compile diagnostic is spec business only where a program can read it (`e.code`, `spec:136-148`) or where the message **is** the feature (`???`, `spec:192`); this is neither, so it costs nothing by construction rather than by grace. And the repair the message must name is **already in the spec**: `spec:222`'s `record Font partial` is how a field with no size is omitted, so the message must POINT at it rather than the document restate it — the priced sentence was +26 and would have been the growth §1.6 calls a failure of §1.2. **The wording LANDED the same day** (`fa2636b`), and the fact it needed was a constant the emitter already had: a flexible array member sits at `offsetof(T, m) == sizeof(T)`, so its **tail is 0** where a sized array leaves a positive one — measured `{4, 4, 0}` against `{12, 4, 8}`. So it is a second `_Static_assert` over numbers the tail measurement already computes, asked **first** and carrying its own marker, and the length question is only reached when it holds. The author now gets `error[ffi_flexible_array_member]` — *"`flex.h` gives `WithFlex.data` no size — it is written there with empty brackets, so it is not a field this record can name at any length"* — with the route `spec:222` already gives, **and the route was run**: `record WithFlex partial` naming `len` alone compiles and prints. The item had filed this as emitter architecture (*"a second assertion or a probe, neither obviously right"*); it was neither. Was | panel 071 R4's condition, owed with the repair that landed | **The flexible-array-member diagnostic must NAME the flexible array member.** The exit-2 is repaired (`extern_record.rs`, the `sizeof` conjunct became a **tail measurement**, `sizeof(T) − offsetof(T, m) >= n * sizeof(elem)`, which is defined for both shapes where `sizeof` of an incomplete type is a hard error) and the author now gets **exit 1 on their own line**, verified. What they get *said* is the generic `ffi_field_type`: *"`WithFlex.data` is not `i8[4]` in `ffi.h` — clang read the header's struct and the field disagrees"*. The ffi-pragmatist made the wording a **condition of its approval**, and it is right: a reader told the field *disagrees* will go hunting for a length that does not exist, when the answer is that **the field cannot be a `record` member at all** and the route is `ptr` plus an accessor. The obstacle is real and is why this is queued rather than done: the diagnostic is recovered from a **failed assertion**, which carries the marker and not the reason, so distinguishing *wrong length* from *flexible array member* needs either a second assertion or a probe. Neither is obviously right and both are emitter architecture | crates/heroes/src/emit/extern_record.rs · crates/heroes/src/emit/ffi.rs | §4.17 asks the message to carry the repair, and this one carries a repair that does not exist
- [x] **CLOSED 2026-08-16, landed with the marker that made it reachable** (`fe091c7`). `emit/extern_record.rs`'s field assertion now carries the **declaration's** name — one token by construction, because it is an identifier — where it carried the C type's, which `tag` makes two words. Verified by running both halves: a wrong field width on a **tagged** record is `error[ffi_field_type]` at **exit 1** on the field's own line, where the whitespace split would have made it exit 2 *"internal error"* while the typedef'd row stayed exit 1 for the same mistake. The item said it was *"a change no test can fire on"*; it is now the fourth row of `a_struct_c_names_only_by_tag_binds_and_says_so_when_it_cannot`. Was | panel 072, ffi-pragmatist rider 1 — **true today, independent of anything the sitting adopts** | **The same author mistake gets two different exit codes, and the difference is a whitespace split.** The field assertion carries `heroes-ffi-field {c_type} {field}`; `emit/ffi_record.rs:46` splits on whitespace and reads token 1 as the record name. That is correct while every `c_type` is one word — and the moment a C type is two (`struct stat`, `unsigned long`), token 1 is `struct` and the recovery cannot find the declaration, so §7's named exception does not reach it. Measured by the seat: **exit 1 `ffi_field_type` on a typedef'd record, exit 2 "internal error" on a tagged one**, for the same mistake. The repair is one line — the marker carries the **declaration's** name rather than the C type's — and the seat applied and re-measured it. **Filed rather than landed because it is only reachable once a two-word `c_type` exists**, which is what panel 072 declined to adopt; landing it now is a change no test can fire on | crates/heroes/src/emit/ffi_record.rs:46 · crates/heroes/src/emit/extern_record.rs | a recovery keyed on a shape rather than on a value is CLAUDE.md §11's premise, and this one expires the day a C type has a space in it
- [x] **CLOSED 2026-08-16 by panel 073** (`docs/panel/073`, `930e66b`), five seats. **The `variant` shape is refused on three vetoes**, and the premise it rested on was corrected before the sitting: `SDL_Event`'s `type` is a member **of** the union overlapping every arm's first field (C11 6.5.2.3p6), not a sibling, and the arm↔value mapping is **124 enumerators over 38 arms stated only in doc comments** — the author would declare what clang cannot refute, inverting §4.19. The historian supplied the precedent this project had never assembled: **Wirth deleted the identical construct from Oberon**, **Ada standardised it only by suppressing its own checks** (RM B.3.3 — punning erroneous, `=` a `Program_Error`), and **rust-bindgen refused the same mapping in 2021** for this item's own reason. Priced at **+131…+186** with no admissible payment (`heroes mutate`'s 13 operators have no arm for it). **What lands costs +0 spec tokens**: `spec:217` already says a group's `record` **is the header's struct**, and a union is not one — CLAUDE.md §12, the compiler has the bug. The detector is `_Static_assert(sizeof(T) >= Σ sizeof(field))` — the **sum-of-sizes** form, because the pairwise `offsetof` form calls a struct of two GNU zero-sized members a union (both measured this session) and because it is O(n) where a 16-field record emits 120 conjuncts pairwise. **It gates construction and `eq`/`hash`, never the declaration** — the historian's correction against the coordinator's own brief (five languages restrict the construction *expression*; the two that restricted the type had users route around them, Go's route-around defeating its own runtime pointer check) and the ffi-pragmatist's measurement, which compiled **three SDL3 programs that read unions correctly today**. Classified as a fifth CLAUDE.md §7 class, `ffi_union_field`, exit 1 with both field names, one `#~` golden. **Implementation owed.** Was | panel 072, ffi-pragmatist rider 2 — **a wrong value at exit 0, and it has nothing to do with tags** | **A `record` cannot express a C `union`, and binding one produces a wrong value silently.** Measured by the seat on a **typedef'd** union — so this is live today with no tag anywhere near it: `UDef(i: 1, f: 1.0)` emits a compound literal `(UDef){.i = t1, .f = t2}`, C's last initialiser wins, and `a.i` reads **1065353216** at exit 0 with only a warning. A union's whole point is that its members overlap, and record construction assumes they do not. Three shapes: (a) refuse a `record` over a union outright, which needs the emitter to know it is one; (b) a marker with construction refused and reading permitted, which is `partial`'s asymmetry one type over; (c) leave it and say so in Part 6 with the falsifier §12 requires. **The seat vetoed extending panel 072's marker to `union` until this is answered**, on the ground that widening reach into a path that already produces wrong values is the wrong direction. Only 2 union tags exist in the 19 headers it measured, so the pressure is low and the silence is the problem | crates/heroes/src/emit/extern_record.rs · design.md §4.19 | §1.12 says a defensive check must surface a defect; here there is no check at all and the defect prints a plausible number

## From panels 073 and 074 (2026-08-16) — what the sittings found on the way

- [x] **FIXED 2026-08-16 (panel 077), and the brief that filed it was wrong about the mechanism.** It said C cannot detect this and that the two obvious predicates miss `union { int32_t i; float f; }` — true of both, and beside the point: **`__builtin_classify_type(*(T *)0)` answers 13 for a union and 12 for a struct**, in a `_Static_assert`, on a ONE-member union, using the builtin `extern_record.rs` already calls on every field. Two seats that compile found it independently. The predicate replaced `sizeof(T) >= Σ sizeof(field)` wholesale, which was **also incomplete for a padded union** — `SDL_Event` is padded, and it was still printing 1065353216 three hours after panel 073. A **premise-death control** ships with it: two assertions stating this clang still answers 13 and 12, because clang answered 12 for unions before 2019. Verified today: exit 1. Was | panel 073, spec-warden — **measured, and the assertion cannot see it** | **A non-`partial` `record` naming ONE field of a C union compiles silently and claims completeness falsely.** Panel 073's detector needs two declared fields to fire, so a record that names one member of a two-member union produces **zero warnings and exit 0**, keeps `==` and `hash` (which then compare and hash bytes that may hold another arm), and asserts by its own non-`partial`-ness that it names *all* the header's fields — which is false. The seat filed it as a condition of its approval rather than letting it be discovered later. Two shapes: the completeness check learns to compare the record's field count against the C type's member count (it cannot today — `hero_ffi_complete_*` initialises with N zeros and a one-member initialiser of a union is legal C), or a union-over-`record` must carry the marker panel 073 filed and is refused without it | crates/heroes/src/emit/extern_record.rs · docs/panel/073 § Two holes | it is the same silent-wrong-answer class panel 073 convened over, in the one shape the repair does not reach
- [x] **ANSWERED and LANDED 2026-08-16 (panel 077): the spec says the SIZE and deliberately not the construction**, +13 measured (3493 → 3506), ledger row written. This is the rare silence panel 035 R4 does **not** dismiss, and the reason is exact: R4 refuses a silence when the compiler is loud at the moment of the mistake, and here **there is no mistake** — a reader who believes the record is the size of its field list gets a **correct program**, because the emitter declares the local with the header's own C type. What the silence cost is a **binding never written**: two blind seats in two sittings refused to write a POSIX out-parameter binding rather than guess. The historian funded it: **every FFI that let the field list decide the size shipped a wrong number** — Nim's `dirent` 272 against 1, Rust's `struct stat` 136 against 120. Construction is refused rather than deferred: every wrong guess there is exit 1 with the rule in the message. Was | panel 073 + 074, both blind seats independently | **`partial`'s SIZE is unstated, and a reader binding an out-parameter has to guess whether their buffer is 4 bytes or 128.** Measured twice today and the behaviour is **right**: a `record Big partial` naming one `i32` of a 128-byte C struct, passed as `@b` to a C function that writes 124 bytes, comes back complete (checksum **21204 = 124 × 0xAB**, exit 0), and the ffi-pragmatist confirmed the same on a real `SDL_Event` — the emitter declares the local with the **header's own C type**, so size and alignment are C's. The spec says none of this. The 073 seat could not write its SDL task without assuming it and wrote *"if that assumption is wrong my program corrupts the stack, and the specification gave me no way to find out"*. Decide whether § FFI's `partial` sentence gains a clause (priced: the seat's own wording is ~4 lines) or whether this is a golden case's job | spec:220 · docs/panel/073 · docs/panel/074 | the one thing a reader must know before handing C a buffer is how big it is, and the document is silent
- [x] **ANSWERED 2026-08-16 by measurement, and two of its three points are dead.** (a) **A `partial` record CAN be constructed**, naming the fields it declares, and the rest of the C object is zero — C's own `= {0}` (C11 6.7.9p21). Measured end to end with panel 074's marker: `record FileStat tag stat partial` + `FileStat(st_size: 0)` + `stat(path.cstr(), @st)` gives **rc = 0, size = 13** on a 13-byte file. Panel 061 had already decided it with a veto threat behind it — *"refusing it deletes the only expression in the language that produces an `SDL_Event`"*. No sentence is owed (panel 035 R4: every wrong guess is exit 1 with the rule in the message). (b) **Fields match by NAME, not by declaration order** — a record declaring `second` before `first` against a header that says the opposite prints the right values, so a transposed pair is *not* a silently different program. (c) What survives is resolving `mode_t` to a width and a sign, which is a real second lookup the spec demands and the header does not answer — filed as the residue rather than as this item. Was | panel 074, llm-ergonomist — **three hesitation points that block a POSIX binding even after `tag` lands** | **Can a `partial` record be CONSTRUCTED, and with which fields?** The seat could not write `stat`, `gettimeofday`, `dirent` or `addrinfo` without knowing: *"I wrote `stat(st_size: 0)` and I do not know if that is a program."* Every out-parameter binding in the platform needs the answer. **Two of its three points are now measured and one is dead**: fields are matched **by NAME, not by declaration order** — a record declaring `second` before `first` against a header that says the opposite prints the right values (measured today), so a transposed pair is *not* a silently different program; and the third, resolving `mode_t` to a width and a sign, is a real second lookup the spec demands and the header does not answer. What is left open is construction | spec:97 (construction names every field) · spec:220 · docs/panel/074 | the marker makes the type nameable and this is what still stops the program
- [x] **CLOSED 2026-08-16, and the question had been answered four days earlier.** Panel 078's spec-warden read forward in the record where the coordinator had not: **the word `integer` at `spec:149` is panel 035's own artifact** — 2026-08-12, three seats, commit `ec5558b`, a one-word diff, recorded at `DESIGN-LOG.md:207`. So the document is **true and deliberate** rather than silent, this item's premise (*"an inconsistency the spec never states"*) is false, and panel 035's historian had already found the precedent running the other way: *"Zig shipped that abort and moved off it; Python traps and NumPy overrode it."* Two further claims in the convening brief were measured false: the routes are **not** mostly division (`inf` is reachable four ways with none — a 400-digit literal, `to_f32`, eleven squarings; `nan` five, including `extern sqrt(x: -1.0)` at exit 0), and candidate 2 owed `spec:61` no repair, because that bullet is about **conversions**. The cheapest option measured **−1 token** and was vetoed anyway, on design.md §1.1's own warning that the token indicator is *"frequently a misleading one"*: its **program** cost is the highest on the ballot. **What this item bought is the rule now in CLAUDE.md §1** — a silence in a document and a silence in the record are opposite things, and a sitting convened on the first must check the second. Was | panel 075, historian — **the shape nobody tabled, and it is the one with the precedent** | **Abort where the `nan` is BORN rather than where it is compared.** Erlang has no `nan` at all — `0.0/0.0` raises `badarith`, decades in production, never relaxed — and Zig's live proposal (`ziglang/zig#23173`, 2025-03-10, andrewrk, "urgent") adds `real16/32/64/128` that **cannot store NaN or inf**, having **closed** `#11234`, which proposed exactly this sitting's comparison-abort. Measured here today and it is an inconsistency the spec never states: **integer division by zero aborts, `1.0 / 0.0` gives `inf` at exit 0**, and `0.0 / 0.0` gives a `nan` at exit 0. §4.14's own dialect — overflow aborts, division by zero aborts — points at the source. **Unpriced**, and it interacts with `spec:61`'s *"Nothing fails to fit a float: too large is `inf`"*, which names `inf` as a value the language produces on purpose. It is a separate sitting from the comparison and it should be convened as one | spec:61 · spec:149 · runtime/parts/f64.c · docs/panel/075 | the sitting on comparisons found the language already aborts for integers and not for floats, and nobody had noticed

## Open — found while the sittings ran (2026-08-16)

- [x] **ANSWERED 2026-08-16 by `docs/panel/081`, landed: the print sentence gains it, +12 against a −10 named removal, net +2 → 3512.** Adopted where the item's second option said it might go instead — the *print* sentence rather than the built-ins list — because that is where the rendering rule lives and the built-ins list is a list of names. Five judges, no veto; the spec-warden's approval was conditional on payment and it **found the payment itself**: `spec:177`'s *"None of these names may be redeclared."*, −10 measured, verified loud by the coordinator on four shapes before it was spent (`function len`, a local `len`, a parameter `print`, a prelude-written `map` — each `error[builtin_name_taken]` with the rule in the message) and stricter than the language besides, since a record **field** named `len` compiles at exit 0. **Two things the sitting added that the item did not ask for.** The compiler-engineer attacked eleven rendering shapes and reported the clause *exactly* true rather than approximately — there is no shape in this language where a `bool` prints `1`, and 27 of the 81 `run/` goldens already pin it. And the removal **broke an instrument nobody knew depended on it**: `measure/spec.rs` delimited the built-ins list by searching for that very sentence, with a blank-line fallback its own comment called wrong, so deleting it made the parser read `inf` out of the float-printing sentence. CLAUDE.md §11's shape, expiring in silence with the comment still reading as correct — repaired by asking the value (where does *this* sentence end) instead of the world (does that other sentence exist), and pinned at both ends so a delimiter that shortens the list fails instead of quietly shrinking its own coverage. Was | panel 076, llm-ergonomist hesitation H6, verified by the coordinator | **`print` on a `bool` is undocumented, and one seat's whole answer rode on it.** Measured: `print(true)` writes `true`, `print(false)` writes `false`, `print("x", true, "y")` writes `xtruey`. The spec specifies float printing **to the character** — `1.0`, `1e-06`, `inf`, `nan` at `spec:179` — and says nothing about `bool` at all. The seat's task printed exactly one value, a `bool`, and it could not say whether the answer was `true` or `1`; it registered the prediction that **≥30% of readers cannot state what `print(a == b)` outputs, or the population splits between `true` and `1`**. Orthogonal to the sitting that found it. Decide whether the print sentence gains `bool`, or whether the built-ins list carrying `print` is where it belongs | spec:179 · runtime/parts/ (the bool printer) · docs/panel/076 | the document specifies one scalar's rendering to the character and is silent on another, and a reader cannot tell deliberate from missing
- [x] **CLOSED 2026-08-16: clause B landed after its repair, +13 against a −11 removal, net +2 → 3514.** R2 held the clause on the spec-warden's condition *"exit 0 **or exit 1**"*; R3 landed exit 1 the same day and the condition was re-measured as met before the clause was written. The wording **shows** the form — `never a `[T]`; build one with `[a, b, c, d]`` — rather than naming it, on the historian's only measured discriminator: this document held exactly **one** array literal and it was the empty one, and C's brace initialiser is the reader's prior while `{}` is this language's map. **The payment is a §11 expiry the sitting's own warden did not find**: *"…because clang converts a wrong one in silence"* was true when written and the compiler falsified both halves — a wrong parameter is `ffi_parameter_type` and a wrong field is `ffi_field_type`, both exit 1, measured today. §1.4 makes that a removal rather than a rewrite: redundancy is spent where a wrong guess is silent, and this one has been loud since panel 071. Was | **ANSWERED 2026-08-16 by `docs/panel/081`: neither — the question was premature, and the sitting found out why.** Not four tokens and not a golden: the clause is **unripe**, held until the surface it describes stops crashing the compiler. Three judges went and *used* fixed-array fields instead of discussing the sentence, and the surface is broken in **four positions**, every one of them `heroes check` **exit 0** — `r = a.reserved` (exit 2), `reserved: a.reserved` (exit 2), `w.reserved @ [...]` (exit 2), `[a.reserved]` (exit 134, `hero_unreachable`). Only reading by index works. CLAUDE.md §7 says exit 2 means the compiler is wrong, and here it is. **The brief's own reasoning was falsified by the judge that approved the clause**: the brief called the misreading *loud* (three measured diagnostics), and the ffi-pragmatist showed the misreading panel 077 actually made — *"therefore `partial`"* — **exits 0 with fields silently absent**, so it is silent and §1.4 funds it after all. The coordinator falsified the historian's counter-inference the same way, by running it: the `partial` route is **memory-safe**, zero-filled by C11 §6.7.9p21 through the compound literal the emitter writes, verified with the stack deliberately dirtied and under `--sanitize`. **What outranks all of it**: `char[N]` cannot be declared at any of Heroes' eight widths — 26 of 81 fixed-array fields in eleven real header sets, `uname()` unreachable at any price — because `emit/extern_field.rs` asks class+width+sign for a scalar and `_Generic` type identity for an array element, and C's `char` is a third type. Repair built and compiled by that seat, nine cases, three real headers. **Open, and deliberately so**: the four shapes (R3) and the `char[N]` hole (R4) are the work; the wording is chosen **after** the repair, because two seats showed the offered wording is true only of the corner that survives | docs/panel/081 · crates/heroes/src/emit/storageless.rs · crates/heroes/src/emit/extern_field.rs · crates/heroes/src/types/ffi_decls.rs | Was | panel 077, llm-ergonomist Task C, **corrected by the coordinator** | **A reader concluded a record with a fixed-array field has no constructible value, and the reasoning was sound.** From `spec:219`'s *"a fixed array of one: `i32[4]`, never a `[T]`"* plus the absence of any literal for one, the seat inferred that `record Widget { x: f32, y: f32, reserved: i32[8] }` cannot be built, and therefore that `partial` is the only route to a binary — *"the page reaches it by elimination rather than by saying so"*. **Measured and it is false**: `Widget(x: 1.5, y: 0.0, reserved: [0, 0, 0, 0, 0, 0, 0, 0])` compiles and runs at **exit 0**, and omitting the field is `error[missing_fields]` at exit 1. So an array literal **is** how a fixed array is written, and the clause that says a fixed array is *"never a `[T]`"* — true about the representation, and panel 062 chose it for exactly that — reads to a careful reader as *there is no literal*. The inference was valid and the premise it rested on is a phrasing. Decide whether `spec:219` gains the four tokens that say the literal is the same one, or whether this is a golden's job | spec:219 · docs/panel/077 · docs/panel/062 | a sentence true about representation is being read as a sentence about syntax, and the reader it misled was the instrument this project trusts most

## Open — found by panels 083 and 084, measured, not fixed

- [x] **FIXED 2026-08-16, and the repair is two halves because the defect was.** The answer moves onto the **copy**: `Function::call_instances` is per-instance and substituted by `mono_subst`, so each copy knows which callee its inner calls reach, and the emitter asks the copy before the checker's span-keyed table. And the fallback goes in the **loud direction** (CLAUDE.md §11) — a missed lookup used to emit the literal NAME `hero_unreachable`, handing arguments to a zero-argument `_Noreturn`; it now emits `hero_unreachable()` and says *compiler bug*, which compiles and aborts instead of failing at clang. Golden `run/fixedbugs-a-generic-calling-a-generic`, and the shapes around it were attacked before it was written: **three levels deep at three element types** (`7`, `ziggy`, `5`), one generic reaching **two different generics** at two types, and polymorphic recursion, which must still be refused and is. Was | panel 084, ffi-pragmatist + compiler-engineer, reproduced by the coordinator 2026-08-16 | **A generic that calls a generic does not compile — `check` 0, `build` exit 2, and no golden covers it.** `function head<B>(ys: [B]) -> B` returning `firstof(ys)` gives `internal error: compiling the generated C failed: error: too many arguments to function call, expected 0, have 1` on `t2 = hero_unreachable(t1);` — a line the compiler wrote. Cause located: `Checked::instantiations` is `BTreeMap<u32, Vec<TyId>>` keyed by `span.start` (`types/checked.rs`), **one answer per span**, so it cannot hold two instantiations of one enclosing generic; `emit/ops.rs`'s `Callee::Heroes` lookup then misses and falls back to the literal name `"hero_unreachable"`, passing arguments to a zero-argument `_Noreturn`. The correct callee is defined a few lines below the bad call. **Of the four goldens with two or more generics, every call is from `main`**, which is why this survived. It blocks composing generics, and §1.11's Tier 2 — `map`, `filter`, `fold`, `find`, `any`, `all` — **is** a library that composes; the port will want it. Not a panel question: it is a defect with a located cause | crates/heroes/src/emit/ops.rs (the `unwrap_or_else` fallback) · crates/heroes/src/types/checked.rs · crates/heroes/src/ir/mono.rs | the fallback is CLAUDE.md §11's quiet direction where the loud one was owed — a missing instance should be `hero_unreachable()` or an assertion, never a name that silently type-checks wrong
- [x] **DECIDED 2026-08-16 and MOVED to `SCHEDULED.md`, joining panel 082 R3 — and the measurement that decides it is new.** The question was whether this needs its own repair or the queued call-site pass. It needs the pass, and it can wait for it, because **the hazard is guarded**: measured today, a real `nan` key reaching a map through a generic gives `panic: a map key that is not equal to itself (nan)` at exit 134 — a deliberate, clean stop, not corruption. So this is a **consistency** defect and not a **safety** one: §12 says the compiler is wrong because the spec forbids the program, and §1.12 is satisfied because it stops visibly. **Why it cannot take R1's shape**: `sort`'s domain is restricted, so refusing `sort(xs)` on `[A]` deletes nothing that worked; a map keyed on `K` is legal at `str` and at every integer, so refusing the body would delete working programs. The concrete key is known only at the call, which is exactly what panel 082 R3's pass reads. Was | panel 084's residue, measured by the coordinator 2026-08-16 | **A restricted domain that is a TYPE escapes through a generic, and it runs.** `m: {f64: i64}` written directly is `error[float_map_key]` at exit 1. The same map built **inside** `function tally<K>(k: K)` as `m: {K: i64} @ {}` and keyed at `f64` by the call is `check` exit 0 and **runs at exit 0**. `float_map_key` exists because a `nan` key is stored and never found again (panel 069 R4), so this is a live hazard rather than a tidiness question — and unlike `sort`, which at least died at `build`, this one produces a program. **It is why panel 084 R1's wording is over *restricted domains* rather than over *built-ins***: the compiler-engineer named the second domain and the coordinator reproduced it. Whether the repair is the same shape as R1 (refuse at the declaration inside the generic) or needs the call-site pass panel 082 R3 queued is the open part | crates/heroes/src/types/map_keys.rs · crates/heroes/src/types/ordering.rs (R1's shape) · docs/panel/082 R3 | one rule refused two ways depending on whether a generic stands in the middle
- [x] **DECIDED 2026-08-16 and MOVED to `SCHEDULED.md` with the row above — same class, same pass, same measurement.** Verified today under `--sanitize`: it is panel 061's **own deliberate** `hero_panic` — `a partial record has no structural equality`, exit 134 — so nothing is corrupted and nothing is silent. §12 still says the compiler is wrong (spec:221 promises a compile error *"for it and for any value holding it"*), and §1.12 says it can wait, which is the whole disposition. It cannot take panel 084 R1's shape either: `==` is legal on almost every type, so refusing it on a type parameter would delete far more than it saves. The concrete type arrives at the call. Was | panel 084, ffi-pragmatist, reproduced 2026-08-16 | **`spec:221` promises a compile error and delivers a run-time abort when a generic stands in the middle.** *"comparing it and using it as a map key are compile errors — for it and for any value holding it"*, of a `partial` group record. Direct: `error[ffi_partial_operation]` at exit 1. Through `function same<A>(x: A, y: A) -> bool` with `x == y`: `check` 0, `build` 0, **run 134**, `panic: a partial record has no structural equality`. §12 asks which artifact is wrong; the spec's sentence is the promise and the compiler is not keeping it. Same class as the row above and probably the same repair | spec:221 · crates/heroes/src/types/partial.rs · docs/panel/084 § Filed | the FFI's own row, and it fails later than `sort` did

- [ ] **[089 row 7]** M-selfhost-port, found by the knot's own first test (2026-08-17) | **The spec's §Tests example does not compile, and both compilers agree.** `spec/heroes-spec.md:186` reads `assert dist2(Point(x: 0, y: 0), Point(x: 3, y: 4)) == 25` — and `dist2(a: Point, b: Point)` has two parameters of one type, so §Functions' own rule ("named arguments are mandatory") makes the call `needs_label`, twice, exit 1. Measured today against the Rust compiler AND the Heroes port of the checker: identical refusal, identical fixes (`a: `, `b: `). The spec is in disagreement with itself — the rule at § Functions and calls versus its own example at § Tests and holes — and the repair (adding the two labels, +2 tokens by eye, to be measured for §1.2 at the edit) is a `spec/**` change, which is a PANEL PATH per CLAUDE.md §4. Not repaired in the loop for that reason; queued instead. Panel 018:41 quotes the same call in a dated record, which stays as written per §14 | spec/heroes-spec.md:186 · §Functions' rule at :98-99 | the first program the ported checker ever refused was the specification's own example, for a reason both compilers state identically — the thesis could not have asked for a better witness
- [ ] **[089 row 10]** **`continue` as the sanctioned arm no-op is a semantic trap inside counted loops** (M-selfhost-port, found 2026-08-17). The language refuses `_ = 0` as an arm body and the natural fillers are bare `continue`/`return`. But inside a `for`/`while` whose body ends with a counter (`at @ at + 1`), a `continue` arm SKIPS the counter — it is a loop jump, not a "nothing here". The port hit this three times in one session: an infinite loop in ir_own.hero's rewrite (a while whose index increment was skipped), and three silent position-counter desyncs in ir_phases.hero (twelve false "returns without releasing" the first time the owned checks ran on real IR). Outside a loop the compiler catches it (jump_outside_loop); inside, it is legal and wrong. Question for the author (spec/** path, panel if pursued): should arms have a true no-op form (e.g. `()` call is today's other option), or should the checker warn when a match arm's `continue` targets a loop whose body continues below the match? | the three fixes are in ir_own.hero and ir_phases.hero, commit pending
- [x] **ANSWERED 2026-08-18 by a FOURTH way the item had not listed, and the divergence is closed.** The three offered were: implement decimal->binary64 in Heroes over i64 arithmetic ("a real sub-project"), keep `selfhost/` float-free and gate float literals, or make both emitters pass the source text through. The fourth is the one §1.11 is built on: **C already owns a correctly-rounded decimal->binary64**, so the port asks `atof` for the VALUE and derives `%a`'s three fields with f64 arithmetic that is exact at every step — halving a normal, doubling a subnormal, `m - 1.0` for `m` in [1,2) by Sterbenz, and a mantissa scaled by a power of two under 2^52. No hand-written decimal parser, no byte reinterpretation, no language change, no runtime change, no new link (`stdlib.h` is already included). Measured over 127 programs, bootstrap C against port C: **19 differed on float spelling before, 0 after**, and the whole corpus went 26 -> 127 byte-identical with the two other repairs. Fourteen expectations in `emit_literal.hero`'s new test were read off the bootstrap's own emitted C rather than derived on paper. **And the item's premise was half false**: "Heroes has no str->f64 by design" is true of the LANGUAGE and irrelevant to the compiler, which may bind one — the same move `system()` already makes in `cli_toolchain.hero`. The sub-item nobody had filed at all: `x: f64 @ 1_0.5` printed **0.0** at exit 0 in the BOOTSTRAP, `parse::<f64>().unwrap_or(0.0)` under the comment "the parse cannot fail", and `lexer/number.rs` accepts `_` on both sides of the point. Repaired, with the fallback made loud, and a case named after it. Was | (M-selfhost-port, found 2026-08-17). The bootstrap emits f64 literals as C hex floats (`%a`, round-trip-exact, design.md §3.1) computed FROM the f64 bits. The port carries a float literal as its SPELLED TEXT, because Heroes has no str->f64 by design — so it cannot compute the bits, and would emit the decimal text instead. If the fixpoint compares bootstrap-C against port-C byte for byte (the mangler's own doc says it does), any float literal in selfhost source diverges. Three ways out, none free: (a) implement decimal->binary64->hex in Heroes over i64 arithmetic (exact, a real sub-project); (b) keep selfhost source float-free and gate float literals in the port until (a); (c) change BOTH emitters to pass the source text through (reintroduces the decimal round trip the hex rule exists to kill — likely refused). Needs an author call; (b) is the conservative default the port adopts meanwhile. | emit_literal.hero carries the divergence note

## The word width and the host's name (M-selfhost-port, emit_c_spellings)

The bootstrap derives C `long`'s width from `size_of::<c_ulong>()` and names
the host from `env::consts::OS`; Heroes has neither, and an extern constant
cannot carry a platform-varying width by construction — declaring `ULONG_MAX`
at either width fails the width assertion on the other platform, which is
this table's own lesson applied to itself. The port therefore takes the width
as a PARAMETER (`spelling(c_type, word_bits)`), still derived and never
tabled, and its caveat names both widths without naming the host OS.

**The question**: where does the selfhost compiler get `word_bits` when the
CLI department is ported?
- (a) a runtime entry point (`hero_word_bits()` in hero_os.h) — one C line,
  ABI addition;
- (b) a builtin the language grows — heavier, §10's stopping rule applies;
- (c) hardcode 64 with a premise-death test — exactly what the bootstrap's
  own doc forbids ("derived, never tabled").
The bootstrap's test contract (`the_word_width_this_compiler_claims_is_the_
one_clang_uses`) should hold whichever way; (a) looks cheapest and keeps the
derivation. Also: the port's caveat drops the "(macOS)" host name — accept
the divergence, or add the host's name to the same runtime entry?

## How the selfhost CLI reaches clang (M-selfhost-port, the CLI department)

The language has read_file/write_file/args/exit and no subprocess form. The
port's CLI needs to invoke clang and run the produced binary. The decision
taken (revocable): bind C's own `system()` via the language's FFI —
`extern "stdlib.h" function system(command: cstr) -> i64` — with clang's
stderr redirected to a file under build/ and read back with read_file, so
the ffi error mapping keeps working. This is §1.11 eating its own cooking:
the compiler written in Heroes reaches its toolchain the way any Heroes
program reaches C. The alternative (a runtime entry point wrapping
posix_spawn with argv separation) is safer against quoting but grows the
runtime; system() + careful quoting of the few paths we pass is v1. Veto or
confirm.

## The self-compilation cost, measured (M-selfhost-port, 2026-08-17)

The port compiles small programs instantly and its own 34,512 lines not at
all within 20 minutes. Measured with the -O0 selfhost binary, `check` only
(frontend, no emit), wall clock:

| input | modules pulled in | time |
|---|---|---|
| `token.hero` (193 lines) | 2 | 0s |
| `parse.hero` | the lexer + parser subtree | 24s |
| `checker.hero` | the frontend subtree | 152s |
| `emit.hero` | + the whole backend | >600s (killed) |
| `main.hero` (everything) | 143 | >1200s (killed) |

Superlinear, and the shape says where to look before any guess: every
`for … in xs` over a growing `[T]` in this port copies (COW value
semantics, §4.20), and several walks are O(n) scans inside O(n) loops —
`out_edges` in check_sized rescans every edge per node, `resolve_state`'s
`top_visible` scans the table, `emit_ctype`'s tables are maps keyed by
declaration. None of that is a language defect; it is the port written the
way the Rust reads rather than the way this language costs.

**This is CLAUDE.md §13's named case, and it is the author's to route.**
Performance is not a goal *and not a licence*: where a cost stops a program
the closure list needs from running at all, that is §1.0 compiler-need and
it goes to the panel rather than to an assistant's optimisation instinct.
The fixpoint (M-selfhost-fixpoint) is the v1 finish line and it cannot be
reached at these numbers.

**Measurement (a) is now done, and it moves the numbers without moving the
question.** The same inputs, the selfhost binary built at -O2 instead of
-O0 (4.1 MB against 6.3 MB):

| input | -O0 | -O2 | ratio |
|---|---|---|---|
| `token.hero` | 0s | 1s | — (both noise) |
| `parse.hero` | 24s | 8s | 3.0x |
| `checker.hero` | 152s | 54s | 2.8x |

A constant factor of ~2.8x against a superlinear curve: it buys one step
along the curve, not the shape of it. Whether it is enough for all 143
modules is being measured as this entry is written and the answer belongs
under it.

**Measurement (b) is still owed, and it is the one a sitting needs**: a
profile naming the top three call sites, so the panel is briefed with where
the time goes rather than with the shape argument above — which is a
reading of the code, not a measurement of it (§1's rule about an inference
presented as a measurement). It needs an instrument this project does not
have on the surface, and adding one is itself a §10 stopping-rule question:
`--profile` would be a flag nothing in the fixpoint invocation types.
The cheap alternative that needs no surface at all is `sample` (macOS) or
`perf` (Linux) against the running binary, which is the machine's
instrument rather than this compiler's.

- [x] **RATIFIED 2026-08-23** (author instruction, *"ratifica tutto"* — blanket over 085, 086 and 087; `docs/panel/085` § Author's verdict says what the yes settles). R1 and R4 final, R2/R3/R5–R8 as landed; the two engineer vetoes (compressing the seed, a `heroes golden` subcommand) keep their lift conditions; R7's mirror stays open in its own item below. Verified with the yes rather than assumed: the seed rebuilt in **3.75 s** and the compiler it makes ran the net at **845 passed, 0 failed**. Was | panel 085, provisional 2026-08-18 | **Ratify the seed and the split, or overturn either.** Adopted provisionally: **A1** — the generated C committed **raw** at `seed/heroes.c`, refreshed in the commit that breaks it — and **B4**, the archive of `crates/` leaving this milestone for one of its own. What a yes settles and what it does not: it settles that the seed lives in git rather than in a release (A3 is CVE-2024-3094's shape, and CLAUDE.md §14's publishing stop blocks it anyway) and that it is **not** compressed (git deltas plain text and cannot delta a compressed stream — measured, 2.74 MiB raw against 5.78 MiB gzipped by the third stored copy; the first copy really cost **2.50 MiB**, `git count-objects` before 6.56 and after 9.06 MiB). It does **not** settle the archive's own milestone chain, which is the row below. Two vetoes ride with it and both are the compiler-engineer's: **A2** (compression) lifts only on three consecutive real refreshes where gzip stays under raw, and **B2** (a `heroes golden`-shaped subcommand) lifts only on a harness capability `heroes run <harness>.hero` cannot express — its 79-line probe ran 65 of 65 `check/` cases green, so §10's third clause ("nothing if two existing invocations already compose to it") refuses the subcommand by its own text | docs/panel/085 · seed/README.md | the seed is the one artifact a stranger cannot rebuild without a working compiler, so where it lives is not a filing question
- [x] **RATIFIED 2026-08-23**, hours after being queued (author instruction, *"ratifica tutto"* — blanket over 085, 086 and 087; `docs/panel/086` § Author's verdict says what the yes settles). R1–R7 final as landed, each re-verified in the ratifying session: the ledger at `docs/measurements/010`, the three-way lock green inside the net (`spec: 8 passed`), `heroes measure` at **maximum 3512**, the forwarding pointer at `gate.rs:100-101`, the citations check at `suite_records.hero:235`. Still not settled by the yes: R4's four unmarked `ORDER:` walks (in `SCHEDULED.md`) and R6's CLAUDE.md:112 wording, which R6 left to the author on purpose. Was | panel 086, provisional 2026-08-19 — **queued 2026-08-23, because the panel file said `pending — docs/debrief/DECIDE.md` and this list had no such item.** The gap is the finding: a sitting that points at a list which does not carry it is a sitting nobody can ratify, and it went unnoticed for four days | **Ratify the ledger's move, or send the rows back.** Adopted provisionally: the 38 spec-budget rows leave `measure/gate.rs`'s doc comment for `docs/measurements/010-spec-budget-ledger.md`, and `tests/harness/suite_spec.hero` gains `LEDGER_ROWS` plus a three-way lock — the record's row count against the pin, the newest row's figure against `SPEC_TOKENS`, and `SPEC_TOKENS` against what `heroes measure` reports. What a yes settles: the ledger is a **dated record** under CLAUDE.md §14 rather than a comment, so it is append-only and outlives the file it was written in — which is what happened to `gate.rs` itself. What it does not settle: R4's four unmarked `ORDER:` walks, which are in `SCHEDULED.md` with a home. The recommendation is **yes** — the rows survived the archiving of the file that held them, which is the argument, and the lock is the first thing that ever checked them against the constant | docs/panel/086 · docs/measurements/010-spec-budget-ledger.md · tests/harness/suite_spec.hero | 38 numbers lived in a doc comment of a file that no longer builds, and nothing compared them to the value they were about
- [ ] **[089 row 9]** panel 085 R7, found 2026-08-18 | **The mirror of the field-assertion hole is still exit 2, and its repair needs a decision.** A field declared as a **scalar** where the header has an array is now exit 1 (`ffi_field_type`, fixed this milestone). The **mirror** — a fixed array declared where the header has a scalar, `d_type: i8[4]` against `unsigned char` — is still `internal error`, because that branch subscripts the member (`place[0]`) and a scalar cannot be subscripted. Measured today on `dirent.h`: `i8[4]` on `d_type` and on `d_ino` both. Three candidate repairs, and the choice is a §7 question because two of them change every generated file: **(a)** enumerate C's twelve integer types as array-pointer `_Generic` associations, each row asking `sizeof`/sign of the *C type* (well-formed everywhere, target-independent, ~12 rows per integer-array field); **(b)** define the sign macro in the probe preamble unconditionally, which panel 063 measured as renumbering **56 `#line` restores across five `tests/golden/emit/` files** where `UPDATE_GOLDEN` is forbidden; **(c)** leave it, and add the shape to §7's list of what stays the compiler's. Neither `__builtin_choose_expr` nor `_Generic` protects an unselected operand from type-checking — both measured on this clang today, so the obvious route is closed | crates/heroes/src/emit/extern_field.rs (the `Ty::Fixed` integer-element branch) · tests/golden/fixedbugs/ffi-a-field-the-header-has-as-an-array.hero | the author's own `extern` is the one place §7 says the compiler must not blame itself
- [ ] **[089 row 11]** panel 085, **could not reproduce** 2026-08-18 | **Two of the ffi-pragmatist's dirent findings did not reproduce, and the record says so rather than carrying them as fact.** The seat reported (1) that a Heroes program lists a directory today through `readdir_r` with `record Dirent tag dirent partial` and `d_name: i8[1024]`, run against the bootstrap, printing real names, clean under ASan and UBSan; and (2) that the port refuses the same file **only when the body reads `e.d_name[0]`**, making a declaration's legality depend on an unrelated expression. Attempted four ways today and blocked before either could be observed: **a group record with a `char[1024]` member cannot be constructed at all**, because a fixed array is built with a literal of exactly its length (`spec:219`), so `Dirent(d_name: [0])` is `fixed_array_length` and 1,024 elements is not a program anybody writes. Both compilers agree, in both the read and no-read variants — measured. So either the seat had a construction this session did not find, or the finding rests on a program that does not compile. **The open question is the useful half**: if `struct dirent` is genuinely unconstructible, then `readdir` is unbindable for a reason that has nothing to do with pointers, and design.md Part 6's row on struct passing is not what blocks it. **Note 2026-08-18 (M-harness-port step 1): nothing is waiting on this any more.** The harness's directory walk was the one caller that needed an answer, and it needs no `readdir`: `find -print0` plus `read_file` plus byte indexing lists a directory soundly with **zero** externs, measured on a name holding a newline, on both compilers (`DESIGN-LOG:389`). The question stands as one about the FFI's *completeness* (§1.11 — a header Heroes cannot bind is a library the author must leave C code around for), not as a blocker | docs/panel/085 R7 · spec:219 · crates/heroes/src/types/ffi_decls.rs · tests/harness/shell.hero | a claim that cannot be reproduced is not a defect and not nothing — it is a question with a witness attached
- [x] **DECIDED 2026-08-24: nothing — `5.` is a shape nobody writes** (author answer `7b`, `/decide`; the same answer kept the `continue` trap and sent it to a sitting, so this is a choice between two candidates rather than a blanket no). Reproduced first, so the no is over a measured claim and not a remembered one: `x: f64 @ 5.` gives `error[unused_binding]: x is declared and never read` on today's compiler — the number is never named, exactly as the item said. What decides it against the exponent precedent is **who meets it**: `1e3` is what a person writes when they mean 1000 and the language has no exponents, so `exponent_literal` catches an intention; `5.` is not an intention in any language whose float literals allow a bare trailing point, because a writer who means 5.0 in Heroes writes `5.0`. The lexer's rule stays as it is and stays right — a dot is a float only when a digit follows, or `1.str()` stops being UFCS. **The falsifier, so this refusal is held to §12's standard**: a real `.hero` file where a trailing-dot literal was written and the writer's next stop was the lexer. The check, over `selfhost/`, `tests/harness/` and `examples/` — strings and comments stripped first, because the naive form is worthless and that was **measured before this sentence was written**: `grep -nE '[0-9]+\.($|[^0-9a-zA-Z_])'` alone reports **95** hits, every one of them prose in a comment (*"exit 0."*), and stripping only comments still reports **13**, all inside string literals (*"20. We can be heroes"*) plus two genuine UFCS calls (`5.double()`, `1.str()`) that the pattern must not claim — a falsifier that fires on the status quo cannot detect anything. The form that works excludes a following dot as well, so `0..63` inside a message does not count:<br>`for f in selfhost/*.hero tests/harness/*.hero examples/*/*.hero; do sed 's/\\"//g; s/"[^"]*"//g; s/#.*$//' "$f" | grep -nE '[0-9]+\.($|[^0-9a-zA-Z_.])' | sed "s|^|$f:|"; done`<br>**0 hits today**, and verified in both directions rather than only the quiet one: it FIRES on `x: f64 @ 5.` and stays silent on `5.to_str()`. Was | measured 2026-08-18, no panel | **`5.` is `int` then `dot`, and the reader is told about a binding instead.** `x: f64 @ 5.` gives `error[unused_binding]: x is declared and never read` on **both** compilers — the trailing dot becomes UFCS on the next line and the declaration loses its value. Compare `1e3`, where panel 035 refused exponents and `exponent_literal` names the number and says what to write instead. The lexer's rule is deliberate and right (`1.str()` must stay UFCS, so a dot is a float only when a digit follows), so the question is only whether a digit run followed by `.` and **no** digit deserves its own diagnostic the way an exponent does. Two lines in `lexer/number.rs`, one code, one `#~` case; or nothing, if the author reads `5.` as a shape nobody writes | crates/heroes/src/lexer/number.rs:138-145 · docs/panel/035 | the exponent refusal is the precedent, and it exists because a number that lexes as something else sends the reader to the wrong file
- [x] **DEFERRED 2026-08-24 to M-guide-book, and MOVED back to `SCHEDULED.md`** (author answer `8b`, `/decide`). Not (c): the metric is **not** retired, so the thesis does not settle for one measured half and one argued half. The home is named, which is what this list requires of a deferral and what the 2026-08-18 version of it lacked — *"defers again"* without a milestone is how the previous stretch became unrecoverable. **Why M-guide-book and not the next milestone**: the tasks must be author-written or they measure the assistant's priors, and M-guide-book is the one milestone whose own work *is* the author writing programs — so the held-out set comes out of work already being done rather than being a separate chore that competes with the compiler. Verified rather than assumed before deferring: `harness/tasks/` holds **only `README.md`** — **zero** task files against the 15 the README requires — so nothing has been silently accumulating, and metric 3 is unaffected and has run on every commit since panel 011. **The duplication this item's own existence created is resolved in the same edit**: `SCHEDULED.md` still carried a 2026-08-11 line on the same question beside the 2026-08-18 line that promoted it here, so two lists and one file held three copies. There is now one item, in `SCHEDULED.md`, with a milestone. Was | SCHEDULED item, due now 2026-08-18 | **Metric 2 said "nothing will be taken until M-selfhost-fixpoint", and that milestone is closing.** The item is ready and blocked on the one thing the assistant must not supply: `harness/tasks/README.md` requires the held-out tasks to be **author-written**, because assistant-written tasks measure the assistant's priors. Nothing else is missing — metric 3 has run on every commit since panel 011. The decision is whether the author writes tasks now, defers again with a named milestone, or retires metric 2 and records that the thesis keeps one measured half and one argued half | docs/debrief/SCHEDULED.md · harness/tasks/README.md · docs/panel/011 | the pre-amendment numbers are already unrecoverable, and each deferral makes the same true of the next stretch
- [x] **DECIDED 2026-08-24, option (a), and LANDED in the same session** (author answer `3a`, `/decide`). `hero_write_err(HeroStr)` is in `runtime/parts/os.c` over `fwrite(…, stderr)`; `selfhost/cli_io.hero` binds it from `hero_os.h` and its `extern "unistd.h"` group is gone, so **`seed/heroes.c` has no POSIX include left** and the self-hosted compiler is buildable on a platform without POSIX headers. **The item's own measurement is what chose the shape**: (a) was never one line — `extern "stdio.h" constant stdout: ptr` is `error[ffi_not_constant]` at exit 1, reproduced again today with the diagnostic's note intact, so any stdio route needs a C function on the other side, and putting that function in the runtime is what makes the platform question live in the one file allowed to know what machine it is on. (b) is refused because this language has no `#if` by design; (c) is refused because a Part 6 row here would have to name a falsifier it cannot name — the port builds on Windows now. **What the repair bought beyond the include, and it is §1.12 rather than tidiness**: `write(2)` may write FEWER bytes than it was given, and of its three call sites the two that shipped discarded the count, so a truncated diagnostic was indistinguishable from a whole one. `hero_write_err` returns **nothing** and loops until the blob is out or the process has no error channel left, so there is no count left to discard. No `fflush`, measured rather than assumed (C11 7.21.3p7; `fwrite`-then-`abort()` verified through a file and a pipe), and **no ABI bump**: adding an entry point makes a stale runtime a **link error**, which is louder than the stamp, whose job is a wrong *answer*. **Where the old test went**: `cli_io.hero`'s test asserted `write` returned 5, and there is no count to assert any more — so it was replaced by the shapes NEXT TO the change (empty, multi-byte, already-newline-terminated, past the stdio buffer) and the comment names the real instrument, which is not this test: every `tests/golden/check/` case compares the text that leaves through here byte for byte, **65 `check` + 76 `annotations` checks**, so a truncating write is 141 red checks. A first draft of that test wrote 70,000 bytes to stderr on every suite run and was cut to 8,192 — a test nobody can read the output of is a test nobody reads. **Citation rot repaired in passing**, both found by grep today: `hero_os.h`'s doc pointed at `crates/heroes/src/library/source.hero` (now `selfhost/library_source.hero`), and this item's own tail pointed at two Rust tests in the archive. Was | measured 2026-08-18, and it is the archive's sharpest open question | **The self-hosted compiler cannot be built on Windows, because it binds `unistd.h`.** `selfhost/cli_io.hero:14` is `extern "unistd.h" function write(fd: i32, buf: cstr, count: u64) -> i64`, so `seed/heroes.c` `#include`s it — and neither MSVC nor clang-cl ships that header. The bootstrap has no such dependency (`grep unistd crates/` is empty), which is why nobody met this: the third CI leg has been testing a compiler that does not have the problem. Both new tests skip on the machine's own answer rather than on a `cfg!` list (they match clang's *"'unistd.h' file not found"*), so the skip lifts by itself the day the port stops binding it — **untested on Windows from here**, and the first tag run is what confirms the message. The decision is what Windows gets: **(a)** `write` through `stdio.h`'s `fwrite`, which is C89 and needs no POSIX at all — one line in `cli_io.hero`, and it also removes the seed's only POSIX include; **(b)** a platform arm in the port, which means a `#if`-shaped question this language has no form for; **(c)** POSIX-only, recorded in design.md Part 6 with the falsifier that row owes. (a) is one line and looks free, which is exactly when this project measures instead of assuming: `fwrite` is buffered where `write` is not, and the CLI's error channel is the one thing that must survive an abort. **Two measurements, 2026-08-23, and the first one falsifies (a)'s cost line.** (1) *(a) is not one line*: `extern "stdio.h" constant stdout: ptr` is `error[ffi_not_constant]` at exit 1 — reproduced, both streams — and the diagnostic's own note says why and where the exit is: *"a `constant` names one value forever (§4.4), and `stdout` is an object or a macro that expands to one … A C object is reached through an `extern function` that returns it."* `cli_io.hero`'s module doc had already written that down; so **any** stdio route needs a C function on the other side, and (a) is one line in `cli_io.hero` **plus** a function in the runtime. (2) *The buffering worry is answered for the channel that matters*: C11 7.21.3p7 says the standard error stream is not fully buffered, and measured here — `fwrite(m, 1, n, stderr)` then `abort()` — the text survives to a file **and** through a pipe, exit 134. **The recommendation is (a) with its work inside the runtime**, which makes it the shape this project already uses everywhere else: `hero_os.h` owns `hero_file_read`, `hero_file_write`, `hero_args_count`, `hero_args_at` and `hero_exit`, and `write` is the single OS call the port reached for directly. One `hero_write_err(HeroStr)` in `runtime/parts/os.c` over `fwrite(…, stderr)` is C89, removes the seed's only POSIX include, needs no `#if` in a language that has none, and keeps the platform question in the runtime's C — the one file in this project that is *allowed* to know what machine it is on. It also deletes the port's only raw `extern` to a POSIX header, which is a §1.12 gain rather than a tidy-up: `write` may return a **short count**, and of its three call sites the two that ship (`:22`, `:27`) discard the result — only the test at `:49` looks at it, and it asserts the count is 5, which is the one case where a short write cannot happen. (Citation rot in this item's own tail, found while verifying: the two Rust tests are `archive/bootstrap-rs/heroes-cli/tests/`, and `differential.rs` is dark) | selfhost/cli_io.hero:14 · seed/heroes.c:5 · runtime/hero_os.h · archive/bootstrap-rs/heroes-cli/tests/seed.rs | after the archive there is no other compiler, so a platform the port cannot build is a platform Heroes does not have
- [x] **ALREADY FIXED, ticked 2026-08-24 without being asked** (`/decide`'s verify-before-asking rule). The repair landed 2026-08-23 as author decision `3c` — option **(c)**, this emitter's own idiom: `descriptor_set` asks the **emitted C** whether anything takes the descriptor's address, the way `emit_unread` already asks whether a slot is read, rather than asking the IR what the program reaches (`DESIGN-LOG` 2026-08-23, commit `d43684b`). Neither (a) nor (b) was taken, so the seed was not re-blessed for it and the 14m05s this item priced was never spent. Verified today by running the exact command the item names: `heroes test tests/harness/probe.hero` → **14 tests, all passed, zero clang warnings**, where it used to print `probe.c:409:23: warning: unused variable 'h_probe_Probe_desc'`. The item stayed open for one day after its own fix, which is the same gap panel 086 R7 was written about. Was | measured 2026-08-18, found by the Heroes harness | **A record's descriptor can reach clang unused, and the generated C warns.** `heroes test tests/harness/probe.hero` → `probe.c:409:23: warning: unused variable 'h_probe_Probe_desc' [-Wunused-const-variable]`, 1 warning, exit 0. The same file under `build --emit-c` defines the descriptor and its three functions (copy, drop, eq) at :249-263 and **uses none of them either** — so the difference is which translation unit clang is given rather than which C is written. CLAUDE.md §7's bar is zero warnings and `tests/harness/suite_warnings.hero` now enforces it over the goldens and the corpus; this file is neither, which is why nothing was red. Not reproduced by three smaller shapes (a scalar record, a record holding `[str]`, a record copied through `out: R @ r` with a pushed field), so the trigger is narrower than "a record with a container" and is not yet named | **The cause is located** (2026-08-18, same session): `descriptor_set.rs::generated` builds its worklist from types the program REACHES — `if !reachable.contains(&id.0) { continue }` — and reachability is not the same question as "does the emitted C write `&{prefix}_desc`". `probe.hero` interns `[Probe]` because `run_table` takes one, the type is reachable, the descriptor is emitted, and nothing in the emitted C points at it. The file's own doc already says this is the failure mode it exists to prevent (`descriptors.rs:101`, and a 2026-08-12 defect with the same shape is quoted in `descriptor_set.rs:40`) — so the worklist is asking the closest question it can ask BEFORE the bodies are written, and the true question is only answerable after. **The precedent for answering it is in this emitter**: `emit/unread.rs` decides whether a slot is read by asking the EMITTED C rather than the IR, which is exactly this shape one artifact earlier (DESIGN-LOG:385) | Three candidate repairs, and the third is this emitter's own idiom: **(a)** emit the descriptor only where something references it, which asks the emitter a question it does not currently ask; **(b)** `__attribute__((unused))` on the descriptor, which is exactly what `emit/unread.rs` already writes for a slot the C never reads and what `extern_complete.rs` writes on its probes — one line, no question about who references what. **(c)** ask the emitted C, as `unread.rs` does — the descriptors are written before the bodies, so it needs the bodies buffered first, which is the same restructuring `unread.rs` already paid for. (b) changes every generated file that declares a record, so it changes `tests/golden/emit/` expectations and **regenerates the seed** (14m05s measured), which is why it is a step of its own rather than a fix folded into another | crates/heroes/src/emit/ · tests/harness/probe.hero · CLAUDE.md §7 · panel 020's zero-warning disposition | the bar is zero because a warning nobody sees cannot falsify anything, and a warning the harness itself provokes is the first one a newcomer will meet
- [x] **DECIDED 2026-08-23, option (a) — all three pointers, applied in the same session** (`/decide`). CLAUDE.md §4 now reads *"surface syntax or semantics (what `selfhost/`'s lexer, grammar and checker DO, not how they do it)"*, with the braces gone so the sentence is finally readable by `suite_records.hero`'s dead-citation check (panel 086 R7's measured limit); `.claude/skills/panel/SKILL.md:44` points the compiler-engineer at `selfhost/` and `runtime/`, says **never `crates/`**, and hands the seat the 3.4 s seed route beside the ~20-minute rebuild that killed four of five at panel 087; `.claude/skills/step/SKILL.md:70` points at `docs/measurements/010-spec-budget-ledger.md`. Verified after: `grep -rn "crates/" .claude/skills/` returns only the two sentences that *warn* about the path, and `records: 6 passed, 0 failed`. Was | M-bootstrap-archive, 2026-08-19 | **CLAUDE.md §4's panel trigger still names the archived tree.** The sentence reads *"surface syntax or semantics (`crates/heroes/src/{lexer,syntax,types}/` behaviour, not internals)"*, and those files are `archive/bootstrap-rs/` now — so the trigger for the most common panel path points at a directory nobody maintains. It is left for you rather than rewritten because §4 is the contract itself and CLAUDE.md is amended by author instruction. **Proposed wording, and it is shorter than what it replaces**: *"surface syntax or semantics (what `selfhost/`'s lexer, grammar and checker DO, not how they do it)"* — the port's equivalents are `selfhost/lexer.hero`, `selfhost/grammar_*.hero` and `selfhost/check_*.hero`, and naming the behaviour rather than the files is what the parenthesis already tried to say. The brace form is also the one citation shape `records/citations` cannot check (panel 086 R7's measured limit), so a rewording without braces gains a checked citation. **And the same rot is in two skills, measured 2026-08-23 — `grep -rn "crates/" .claude/skills/` returns exactly two hits**: `.claude/skills/panel/SKILL.md:44` briefs the compiler-engineer with *"pointers into `crates/heroes/src/`"*, which is why four of five seats at panel 087 were sent to rebuild a compiler that no longer lives there, and `.claude/skills/step/SKILL.md:70` names `crates/heroes/src/measure/gate.rs` as where a prediction is scored — a file that is archived **and** whose ledger panel 086 moved to `docs/measurements/010`. Both are process, so both are yours by CLAUDE.md §3; the recommendation is to fix them in the same breath as §4, since a brief built from a stale pointer is the exact failure CLAUDE.md §1 was written for | CLAUDE.md:112 · .claude/skills/panel/SKILL.md:44 · .claude/skills/step/SKILL.md:70 · docs/panel/086 R6 | the rule that decides when a panel is mandatory should not point into the archive, and neither should the brief that convenes it
- [x] **DECIDED 2026-08-24, option (a) — the port gets its Windows path and the leg covers the whole net again** (author answer `a1`, `/decide`, taken together with `3a` because (a) is what `3a` built). Every `if: runner.os != 'Windows'` is gone from `.github/workflows/ci.yml` — the seed build, `doctor`, the compiler's own tests and the net all run on the third platform, and the `::warning::` that announced what the leg had stopped covering is now a `::notice::` naming the one thing it genuinely does not: the debugger, because `lldb.exe` exits `0xC0000135` before running a command. (b) — dropping the leg — is refused because what (a) buys is measurable rather than notional: `INT64_MIN % -1` and the ASan leak detector already differ across the two legs that exist, so a third is a third instrument. (c) is what today replaced. **UNTESTED ON WINDOWS FROM HERE, and it is written into the file rather than left implied**: nobody in this project has a Windows machine, so the first tag run after 2026-08-24 is this decision's instrument. Two platform differences are predicted and handled instead of discovered — `shell: bash` on every step, because `windows-latest` defaults to `pwsh` and these are bash scripts, and a `matrix.exe` suffix on the built binary, because clang honours `-o` exactly and Windows will not execute a file without the extension. A red leg naming anything else is the finding. Verified here rather than assumed: the YAML parses and reports **10 steps**, and `grep "runner.os != 'Windows'"` returns nothing. Was | M-bootstrap-archive, 2026-08-19 | **The Windows CI leg now covers the toolchain and nothing else.** It ran the four cargo steps; they left with `crates/`, and the Heroes steps cannot replace them there because `selfhost/cli_io.hero` binds `extern "unistd.h"` — there is no self-hosted compiler on a platform without POSIX headers. The leg still runs `clang --version` and reports what is missing, and it now prints a `::warning::` saying what it stopped covering, because a green job that tests nothing is what CLAUDE.md §9 calls a decoration. **Three honest answers and the choice is yours**: (a) give the port a Windows I/O path — the externs are `read_file`/`write_file`/`getenv`/`system`, and the runtime already compiles under a Windows target (panel 049); (b) drop the leg and say in `ci.yml` that Windows is unsupported for the compiler and supported for the runtime; (c) keep it as a reporting leg, which is what it is today. What (a) would buy is measurable rather than notional: the whole net on a third platform, which is where `INT64_MIN % -1` and the ASan leak detector already differ | .github/workflows/ci.yml § the matrix note · selfhost/cli_io.hero | the third platform was added by author instruction, and the archive quietly took most of it away
- [x] **CONFIRMED 2026-08-24, all four** (author answer `2a`, `/decide`). Nothing overturned. Verified in the ratifying session rather than taken on trust: `grep -rn "cargo" .claude/skills/` returns exactly two hits and **both are the historical notes** (`step/SKILL.md:39` and `where/SKILL.md:27`, each saying what the command *was* until M-bootstrap-archive), so no live instruction in either skill names a command that cannot run; `README.md:41` carries the seed line; CLAUDE.md §5 reads *spent*, §10's exception reads *expired*. **One number in the batch had gone stale by six and is repaired in this commit rather than asked about**: `README.md:68` said the spec measures **3512** *"as of 2026-08-19"* — correctly dated, so not false, but six tokens behind the live document. Measured now with `heroes measure`: `claude-legacy 3434 · cl100k_base 3506 · maximum 3506 · spread 72`, and `SPEC_TOKENS` already reads 3506, so the harness was right and only the prose was old. `docs/ROADMAP.md:140`'s `3440 / 3512 / spread 72` is **left exactly as written** — it sits under *"Closed 2026-08-19"*, and CLAUDE.md §14 does not rewrite a dated record. Was | M-bootstrap-archive, 2026-08-19 | **Four process texts were edited rather than queued, and this item is so you can overturn any of them in one word.** The archive made them false as written, and the substance of each rule is unchanged — only the command names moved. (1) **CLAUDE.md §5**, the Cyclone rule: marked *spent, and kept as the record of what it bought*, because the Rust it governs is `archive/bootstrap-rs/` and nothing lints it; the replacement named in the text is the language itself. (2) **CLAUDE.md §10**'s declared exception: its expiry arrived, so it now says the way in is one clang line over `seed/heroes.c`. (3) **CLAUDE.md § Commands** and **README.md § Build and try it**: the seed line replaces `cargo build && cargo test`, and the README's stale `2363 tokens` is now the measured 3512 with the ledger cited. (4) **`/step` §2 and `/where` step 1**: `cargo test`/`cargo clippy` become `heroes test selfhost/main.hero` and `heroes run tests/harness/main.hero -- <compiler>` — which matters operationally rather than editorially, because a session reading the old line would run a command that cannot work. **What was NOT touched, on purpose**: `site/**` (a parallel session owns it and its pages name `./target/debug/heroes`), CLAUDE.md §4's trigger paths and the Windows leg, both of which are their own items above | CLAUDE.md §5, §10, § Commands · README.md · .claude/skills/{step,where}/SKILL.md | a contract that tells the next session to run a command that no longer exists is worse than one with a gap in it
- [x] **DECIDED 2026-08-23, option (b) — the compiler's own tests move to tags, applied in the same session** (`/decide`). `.github/workflows/ci.yml` gates the 482 on `github.ref_type == 'tag' || github.event_name == 'workflow_dispatch'`, the net stays on every push, and the step's comment carries the re-measured number (**20m35s / 1219 s user, 2026-08-23**, against the ~16 minutes this item recorded five days earlier) so the trade is legible where it is paid. What the trade gives up is named there too: the 482 in-module assertions, against 845 net checks that drive the same compiler. (c) — making the port faster — was not chosen and is not refused: it is a milestone, and this decision does not price it. Was | M-bootstrap-archive, 2026-08-19 | **The verification loop got 90× slower and that is worth one decision, not a shrug.** `heroes test selfhost/main.hero` — the compiler's own 482 tests — took **13 s** through the Rust bootstrap and takes **~16 minutes of CPU** through the seed-built compiler, because running those tests means compiling the whole 37,137-line compiler with the compiler. Measured today, and it is the price of self-hosting rather than a defect: the emission alone is 15m41s, the fixpoint is the same number, and the port is about 15× slower than the Rust at compiling. **Where it bites is CI**: each non-Windows leg now pays that plus the net, so a push costs roughly half an hour where it used to cost eight minutes. CLAUDE.md §13 says performance is not a goal *and not a licence* — this is the second clause, so it goes to you rather than being absorbed. **Three shapes**: (a) leave it, and CI is slow but complete on every push; (b) move *the compiler's own tests* to tags only, the trade the matrix already makes for Darwin and Windows, keeping the net on every push — the net exercises the same compiler over 838 checks, so what tags-only loses is the 482 in-module assertions; (c) make the port faster, which is a milestone and not a decision. Nothing is broken either way: the local loop for a compiler change is `heroes test` through a *previously built* binary, which is the 13 s number again. **Re-measured 2026-08-23 and the number went the wrong way**: **482 tests, all passed, 20m35s wall / 1219 s user** on the same machine, against the ~16 minutes this item recorded five days ago — 27% more, with two seconds-long builds running alongside for part of it, so treat it as a floor rather than a ceiling. Nothing here is a regression to chase: it is the same 37,137 lines compiled by the same port, and the drift is what makes (b) stronger than it was when this item was written, because a number nobody watches grows | .github/workflows/ci.yml § The compiler's own tests · docs/journal/024 | the archive traded a fast oracle written in another language for a slow one written in this one, and the trade should be visible rather than discovered by waiting

## Open — found 2026-08-24 while ratifying, and it is about the record itself

- [x] **RATIFIED 2026-08-24, all eleven, option (a)** (author instruction, *"ratifica anche quelle 11"*, chosen over reading them first after being told plainly that every one had shipped and the tree was green over all of them). Panels **069-079** each carry a `## Author's verdict` naming what its yes settles — the batch is not a rubber stamp, and each file says so — plus one shared note: **a yes over a resolution is not a yes over the questions that resolution deferred**, so every reserved veto, return condition and queued follow-up inside them stands exactly as written. **080 needed nothing** and my count of it was wrong: it is a `retro-record`, adopted on author instruction given *in advance* of the sitting, and its own text says *"no ratification pending"* — which is the phrase my checker matched on. Verified after, with a control this time (053 and 089 must register as ratified, and do): **no sitting in `docs/panel/` is unratified.** **The instrument is still owed and is now the only live half of this item** — see the row below, which is where it went. Was | measured 2026-08-24, five attempts | **Eleven sittings from 2026-08-16 were queued and never ratified, and no grep can tell you that.** `docs/panel/069` through `080` — the shortcut that is not about floats, the stack nobody was counting, six holes and the one nobody listed, the name C keeps in another drawer, the fields that share one address, the word and the drawer it opens, the branch nobody took, the conjunction that needed no continuation, the builtin that was already there, the question that was already answered, the dot that two passes read differently, the egg that cannot hide. Measured: eleven of the twelve still read `ratification pending` in their own status lines, they carry **0 to 8** `DECIDE.md` references each so they WERE queued, and not one carries a yes. **This is panels 085–087's gap at four times the length** — those sat unratifiable for four days because no list carried the item asking; these have sat for eight days. Nothing is blocked: every one shipped as a provisional default and the tree has been green over all of them since. **The useful half is the instrument, not the eleven.** Five attempts at "which sittings are unratified" returned **51, 7, 16 and 11**, every one wrong in a different way — the first because `## Author's verdict` is a recent convention, the second because *"ratification pending"* contains the string *"ratif"* so every unratified sitting counted as ratified, the third because this project records a yes as *ratified*, *author decision*, *author instruction* and *decided by the author* across `docs/panel/`, `DESIGN-LOG.md`, `docs/debrief/DECIDE.md` and `docs/panel/OPEN-QUESTIONS.md`. A question this basic should be answerable by an instrument, and `tests/harness/suite_records.hero` is where one belongs — panel 086 R7 built the dead-citation check into exactly that file for exactly this class of rot. The decision is whether to (a) ratify the eleven as a batch after reading them, (b) ratify them blind the way 085–087 were ratified, or (c) build the check first and let it tell you what it finds | docs/panel/069-080 · tests/harness/suite_records.hero · docs/panel/086 R7 | a project whose contract says a decision is not real until the author says so should be able to say, mechanically, which decisions the author has not said so about

- [ ] found 2026-08-24, and the eleven are closed but this is not | **Nothing tells you which decisions are waiting for you, and five attempts to find out by hand returned five different numbers.** The counts came back **51, 7, 16, 12 and 11**, each wrong in its own way: `## Author's verdict` is a recent convention, so old sittings looked unratified; **the phrase `ratification pending` contains the string `ratif`**, so a grep for *ratified* counted every unratified sitting as ratified; `docs/panel/080` says *"no ratification pending"* and matched as pending; and a yes is written as *ratified*, *author decision*, *author instruction* or *decided by the author* across `docs/panel/`, `DESIGN-LOG.md`, `DECIDE.md` and `docs/panel/OPEN-QUESTIONS.md`. **The gap this let through was eight days and eleven sittings**, and panels 085-087 sat in the same one for four. `tests/harness/suite_records.hero` is where the check belongs — panel 086 R7 put the dead-citation check there for exactly this class of rot, and it already walks these files. The shape that would work is the one this session had to reach for by hand: **every panel file must carry exactly one `## Author's verdict` section, and that section must not say `Pending`** — a rule about the *value in hand* rather than about the vocabulary of the record, which is CLAUDE.md §11's own distinction and the reason the vocabulary greps all failed. Cheap, and it fires the day a sitting closes without being queued | tests/harness/suite_records.hero · docs/panel/086 R7 · CLAUDE.md §11 | a project whose contract says a decision is not real until the author says so should be able to say, mechanically, which decisions the author has not said so about

## From panel 089 (2026-08-24) — text from C, and what the sitting left for you

**The sitting is held and the repair has landed** (`docs/panel/089`,
`provisional — author ratification pending`). Five rows below: one ratification,
one surface removal a judge made a condition of its approval, one hole neither
option reached, two compiled riders, and one correction to CLAUDE.md itself.

- [x] **RATIFIED 2026-08-24**, hours after the sitting closed (author instruction, *"ok a tutte le ratifiche e alla modifica al CLAUDE.md"*; `docs/panel/089` § Author's verdict says what the yes settles). The resolution stands as landed: `validated(c: cstr) -> str?` as a Tier-2 library function, one validation walk, ABI at 15, spec 3530. Verified with the yes rather than assumed — the net is **1146 passed, 0 failed**, the compiler's own tests **491 passed**, the seed is **identical** to what today's source emits, and `HEROES_RUNTIME=$'\xff\xfe' heroes doctor` is **exit 0** where it was 134. **The same instruction ratified the CLAUDE.md correction below**, which is not a tidy-up: §12's SEGV sentence is the premise the sitting's whole payment rested on, and under the stale version the −15 removal would have read as deleting a warning rather than spending redundancy the compiler had already made safe. What the yes does **not** settle: the four rows below it. Was | panel 089, provisional 2026-08-24 | **Ratify the resolution, or overturn it.** Adopted and landed: `validated(c: cstr) -> str?` as a **Tier-2 library function** — zero checker lines, zero emitter lines — over a new runtime entry point `hero_str_try_from_cstr` written **beside** `hero_str_from_bytes` for one validation walk, ABI held at **15**, spec **3506 → 3530 (+24 net, gross +39 against a −15 named removal)**. What a yes settles: that the second-name form is the one this language takes, which is what **panel 059 reserved when it refused `to_str` as the carrier** — so this is 059's own continuation rather than a new direction, and (A) carries the compiler-engineer's veto besides. What it does **not** settle: the four rows below. **What the sitting is most worth reading for is that the brief was wrong four times and the seats found all four** — a ratified ruling read as a silence (the coordinator's pre-convening grep listed `panel/059` and the coordinator read the filename, not the file), a failed search written as an impossibility, a runtime mechanism described wrongly, and the framing itself: the measured question was **171 tokens against 51**, ergonomics under §4.19, not possibility under §12 | docs/panel/089 · docs/panel/059:302-305 · docs/measurements/010 | the repair is in and the record of how the brief went wrong is the part that transfers
- [ ] panel 089, ffi-pragmatist condition 2 — **the one condition of an approval that was NOT met** | **Retire `cstr.to_str()`, or rule that it stays beside `validated`.** That seat approved the resolution on three conditions; two are met (one validation walk; the riders queued below) and this one is deliberately left to you, because it is a **surface removal** and CLAUDE.md §4 makes that yours. Its argument, verbatim in force: *"Keeping it leaves the aborting form as the one `spec:229` teaches, and §12 does not let the crashing spelling be the default."* **The substance is already met at the documentation level** — the spec no longer teaches it, the removal of *"test `c == nullptr` first"* landed, and the live diagnostic note in `value_errors.hero:36` now names `validated` instead. What remains is the *code*: `c.to_str()` still compiles and still aborts. Measured cost of the removal by that seat: **7 shipping sites** — `selfhost/cli_toolchain.hero` (already migrated), `examples/curl/main.hero:49,58,61`, `examples/sdl/main.hero:56,63`, `tests/harness/shell.hero:232` — plus 8 golden sites. It is also a **checker** change, which the compiler-engineer's "zero checker lines" costing does not cover, so its price is not yet measured | docs/panel/089 § conditions · selfhost/check_builtins.hero | a language that ships two spellings of one conversion, one of which kills the program, has to say why
- [ ] panel 089, ffi-pragmatist, **the hole neither option reached** | **`args()` has no error channel and `validated` cannot give it one.** `hero_args_at` converts **eagerly** (`runtime/parts/os.c:62` → `hero_str_from_cstr`), so no `cstr` ever reaches the program and the route that rescues SQLite cannot rescue argv. Reproduced today: `./argv $'\xff\xfe'` → exit **134**. The seat compiled the shape it wants and it is **`args_checked() -> [str?]`, not `[str]?`** — one undecodable argument must not cost a program the other nine, and `heroes build good.hero $'\xff'` should *name argument 2*. It needs one added runtime function (`const char *hero_args_raw(int64_t)`, borrowed, **NULL** out of range rather than a panic, because a caller that must branch cannot branch on a panic) and **zero language change**: `args()` already lives in Heroes, so the branching goes in the library beside `validated`. `args() -> [str]` keeps its signature and its abort for the 20+ programs that use it | docs/panel/089 · runtime/parts/os.c:62 · selfhost/library_source.hero | argv is the one input a program cannot decline to receive, and it is now the only door of the four still open
- [ ] panel 089, ffi-pragmatist, **two riders compiled during the sitting** | **Three of five real C string shapes are closed in EVERY option, and two of them cost one line each.** The seat compiled all five: a result `const char*` (now sound), a `const char **` out-parameter (sound), and three that no option touches — a **`char *` struct field** is `error[ffi_field_type]` because the emitted `_Generic` asks for `const char **` and a `char *` member is a third type; a **`char **` out-parameter** is `error[ffi_writable_parameter]`; a **callback taking `const char *`** is `error[type_mismatch]`. Two have a compiled repair: `HERO_RET_CSTR`'s own six-way set applied to the *member* accepts `pw_name`, `pw_dir` and `pw_shell` from `struct passwd` — one line in `selfhost/emit_extern_field.hero:148`; and `ffi_writable_parameter` exists because `s.cstr()` lends a COW buffer's interior, which **an `@` parameter is not** (§4.8 makes it the address of a Heroes local, and `selfhost/emit_ops.hero:163` already excludes `@cstr` from the null guard *for this exact reason*) — the same narrowing is missing here | docs/panel/089 § completeness table · selfhost/emit_extern_field.hero:148 · selfhost/emit_ffi_mutable.hero | §1.12 says the boundary must be COMPLETE, and this sitting repaired the one shape that already had a working route
- [x] **APPLIED 2026-08-24 by author instruction** (*"ok ... alla modifica al CLAUDE.md"*). CLAUDE.md §12 now reads *"**was** a SEGV in libsystem"* with the closure attached: the same program is `panic: a null `cstr` was passed to a C function`, exit 134, because `guard_cstr_arguments` wraps every `cstr` argument on its way out. **The date is kept and the sentence corrected rather than deleted**, because panel 053's measurement was right when it was taken — what expired is the world, not the record. The clause also now says why it matters, so the next reader does not have to rediscover it: panel 089's whole payment is §1.4 redundancy **paid back**, and only the loud abort makes it so. Was | panel 089, spec-warden, **and it is a correction to CLAUDE.md, so it is yours by §4** | **CLAUDE.md:432 says a null `cstr` handed onward is "a SEGV in libsystem", and it is not.** Measured by that seat and reproduced independently by the coordinator: `panic: a null `cstr` was passed to a C function`, **exit 134** — a clean abort with a name, because `guard_cstr_arguments` (`selfhost/emit_ops.hero:166`) emits the guard on every `cstr` argument. The sentence was **true when panel 053 measured it on 2026-08-14** and the guard closed it since; CLAUDE.md §11's own shape, in CLAUDE.md. **It is load-bearing rather than cosmetic**: it is the reason `spec:229`'s warning is §1.4 redundancy that can be *paid back* rather than protection being deleted, which is what funded this sitting's +24. Proposed: the clause keeps its date and gains its closure — *"was a SEGV in libsystem until the guard (panel 053); it is now a named abort, which is what made panel 089's removal payable"* | CLAUDE.md:432 · selfhost/emit_ops.hero:166 · docs/panel/053 | a contract that states a danger the compiler has already closed will fund the wrong decision next time

## The 089 brief — the C boundary, and what the document says about it

**Assembled 2026-08-24 in `/decide` (author answers `4a 5b 6a 7b 9a`), and it is
a brief rather than a sitting: `/decide` hands a panel question over and stops,
because convening is the author's call and the full panel is expensive.** Eleven
rows, all of them already open items below, all of them reproduced or measured on
2026-08-24 rather than recalled. The author chose one sitting over six separate
ones, and the reason is the shape of the finding: these are one question seen
through eleven doors — *what happens at the boundary where the language's own
guarantees stop* (design.md §1.11: everything comes from C) — so six briefs would
ask six judges to re-derive the same argument.

**Lane: full panel.** Not the soundness lane, and that is not a default: five of
the eleven rows want a **spec token** (`spec:14`, `spec:186`, `spec:229`, the
unnamed aborts, a bytes reader), two are a **diagnostic class**, and the
llm-ergonomist is the seat that found row 1 independently from the document
alone. The soundness lane would lose exactly the judge that matters most here.

**The one number every row is priced against, measured today**: `heroes measure`
reports **3506** of a hard **4096**, so the headroom is **590 tokens** and the
soft-2000 rule means every addition needs a named removal or a registered
falsifiable prediction. Five rows want tokens out of one 590-token budget, which
is itself a reason to run them together rather than five times.

**How to split it if eleven rows is too big for one sitting** — and it may be;
this is written so the author can cut it without re-reading the eleven items:

- **089A — the four that kill a correct program.** Rows 1, 2, 3 and 9 below. Every
  one ends at exit 134 or exit 2 on a program whose author did nothing wrong, and
  §12 makes robustness the tie-break that outranks elegance, tokens and speed.
  This is the half that cannot wait.
- **089B — what the document promises.** Rows 4, 5, 6, 7, 8, 10 and 11. These are
  spec truth and vocabulary: sentences that are false, silences that read as
  guarantees, a status the runtime knows and the language cannot ask about.

- [ ] **THE BRIEF, eleven rows, each with the direction the author chose in `/decide` 2026-08-24** | **1. `.to_str()` kills a binding author who did exactly what `spec:229` told them to** (item below, panel 087). Reproduced today end to end, on a C function returning `{0xff,0xfe}`: null-checked first as instructed, then `.to_str()` → `panic: hero_str_from_bytes: not well-formed UTF-8`, **exit 134**. The spec names `nullptr` as *the* reason conversion aborts and it is one of two. Direction: **open**, and the spec-warden pre-refused the two-path enumeration (+11, *"row 3405's exact anti-pattern"*), so the sitting chooses between a floor covering both reasons and a pointer to a fallible form that does not exist. · **2. `args()` has no error channel at all.** `./argv $'\xff\xfe'` → same panic, **exit 134**, reproduced today. `args() -> [str]` is not fallible and the shell hands a program whatever bytes it likes; it is the one door `read_file`'s panel-087 repair shape cannot be applied to, because there is no status to return. · **3. Asking C whether a pointer is safe is categorically impossible.** Built and run today: a `bool hero_cstr_is_text(const char *)` bound through an `extern` group emits `t3 = is_text(hero_cstr_nonnull(t2));` — the guard fires **before** the predicate can answer, `panic: a null cstr was passed to a C function`, exit 134. Either the wrapper gains an exemption for predicate-shaped bindings, or the answer is a fallible `cstr → str`, which is rows 1 and 2's answer one level up — **and that is the reason these three are one sitting**. · **4. ~12 reachable aborts the spec never names.** Measured today: **66 `hero_panic` sites / 51 distinct messages** in `runtime/` (63/49 when the item was written — the place store added three), against **7 lines / 8 occurrences** of the word *abort* in the spec. The reader's rule is *"aborts are stated"* and the document keeps it about two thirds of the time. · **5. No bytes reader.** `read_file(path: str) -> str?` is the only reader (`spec:179`), so `fail(…)` hands a program a branch it can observe but not act on; zero of six surveyed languages abort. The counter-argument is in the record and is the provoking case: `suite_records.hero`'s `continue` skips the file and finishes the walk, so Principle 0 has no compiler-need **today**. · **6. The not-text status is collapsed into `read_failed`.** `library_source.hero:150-153`, and one `constant` line reverses it. Cost measured today: a minimal program emits **10 `_Static_assert`s** and 156 lines of C, so a new code is **+2 asserts in every program in the language** and **291 re-blessed emissions** — against `heroes mutate`'s `typo-code` row at 25 mutants / 0 killed, the table's only zero. Its reversal condition is unchanged: a named reader in the same commit plus a golden that fires the arm. · **7. The spec's own example does not compile.** `spec:186` versus `spec:98-99`, reproduced today on today's compiler: **two `needs_label`**, exit 1, with certain fixes `a: ` and `b: `. The repair is +2 tokens by eye and is a `spec/**` path, which is why a loop did not just fix it. · **8. `spec:14` is false and the repair is free.** *"strings and comments may contain any UTF-8"* — U+000D is valid UTF-8 and a raw CR is `error[raw_carriage_return]` at exit 1 (golden confirmed today). The diagnostic is deliberate and right, so the sentence moves; priced at **~±0 tokens**, which is why it funds nothing and cannot ride on another row's payment. · **9. A fixed array declared where the header has a scalar is exit 2.** Reproduced today on `dirent.h`: `internal error … subscripted value is not an array`. §7 says exit 2 means the compiler is wrong, and here the trigger is the author's own `extern`. **Direction chosen: (a)** — enumerate C's twelve integer types as array-pointer `_Generic` associations, ~12 rows per integer-array field. **(b) is refused and the reason is measured, not aesthetic**: it renumbers **56 `#line` restores** across five `tests/golden/emit/` files where `UPDATE_GOLDEN` is forbidden by CLAUDE.md §9. · **10. `continue` as an arm's no-op silently skips a loop's counter.** Reproduced live today: the program **compiles at exit 0 and hangs forever**. The language refuses `_ = 0` as an arm body, so `continue` is the natural filler, and inside a `for`/`while` whose body ends with `at @ at + 1` it is a loop jump rather than a nothing. **The compiler's own source is the witness**: `ir_phases.hero:125` carries the warning *"The no-op arms MUST NOT say continue"*, and `check_sized.hero:292` is a `fixedbugs` case named after it — *"`.function_decl => continue` skipped the loop's own index increment"*, found by the selfhost compiler refusing its own `parse.hero`. Three times in one session during the port. The question is a true no-op form versus a checker warning when an arm's `continue` targets a loop whose body continues below the match. · **11. `struct dirent` is unconstructible, and not because of pointers.** Reproduced today: `Dirent(d_name: [0])` on `d_name: i8[1024]` is `error[fixed_array_length]` — a fixed array wants a literal of its exact length, and 1,024 elements is not a program anybody writes. Nothing is waiting on it (the harness lists directories with `find -print0` and zero externs), so it is here as a **completeness** question under §1.12: a header Heroes cannot bind is a library the author must leave C code around for | docs/panel/087 · docs/panel/085 · spec:14 · spec:186 · spec:229 · runtime/parts/os.c:62 · selfhost/library_source.hero:150 | eleven doors, one room: this is where §1.11's *"everything comes from C"* meets §1.12's *"must not segfault and must not corrupt memory"*, and four of the eleven are a correct program dying

## From panel 088 (2026-08-23) — the sitting that had already been held

- [x] **RATIFIED 2026-08-24** (author answer `1a`, `/decide`). The resolution stands as adopted: O1 and O3 **closed** rather than deferred, O4 **vetoed** on §1.11, and the place store — which was *"adopted and waiting"* when this item was written — has since landed on the author's own trigger and is no longer waiting for anything (measurement 013, 944.76 s → 188.51 s). What the yes settles is what the sitting itself contributed: the measurement panel 037 asked for and M-selfhost-probe never took, and the finding that it said **not yet** at a moment when the compiler already self-hosted. What it does not settle is anything about the landing, which was its own decision one row below and is already ratified there. Verified with the yes rather than assumed: `docs/panel/088` § Author's verdict already carried the author's *"approvo panel 037"* from the same morning, so this tick closes the **formal** ratification that section explicitly left queued — the last of the four sittings (085, 086, 087, 088) that could not be ratified because nothing asked. Was | panel 088, provisional 2026-08-23 | **Ratify the resolution, or overturn it.** Adopted: **nothing lands** — O1 (in-place on refcount 1) and O3 (geometric growth alone) are **closed rather than deferred**, O4 (refuse and document) is **vetoed** by the ffi-pragmatist on §1.11, and the **place store stays adopted and waiting** exactly as panel 037 item 2 left it. What a yes settles: that this sitting's contribution is the measurement 037 asked for and M-selfhost-probe never took, and that the measurement says **not yet** — the compiler self-hosts, 1,011 s *finishes*, and removing **99%** of push's element copying moves the harness emission from **12.29 s to 11.46 s (1.07×)**. What it does not settle: the row below, which is the author's alone by 037's own words | docs/panel/088 · docs/panel/037 § Resolution · design.md:1531 | five seats spent three hours re-deriving a twelve-day-old ruling, and what they added was a prototype and a number that killed the headline
- [x] panel 088 R2 — **ANSWERED 2026-08-24 by the author: “approvo panel 037” — and the place store landed in the same session (measurement 013).** The build the trigger was written about: 944.76 s on 2026-08-23, **188.51 s** with the store landed (5.01×); the suite 20m35s → 198.72 s (6.2×). All five R4 conditions honoured — `**` signature; the copy-guard with `place-store-c5` as its firing case; `Place.path.len == 0` held by ir_verify with a test; the spec amended in the same commit at **3506** (predicted 3508, falsifier ≥3512); the ABI bump honoured at 15, not overturned. What landing exposed and repaired on the way: a borrowed load outliving an `@` call on its own root was kept alive only by rule 5’s accidental refcount 2 — honest counts made it a use-after-free, caught by c5 case 5, repaired in ir_own (`load_survives_write`), ASan-verified both ways. Was | **Is the compiler's build time unacceptable?** Panel 037 item 2 lands the place store on one of two triggers: *"if a closure-list stage does not finish **or the author declares a stage unacceptable**"*. Nothing fails to finish, so the second is the only live one and only you can pull it. The numbers, all measured 2026-08-23: `heroes build selfhost/main.hero` **16m51s** · `heroes test selfhost/main.hero` **20m35s** · `heroes check selfhost/main.hero` **8m03s** · the net **6m28s** · the seed's own build **3.95 s**. What a yes buys, measured rather than hoped: 100k pushes **22.34 s → 0.00 s** on the prototype, and **1.07×** on the compiler's emission — so a yes is *not* a bet on the build getting faster; it is a decision that a quadratic in the language's commonest line is unacceptable **on its own terms**. Five conditions ride with it (panel 088 R4) and one of them contradicts a ratified item: 037 required `HERO_RUNTIME_ABI` bumped, and today's argument is that a missing function is a link error, louder than a stamp. **My recommendation: not yet** — say no here and yes to the row below, because the same hours spent on the 46% buy the whole compiler rather than 7% of it. **Update 2026-08-24 — the row below is taken, and it points back here (measurement 012).** Two repairs landed without the place store — the writer's chunked accumulation and the loader's incremental line count: build 944.76 s → **424.67 s (2.22×)**, tests 2.77× faster, all without touching this question. But the profile they produced says the rest is yours: **load+lex 205.53 s + parse 124.66 s = 78% of the remaining build**, and both are `p @ push(p, v)` accumulators that chunked accumulation cannot reach — chunks work for strings because `join` rebuilds in one pass, and the language deliberately has no array concatenation (panel 043), so a `[[Token]]` flattens at the same quadratic it left. The place store is the one repair with a name for that shape, prototyped at 088 (100k pushes 22.34 s → 0.00 s). **My recommendation changes to: yes** — the measurement this file said to take first has been taken, and a build whose majority cost is one lowering pattern is what 037's *"the author declares a stage unacceptable"* trigger was written for | docs/panel/037:147-160 · docs/panel/088 R2, R4 · docs/measurements/012 | the only landing trigger left is a sentence only the author can write
- [x] panel 088 R5 — **ANSWERED 2026-08-24 (measurement 012), and both repairs landed in the same session.** The inference measured true: the writer was **407.16 s of the 944.76 s build (43.1%)** — repaired with §4.10's own chunked accumulation — and the sampler then found a second culprit nobody had suspected: **the loader re-read the whole accumulated text once per file** (`source.hero:156`, `starts_of` over the concatenation for each `lines_before`), **112.6 s** of every command, repaired with an incremental newline count. Build: **944.76 s → 424.67 s (2.22×)**, output byte-identical on 764k lines after each repair, seed regenerated in the same commit, 488 tests and the 1132-check net green. The phase profile the entry asked for now exists and is readable — load+lex **205.53 s (49%)** · parse 124.66 s (30%) · check **34.52 s (8%)** · emit 54.33 s (13%) — so the check phase and the type-table walks are **exonerated**, and **79%** of what remains sits in `p @ push(p, v)` accumulators (`lexer.hero:69` re-pushes every token of every module; the parser grows AST arrays the same way, sampled) that chunking cannot reach, because the language has no array join (panel 043). That is R2's shape, and R2 now has its number. Was | **Where does the compiler's time actually go? It is not push.** Emission scales **9.6× source → 46× time** (3,630 lines / 22.0 s against 34,812 / 1,011 s, exponent ≈1.66), and the engineer removed **99% of push's copy traffic** for a **1.07×**. So 93% of the superlinearity is somewhere nobody has looked. Three candidates, none measured: the **check phase** (8m03s of the 16m51s, and it does no emitting at all), the **type-table and interner walks** (`check_table.len_of(c.types)` drives at least three O(types) sweeps per unit — `emit_descriptor_set.generated` walks every type id, and `mentions_generic` is called inside that walk), and the **writer's own accumulation** — which is no longer a candidate but **the leading suspect, measured 2026-08-23 an hour after the sitting closed**. `selfhost/emit_writer.hero:82` is `w.out @ w.out + text + "\n"`, called once per output line, and `spec:167` says in its own words that `+` on `str` **copies both sides**. Concatenating the emitter's own shape of line: **25k lines ~0.45 s · 50k ~1.2 s · 100k ~8.4 s of work** — 2.7× then 7× for a doubling, so superlinear and getting worse as the buffer grows. The compiler emits **724,245** lines through that function. Two independent shapes agree: the *observed* emission scaling is 5.83× lines → **46×** time (124,149 lines / 22.0 s against 724,245 / 1,011 s), which is between n² and n^2.2 and matches this, while push was measured at **7%**. **The extrapolation to 724k lines (~440 s, about half the emission) is an inference and is marked as one** — the measurement that settles it is an instrumented `push_line` or a writer that appends into a `[str]` and `join`s once at the end, which is the repair the spec's own sentence already recommends (*"`join` and `repeat` build in one pass"*). That repair needs **no runtime change, no ABI, no panel** — it is one module's internals, `emit_writer.hero` is 204 lines, and the fixpoint is the test. The instrument that would settle it does not exist and is cheap: `heroes lex` / `heroes parse` / `heroes check` already split the phases on the command line, so timing the same file through each gives the phase profile without a profiler; `sample` (macOS) against a running `heroes build` gives the rest, and it is the machine's instrument rather than this compiler's surface (DECIDE's own `--profile` refusal). **My recommendation: do this next, before anything else in the emitter** — it is the one measurement that could make every other verification cheaper, and today it is a blank | docs/measurements/011 § Corrections · docs/panel/088 R5 · selfhost/emit_writer.hero:52 | the 46× was blamed on the wrong line, and the right one has never been looked for

## Open — found 2026-08-23 while writing a harness scanner

- [x] **CLOSED 2026-08-24: the panel was convened, and the repair it pointed at has landed** (ticked in `/decide` without being asked — the item's ask was *"convene it, and soon"*, and both halves happened). The sitting is `docs/panel/088`, convened **within the hour** on 2026-08-23, and its first finding was that this brief's own question had been ruled on twelve days earlier by panel 037 — the coordinator had written a brief for a sitting that had already been held. The panel's own resolution was **nothing lands** (O1 and O3 closed, O4 vetoed), and what it added instead was the prototype and the number that killed this item's headline: removing **99%** of push's element copying moved the harness emission only **12.29 s → 11.46 s (1.07×)**, so the 46× this item described as *"shape rather than proof"* was correctly hedged and correctly not attributed. The 46× turned out to be the writer and the loader (measurement 012, 944.76 s → 424.67 s), and only then did the place store land on the author's trigger (measurement 013, → **188.51 s**, 5.01× overall). So this item was right about the quadratic, right that it needed a panel rather than a fix, and wrong about nothing — the cost it measured (25k/50k/100k = 0.9/3.0/11.7 s) is exactly what `hero_array_push_owned` now removes. Was | measured 2026-08-23, **needs a panel and this item is the brief** | **`xs @ xs.push(v)` is quadratic, and it is the line every Heroes program uses to grow anything.** `runtime/parts/array.c:110` allocates a **new array of exactly `len + 1`** on every push and copies every element through `elem->copy` — an indirect call per element, per push. No capacity, no geometric growth, no fast path for a uniquely-owned array. Measured, one program per size: **25k pushes 0.9 s · 50k 3.0 s · 100k 11.7 s** — a doubling costs 4×, so building n elements is O(n²). Found because `strings.lines` over `seed/heroes.c` (22 MB, 724k lines) **never finished** — ten minutes and killed — where a byte walk over the same text is **0.88 s**. The compiler's own cost has the matching shape: `tests/harness/main.hero` is 3,630 lines and emits in **22.0 s**, `selfhost/main.hero` is 34,812 and emits in **1,011 s** — 9.6× the source for **46×** the time (exponent ≈ 1.66). Nothing here attributes that 46× to one cause, and no `grep` was run over `selfhost/` for the idiom's count, so both are shape rather than proof. **Why it is a panel and not a fix**: `xs.push(v)` is a value — `ys = xs.push(1)` must leave `xs` untouched (spec:73, *"no aliasing exists anywhere"*) — so growing in place is sound only where the old array provably dies, which the runtime cannot see from a pointer and the **emitter** can. That makes it the runtime ABI plus the emitter's ownership contract: CLAUDE.md §4 architecture, and panel 022's *"one unshare per step of a mutated place"* is the same idea one artifact earlier. Three costs for the panel to weigh: a capacity field bumps `HERO_RUNTIME_ABI` and regenerates the seed; a second entry point (`hero_array_push_owned`) is emitter surface and a new way to be wrong; any in-place path must survive `--sanitize` plus `hero_runtime_check_leaks()`, where an aliasing mistake is a use-after-free rather than a wrong answer. **My recommendation: convene it, and soon** — not for elegance but because CLAUDE.md §13's second clause is the author's own (*"performance is not a goal but must not be a ceiling"*), and a 20-minute test loop is what makes a verification stop being run. It is also the one open item whose repair would make every other one cheaper to check | docs/measurements/011-array-push-is-quadratic.md · runtime/parts/array.c:110 · docs/panel/022 | the commonest line in the language is the slowest thing in it, and nothing was watching

## From panel 087 (2026-08-19) — the guard that could not fire, and the three doors still open

- [x] **RATIFIED 2026-08-23**, hours after being queued (author instruction, *"ratifica tutto"* — blanket over 085, 086 and 087; `docs/panel/087` § Author's verdict says what the yes settles). The repair stands as landed: the runtime pre-check, `HERO_OS_NOT_TEXT`, +0 spec tokens, no ABI bump, the abort in `hero_str_from_bytes` untouched. **The nine queued items are untouched by the yes**, `file_not_text` included — the engineer's reversal condition (a named reader in the same commit plus a golden that fires the arm) is unchanged, and the collapsed message is accepted as a stated cost, not as a closed question. Was | panel 087, provisional 2026-08-19 — **queued 2026-08-23, and the panel file had no `## Author's verdict` section at all** (added in the same commit as this line, pointing here; 085 and 086 both carried one). Two sittings in a row could not be ratified because nothing asked | **Ratify the repair as it landed, or overturn it.** Adopted provisionally and shipped: `read_file` on bytes that are not text **fails instead of aborting** — `runtime/parts/os.c:130` pre-checks `hero_utf8_valid`, releases the buffer, sets the new `HERO_OS_NOT_TEXT` and returns the empty str, **five lines of C and +0 spec tokens**. What a yes settles: the spec was never wrong (`spec:179` already types the built-in `-> str?`), so this was a §12 repair in the one place the spec cannot see, and paying 11–21 tokens to *describe* the old behaviour would have written a defect into the document. It also settles the ffi-pragmatist's uncast veto: **the abort inside `hero_str_from_bytes` stays**, because `hero_str_chars` (`runtime/parts/text.c:87`) takes one byte off an invalid sequence and depends on it for the language-wide well-formedness invariant. What a yes does **not** settle: the eight items below, of which the first is the only one that changes what a program sees. Re-measured 2026-08-23 in one session: the five-line reproducer is `fail: read_failed` at exit **0**, and the whole net is green with a summary line where it used to be exit 134 with no summary at all. The recommendation is **yes** — this is the cheapest shape anybody found (three seats hunted for a spec removal and the warden refused all five non-zero options), and it is the only one that keeps `docs/defects/002`'s dead guard alive without weakening the invariant that the rest of the language rests on | docs/panel/087 § The resolution adopted · docs/defects/002 · runtime/parts/os.c:130 | a legal program that handled both arms of a fallible was killed before either, and the repair cost the spec nothing
- [ ] **[089 row 6]** panel 087, the two seats that compile disagreed | **Name the not-text status as its own `e.code`, or leave it collapsed into `read_failed`.** Landed provisionally: **collapsed**, because that is the resolution with the least Heroes-visible surface, and its cost is stated rather than smoothed — the message is `could not read <path>` for a file that read perfectly well. The runtime already distinguishes the two states (`HERO_OS_NOT_TEXT 3`); only `selfhost/library_source.hero:150-153` collapses them, and **one `constant` line reverses it**. **For** (ffi-pragmatist): `library_source.hero:143-145`'s own comment says the codes are distinguished *"because a caller almost always wants to"*, and a JPEG is not a failing disk; the absence of the declaration is caught loudly, `error[ffi_unknown_name]` at exit **1** on the author's own line, measured. **Against** (compiler-engineer): measured **+2 `_Static_assert`s in every program in the language** (10 asserts → 12 on a probe), **142** emissions re-blessed — and `tests/harness/suite_emission.hero:12-17` says those bytes are what *two independent implementations* agreed on, so re-blessing spends the last cross-compiler agreement now that `differential.rs` is dark; plus `heroes mutate`'s `typo-code` row is **25 mutants, 0 killed**, the table's only zero, so a new code lands in the one contract nothing checks; plus **no reader exists** — `selfhost/cli_input.hero:26` collapses every read failure into `unreadable`. Its reversal condition: **a named reader in the same commit** (`cli_input.hero:24-28` widened so `heroes check <a binary>` says why) plus a golden that fires the arm. Also refused, and not on taste: the cheap `status == 3` literal, because `library_source.hero:129-134` records that **M-header-constants** moved those numbers into the header and that *"the header is the only place they are written"* | docs/panel/087 § Where the seats disagreed · selfhost/library_source.hero:150 · runtime/hero_os.h | a status the runtime knows and the language cannot ask about is the same shape this sitting existed to fix, one level up
- [x] **CLOSED 2026-08-24 by panel 089's landing.** `spec:229` no longer sends anybody through a null test: the sentence *"test `c == nullptr` first, because converting one aborts"* is **deleted** (−15 tokens, the named removal that funded the sitting), and the paragraph now reads *"`c.validated()` copies one back as a `str?`"*. The route it names exists and was run on ten shapes — ill-formed bytes, a null pointer, a good string, empty, 70,000 bytes, a UTF-8 sequence truncated mid-character, UFCS, `?`, `.default()` and a record field — all exit 0, clean under ASan/UBSan and the leak gate. The spec-warden's veto of the obvious extension never had to be tested: the sitting did not enumerate two paths, it **removed** the sentence and named a form that has one. Was | panel 087, found independently by two seats | **`spec:229` sends a binding author through a null test and then kills them anyway.** *"test `c == nullptr` first, because converting one aborts"* names `nullptr` as **the** reason. Measured against real `sqlite3.h` — §4.19's own ladder step 3, `:memory:`, `insert … cast(x'fffe' as text)`, `sqlite3_column_text`, **null-checked exactly as instructed**, then `.to_str()` → `panic: hero_str_from_bytes: not well-formed UTF-8`, exit **134**. The emitted C shows the path: `t37 = (const char *)sqlite3_column_text(...)` → `t44 = hero_str_from_cstr(t43)`. The llm-ergonomist reached the same sentence from the document alone. **The wording is the decision, not the fact**: the spec-warden refused the obvious extension in advance — turning a true floor into a two-path enumeration is *"row 3405's exact anti-pattern"*, +11 tokens, and panel 059 ratified the floor. So the choice is a floor that covers both reasons, or a pointer to a fallible form that does not exist yet | docs/panel/087 § The four doors · spec:229 · runtime/parts/str.c:266 | `.to_str()` is the door **every** binding uses, and §1.11 says everything comes from C
- [x] **PROMOTED 2026-08-24 to its own item in the 089 section above, because the sitting proved it is a DIFFERENT hole.** Neither the adopted resolution nor any option on the ballot reaches it: `hero_args_at` converts **eagerly**, so no `cstr` ever reaches the program and the route that rescues a SQLite column cannot rescue argv. The ffi-pragmatist compiled the shape that does — `args_checked() -> [str?]`, not `[str]?` — and it needs one added runtime function and zero language change. It is now **the only one of panel 087's four doors still open**. Was | panel 087, ffi-pragmatist | **`args()` has no error channel, so no status code can reach it.** Measured: `./argv $'\xff\xfe'` → `panic: hero_str_from_bytes: not well-formed UTF-8`, exit **134**, via `runtime/parts/os.c:62` `hero_args_at` → `hero_str_from_cstr`. `args() -> [str]` (spec:179) is not fallible and the shell hands a program whatever bytes it likes, so this is the one door of the four that `read_file`'s repair shape cannot be applied to. Options are all surface changes and none is obviously right: `args() -> [str]?`, a per-element `[str?]`, lossy replacement, or skipping the ill-formed arguments and saying so. Reproducer left at `panel087-ffi2/work/argv.hero` | docs/panel/087 § The four doors · runtime/parts/os.c:62 | argv is the one input a program cannot decline to receive
- [x] **CLOSED 2026-08-24, and the item's own claim was FALSE** (panel 089, compiler-engineer, reproduced by the coordinator the hard way — by writing the defect into the first draft of the repair and watching it fire). The claim was that *"the whole class of 'ask C whether this pointer is safe' is closed"*. What is closed is asking **C** whether a pointer is **NULL** — and NULL is the one question the language already answers for itself, `c == nullptr`, so it never has to cross. Everything else about a non-null pointer is askable. The first draft of `validated` put the null test in C and died exactly as this item predicted (`panic: a null cstr was passed to a C function`, exit 134); moving that one line into Heroes fixed it, and `library_source.hero` now carries the measurement in its own comment so nobody repeats the trip. **`hero_cstr_nonnull` is untouched and needs no exemption** — an exemption would have been a hole in §12's guard for a problem that does not exist. Was | panel 087, ffi-pragmatist, §1.12 completeness | **A null-safety predicate is categorically unbindable, and that is a hole in §1.12's own words.** The seat wrote `bool hero_cstr_is_text(const char *p)` — false for NULL *and* for ill-formed bytes — bound it through an `extern` group and compiled it: the emitter wraps **every** `cstr` argument in `hero_cstr_nonnull`, so the emitted call is `hero_cstr_is_text(hero_cstr_nonnull(t4))` and a null argument panics **before the predicate can answer**, exit 134. So the whole class of *"ask C whether this pointer is safe"* is closed, and §1.12 says the boundary must be **complete**. Either the wrapper gains an exemption for predicate-shaped bindings, or the answer is emitter-side composition — a fallible `cstr → str` form — which is `read_file`'s shape one level up | docs/panel/087 § The four doors · panel 053 (`hero_cstr_nonnull`) | the guard that makes every binding safe is the guard that makes one kind of binding impossible
- [ ] **[089 row 4]** panel 087, spec-warden, and it is the honest version of a vetoed option | **~12 reachable aborts the spec never names — one sitting, not a clause.** The veto killed *"this document names them all"* by running a program: a `{K: i64}` map built inside a generic and keyed at `f64` with a `nan` is `heroes check` exit **0** and `heroes run` `panic: a map key that is not equal to itself (nan)` exit **134** (reproduced independently by the coordinator; already filed at `docs/panel/084` and `SCHEDULED.md`). The runtime has **63 `hero_panic` sites / 49 distinct messages**, and the reachable non-compiler-bug ones the spec is silent about include `out of memory` (`alloc.c:51`), `out of memory comparing a deep value`, `array`/`string length overflow`, `array too large`, `array too large to sort`, `map too large`, `f32`/`f64 render overflow`, `cannot create the C locale for rendering`, `a null cstr was passed to a C function`. The spec announces every abort it *does* name with the word "abort" — **7 lines, 8 occurrences** — which is why the silences read as guarantees. Naming them is a spec cost nobody has priced and a §12 question for each | docs/panel/087 § The veto · docs/panel/084 | the reader's rule is "aborts are stated", and the document keeps it about two thirds of the time
- [ ] **[089 row 5]** panel 087, historian | **Every language that fails on bad bytes ships a bytes reader beside it; this one has none.** Verified across six: Rust fails (`ErrorKind::InvalidData`) **and** ships `fs::read`; Python raises **and** offers binary mode plus `surrogateescape`; Go, Nim and Elixir never ask because their string is bytes. **Zero of six abort.** PEP 383 is the record of shipping the strict half alone: Python 3.0 made a class of file unreachable and `surrogateescape` was retrofitted **one minor version later**. Measured here: the spec's only mention of bytes is `str` being *"indexed and measured in bytes"* (`:51`), and the emitter's built-in list has no byte reader — so `fail(…)` gives a program a branch it can **observe but not act on**. **The counter-argument is in the record too, and it is the provoking case**: the seat's own falsifier is *"a real program that does something useful other than reporting and exiting"*, and `tests/harness/suite_records.hero`'s `continue` is exactly that — it skips the photograph and finishes the walk. So Principle 0 has no compiler-need for a bytes reader today, measured, and the day a harness must read a non-`.hero` artifact byte-for-byte it does | docs/panel/087 § Where the seats disagreed | the escape hatch is what the precedent says you owe, and the one program that provoked the sitting does not need it
- [ ] **[089 row 8]** panel 087, spec-warden, found while hunting a removal | **`spec:14` is false and the repair is free.** *"Syntax is ASCII-only; strings and comments may contain any UTF-8."* U+000D is valid UTF-8, and a raw carriage return inside a string is `error[raw_carriage_return]` at exit **1** — `tests/golden/check/raw-carriage-return.expected`, confirmed by the coordinator. §12 says the compiler has the bug, but here the *diagnostic* is deliberate (it exists because a CR is invisible on screen and the next reader deletes it by retyping the line), so the document is what has to move. The warden priced the honest rewording at **~±0 tokens**, which is why it funds nothing and is its own item | spec:14 · tests/golden/check/raw-carriage-return.hero | one of the two artifacts is wrong and the cheap one to fix is the sentence
- [x] **DECIDED 2026-08-23 — (a) at tags **and** (c), both applied in the same session** (`/decide`). The check exists now: CI's *The seed is what today's source emits* re-emits `selfhost/main.hero` from the seed-built compiler and `cmp`s it against `seed/heroes.c`, on **Linux at tags and `workflow_dispatch`** (15m41s, and Linux is the cheap leg). Two premises were verified rather than assumed before choosing that host: `seed/heroes.c` holds **zero** absolute paths and its `#line` directives carry relative `selfhost/*.hero` paths, so any checkout matches from the repository root, and `word_bits` — the one platform-varying quantity the port derives — reaches **diagnostics only**, never the emitted C (`emit_c_spellings.hero:44`, called at `cli_verbs.hero:85,108` with a literal 64). If it ever goes red on spellings alone, that is the finding and it returns here: the seed would be platform-specific, which nobody has decided. (c) landed as `seed/README.md`'s **third forcing condition** — anything `selfhost/` says that changes what the compiler *does*, with the library's own text as the case conditions 1 and 2 both miss — plus the rule of thumb that needs no instrument: if the diff touches `selfhost/`, the seed is regenerated in the same commit. And the false sentence at `suite_determinism.hero:13` now names its instrument and records that it stood for four days with nothing behind it. Verified after: `yaml` parses (10 steps), `determinism: 114 passed, 0 failed`. Was | panel 087, compiler-engineer, confirmed by a second grep | **`suite_determinism.hero:13-14` names an instrument that does not exist, so the seed can go stale in silence.** The comment reads *"this repository now has one that matters — `seed/heroes.c` is checked by `cmp` against what the seed-built compiler emits."* Two independent greps found **no such check** anywhere in `tests/harness/` or `.github/workflows/`: CI builds *from* the seed (`ci.yml:226`) and never compares it against a fresh emission. Related and worse, from the same seat: `seed/README.md:56-66` names two forcing conditions for regenerating the seed (the ABI moves; `selfhost/` uses a form the seed cannot parse) and **a library-text change is neither** — so a stale seed passes the 3.4 s clang check while CI's harness runs a compiler carrying the *old* library. The decision is whether the check is added at all: the fixpoint leg is **15m41s measured**, which is the same trade as the 90×-slower item above | tests/harness/suite_determinism.hero:13 · seed/README.md:56 · .github/workflows/ci.yml:226 | CLAUDE.md §11's exact shape — a premise that expired while the comment went on reading as correct
