# Decisions the compiler is waiting on

Read by **`/decide`**. Every item asks **what should be true**, and until it is
answered the compiler goes on behaving some way by default — which is the whole
difference from `LEARN.md`.

Two rules bind this file, both learned the hard way:

- **Verify before asking.** An entry is a claim from the day it was written, and
  entries outlive their causes: eight were found already settled in the first two
  sessions. Asking the author about a settled question is the one cost this list
  cannot pay.
- **Rank by what it blocks**, never by age, and say the blocker in the question.

Format: `- [ ] <origin> | <what> | <where to look> | <why it matters>`

**Split from `docs/debrief/QUEUE.md` on 2026-08-12, by author instruction.** One
file held 192 open items of three different kinds, and a list you have to filter
before you can read it is a list nobody opens. The closed items — the record —
stayed in `QUEUE.md`, because every commit subject in this repository cites that
path and CLAUDE.md §14 does not rewrite a record.

An answer is applied in the same session it is given — spec, design.md or
CLAUDE.md amendment, DESIGN-LOG line, the item ticked with the verdict written
into it. A decision recorded and not applied is the same open question with more
paperwork.

**The order is the ranking** (2026-08-12): the three headings below are what each
group blocks, and inside a group the first item is the one the next milestone
reads first. Nine items left this file the same day for `LEARN.md` — seven
milestone-close offers and two questions the code had already answered — and one
was ticked into the record because the diagnostic it asked for exists and fires.

## Open — blocks M-program-corpus, the next milestone

- [ ] panel 037 / spec | **The spec is silent on the cost of building an array, and its one cost sentence gives the wrong answer.** The llm-ergonomist, reading only the spec: `@` is "copy in, copy out", so a literal reading of `push(@xs, x)` is n² — *"the document does not fail to state the cost; it states semantics whose literal reading gives the quadratic answer, and I overrode it on a hunch"*. Its remedy is one clause naming `join`; it **pre-vetoed** any wording that explains copy-on-write, because that makes a line's cost depend on whether some other binding is live fifty lines away. **The warden's measurement, owed and now taken (2026-08-12, base 2588): the `join`-only clause is +23 → 2611; the honest one that also names `push`'s copy is +39 → 2627.** Panel 012's rule applies above the soft 2000, so either needs a named removal or a pre-registered falsifiable prediction | docs/panel/037 § The llm-ergonomist answered a different question · design.md:1355-1365 | the only silent hesitation in a five-program experiment; every other wrong guess failed loudly — and M-program-corpus is where programs start building long strings

## Open — ratifications and confirmations owed

- [ ] panel 037 | **RATIFICATION OWED — array growth.** Ratify or overturn: the proposal refused (two compiled vetoes) · the **place store** adopted as the shape and **deferred to M-selfhost-probe**, which measures whether anything needs it · design.md §4.10's `join` gap corrected now (verified landed, design.md:1358-1365) · the spec clause **queued, not adopted** · `/panel`'s soundness lane. The one to read first is the second: it is a decision to *wait*, and the warden's rule behind it is "soundness without need still waits" | docs/panel/037-array-growth.md § The resolution | the first panel whose proposal was refuted by implementing it rather than by arguing about it
- [ ] panel 038 | **RATIFICATION OWED — constants from headers.** Ratify or overturn: a bodyless `constant` inside an `extern` group · the value never written in Heroes · `DeclKind::Constant` widened rather than a sixth variant · the **mangled accessor** as CLAUDE.md §7's first named exception to unmangled `extern` names · `__builtin_constant_p` as the constancy check · four diagnostics (`ffi_type` extended, `ffi_constant_type`, `ffi_not_constant`, `ffi_unknown_name`) · the spec at **+28** · the `@out: ptr` repair. The one to read first is the **spec-warden's objection, which stands**: the digit class is 267 sites and this reaches 5, and what carries the decision is your instruction, not a verdict | docs/panel/038-constants-from-headers.md § The resolution | the first sitting where a judge's prediction was pre-registered and scored inside the same session — and it did not go the proposal's way
- [ ] panel 037 / ABI | **`HERO_RUNTIME_ABI` stayed at 10 across a semantics-only runtime change and every generated unit accepted it** — measured by the ffi-pragmatist while building three runtimes. *"The least-guarded change this ABI has."* Decide whether the stamp should cover behaviour as well as declarations, and how it could. **Verified 2026-08-12: the cache key already hashes the runtime's *whole contents*, every file under `runtime/parts/`, and CLAUDE.md §7 was amended the same day to say the stamp's job is version skew "and it is worth keeping for that alone" — so this is a confirmation that the answer is "no extension", not an open design question** | runtime/heroes_runtime.h · crates/heroes-cli/src/commands/toolchain.rs:119-128 · CLAUDE.md §7 | the guard exists to catch a decoy runtime, and a decoy that changes only what the code *does* walks straight past it
- [ ] open question, author 2026-08-11 | **Do subdirectories ever arrive, and in what spelling?** Today `use geom` reads `geom.hero` beside the file, and `use shapes/geom` / `use geom.shapes` are refused by name. The escape valve is Nim's and it is **additive**: the identifier stays the normal form and a string arrives only for paths that are not identifiers (`import "gfx/3d/somemodule"`), so adding it later breaks nothing and removing it would have. The unresolved half is not the syntax but the **qualifier**: from `use "shapes/geom"`, is the module `geom`, `shapes.geom`, or `shapes/geom`? The ergonomist, writing from the spec alone, **declined to use a subdirectory at all** because it could not predict the answer, and produced `use "geom"` + `use "shapes/geom"` — two basenames, one qualifier, unstated — which the current rule makes unwriteable. Recommendation on the record: decide at **M-package-manager**, where packages create the real pressure (naming a module of another library) and the case that determines the answer finally exists; the separator question is settled by precedent (`/` always in the source, mapped by the compiler — Go, C's `#include`, Python) | docs/panel/031 § the ergonomist's H11 and ct4 · crates/heroes/src/syntax/decl.rs (`module_path_has_no_parts`) | the machinery for a basename collision already exists — it is what refuses `geo_m` beside `geom`. **Ratifying the deferral moves this item to `SCHEDULED.md`; refusing it makes it a panel**

## Open — blocks M-selfhost-probe and the port

- [ ] panel 038 / §11 | **`emit/decls.rs` is 768 lines and CLAUDE.md §11 says ~300.** Panel 038's compiler-engineer predicted exactly this and it scored correct: 717 before M-header-constants, 768 after, so the breach predates the milestone and the milestone widened it. It is not one concern either — the `#include` walk, the `link` walk, the assertion emitter, the prototype, the definition, the `main` shim, the string table and the reachability cull all live there. The cut that suggests itself is `emit/externs.rs` (the three group walks plus `extern_assertions`) and `emit/main.rs` (the shim and the test runner), which is the same cut `syntax/externs.rs` already made at panel 036. **Decide: now, or at M-selfhost-probe when the port reads it?** — a split touches every caller, and doing it inside a milestone that is also changing behaviour is how a mechanical change hides a real one | crates/heroes/src/emit/decls.rs · docs/panel/038 § Predictions to score | a judge predicted the file would end over 750 unsplit and was right, which is the second time a line budget has been the thing a panel got exactly right
- [ ] M-scalars-run | The compiler has no line budget and `crates/heroes/src` is already 15,145 non-test lines — about 4x Pascal-P4. **Re-measured 2026-08-12: 25,482 non-test lines in `crates/heroes/src`, 27,399 across both crates, and 21 files over §11's ~300.** "Does it fit one person" is no longer answerable globally; the only live ceiling is CLAUDE.md §11's per-file one. Recorded by the engineer, unanswered by design.md: decide whether a global number is wanted before the port makes it a Heroes number | docs/panel/020 § Watch list · design.md Part 0 | the port's size is the self-hosting schedule, and nothing is watching it
- [ ] defect-adjacent, harness | Metric 2 minimum viable protocol (panel 011): local open-weights model, or 2 pre-registered tasks per milestone. NOT an API-key question. **Verified 2026-08-12: nothing has been taken — `docs/measurements/` holds no metric-2 file and `harness/tasks/` holds only its README — and panel 018's open colon question names the first metric-2 run as its designated tiebreaker, so this blocks a *language* decision and not only a number** | docs/panel/011-measurement-without-an-api-key.md · docs/panel/018 § E vs F | ~16 samples by v1 beats zero; the pre-amendment number is still unrecoverable later

## Open — language and process, blocking nothing dated

- [ ] reasoning 003 | **Does Part 6's `Macros` row cover compile-time *evaluation*?** The row's reason — *"the code you read is not the code that runs"* — reads as covering it, and Part 6's preamble gives that reading force, but `comptime`/`const-eval`/`constexpr`/`CTFE` appear in the whole record exactly **once**, at design.md:1821, and there as a remark about Zig getting an answer right by accident (re-verified 2026-08-12: still exactly one occurrence; the row is design.md:2124). A permanent-rejection list that names one spelling of a mechanism and is silent on the other is how a refusal gets rediscovered as an idea. Part 6 is design.md Parts 1–11, so amending it is a **panel** path (CLAUDE.md §4) — this item decides only whether that panel is convened and when. **It must be numbered 039**: `docs/panel/` now ends at 038, and the skill's "next NNN" is right for once, but the number is worth stating because the last sitting was numbered from a worktree | design.md:2114-2132 · design.md:1821 · docs/reasoning/003-comptime-and-macros.md § What stayed open | the cheapest possible outcome is one Part 6 row at zero spec tokens, and the expensive one is re-litigating this every time a new language ships comptime
- [x] reasoning 003 | **ANSWERED 2026-08-12 — adopted, and the branch is merged and deleted** (author instruction, given as a branch cleanup: recover the notes, then remove every branch but `main`). The merge landed as measured: `CLAUDE.md` and `QUEUE.md` clean, three append-at-EOF conflicts (`DESIGN-LOG.md`, `docs/book/beats.md`, and `OPEN-QUESTIONS.md` — one more than `merge-tree` predicted, because reasoning 003's own watch-list entries had since been appended at the same point), all three resolved chronologically with the 2026-08-10 lines in date order rather than at EOF. **The hand-off commit's seven items were dropped, every one verified as already done on `main` independently** — and done better: K's superlative is now sourced to kdb+'s STAC-M3 Antuco/Kanaga results (design.md:2555), R/MATLAB carries precedent *and* failure mode (2559), the missing memory-model rejection table exists with ARC/ORC, Zig allocators and C++ RAII (1315–1320), the non-atomic constraint is stated with its thread condition (1325), arena-plus-indices is named for Heroes (1330), Part 7.13 got its shape (DESIGN-LOG 2026-08-10), and both future-proofing rules landed (design.md:1937, 1947, 2246–2248). Appending them as open would have broken this file's own verify-before-asking rule. Repairs applied: the README's routing table now names `LEARN`/`DECIDE`/`SCHEDULED`, and rather than rewrite three dated records it gained a § A note on the vocabulary of 000–002 mapping `/debrief` and note 002's retired numbered milestone id forward. Was: This directory now holds exactly one file, numbered `003` because its README and notes `000`–`002` live on `origin/claude/project-reasoning-wdcls1` — 5 commits, +584 lines, **now 132 commits behind `main`** (was 119 when this was written). Measured with `git merge-tree`: `CLAUDE.md`, `QUEUE.md` and `OPEN-QUESTIONS.md` merge clean; `DESIGN-LOG.md` and `docs/book/beats.md` conflict as append-at-EOF (both mechanical — the branch's line is dated 2026-08-10 and goes before main's block). Repairs owed if it lands: the README and note 000 cite `/debrief`, split into `/learn` and `/decide` on 2026-08-12, and note 002 keys its obligations to a numbered milestone id that § The names has since retired. The alternative is to keep this one note and let the class stay implicit. CLAUDE.md §4 makes this **author instruction, not a panel** — the teaching process is amended by instruction | origin/claude/project-reasoning-wdcls1 · CLAUDE.md §4, §14 | a note written into a directory that does not exist adopts an artifact class by writing rather than by deciding, and this one did
- [ ] panel 035 | The historian's structural condition, recorded rather than acted on: variant construction is unstated **because Heroes is asymmetric** — `.case(field: value)` builds and `.case name` matches, where ML, Haskell, OCaml and Rust use one syntax for both and so cannot omit it. Decide whether v2 restores the symmetry or the spec gains an EBNF appendix in Wirth's manner; either removes the *possibility* of the omission rather than patching this instance | docs/panel/035 § What a veto would compel | it is the only proposal in the session that would stop the class rather than the instance
- [ ] panel 020 | `print`'s separator is 2-1 against Heroes in the record: Pascal's `WriteLn` inserts nothing (the cited precedent, verified against ISO 7185), but Go's `println` adds spaces *and* writes to stderr, and Python 3 defaults `sep=' '`. The trailing newline is unanimous. Panel 006's historian and panel 020's both asked for a Go-style hedge in the spec by v1. **Measured 2026-08-12: naming the two counter-precedents inline costs +17 → 2605, and panel 012's rule above the soft 2000 wants a named removal or a pre-registered prediction for it** | docs/panel/020 § Watch list · docs/panel/006 | a model's prior is the thing the spec is buying, and here the prior is split
- [ ] panel 021 | Optimisation level is chosen by **verb** (`build` = -O0, `run` = -O2) and sanitising by **flag**. Unifying them is a larger surface nothing must type (§10), so it waits — but the asymmetry is now visible in `--help` (verified 2026-08-12: `run` reads "compile at -O2 and execute (the dev loop)", `build` names no level, and `--sanitize` is a flag on three verbs) | crates/heroes-cli/src/cli/table.rs · CLAUDE.md §10 | a surface that grew by accident is the thing the stopping rule exists to prevent
