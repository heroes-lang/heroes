# Decisions the compiler is waiting on

Read by **`/decide`**. Every item asks **what should be true**, and until it is
answered the compiler goes on behaving some way by default — which is the whole
difference from `LEARN.md`.

Two rules bind this file, both learned the hard way:

- **Verify before asking.** An entry is a claim from the day it was written, and
  entries outlive their causes: eight were found already settled in the first two
  sessions. Asking the author about a settled question is the one cost this list
  cannot pay.
- **Rank by what it blocks**, never by age, and say the blocker in the question.

Format: `- [ ] <origin> | <what> | <where to look> | <why it matters>`

**Split from `docs/debrief/QUEUE.md` on 2026-08-12, by author instruction.** One
file held 192 open items of three different kinds, and a list you have to filter
before you can read it is a list nobody opens. The closed items — the record —
stayed in `QUEUE.md`, because every commit subject in this repository cites that
path and CLAUDE.md §14 does not rewrite a record.

An answer is applied in the same session it is given — spec, design.md or
CLAUDE.md amendment, DESIGN-LOG line, the item ticked with the verdict written
into it. A decision recorded and not applied is the same open question with more
paperwork.

## Open

- [ ] harness | `heroes mutate` prints rates, not mutants — so a per-operator rate that MOVES between runs cannot be read, only guessed at (measurement 002 records two that moved and declines to explain them). It wants a way to print the surviving mutants of one operator | docs/measurements/002-metric-3.md § What cannot · crates/heroes/src/mutate/ | CLAUDE.md §12 says measurement beats opinion; a number nobody can attribute is an opinion with a decimal point
- [ ] M-ir-lowering close | Milestone debrief offers, all optional: walkthrough (one function from source to dump, following the four blocks of a `for` and the copy-out chain) · ratify the 5 adversarial IR goldens (continue-steps, try-copies-out, diverging-arms, nested-shortcircuit, for-evaluates-once) · mutation drill (change `continue_to: step` to `continue_to: test` in control.rs and predict which tests fail — the answer is interesting) · exit-quiz (hand-lower `for x in xs` or `e?` on paper, then diff against `--dump-ir`) | /debrief | the author's call, when and how much
- [ ] harness | Metric 2 minimum viable protocol (panel 011): local open-weights model, or 2 pre-registered tasks per milestone. NOT an API-key question | docs/panel/011-measurement-without-an-api-key.md | ~16 samples by v1 beats zero; the pre-amendment number is still unrecoverable later
- [ ] M-syntax-tree.1 | A `#` comment, then a blank line, then a declaration: does the comment become documentation — and which two comparisons decide it? | syntax/cursor.rs take_docs (§4.1) | Go's adjacency rule is two integer comparisons, not a parser mode
- [ ] M-syntax-tree close | Milestone debrief offers, all optional: walkthrough (tokens → tree → text, the two renderings and why both exist) · ratify the 5 adversarial cases (`fn` both positions, depth-zero continuation, braces as a block, signature without `:`, trailing comma) · mutation drill (break one line of `expr.rs`'s precedence table and predict which test fails) · exit-quiz (re-implement `wrapped` in fmt_expr.rs, or the three-line-shapes lookahead, on a throwaway branch) | /debrief | the author's call, when and how much
- [ ] M-name-resolution/M-data-declarations | Keep *declarations* out of inline arm bodies (historian, panel 014 prediction 6): Kotlin's grammar admits them and a name bound in an arm is a name nothing can read | crates/heroes/src/syntax/stmt.rs (statement), docs/panel/014-match-arm-body.md | Rust keeps declarations out of expression position deliberately
- [ ] M-name-resolution close | Milestone debrief offers, all optional: walkthrough (`heroes check --dump-scopes` on the appendix: the alphabetical table, the nesting, the read/write counts) · ratify the 5 adversarial cases (one tier · the `@`-typo · the four unused shapes · the hole's reach · shadowing with sibling loops as the control) · mutation drill (make `report_unused` count writes as reads and predict which of the 164 tests fail) · exit-quiz (re-implement the `nearest` candidate rule, or the sequential shadow check, on a throwaway branch) | /debrief | the author's call, when and how much
- [ ] M-typed-frontend close | Milestone debrief offers, all optional: walkthrough (a mismatch from `heroes check` end to end: which pass produced it, what `Want` was in play, why the message names the *other* end) · ratify the 5 adversarial cases (mixed arithmetic · non-exhaustive and the `_` ban · no implicit `T?` · swapped arguments · the jump-and-unit pair) · mutation drill (make `join.rs` treat a diverging branch as a value and predict which of the 259 tests fail) · exit-quiz (re-implement the `Want` enum's three cases, or the same-typed-argument rule, on a throwaway branch) | /debrief | the author's call, when and how much
- [ ] panel 020 | `print`'s separator is 2-1 against Heroes in the record: Pascal's `WriteLn` inserts nothing (the cited precedent, verified against ISO 7185), but Go's `println` adds spaces *and* writes to stderr, and Python 3 defaults `sep=' '`. The trailing newline is unanimous. Panel 006's historian and panel 020's both asked for a Go-style hedge in the spec by v1 | docs/panel/020 § Watch list · docs/panel/006 | a model's prior is the thing the spec is buying, and here the prior is split |
- [ ] M-scalars-run | The compiler has no line budget and `crates/heroes/src` is already 15,145 non-test lines — about 4x Pascal-P4. "Does it fit one person" is no longer answerable globally; the only live ceiling is CLAUDE.md §11's per-file one. Recorded by the engineer, unanswered by design.md: decide whether a global number is wanted before the port makes it a Heroes number | docs/panel/020 § Watch list · design.md Part 0 | the port's size is the self-hosting schedule, and nothing is watching it |
- [ ] M-scalars-run close | Milestone debrief offers, all optional: walkthrough (one function from `.hero` through the IR to the C to the binary, following the `#line` directives and the copy-out) · ratify the 5 adversarial `run/` cases (overflow-aborts, division-edges, short-circuit, copy-out-edges, join-slot) · mutation drill (delete the `preds.is_empty()` test in `emit/decls.rs::reachable` and predict which of the 55 goldens fail — the answer is interesting) · exit-quiz (hand-emit the C for a `while` loop, then diff against `--emit-c`) | /debrief | the author's call, when and how much
- [ ] panel 021 | Optimisation level is chosen by **verb** (`build` = -O0, `run` = -O2) and sanitising by **flag**. Unifying them is a larger surface nothing must type (§10), so it waits — but the asymmetry is now visible in `--help` | crates/heroes-cli/src/cli/table.rs · CLAUDE.md §10 | a surface that grew by accident is the thing the stopping rule exists to prevent
- [ ] M-strings-ownership close | Milestone debrief offers, all optional: walkthrough (one `str` from literal to heap block to sweep, following `--dump-ir` then `--emit-c`) · ratify the 2 new adversarial cases (str-copy-out, str-self-assign) · mutation drill (swap the incref and decref around a store in `own.rs` and predict which of the 68 goldens fail — and which configuration catches it) · exit-quiz (hand-write the refcount instructions for `out @ out + to_str(i)` inside a loop) | /debrief | the author's call, when and how much
- [ ] panel 029 | The ownership pass emitted `store total <- $t1` with no incref into a generic body, and `incref $t2` on the `[A]` beside it. Question: why did the array get counted and `B` not — and what would have happened at `B = str` if monomorphisation had run second? | crates/heroes/src/types/counted.rs:89 · crates/heroes/src/own.rs · docs/panel/029 R1 | "it cannot decide yet" and "it decided wrongly" call for the same fix and are not the same finding
- [ ] open question, author 2026-08-11 | **Do subdirectories ever arrive, and in what spelling?** Today `use geom` reads `geom.hero` beside the file, and `use shapes/geom` / `use geom.shapes` are refused by name. The escape valve is Nim's and it is **additive**: the identifier stays the normal form and a string arrives only for paths that are not identifiers (`import "gfx/3d/somemodule"`), so adding it later breaks nothing and removing it would have. The unresolved half is not the syntax but the **qualifier**: from `use "shapes/geom"`, is the module `geom`, `shapes.geom`, or `shapes/geom`? The ergonomist, writing from the spec alone, **declined to use a subdirectory at all** because it could not predict the answer, and produced `use "geom"` + `use "shapes/geom"` — two basenames, one qualifier, unstated — which the current rule makes unwriteable. Recommendation on the record: decide at **M-package-manager**, where packages create the real pressure (naming a module of another library) and the case that determines the answer finally exists; the separator question is settled by precedent (`/` always in the source, mapped by the compiler — Go, C's `#include`, Python) | docs/panel/031 § the ergonomist's H11 and ct4 · crates/heroes/src/syntax/decl.rs (`module_path_has_no_parts`) | the machinery for a basename collision already exists — it is what refuses `geo_m` beside `geom`
- [ ] panel 035 | The historian's structural condition, recorded rather than acted on: variant construction is unstated **because Heroes is asymmetric** — `.case(field: value)` builds and `.case name` matches, where ML, Haskell, OCaml and Rust use one syntax for both and so cannot omit it. Decide whether v2 restores the symmetry or the spec gains an EBNF appendix in Wirth's manner; either removes the *possibility* of the omission rather than patching this instance | docs/panel/035 § What a veto would compel | it is the only proposal in the session that would stop the class rather than the instance
- [ ] M-ffi-ladder close | Milestone debrief offers, all optional: **walkthrough** (one `extern` from the group's head line to the emitted `#include`, the `_Static_assert`, the unmangled call and the `-l` flag — four artifacts from one declaration) · **ratify the 5 adversarial cases** (`check/ffi-type` · `fixedbugs/ffi-return-type` · `run/ffi-libm` · `run/ffi-cstr` · `run/edges-file-args-exit`) · **mutation drill** (change `HERO_RET_INT`'s `+(c)` back to `(c)` and predict which of the two acceptance programs fails — the answer is the milestone's best finding) · **exit-quiz** (write the `extern` group for three `curl` functions from `curl.h` alone, then compile it) | /debrief | the author's call, when and how much
- [ ] panel 037 | **RATIFICATION OWED — array growth.** Ratify or overturn: the proposal refused (two compiled vetoes) · the **place store** adopted as the shape and **deferred to M-selfhost-probe**, which measures whether anything needs it · design.md §4.10's `join` gap corrected now · the spec clause **queued, not adopted** · `/panel`'s soundness lane. The one to read first is the second: it is a decision to *wait*, and the warden's rule behind it is "soundness without need still waits" | docs/panel/037-array-growth.md § The resolution | the first panel whose proposal was refuted by implementing it rather than by arguing about it
- [ ] panel 037 / spec | **The spec is silent on the cost of building an array, and its one cost sentence gives the wrong answer.** The llm-ergonomist, reading only the spec: `@` is "copy in, copy out", so a literal reading of `push(@xs, x)` is n² — *"the document does not fail to state the cost; it states semantics whose literal reading gives the quadratic answer, and I overrode it on a hunch"*. Its remedy is one clause naming `join`; it **pre-vetoed** any wording that explains copy-on-write, because that makes a line's cost depend on whether some other binding is live fifty lines away. Owed before adoption: the warden's token measurement (it measured the runtime change at 0 and never measured the clause) | docs/panel/037 § The llm-ergonomist answered a different question | the only silent hesitation in a five-program experiment; every other wrong guess failed loudly
- [ ] panel 037 / ABI | **`HERO_RUNTIME_ABI` stayed at 10 across a semantics-only runtime change and every generated unit accepted it** — measured by the ffi-pragmatist while building three runtimes. *"The least-guarded change this ABI has."* Decide whether the stamp should cover behaviour as well as declarations, and how it could | runtime/heroes_runtime.h · docs/panel/037 § Conditions | the guard exists to catch a decoy runtime, and a decoy that changes only what the code *does* walks straight past it
- [ ] panel 038 | **RATIFICATION OWED — constants from headers.** Ratify or overturn: a bodyless `constant` inside an `extern` group · the value never written in Heroes · `DeclKind::Constant` widened rather than a sixth variant · the **mangled accessor** as CLAUDE.md §7's first named exception to unmangled `extern` names · `__builtin_constant_p` as the constancy check · four diagnostics (`ffi_type` extended, `ffi_constant_type`, `ffi_not_constant`, `ffi_unknown_name`) · the spec at **+28** · the `@out: ptr` repair. The one to read first is the **spec-warden's objection, which stands**: the digit class is 267 sites and this reaches 5, and what carries the decision is your instruction, not a verdict | docs/panel/038-constants-from-headers.md § The resolution | the first sitting where a judge's prediction was pre-registered and scored inside the same session — and it did not go the proposal's way
- [ ] panel 038 / harness | **`heroes mutate` does not check that the base program compiles**, so a corpus that does not is reported as a perfect defence. Measured: pointed at `crates/heroes/src/library/`, it reported **7 sites, 7 killed (100%)** — and `library/source.hero` does not type-check standalone at all (`error[builtin_name_taken]: 'range' is a built-in`, because the library *is* the built-ins). Every mutant was killed by a diagnostic that was already there. Decide whether the fix is a refusal (exit 2, "this corpus does not compile") or a reported exclusion | crates/heroes/src/mutate/mod.rs · docs/panel/038 § The digit class | metric 3's one number that can be wrong in the flattering direction, and it was found by a scaffold rather than by the harness
- [ ] panel 038 / §11 | **`emit/decls.rs` is 768 lines and CLAUDE.md §11 says ~300.** Panel 038's compiler-engineer predicted exactly this and it scored correct: 717 before M-header-constants, 768 after, so the breach predates the milestone and the milestone widened it. It is not one concern either — the `#include` walk, the `link` walk, the assertion emitter, the prototype, the definition, the `main` shim, the string table and the reachability cull all live there. The cut that suggests itself is `emit/externs.rs` (the three group walks plus `extern_assertions`) and `emit/main.rs` (the shim and the test runner), which is the same cut `syntax/externs.rs` already made at panel 036. **Decide: now, or at M-selfhost-probe when the port reads it?** — a split touches every caller, and doing it inside a milestone that is also changing behaviour is how a mechanical change hides a real one | crates/heroes/src/emit/decls.rs · docs/panel/038 § Predictions to score | a judge predicted the file would end over 750 unsplit and was right, which is the second time a line budget has been the thing a panel got exactly right
