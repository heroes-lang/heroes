2026-09-14 | **The mark is written and never inferred, it names what ends the life, and the more robust option turned out to be the cheaper one** (M-marked-acquisition step 3, panel 148, five seats plus the completeness critic; convened without asking because `spec/` is a trigger and the author had asked for the milestone to be carried to its conclusion). **Option C — infer the obligation from the group's own `consumes` function — is REFUSED unanimously, on five independent measurements and not one of them a matter of taste.** The ffi seat opened **nineteen real headers** and asked whether one header declares both a releaser for handle type `T` and a function handing back a **borrowed** `T`: **twelve of nineteen**, eight of the nine the brief named. The pair that settles it alone is two adjacent lines of `openssl/ssl.h` — `SSL_get0_peer_certificate` and `SSL_get1_peer_certificate`, identical to clang, opposite in ownership, and a probe taking both through **one** function type compiles with zero warnings under `-Weverything`. Then it stopped arguing and **reproduced the corruption twice against real libraries**: `AddressSanitizer: attempting free on address which was not malloc()-ed` inside `sqlite3VdbeDelete`, and a SEGV inside `curl_slist_free_all`. **Under the written mark a mistake loses a leak check; under C a mistake COMPELS a release the program must not make** (§1.12, CLAUDE.md § Precedence rank 3). The engineer found C is panel 147's R1 in a group-shaped hat and that it **has no off switch** — the only way to disarm it for `sqlite3_db_handle` is to delete `consumes` from `sqlite3_close`, which also deletes panel 145's use-after-free rule for that type. The ergonomist, reading three label-stripped specs and nothing else, found the same hole **blind**: under C the `extern` group is byte-identical to today's, so no line says which calls acquire, and of four escapes from a borrowing accessor the one the language steers you to is a **double free**. The historian found the purest shipped instance of C — Clang ARC, which infers ownership from the method name — and what it cost: it **never removed the annotation**, it relocated it to the exceptions and then paid four attributes, a fifth for family control, a namespace prohibition (*"you cannot give an accessor a name that begins with `new`"*) and a warning class, with Apple's own `CFBase.h` comment begging you to rename your functions instead | **What lands is the historian's option, which no seat could measure and which turned out to be the cheapest correct text on the table.** `acquires <releaser>` — the mark names the function that ends the life — is the shape this repository **already ships** as `@error: cstr owned sqlite3_free` and the shape GCC ships as `__attribute__((malloc (fclose, 1)))`. Measured by the coordinator after four of five reports were in: **+66 vendored, against +70 for the bare word once the warden's two repairs are applied**. So § 4's rule — the most robust resolution, never the cheapest — and the budget point the same way for once. Its parser exists: `owned_marker` reads that exact shape in both positions today. **The bare word needed two repairs and the warden found both**: its grammar did not derive its own example, `-> Curl acquires` being underivable because only `CParam` was amended (the shape panel 133 caught three days earlier, in the same document), and it duplicated a clause the spec already carries for `lease`, where merging beats appending. **The critic found the grammar was also too WIDE** — `@` is optional in `CParam`, so a mark on a plain input parameter is derivable and the prose does not define it, a looseness `consumes` carries today | **The critic changed the resolution, and the thing it changed was a cost everybody had accepted.** The engineer priced a group-keyed rule at **+90 lines over a key the language does not define**, and every line of that reproduces — `parse/group.hero` says nothing after the parser knows the word *group*, and the compiler's own source holds two `extern "hero_os.h"` groups and, the critic added, two `extern "stdlib.h"` groups. **But the compiler had already solved it**: `selfhost/check/decls.hero`'s `one_tag_one_type` is **17 lines**, program-wide, and its own comment is the answer to the hinge, written before the sitting convened — *the walk is over the declarations rather than the groups because a group is flattened in the parser and no later pass knows the word*. **One tag is one handle type across the whole program**, proven by probe. So a completeness question keyed on the **TYPE** needs no group identity and no new table, and the sentence that was about to be carried as C's cost would have killed the two better options with it. C dies on correctness regardless of the key. **The critic also collapsed three proposed third options into two axes**: the ergonomist's diagnostic and the ffi seat's `borrows` are one axis where `borrows` dominates — `curl_easy_init` and `curl_easy_duphandle` are two producers in one group, so a per-type check is satisfiable while an unmarked producer leaks — and the historian's named releaser is orthogonal to both. **The maximal resolution is both, and no seat proposed it**; it is R2 plus R5 | **Three procedure findings, each about the sitting rather than its subject.** **The timing is discarded**: the engineer reported +1.2% and declared the machine still, while the five seats ran **concurrently** — the ffi seat linking under ASan, the warden running `measure`. CL-025 is unambiguous, and the consequence is general: **a panel that runs its seats in parallel cannot produce a valid timing from any of them**, so a seat that times must be serialised or no timing asked for. **The blind is not enforced and the critic proved it of its own prompt**: the ergonomist disclosed unprompted that a commit subject in its environment named the refused variant, and the critic confirmed the harness injects that block into every subagent along with **the whole of `CLAUDE.md`**, which that seat's own definition forbids it. Its findings survive — the critic re-checked each against the compiler it never opened and each holds — but its agreement with two other seats is **counted once and not three times**. **And nobody judged the budget the mark actually breaches**: *`ast.hero` 505 → 508* reads as a before-and-after and is not, 505 being the DECIDED ceiling against today's 503, so landing the mark moves two ceilings in `suite_layout.hero`'s table. The warden judged the spec budget; the layout budget went unexamined by five seats | **And the sitting settled a question it was not asked, by accident, which is the finding to carry forward.** Both drafts ended *"aborts when `main` returns"*, so **a compile-error instrument was never on any seat's page**, the comprehension veto included. R6 returns it to the milestone undecided, with what the critic says it must answer first: every draft amends `Member` and `CParam`, so the mark is **extern-only**, while this corpus acquires inside **Heroes wrappers** one module away — `opened` with 7 call sites and `prepared` with 6, because §4.19 refuses an `extern` call across a module boundary. A runtime counter does not care where the value goes next. **A compile error would need somewhere to write the fact on an ordinary function, and no text on the table has room for it.** One prediction was scored inside the sitting: the historian estimated its own option at *"more than A's +64"* and it measured **+66** — held in sign, and four tokens under the corrected bare word it was being compared against | design.md §1.7, §1.11, §1.12, §4.17, §4.19, Part 6; `spec/heroes-spec.md` § 13; CLAUDE.md § 4, § 11, § 12, § Precedence rank 3; CL-025, CL-036, CL-040, CL-057; `docs/measurements/031`, `032`; panels 133, 145, 147, 148 | 148
