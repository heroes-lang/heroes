2026-09-14 | **The full net before the push came back 1813 passed and 6 failed, over a milestone that had gated every sub-step, and the two roots were worth more than the six lines** (M-marked-acquisition, after the close commit and the tag). **Root one is a rule that invalidated a case written one step earlier.** `check/acquiring.hero` landed at step 5 and refuses an unmarked producer of a consumed handle type; `tests/golden/run/abort-handle-given-back-unmarked.hero` was written at step 4 in exactly that shape, to prove the runtime counter's NEGATIVE direction. It stopped compiling, so it could no longer reach the runtime at all, and `run`, `emission` and `determinism` all went red on one file. **Root two is three emissions owed a blessing**, one new program and two examples whose `#line` directives moved when the corpus gained its teaching comments — proved to be nothing else by comparing the two emissions as multisets: nothing was REMOVED from either, and the only additions are five `hero_handle_acquired()` / `hero_handle_consumed()` calls in the ledger and eleven in sqlite, which is the milestone's own feature appearing in the C | **And under root one sat a worse defect than the case that revealed it: the counter's negative message named one cause of three, and not the worst.** It read *a `consumes` in this program's bindings has no `acquires` to pair it, so the call that hands the handle over is unmarked*, which was the only cause there was when it was written and stopped being so one step later. **Three programs were run rather than reasoned about**, all three arriving at exit 134 with that sentence: the same handle given back TWICE, which is a double release and was being reported as a missing annotation — sending its reader to edit a declaration while the program corrupts memory; an `extern` marked `borrows` whose C function in fact hands ownership over, reached directly and through an ordinary Heroes function, where the mark is a lie no rule here can catch; and a handle arriving inside a record FIELD. The message now states what it measured and names all three, worst first, and **says why it cannot tell them apart** — it is a counter and not a set, so distinguishing them needs the identity of every live handle and a lookup at every call site, which is a different instrument rather than a better sentence | **Three golden cases where there was one, one per cause, and they are what keeps the three-cause claim honest**: `abort-handle-given-back-twice`, `abort-handle-borrows-that-gives-away`, and `abort-handle-given-back-unmarked` rewritten onto the record-field shape, which is the only one that still reaches the runtime. If a later rule catches one of them at compile time, that case goes red and the message is what must change — which is exactly how the old one was caught | **Defect 033 is FILED and not fixed, and the reason is the contract's.** `unmarked_handle_producer` reads results and `@` out-parameters and does not look inside a returned record, so `pair_make(n: i64) -> Pair` with a handle in a field compiles clean, prints, and aborts at exit. It is a real C shape — `struct addrinfo` carries pointers a caller must free. Widening what a diagnostic refuses is a diagnostic CLASS and CLAUDE.md § 4 sends it to the panel, and the question for that sitting is not *should the rule see fields* but **how deep**: behind two records, behind an optional, behind a list. The cheap answer stops at one level and would be a premise about the world. **What holds meanwhile is written down rather than assumed**: nothing is silent, the counter catches it at exit, and the message names that shape by name | **And the process defect under all six is the one to keep.** `.claude/rules/verification.md`'s `selfhost/**` row names `canonical` `layout` `order` `records` and the compiler's own tests, and **not one of those compiles a golden program** — so a change to what the CHECKER REFUSES can invalidate any program already in the repository and no gate will say so. The row added is narrow on purpose, because CL-063 refuses *run the full net for every compiler change* on cost: the trigger is a new diagnostic, or an existing one firing where it did not, and then every golden tree judges it. The map was not wrong in 2026-09-09 — it was right about which suite reads which DIRECTORY, and this is the case where the directory is not what decides, because a refusal reaches further than the file it is written in | **Two numbers corrected rather than carried.** § Where we are had written `run` **116** and `emission` **462**, the counts of what PASSED while one case in each was failing, and said the net's figure would be written at the push; a passing count is not a suite's count while anything in it is red. The true figures after the repair are `run` **119**, `lines` **120**, `warnings` **179**, `determinism` **148**, `emission` **468**. And `suite_emission.hero`'s floor was re-based from 225 to **233** — emit 6 · fixedbugs 30 · ir 23 · run 119 and 55 examples — having drifted 8 in two days, on that file's own rule that slack in a floor is a case that may stop running | CLAUDE.md § 4, § 8, § 12, § Precedence rank 3, § RUN IT, § Verification; design.md §4.17, §4.19; CL-004, CL-036, CL-054, CL-057, CL-061, CL-063; `.claude/rules/verification.md`, `.claude/rules/module-shape.md`; `runtime/parts/alloc.c`; `selfhost/check/acquiring.hero`; defect 033; panel 148 R2 and R5 | none
