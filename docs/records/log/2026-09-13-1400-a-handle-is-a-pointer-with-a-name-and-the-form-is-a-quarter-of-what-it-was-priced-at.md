2026-09-13 | **A handle is a pointer with a name, defect 029 is closed, and the form is a quarter of what it was priced at** (M-handle-verdict step 4, on panel 145 as the author ratified and then narrowed it). **A fieldless `record` inside an `extern` group with a `tag` and without `partial` is a handle: C's pointer to what the header names and leaves opaque.** `record Db tag sqlite3` is `sqlite3 *`, the tag **verbatim** and never `struct sqlite3 *` — the critic measured that `curl/curl.h` reads `typedef void CURL;` on this Mac's SDK, so the spelling every seat had proposed reached one of the three handle types in the tree. `nullptr` is its null, construction is refused, `==` compares the address, and **a handle does not convert to `ptr`**, which is the clause the author struck because implementing it would have re-opened the class through every function still declared `ptr`. **The reproducer that opened the defect no longer builds**: `sqlite3_step(db)` in `examples/sqlite/main.hero` is `error[type_mismatch]: expected Stmt, found Db` on its own line, and the unchanged example prints `rows: 3` / `longest: 6` byte-identical to before | **The measurement the step is judged by, and it is why the instrument was built first.** `heroes mutate --operator swap-ptr` read **15 mutants, 8 killed, 53%** before the form — and **0 of the 7 that were the class**, the eight dying on `unused_binding` and `aliased_mutable_arguments`, rules that fire on the shape of the edit and know nothing about handles (`docs/measurements/029`). It now reads **15 of 15**, and the honest half **7 of 7**. **The denominator did not move**, which is what makes the two comparable: the operator learned the form in the same commit, so a `Db`-typed name is a site exactly as a `ptr`-typed one was. Had it not, retyping the bindings would have hidden every site and the form would have been scored against silence — the failure the measurement's own prediction named, and the prediction is scored here as HELD in both halves | **The form is ~170 lines against the ~203 the scouts priced and the 60-90 the engineer estimated, with two ceilings untouched.** What landed: **`selfhost/handles.hero`**, the predicate written ONCE and called by the parser, the checker, the emitter and `mutate`, because five scouts had proposed four definitions in four homes and CLAUDE.md's opening rule is that each rule is written in exactly one place; the form in `parse/tails.hero`; the spelling in `emit/ctype.hero`, whose comment was cut to a citation rather than raising a `DECIDED` row it would have breached by 4; `nullptr` adopting a handle in `check/contextual.hero`, threaded through three call sites in `walk.hero`; and **`const_pointer`**, because `const T *` qualifies the POINTEE when T is itself a pointer and clang refused `&db` at every generated `_eq`, in the descriptor's copy and at the prototype — five sites, one function. **Zero new `Ty` cases and `check/table.hero` untouched**, which is the compiler-engineer's own prediction at panel 145, held. `ffi_errors.hero` crossed §11's 300 and was not split at a line count: the two new diagnostics are about handles, so they moved to the module that owns that question, which took it back to 268 | **Two refusals rather than implementations, and a third refused on evidence.** A handle as a **map key** — directly or through a record that holds one, with the dotted path naming the field — because a handle is an address C owns and C recycles an address, so a value stored under a closed handle is found again by a fresh one; panel 145 says `hash` zero times, so the language refuses the shape rather than choosing an answer nobody argued. **Two records sharing one tag**, because they are two Heroes types and one C type, so this compiler refuses handing one where the other belongs and clang accepts it: the mutant survives every instrument the language has, permanently. **And the third proposed refusal was withdrawn before it was written**: a `tag` naming a type the header declares COMPLETE was to be refused, until `record File tag FILE` was compiled and run — `FILE` is complete on this SDK and the binding writes through `FILE *` correctly. The fieldless form MEANS *the pointer*, so completeness of the tag is irrelevant, and refusing it would have broken a legitimate binding. The critic's framing assumed the author wanted the struct; the form's own meaning says otherwise | **The specification, and the fence is the half that decides it.** § 13's `record` paragraph gains five lines and **§ 13's fenced example is rewritten to use the form** — panel 145's llm-ergonomist found that from the prose and the spec-warden from §1.2, independently: a form the document does not SHOW is a form a model never writes, and the old fence taught `@out: ptr` and `sqlite3_close(db: ptr)`, which would have re-opened 029 for exactly the two functions it teaches. **+76 vendored net**, 5722 → 5798, with § 13's package-reason clause removed at −17 measured alone; real 7610 → **7721** on the authorised refresh, digest `2b0304cb09af0f99`, 394 free. And `suite_special` runs that fence as a program, so its body moved with it: the spec's example is a program and not a picture | **What this step still owes, said rather than implied**: the two repairs panel 145 charged to it and this commit does not carry — `emit/ffi_tag.hero` reading clang's *incomplete definition* as *the tag is absent*, and `check/freer.hero` retyping any `owned` parameter to `str?` so a `ptr` gets two diagnostics about a string. Both are filed, neither is done, and saying so is cheaper than a half-done repair. **Defect 031 is the one still open**, and its prototype is step 5 | design.md §1.7, §4.19, Part 5; `spec/heroes-spec.md` § 13; CLAUDE.md §1, §12, § RUN IT; CL-017, CL-036, CL-061; panels 109, 135, 139, 145 | 145 (109, 135, 139 inherited)
