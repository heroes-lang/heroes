2026-09-06 | **The sitting was convened on a silence that was a ruling, and the brief carried two other false claims** (panel 114 § What the brief got wrong). CLAUDE.md §1 says to grep `DESIGN-LOG.md` and `docs/panel/` for the thing that is NOT there before convening on something the documents do not say. The convener did not, and *"Heroes has no conditional compilation"* is the artifact of three rulings — `DESIGN-LOG.md:407`, `:455`, `:485` with panel 097 — one of which `selfhost/cli/io.hero:22` states in the repository's own words: *"The route out is not a platform arm — this language has no `#if`, by design."* The other two: the brief told five judges that CI compares `seed/heroes.c` **on every leg**, and `ci.yml:604` reads `if: runner.os == 'Linux'` — one leg; and it told them panel 049's return condition had opened the door, when condition 6 unblocks the WORD `windows` and not the feature, the veto's ground being §1.0 and §1.3 which it does not touch. All three were caught by seats, two of them by the judge who wrote the veto being misquoted | §1.0, §1.3, §11 | 049, 114
