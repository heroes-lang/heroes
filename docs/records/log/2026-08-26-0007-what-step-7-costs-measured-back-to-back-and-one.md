2026-08-26 | **What step 7 costs, measured back to back — and one thing it was believed to save that it does not** (M-separate-compilation step 7, third entry; four timings taken within fifteen minutes on one machine and one source tree, the pre-step-7 binary rebuilt from the committed seed as the control). Per-module self-build of the compiler: **cold, from an empty `build/`, 142.29 s before and 145.53 s after — +3.2 s, +2.3%**; **fully warm, nothing edited, 133.08 s before and 132.68 s after — unchanged**. So the freshness check, which is the half that runs on every warm build, costs nothing measurable, and the cold build pays about three seconds for 157 depfile writes plus one pass over the 797 KB of headers a TU reaches. The author's rule is that a change must not slow the compiler down; this is a **measured 2.3% on cold only**, spent to stop a build that returns a wrong answer at exit 0, which §12 puts ahead of speed by name. **And the correction: taking `runtime_text` out of the per-TU loop does NOT show up in wall clock.** It removes 157 shell `cat` invocations and 25 MB of hashing per build — that count is real and re-measured — and the two warm numbers are the same to within 0.4 s, so the work removed was not where the seconds were. It stays because it is less work for the same answer, not because it bought time. **A third measurement, recorded because a decision rests on it**: `SCHEDULED.md`'s open item on per-module slowness carried **cold 261 s / warm 250 s** from 2026-08-25, and those are **not reproducible on today's tree** — the same command with the pre-step-7 binary gives 142 s / 133 s. The old pair is left in the record with its date (§14) and the item now carries both, because the author's decision on that item was about to be taken on numbers 1.9× too large. No cause is asserted for the difference: none was measured. | a change is not fast because the work it removed was large, and yesterday's number is not today's; both halves of that cost a measurement each and both were run | §3.1, §12, §13 | —
