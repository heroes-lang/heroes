2026-09-13 | **The seed was a commit behind `selfhost/` for four hours, and the condition that catches it is the one a green build cannot see** (found by running the suite this step had no reason to touch, CL-054). `2d905817`, *M-declared-thresholds step 3*, rewrote `selfhost/cli/measure.hero` and `selfhost/measure/` at 23:11 on 2026-09-12 and **did not regenerate `seed/heroes.c`**, which had last been written at 21:20 by `7d31838a`. `seed/README.md` § When this file must be regenerated names three forcing conditions, and this is **the third**: *"anything else `selfhost/` says changes what the compiler DOES … the condition the first two miss and the one that bites"*. The ABI stamp did not move and no new form entered the grammar, so the stale seed went on building in 2.9 s and the compiler it made carried **yesterday's `measure`** — judging `CLAUDE.md` against the 5500-token vendored ceiling panel 134 had already replaced, printing `BREACH: 5570 over the 5500-token ceiling` and **exiting 1**, where the same command from a `selfhost/`-built compiler prints `real 7402` against the contract's own 12288 and exits 0. **Measured both ways before anything was touched**: seed-built exit 1, `selfhost/`-built exit 0, same file, same working directory. Three checks of the `spec` suite were red on this tree — `spec/contract`, `spec/spendable` and `spec/real` — so the suite stood at **12 passed, 3 failed**, and it is the suite that no step of M-deferral-ledger has any reason to run, since the ledger writes design.md and never the specification. **The repair is the one command `seed/README.md` gives**, `heroes build selfhost/main.hero --emit-c -o seed/heroes.c`, run from the `selfhost/`-built compiler in 40.6 s: the file goes from 794,507 to 796,427 lines, and **222,634 of the 230,570 changed lines are `#line` directives** shifting because the measure modules changed size, with the substantive remainder being the new `measure`'s own strings. **The fixpoint was then verified rather than assumed**: the compiler built from the new seed re-emits `seed/heroes.c` **byte-identical**, `cmp` silent. After it, `spec` is **15 passed, 0 failed** and `measure CLAUDE.md` exits 0 | **What this cost and what it says about the instrument.** Nothing was broken in the compiler and nothing was wrong in the commit that caused it: `2d905817` is correct Heroes and its own suites were green, because a session that builds from `selfhost/` tests the compiler it just built and not the one the repository ships. That is the failure `docs/records/journal/051-reflection-verdict.md` records happening **twice in one milestone**, and `seed/README.md` says in its own words that conditions 1 and 2 exist to make forgetting loud and *"this one is why you should not rely on them"*. **The rule of thumb needs no instrument and was not applied**: if the diff touches `selfhost/`, the seed is regenerated in the same commit. What found it here was neither the rule nor CI — it was CLAUDE.md § Verification's *run the suite you did not expect to move, after the last edit rather than after the last interesting one*, run on a step that had touched only `docs/`. CI's own instrument, the `cmp` on the Linux leg of every push, would have caught it at the next push; between 23:11 and this repair it had not been pushed, so the public branch was never red — **that is luck rather than design, and it is written down as luck**. The four hours are `git log`'s, not an estimate | CLAUDE.md §7, §14, § RUN IT, § Verification; `seed/README.md` § When this file must be regenerated (condition 3), § If the seed is already broken; CL-054, CL-063 | —
