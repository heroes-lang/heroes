2026-08-19 | **The spec's own gate moves into the net — 7 checks, green against both compilers** (M-bootstrap-archive step 3). `measure/gate.rs` and `measure/spec.rs` are 435 lines and every check in them is `cfg(test)`, so the budget, the built-in inventory and the reserved-word rule would have died leaving nothing: the spec measures 3512 and the registry 916 or the number moves in a commit that says why; the spec names every built-in the compiler reserves and offers none it lacks; no word the lexer refuses appears in the spec's code; the built-in list still ends at `range` | **The compiler's side is read out of the compiler's own source, not copied** — `builtins.hero`'s 37 rows and `keywords.hero`'s 23 match-arm words, each extraction floored, because an extractor reading the wrong shape reads nothing and turns two checks into decorations. Three of those arms carry several words (`"null" | "nil" | "None"`), which a one-per-line reader would have missed. Every check has a test that makes it fire (CLAUDE.md §9): five failure injections and three stub compilers. **What broke**: the first draft walked bytes and sliced them one at a time — the obvious port of `char_indices` — and died on the spec's own em dash, `panic: string slice splits a character`. The abort is the language keeping a promise; the module now takes `[str]` from `chars()` | tests/harness/{suite_spec,spec_text}.hero · CLAUDE.md §9 | 085
