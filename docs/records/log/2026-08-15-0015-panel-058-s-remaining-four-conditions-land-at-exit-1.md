2026-08-15 | **Panel 058's remaining four conditions land: `ffi_writable_parameter` at exit 1, the spec at +2, and the wart named in Part 8** (author instruction, `/decide`). `-Werror=incompatible-pointer-types-discards-qualifiers` joins `FLAGS` as the twelfth, `emit/ffi_mutable.rs` is the **fifth** exit-1 class under panel 048's `declaration()` narrowing, and the hardest case works: `extern function strtok(s: cstr, sep: cstr)` **declared and never called** is now exit 1 on the author's own line rather than exit 2 with generated C — which it is only because condition 1's bare `0` had already stopped the emitter tripping over its own `_Static_assert` | **the diagnostic's repair is not "add `const`"**, which is the header's to choose and not this program's: it is *"declare it `ptr` and give it a buffer C owns"*, and the parameter's declared type is read **from the value** rather than asserted to be `cstr` (CLAUDE.md §11). The spec takes the warden's two-word rewrite — `s.cstr()` **lends** a `str` to C **to read** — ledger row `3210`, and the two clauses that were on the ballot at +32 and +55 are not taken, the second because it *licenses* the pointer C keeps. Part 8 gains item 17 with the falsifier §12 requires, and it is a **wart rather than a Part 6 row** on purpose: three repairs were priced and refused, so what is left is a real cost that is named instead of presented as a design. **What would make the row wrong**: a binding the closure list or §4.19's ladder needs that requires a buffer whose *length Heroes knows* — `malloc` + `ptr` returns none, so `snprintf`'s truncation and `readlink`'s no-NUL result have no correct spelling; one on the critical path and the mutable-buffer type stops being a Part 7 wish. Measured against that bar today: the closure list's only outbound `cstr` are `hero_file_read` and `hero_file_write`, both `const char *` | design.md §1.12, §4.19, §4.20, Part 8 · CLAUDE.md §7, §11, §12 | 058 |
