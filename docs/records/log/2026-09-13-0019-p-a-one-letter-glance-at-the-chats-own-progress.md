2026-09-13 | **`/p` is a one-letter glance at how far this chat's own work is, and the answer follows the chat's language** (author instruction 2026-09-12, given in four turns as the shape settled: a command for the running session and not for the roadmap, which `/where` already is; in the repository rather than at user level, so it travels with git between machines; one letter, the fastest thing to type; then the periodic form the same evening, `/p 60` for a glance every sixty seconds, `/p 0` for once). It lives at `.claude/skills/p/SKILL.md` and reads nothing but the conversation: the plan the chat set for itself, counted rather than felt — done is finished AND verified, in progress counts as not done and is named, the percentage is done over total and never rounded up — in at most eight lines, every label rendered in the language the chat is held in, Italian today, because the file itself is English as CLAUDE.md § 11 asks and `suite_records.hero`'s `english` check walks `.claude/skills/` with one exemption that is not this file | **The periodic form is a standing rule for the session and not a scheduled loop, on a documented fact**: a scheduled prompt fires between turns and never inside one, and a long `/step` chain is one turn, so after each tool call the session reads the clock with one `date +%s` and speaks when N seconds have passed, CL-034 and CL-045 with N chosen by the author; a loop inside `/p N` was considered and refused because it could not fire during the work and would repeat itself while idle, and `/loop 60s /p` already exists for a heartbeat across turns. **The CLAUDE.md sentence on the skills gains nine words, +14 vendored tokens measured by `heroes measure` on a compiler rebuilt this session**: 5556 to 5570 against a `DELTA_GATE` of 50 with the pin untouched, the `real` row 7402 unchanged against 12288, exit 0 — the first amendment judged under panel 134's gate, and it fits without a refresh, which is what the gate was built to allow. The `heroes` binary in the tree was from 21:47 and step 3 had rewritten `measure` at 23:11, so the first reading this session took came from a stale instrument and was discarded; the number above is from the rebuild, 50.4 s | CLAUDE.md § 3, § 11, § RUN IT, § Where the rest lives; CL-034, CL-038, CL-045 | — |
