2026-09-05 | **Panel 110: two of four spec silences bought, one refused by two vetoes, one measured out of existence — and the sitting corrected three of the brief's own claims** (full panel, five seats, sat 2026-09-04, ratified 2026-09-05 by the standing delegation quoted on the page; `docs/panel/110-places-not-roots-and-the-repair-that-printed-a-third-answer.md`). Spec **3750 → 3799, +49**, headroom **297** — 26 tokens below the tabled proposal, and one token inside the spec-warden's own registered bound. **What landed**: a `()` row (*nothing: what a function with no `->` returns; `ok()` is the `()?`*) plus, at `spec:100`, *which a `()` line refuses: it stands alone*; and the permissive half of the `constant` body rule, subject the **written body**. The row's warrant is not the missing row: the unit type appears in the document **once**, `ok(())` is refused because `()` is not an expression, and **`return ok()` argument-less is the only way to build a `()?`** while `spec:146` shows only `ok(v)` — so a reader with the document alone could not write a function of the shape `spec:191` displays, and `ok()` appears **19 times in `selfhost/` and 62 across 587 `.hero` files**. **What did not land**: *"`T??` is a compile error"*, vetoed twice and false as tabled — `m: {str: i64?}` is accepted, `v = m["a"]` then `v.must().must()` prints 1, a **generic** signature names the type and nests without bound, and the §1.11 built-in `find` over `[i64?]` produces it, while `spec:157` promises `V?` unqualified; docketed at M-check-completeness, which `docs/ROADMAP.md:363` recorded as the only scheduled row **with no warrant** and which now has one. And *"refuse a multi-line parameter list"* did not land because **the premise was false**: all three shapes `check` and `run` at exit 0 and `heroes fmt` joins them, measured three times independently, so the resolution is accepted input with non-canonical output. **Three of the brief's five claims were wrong about the compiler and every one was caught by a seat** — CLAUDE.md §1's inference-as-measurement in three fresh costumes, and the reason the full lane was right. The declined payment is on the record: **−19** by deleting `spec:176`'s quadratic-concatenation cost model, refused because §15's *never slow the compiler* is a standing instruction in capitals and the spec is that fact's only carrier, a concat loop drawing no diagnostic at all.
