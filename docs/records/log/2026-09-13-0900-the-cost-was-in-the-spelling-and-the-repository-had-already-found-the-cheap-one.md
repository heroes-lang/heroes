2026-09-13 | **The cost is in the spelling, the repository had already found the cheap one and forgotten it, and the last item of the ledger closes** (panel 144, M-deferral-ledger step 9, five seats plus a completeness critic; `docs/panel/144-the-cost-is-in-the-spelling-and-nobody-had-written-the-cheap-one.md`). The ledger's second item was Part 8 warts **5**, **8** and **11** plus **coverage**; 5 and 11 carried rulings already, so the sitting owed 8 and coverage — **and found that the two it thought were settled are not**. **Wart 8 STAYS A WART, rewritten, and what it names changes.** Medians of three runs, `/usr/bin/time -p`, `user` read: **`parts @ parts.push("x")` on a bare local, 1 000 000 in 0.02 s**; **`w.parts @ w.parts.push("x")` through a field, 50 000 in 11.51 s**; **`put(@w.parts, "x")`, the field lent to an `@` parameter, 1 000 000 in 0.02 s**; hoisted into a local and stored back once, 200 000 in 0.00 s. **Twenty times the work in five hundred times less time, between two spellings of one operation** — so the entry's *"and so is the `[str]` you build"* is false for two of three spellings and true for the third, and **the variable is the spelling rather than the shape**. The mechanical witness is `--emit-c | grep -c hero_array_push_owned`: **1** lent, **1** hoisted, **0** in-field, because `selfhost/ir/place_store.hero:27-31` fires only on an empty path and `@w.parts` resolves the path **at the call**. The repair the entry never absorbed landed in **`aacd2dbd`, 2026-08-24** (*"944.76 s to 188.51 s"*) and §4.10 has recorded it ever since while Part 8 went on quoting panel 037's 806 s — **design.md contradicting itself for twenty days**. **And this repository had already found the cheap spelling and forgotten it**: `docs/records/log/2026-08-26-0001-…` reached the compiler's own arena by changing the call shape to `push_expr(@exprs: [Expr], node)`, letter for letter the lent-field form, and `heroes check` went **191 s to 88 s**. The reachability count nobody had taken: `grep -rn quadratic selfhost/` returns **6 hits, all comments**, six places where this compiler's own authors hit the cliff and worked around it by hand. The **`Builder` promise is struck** on the compiler-engineer's veto — `join` shipped and is Tier 1, the largest Heroes program accumulates through a 267-line `Writer` record of ordinary Heroes, and a builtin `Builder` is **core** by §1.7's own test at 500-800 lines across six modules, to replace a record a user can write | **Coverage is REFUSED as a Part 6 row, the first there whose subject is a tool**, and the column head is widened to *Feature or tool* in the same edit so the precedent breaks loudly rather than silently — the completeness critic caught that all **31** existing subjects are language or source forms and that the tool-row precedent a seat claimed does not exist, `heroes doc` appearing only in a *reason* column. The reason taken is the **ffi-pragmatist's, which is the strongest and one no other language can make**: instrumented rather than argued, a real SQLite binding compiled with `-fprofile-instr-generate -fcoverage-mapping` against real `-lsqlite3` executed eight entry points, prepared and stepped a 65 KB statement and bound 65 532 values, and **`sqlite3` appears 0 times in the `llvm-cov` report** while **all 8 `hero_ffi_probe_*` functions read 0.00%** by construction — so on a §1.11 program the number **measures the glue and reports it as the program**. `heroes mutate` reaches where coverage cannot: 79 mutants over the two pure bindings, 66 killed, **not one survivor at a C call site**, **44 of 44** one-character edits to `extern` names dead at compile time. **The ledger's own falsifier half-fired**: `selfhost/mutate/edits.hero:322-323` returns `""` for every arithmetic and relational operator, so `i < n` → `i <= n` is unreachable by mutation — but it sits on a line that ran in a test that passed, so coverage is blind to it too, and it argues for a **sixteenth operator** rather than a tool | **Two neighbours in the same ledger item were stale, and both are corrected.** Wart 5's *"0 of 25"* re-runs as **59 mutants, 0 killed, 0% over 120 programs** — the rate survives exactly, the magnitude is **2.4× stale**. And **warts 11 and 15 contradict each other, both dated 2026-09-13, both written by this milestone**: :3239 said *"all 22 files holding an `extern`"*, :3296 says *"the 24 files that declare an `extern`"*, and measured today **neither method reproduces 22** — `grep -rl` counts 24, `grep -rlE '^extern'` counts 21. CL-017 inside the milestone's own work | **The specification owes a correction that this session may not apply, and the reason is a hard stop.** § 10's *"grows in place while nothing else holds `xs`"* is **true and insufficient**, which is worse than silence: nothing else holds `w.parts` and it is 500× slower. **Two seats reached that sentence from disjoint inputs** — the spec-warden from the measurements, the llm-ergonomist from the prose alone, which named it the one hesitation whose wrong guess produces *a silently different program*. The replacement is drafted and **measured at +35 vendored** (5655 → 5690), its `real` delta **estimated** at ~+47 and said to be an estimate. It does not land because `tests/harness/suite_spec.hero:158` pins a content-addressed `SPEC_DIGEST` and `measure` **withholds its verdict** when it disagrees, clearable only by `heroes measure spec/heroes-spec.md --refresh`, which reaches the network. **The correction is adopted and queued, not applied** — and it shares that single key with defects **030** and **031**, which name § 3 and § 13 of the same document | **Three findings about the sitting itself.** The critic **falsified the compiler-engineer's own generalisation**: it measured `put(@w, …)`, the whole record lent with the path step still inside, got quadratic correctly, and concluded *"The `@` parameter is not the cost. The field path is."* — where `put(@w.parts, …)` is linear. The measurement stands and the sentence drawn from it does not. The **spec-warden cited a subtraction as a quotation**, *"103 s of `heroes check`"* against a file whose `grep -c 103` returns 0 and which says 191 s to 88 s. And **the critic itself miscounted**, reporting five `quadratic` comments in `selfhost/` where the command returns six — recorded rather than smoothed, because the rule binds the seat that enforces it | **Two doors this sitting could not close, both open since panel 143.** The **historian still has no file write**, so its report reaches the coordinator through the run's journal and never the sitting's directory, and **the critic could not audit it** — the same gap, one sitting later, now twice on the record. And **the llm-ergonomist's isolation is not enforced by the machinery that convenes it**: the seat reported that `.claude/rules/spec-shape.md` and `.claude/rules/verification.md` were loaded into its context unasked, refused them as evidence and rested no sentence on either. The seat whose entire value is reading **only** the specification cannot be guaranteed that | design.md §1.2, §1.6, §1.7, §1.11, §1.12, §2, §4.10, §4.19, Part 6, Part 8 warts 5, 8, 11, 15; CLAUDE.md §4, §10, §12, § Hard stops; CL-017, CL-025, CL-057, CL-061; panel 037, panel 088, panel 143 | 144 (037, 088 inherited)
