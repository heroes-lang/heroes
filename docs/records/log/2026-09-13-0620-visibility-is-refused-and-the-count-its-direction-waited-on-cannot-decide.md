2026-09-13 | **Declaration visibility is refused, and the count its direction had been left to is closed by measuring that it cannot decide** (panel 141, M-deferral-ledger step 6, five seats plus a completeness critic; `docs/panel/141-the-premise-expired-the-ancestry-was-for-another-form-and-the-count-cannot-decide.md`). The ledger's sixth verdict, on the best-documented item in Part 7 — the only one a sitting had already costed — and the one whose failure shape is the milestone's thesis in its purest form: **it did not merely carry a stale reason, it carried an instruction to wait for a measurement that cannot decide.** *"Direction unresolved and left to a count"*, and three methods run on 2026-09-13 read **49.5%**, **50%** and **50%** private-able of **1868** declarations, against a **42%** that was another method on a pre-port tree a third the size in another language — so it never was one instrument run twice. **A ratio hovering at half is not a tie-breaker**, and a fourth year of waiting would produce a fourth number near half. **The item's hinge also expired two weeks after it was written.** Panel 033 rested on *"one whole-program translation unit gives visibility no linkage consequence"*, and **M-separate-compilation landed 2026-08-26**: measured, the compiler emits **157** translation units, a prototype per definition, and a never-called helper takes external linkage. The conclusion survives the premise anyway, because reachability pruning already leaves a neighbouring unit zero prototypes for that helper, so what the keyword adds is `static` — performance, a stated non-goal. **And the ancestry is for a different form than the one costed**: all six languages panel 033 named default to hidden and **not one marks hiding** — Modula-2 a separate export list in a separate file, Oberon and Nim an asterisk on the declaration, Zig `pub`, Erlang a module attribute, Go the initial capital in the name — so a `private` keyword on a default-visible language has **zero** ancestry in that set, its real ancestors being Kotlin and C's `static`; and both reversal citations are wrong as cited, Rust RFC 0001 being about struct fields rather than cross-module visibility. **The two directions were also priced as one and are not**: the compiler cost is identical because the gate inverts rather than grows, but default-private carries **~1181 declarations** to mark and a **two-stage seed bootstrap**, the seed being a checked-in 796,427-line artifact that must learn to parse `export` before it can be regenerated over a marked tree. Panel 033's own refutation re-checked: **conclusion sound, evidence sentence wrong** — an uncalled top-level function gives `heroes check` exit **0, silently**, so the unused rule does not reach the top level today | **The route the critic found is already in the compiler and needs no keyword at all**: `HERO_TU_LOCAL` expands to `static` per module and `reach_of` already walks reachability, so the item's one surviving benefit ships as an error class — *a top-level declaration that no other module names and that nothing in its own file uses is a compile error* — with no keyword, no AST field and no change to what `use` binds. It does not record intent, which the reader's seat says is the one real win of a written marker, and that trade is written down rather than left for the next sitting to rediscover. The critic also measured that **Go's route is unavailable here**: of 1868 declarations the 275 beginning with a capital are exactly the 143 records, 86 constants and 46 variants, so **the case bit is spent on kind** and a Go rule would export every type and hide every function against a private-able set that is ~89% functions. **The seats split 2-2 on the direction with all four claiming to rule**, so the sitting records that it has no ruling rather than inventing one, and names the discriminator that is runnable today and that nobody adopted: the per-file marker economy on a real multi-module binding, **14 `export` against 33 `private`** once the 26 extern members whose visibility `extern_across_modules` already fixes are set aside. **Two corrections to the coordinator are on the record rather than quietly fixed**: a brief asserted that the specification says an error carries everything needed to fix a program without opening another file, which is design.md's §4.17 and not in the specification at all — the seat forbidden design.md said so and judged anyway — and a brief's line citation was invalidated by this same session's edits to the document above it, which is CL-037's shape with this sitting as its own example | design.md §1.0, §1.7, §4.4, Part 2, Part 6, Part 7 item 14; CLAUDE.md §2, §12, §14; CL-037; panel 033, panel 093 R2 | 141 (033 inherited)
