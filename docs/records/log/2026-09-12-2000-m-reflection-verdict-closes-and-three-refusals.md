2026-09-12 | **M-reflection-verdict closes: three refusals are sound and three of their reasons were false** (scheduled by author instruction 2026-09-03 as a decision and not a feature; panels 131 and 132, both ratified the evening they sat). **Run-time reflection is REFUSED** as a design.md Part 6 row, unanimous with two vetoes engaged, and **not on the Ruby row's ground**: *requires runtime dispatch, i.e. an interpreter* is false of reflection over types and Go falsifies it, so the row rests on layout — a descriptor is emitted per translation unit and only for a type used as a container element, `strip -x` deletes the symbol while `dladdr` still returns success with the name at NULL, metadata in the value moves raylib's `Vector2` from 8 bytes to 16 and its floats out of `s0`/`s1` into `x1`, and metadata in `HeroDesc` zero-fills 125 descriptors at exit 0 with zero warnings. **User-defined tags are REFUSED**, unanimous, and **not on the brief's hinge either**: a user-defined set CAN be closed, because Java's annotations are declarations that resolve while Go's struct tags are strings; the row rests on inertness, since no macros, no compile-time evaluation, no closures, no run-time reader and §10's one binary leave nothing that could read such a tag. **Panel 117's sixth option is WITHDRAWN** on the three measurements it had itself demanded, reversing the author's ratification of 2026-09-07 on a separately queued item, and its restoration clause leaves `spec § 12` because no shape adopted or scheduled here is a renderer `assert` could use. **Compile-time derivation was NOT ruled at the first sitting and the reason is the sitting's own brief**, which framed it as rendering Heroes' construction call: two seats measured that such a derive deletes 0 of 56 and 0 of 31 hand-written renderers, both correct and both fatal to the drafts rather than to derivation | **What entered is `Point::x`, the field-name operator, and four of five seats invented a fifth shape when the brief asked them to break four.** Two convergences, each independent: the seat holding only the specification and the seat holding the whole compiler invented the same construct unseen by each other, and the seat at the C boundary and the historian reached the same different one, one by compiling it and one by reading Ada's `T'Write` (ISO 1995) and `T'Put_Image` (ISO 2022) — which also **falsifies the negative claim the brief asked for**, since Ada shipped compiler-derived per-component serialisation with no macro, no compile-time execution, no trait and no separate generator. **The number, run and not predicted**: one program in two spellings, bare string keys give `typo-key` four slips and **0 of 4** killed, `m[Room::width]` gives `typo-ident` five and **5 of 5**. **The cost is the author's ratified choice of spelling and is recorded rather than rounded**: the prototype spelled `Point.x` measured +59 code lines in three files with no ceiling moved, by reusing the existing field node; `::` must be in the tree for the formatter to re-print it, so it is a node of its own, and a new node is a new arm in 68 exhaustive matches, four ceilings moved, and the new compiler about 1.0% slower on a fixed job. **Two compiler defects repaired at their classes**, 027 and 028, one of which was a rule panel 089 had already decided and the construction had not followed. **And a defect in the net's own instrument**: the annotation reader trimmed before testing for the `v` of `#~v`, so a whole class of diagnostic codes could not be annotated, silently, for as long as that reader has existed | CLAUDE.md §1, §2, §4, §6, §8, §9, §10, §12; design.md §1.6, §1.7, §1.11, §1.12, §4.17, §4.19, Part 6, Part 11 | 018, 027, 039, 076, 089, 114, 117, 119, 123, 131, 132 |
