2026-08-23 | **Panel 088: the sitting that had already been held** (provisional — author ratification pending). Convened within the hour on `xs @ xs.push(v)` being quadratic (`runtime/parts/array.c:110` allocates `len+1` and copies every element per push; 25k/50k/100k pushes = 0.9/3.0/11.7 s of work, measurement 011) — and **panel 037, twelve days earlier, is titled *"Array growth, and the optimisation that fires nowhere and corrupts where it fires"***, with `design.md:1531-1537` carrying the same ruling and the sound form's name. CLAUDE.md §1 prescribes the grep that finds them; it was not run, and panel 084 already carries this exact class. **What the sitting added, and it was owed**: the measurement 037 assigned to M-selfhost-probe, which closed 2026-08-15 without taking it, and a working prototype nobody had | **The headline died of its own measurement.** Removing **99%** of push's element traffic (1,796,724,220 → 18,398,012) moved the harness emission **12.29 s → 11.46 s: 1.07×** — so push is 7% of the 46× that measurement 011 measured and deliberately did not attribute. **The gate is now measured impossible rather than argued**: `pushes=100000 rc1=0 rc2=100000`, `ys = xs.push(2)` and `xs @ xs.push(2)` both report rc=2 because the count is of slots and not of liveness (`ir_own.hero:14-16`, `:26-40`), and loosening to `rc<=2` prints `zs[10]=99` for 77 at **exit 0, clean under ASan+UBSan**. O3 is inside the noise on two independent seats. **O4 is vetoed** (ffi-pragmatist, §1.11: bulk C data reaches Heroes only as `str → [str]` through push). The sound form was **built**: `hero_array_push_owned(HeroArrayHeader **slot, …)`, 69 lines, ABI unchanged, 100k pushes **22.34 s → 0.00 s**, seven adversarial shapes byte-identical and sanitizer-clean — and two seats' prototypes differed exactly at `n.kids @ n.kids.push(n)`, where one exited 138 and the one whose guard re-reads the refcount **across** `elem->copy` returned a clean snapshot. **The spec's fear was inverted**: `spec:167-169` teaches the quadratic cost, so this was never a §12 compiler bug, and the *true* wording measures **3508 against 3512 — a 4-token refund**; leaving the sentence false is inadmissible (it has a dependant at `selfhost/measure_bpe.hero:51`, inside the tool that scores §1.6) and deleting it was already refused by panels 043 and 054. The ergonomist found the sentence **already doing harm**: warned about arrays and told nothing about maps, it wrote a counter-keyed map used as a list, which loses ordering at exit 0 and is **15× faster** (0.75 s against 11.7 s). Resolution: **nothing lands**, the place store stays adopted and waiting on 037's own two triggers, five conditions ride with it, and **the 46× becomes its own question** — with `emit_writer.hero:82` (`w.out @ w.out + text + "\n"`, 724,245 times, and `spec:167` says `+` copies both sides) measured at 25k/50k/100k lines = 0.45/1.2/8.4 s as the leading suspect. Three of the sitting's four process errors are the coordinator's, including a "frozen" worktree shared by five seats in which one instrumented the runtime another was timing | docs/panel/088 · docs/panel/037 · design.md:1531 · docs/measurements/011
