2026-09-03 scheduling measured *"three mentions and no ruling"* over a wider set;
the count has since grown to 22 across design.md, `DESIGN-LOG.md` and
`docs/panel/` while **none of them is in design.md**, because every new mention is
the record talking about the absence. So the refusal was citable from no ruling at
all. **Run-time reflection is REFUSED**, unanimous, two vetoes engaged — and not
on the Ruby row's ground, because *"requires runtime dispatch, i.e. an
interpreter"* is false of reflection over types and Go falsifies it; that clause
is true of `method_missing` and untrue of reading a type's fields, and panel 039's
C4 row is corrected underneath itself. The measured ground is layout: a descriptor
is emitted **per TU** and **only for a type used as a container element**, so a
two-record program with no container emits none, and `strip -x` deletes the symbol
while `dladdr` still returns success with the name at `NULL` — which **overturns
the 2026-09-06 `nm` finding** that the descriptor's own symbol is the type name
`HeroDesc` does not carry. Metadata in the value takes raylib's `Vector2` from 8
bytes to 16, `offsetof(y)` from 4 to 12, and moves its floats from `s0`/`s1` into
`x1`: **a different register class**, which is §1.11's level-3 glue. Metadata in
`HeroDesc` zero-fills **125** descriptors at exit 0 with zero warnings and reads
`NULL`. **User-defined tags are REFUSED**, unanimous — and not on the brief's
hinge either, because *a user-defined set cannot be closed by construction* is
false: the property is whether a tag is a **name that resolves or a string**, and
Java's resolve while Go's struct tags do not, their `govet` check having been
added in the same 2011-06-29 commit that introduced them and still not noticing
`jsn` for `json` fifteen years later. The row rests on **inertness**: no macros,
no compile-time evaluation, no closures, no run-time reader, and §10 forbids a
plugin, so a general mechanism would make §4.19's six contextual words cheaper to
write and impossible to check — `owned sqlite3_notafunction` is
`error[unknown_freer]` today with two notes, and would become a stored string.
**Panel 117's sixth option is WITHDRAWN on the three measurements it itself
demanded**, which is what they were owed for. The prototype exists,
`selfhost/emit/differ.hero`, **160 code lines** type-checking clean in the real
graph, but the real figure is **350-450 across six modules and the runtime**; the
map case reports **two different leaves for two `==`-equal left operands**, so the
leaf named is a function of insertion history; the depth case gives a path 28,813
characters long at 3600 and `panic: stack exhausted` at 3700, exit 134, where
today's thin `assert failed: a == b` is correct at exit 0 at depth 100,000; and
the load-bearing premise *"the walk is `eq`'s walk"* is false at
`runtime/parts/array.c:185-190`, where `HeroEqWork` is `{a, b, elem, len}` with no
path and drains LIFO. Panel 076's veto is engaged. `suite_special.hero`'s
aggregate pin stays and stays correct. **Compile-time derivation is deliberately
NOT RULED, and the reason is the sitting's own brief**: its drafts render Heroes'
construction call, which is display, while the precedent and the pressure are
about a per-type walk aimed at a **foreign notation**, which is derivation — three
seats answered the question and two answered the brief. The warden's Principle 0
count, run because nobody had run it, is **0 of 56** renderers a construction-call
derive would delete, and the compiler seat's is **0 of 31**: both fatal to the
drafts and silent about derivation. Against them the FFI seat wrote
`encoding/json` by hand, **120 lines for three records**, changed `f["width"]` to
`f["witdh"]`, and got **exit 0, zero diagnostics and the wrong key in the
output** — caught only because that program round-trips, so a program that only
marshals never finds out. Re-sat as step 2 on the corrected question, with every
measurement handed over rather than re-derived. **Spec unchanged, 5662 tokens**,
and the warden's own instrument limit goes on the record: `heroes measure
--refresh` refuses any file but `spec/heroes-spec.md` (panel 123 R5), so **no
draft can get a binding count without landing first** and every delta this sitting
weighed is provisional by construction. **One defect filed beside the sitting**,
027: `cstr` emits `const char *` while `struct passwd.pw_name` is `char *`, so a
legal binding is refused by this project's own `-Werror` and the refusal arrives
as `internal error: compiling the generated C failed`, naming no repair where
`spec § 13` promises a refusal | design.md Part 6, §1.11, §1.12, §4.19; CLAUDE.md
§1, §2, §6, §10, §12 | — (018, 027, 039, 076, 114, 117, 119, 123 are what it
inherits or corrects)
