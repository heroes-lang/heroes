2026-08-16 | **Panel 071 item A: three `extern` parameter types stop being exit 2, and the widening caught a latent error in the corpus itself** (provisional — author ratification pending). A parameter declared `ptr`, `cstr` or `str` against a header that says otherwise gave *"internal error: compiling the generated C failed"* — the compiler blaming itself — while the same parameter declared `i64` gave the correct `ffi_parameter_type` at exit 1. The cause was narrow: `ffi_narrowed.rs` matched exactly two clang strings, both **width** diagnostics. A **class** mismatch phrases itself two other ways and both contain `" to parameter of "`, which is the whole matcher. **The gate is unchanged** — `extern_at_line` still asks whether an `extern` is declared at exactly this file and line, which is what panel 052 made the class safe with; only the phrase set widened. All four types are exit 1 now, measured | **The diagnostic fires whether or not the C type has a Heroes name, and only the `Fix` consults the table** — the ffi-pragmatist made that a condition, and it is the difference between a repair and a regression: the types with no Heroes name are exactly the interesting ones, and declining would return `Vec2` to exit 2, which is **356 of 1159** entry points by panel 052's count. A `Vec2` parameter's note now says *declare it as a `record` inside this same group*, a sentence rather than a type name. **Two things the widening broke and one it found**: it swallowed panel 058's `ffi_writable_parameter` class, whose clang text also contains the phrase, and then told an author to *"declare it `cstr`"* when they already had — repaired by naming `ffi_mutable.rs`'s own `discards qualifiers` string as that class's property, a fact about the text rather than a guess about precedence. And it surfaced a **real mistake sitting in the corpus**: `ffi-incomplete-record.hero` declared `CheckCollisionPointRec(point: Rectangle, …)` where raylib says `Vector2`, silent for as long as a class mismatch fell through to exit 2. `ffi_narrowed.rs` split at 348 lines — `c_spellings.rs` holds the C-type table, a lookup that knows nothing about diagnostics | design.md §4.19 · CLAUDE.md §7, §11 | 071
