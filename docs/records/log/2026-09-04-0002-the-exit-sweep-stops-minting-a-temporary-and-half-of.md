2026-09-04 | **The exit sweep stops minting a temporary, and half of every frame goes with it** (M-corpus-depth, building panel 106's adopted resolution; `docs/measurements/015-the-sweeps-own-temporaries.md`). `selfhost/ir.hero` gains `decref_slot`, an `OpKind` case carrying a **slot** and not a Place — so it cannot name `p.x` and release half a value, and the verifier does not have to check what cannot be spelled. `ir/own.hero` mints one per swept slot where it used to emit a load into a fresh temporary followed by a decref of it; `emit/inst.hero` emits `release(&slot)` through a `reference_named` that the value door now also goes through, so the two ways to reach a refcount call cannot learn a type apart; `ir/uses.hero::slots_of` reports it, which is the one line that keeps a swept slot counted as read rather than gaining `__attribute__((unused))`. **The file that had asserted the old rule for a year is the file that withdraws it in writing**: `ir.hero`'s own comment said releasing a slot *is* a load followed by a decref, *"which keeps one form instead of two and makes the dominance check cover it free"* — both halves true, and the price was 58.5% of all declared frame bytes. **Measured before and after on this Mac, `-fstack-usage` over the emitted C**: the interpreter's frames **639,792 → 294,128 bytes (−54.0%)**, its worst function **112,704 → 17,296**, one nesting level of its grammar **36,896 → 20,608**; the compiler's own **6,563,376 → 4,227,600 (−35.6%)**; the seed **846,804 → 719,955 lines (−15.0%)**; the emitted interpreter **69,052 → 54,610**. **The ceilings, binary-searched, all stopping with the guard naming the function**: nesting **104 → 166** under `--sanitize`, **191 → 314** at `-O0`, **349 → 585** at `-O2`; script recursion **40 → 76**, **50 → 107**, **80 → 239**. **The fixpoint holds byte for byte.** 560 own tests, 97 harness tests, 135 of 189 emission traces re-blessed, and **twelve `tests/golden/ir/` plus three `tests/golden/emit/` cases updated BY HAND** because §9 forbids `UPDATE_GOLDEN` in exactly those two directories — every hunk classified before anything was written, and every one is the same pair replaced by one release naming the slot. Three `suite_layout` ceilings raised with their reason, the pinned table count 16 → 17. **What the milestone asked for and did NOT get**: a 500-deep expression. `--sanitize` tops out at 166 and the corpus runs all three configurations, so the interpreter's case moves 60 → **83** and its evaluator's guard 24 → **38**, both re-derived from the measurement. A coordinator prediction is scored **FALSE** on `docs/panel/106`: it said the case would at least triple, taking the `-O0` number and stating it about a case the tightest configuration governs | CLAUDE.md §7, §9, §12; design.md §1.7, §1.12, Part 5 | 106, 021, 104
