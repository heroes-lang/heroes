2026-09-13 | **The handle form narrows before it lands, and the false clause that nearly reached the specification was the coordinator's brief rather than the sitting's resolution** (M-handle-verdict step 4, scouting; author decisions 2026-09-13 on two questions the scouting raised). Five scouts mapped what panel 145's form changes — parser, checker, emitter, the CL-036 re-printer walk, the spec wording — with a completeness critic over them before a line was written. **Two things came back that change the work, and one of them is about the coordinator.** | **The clause that would have re-opened the defect it closes.** The brief the coordinator wrote for the scouts said *"a parameter declared `Db` takes a `Db` or `nullptr` and no other handle; **a `ptr` still takes any**, so today's bindings keep compiling."* The spec scout took it from there and drafted it into `spec/heroes-spec.md` § 13. The critic ran it: **`error[type_mismatch]: expected ptr, found Db`** — the second half is false today. Reproduced by the coordinator, and then the consequence, which is worse than the falsehood: **if the widening were implemented, every function still declared `ptr` would accept every handle, and the swapped-handle class would return through it.** The form would close the defect for whoever adopts it everywhere and leave it open everywhere else, which is the ergonomist's H9 finding one level deeper. **The author struck the clause**: a handle does **not** convert to `ptr`. Cost of the decision: **zero lines**, because it is today's behaviour; the price is that inside a group every parameter that receives a handle is retyped — 7 `ptr` parameters in `examples/sqlite/main.hero`, 18 in `examples/ledger/db/sqlite.hero`, 2 in `examples/curl/main.hero` — and a `ptr` that never receives a handle stays `ptr`. **The provenance is recorded under the coordinator's name and not the sitting's**: panel 145's ratified resolution does not contain the clause, checked line by line. It is **the third framing fact a coordinator's brief carried wrong this evening** — panel 145's census was wrong four ways, all understating exposure, and this is the fourth error and the first that would have reached a judged document. **A brief is a measurement** (CL-057), and it keeps being the weakest link in a sitting whose seats are otherwise held to *run it or say it is unrun* | **The engineer's own condition fired, and the answer is to narrow rather than to build or to retreat.** Panel 145 recorded: *a compiled prototype needing more than 150 code lines moves 029 from ENTERS to WART*. The five scouts' proposals add to **≈338 lines**, or **≈203 for the form alone** once the two repairs are set aside, and the critic measured two ceilings the scouts had not: `selfhost/emit/ctype.hero` at **371 against a `DECIDED` 380** with +38 proposed, and `selfhost/emit/structural.hero` at **293 against §11's 300** with +23 proposed. Both reproduced. **But most of the excess is scope the sitting never ruled on**, which the critic found by attacking the shapes beside the form (CL-061): a handle as a **map key**, where the checker scout wanted a refusal and the emitter scout had implemented an address hash — mutually exclusive, and panel 145 mentions `hash` zero times; **two records sharing one tag**, which are two Heroes types and one C type, so a swap between them is a mutant clang accepts and the language could never kill; and **a `tag` naming a type the header declares COMPLETE**, which silently becomes a pointer where the author meant the struct. **The author's decision: refuse all three with a loud error rather than implement them.** A refusal costs a few lines where an implementation costs dozens, it is this project's own habit, and it keeps the three questions the sitting reserved from being settled by omission in an implementation | **What the narrowing leaves to build**: the form as ratified — a fieldless group `record` with a `tag` and without `partial` spells `<tag> *` verbatim, `nullptr` is its null, construction is refused, `==` compares the address — plus the three refusals, the three repairs the route exposed (`ffi_tag`'s *incomplete* read as *absent*, `freer`'s silent retype of `owned` on a non-`cstr`, `empty_record`'s caret landing on the next declaration), the § 13 sentence with the fence rewritten to use it, and CL-036's walk. **What the critic found that nobody else did**: 15 files that read `record_decl` or `.fields` and no scout named, the load-bearing one being `selfhost/cli/pointee.hero:116`, where `.named` returns *not numeric* — so `@out: Db` gets **no pointee width assertion at all** and the whole out-parameter story rests on the `_Static_assert` probe alone; and **the handle predicate had four definitions in four proposed homes**, which is CLAUDE.md's opening rule broken four ways inside one step, to be written once and called three times | CLAUDE.md §1, §12, § RUN IT, § Hard stops; design.md §1.7, §4.19, Part 6, Part 8 wart 20; `spec/heroes-spec.md` § 13; CL-036, CL-057, CL-061; panel 145 | 145
