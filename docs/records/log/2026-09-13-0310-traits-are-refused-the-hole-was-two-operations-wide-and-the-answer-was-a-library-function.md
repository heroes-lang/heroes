2026-09-13 | **Traits are refused to Part 6, the iteration wart's stated cause is falsified six times, and what the corpus actually wanted is a library function** (panel 137, M-deferral-ledger step 3, full five seats, one veto engaged; `docs/panel/137-the-hole-was-two-operations-wide-and-the-answer-was-a-library-function.md`). The ledger's third verdict, sitting on **two items at once** — Part 7 item 8 and Part 8 wart 11, which is the same question from the other end — because ruling one alone would leave the second standing as a promise the first had already broken. **The sitting was reframed before the briefs went out**: a generic sort with a comparator, `sort_by<A>(xs: [A], before: (function(a: A, b: A) -> bool)) -> [A]`, **compiles and runs today over a `[Point]`** at exit 0, because generics are unconstrained and top-level functions are values — so the twenty-two hand-written lines of insertion sort at `selfhost/modules.hero:159-190` are evidence that nobody wrote `sort_by`, not evidence that traits are needed. **Three measurements decided it.** The compiler seat compiled eight cases and found **the hole is two operations wide, not one feature wide**: a generic needing two operations works, `==` on a type parameter over a user record works, a `{A: i64}` map key works, a recursive generic works, a cross-module generic `sort_by` works, and only `<`, `sort([A])` and `print`/`to_str` on a type parameter are refused — because Part 5's descriptor pass already generates `copy`, `drop`, `eq` and `hash` per reachable type and has no `ord` and no `show`. The warden and the FFI seat found independently that **the corpus does not want them**: all **26** `sort(` sites in `selfhost/` sort `[str]` from `keys(m)` and **none** sorts a record, `function next(` appears **zero** times, and the brief's own premise that every binding writes an advance loop by hand is **false** — seventy `while` lines across the 22 files holding an `extern`, and **exactly two** advance a C handle. And the item **overstated its own cost by two thirds**: `for` is a special case for **arrays alone**, `range` being a library function and a map `not_iterable`. **Route A, nominal traits: REFUSED to Part 6 with its falsifier** — 900 to 1400 lines, two `DeclKind` arms against 219 enumeration sites in 53 modules, coherence with no home because the name table is keyed by module precisely so two modules may each declare `Point`, §1.7's subtraction returning zero, and the strongest argument for traits, the one-canonical-instance guarantee, **evaporating** because unconstrained generics give no site where a container calls a user operation behind the programmer's back. Precedent: C++ voted concepts in in 2008 and out in 2009, returning eleven years later deliberately minimal. **Route B, a structural `for`: DEFERRED, and on the compiler seat's ground rather than the reader's** — it contradicts **§4.8**, *"UFCS does not apply when the first parameter is `@`"*, because `for x in v` calling `next(@v)` writes no `@` at all and the compiler already refuses the neighbouring shape by name; the reader's separate veto was on locality and on the loop **dropping a `T?` at every exit**, against the answer-the-error rule. **Wart 11 is rewritten rather than dated**, its *"since there are no traits"* falsified six times — C++ looks for `begin`/`end` by name, Python duck-types, **Go shipped `range` over a plain function in 1.23 having declined the interface proposal on its own table**, Lua uses a metamethod, Nim rewrites `for` to a named `items` call which is the machinery UFCS already is, and Zig refuses both **independently**, which is what shows they are two choices. **What the sitting adopts is not a language change at all**: `sort_by` joins the six generic higher-order functions the library already ships, scheduled at **M-core-packages** with the engineer's prediction as its acceptance test — two seats named M-generics-library, which closed 2026-08-11, and the correction is recorded rather than quietly made, because `records/homes` is what would have failed — ≤ 20 added lines, ≥ 60 deleted across the four hand-written sorts, no compiler module changed | **Found beside the sitting and filed as defect 030, and it is the more serious half.** Two copies of one record holding a `sqlite3_stmt *` **advance the same C cursor**: `a sees id 1`, `b sees id 2`, `a.handle == b.handle` — with `b` copied before `a` advanced. Hand-written C of the same shape is accepted at zero warnings and answers identically. Spec § 3 says *"Every value behaves as an independent copy … No aliasing exists anywhere"*, and `examples/ledger/db/sqlite.hero:287` **ships** a function taking that wrapper by value and mutating the C cursor through it. The sentence is true of Heroes-owned memory and false of a copied address, and the document does not say so; the repair is a panel, because it states what a value is, and CLAUDE.md § 12 is why the spec is the defect rather than the compiler. Recorded separately for **M-cleanup-verdict**: breaking out of a handle-advancing loop leaves the statement open, SQLite answering `statements still open: 1` and `sqlite3_close says BUSY: true`, and neither `owned` nor `lease` reaches a handle. **A process note the sitting keeps**: the reader's seat was briefed before its files existed and returned **UNRUN** rather than substituting the repository's own specification, writing down the names it had searched for and the check that would catch it having cheated. That is the behaviour the panel is built on and it is recorded as such rather than smoothed away | design.md §1.0, §1.2, §1.3, §1.7, §4.4, §4.8, §4.12, Part 5, Part 6, Part 7 item 8, Part 8 wart 11; CLAUDE.md §2, §4, §12, §14 | 137 (033, 119 inherited)
