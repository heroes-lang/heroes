2026-09-10 | **A hard stop was resting on a file that never did anything, and the file is gone with the rule repointed at what actually publishes** (**author instruction**, given after the session reported the file and declined to delete it alone). `site/public/CNAME` held `heroes-lang.org` and was a GitHub Pages artifact on a site that ships to **Cloudflare Pages by Direct Upload**, which does not read one; `DESIGN-LOG.md`'s 2026-08-30 entry had already measured that GitHub Pages was never in play, and re-measured today, no workflow under `.github/` mentions `deploy-pages`, `upload-pages-artifact`, `configure-pages` or a `gh-pages` branch. Astro copies `public/` verbatim, so the file was **served publicly at `/CNAME`** while governing nothing. **What made this more than tidying is that four LIVE documents cited it as the reason a push is asked for**: § Hard stops, the step skill's close checklist, `docs/ROADMAP.md` and `docs/contract/case-law.md` CL-042. Deleting it alone would have left four citations resolving to nothing, which is the failure `records/citations` exists to catch, so the citations moved first. **The replacement says strictly more than the sentence it replaces**: `.github/workflows/deploy-site.yml` deploys on any push to `main` touching `site/`, `examples/`, `spec/heroes-spec.md`, `selfhost/keywords.hero` or the workflow itself, so a push that touches only `examples/` publishes the site as well, and a session reading the old sentence would have thought that safe. **CL-042 was corrected underneath and not over**, per §14: its conclusion was right and only its reason was wrong, so the false sentence stands with today's date beside it. **The contract's own budget was the binding constraint on the wording**: `heroes measure CLAUDE.md` read **12** tokens of headroom against the 5500 ceiling before and **9** after, and the first draft of the replacement spent 7 of the 12, so it was rewritten shorter; the named removal for the three it does spend is the CNAME sentence itself | a rule whose stated reason is false teaches the wrong thing even while it is obeyed, and this one understated its own scope: the artifact it named could be deleted with no effect, while the mechanism it did not name publishes on four paths it never mentioned | — | —
