2026-09-10 | **A microcontroller enters the chain as a verdict and not as a target, RISC-V first, and the two facts a 32-bit build refuses were measured before the row was written** (**author decision**, out of the session that asked whether supporting a microcontroller such as the ESP32, or a program under an RTOS, would be worth a step, since the language compiles without a garbage collector; three recommendations put, three accepted; no board yet, the author is ordering one). **What the record said was nothing**, measured: zero hits for microcontroller, RTOS, bare metal, freestanding, newlib or ILP32 across design.md, `spec/`, `docs/` and the site in that sense, so the question went to the compiler and the runtime first (`docs/measurements/026-the-two-facts-a-32-bit-target-refuses.md`). **What came back**: the runtime asks the C library for **73** functions, fifteen of them `pthread`, eight for processes, seven for signals and the stack guard; under `--target=riscv32-unknown-elf` and `arm-none-eabi` it refuses at `runtime/heroes_runtime.h:122`, a 64-bit atomic that must be lock-free because a string literal's block is read-only, which on a device is the flash, so the header's own argument grows there rather than shrinks; the emitted C of the smallest gallery program is 189 lines with `main(argc, argv)` and includes `<math.h>`; `runtime/parts/stack.c` delivers `spec:167` through a POSIX branch and a Windows branch, and FreeRTOS has neither; a hello at `-O2` is 89,160 bytes stripped on this Mac and links libSystem alone. No Espressif toolchain is installed, Homebrew's QEMU 11.1.1 knows no `esp32` machine and Apple clang 21 has no RISC-V backend, so newlib, GCC and the device itself are **unrun**, and the row says so. **Three decisions**: the row enters now, at 64 after M-deployable-binary, whose question this is with a different answer, and before M-install-channels; it is `M-microcontroller-verdict`, a ruling on `M-cleanup-verdict`'s precedent so that it may close with a refusal, because `DESIGN-LOG.md:539` refused cross-compilation on the ground that platforms are measured on real machines, and a board is one while the 5,666,664-byte compiler cannot run on it, which is the first of the seven questions the sitting is handed; and the RISC-V chips first (ESP32-C3, C6), because upstream clang and GCC carry the target and Apple clang already type-checks for it, while Xtensa lives only in Espressif's fork, with ESP-IDF over FreeRTOS rather than bare metal, because Part 2's *not a systems language* is met to the letter when the registers are Espressif's C and every `str`, `[T]` and `{K: V}` needs `malloc`. Not Principle 0: a platform is not a form, and the thesis effect on firmware is unrun; what fits is §1.11, a language with no standard library has nothing to port. The first step is a measuring session on ESP-IDF's toolchain and not the sitting | the conservative route, a note and no row, was on the table and declined (CL-040); the `records/lists` check caught the banner of `docs/work/SCHEDULED.md` at 51 against 52 items before the commit, as it exists to; and § Where we are's count of open scheduled items said **43** against a measured 51 before this row joined, corrected to 52 with its instrument named | design.md §1.11, §1.12, Part 2; `spec:167`, `spec:228`; CLAUDE.md §4, §14, § RUN IT | 049, 114 as precedent; no sitting convened, since scheduling changes no language
