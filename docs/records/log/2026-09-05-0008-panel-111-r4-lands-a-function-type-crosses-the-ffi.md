2026-09-05 | **Panel 111 R4 lands: a function type crosses the FFI as a parameter, and three defects came out that no seat had predicted** (M-c-callbacks step 0). **The permission is one line in the checker** — `crosses_the_boundary` answers yes for `.function_ty` — because C spells a function pointer everywhere it spells a type, so the vocabulary question could never have been the one that says where a callback may stand. The positions are refused separately and there are **three**, not the two R4 named: an `extern`'s **result** (`signal` is the header every reader meets first), an **`@` out-parameter** (`void (**)(void)` is the same case wearing C's other direction), and an **`extern constant`**, which no seat named and which reaches the same danger through the one door that does not go through a signature. The argument for all three is one sentence and it is the permission's own mirror: an address passed OUT is one this compiler emitted, so C calls a body this compiler type-checked; an address handed BACK is a body nothing here has seen. **Measured end to end**: `atexit` binds and C calls it back at exit 0, `pthread_create` binds, spawns and joins with no cast and no trampoline. **Three defects, and every one was found by running rather than by reading.** (1) `emit/extern_probe.hero`'s `c_type_of` had no `.function_ty` arm, and one unspellable parameter withholds the whole list, so binding a callback would have switched the header check off for **every other parameter of that extern**, in silence — `DrawText`'s defect with a new cause, and R4 predicted this one. (2) A per-module TU never joined an `extern`'s parameter types to its typedef reach, because an extern is in neither `defined` nor `needed`: `unknown type name 'h_0fn_294870dd'`, exit 2 on a correct binding. (3) The probe was emitted in the prelude, **before** the typedef section, against `emit.hero`'s own written invariant *"types before anything that can mention one"*; both emitters now write it after `typeorder.definitions`, and the six blessed emissions moved by exactly that block — verified as the **same multiset of lines and the same line count**, so nothing was added or dropped. Gaps 2 and 3 were latent from the day the probe existed and could not fire while every boundary type spelled itself without a table. **`qsort` still does not bind**, and the compiler said in writing that it does: `int (*)(const void *, const void *)` has no Heroes spelling, and R4's `const` question is scheduled with the wart as step 0's provisional default. **R4's new diagnostic class is NOT opened**, provisionally and queued: measured, a wrong callback signature is already `ffi_parameter_type` at exit 1 on the `.hero` line, because clang's text carries panel 071's own ` to parameter of ` needle — so the ffi-pragmatist's *"a `qsort` binding exits 2"* is **falsified**, and what was really wrong was the note, which offered the struct repair under a function pointer | §4.19, §4.17, §1.11, §1.12 | 111 R4, 071, 062
