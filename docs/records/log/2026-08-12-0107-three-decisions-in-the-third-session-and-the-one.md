2026-08-12 | **Three decisions in the third `/decide` session, and the one that costs something is the cheap question being made to wait.** (1a) **The hexadecimal literal rides in the unsigned panel's sitting** rather than getting its own or waiting for the next FFI rung — so the sitting owed on `u64`/`size_t` now has two questions in its charge, the type vocabulary and the literal notation. (2a) **The §11 breach becomes a standing sweep with its own commit, run to zero**, over a per-milestone obligation in `/step` and over restating the number: the rule keeps saying ~300 and the tree is brought to it, one file per commit, byte-identical emitted C wherever the cut touches `emit/`. (3d) **`// ORDER:` is deferred to M-selfhost-probe** and moved out of the decisions into the scheduled work, over marking the sites now and over turning design.md's sentence into a tested claim | each verdict's cost is written into its ticked item, and two are worth stating here. **1a makes the cheap question hostage to the expensive one**: a mask stays decimal for as long as the panel is unconvened, and the notation gap costs one spec clause with no new type and no conversion rules. **3d leaves a false sentence in design.md**: §4.9's port note names `types/holes.rs` as the bootstrap's one map iteration and that is obsolete — `holes.rs` walks vectors now, and the live walk is `resolve/mod.rs:198` `names_in` over the `BTreeMap top`, reaching *did-you-mean* text and `--dump-scopes`. The correction rides in the scheduled item so the probe does not re-derive it, but until then the document misidentifies the file, which is CLAUDE.md §11's expired-premise class living in design.md instead of in a comment. The session's verification pass earned its keep before a question was put: two citations in the unsigned item were wrong (the two header constants are panel **040**'s, not 039's, and are a judge's compiled reading rather than `ffi_constant_type` transcripts), `0xFFFFFFFFFFFFFFFF` is **not** `int_out_of_range` because Heroes has **no hex literal at all** — a claim panel 040 also makes — and one scheduled item was already done in full | CLAUDE.md §11, §4, §12, §1.6, design.md §4.9, Part 7 item 10 | 022, 040 |
