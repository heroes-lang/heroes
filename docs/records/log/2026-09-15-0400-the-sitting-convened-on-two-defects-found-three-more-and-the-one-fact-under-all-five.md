# The sitting convened on two defects found three more, and the one fact under all five

2026-09-15. Panel 150, convened without asking under CLAUDE.md § 4, on
`docs/work/DEFECTS.md` 037 and 038. Full panel plus the completeness critic. The
sitting is
`docs/panel/150-the-runtime-holds-a-number-where-it-needs-a-set.md`.

## The resolution is not what any seat proposed

Four of the five shared a premise the critic falsified: that the repair is a
refusal and not a build. **Every finding of the sitting is one fact — the runtime
holds a NUMBER where it needs a SET** — and `runtime/parts/alloc.c` had already
named that instrument in writing, *"a different instrument, not a better
sentence"*, while pricing it at nothing.

**The coordinator's own shared brief foreclosed the question in its own words**:
*"Nothing has to be built; something has to be refused."* The critic quoted it
back.

## What each seat measured, and every one mattered

**The mark on a `ptr` cannot be read**: `acquires sqlite3_notafunction` on a
`ptr` producer checks at exit 0 (spec-warden), because `consumed_types` keys on a
declaration index and a `ptr` has no declaration (compiler-engineer).

**And obeying a wrong one corrupts, twice.** `fopen … acquires free` released
with `free`: **`AddressSanitizer: bad-free`**. Two non-interchangeable releasers
in `netdb.h`, swapped: **`SEGV` inside `freeaddrinfo`**. Both re-run by the
coordinator.

**The per-element mark is refuted by construction**: emitting one increment per
DECLARED element against a header filling two of four turns defect 038's `-3`
into a `+2`.

**And the instrument is pointed the wrong way**: the program releasing four
elements corrupts nothing and **aborts at 134**; the one releasing elements AND
the composite is a real double free and **exits 133 with zero bytes on both
streams**.

## The two routes nobody listed were one route, and each seat had half

Neither saw the other's. `record Blob tag void` is **capped at one per program**
by `duplicate_tag`, and defect 037's own header needs it twice, so under that
route the briefed program is **unwritable** — and `netdb.h` has nineteen
`struct X *` producers and zero `void *` ones, so it covers none of them.
`record AI tag addrinfo` **checks at 0 and clang refuses the build**, eighteen
times; the ffi seat's hand-edit was a **one-token substitution**.

**The composed sentence nobody priced: a `tag` names a C TYPE NAME**, where the
grammar holds one identifier and C writes two.

And `tag void` **gives up exactly the §4.19 check**: an 8-byte `malloc` handed to
`freeaddrinfo` builds clean with zero clang diagnostics, where the same
cross-wire under `struct addrinfo` is a hard `-Werror`.

## Three defects the sitting produced

**039**, by the ffi-pragmatist on a `ptr` and **re-run by the coordinator on a
handle**: `acquires` counts a failed producer's NULL, so the correct failure path
aborts. Three of five real producer sites are exposed and
`examples/sqlite/main.hero` is measured.

**040**, by the critic, and it is the worse half: **releasing a null discharges a
real obligation**, so a leaked handle plus one null release **exits 0 in
silence**. The escape from 039 and the hole in the leak check are the same
construct.

**041**, by the critic: the demand crosses module boundaries and the diagnostic
**names no location for the declaration that armed it**. Reproduced here in two
files: the refusal lands in a file that imports nothing, naming a function it
never mentions.

## Two brief defects that are the coordinator's

The **llm-ergonomist reads neither the shared brief nor any sitting by charter**,
so *read these three sittings* was unsatisfiable for one seat in five and the
brief did not say so. And the blast-radius-of-one-file the brief measured counts
a conjunction the checker never evaluates, which is what dissolved the
historian's approval.

**What did NOT recur**: panel 149's citation failure. 147 is cited by three
seats, 148 by three, 149 by four.
