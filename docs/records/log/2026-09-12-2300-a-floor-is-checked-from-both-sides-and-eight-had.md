2026-09-12 | **A floor is checked from both sides, and eight of them had gone slack** (author instruction: the floors are the contract ceiling's case, housekeeping rather than a forcing function — which decided the shape and also decided there would be no panel, a verifier being changed the way `DECIDED` and `WHERE_CEILING` were changed the same evening). Measured by the check itself on its first run: `annotations` 207 against **319**, `canonical` 340 against **524**, `emission` 142 against **225**, `golden/check` 65 against **112**, `order` 10 and 14 against **22** and **23**, `layout` 140 against **204**, `lines` 87 against **111**, plus `golden/ir` and `golden/unsupported`. `suite_canonical` is the proof rather than the argument: its own comment records the defect it was written for — the first draft used the flat listing and found **325 files of 371**, which a floor of 340 caught — and at 524 files the same fault finds 458 and passes, so that floor would no longer catch the thing it exists for. And `tests/golden/check` is the row to remember, because it is where `UPDATE_GOLDEN=1` is forbidden OUTRIGHT by the hard stops: the most protected corpus in the repository had the slackest floor, with 47 cases free to stop running in green | **What was built, and three things that deliberately were not.** A floor is a PREMISE ABOUT THE WORLD, which `.claude/rules/module-shape.md` already prices — *a fact about the value cannot expire; a premise about the world expires silently, and the comment justifying it goes on reading as correct* — and that rule asks for **a test that fires when the premise dies**. None of the twenty had one. `tests/harness/floors.hero` is that test, and it is small because the suites were already doing half of it: every one counts the real number on every run and compares it to the floor to catch a walk that found nothing. What was missing is the other direction of the same comparison. *No register was built*: a list of floors is a second thing to forget, staling exactly the way the floors did. *No percentage is tracked*: `SLACK` is 25, wide enough that adding a case or a module never trips it and narrow enough that all eight rows fire today, which is the only way to know the check is not decoration. *And no floor may be lowered*: the repair is always to raise to today's count, which is the ratchet — the one shape panel 134's historian found that has ever held a budget, because the number moves one way and the human is left with nothing to decide but when. Two seams the work found: `sound()` returns the verdict for callers that must STOP on a bad count, an extractor's floor guarding everything under it so that carrying on means passing vacuously on the next comparison; and `Shape.watched` in `suite_golden`, because four real shapes are watched while five fixtures built over two-file trees with a floor of 1 are not, and a decay check would otherwise call every case's stub tree outgrown. `suite_annotations`'s floor was the one the census could NOT measure from outside and wrote down as **NOT MEASURED** rather than estimating; the check measured it from the inside on the first run, by the method that file's own comment describes. `suite_runtime`'s three floors are carried forward: they are read in composite conditions over one sweep | CLAUDE.md §9, §11, § Verification; `.claude/rules/module-shape.md`; CL-004, CL-011, CL-054 | 134 |
