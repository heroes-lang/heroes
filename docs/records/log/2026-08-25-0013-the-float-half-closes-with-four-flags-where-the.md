2026-08-25 | **The float half closes with four flags where the sitting expected one, and the probe block becomes the place where a conversion is always a verdict** (M-separate-compilation step 4, landing panel 092's queued half; no new sitting — the mechanism, the scope and the sentence are 092's own ratified rows, executed after the measurements its engineer named). **The queued premise fell to its own protocol**: `-Wdouble-promotion` closes a quarter of the float half, measured before a line was written — `f64` against a header's `float` still demoted in silence under it, a float against a header's INTEGER dropped its fraction in silence, and `-Wconversion` cannot see the promotion at all because it is value-exact. Attacking the adjacent shapes (CLAUDE.md §1's rule for repairs) found the integer family open below the 64→32 window too: `i64` against `short`, `i32` against `short`, `i16` against `char`, all silent. The landed pragma carries four errors — `double-promotion`, `implicit-float-conversion` (which brings `implicit-int-float`), `float-conversion`, `implicit-int-conversion` — each with a witness made to fire first. **The scope is load-bearing and now measured, not argued**: enabled globally the four fire on **19 of 146** blessed emissions of correct programs (`s[i]` into a `u8`, `f32` literals, one float→int the emitter writes deliberately), so none of them can ever enter `flags()`; inside the block a probe's arguments are exactly the declared types, so any implicit conversion IS a declaration disagreeing with the header. The engineer's other two measurements: runtime exit 0, zero warnings under all four; all four wrong directions exit 1 on the author's line with the right fix (`f64`, `f32`, `i32`), and the corpus's correct float bindings (raylib `f32`, gallery `f64`) run untouched. The mapper learned three needles and got the fourth free — clang files the sub-32 narrowings under the SAME text NARROWED matches. `emit_c_spellings` gains its two float rows (`double`→`f64`, `float`→`f32`), so the fix names the repair instead of declining; `long double` is the new live decline. **The spec sentence landed once, at its final size, +32** (3560 → 3592, ledger row 45, paid by a registered prediction scored at this milestone's close): a parameter that disagrees is refused, except what C converts exactly (`i16` against int — bare, because `spec/rejected` refuses spec code the lexer rejects, and it caught the backticked form the minute it was written) and what a pointer points at. Cost elsewhere: 146 emissions re-blessed +2 new (the diff is the pragma block and the `#line` renumbering under it, no content line), six `emit/` goldens regenerated with their diffs read, seed regenerated, fixpoint byte-identical | a repair shipped without its adjacent shapes is CLAUDE.md §1's named failure, and this one had four neighbours — the sitting's flag was necessary and not sufficient, and only running the shapes said so | §1.12, §4.19, spec:205-207, CLAUDE.md §1, §7, §9, §12 | 092's queued half, closed
