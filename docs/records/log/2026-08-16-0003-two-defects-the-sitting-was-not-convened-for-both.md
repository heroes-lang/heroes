2026-08-16 | **Two defects the sitting was not convened for, both queued** (`docs/panel/067` § what it found). **`ptr` and `cstr` cannot go in any container**: `[ptr]`, `[cstr]`, `{i64: ptr}`, `{i64: cstr}`, `{ptr: i64}`, `{cstr: i64}` all pass `heroes check` at **exit 0** and abort at **exit 134** — *"entered unreachable code — this is a compiler bug"*. Cause: `emit/descriptors.rs`'s `_ => None`, whose doc says *"which `gate.rs` has already refused"*, **a dead premise** — the gate refuses only `Ty::Unit`. Panel 029 filed three types and the fix covered one | CLAUDE.md §11's class and §1.12's severity, and the **same shape** as the option-payload defect this port fixed hours earlier: a narrowing resting on a claim about the world rather than about the value. The second defect is its own test: the case named after it carries `[ptr]`, `{str: ptr}` and `[[ptr]]`, but its `[()]` line fires first and stops the build, so **the live half is never reached** — §9's `#~` invariant proves where a diagnostic points and cannot prove that the lines after the first firing one ran | crates/heroes/src/emit/descriptors.rs · tests/golden/unsupported/fixedbugs-container-element-reached-clang.hero | 029, 067 |
