2026-09-08 | **Closures are refused on cost alone, inline blocks are left deliberately unplaced, and the rule that would warn the reader is the one that does not fit** (M-closures-verdict step 1; panel 119, five seats, provisional pending ratification). **The count the sitting was owed, and design.md's own sentence about it, are both settled by measurement.** Part 8 wart 1 says the one-line helpers *"exist mostly to be passed around"* and Part 7 item 1 says closures *"will delete the handful … that currently exist only to be passed around"*; under that second, narrower wording the whole tree holds **21 declarations, 66 lines, every one in an example program**, four of the seven files being programs whose subject IS the higher-order function, and the compiler — 190 modules, 55,361 lines — holds **zero** counting every use and **one** counting production uses only. It passes a function as a value at **4 production sites**, none of them capturing, with 13 of its 17 value sites being `test` doubles. The instrument is `.func_ref` in the IR, validated **78 against 78 with zero disagreement** over the 150 modules that build standalone, and the first census was thrown away for pooling names across files and reporting 217, most of them locals called `at`, `after` and `env`. **Three vetoes and four compiled corruption classes.** `hero_desc_func.size` is 8 and a closure over one `i64` and one `str` is 32: the cast at a real `sqlite3_busy_handler` is **clang exit 0 with zero diagnostics under all fourteen flags and exit 139 at run time**; the array route is a heap-buffer-overflow with 24 of 32 bytes silently truncated; `hero_copy_func` is a bare assignment with `hero_drop_nothing`, so a captured `str` is use-after-free; and `hero_eq_func` reads 8 of 32, so either field order answers EQUAL about a case it should not. The compiler seat made the compiler count the surface rather than reading it: **282 exhaustive-match arms in 74 of 190 modules**, five DECIDED file ceilings breached, and **no legal home for the parser** — a separate module gives `error[module_cycle]`. **The reader-facing cost is the `@` cell and it is unrefusable.** A `total: i64 @ 0` captured by copy freezes at creation; a write to it is refusable and a read is not, because whether the frozen value was wanted is undecidable — and **the only draft of the clause that states this costs +118 against 71 free tokens**, which is the sitting's title. Two seats reached that class from inputs that do not overlap, the ergonomist holding only the spec. **The historian's null result came back not null**: four languages under this one's exact constraints refuse capture in writing — Zig (#229, 2020), Odin, C3, and Oberon since 1988, which self-hosts — and the single most decision-relevant fact is about the CHEAP option, `ziglang/zig#1717`, the capture-free anonymous function, **accepted in 2020 and rejected in 2023**. It also **corrected the panel's framing**: the historical hazard is capture by REFERENCE, which this language cannot express (Go 1.22 and C# 5.0 both shipped breaking changes for it, Java 8 forbade mutable capture outright), and no language that shipped capture by copy has reversed it — so the Part 6 row's falsifier deliberately rests on compiler need and a Part 11 effect rather than on copying being dangerous. And **no study in any language measures how much code closures deleted**: the literature counts sites converted and lambdas written, so this sitting's 21 is the only measured number in the room. **What the sitting found that was not on its agenda**: the spec writes `map`'s signature and no other while naming `filter`, `fold`, `find`, `any` and `all`, the four missing ones cost **+63** and therefore compete for the same 71 tokens as the closure clause, `find`'s `-> A?` being the one no reader can derive; both live statements of where a function type may cross the FFI are wrong, the real answer being **four positions refused and one admitted**; and two defects, 018 and 019, filed rather than repaired — `<` naming two of the ten types it accepts, and the compiler's own `guess` fix offering `fit_<width>(x)`, a family it shipped and withdrew. **One repair belongs to no option and is measured free**: `-Werror=cast-function-type-strict` catches the exit-139 cast where `-fsanitize=function` does not, and the seed, the runtime, the thread example and the SQLite binding all compile clean with it | `docs/measurements/020-four-sites-in-fifty-five-thousand-lines.md`; `heroes measure` 3965 of 4096, 71 free; timings `/usr/bin/time -p`, seed 3.09 s, `heroes check selfhost/main.hero` 13.17 real against 12.96 user | design.md §1.0, §1.1, §1.2, §1.6, §1.7, §1.11, §1.12, §4.13, §4.19, Part 5, Part 6, Part 7 items 1 and 12, Part 8 wart 1; CLAUDE.md §4, §12, §13 | 119 (013's condition discharged and sharpened; 039's unplaced shape reused; 043 and 017 for `fit_`)
