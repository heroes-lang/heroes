2026-09-08 | **Panel 121 adopts string interpolation and refuses the spelling it was asked to adopt: a brace may not be active inside an ordinary string literal** (M-interpolation-verdict, five seats; `docs/panel/121-the-brace-was-already-taken.md`). The author decided the form ENTERS and raised the ceiling to fund it, so the sitting was asked the SHAPE, and two seats vetoed on grounds the ballot had not contemplated. **The unanimous half: 211 brace-bearing literals in 39 of `selfhost/`'s 190 modules, nearly all diagnostic text naming a map type, stop parsing under an ungated brace, so the compiler stops compiling itself** — a Principle 0 regression and not a price; the historian could source **no** brace-hole language that makes a bare `{` active in an unprefixed literal, and the spec-warden found the two-stage bootstrap hazard, that the old seed reads `{{` as two braces so doubling `selfhost/` before the compiler understands doubling corrupts every diagnostic that passes through it. Gating erases all of it: a plain literal is unchanged, the 312-literal migration goes to zero, and the lone-brace silence closes. **Adopted: any expression in a hole, with WG21 P3412R3's scanner rule stated in the spec** (*"scan for the first `}` … skipping over nested parenthesis, curly brace and square bracket pairs as well as nested string and character literals"*), because the ergonomist's locality veto rests on a literal spanning lines and **the compiler refuses that** — `error[unterminated_string]: strings are single-line` — while the spec never says so, the third instance this milestone of a silence the compiler already fills. **One node carrying the whole literal's span, desugared in LOWERING**: the parser route is self-defeating because `print/fmt.hero:1266` prints a literal by span and a `.binary` node structurally, so `heroes fmt` would erase the form from the project's own tree, and the cheap payload-on-`.str_lit` route would leave 59 walks treating an interpolated string as a childless leaf with no compile error. Costed: **4113** tokens, ceiling **4253** rather than the 4224 confirmed, because 87 free was nine more than the spread and not one | a sitting asked only about the shape found that the shape it was given could not compile the compiler, and the axis that decided it, whether the literal is gated, was in nobody's ballot | §1.2, §1.6, §1.7, §1.12, §2, §4.19, Part 5, Part 7 item 7 | 006, 008, 012, 024, 046, 119, 121 |
