2026-09-14 | **The mark lands in both positions, the spec ceiling moves to 10240 by author decision, and the AST dump turns out to have been eating `consumes` for a day** (M-marked-acquisition step 4, panel 148 R2 to R4 landing). **`acquires <releaser>` is in the language**: it parses on a handle result and on an `@` out-parameter — the two positions `owned` already rides — requires its name with `error[expected_releaser]` carrying the shape, round-trips through the formatter, and prints in the AST dump. The runtime gains its **fourth** counter beside the three that were there, and it is a COUNT rather than a header because a C handle is the one resource the runtime never allocated and can never size. **It is measured in both directions and they are two different faults**: positive accuses the PROGRAM — `panic: 1 C handle(s) never given back`, exit 134, on precisely the shape `docs/measurements/030` had measured as **exit 0 in silence** — and negative accuses the BINDING. **The second message exists because the first version killed a correct program**: `examples/curl/main.hero` has carried `consumes` since panel 145 landed it on 2026-09-13 and had no `acquires` to pair it, so the count went to -1 and the program died saying *1 C handle(s) never given back*, the exact opposite of what had occurred. The repair to the corpus is **one word**, and it names what gives the handle back | **The ceiling is 10240 by author decision, taken while this text was landing**, with the instruction that runs beside it: *privilege the most robust solution over the one with fewer tokens*. **It is 10 × 1024 and not 10000**, and that is not a style choice — `site/src/lib/claims.ts`'s `ceilingK` refuses a ceiling that is not a whole number of K because the pages say it in K, so 10000 would stop the site's build by construction. Moved in **six live places**, each with the reason beside it, and **the site needed none of them**: it derives the number by reading `constant CEILING: i64` out of the suite rather than restating it, which is one-number-one-home working. Two `8192`s were left alone on purpose — in `runtime/parts/dir.c` and `selfhost/cli/io.hero` that figure is a buffer size, and a blind substitution would have corrupted both in silence. **The spec itself is +82 vendored and +115 real**, 7806 to **7921** on `claude-opus-5`, a ratio of **1.40** that lands inside the 1.20-1.45 band panel 148's warden gave while refusing to use any single factor — which `docs/measurements/010` forbids in terms. The ledger's row carries the payment, and it is a **registered prediction** and not a removal, because no removal exists and the warden said in the sitting that a saving against a draft is not one: the shipped tree needs exactly three `acquires` marks and no fourth, instrument `grep -c` plus `heroes build examples/`, scored at this milestone's close | **And the walk found a defect a day old that no suite could see.** `heroes parse --dump-ast` printed `function curl_easy_cleanup(handle: Curl) -> ()` for a program that says `consumes` — the word gone, exit 0, no diagnostic, since 2026-09-13. **The comment one line above the hole had already described the damage**, about a different word: *"this dump is what `heroes fmt` compares its own output against, so a form missing HERE makes the formatter's self-check agree with a formatter that is deleting it — which is exactly how `as` shipped."* The guard and the guarded had the same blind spot. **Why nothing caught it**: the dump's own fixture carries `owned free` and `owned drop` and neither new mark, so the case that would have failed did not exist; it now carries both. **And CL-036 fired twice more in one day on one form** — the formatter dropped the mark off a PARAMETER in the pricing prototype and off a RESULT an hour later in the real tree, each time silently. Three times in one day, one rule, one form | **What was deliberately not done, and it is a choice rather than an omission.** Neither thing that COLOURS a program knows any of the **eight** contextual marks — `owned`, `consumes`, `acquires`, `tag`, `partial`, `link`, `package`, `as` — because the site derives only KEYWORDS from `selfhost/keywords.hero` and a contextual mark is deliberately not one. Adding `acquires` to two files and leaving seven uncovered would make the gap harder to see, so the CLASS is filed with what `.claude/rules/diagnostics-and-goldens.md` already says is owed: a check comparing each highlighter's word list against the compiler's own tables, which has a home beside `suite_spec.hero`'s reading of `spec/reserved-words.md`. **Build the check and the colouring follows from it** | design.md §1.6, §4.15, §4.19, Part 6; `spec/heroes-spec.md` § 13; CLAUDE.md § 4, § 11, § Verification; CL-036, CL-057, CL-063; `docs/measurements/010`, `030`, `031`, `032`; panels 145, 147, 148 | 148
