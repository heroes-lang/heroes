2026-09-14 | **Panel 147 is ratified in full on the day it sat, and the sitting's briefs get a home that outlives the session** (author decision, both items verified against the repository before being put). **All six resolutions stand as adopted and the author took the ROBUST one**, so R5's conservative alternative — Route C, refuse, with a falsifier redrafted on the corrected census — stays in the file as the record of what the other answer would have been and nothing else, which is what CL-040 asks of a sitting that takes robust over conservative. **R1**: a releaser keyed on the handle TYPE is refused on its axis and not its spelling, with a design.md Part 6 row naming the falsifier that would make it wrong — a header-derivable signal separating a C function that gives ownership from one that lends it, which panel 139 measured does not exist, `sqlite3_column_count` not being const-qualified so a mutating step and a pure read have the identical signature. **R2**: a scope-bound statement does not enter as drafted, and would be block-scoped if it ever did. **R3**: refusing outright is not taken either, C having adopted `defer` this year in TS 25755. **R4** is the one that decides the next milestone: a form enters, its axis is the acquiring **call**, and **the compiler never picks the release call**. **What the yes does not settle** is the spelling, which the milestone file forbade the sitting from choosing, and which of the two instruments wins — a `consumes`-shaped mark at 114 code lines' precedent, or escape refusal whose machinery ships twice already — which `M-marked-acquisition` prices rather than inherits | **And the second item is a rule that had an executor, a ratification, and still was not being performed** (panel 140's question, found at 147 while performing it). The decision of 2026-09-13 recommended `docs/panel/NNN-briefs/<seat>.md` and was ratified *as adopted, nothing changed* — and what had been adopted in `/panel` step 2 was the **session scratchpad**, which is a different place and does not do the job the first was asked for: a scratchpad is session-specific and **goes away with the session**, so panel 140's own finding, *"the briefs are prompts; they vanish when the workflow ends"*, was still true of panels 145 and 146 four days after the rule meant to answer it was ratified. Measured on the day it was settled: `find docs/panel -maxdepth 1 -type d` returned **one** directory and it was from 2026-08. **The skill is amended by author instruction**, which CLAUDE.md § 4 says is how a skill changes and no panel: the committed home is named, the seats' reports go beside the briefs at `docs/panel/NNN-reports/`, and the historian's is written out by the coordinator because that seat has no write tool — which panels 143 and 144 both recorded as unauditable and neither fixed. Cost at the first sitting to do it: one `cp`, 2396 lines across twelve files | **The consequence was found by the instrument the same day, and the predicate had been wrong since it was written.** `is_sitting` in `tests/harness/suite_records.hero` read every `.md` under `docs/panel/` as a sitting — prefix, `.md`, first character a digit — so `docs/panel/000-plan-review/README.md` has satisfied it since the day that file was created, and was skipped only by `VERDICT_FLOOR` because its number is 0. Panel 147 is above the floor, so **twelve briefs and reports were asked for an author's verdict** and a predicate masked for a month answered out loud. Narrowed to *a file directly in that tree*, which is a fact about the path in hand rather than about what the tree happens to contain, with a test asserting all four clauses — **and the new clause was verified to fire by removing it**, `assert failed: !is_sitting("docs/panel/147-briefs/00-shared.md")`, because a check nobody has seen fail is a decoration (CLAUDE.md § 9). The alias table's pinned row count moved 81 to 82 in the same run, which is the third time this close that a number fixed in a test caught a document changing under it | CLAUDE.md § 4, § 9, § 11, § 12; CL-003, CL-037, CL-040, CL-044; `.claude/skills/panel/SKILL.md` § 2; `tests/harness/suite_records.hero`; panels 140, 143, 144, 145, 146, 147 | 147, 140
