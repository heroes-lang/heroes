2026-09-05 | **The callback permission reaches the document, at the bound the spec-warden predicted and with nothing to spare** (M-c-callbacks step 3; panel 111 R4). One paragraph replaces `Neither may be an absolute path.`: *A callback is a **parameter**, never a result; its parameters follow the same rule and `()` is `void`: `atexit(f: (function() -> ()))`.* **Two sentences and two jobs**, both named by R4: the spec-warden's **parameter-only restriction**, which the proposal's own longer wording never mentioned and whose colon was orphaned — it introduced a spelling and pointed at an example holding no function pointer — and the ergonomist's, which is *"the only thing that makes `atexit` writable from the document alone"*, because without it a reader knows a callback may be passed and not at which widths, nor that a `void` callback is spelled `(function() -> ())`. **Field position is not mentioned and costs zero**, the spec's field list being closed and already refusing it in writing. **Measured across four wordings rather than estimated once**: 3799 → **3830**, gross **+39** against a measured **−8**, net +31, headroom 266; the first draft read 3838 and was cut twice. **The removal was verified by running, not taken from the sitting**: an absolute header and an absolute `link` are both `error[machine_locked_path]` at exit 1 on the group head, and the message carries the entire rule plus three repairs — `CPATH`, `package`, and *`link` takes the library's name* — so the sentence is §1.4 redundancy the compiler pays back loudly, which is panel 089's shape and the only condition under which redundancy is repayable at all. **The payment was already in the record before the sentence was written**: panel 111's spec-warden registered *at M-isolated-threads close, `heroes measure spec/heroes-spec.md` reads ≤ 3830*, falsified if the callback's own RESULT width needs stating since *"a result may be wider than C's"* reverses for a function C calls back. This lands at **exactly** 3830 — the bound, not room under it — so any further callback text falsifies it, and the ledger row says so rather than leaving it to be found. `suite_spec.hero`'s `SPEC_TOKENS` and `LEDGER_ROWS` move to 3830 and 52 in the same commit, which is the whole reason that instrument exists | §1.6, §1.2, §1.4, §4.19 | 111 R4, 089, 012, 046
