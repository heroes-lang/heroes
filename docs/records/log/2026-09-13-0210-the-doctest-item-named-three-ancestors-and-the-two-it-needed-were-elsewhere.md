2026-09-13 | **Doctests are deferred again, the expired clock is struck, and the item's three named ancestors turn out to be the wrong three** (panel 136, M-deferral-ledger step 2, full five seats, no veto on the form and one on a route; `docs/panel/136-the-item-named-three-ancestors-and-the-two-it-needed-were-elsewhere.md`). The ledger's second verdict, on design.md Part 7 item 6, written into both places the item lives — the Part 7 line and §4.18's substantive paragraph — as corrections beneath the originals. **The stated condition is MET and triggered nothing**: *"until the first mechanism is proven"* against 641 `test` blocks in `selfhost/`, 545 in `examples/` and the compiler's own tests being one of the three suites. A condition that expires without waking anybody is the promise without a date this milestone exists to end, so the clock is struck rather than re-wound. **What survives is not a clock.** §1.7's subtraction returns **zero** — no `test` block, of 641, is deleted by a fence — and the *"form budget"* the paragraph rests on is **defined nowhere in this repository**, its own sentence being the only occurrence; the rules it reached for are §1.7's subtraction test and §4.15's *exactly one correct way*, now named instead. **The measurement that decided the sitting was found three times without conferring**: the whole tree holds **one** markdown fence inside a comment, `selfhost/parse/group.hero:33-36`, it documents `function extern_group` and holds an `extern` group — the compiler seat ran it as statements and got `error[expected_expression]` at column 5 exit 1, so the proposal stops the compiler compiling itself the day it lands; the FFI seat reproduced that and found a **second, independent** failure at name resolution, since the module documenting it declares no extern; and the warden reached the same place from the grammar, an `extern` group being a Declaration and never a Statement. The fence in this project is a **form exhibit**, which is why: at the FFI what is worth showing beside a signature is a declaration, and §4.19's own illustration and spec § 13's are both that shape. **The three ancestors are corrected.** Rust pays its own published 775 seconds of compile time for one crate's doctests and eight attributes of dialect `#[test]` does not need, having changed the default twice; Python's own docs warn that one character breaks a doctest; **Elixir is not a third mechanism at all**, `doctest MyModule` being a macro that generates ordinary ExUnit tests, opt-in per module. **The two the item needed were elsewhere: Go and D**, which obtain the identical stated benefit with no second test mechanism, because the example is a test and the doc tool reads it — the direction Part 6 already chose in writing. **So the form that returns is not the doctest but the documented test block**, homed at M-doc-generator, which is also the missing consumer: no `heroes doc` exists, so a doctest would today be the only reader of a doc fence, *"Rust's mechanism without Rust's reason"*. Four return conditions, any one reopening it: 641 falling to ≤ 591 under a candidate implementation with the suites green; four or more fences whose first body line is not a declaration keyword, today zero of one; ≥ 50 fences with a measured ≥ 1-point kill-rate gain from `heroes mutate`; or M-doc-generator shipping the Go/D form. **Found beside the sitting and filed at the milestone that owns panics**: a failing `assert` names no file and no line (`runtime/heroes_runtime.h:300-301`), so what locates a failure is the author's title — M-panic-location's 2026-09-03 census measured `hero_panic` and named four abort classes, and the assert path is a fifth surface it did not name, which `selfhost/emit/ffi.hero:58` records the compiler's own authors hitting | **Why deferral and not the bolder or the cheaper verdict, and what the seats gave the sitting that a cheaper one would have lost.** ENTERS would land a form that breaks the self-build on day one; a bare re-deferral would leave the expired clock in place. The reader's seat counted the form at **222 characters against the test block's 88**, 2.52×, and found the shape that looks checked and is not — a fence above a `use` line, which spec § 1's grammar makes not a declaration, so the clause never attaches and the assert is false forever. The FFI seat measured what a doc example costs at the boundary by running one: an abbreviated example, which is what documentation is, fails `SQLITE_MISUSE` because it omits the return-code check, so an FFI doctest must become a resource-correct program or the build goes red. It also **withdrew one of its own concerns after measuring**, a doctest adding no link the module's extern group did not already impose. Conservative recorded, not adopted: the same deferral with the clock left in place and only a date added | design.md §1.0, §1.2, §1.6, §1.7, §4.15, §4.17, §4.18, §4.19, Part 6, Part 7 item 6; CLAUDE.md §2, §4, §9, §12, §14 | 136
