2026-09-13 | **A call that ends a value's life may not be handed one the caller borrowed, defect 031 closes, and both lists reach zero** (M-handle-verdict step 5, on panel 145's own return condition). **`consumes` after a C parameter** says the call ends that value's life; a parameter written without `@` is **borrowed**, the caller keeps the value and reads it again, so handing one to the other is a promise the signature cannot keep. Defect 031's filed reproducer — `free(h.cell)` inside `closed(h: Holder)` — is now `error[consumed_borrowed_handle]` on the line that frees. **Panel 145's condition is met exactly**: *a prototype at ≤ 150 code lines refusing the reproducer in `heroes check`*, and it is **114** in `selfhost/check/consuming.hero`. The rule is LOCAL: the argument's place root, and what the resolver says that name is — a `.param` local that is not `mutable` is a parameter written without `@`, and nothing else in the language is — so no flow analysis, which `check/leasing.hero` states the checker does not have | **The measurement settles a contradiction between two seats of that sitting, and adds a cost neither counted.** The compiler-engineer predicted the rule would refuse **0 of the 17** correct calls in the shipped SQLite binding; the completeness critic read the code and said **2**. Run: **2**, and they are the two the critic named, `closed(db: Db)` and `finalized(statement: Statement)`, both repaired by `@` exactly as step 1 repaired `stepped`. **The third cost is that the mark PROPAGATES**: `@` on those two wrappers obliged every caller to hold a cell rather than a binding — **24 lines across `examples/ledger/`** for two marks, 2 signatures and 13 call sites and 7 bindings, with output byte-identical and sixteen tests green. A refusal rate of 2 of 17 reads cheap and the adoption is twelve times that | **And the 2 of 17 fired a falsifier somebody else had written.** design.md's Part 6 borrow-checker row named its own: *an annotation a binding author writes once per function, with a measured refusal rate on `examples/` below the 13 of 17 above*. This mark refuses 2. **So that row's ground moved from soundness to cost the same day**, in its own words, and the row now says so with the three costs named: the affine handle that would close the class rather than the call is core by §1.7; the mark propagates; and the signal can only come from the binding author, since panel 139 measured that no header-derived rule separates a call that invalidates a handle from one that reads it | **What closed and what did not, measured rather than assumed.** Repaired to `closed(@h: Holder)` the program compiles again and `--sanitize` **still** reports `heap-use-after-free`, because a copy made before the call holds the freed address. The defect as FILED is closed; the class is design.md **Part 8 wart 20**, which now states it in the present tense with this measurement written into it. That is what a wart is for, and the diagnostic's second note says the same thing to the reader who meets it | **Two repairs the sitting charged to step 4 land here too.** `owned` on a non-`cstr` parameter is refused **where it is written** — it used to be retyped to `str?` in silence, so `ptr owned f` came back as two diagnostics about a string on a program that had none, at the call rather than at the declaration; three of panel 145's five seats found it independently. And `ffi_unknown_tag` **stops claiming what clang cannot tell it**: it said *the header declares no `struct X`* where clang had only said *incomplete definition of type*, which is the same message for a misspelled tag and for one that is opaque on purpose. It now says what clang said and carries both repairs, the second being the handle form that landed a step earlier | **The formatter dropped the new mark in silence until it was taught**, measured the hour the word landed: `heroes fmt` on a binding stripped the safety mark off it and said nothing. That is CL-036's whole sentence — a tool that has not learned a new form does not error, it drops it — and it is why `print/fmt.hero`'s `DECIDED` ceiling went 1150 to 1156 with the reason written beside it rather than the change being squeezed in. **Still owed and recorded rather than done**: the two things that COLOUR a program know none of the contextual marks — `owned`, `tag`, `partial`, and now `consumes` — which is the gap CL-036 already carries, and adding a word without a test that judges it is what that rule warns against | **Both lists are at zero for the first time since the ledger opened**: `docs/work/DEFECTS.md` **0 open**, `docs/work/DECIDE.md` **0 open**. So `m-deferral-ledger`, withheld on 2026-09-13 because `records/tagged` allows no tag over an open defect, lands on this commit — the way `m-cstr-lifetime`'s did on 2026-09-09, and on the precedent M-anchored-spec set at chain rows 45 to 47 | design.md §1.7, §1.12, §4.19, Part 5, Part 6 borrow-checker row, Part 8 wart 20; `spec/heroes-spec.md` § 13; CLAUDE.md § Precedence, § Verification; CL-036; panels 139, 145 | 145 (139 inherited)
