2026-09-12 | **A grammar is proposed, and five seats falsify the same half of it** (author request: a specification file beside the prose one, in BNF or some super formal shape, and explicitly without regard to the token count). The proposal was `spec/grammar.md` in Wirth's EBNF, outside §1.6's budget on `spec/reserved-words.md`'s precedent, kept honest by four checks reading the compiler's own tables. **Panel 133 adopts a split by derivability instead**: `heroes grammar`, a subcommand, prints the half that is data (the 21 keywords, the token spellings, the nine precedence levels **with their `ast.BinaryOp` names**), and the productions — the half that is not data — do not ship by hand at all. The question of whether they enter `spec/heroes-spec.md` inline, with `spec/grammar.md` generated as the collected view, is a §1.6 ceiling decision and is queued as `panel 133` | **The sitting's own premise was false, and the seat that reads only the spec found it**: design.md Part 7 item 16 says *"construction stays unspecified, so a program written from the spec alone cannot build a variant"*, and `spec/heroes-spec.md` § 9 has said how to construct one since **2026-09-11** (`git log -S` → `d245239a`, panel 126's re-shaping, one day before this sitting). The llm-ergonomist wrote `return .circle(r: 3)` from the spec alone, citing that clause, before it was allowed to open the grammar; 0 of its 3 programs changed afterwards. **Five independent falsifications of hand-writing the productions, from five different inputs**: the ergonomist found four wrong productions including one that cannot derive the document's own example, because an `if` or `match` ends with a DEDENT and not the NEWLINE the production requires; the ffi-pragmatist found the productions narrower than the language (a multi-line `extern` parameter list, exit 0, underivable) and wider (`[ Generics ]`, derivable and dead, since `check/ffi.hero` refuses `.generic` at every crossing) and predicted the guarding check would pass green over it **forever**, because `parse/tails.hero` reads generics before the extern branch; the engineer measured 74 productions against 98 parser functions needing ~82 hand-written exception rows, and ruled that `suite_layout.hero`'s `DECIDED` does not license them, since a `DECIDED` row ratchets and has its length asserted where a name-only row is a mute button; the warden measured 1322 of 2903 tokens as hand-written prose carrying **five language rules absent from the prompt** (associativity, trailing comma, `T??`, absolute paths, `use` adjacency, all confirmed at 0 grep hits) and vetoed; the historian found the arrangement dead everywhere it was tried — Rust removed `src/doc/grammar.md` (PR #64896, 2019-10-01) and archived `wg-grammar` (2024-04-08), and **`ziglang/zig-spec`, this proposal feature for feature, was archived 2025-11-23** three years after its checker correctly reported six divergences nobody repaired. **Each of the four proposed checks would have been green over at least one defect**, and `grammar/powers` would have passed panel 067's own defect class: the draft names `%` and level 9 and never names `.rem` (`BinaryOp`, `.rem`, `.add`, `.mul`: 0 occurrences), so `.percent => .add` is invisible to it. **Two corrections the sitting owes older records**: Wirth shipped the grammar **twice** in both the Oberon and the Pascal reports, inline and again as an appendix, so *"the prose plus a compiler diagnostic beats a second description"* is a departure from him rather than an application; and panel 024 read Aycock et al. (arXiv 2409.19151, ICLR 2025) backwards — its *"almost all improvements stem from parallel examples"* is about **translation**, while on grammaticality judgment and glossing its best arm was a compact structured description **plus** examples, +25 points on morpheme accuracy over the no-examples arm | CLAUDE.md §4, §10, §12; design.md §1.6, §1.7, Part 7 item 16, §4.19; CL-018, CL-040, CL-057 | 024, 035, 067, 126, 133 |
