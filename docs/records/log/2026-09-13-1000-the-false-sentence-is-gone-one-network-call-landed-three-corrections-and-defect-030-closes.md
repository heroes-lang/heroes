2026-09-13 | **The false sentence is gone, one network call landed three specification corrections, and defect 030 closes** (M-handle-verdict step 1; panels 139 and 144's spec halves, adopted at their sittings and blocked behind `heroes measure spec/heroes-spec.md --refresh`, the one command in the compiler that reaches the network; the author authorised it the evening M-deferral-ledger closed, in answer to a question that named all three corrections). **Three edits to `spec/heroes-spec.md`, each measured alone, and one removal.** § 3's *"No aliasing exists anywhere"* becomes panel 139's merged wording — *"No aliasing exists among the values this language owns. A `ptr` is a copied ADDRESS, wherever it sits: two copies reach one foreign thing, so a function taking one without `@` may still change, or free, what C holds. A `cstr` copies an address too, and only a group's `record` may hold one"* — **+75 vendored**. § 10's *"grows in place while nothing else holds `xs`"*, which panel 144 measured **true and insufficient**, becomes *"grows in place when `xs` is a plain name; reached through a field or an index it copies the whole array, so lend the array itself to an `@` parameter or hoist it into a name"*, **+35 vendored**. § 4 loses its restatement of the four kinds, **−43 vendored** (priced −54 at panel 139; the rule sentence stays and only the half the code block and the `Declaration` production already say goes), and the `names` and `offered` checks the sitting flagged as unrun against it **pass**. Net **+67 vendored, 5655 → 5722**; on the reader's instrument **7531 → 7610, +79**, digest `196e3bfd0166e373`, **582 free** and 522 net of the FFI floor | **Defect 030 closes on panel 139's four acts, not on the sentence alone.** The descendants moved with it: the `aliased_mutable_arguments` note and the `writes.hero` comment that quoted the old sentence, three runtime comments and three golden headers — the goldens corrected UNDERNEATH with the date, being records, and every one of the eight about a value the language owns, so no test changed meaning. **`examples/ledger/db/sqlite.hero`'s `stepped` takes `@statement`**: thirteen lines across two files as the FFI seat priced it, the compiler catching the two call sites the coordinator's first pass missed with a `fix (certain)`, and the example running to byte-identical output with all sixteen of its tests green. The marker changes nothing in C, which is the point: the signature stops promising what it cannot keep. It is one of thirteen by-value wrappers in that file and the one that shipped the counterexample | **Two things the landing found that the sittings had not.** `tests/harness/suite_spec.hero` held **one** `REAL_TAKEN` date for **two** independently refreshed documents, and went red the moment the spec's count was re-taken and the contract's was not — `selfhost/measure/pinned.hero` had carried `SPEC_REAL_TAKEN` and `CONTRACT_REAL_TAKEN` separately all along, so the harness is brought level with the compiler's own record and the coupling is named in a comment. And **panel 139's spec-warden prediction is falsified**: *≤ +45 real, lands ≤ 7525* against a measured **+79 and 7610**, for two nameable causes — the sitting adopted the merged wording at +75 where the warden's own W2 was +33, and the landing carries panel 144's § 10 correction the warden could not have known about | **What paid**: the named § 4 removal for part, and CLAUDE.md §12 for the rest, the shape panels 128 and 129 used — a document that says something false about what a copy is, or something true-and-insufficient about what a push costs, manufactures the defects the corrections close. **What this does not close, said rather than implied**: 031, the same address freed through one copy and written through another, which the new sentence names and no rule refuses; and 029, the swapped handle. Both are M-handle-verdict's sitting, next | spec § 3, § 4, § 10; design.md §1.6, §4.10, Part 6 borrow-checker row; CLAUDE.md § Hard stops, §12; `docs/measurements/010` row 70 | 139, 144 (128, 129 inherited)
