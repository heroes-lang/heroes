2026-09-13 | **Variant constructors as values are refused, every testable clause of the row is struck, and a completeness critic finds the route five seats missed** (panel 138, M-deferral-ledger step 4, five seats plus a critic, two vetoes engaged; `docs/panel/138-the-item-named-a-parser-that-never-existed-and-the-route-was-already-in-the-grammar.md`). The ledger's fourth verdict, and the third in a row whose item was falsified before it was judged. **The row names a parser this repository has never had**: the compiler's expression parser has been precedence climbing, one `binary(@c, @a, text, min_power)`, since the commit that first built it — **2026-08-04 at 04:10, the day the item was written** — and `git log -S` over all history finds the `term`/`expression` pair only in `examples/`. It is **one example program written twice**, and the spreadsheet is not a third instance, its pair returning `f64?` and building no variant. *"Identical except for two names"* is three by one seat's count and four by another's. And *"would unify into `sequence(@p, is_times, .product)`"* **would not compile even with the feature granted in full**, because **a function type cannot carry `@`** — `error[expected_type]: expected a type, found `@`` — so the sub-parser is not passable and the spelling is one parameter short. That last fact also falsified the coordinator's own brief, which had told five seats that two of the three differing names were already values; the engineer compiled it and the coordinator reproduced it, and the correction is in the sitting rather than quietly made. **Two vetoes and no seat in favour**: the compiler seat on §1.7 — a case lowers to a `.construct` shape so **no C function exists whose address a case value could take**, `callback_guard.hero:77-82` indexes the thread guard **by declaration** which a synthesised constructor has none of, two files sit at exactly their DECIDED ceiling, and the subtraction is **zero** wrappers in `selfhost/` and zero in `examples/`; and the reader on locality — the spelling contradicts § 9's own `:238`, which already makes `.plus` a value, so one bare form would mean a value or a nullary function depending on the expected type, and a stored case would compare **as an address** where a case value compares structurally. Precedent runs the same way: **OCaml refused for 25 years** and stalled on exactly this language's case, *"curried, tupled, or labelled"*; **Swift ships it and drops the labels**, SE-0111 forbidding them in a function type where this draft keeps them; **Scala 3 un-shipped it** and its migration guide recommends the hand-written wrapper. **What the corpus wanted was compiled instead**: dispatch the constructor with an exhaustive `match` on the value already in hand, which deletes both wrappers, needs no feature, and is **strictly safer** — adding a case makes the dispatch `error[non_exhaustive]`, a compile error a function value would not have caught | **The critic is new to this panel and earned its place on its first run**, which is the process finding of the sitting. It found **`Expr::product`** — the `::` operator, which since panel 132 already means a member named on its declaring type and checked against the declaration — a route **no seat had named**, and which closes every silence by construction: `.plus` keeps one meaning, the variant is named so nothing depends on the expected type, and `xs.map(Expr::number)` is decidable where `xs.map(.number)` is not, that last being the feature's headline use and a fifth silence nobody had written down. It is recorded and **not adopted**, because no seat measured it and a sitting does not escape a measured refusal into an unmeasured route; its floor is measured — `Expr::sum` is `error[variant_in_value_position]` today, and panel 132's own landed price was +388/−95 across 28 files. The critic also **settled a contradiction between two seats** by reading the disputed file: the warden's third wrapper takes no parameters and has two statements, so the engineer's count of zero stands and the warden's break-even arithmetic rested on an over-count; it caught that a cited command used Rust syntax, `fn term(`, where `function term(` is Heroes; and it caught that **all five seats judged the feature while none judged the row**, which is why this sitting strikes the text as well as ruling, the method panel 137 used for traits. A sixth seat would have added a sixth opinion; a critic added the questions | design.md §1.0, §1.7, §4.3, §4.5, §4.12, Part 5, Part 6, Part 7 items 9 and 16; CLAUDE.md §2, §4, §12, §14; CL-005, CL-018, CL-036, CL-061 | 138 (132, 119 inherited)
