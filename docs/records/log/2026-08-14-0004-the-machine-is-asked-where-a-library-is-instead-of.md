2026-08-14 | **`package` — the machine is asked where a library is, instead of the program being made to say** (author decision, `docs/panel/050`; retro-record, the evidence being panels 047–049). +123 spec tokens on the author's instruction to spare no expense on the FFI, and it **subsumes the platform axis panel 049 refused at +144** while putting no machine's name in any program: `pkg-config` answers with `-framework Cocoa` where that is the answer and `-lGL -lX11 -lm -ldl` where that is, from one spelling. And it says the thing no proposal in three sittings could say at all — `-I` and `-L` — which four independent measurements had found to be the actual blocker at §4.19's ladder rung 5, and which design.md:2058 had recorded a milestone before anyone looked. **The rung is climbed**: `examples/raylib/main.hero` opens a window in a clean environment with no platform token | **the allow-list is the feature, and it is here on the first day rather than after a disclosure.** A `.pc` file is input this program did not write; Go shipped the same idea without a filter and it became **CVE-2018-6574**, a repository shipping `attack.so` beside `#cgo CFLAGS: -fplugin=attack.so` and `go get` loading it into the host compiler. Only `-I`, `-L`, `-l`, `-F` and `-framework` pass, the list is named in the spec because the list *is* the safety, and a package that answers otherwise is `ffi_package` at exit 1 on the author's line. **Windows was asked for and was therefore built rather than named**: the 18 mingw errors panel 049 measured were all in `runtime/parts/f64.c`, and the port makes the code better rather than merely portable — `uselocale` switches a thread's locale and must be switched back, while `_snprintf_s_l`/`_strtod_l` take it as an argument and remove the window. A whole Heroes program now cross-compiles to `PE32+`, and `windows-latest` is the third CI leg. Two POSIX assumptions fell with it, and the second was a defect the first had hidden: `/tmp` in two `.hero` files existed because `golden.rs` ran one leg from the crate directory and two from the root, so a relative path was a different file in each — the workaround had outlived its cause and the disagreement it hid was still live | CLAUDE.md §7, §10, §11 · design.md §1.11, §4.19 | 036, 047, 048, 049, 050 |
