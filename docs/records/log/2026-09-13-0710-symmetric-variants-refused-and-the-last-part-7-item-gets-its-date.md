2026-09-13 | **Symmetric variant syntax is refused, and with it the last Part 7 item the ledger owed a verdict** (panel 142, M-deferral-ledger step 7, five seats plus a completeness critic, one veto; `docs/panel/142-there-is-no-asymmetry-to-restore-because-there-is-no-destructuring-anywhere.md`). **Seven Part 7 items entered this milestone and seven now carry dated verdicts**: 5 `alias` and 6 doctests deferred with conditions, and 8, 9, 11, 14 and 16 refused to Part 6 with falsifiers. Five of the seven carried a stated reason that was false or expired. **This one is the sharpest, because its premise is false ONE LEVEL UP: there is no asymmetry to restore, since nothing in this language destructures.** A record pattern is refused by name — `error[expected_pattern]: expected a pattern, found a name (`Point`)`, compiled — so *build by label, read by dot* is a uniform rule that holds for records exactly as for variants, and nobody calls that asymmetric. A symmetric variant pattern would be the **first destructuring form in the language**, owing `Point(x: a, y: b)` in patterns too or leaving a new asymmetry where variants destructure and records do not: **the proposal creates the thing it removes**, and no seat saw it — the critic did. **The price of the symmetric spelling is a rule already here**: `.circle(r: r)` asks to bind the field's own name and shadowing is an error, compiled to `error[shadowed_binding]`, so the choice is between a reader inventing a second name at every pattern — where the same arm compiles or not depending on what the enclosing function binds, a **non-local intermittent** error — and exempting patterns, where an arm **silently captures** an outer name, a new silent class in a language whose thesis is that plausible mistakes are compile errors. The reader's own sentence: *"It does not merge two rules; it makes two rules look identical and mean different things."* **The item's last live claim died under the experiment**: given only the specification, that seat wrote all six acts first try — three constructions, three matches — with its longest hesitation at forty seconds, on a two-field *pattern*, settled by an inline production it noted it would have got wrong without. **The four cited ancestors do not survive checking for named fields**: Haskell's omission means ⊥ building and wildcard matching, Rust's `E::V { x }` is a use in one position and a binding in the other, OCaml patterns may omit where expressions may not, and **ML has no named-field data constructors at all**. **And the decisive precedent went the other way deliberately**: Swift's SE-0155 proposed labels required in patterns and its core team modified it on 2017-04-20 because *"requiring associated value labels would be unduly onerous"*, landing on exactly this language's asymmetry with the symmetric option written down in front of them; where symmetry was kept, Erlang's records and Python's class patterns, the documented price is that one omission means two different things. Symmetry is also a **cost** — +12 at best, +67 honestly stated — and §1.7's subtraction is **zero**, the two spellings being two algorithms that share a token shape rather than two copies | **"v2" is struck as a verdict this ledger can carry**, which is the milestone's own thesis applied to the last word left standing: a promise with no date and no return condition is what the ledger exists to end, and it was the only thing panel 133 had left of this item. **What returns is a sentence rather than a syntax**, the critic's route and the only one that ends the promise by writing a rule: the specification states construction in one clause and matching in another and **never states the contrast** — a grep returns nothing — so one clause at **+35** would convert a structural silence into a stated rule at zero productions, zero compiler lines and zero migration. Recorded, not adopted: §1.6's payment is unpaid and a verdict is not an amendment. **Three corrections to the coordinator are on the record.** The brief's construction count of 1457 could not be reproduced by three independent attempts, and the cause was found: the regex counted **every dotted call with a labelled first argument**, `p.slice(from: 0)` among them, rather than variant constructions — the true figure from the compiler's own AST is about **626**, while the match-arm count of **994** is exact and reproducible. A regex that looks like a measurement is CL-057's shape, and a seat restated the wrong number as fact inside a Principle 0 argument. **And two seats had their input polluted**: the reader reports that `CLAUDE.md` and two rules files *"were pushed into my context by the harness, unbidden"*, where its seat exists to see only the specification; it ignored them and said so, which is the correct handling, but the panel's differentiation claim reads stronger than it is while the harness injects the contract into a seat that must not have it | design.md §1.1, §1.7, §4.5, §4.9, §4.15, Part 5, Part 6, Part 7 item 16; CLAUDE.md §2, §4, §12, §14; CL-057; panel 035, panel 126, panel 133 | 142 (035, 133 inherited)
