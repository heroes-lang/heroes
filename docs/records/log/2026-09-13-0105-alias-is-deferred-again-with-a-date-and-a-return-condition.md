2026-09-13 | **`alias` is deferred again, this time with a date, a return condition and the three claims under it struck** (panel 135, M-deferral-ledger step 1, full five seats, no veto; `docs/panel/135-the-form-was-cheap-and-the-reasons-under-it-were-borrowed.md`). The ledger's first verdict, on design.md Part 7 item 5: **DEFERRED AGAIN**, written as an indented paragraph beneath the original so the record is corrected under itself. Principle 0 unmet on both branches — the compiler compiles itself with zero uses of the form, and no Part 11 effect is measured or measurable (metric 3 is blind to a transparent alias by construction, metrics 2 and 4 have no instrument). The token case run rather than argued: one alias for the six copies of a 12-token function type in `examples/interpreter/run/expr.hero` saves **48 tokens, 1.4%** of that file, the strongest case in the tree; `selfhost/`'s most repeated function type is generic and cannot be aliased, its most repeated spelling of any kind (`{i64: bool}`, 77) is six tokens long. **Two seats judged the form and found nothing to refuse; three judged the warrant and found none**, and that split is the resolution, not a compromise: Principle 0 is a burden on what enters, so the warrant seats decide the verdict and the form seats decide what the form is when it returns. **What the sitting settled for that day**: `alias Env = {str: i64}` (item 5's own `Env = alias` corrected, because § 4's *every top-level line starts with its kind* is a rule); transparent, *everywhere* reaching the constructor and `::`; an error writes both forms, Swift's `(aka …)` the one shipped default-on precedent; self-reference refused by an iterative resolver walk and asserted again in the checker so a missed cycle aborts and never overflows (§1.12); chains legal, the expansion alias-free; no generics; not a `Member`; `T??` through an alias re-refused in the checker because the parser can no longer see it (`selfhost/check/table.hero:66-67` rests on the parser today); route R2a — the alias erased at `selfhost/check/lower.hero`, where interning makes transparency one arm and keeps it out of `content_key`; `selfhost/print/fmt.hero` (room 3 of 1150) split first; `alias` reserved in the same commit as the spec text; the payable text **+26 vendored net**, paid by § 4's kinds sentence the `Declaration` production already states (−43) and § 3's *No aliasing exists anywhere* (−6). **Return condition, any of three**: a non-generic spelling of ≥ 12 tokens at ≥ 10 sites in one file (today 6); a commit changing one structural spelling at ≥ 8 sites (Go's own warrant); or item 10's sitting at M-core-packages naming it as the C-width vocabulary's mechanism. **Three claims struck**: *five years, nobody died* (Go's `=>` merged and reverted 2016-11-04; 1.9's warrant was refactoring, not brevity), *the only purely additive thing* (additive for programs, not tools — Go broke ten in 2017 and three in 2024; a reserved word is not additive), *call it `alias`, not `type`* kept as a departure from Pascal, Modula-2, Oberon-2, Go, Rust and Haskell rather than as their precedent. **The historian's own prediction failed at the sitting** (below 10, found 77) and stays in the table. **Found beside the sitting and reproduced by the coordinator before filing: defect 029** — `examples/sqlite/main.hero` with `sqlite3_step(db)` for `sqlite3_step(statement)` builds at zero diagnostics and exits 139, because `sqlite3 *` and `sqlite3_stmt *` are both `ptr` and the probe passes a `void *`; no ruling and no wart covers the class, a transparent alias catches none of it, and the repair is the *distinct types* door item 5 keeps open, owed a sitting. Also recorded: the emitter already writes two typedefs for one function type when a labelled type meets an unlabelled value (`h_0fn_539ea1ea`, `h_0fn_5f8c2b52`), legal and deterministic, the shape an alias implementation could make worse. Row 54 opens in the chain; the gate CLAUDE.md § 4 asks once per milestone was the `/step` that opened a milestone whose file says *the steps are the sittings*, written down so the author can disagree | **Why deferral and not the cheaper or the bolder verdict**: ENTERS would set aside a rank-4 rule on a 1.4% saving, and a bare *deferred, v2* is the promise without a date this milestone exists to end; the complete resolution is the one that leaves the next sitting nothing to re-derive and the reader a condition to check. Conservative recorded, not adopted: the engineer's later threshold (a 30-character spelling at 20 sites) | design.md §1.0, §1.3, §1.5, §1.6, §1.7, §1.12, §4.15, Part 7 item 5; CLAUDE.md §2, §4, §12, §14 | 135 (033, 119, 122, 133 inherited)
