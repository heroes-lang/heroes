2026-09-14 | **The first push after the M-marked-acquisition close went red on both workflows, and neither failure could have been seen from this machine** (M-marked-acquisition, after the push). **The site's claim checker had not run since the ceiling moved.** It runs only when the site republishes, and no push had touched `site/`, `examples/` or `spec/` between the ceiling moving at step 4 and this one — so **six pages went on saying `8K` against a tree saying 10240**, three in English and three in Italian, and the build died prerendering `/it/spec` with *1 claim(s) on the page disagree with the repository* | **And that falsifies a sentence written at step 4, which is corrected here rather than left standing**: that close recorded *the site needed none of them: it derives the number by reading `constant CEILING` out of the suite*. It derives the number for the CHECKER; the six sentences are typed by hand ON PURPOSE, and `site/src/lib/figures.ts` had already written down why, having paid for it when the ceiling moved 6144 to 8192 on 2026-09-12 — **`checkClaims` runs BEFORE the `{{ceilingK}}` fills**, so a claim over a generated value would read the braces and never the number. The two mechanisms are exclusive by construction and the choice is per SENTENCE: a number a hand types gets a claim, a number the build writes gets the placeholder. So the hand repair was the correct one and not a missed chance to generate | **The second failure was mine from the same morning, and the repository refused it on all three legs.** `talks/`, an untracked second tree parked beside the compiler — a talk ABOUT this language, its own rules file and reviews, its own citations into paths that do not exist here — had been added to `suite_records.hero`'s RECORD skip list, and `records` went green on the one machine that had the tree. `records/exemptions` caught it in CI: **the entry selected no file this repository TRACKS**, because the tree exists on one machine and in no clone. A dead exemption is not free, it reads as a rule to whoever finds it next, and that check exists for this exact shape | **The sentence filed beside that entry was false too, and this is its correction.** It said *this walk reads the WORKING DIRECTORY and not what git tracks*. It does not. `tests/harness/shell.hero`'s `project_files` runs **`git ls-files --cached --others --exclude-standard -z`**, so git answers, and an untracked-but-not-ignored file is included DELIBERATELY — that is how a record you have just written is judged before you commit it, which is a property to keep rather than a leak to plug. The function's own error text says so: *a `find` reported a gitignored TLS key as a milestone identifier, which is why this function exists* | **The right instrument was never that list, and the lesson is the level.** *This tree is not this project's* is a thing to say to GIT, not to a suite's exemption list — which is why the suite refused to hear it. `--exclude-standard` honours **`.git/info/exclude`**, local and entering no commit, so a foreign tree parked in one working directory is excluded on the machine that has it and named nowhere in what anybody clones. It also makes the author's standing constraint structural rather than remembered: `git status` no longer reports the tree at all, so nothing from it can reach a commit by accident | **What was run before the second push**: the site built locally at 186 pages with **32 claims over 18 pages** and the ceiling reported as 10240; `records` 23; the net's own tests 154; and the full net once, as CL-063 asks. **All three CI legs had failed on the one check** — Windows 1808 passed 1 failed, Darwin 1833 passed 1 failed, Linux the same — which is what a single root looks like when three platforms agree | CLAUDE.md § 1, § 11, § 14, § RUN IT, § Verification; CL-004, CL-047, CL-057, CL-063; `.claude/rules/platforms.md`, `.claude/rules/records.md`; `site/src/lib/claims.ts`, `site/src/lib/figures.ts`; `tests/harness/shell.hero`, `tests/harness/suite_records.hero`; panel 133 | none
