2026-08-26 | **M-separate-compilation step 6: one `.c` per module lands, and the four errors it took to get there were one cause said four ways** (panel 093 R1–R6 executed; no new sitting — the architecture is the one the author ratified 2026-08-25). The compiler now **builds itself as 157 translation units**, links, and the binary is correct: it checks its own source at exit 0, runs `examples/calculator` correctly through 5 TUs, and re-emits all **297** blessed emissions byte for byte. **What the first per-module self-build actually did was fail — 20 clang errors — and that is the finding**: every SECTION of a TU was answering "what do I need" for itself, and four answers cannot share one file. Descriptors came from the whole program's aggregates while typedefs came from the module's reach (`use of undeclared identifier 'h_token_Span'`); prototypes came from all 1,318 functions while the types they name came from the module (`h_source_InputFile`); a wanted record's `_hash` called its FIELD's `_hash`, which `check_table.contained` never reaches because it stops at a declared type (`h_diag_Kind_hash`); and a wanted `T?` took its payload's descriptor address where the descriptor set had a narrower reach (`h_clitoolchain_Toolchain_desc`). **One set now feeds all four**, closed the way C reads a declaration rather than the way the arena holds a type — fields, cases' fields, and a FIXED array's element, which `contained` also stops at and which the FFI corpus found (`h_ffiacarraymember_Matrix_hash`, 6 errors, the net's own `ffi-a-c-array-member`). Two consequences worth their own line: a TU prototypes **what it defines and what it calls**, not the program, which is R3's guarantee unchanged (the declaring TU still compiles its own prototype beside its definition) and 1,318 prototypes per TU deleted; and the fused unit is no longer emitted on the per-module path at all, where it was printed and dropped — 785,323 lines per build | five defects and every one of them was two sets where the architecture needs one; the emitted-C sections are not independent and the compiler cannot pretend they are | §3.1, §4.19, §4.20, CLAUDE.md §7 | 093 R1-R6; 030 R2; 033
