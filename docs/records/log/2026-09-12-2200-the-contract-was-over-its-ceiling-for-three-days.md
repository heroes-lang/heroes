2026-09-12 | **The operating contract was 1806 tokens over its ceiling for three days and every instrument in the tree printed nine** (author instruction: analyse every number that acts as a ceiling, because sessions keep hitting their heads on them; then the same of the floors; then *take CLAUDE.md to 8K as well*). Measured this sitting: `CLAUDE.md` is **7306 real** tokens on `claude-opus-5` through `POST /v1/messages/count_tokens` against a stated `CONTRACT_CEILING` of 5500, while `heroes measure` printed `Headroom: 9` — because the contract alone is judged on the maximum over two VENDORED tables whose larger is OpenAI's `cl100k_base`. The method validated itself by reproducing the spec's pin exactly at 7531. **The trace was already in the tree and nothing read it**: `docs/measurements/023` recorded the contract at 5389 vendored / 7190 real on 2026-09-09. The margin was `5127 + 373` on 2026-09-07 and **+364 of it went in five days over 23 commits**, 97.6%, three commits carrying +347 | **Six parts, landing together, and three of them are refusals.** *The change is an EXTENSION and not a repair*: both technical seats found independently that §1.6 governs *"the entire language specification"* and that `grep -n "CLAUDE.md" design.md` returns fifteen hits with no measurement rule among them, so the brief that convened the sitting was wrong to call it an inconsistency and the raise has to stand on the 7306 instead. *The ceiling is 8192 WRITTEN AS `7306 + 886`*, the author's number in the engineer's form, because CLAUDE.md forbids a round figure in its own words and a number copied from another document invites a later tidy-up to unify two constants that must never be one — and the room is stated with its measured duration, about nine days at the five-day rate, which is why the gate is not optional. *The digest is REFUSED for the contract* on the engineer's measurement, 34 commits in seven days against the spec's 17, which would mean ~34 withheld verdicts a week clearable only by a network refresh and not clearable at all on a fork PR — panel 123 R1 returning through the window; one vendored `CONTRACT_TOKENS` pin replaces it and does three jobs, staleness detector, gate baseline and the movement detector the contract never had. *The delta gate lands in the same commit*, both documents, +50, vendored, which fires on 2 of the last 22 contract commits and those two carry +255 of the +364 — adopted knowing what the historian proved it buys: Chromium runs a 16 KiB per-CL gate with a mandatory footer and grows ~100 KB/week, so **a delta gate bounds attribution and not level**. *`contract()` must read the `real` row and a stub compiler printing only `maximum` must make it red*, the engineer's find that would otherwise have left the whole change green forever on the scale it exists to abolish. *And two false sentences are corrected while the block is open*: the comment saying the contract *"is 322 lines now"* when it is 341, and its citation of *"Claude Code's own documentation asks for under 200 lines"* — the historian fetched that page and the figure is not on it, it is third-party blog content, so the claim becomes the weaker and true *"bloated CLAUDE.md files cause Claude to ignore your actual instructions"* with Chroma's Context Rot and Liu et al. behind it. **Conservative was costed and recorded rather than adopted** (CLAUDE.md §4): `7306 + 373` is 7679 and about four days of room, refused because a threshold nobody can obey is the defect this milestone exists to remove | CLAUDE.md §3, §4, §11, §12, § Verification; design.md §1.6; CL-018, CL-023, CL-040, CL-069 | 123, 133, 134 |
