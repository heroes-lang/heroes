# Four rows move behind the gate, by author instruction

2026-09-15. The author's instruction, in full: put M-microcontroller-verdict,
M-qbe-backend, M-journey-book and M-guide-book after M-publication-gate. Four
names, in that order, and the order is kept. No reason came with it and none is
invented here; `docs/roadmap/scheduling.md` carries the move keyed by name, as
every scheduling fact has been since 2026-09-03.

## What moved, and what did not

A reorder and not an insert. The table stays 78 long and the boundary between
closed and scheduled stays at 57, so nothing that reads that boundary moves.
Nine rows change number: the four named, which go to the end, and the five that
stood between them and the end, which each come forward. The gate stands at 74
today, with the verdict, the backend and the two books behind it.

## What the position now says

The gate's outward act waits on no second backend, no ruling about a device and
neither book. Two living files said otherwise and each carries a dated
correction under the sentence it corrects: M-microcontroller-verdict's own file
placed it before M-install-channels, and M-publication-gate's file says the site
and both books will state the thesis claim, which at the gate only the site can
now do. M-qbe-backend's *after the tools and before the books* still holds, and
its file says so with the date.

## Verified, not assumed

78 rows and no gap, by a count over the table; and `site/src/lib/chain.ts`,
which refuses a gap at build time, read the reordered table in a site build of
186 pages that completed in 2.08 s.
