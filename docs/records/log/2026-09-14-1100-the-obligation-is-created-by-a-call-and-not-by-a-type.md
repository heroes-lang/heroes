2026-09-14 | **The obligation is created by a call, and not by a type** (M-cleanup-verdict step 3, panel 147, full five seats plus the completeness critic, on the author's authorisation of the narrowed question). **Route A — a handle naming its releaser, `record Stmt tag sqlite3_stmt released sqlite3_finalize` — is REFUSED on its axis rather than on its spelling, and no spelling repairs it.** The ffi seat wrote the sentence and compiled it: **ownership is a property of the CALL, not of the type.** `sqlite3_db_handle`, `sqlite3_next_stmt`, `cairo_get_target` and `hb_font_get_face` all hand back the very type the mark would carry, **borrowed** — `borrowed == owned: true`, compiled — and with the sweep in place the program printed **0 rows where the table holds 3**, exit 0, ASan silent. The compiler-engineer reached the same refusal from the other end and cast the sitting's **one formal veto**: it reduced `examples/ledger/db/sqlite.hero`'s `opened()`, emitted its C and inserted **the single line the sweep adds**, and the next statement answered **`rc = 21`**, `SQLITE_MISUSE` — the connection closed before its first use, exit 0, and the sanitizer silent because the object is freed inside system libsqlite3, while a control in the same session fired `heap-use-after-free` at 134. The spec-warden reached it from a third: **22 of the 23 escaping paths acquire through `sqlite.prepared(...)`, a Heroes function in another module**, so the scope that acquires is not the scope that leaks, and Route A's own *may not be returned or stored* would refuse the reference binding — write the mark and the corpus stops compiling, omit it and nothing is checked, **measured yield zero paths at +80 tokens** | **And the rule that settles it was already written, which is the finding the sitting paid three seats to reach.** The compiler has answered *where does an FFI lifetime fact live* four times and **never once on a type**: `owned` rides a RESULT, `lease` a DECLARATION'S INITIALISER, `consumes` a PARAMETER POSITION, the `cstr` lend an EXPRESSION'S POSITION. CLAUDE.md §11's **a narrowing asks the value, never the world** decides Route A in one line before any C is compiled (CL-004). **Route B does not enter as drafted**: the llm-ergonomist and the spec-warden found the same `@`-cell defect from opposite ends without citing each other — the deferred call's arguments are read where the statement is written, so the natural program registers the release of a value the acquiring call has not yet filled, **and it compiles**. If a scope-bound statement ever enters it is **block-scoped**: the historian found five languages independently correcting Go's function-scoped choice, with linters shipped for the loop leak, and the originator the only dissenter. **Route C is not taken either, because its ground moved twice**: C itself adopted `defer` this year (TS 25755, Clang 22, 2026-02-15, primary source fetched), and the Part 6 row that would host the refusal names a falsifier that **already fired** when `consumes` landed at 2 of 17 on 2026-09-13 — a refusal whose falsifier has fired is owed a new one. **What enters is an axis and not a spelling** (R4): the obligation is marked where it is CREATED, on the acquiring call, and **the compiler never picks the release call** — the one clause every refusing seat and all three independently-proposed fourth routes agree on. It takes its own milestone, whose first act is to price two instruments against each other: a `consumes`-shaped mark (nearest shipped neighbour 114 code lines) and **escape refusal**, whose machinery ships twice already as `cstr_escapes` and `lease_escapes`, five shipped relatives measuring 93 to 241 code lines, blast radius **five sites** | **The completeness critic changed the resolution, as it has in every sitting it has run.** It found the frame no seat stated: **Route A cannot be compiled** — `released` does not parse, `error[expected_extern_signature]` — so *veto, measured* and *approve, measured* were **not two measurements of one object**, and the file says so where a reader meets them. It found that the three proposed fourth routes are **one route with three instruments**, agreeing on the only axis that matters, and that the engineer's runtime counter **carries its own veto's defect**: keyed on the type, a borrow increments and never decrements. It found the **fifth route nobody listed** — escape refusal — and that **panel 122 refused an inferred release and not escape refusal**, so the precedent cited against it is for another form. It falsified the ergonomist's strongest claim, *double release is impossible under Route A*, by compiling one; the seat could not have seen it, because its binding contained no borrowing function. And it falsified two *cannot* claims, one from each of the two seats that compile | **The census that convened the sitting was the coordinator's and it carried four errors, corrected underneath itself the same day.** Live exposure is **22 paths in one file, not 23 in two** — the 23rd was repaired earlier in the same session, and the document's own § 2 said so while its headline did not. The pair list is **short by at least seven**, `main`'s own pair and six test blocks, so it is **≥19 and not 12**: the list is a measurement too (CL-057) and this one was read rather than enumerated. *"10 of 21 pairless"* is **8**, an arithmetic error in the section handed to the seats as *what the sitting is handed*. **And the one that changes the argument: all 22 paths end in `exit(1)` or `abort`** — `main` closes and exits on `is_err()`, the six tests use `.must()` — so **no shipped program leaks a handle and then goes on running**, and the *exit 0, in silence* headline was measured on a synthetic reduction. The class survives, the urgency does not, and R5 records the conservative resolution that correction supports so the author can take it (CL-040). One further coordinator error, in the briefs: vendored token counts converted to real ones at 1.331, where `docs/measurements/010` says in terms that **no row is convertible**; the warden caught it and answered with a range | **Two predictions were scored inside the sitting that made them.** The historian predicted a per-type mark would prove insufficient because some producing function returns a **borrowed** instance of the marked type, naming `sqlite3_next_stmt` — *the function this milestone's own census had used* — and the ffi seat collected it an hour later by compiling `sqlite3_db_handle`. **Held.** The spec-warden predicted a re-count would read 22 in one file rather than 23 in two, and the critic's audit found exactly that. **Held** | design.md §1.7, §1.11, §1.12, §4.19, Part 6 borrow-checker row, Part 8 wart 20; `spec/heroes-spec.md` § 13; CLAUDE.md § 4, § 11, § 12, § Precedence rank 3, § RUN IT; CL-004, CL-040, CL-057, CL-061; `docs/measurements/030`; panels 122, 139, 145, 147 | 147
