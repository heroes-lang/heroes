2026-09-13 | **The sixteenth mutation operator lands before the form it measures, and its kill rate means the opposite of what it reads** (M-handle-verdict step 3; `selfhost/mutate/handles.hero`, `docs/measurements/029`). Panel 145 ordered the instrument first, because four seats had disagreed on how many such mutants `examples/` even holds — 5, 6, 7 and ~14 — and the spec-warden had written that the whole §1.2 case for the handle form rests on an after-number **nobody had run**. `swap-ptr` plants one C handle where another belongs: inside one function, a bare `ptr`-typed argument name replaced by another `ptr`-typed name in scope. **The answer is 15, and nobody had it** — twelve from `examples/sqlite/main.hero`, whose `first_int` holds **three** handles at once, three from `examples/ledger`, **zero from `examples/curl`**, which is one handle per function and therefore no second operand. The FFI seat was counting *sites* where the engineer was counting *mutants* and neither named its unit, which is CL-017's shape and panel 144's own finding one sitting earlier | **The headline reads 8 of 15 killed, 53%, and not one of the eight dies because the compiler can tell one handle from another.** It cannot: every C pointer that is not a `cstr` is one Heroes type, which is the whole of defect 029. The eight die on two rules that fire on the **shape of the edit**, both run to confirm it: `error[unused_binding]`, because `db: db` → `db: statement` leaves the parameter `db` never read; and `error[aliased_mutable_arguments]`, because `statement: @statement` → `@tail` beside `tail: @tail` puts two `@` arguments on one place, which is panel 010's rule. Both are honest diagnostics catching real mistakes; neither is about handles. **Where the swap is a pure use — an argument read and nothing orphaned, which is defect 029's own shape — the program compiles every time: 0 of 7 killed, and one of the seven is the filed defect verbatim.** So the number to carry to step 4 is **7**, not 53%, and when the form lands the headline will read 15 of 15 while the honest half reads 7 of 7. This is panel 011's *never pool these into one headline* arriving in a new place: a per-operator rate can be right and still say the opposite of the truth, because the operator's mutants are not all of one class | **The engineer's prediction is corrected underneath the sitting rather than quietly**: it said *0 of ~14 die today*, and the mechanism it named is right while its number is wrong. The correction is appended to `docs/panel/145-…` with its date, as a record is | **And the count that pins the operator total was in THREE places, not two.** `selfhost/mutate/ops.hero` asserts it twice and `selfhost/cli/mutate.hero` once; moving two left the third red, at exactly the assertion whose own comment says a number in prose is a premise about the world and expires in silence. The file records that it was two places the last time this happened; it is three now, and the suite caught it in the second it existed to catch it | **What the instrument is blind to, stated on its row rather than discovered later**: a handle reached through a field path, which hides **17 of `examples/ledger`'s 18** `ptr`-passing sites behind its hand-written `record Db`/`record Statement` idiom; an untyped `=` binding; and a UFCS receiver. The ledger's own discipline is therefore invisible to the measurement **and** to the swap — which is the argument for the form panel 145 adopted, since a `tag` the header checks does what a hand-written newtype cannot | design.md Part 11, §4.19; spec § 3; CLAUDE.md § RUN IT, §11; CL-017; panels 010, 011, 145 | 145 (010, 011 inherited)
