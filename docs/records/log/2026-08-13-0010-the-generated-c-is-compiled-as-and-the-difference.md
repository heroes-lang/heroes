2026-08-13 | **The generated C is compiled as `-std=gnu11`, and the difference between the two dialects is one predefined macro** (`docs/panel/047`, provisional — author ratification pending; three judges, opened by the CI's first run on a second machine). `-std=c11` defines `__STRICT_ANSI__`, and on glibc that is the *only* thing it does: it hides `M_PI`, `M_E`, `strdup`, `strndup`, `fileno`, `popen`, `setenv`, `unsetenv`, `newlocale`, `uselocale`, `clock_gettime` and `CLOCK_MONOTONIC` — most of what §1.11 says a program binds — while Darwin's headers do not guard them at all. So `tests/golden/run/ffi-constant.hero` passed here and failed on Linux, and **the flags rather than the types decided whether a correct binding compiled**, which is the failure §4.19 exists to prevent arriving by the one road nobody was watching. What the emitter *writes* is unchanged and still C11; only what the platform's headers *expose* moved | **the veto holder went looking for breakage with a cross-compiler and reported *"I came to break this and could not"***. The ffi-pragmatist found a real glibc/musl/FreeBSD header tree on the machine and ran the **real pipeline against real glibc headers** rather than a model of them: identical LLVM IR for the runtime and for a generated unit at both levels, 16 glibc struct layouts unchanged with three types going hidden→visible and none re-laid-out, §4.19's own `_Generic` classifier returning identical answers on ten header tokens, and **63 of 63 `run/` goldens cross-compiling for `x86_64-linux-gnu` under `gnu11` against 62 of 63 under `c11`** — the one failure being this defect. Option (b), feature-test macros in the generated C, was refuted from three directions including its own: `_POSIX_C_SOURCE` cannot expose `M_PI` on glibc (it sets `__USE_XOPEN2K8`, not `__USE_MISC`) and **measurably hides five identifiers on Darwin**, `_DEFAULT_SOURCE` alone is *exactly equivalent* to gnu11 for 127 hand-edited golden lines in a directory where `UPDATE_GOLDEN` is forbidden, and it needs the same rule written in two translation units where `toolchain::FLAGS` is already the one place both are configured. The historian's fourth option — pass no `-std=` at all, which 3 of 3 sourceable C-emitting compilers do — was **declined on this project's own criterion**: a byte-identical fixpoint cannot inherit clang's default dialect, which is `gnu17` today and was `gnu11` before Clang 11. **`-std=c11` was buying nothing it was believed to buy**: the emitter has never written portable ISO C — `__builtin_*_overflow` is mandated three sentences above it in §7, and every generated unit carries `__builtin_types_compatible_p` — so the flag narrowed the *headers* on one platform and the *output* on neither. Two premises repaired in the same commit under §11 and §12: design.md §4.19's `true`-is-`i64` sentence rested on the flag rather than on the value (the fact survives, measured on both dialects and both platforms), and the 128-bit refusal's first leg was *"§7 says C11"*, which had to be rewritten because §12 holds a refusal to a feature's burden of proof. And CLAUDE.md §7's own "Compile flags:" list did not contain `-std=` at all — both judges found that independently | CLAUDE.md §7, §11, §12 · design.md §1.11, §3.1, §4.19 | 038, 040, 046, 047 |
