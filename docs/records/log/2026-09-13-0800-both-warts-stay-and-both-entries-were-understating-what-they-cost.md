2026-09-13 | **Both Part 8 warts stay, both entries are corrected, and the ledger's first item closes at nine of nine** (panel 143, M-deferral-ledger step 8, five seats plus a completeness critic; `docs/panel/143-one-wart-is-an-inconvenience-and-the-other-erases-the-evidence.md`). Warts 15 and 16 were the **two watch-list entries with no home at all** until this milestone gave them one on 2026-09-03, and they are its last two. **The shape this step adds to the ledger's account is new**: a wart can be wrong in the direction nobody checks — not by claiming too much, but by **understating what it costs**. **Wart 15 understates it three ways.** Two escapes are eaten and not one: `"C:\temp\report.txt"` is 16 bytes, `C : \t e m p \r e p o r t . t x t`. The emitted C is **not a witness**, since `HERO_STR_STATIC(…, "C:\temp\015eport.txt")` shows the tab half **textually identical to the Heroes source** and only the carriage return as `\015`. And the cost nobody had named: **the failure erases its own evidence** — `perror` writes `C:` TAB `emp` **CR** `eport.txt: No such file or directory`, the carriage return being a control byte **this language injected**, so a terminal rewinds and the reader sees `eport.txt: No such file or directory` with the path gone from the screen. The one line that would name the bug deletes it. Measured byte for byte by the FFI seat and reproduced by the coordinator. **The silent set is exactly five letters** — a path component beginning `n`, `t`, `r`, `\` or `"` passes and the other twenty-one are `error[unknown_escape]`; seven of twelve realistic components measured silent. **And the seat that reads only the specification wrote the bug first try**, before seeing any proposal, reporting honestly that it noticed only because the task said *Windows path* and that its self-report is weak evidence beside a harness run. **"The remedy is raw string literals" is an unenumerated option set** (CL-057): Python spent a decade making unknown escapes stricter and **it does not help**, because the escapes that bite are the legal ones — its own example is *"a corrupted file name, but no warning"*; a raw literal aids the writer **who already noticed**, and that writer was going to write `\\` correctly; and it has a permanent hole on this exact path, a raw string being unable to end in an odd number of backslashes. Three cheaper routes were never listed: **`"C:/temp"` works today at zero language cost**, Windows normalising forward slashes; **one clause of about 51 characters** targets the failure that actually happens; and `hero_cstr_nonnull` is already emitted at **every** `cstr` lend, so a lent literal carrying a C0 control byte is checkable at one existing site — the compiler already refusing a **raw** carriage return in a literal for word-for-word the same reason. None adopted: nobody built the check | **Wart 16 was filed as the wrong kind of thing, and the critic measured it.** The entry reads as a missing convenience; the gap is reachable three ways and **two of them silently reorder the output**. `write_file(path: "/dev/stdout", …)` writes without a trailing newline at exit 0 with **zero built-ins and zero spec tokens**, and interleaved with `print` the source order *label: · value · second: · again* comes out **`label: second: value\nagain\n`** — both writes before both prints, exit 0, zero diagnostics, **using only what the language ships**. The same happens through C, since `stdout` is not nameable and an author reaches for `fdopen(1, "w")`. **So the cause is not §4.19 refusing `stdout`, as the FFI seat argued from its own case — it is two buffers on one descriptor**, and a reader who finds the route finds the hazard with it. **A wart whose workaround fails silently is not an inconvenience.** What was NOT found is a silent failure of `print` itself: every wrong guess about it shows in the output on the first run. The remedy is named and priced — a second built-in `write` at +5 for the name and +17 with prose, or `print` gaining `end: ""` at +8 — with the critic correcting the compiler side from *"the cheapest thing this panel has seen"* to one line **plus renaming a shipped example**, because `builtin_name_taken` reserves the name everywhere. **Two process findings about the sitting itself.** The critic caught the first draft of the synthesis calling this a five-seat lane while **four seats wrote files**: the historian's brief gives it no file write, so its report reaches the coordinator through the run's journal and never the sitting's directory, and **a seat whose report never touches disk cannot be audited by the seat whose job is auditing** — the same shape panel 140 found about briefs, one door further along. And the coordinator's three counts all reproduce but **none states its unit**, `print(` being 646 lines or 676 occurrences and backslash literals 138 or 221, which is CL-017's rule. **Recorded rather than decided**: this language has **no warning level** — a diagnostic is exit 1 or nothing — so every *just warn about it* reached for in this sitting was reaching for something that does not exist | design.md §1.1, §1.2, §1.6, §1.11, §4.19, Part 8 warts 15 and 16; CLAUDE.md §4, §12, §14; CL-017, CL-057; panel 008, panel 034 | 143 (008, 034 inherited)
