2026-09-12 | **The grammar splits by derivability, and the half that cannot be derived is not written by hand** (author request for a formal specification of the syntax, then the ceiling raised to 8192 the same day). The productions are authored INSIDE `spec/heroes-spec.md`, each beside the prose of the construct it governs, in Wirth's notation, indented and never fenced. `heroes grammar` prints what is already data in the compiler — the twenty-one keywords and the nine binding powers, each operator with its `ast.BinaryOp` name beside its spelling — and prints the productions collected, reading them at run time and writing no file | **Four decisions the milestone took that were nobody's to delegate, each against something.** *Against the sitting's own R2*: no `spec/grammar.md` on disk, because the historian's survey found every free-standing grammar archived, `ziglang/zig-spec` on 2025-11-23 with this proposal's exact mechanism, three years after its checker had correctly reported six divergences nobody repaired — so the collected view is printed instead, which satisfies R2's intent without its hazard. *Against the ffi-pragmatist's condition 1*: `[ Generics ]` is dropped from `ExternFunction` rather than kept with a sentence, because `check/ffi.hero` refuses `.generic` at every crossing so every program filling it is an error, and documenting a dead form invites a model to write one. *Against the engineer's condition 1*: the production-versus-parser check was not built at all rather than built with a length-asserted exception table, because a check that only asserts a named function exists is thin and its cost is in spec tokens; what was built instead is the productions against themselves, `ebnflint`'s class, which costs none. *Against §11's ratchet twice, and both times the seam was real rather than the number rounded*: `measure/pinned.hero` took the reader's own count and `--refresh` out of `cli/measure.hero`, and `operators.hero` took the spelling and naming tables out of `grammar_expr.hero`, where `op_text` had been sitting under the test heading while being the only spelling table in the compiler | CLAUDE.md §4, §10, §11, §12; design.md §1.6, §1.7, §4.14, §4.19, Part 7 item 16; CL-020, CL-025, CL-040, CL-063 | 024, 035, 067, 126, 133 |
