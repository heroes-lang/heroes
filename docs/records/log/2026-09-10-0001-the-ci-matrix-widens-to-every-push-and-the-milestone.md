2026-09-10 | **The CI matrix widens to every push and the milestone tag stops starting a run of its own** (author instruction 2026-09-10: the complete build on all three platforms, once, and no second workflow). The narrowing to Linux between tags rested on a runner minute costing money, and that expired when the repository went public: measured on the account 2026-09-08, the gross is discounted in full on all three legs, macOS included. So `matrix.include` is the three legs unless a `workflow_dispatch` narrows it, the five steps that ran only at a tag run on every push, and `push.tags` drops `m-*` and keeps `v*`. What the widening buys is that a Windows or macOS divergence is found on the commit that causes it; what it costs is nothing, and what it removes is four full runs of one commit's work every time a session closes four milestones. The mutation score stays gated on a release tag or a request, at 20 to 30 minutes a leg for a table that came out byte-identical on all three | the trade this file described for three weeks, the third platform read at the tag rather than at the commit, was a price in dollars and the dollars are gone; the comments that explained it are corrected in place with their date rather than deleted, because they were true when they were written | CLAUDE.md §14, § Verification; `.claude/rules/platforms.md` | — |
