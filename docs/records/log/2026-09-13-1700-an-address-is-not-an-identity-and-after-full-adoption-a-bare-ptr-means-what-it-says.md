2026-09-13 | **An address is not an identity, and after full adoption a bare `ptr` means what it says** (M-handle-verdict steps 6 and 7). **The hazard panel 139 could not file is reproduced, and both people were right.** That sitting's completeness critic saw a record holding a **freed** pointer compare `==` **true** to a record holding a fresh, unrelated allocation; the coordinator ran the same shape, got **false**, and filed it as a question rather than a defect because `docs/work/DEFECTS.md` holds measured failures with reproducers. Re-run against the shipped compiler: on **Darwin arm64 the same binary answers `true` 22 times in 40**, and on **x86-64 Linux under glibc 20 times in 20**. The class is real, the reproducer is sound, and **its shape changes across platforms — intermittent on one, certain on the other**, which is the third time this milestone has met that pattern after defect 029's segfault-here and wrong-answer-there. A test written for it would pass half the time on a Mac. **Panel 145 refused identity as a form**, so by that item's own words § 7 owed a sentence and got one: *a `ptr`, a `cstr` and a function value compare as an address **and not as an identity — free one and the next allocation may reuse that address***. **+16 vendored**, the cheaper of two drafts, the longer refused for saying it twice; real 7785 → 7806, digest `7e8ad63e0a607452`. **Filed as neither a defect nor a wart, deliberately**: the behaviour now matches the document, and the item asked for a sentence in § 7, so adding a Part 8 entry beyond it would be this milestone deciding something nobody asked | **`examples/curl` adopts the form, and it is the case the critic raised.** `CURL` is `typedef void CURL;` on this Mac's SDK — a struct only under `CURL_STRICTER` — so there is no `struct CURL` to name, which is exactly why the completeness critic corrected the spelling every seat had proposed from `struct <tag> *` to the header's own name, starred. `record Curl tag CURL` builds, the program runs unchanged, and the emitted C says **`CURL *` thirteen times**. **The historian's prediction at panel 145 is scored and HELD** — *the 2 curl sites compile after renaming to the header type* — and it was scorable **only** under the verbatim spelling, which the historian itself flagged. `curl_easy_cleanup` takes its handle `consumes`, so the binding carries the step-5 mark too | **The census the naming sitting is owed, and it is the argument.** After all three bindings adopt the form, `examples/` holds **9** bare `ptr` parameters, `selfhost/` **0**, `tests/golden/run/` 3 — and **not one of the nine is a handle**: two `tail` (a `char **` out-parameter), two `context` and two `callback` (SQLite's callback protocol, which §4.19 fixes at `ptr` until the boundary has a C-width type vocabulary), one `p` (`sqlite3_free`, which takes any pointer by design), one `error` (a `char **` the program passes `nullptr` for) and one `destructor` (`SQLITE_TRANSIENT`, a function-pointer constant). **So a bare `ptr` now means, in every place it survives, a pointer whose type this language cannot name.** In `examples/` as a whole, counting every type position rather than parameters alone, it is **49 before the form and 12 after**, measured against `git archive 101ab191`; the 37 that went away were every one of them an opaque handle. **And the census had a sixth position it had missed until it was re-run: a `ptr` as a record FIELD**, six of them in `tests/golden/` and none in `examples/`, among them raylib's `Font { recs: ptr, glyphs: ptr }` — which the handle form does not reach either, for the **opposite** reason from sqlite's: those point at arrays of structs the header *does* size. That is the number the sitting could not have had before the form landed, and it is why the author ordered the sitting to wait for it: today the question is argued over nine positions that are genuinely untypeable, not over thirty-seven that merely had not been typed yet | design.md §4.19, Part 6 borrow-checker row; `spec/heroes-spec.md` § 3, § 7, § 13; CLAUDE.md §12; panels 139, 145 | 145 (139 inherited)
