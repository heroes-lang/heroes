2026-09-13 | **The specification's promise about copies is repaired in words, four of its neighbours turn out suspect, and one of them frees memory under every copy** (panel 139, five seats plus a completeness critic, convened on a defect rather than a proposal; `docs/panel/139-the-sentence-was-false-and-so-were-four-of-its-neighbours.md`). Defect 030 said § 3's *"No aliasing exists anywhere"* is false through a `ptr` field, and CLAUDE.md § 12 makes the sentence the defect rather than the compiler's behaviour, so the sitting was asked to choose words. **It came back with four acts instead of one.** **The defect is six times wider than its witness**: the FFI seat compiled twelve shapes and found aliasing unnoticed in a record field, a **variant case payload**, a record in an array, a record in a map, a record inside a record, and — the shape that breaks every candidate wording — **a bare `ptr` as a plain parameter with no record in sight**; the compiler looks in exactly one place, panel 068's map-key gate, and it is looking for a hash. **Both candidate sentences merged `ptr` and `cstr` wrongly**, since a Heroes record may not hold a `cstr` at all (`error[cstr_in_a_record]`, compiled), so a reader following them is led somewhere that does not compile. **The mutable marker is a no-op that costs**: marking the shipped `stepped` `@statement` does change the emitted C — a pointer parameter, one copy-in, three copy-outs — and running it against real SQLite gives **29 lines changed, identical output**, while the marker's fallout was measured at 6 broken call sites and 6 broken bindings, 13 lines for one of 13 by-value wrappers. **The compiler repair is refused for a better reason than the brief gave**: the coordinator asserted refusing this would refuse the FFI, and the FFI seat tested it instead and found that **the header carries no signal a rule could key on** — `sqlite3_column_count` is not const-qualified, compiled, so a mutating step and a pure read have the identical C signature, and a rule would refuse **13 of 17** functions in the shipped binding with §12's burden unsatisfiable. **The false sentence has descendants and two are printed at users** (`selfhost/resolve/writes.hero:307`, `selfhost/size_errors.hero:37`), and no suite links the document to them, which is what makes the landing a step with a gate | **The finding that outlives the sentence, and it was the question the critic was asked**: one false absolute was found by accident, so the sitting enumerated the others. Four suspects from the warden's grep of every absolute in the document, five more from the critic's own read, and **the sitting ran the sharpest**: § 13's *"Unmarked pointers are never freed"* is true of the compiler and read as a fact about the program, while `examples/ledger/db/sqlite.hero:225` and `:270` ship `closed(db: Db)` and `finalized(statement: Statement)` **by value**, handing C a pointer that frees what every copy holds. The warden proposed the test, the coordinator ran it: **build exit 0, zero diagnostics, run exit 0**, and under `--sanitize` `heap-use-after-free, WRITE of size 8`. Filed as **defect 031**, the sharper twin of 030 — not *two copies reach one thing* but *one copy can destroy what the other holds* — and §1.12 ranks above cost, so it does not close by being documented. **And design.md carried the same falsehood inside a refusal**, which the critic found and no seat did: `:2588`'s borrow-checker row said value semantics removes aliasing *so there is nothing to check*, and the fact that would make it wrong is now in the tree twice over. That half costs **zero tokens** — design.md has no budget — and it landed in this commit with the row resting on cost and on §1.11 instead, with a new falsifier stated so it can expire in its turn. **Two corrections the sitting made to its own seats**: three seats priced one draft at +50 and the critic re-measured it at **+48**, the difference being re-wrapped transcription; and the critic's own measurement that a record holding a freed pointer compares `==` **true** to a fresh one **did not reproduce** for the coordinator, which makes it a hazard rather than a defect — a `ptr` compares as an address and an address can be recycled, but the reproducer depends on the allocator, so it is handed to M-handle-verdict rather than filed. **What blocks the sentence is a hard stop and not a judgement**: amending the specification trips the staleness detector, measured in a copy, and `measure` gives no verdict against a stale count, so the landing needs `--refresh` — the one command in the compiler that reaches the network. It is queued with the sitting rather than taken | design.md §1.11, §1.12, §4.10, Part 6; spec § 3, § 7, § 9, § 13; CLAUDE.md §12, § Hard stops, § Precedence; CL-018, CL-061, CL-062 | 139 (068, 137 inherited)
