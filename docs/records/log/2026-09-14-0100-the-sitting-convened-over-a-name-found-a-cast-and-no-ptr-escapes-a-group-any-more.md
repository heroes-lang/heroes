2026-09-14 | **The sitting convened over a name found a cast, `ptr` keeps its name, and no `ptr` escapes a group any more** (M-handle-verdict step 8, panel 146 — the milestone's last open item, sat on the author's own order that it wait until the form had landed). **`ptr` KEEPS ITS NAME, four seats to one advisory objection, and the refusal is held to a feature's standard.** The ffi seat compiled the whole survivor binding, emitted its C, renamed every `ptr` spelling **in that C** to `unsafe_ptr`, and compiled both: the object files hash **identically**. The rename was done in the emitted C because it cannot be done in the source — `heroes check` on a binding spelled `unsafe_ptr` is `error[unknown_type]` at the first type position. A name is front-end text and reaches no ABI. The ergonomist wrote four programs from the specification alone and respelled them: *the load-bearing line is unchanged in all three*, and the wrong-object call `sqlite3_close(stmt)` type-checks under every spelling, because what makes it an error is `record Db tag sqlite3` and not a word. The warden vetoed the rename on **Principle 0 rather than on the ceiling**, which it measured and cleared at +10 vendored against 326 free: `selfhost/` writes **zero** bare `ptr`, so the compiler-need limb is silent, and §1.7's subtraction is zero. **The falsifier is the engineer's and it is written into the resolution**: a compiled program in which a bare `ptr` reaches a position *not inside and not adjacent to an `extern` block*. The critic then found where that shape lives — `tests/golden/surface-fixtures/qualifyptr/main.hero:4` holds a `ptr` in a **six-line** program with **no `extern` group at all**, six since 2026-09-03 and never four — so `ptr` is not an FFI-only type, which every argument in the brief had assumed, and the sitting records it rather than letting the assumption stand | **And the sitting convened over a name found a CAST, which is the thing that actually mattered.** The completeness critic refused the brief's central claim — *a bare `ptr` now means, in every place it survives, a pointer whose type this language cannot name* — and compiled three counter-examples. Re-run here rather than accepted, and they do not all survive: `error: cstr` is **refused** (`char **` has no name here), `destructor` as a function value is **refused at the call site** (its only value is a `constant: ptr`, and `check/ffi.hero:175` refuses an `extern constant` of function type — *a value the language cannot hold, not a type it cannot spell*, a fourth category the census never had). But **`@tail: cstr` builds, runs byte-identical, and changes the emitted C**: `@tail: ptr` gives `void ** a4` and a call that writes `(void *)a4`, **a cast this compiler inserts**; `@tail: cstr` gives `const char ** a4` and hands clang the exact type to check. That is **§ Precedence rank 3**, robustness, which outranks every naming argument at the sitting by the contract's own order. Adopted at both sites, and **`cstr` turns out to be the STRICTER type**, measured only after the sitting closed when the review put the sharpest objection to the narrowing — that it accepts a use-after-free `ptr` refused. It does not: the fault is byte-identical under both spellings, same address and same freed region, a local `str` dying while a pointer into it escapes, which neither type sees; the supposed compile-time refusal was a **missing method**, `.validated()` being `cstr`-only and caretted at the read rather than at the escape. The direction inverts by grep: **four** compile-time diagnostics are keyed on `cstr` and **zero** on `ptr`. And the repository had already blessed this spelling for this exact position — `tests/golden/run/lease-tail-points-into-the-bytes.hero`, filed by panel 124's ffi seat against `*pzTail` and naming `sqlite3.h:4472`, says *the tail cell itself is a plain `cstr` cell, which is right* — so the narrowing is the bindings catching up with a golden nobody at the sitting cited. **Panel 140 measured `ptr` escaping an `extern` group 11 times in 2 files on 2026-09-13; it is 0**, and the eight positions that remain are every one of them inside a group | **A third defect, in the build cache, found by walking into it.** `suite_warnings` failed the new golden while every direct run of that golden was silent, cold and warm — and it replayed the two warnings **with the line numbers of a header that no longer existed**, which is what named the cause. A warm object replays the warnings its compile produced, added deliberately so that a warning is not swallowed after the first hit; `warnings.txt` was written **only when clang said something**, so a clean recompile left its predecessor's text on disk and every later build replayed a compile that was gone. **A repaired program reported its old warnings forever**, until the cache directory was deleted by hand. It is the exact mirror of the case `suite_cache.hero` already holds — that one turns a failure into a pass, this one turns a pass into a failure — and the sixth row of that suite now pins it with four builds, because the defect lives only in the hits **after** the rebuild and a three-build case would have passed. Measured on both compilers: old reads 3 warnings, 0, **3**, **3**; repaired reads 3, 0, **0**, **0**. `deps.fresh` was not the fault and is measured correct: a header edit still rebuilds and still changes the answer | **A defect one day old, found by testing the census rather than by using the language.** A handle used as a **record field** builds at exit 0 and makes clang say *braces around scalar initializer*, twice, on a correct program — a handle is declared with `record` so its type is `.named`, and `emit/extern_record.hero` braced it, but a handle emits as `<tag> *`, a scalar. Measured: **no handle was a field of a record declared INSIDE an `extern` group**, which is the only position the completeness probe reaches — so the form landed at step 4 without ever meeting the position that breaks it. (That sentence said *not one `record X tag Y` anywhere in the tree was used as a record field* until the review falsified it by grep: **three were**, two in `examples/ledger/db/sqlite.hero` and one in `tests/golden/check/ffi-handle-refusals.hero`, all of them plain Heroes records, which get no probe at all and so could not have found it. The verdict held and the sentence under it was three counter-examples too wide.) The fix asks `handles.is_handle` about the FIELD's own declaration, which is a fact about the value in hand rather than a list of shapes that expires. `tests/golden/run/fixedbugs-a-handle-in-a-record-field.hero` warns under the old compiler and is silent under the new one, and the unit test puts a handle and a nested record in **one** probe so the two answers are asserted against each other | **A defect three days old in the editor, and the half that was owed was the instrument and not the fix.** `editors/vscode/syntaxes/heroes.tmLanguage.json` admitted **four** escapes where the spec gives **five**, so a legal `\r` fell through to the rule below it and a **correct program was painted `invalid.illegal`** — in both the string rule and the character rule. `print("a\rb")` runs at exit 0. The defect had been **measured on 2026-09-10** and written into `.claude/rules/diagnostics-and-goldens.md`, which named the reason it survived in the same paragraph: *"Nothing judged either file until then, which is exactly how the grammar rotted in silence."* Three days later the grammar was unchanged and `grep tmLanguage tests/harness/` was still empty. So the repair is `spec/colouring`, an eighth check in `suite_spec.hero` that **reads both sides** — the escapes out of `escape.hero`'s own arms, the keywords out of `keywords.hero`'s own table — rather than adding a third place for the truth to live. Both halves were made to fail on purpose before being trusted. **What it deliberately does NOT do** is teach the grammar the seven contextual marks that step 5 recorded as owed: measured, none of the seven is a keyword, so colouring them unconditionally would paint a variable named `tag` as syntax — trading an under-colouring for a **mis**-colouring, which is strictly worse. Step 5's debt is answered rather than paid, and the measurement that refuses half of it is written down | **What the brief got wrong, counted because a brief is a record too.** **Seventeen across two sittings, counted so a reader can add them up**: four in panel 145's census, one clause reported to the author as ratified text that no sitting wrote, six in this sitting's brief, and **six more in the sitting's own file**, found after it closed by an adversarial review of the whole step and corrected in place — the tally itself having been wrong three different ways until the review counted it. **Not one of the seventeen is a wrong verdict**: every one is a wrong number, unit or citation underneath a verdict that held, which is M-deferral-ledger's own finding of one milestone ago happening to the coordinator's prose. The six in this sitting's brief: § 6 was false in three parts **and it was the section claiming to bind the verdict**: no `certain` fix ships the string `ptr` — one site is a note and two are `.guess`, found independently by the engineer and the critic. Two occurrence counts were **line** counts, the identical error the brief's own text had confessed three lines earlier, because `nullptr` contains `ptr`: the spec holds 5 and not 7, design.md 43 and not 37. The census **glued two denominators**. The warden was handed 386 free tokens when its own instrument says 326, the FFI floor mortgaging the difference — a seat told to veto on budget breach, given a loose input. And three positions were never opened at all: a return type, a variant payload, and the `ptr` in a file with no `extern` group | **The question the sitting could not answer and filed instead: who writes `ptr` now?** **13 lines across 7 files** tell an author to write it, against **one** site that admits it as a type — `resolve/vocab.hero:53` — and `selfhost/`, sixty thousand lines by the people who know the language best, contains zero. So `ptr` is no longer a type a programmer reaches for; it is a type the **compiler recommends**. **And the sitting's own claim that one of them was measured wrong is WITHDRAWN**, run at the close: each of the thirteen names a position no handle can reach, three of them a function pointer, and for the `@tail` position the compiler was already recommending `cstr` — the right answer, as a `guess` fix. So nobody was told to write `ptr` there; the binding author wrote it against the compiler's own advice, which is a better finding than the one it replaces. What stays open is whether a FOURTEENTH place should now recommend the handle where it says nothing. That is filed rather than answered, because answering it is a diagnostics change and this sitting was convened on a name | design.md §1.7, §1.11, §4.19, Part 6 borrow-checker and `raw` rows; `spec/heroes-spec.md` § 3, § 13; CLAUDE.md § 12, § Precedence, § RUN IT; CL-017, CL-036, CL-057, CL-061; panels 139, 140, 144, 145, 146 | 146 (139, 140, 145 inherited)
