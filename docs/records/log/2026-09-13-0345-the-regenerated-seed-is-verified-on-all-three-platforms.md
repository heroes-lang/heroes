2026-09-13 | **The regenerated seed is verified on all three platforms, and the fixpoint holds on each** (the author put the Windows box and Docker up for two hours; `.claude/rules/platforms.md`, CL-048). `eecccb09` rewrote `seed/heroes.c` from 794,507 to 796,427 lines after finding it a commit behind `selfhost/`, and that commit's own verification was **Darwin only** — the one platform the change was made on, which is the shape CL-048 exists to refuse. Run this session on the other two, from the commit the Mac had: **Linux** (the container of the CI leg's architecture, x86-64, Debian clang 22.1.8) builds it in **5.6 s**, re-emits `seed/heroes.c` **byte-identical** (`cmp` silent) and passes **641** of the compiler's own tests; **Windows Server 2025** (clang with `seed/README.md`'s `-Wl,/STACK:67108864`, the box brought from `7d31838a` to `f31497ae` by `git push win main:main`) builds it in **6.8 s** with three pre-existing `_CRT_INSECURE_DEPRECATE` warnings and nothing else, re-emits it **byte-identical**, and passes the same **641**. So the byte-for-byte fixpoint — CI's own instrument for a stale seed, a `cmp` on the Linux leg of every push since 2026-09-05 — is confirmed on three architectures and two C libraries rather than argued from one | **What this was worth checking, and it is not the number.** A regenerated seed is 796,427 lines of machine-written C that no human reads, and the repair was made and verified on the machine that made it; `seed/README.md`'s condition 3 is precisely the one a green local build cannot see. Two of the three legs could have disagreed in ways nobody would have noticed until a push: Windows compiles the same file through a different linker with a stack flag that exists because its main thread gets 1 MB where the others get 8, and Linux runs it under a different libc at a different word-size-neutral architecture. Neither did. **What stays unrun is written down rather than inferred**: the full net was run on Darwin only, the other two legs ran the seed build, the fixpoint and the compiler's own tests, which is what the seed change bears on. The Windows box carries no sqlite3, so the FFI-shaped measurements of the same night could not be repeated there | CLAUDE.md §7, § Verification, § Commands; `.claude/rules/platforms.md`; `seed/README.md` § When this file must be regenerated (condition 3); CL-048, CL-050, CL-065 | —
