# Four sittings ratified, and the default is not flipped

2026-09-20. M-declared-extents step 20, on the author's answer.

## The decision

| | |
|---|---|
| date | 2026-09-20 |
| decision | **panels 167, 168, 169 and 170 are ratified in one act**, and the one question panel 170 said no sitting could spend for the author is answered: **the default is NOT flipped.** `docs/work/DECIDE.md` returns to **zero** |
| reason | the author, in these words: *"I am following your recommendations."* The recommendations were the coordinator's and they rest on the seats' measurements: the default flip's price of eight refusals is the completeness critic's, the extent's three grounds are the spec-warden's and the llm-ergonomist's, and the veto that killed the mark is the compiler-engineer's |
| design.md § | §1.12, §4.19, §1.6, §1.11 |
| panel | 167, 168, 169, 170 |

## How it is recorded, and why not the flattering half

**Not `by delegation`**, because the author did not say so. **Not as a reading of
the four files** either: what was put to them was the summary and the price in
each queued item, in conversation. The author's standing instruction of
2026-09-19 is that they read; **the coordinator does not know whether these four
were**, so the record says what it knows rather than choosing the version that
credits somebody with more than happened (CL-058).

## What the four yeses leave standing

**Defects 066 and 068 have no route in flight.** Four sittings weighed every
candidate and each either closes zero of them or closes them at a price now
declined for a stated reason: **eight refusals of eight correct programs** in
`examples/` alone. The reason is not that eight is many. It is that **nobody has
measured what the ninth costs**, because the corpus is `examples/` and
`tests/golden/` and the author's own programs are not in it.

**So what moves them next is a measurement and not a sitting**: one binding the
author cares about, written both ways — with the lend the language admits today,
and with the pessimistic default that would refuse it.

**Defect 070 keeps panel 170's named route and nothing else changes.** Measured
after the ratification: `consumes` on a `cstr` is `error[unread_mark]` today, so
giving that word a meaning there is a real change, and it would meet panel 170's
own two findings head on. **A fifth sitting would re-derive them**, which is why
none was convened.

## And the shape the records check enforced, which is worth keeping

Appending a second `## Author's verdict` section was refused by
`records/verdicts`, which wants exactly one and reads its **first line**. So the
ratification went to the top of the existing section and the `Pending` paragraph
stayed underneath, under a sentence saying what it is. **Nothing was deleted**,
the reading order carries the answer first, and the check reads what it was
written to read.
