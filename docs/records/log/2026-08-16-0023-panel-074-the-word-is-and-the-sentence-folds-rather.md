2026-08-16 | **Panel 074: the word is `tag`, and the sentence folds rather than appends** (provisional — author ratification pending). The two seats panel 072 could not seat — llm-ergonomist and spec-warden — converged from disjoint inputs. The free-form string `is "struct stat"` takes **two vetoes**: the blind seat because whether the declared thing is a value or a pointer would be decided by characters inside a string Heroes does not parse (*"the spec closed exactly this channel for widths, with exactly this reason, one paragraph earlier"*), the warden on §1.0's smallest self-consistent surface at +39. `as` is objected to because every language spends it on introducing the **new** name (`import numpy as np`) and `spec:9` already says *"No aliases"*; the warden reports **no token difference** between `tag` and `as` (both 3462) and declines to break the tie, leaving it to the seat whose brief it is. **The spec sentence is a fold, not an addition**: appending leaves `spec:217`'s *"the same name"* contradicted four lines above its own example, so the clause is rewritten — *"all its fields, and the same name unless `tag` gives C's"* — at **+26 against a −4 named removal, net +22, 3440 → 3462**, twelve tokens cheaper than the cheapest appended form. **Not compiler-need and the ledger says so**: `selfhost/` has **no `extern` group at all**, and the shim route works today (measured, exit 0, 137 tokens across two files against 96) — what pays under §1.2 is that the repair *"go write a C header"* cannot be expressed as a `Fix` at all, while `tag stat` is `Certainty::Certain`. **Both seats make the same condition**: `emit/ffi.rs:127`'s `declaration()` matches Function and Constant and drops `record` through `_ => return None`, so a record this program declared `extern` never reaches exit 1 — CLAUDE.md §7's own rule, unimplemented for records, and the approval is void without it. Measured this session over six real header sets: **323 struct tags, 109 tag-only**, and **6 of 328 tags are also a function or an object** (`flock`, `sigaction`, `sigvec`, `stat`, `timezone`, `wait`), which is why the marker carries a C name rather than being a boolean. `docs/panel/074`
