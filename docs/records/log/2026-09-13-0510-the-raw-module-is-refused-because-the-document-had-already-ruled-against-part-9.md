2026-09-13 | **The `raw` module is refused, and Part 9 is corrected beneath itself, because this document had already ruled against Part 9's premise in this document** (panel 140, M-deferral-ledger step 5, five seats plus a completeness critic, three vetoes; `docs/panel/140-the-document-had-already-ruled-against-part-9-and-nobody-told-part-9.md`). The ledger's fifth verdict and its fourth item in a row whose stated reason was false or expired before the verdict was written. **The measurement that ended it is a sentence in the same document**: §4.10 says *"§4.19's `ptr`/`cstr` sits outside the guarantee"*, naming the exception rather than leaving it to be discovered, while Part 9 says *"everything that breaks the guarantees goes in `raw`: **pointers**, load/store at offsets, casts"* — and §1.11 decided between them before either was written, because a language with no standard library puts a pointer in **every** binding, and a mark every binding carries carries no information. The FFI seat found the contradiction; no brief named it. **Both of the item's stated preconditions landed on 2026-08-12** — modules at M-module-namespace, sized integers at M-sized-integers — and woke nobody, which is this milestone's second expired clock after doctests. **Measured**: `ptr` appears in **56** extern declarations across **37** files, and escapes an `extern` group in a program's type position **11 times in 2 files**, both SQLite bindings, so the region would mark almost everything and bound almost nothing. **§1.7's subtraction is zero** and what it adds is core by that section's own test: `use X` resolves to a file on disk, and the tree's one synthesised module is unqualified and unconditional, so a qualified conditional pseudo-module is a third kind of name the resolver has never had — ~100 lines across seven sites with **five already at their stop-on-growth ceiling** — while the operations themselves reach as Tier-1 builtins at **3 files**, measured on `chars`. **And the item conflates a move with an addition**: `ptr` exists, load/store at offsets and casts **do not exist at all**, so there is nothing to move. **The FFI seat compiled Part 9 at its word** and found it makes things worse: since Part 9 offers load/store on byte arrays rather than an opaque handle type, a handle becomes an `i64` slot, and a swapped handle then exits 139 with both sides spelled `i64` — defect 029 made unfixable rather than fixed | **The reader's seat found the sharper objection and it is the one to remember**: `raw.load_u8` would **remove** a guarantee rather than fence a danger, because the same read through `[u8]` **aborts** at § 10's bounds rule. A region that makes a safe operation unsafe is not a boundary, it is a hole with a label. Reading only the specification, that seat also found the one real gap the proposal does not fix — a call to an `extern` with scalar parameters is **textually identical** to a call to a Heroes function — and, unprompted, reproduced defect 030 from the text alone: *"I wrote raw code and caught myself assuming `b = a` copies a pointee"*. **The critic found the form that returns, and it is already in the compiler twice**: `error[cstr_in_a_record]` confines `cstr` and `unsupported[pointer_element]` confines `ptr` inside containers, **by declaration rather than by import**, with nothing a file can rename away where `use raw as x` could, and reaching a `ptr` in a file with **no `extern` group at all** — which `tests/golden/surface-fixtures/qualifyptr/window.hero:9` has, verified, and which falsifies the FFI seat's *"the `extern` group already covers that set"* exactly there. Generalising it is **M-handle-verdict's**, and this sitting hands that milestone a **narrowed option set** rather than a clean bill. **A cost nobody priced, tying this sitting to panel 139**: `raw` was the only proposed design under which § 3's *"No aliasing exists anywhere"* could have stayed unconditionally true by quarantining the exception, so refusing it commits the specification to an in-text exception — the sentence panel 139 queued the same night. **And a finding about the coordinator rather than the seats**: two of four framing facts were unverifiable from the sitting's own artifacts, because the briefs go out as prompts and are never written where a later reader can check what the seats were told. Recorded in the sitting; the repair is one line in `/panel` | design.md §1.0, §1.7, §1.11, §4.10, §4.13, §4.19, Part 5, Part 6, Part 7 item 11, Part 9; CLAUDE.md §4, §12, §14; CL-018, CL-062 | 140 (033, 068, 139 inherited)
