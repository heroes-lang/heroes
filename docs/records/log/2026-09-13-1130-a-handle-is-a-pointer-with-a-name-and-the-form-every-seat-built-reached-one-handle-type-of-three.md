2026-09-13 | **A handle is a pointer with a name, the compiler already reads the name, and the form every seat built reached one handle type of three** (panel 145, M-handle-verdict step 2, five seats plus a completeness critic; `docs/panel/145-a-handle-is-a-pointer-with-a-name-and-the-compiler-already-reads-the-name.md`). The sitting on whether one C handle can be told from another, owning defects 029 (a swapped opaque handle builds at zero diagnostics and segfaults on Darwin, returns a wrong answer at exit 0 on Linux), 030 (two copies of one record reach one C cursor; closed as filed at step 1) and 031 (one copy frees what every other copy holds, `heap-use-after-free` under ASan at exit 0). **029 ENTERS**: a fieldless `extern` record is a handle — a pointer to a type the header names and leaves opaque — spelled as **the header's own type name, verbatim, starred**: `record Db tag sqlite3` is `sqlite3 *`. Compiled three ways before it was adopted: the grammar's fieldless record is `error[empty_record]` today; with one dummy field **the checker already refuses `sqlite3_step(db)` with `type_mismatch: expected Stmt, found Db`** at the exact line, no new rule needed, and what fails is the emitter spelling a group record as a struct by value (20 clang errors, *incomplete type*); and in hand-written C against the real `sqlite3.h` the header's own pointer type makes **clang refuse the swap too** under `-Werror=incompatible-pointer-types`, a flag already in `FLAGS`, where `void *` accepts it and exits 139. Cost 60-90 code lines plus two probe files the critic added, **zero new `Ty` cases** — the three vetoes stand: no payload on `Ty.ptr`, the pointee instrument does not feed the checker (`heroes check` never runs clang, so `mutate` would never count a kill), panel 109's counted `ptr owned` stays refused. Spec § 13 gains one sentence **and its fence is rewritten to use the form**, because the reader's seat and the warden reached the same finding from disjoint inputs: an opt-in form the document does not show is a form a model never writes, and the fence's own `sqlite3_close(db: ptr)` would re-open 029 for exactly the functions it teaches. **+52 vendored net** with one removal; the second removal the warden priced is refused because the critic showed it deletes the sentence that reconciles § 13:349 with § 13:356 | **The critic changed the resolution for the seventh sitting in seven, and this time the coordinator's brief was on disk to be audited.** Every seat built `struct <tag> *` and it reaches **one of three handle types in this tree**: `curl/curl.h:112` reads `typedef void CURL;` on this Mac's SDK, so `record Curl tag Curl_easy` has no struct to name; the portable name is the header's own, which `selfhost/cli/pointee.hero` already fetches from clang's AST dump for every extern parameter and discards at line 115 as *not numeric*. **`partial` was planted by the brief's own spelling** and forbids `==`, so `db == nullptr` would have been refused (the reader's H8); it is not part of the form. **The coordinator's census was wrong four ways in the direction that understates exposure**: no `fopen` in `examples/`, seven hand-on sites not six, six C pointee types collapsing to `ptr` in one file not two, and *zero `ptr` in `selfhost/`* true at the `extern` keyword while the compiler's own handles live in `runtime/parts/*.c` as `FILE *` and `DIR *`, where C's type system tells them apart — the exit the thesis says a Heroes program should not need. **The reserved question — the type's name, `unsafe_ptr` or a family, which panel 139 reserved for this sitting by name — was handed to no seat and is NOT decided**, said rather than decided by omission. § 9 extended across calls catches nothing (`sqlite3_step(statement: db)` checks at exit 0, run tonight); `heroes check` cannot see a wrong tag (`tag no_such_struct` checks at exit 0), so every header fact stays build-time and `mutate` is blind to a bad tag | **030 REFUSED as a form, and the refusal already has its home**: Part 6's borrow-checker row carries identity with its falsifier, § 3 states the behaviour since step 1, a refcounted box is an ABI break and an affine handle is core. **Zero words added.** Two things recorded rather than smoothed: the affine handle is the one route that would close 031's class and **nobody priced it**, so it is owed a count of copy sites at the prototype step; and **the consume mark refuses 2 of 17 shipped functions, which is precisely the falsifier the Part 6 row names** (below 13 of 17), so if the mark lands that row's ground moves from *unwritable from headers* to *cost*, and a dated line under the row says so tonight | **031 WART adopted, defect NOT closed, prototype scheduled.** The critic asked what no seat did: is a Part 8 wart admissible for a §1.12 violation at exit 0 in the default build, when CLAUDE.md § Precedence ranks robustness above Principle 0? The answer given: only where design.md already places the operation outside the guarantee (§4.10 says *§4.19's `ptr`/`cstr` sits outside the guarantee*; §4.19 says *free it explicitly*), **and only while the cheapest guard is being built rather than argued about**. Part 8 gains wart 20 stating the cost in the present tense; the remedy is a **consume mark on a C parameter, one word that is not `owned`** (which means the opposite direction), with rule (i), *consume through a borrowed parameter is refused* — which refuses the shipped reproducer as written at the line that frees, and, **as the critic found against the engineer's own 0 of 17**, refuses **2 of 17** correct calls in the shipped binding (`closed(db: Db)` and `finalized(statement: Statement)`, both repairable by `@` as step 1 repaired `stepped`). Priced 120-200 lines, two files at ceiling, zero C-side consequence. The defect closes when the prototype refuses `uaf.hero` in `heroes check` at ≤ 150 lines, or when the author ratifies the wart with that measured cost in front of them. **The order of the steps is RUN IT's**: step 3 the `swap-ptr` operator, ~14 mutants and 0 killed today by construction, so the sitting's central number stops being unrun; step 4 the form, measured by the operator; step 5 rule (i) and the affine count | **Two process facts.** For the first time every brief was on disk before a seat started and the critic read them, finding four framing facts no seat checked; and the historian, auditable for the first time, was found to have read two sections outside its brief, which the sitting treats as uncitable. **Three seat reports carried the same misreading of `owned` on a `ptr` parameter** — retyped to `str?` in silence, `check/freer.hero:90-93` — so it is filed, with `empty_record`'s caret and `ffi_unknown_tag`'s *incomplete read as absent*, as repairs the landing step owes | design.md §1.7, §1.11, §1.12, §4.10, §4.19, Part 5, Part 6 borrow-checker row, Part 8 wart 20; spec § 3, § 9, § 13; CLAUDE.md § Precedence, § RUN IT, §4, §12; CL-036; panels 109, 135, 137, 139 | 145 (109, 139 inherited)
