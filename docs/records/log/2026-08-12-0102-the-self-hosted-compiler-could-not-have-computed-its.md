2026-08-12 | **The self-hosted compiler could not have computed its own names, and the closure list gains the row that says so** (author decision, `/decide`; found while verifying a panel 039 item). `emit/mangle.rs::instance` was FNV-1a over the rendered type arguments, carrying the comment *"written out rather than taken from a crate, because the port must reproduce it exactly"* — and every ingredient is inexpressible in Heroes, measured: the offset basis `14695981039346656037` is `int_out_of_range` because `int` is signed, `hash * 1099511628211` is `panic: integer overflow` because §4.3 makes overflow an abort, and `^` is `reserved_operator`. `h_library_map_<typehash>` reaches the generated C of any program that uses a generic, and M-selfhost-fixpoint compares that C byte for byte. Now a polynomial hash modulo 2^31 − 1, and `tests/golden/run/premise-mangler-hash-in-heroes.hero` computes the same four values in Heroes with a Rust test asserting the two sides agree | three alternatives were refused for reasons worth keeping: a runtime primitive would have bumped `HERO_RUNTIME_ABI` for something the language should be able to say; unsigned or wrapping arithmetic is Part 7 item 10, which calls itself *"the highest-risk addition in the language… never in v1"*; and deferring to M-selfhost-probe was the spawn precedent but not the cheap moment — **zero `emit/` goldens carried a hashed name**, so the change moved nothing today and every later milestone would have made it dearer. A collision is loud (two C definitions of one name, clang error, exit 2), which is what makes 31 bits enough. The lesson belongs to the audit rather than to the hash: measurement 003 is mechanical over the closure list, so a requirement *absent* from the list cannot be seen — this is the second instance after `system()` at panel 036, and both times the missing row was something the **compiler itself does**, not something a Heroes program contains | §1.0, §4.3, CLAUDE.md §7, §11 | 029, 036, 039 |
