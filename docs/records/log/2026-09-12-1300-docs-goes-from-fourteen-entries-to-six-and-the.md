2026-09-12 | **`docs/` goes from fourteen entries to six, and the trees that would have cost the most stay where they are** (author instruction: *"troppe sottocartelle in docs, metti una sottocartella strato che ne aggrega a gruppi"*, the day after M-rotated-records added three of those entries). `log/`, `journal/`, `book/`, `contract/` and `done/` go under `docs/records/`; `glossary/`, `environment/` and `assets/` under `docs/ref/`; `milestones/` and `learn/` under `docs/work/`. **`panel/`, `measurements/` and `work/` do not move**, and that is the decision rather than an omission | **the cost was measured before anything was proposed, and it is silent rather than red**: `records/citations` skips records by construction (`suite_records.hero`), so a citation inside a panel or a journal to a moved tree never goes red — a reader simply cannot follow it, and no instrument says so. Measured: moving the three that stay would have cost **313** such lines against **110** for the other ten, so the shape follows the price. **Nothing was lost, and that is a measurement**: 1504 basenames before and after, the diff empty, every tree's file count identical. The three maps kept their height (1119/621, 4071/3553, 140/115), which is what keeps 161 line-number citations landing. **Five silent failures were closed BEFORE a file moved**: `records/verdicts` had no floor and would have passed green on zero sittings; ten constants were outside `records/exemptions`, now all in, with the inverse rule for `RETIRED_ROOTS`; **`records/rules` is new** and asserts that every `paths:` glob of a tree-local rule still selects something, because nothing verified that a rule still loads; `verification.md`'s own documented command read one path segment; and `suite_spec`'s `other_headings` swallowed a missing file. **The seed was regenerated** because `docs/contract/` was cooked into a user-facing string in `selfhost/cli/measure.hero`, invisible to every check; the fixpoint was proved with a silent `cmp`, not assumed | CLAUDE.md §14, §10; CL-037, CL-069, CL-044, CL-070 | — |
