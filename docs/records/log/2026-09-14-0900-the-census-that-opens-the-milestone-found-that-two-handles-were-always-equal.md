2026-09-14 | **The census that opens M-cleanup-verdict found that two C handles were always equal, and the emitter learns what a handle is by being told rather than by reading a star** (M-cleanup-verdict step 1, defect 032, no sitting: the compiler disagreed with `spec § 13`, so the compiler had the bug, CLAUDE.md § 12). **The decision is where the emitter gets the answer.** `ctype.Aggregate` gains `handle: bool`, carried from `handles.is_handle_record` through `new_names` exactly as `foreign` and `partial` already travel — five lines in five places, the same five the two facts beside it cost. The alternative was free: a handle's C type is the tag starred, so `c_type`'s last character already distinguishes one, and `handles.const_pointer` reads exactly that character four lines away. It was refused because a trailing star is a fact about a **spelling** where the question is about a **declaration** (`.claude/rules/module-shape.md`), and because `handles.hero` exists precisely to stop that question being answered a fifth time in a fifth place — its own module doc says the predicate was about to be written four ways in four files, which is why it was written once | **The second decision is two ceilings, and it is taken the way the table says rather than by rounding.** `emit/ctype.hero` 380 -> **395** (measured 388) and `emit/structural.hero` a NEW entry at **335** against §11's 300 (measured 323), each raised with room, as every entry in `DECIDED` is. The comments were cut FIRST — the defect's story is written once, in the golden case that reproduces it, and the emitter carries the rule and a citation — and the ceilings moved only against what was left. `structural.hero` is not split instead, and its own module doc is the reason: generated `eq` and generated `hash` are ONE invariant and live where a single diff shows both, so the only seam on offer is the one the file was written to refuse. `DECIDED.len()` 18 -> **19**, and the net's own test went red on the addition exactly as it was designed to — the third suite catching a pinned number the way `/step` § 2 says it should | **What the census was actually for, and two of its three premises were false before it could be written.** The milestone's own file says `owned` and `lease` both put *a release obligation on every path*. Measured: **`owned` puts none.** The compiler frees the C string at the CALL, inside a null guard — read in the emitted C and confirmed by running an early return between the acquire and the end of the function — so there is no path for a programmer to miss. **`lease` puts one and it is LOUD**: a lease that escapes on an early return aborts at `main`'s return, exit 134, *1 lease(s) never ended*, saying how many. **The handle is the only one of the three that leaks in silence**, which is panel 137's finding standing and the other two falling. That reframes the sitting this milestone owes: not *two obligations want a scope-bound release* but *one obligation leaks silently and neither existing form reaches it* | `spec/heroes-spec.md` § 13; design.md §1.12, §4.3, §4.19; CLAUDE.md § 12, § Precedence rank 3, § RUN IT; CL-004, CL-011, CL-054, CL-061; `.claude/rules/module-shape.md`, `tests/harness/suite_layout.hero` | none
