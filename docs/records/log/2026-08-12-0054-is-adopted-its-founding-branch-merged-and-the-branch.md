2026-08-12 | **`docs/reasoning/` is adopted, its founding branch merged, and the branch deleted** — closing the one decision that had been left open by a note written into a directory that did not exist. Author instruction, arriving as a repository cleanup ("rimuovi tutti i branch tranne main"): the notes are recovered first, so the class is adopted by decision rather than by writing. What merged is the README, notes `000`–`002`, CLAUDE.md §14's per-session duty and this log's 2026-08-10 entry; what did **not** merge is the branch's hand-off commit, whose four queue items and three watch-list entries were **each verified as already done on `main`, independently and better** — K's superlative now sourced to kdb+'s STAC-M3 results, the memory-model rejection table written, the non-atomic constraint stated with its thread condition, Part 7.13 shaped, both future-proofing rules landed. Three append-at-EOF conflicts resolved in date order, not at EOF; the three dated notes were **not** rewritten to the current vocabulary — the README gained a section mapping `/debrief` and `M5a` forward instead | a branch nobody merges is a decision nobody makes, and this one had been costing a numbering hole (`003` beside no `000`) plus an open item that re-measured its own distance from `main` every time it was read. CLAUDE.md §4 makes the teaching process amendable by instruction, so no panel; CLAUDE.md §14 makes a dated record appendable and never rewritten, which is why the repairs owed landed in the README and not in the notes | CLAUDE.md §14, §4, §11 | — |
