2026-09-21 | M-declared-extents | The milestone asked how far C may read through a lent address and ended somewhere else: what becomes of the pointer afterwards. The author flipped the default on a principle and one condition, the cost, which came back at 33 nanoseconds. Then three sittings looked for a word meaning *this call frees what you hand it*, and the thing that killed the search was nine lines of Heroes where C stashes the pointer on one call and frees it on a later one that takes no parameter at all. There was nowhere to write the word. The runtime could reach it, because it already had a signal handler and already knew how many leases were live, and both seats that compile then vetoed the sentence it printed: it said C had freed the bytes, and a C library's own failed assert says the same thing about a program where nothing was freed. What ships says what it saw and lets the reader check the rest.
