2026-09-06 | M-isolated-threads | A language whose values are never shared should be the easiest place in the world to run two things at once, and it was not: four ways to corrupt memory had been measured, each from one ordinary line. Three of them closed — one counter made atomic, ten buffers given to the thread that uses them, a cached locale that stops racing — and the fourth turned out not to be where the trouble came from at all. A sitting convened on copy-on-write could not make copy-on-write fail, and the real door was one line in the type checker that had been letting arrays walk out to C inside a callback since the day callbacks arrived. With it shut, a Heroes function could finally run somewhere else, and ten programs were written to do it: sums, primes, a Mandelbrot band, eight queens. What crosses into a thread is one number, and that limit turned out to be the point rather than the price — not one of the ten needs a lock, because not one of them has anything to protect
