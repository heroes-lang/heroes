2026-09-18 | M-readable-bytes | The milestone was opened on a wall and half of it was a mirage. Reading a C byte field as text needed one function the runtime almost had — the sibling's `strlen` was the whole difference. Building the struct to hand to C needed nothing at all: you ask the library for one, and this repository's own tests had been doing it in seven places while three sittings argued about how to make it possible. The coordinator's brief said the compiler did not have things it had, three times, every one a negative sentence nobody ran.
