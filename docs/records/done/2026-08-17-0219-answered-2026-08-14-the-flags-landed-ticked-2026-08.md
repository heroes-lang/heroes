- [x] panel 055 | **ANSWERED 2026-08-14 — the flags landed; ticked 2026-08-15 without being asked** (verify-before-asking). `--include` and `--library` are on the argv table at `cli/table.rs:103-131`, on all three commands that reach clang, and `DESIGN-LOG.md`:298 records the decision with its argument. The item stayed unticked for a day, which is the inverse of the failure this list's first rule guards against and costs exactly the same: a ticked item that tracks nothing wastes a verification, an unticked item that tracks nothing wastes a *question to the author*. Was | panel 055, 2026-08-14 | **`--include` and `--library` on the argv table: the judges split, and the split is real.** Three hold that CLAUDE.md §10's stopping rule yields **nothing** — `CPATH`, `C_INCLUDE_PATH`, `LIBRARY_PATH` and `PKG_CONFIG_PATH` are all live because `Command` inherits the environment, measured four ways, and *"nothing if two existing invocations already compose to it"* is the rule's own third clause. One **built it** — `+105/−6`, 75 code lines, all in `heroes-cli`, **zero spec tokens**, 549 tests green — and found and fixed a defect in its own prototype on the way (search paths absent from the cache key, so two configurations shared one artifact path). Its argument is not about cost: an environment variable is **not an artifact a program can carry**, so asked for *"a program that binds SDL2"* a model produces something that cannot be built and the missing half is an unversioned shell line. Against it, and this is §1.12's ground: `-I` and `-L` named **independently** is the only way to produce the skew the engineer measured — header v1, library v2, compiles clean, links clean, exit 0, **wrong number** — while `package` asks one question of one `.pc` and cannot. **What is yours**: land the flags, or write the refusal with its falsifier per §12. If landed, three conditions are already measured and owed — the paths go in the cache key, no allow-list (a `.pc` is a whitespace-split string read as flags, a search path is one argv word clang consumes as a path, and that distinction gets the test §11 asks for), and `ffi_missing_header`'s note names the flag as it now names `CPATH` | docs/panel/055 · crates/heroes-cli/src/cli/table.rs · crates/heroes-cli/src/commands/toolchain.rs | the one question of the sitting where the measurements point two ways, and neither judge is wrong about what they measured
