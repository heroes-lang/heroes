- [x] **010 — a `void *` C parameter bound as `@value: i32` is refused by clang, and the compiler calls it its own internal error at exit 2** | 2026-09-04, found by panel 108's ffi-pragmatist while compiling in-out shapes (`getsockopt`'s `void *optval`) | **repaired 2026-09-04**, the same day, with its adjacent shape | `selfhost/emit/` pointee-width check (design.md §4.19, CLAUDE.md §7's four exit-1 classes) | a wrong exit code and a message that blames the wrong party — no crash, no wrong answer, but §7 says this is the author's line and exit 1

  **Reproducer.** An `extern` group declaring `function getsockopt(fd: i32, level: i32, name: i32, @value: i32, @len: i32) -> i32` against `<sys/socket.h>`, whose C parameter is `void *optval`. `heroes check` accepts the declaration; `heroes build` emits the pointee check as `_Static_assert(sizeof(void) == sizeof(int32_t), …)`, clang refuses `sizeof(void)`, and the compiler answers `internal error … clang refused the generated C`, **exit 2**. Measured on macOS arm64 (Apple clang 21.0.0) in the panel's frozen copy; the FFI seat's files are under the session scratchpad at `p108/ffi2/p108/` (`getsockopt_inout.hero`, first form), and the shape is one line to reconstruct from the sentence above.

  **Why it is a defect and not a diagnostic.** CLAUDE.md §7: a clang failure is exit 2 and says the compiler is wrong, with one named exception — a failure the author's own `extern` declaration caused is exit 1 and a diagnostic on the `.hero` line, in four classes recovered by `declaration()`. This is a fifth shape of the same exception: the author declared a pointee width for a parameter whose C type has none (`void *`), the generated assertion is unsatisfiable by construction, and clang's refusal names it. The narrowing is *whose declaration*, not *whose text* (§7), so it belongs on the `.hero` line.

  **What is owed.** Either the checker refuses `@x: <sized>` against a `void *` C parameter before any C is written (the header is available to the probe, and §4.19 already reads parameter widths off it), with a message naming `ptr` as the binding for an untyped pointer; or the emitter's pointee check recognises `void` and emits no `sizeof` for it, and the exit-2 path learns to recover this class into `declaration()`. The robust one is the first: the mistake stops before clang is asked. Attack the adjacent shapes before the commit (CLAUDE.md §1): `@x` against `const void *`, against an incomplete struct pointer, against a function pointer, and a `record` field of `void *`.

  **The repair.** Two halves in the compiler and one in its cache, because the class has two shapes and the cache hid the first. **`void` (and `const void`, and a typedef'd `VOID`)**: `cli/header_types.hero::check_unit` writes a guard before every width assertion — `_Static_assert(!__builtin_types_compatible_p(H, void), "heroes-ffi-pointee-void <name> <param> <declared> <H>")` — which fails as a proper `static assertion failed` line carrying the parameter's name (measured: `sizeof(void)` and `(void)-1` are hard errors no assertion can wrap, `__builtin_choose_expr` does not skip the type-check of its unchosen branch, and `__builtin_types_compatible_p` ignores qualifiers so `const void` is caught); `emit/ffi_pointee.hero::pointee_void` reads it. **An incomplete struct**: no assertion can turn `invalid application of 'sizeof' to an incomplete type 'struct opaque'` into a marker line — the line names the TYPE and nothing else — so `cli/pointee.hero::check` now hands its caller what the header said each ask points at (`pointee_ask.Answered`, filled on the cached path too), `cli/produce.hero` threads it into `emit/ffi.hero::explain`, and `pointee_unsized` finds the parameter from the type the line names. Both produce `ffi_parameter_type` on the author's line with the repair that is not another width — `ptr`, and no `@` — as a `guess` fix spanning from the `@` (found backwards over the spaces the grammar allows, in this source; measured `@ n` compiles) to the end of the type. **The cache**: the verdict was `verdict.txt` under a key of (fingerprint, asks, headers, search, runtime), and the fingerprint is `VERSION`, a constant — so the repaired compiler replayed the old compiler's verdict on the defect's own reproducer and answered exit 2 until `build/pointee-*` was cleared, measured; the verdict is now named by the digest of the unit clang judged. **Verified**: the reproducer, `tests/golden/fixedbugs/ffi-pointee-void.hero` (`memcpy`, C89, every leg) and `ffi-pointee-opaque.hero` (its own header beside it) all `error[ffi_parameter_type]` at exit 1, cold and warm; `ffi-pointee-width.hero` unchanged; a correct `frexp(@e: i32)` exit 0 cold and warm; two `suite_surface` rows assert the code, the words and that `internal error`, `invalid operands` and `build/` never reach the reader; three unit tests in `ffi_pointee.hero`; fixpoint byte for byte at 725,213 seed lines. **And the ceiling did its job**: threading `answered` took `selfhost/cli/produce.hero` from 297 to 307 lines against §11's 300, and the repair paid for itself along two real seams rather than a raised row — the two explain sites that were the same eleven lines twice became one `blamed`, and `unit_failure_text` and `floor_text`, byte-identical, became one `failure_message` — landing at exactly 300. **One language correction met on the way**: two `str` parameters made `verdict_path(dir, text)` `error[needs_label]` at both call sites, with a certain fix. **Windows, measured 2026-09-04 in the evening once the box answered** (the commit `a8b6c02b` says it was not, because the box was off when the commit was made): `git push win`, checkout at `3fe46bc0`, the documented flagless seed line builds the compiler in **7 s**, and the three goldens are `exit 1` with `error[ffi_parameter_type]` **cold and warm** — four digest-named verdicts on disk after the run — while `examples/tally/` builds and passes its 6 tests. One false reading on the way, and it was the script's: a warm loop that printed `exit 0` for all three, because in bash `$?` expanded inside the same `printf` as a `$(basename …)` is the exit of `basename`; measured with `false; printf "%s %s" "$(basename /x/y)" "$?"` → `y 0`. The Linux run of the identical script had said `exit 1` because Debian's `sh` is dash, which expands `$?` before the substitution runs. The exit is captured into a variable first now, and the reading is recorded in `docs/environment/windows/WINDOWS-MACHINE.md` beside its relative.
