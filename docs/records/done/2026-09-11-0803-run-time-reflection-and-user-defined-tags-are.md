- [x] **panel 131** — ratify the sitting: two refusals, a corrected reason, and one question deliberately not ruled | **RATIFIED AS ADOPTED 2026-09-11** (author instruction, the same evening the sitting closed: *yes to the two refusals, yes to the withdrawal, and go*) | **Run-time reflection and user-defined tags are refused**, each as a design.md Part 6 row carrying its own falsifier, and **the grounds are the ones this sitting measured rather than the ones the record was ready to write**. Reflection is refused on layout and not on interpretation: a descriptor is emitted per translation unit and only for a type used as a container element, so a two-record program with no container emits none and a value cannot reach its own type; `strip -x` deletes the symbol while `dladdr` still returns success with the name at `NULL`, which **overturns the 2026-09-06 `nm` finding**; metadata in the value takes raylib's `Vector2` from 8 bytes to 16, `offsetof(y)` from 4 to 12, and moves its floats out of `s0`/`s1` into `x1`; metadata in `HeroDesc` zero-fills 125 descriptors at exit 0 with zero warnings and reads `NULL`. Tags are refused on **inertness** and explicitly **not** on impossibility, because Java's annotations are declarations that resolve and a user-defined set therefore CAN be closed — what refuses it here is that nothing in this tree could read such a tag, macros and closures and reflection and a second binary all being refused, so a general mechanism would make §4.19's six contextual words cheaper to write and impossible to check. **The Ruby row's reason is corrected in design.md** and panel 039's C4 row underneath itself, because *requires runtime dispatch, i.e. an interpreter* is true of dispatch by name and false of reading a type's fields, and Go falsifies it. **What the yes does not settle, and the author chose that branch knowingly**: compile-time derivation is not ruled, the conservative alternative having been put in full beside it — rule it today, refuse it on the 0-of-31 measurement, close the milestone in one step — and *go* took the other one | `docs/panel/131-the-refusals-were-sound-and-their-reasons-were-not.md` · `design.md` Part 6 · `docs/panel/039-comptime-and-part-6.md` | the sitting's own finding is that three refusals are sound and three of their reasons were false, each falsified by a seat that went and looked
