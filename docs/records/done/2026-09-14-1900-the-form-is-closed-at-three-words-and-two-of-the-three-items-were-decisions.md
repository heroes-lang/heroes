- [x] **M-marked-acquisition** | R6 is DECIDED and the reason is the critic's, not a preference | **closed 2026-09-14** at this milestone's close, on the recommended resolution as CL-002 allows | **A missed release is a LOUD EXIT and not a compile error, and the reason is structural.** Panel 148's completeness critic found it and could not act on it: every text on that sitting's table amends `Member` and `CParam`, so the mark is **extern-only** — while this corpus acquires inside **Heroes wrappers** one module away, `opened` with 7 call sites and `prepared` with 6, because §4.19 refuses an `extern` call across a module boundary. A runtime counter does not care where the value goes next. **A compile error would need the obligation to cross an ordinary function's signature, and no ordinary `Param` in this language carries a mark of any kind** — not `owned`, not `consumes`, not `acquires`, not `borrows`. **The question was settled by accident before any seat saw it**: both drafts in panel 148's brief ended *"aborts when `main` returns"*, so a compile-error instrument was never on any seat's page, the comprehension veto included — which is recorded in that sitting as a failure of the coordinator's brief rather than of the seats. **What is decided is the instrument and not the ambition**, and the return condition is written so this does not sit forever: a measured program in `examples/` that leaks a handle and **continues** rather than exiting. `docs/measurements/031` § 3 records that none exists today — all 22 paths end in `exit(1)` or `abort` — so the day one is written, the loud exit reports at a moment nobody is watching and the case for catching it earlier is made by a program instead of an argument | `docs/panel/148-the-mark-is-written-and-never-inferred-and-it-names-what-ends-the-life.md` R6 · `docs/measurements/031` § 2 and § 3 · `spec/heroes-spec.md` § 4 `Params` | 148

- [x] **M-marked-acquisition** | the corpus discards 22 releaser return codes, and the question is now whether that still matters | **closed 2026-09-14**, re-measured against a ladder that had changed underneath it | **The measurement stands and the verdict inverted.** Two of the three shipped releasers report at all — `sqlite3_close` and `sqlite3_finalize` answer an `i64`, `curl_easy_cleanup` answers `void` — and the corpus discards the answer at **22** sites with `_ =`. design.md:1869 names the shape: *an ignored C return code is C's own classic silent bug*. **What changed between filing and closing is that the counter landed.** It reports the same class at exit 134 saying how many, for **every** library rather than for one, and at every site rather than where somebody remembered to look. So the return code stops being a candidate instrument for the leak — it is **subsumed** — and what survives of it is narrower and still true: `SQLITE_BUSY` is the library's own opinion about a handle, which a count cannot model. **The honest verdict was therefore NOT to build a third instrument**, and what would reverse it is named rather than left implicit: a measured case where `sqlite3_close` reports BUSY and the counter does **not** fire — a handle the library holds live for a reason the count cannot see. Until then the 22 sites are a question about how the corpus reports errors, not about what the compiler detects | `docs/measurements/031` § 4 · `examples/ledger/main.hero` · design.md:1869 · `runtime/parts/alloc.c` | 148

- [x] **M-marked-acquisition** | the form is closed at three words, and each says one thing | **closed 2026-09-14** at this milestone's close | **`acquires <releaser>`** says a call hands a handle over and names the one that takes it back; **`consumes`** says a call takes one back; **`borrows`** says a call hands back one it keeps, so the program owes nothing. **None is inferred and none is the compiler's guess**, which is panel 147's R4 and panel 148's R1 in three words. The third arrived last and only because R5 could not be built without it — a producer of a consumed type with no legal way to say *not this one* is the objection that killed the inferred option, and adopting half the answer to it brought it back. **The rule that needs all three is `selfhost/check/acquiring.hero`, 126 code lines**, keyed on the TYPE rather than the group because `one_tag_one_type` already makes one tag one handle type program-wide — the critic's finding, against the compiler seat's ~90-line estimate over a key `parse/group.hero` says does not survive the parser. It fires on both producing positions with the caret on the right token, and it is silent on all three shipped bindings. **And the corpus demonstrates the form rather than only the tests**, on the author's instruction of the same day: both SQLite bindings carry it, each with a comment saying what the words are for, and both run at exit 0 with no program line changed | `spec/heroes-spec.md` § 13 · `selfhost/check/acquiring.hero` · `examples/sqlite/main.hero` · `examples/ledger/db/sqlite.hero` · `tests/golden/check/unmarked-handle-producer.hero` | 147, 148
