- [x] **The version scheme, decided ahead of M-install-channels: `vX.Y.Z` tags, `v0.1.0` first, `Y` when the spec moved, `1.0.0` the gate's** | decided 2026-09-07 from the author's question (*"how do we handle releases, and what number do we start from?"*); six choices put with a recommendation, all six taken; the `DESIGN-LOG.md` row of that date carries the measurements and the conservative alternative | **What was measured first, at `9b4a3494`**: `heroes --version` is `heroes 0.0.1` and has never moved; that number is line 1 of 213 of 213 blessed files under `tests/emission/`, compared byte for byte by `tests/harness/suite_emission.hero`, and of the six `.expected` under `tests/golden/emit/` (found by the net going 6 red of 1609 after the plan had counted 213: the grep had listed them in an output read only at its head and tail), plus eight fixture uses in the emission suite itself; 37 tags, all `m-*`; no GitHub Release; the repository private; 59 chain rows, 37 closed. **Why 0.1 and not 1.0**: the project's `v1` (design.md §1.0, self-hosting) is a milestone's name; the world's `1.0` is a compatibility promise nobody has written, and M-publication-gate owes it. **Why now and not at row 57**: a release procedure first run at the gate is first run at the worst moment. **Where the rule lives**: CLAUDE.md §14 § Release tags, its only home; `docs/ROADMAP.md`'s M-install-channels and M-publication-gate entries gained one appended sentence each; the `docs/work/SCHEDULED.md` item for M-install-channels lost *"the version question, which nothing else owns"* and kept the channels; `.github/workflows/ci.yml` gained the `v*` trigger, the `--version` assert on every leg, and the `release` job | CLAUDE.md §14 · `DESIGN-LOG.md` 2026-09-07 · `docs/ROADMAP.md` M-install-channels, M-publication-gate · `.github/workflows/ci.yml` | a number in one place, moved by one commit, checked by one instrument
