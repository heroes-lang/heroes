- [x] M-declared-freer step 2's companion | **BUILT 2026-09-06, and the recommendation was taken: the instrument, not the seven exemptions.** The check now reads **every living file** (records exempt by §14) in prose and comments only, **bare tokens as well as backticked ones**, with **brace forms expanded** — which retires the limit panel 086 measured and recorded as permanent — and resolves the way a READER resolves: from the root, through the house shorthand (`ir/lower.hero` is how this tree writes `selfhost/ir/lower.hero`), and from the citing file's directory or any above it. **There is no exemption list**, and the four shapes of legitimate path-like prose that would each have wanted a line in one fall out by principle instead: a path inside a **string literal** is data (`docs/NOSUCH.md` in a fixture proves the compiler says *no such file*); what **git ignores** is not a dead citation, and git is asked rather than guessed at; a **brace that does not close on the line** is half a promise; and the evidence a sentence must carry is a **DATE**, which was measured rather than chosen — a resolving sibling was accepted until `runtime/parts/run.c` was found silencing a real dead citation by coincidence. **From 91,864 path claims read to zero dead ones**, in fifty repairs that each turn a sentence into a dated one. **The seven exemptions the cheap option needed: zero.** Cost: the `records` suite 7 s → 17.7 s. **Four defects surfaced while it was built**, and they are the item's real return: the panel's compiler seat was being sent to an archived tree by its own definition file while the panel skill said *"Never `crates/`"*; `docs/work/DONE.md` was a record missing from `RECORD`; two sentences this session had broken in `f0d95197`, where a mechanical sweep is safe for pointers and not for prose about the move; and `runtime/parts/run.c` claimed a test that `git log --all --diff-filter=A` says was never written, which is now owed in `docs/work/SCHEDULED.md` | found while writing four module docs for the split | `tests/harness/suite_records.hero` § citations | the check exists to catch a moved tree, and the one thing it could not see was the compiler's own source
