- [x] panel 089 | **ONE LANDED, ONE REFUSED — and the refused one is the finding** (2026-08-24, author instruction *"fai queste"*). **Rider 1 lands**: a `cstr` FIELD now asks the six spellings of a C string instead of the member's address identity, so `struct passwd`'s `pw_name` and `pw_dir` bind — measured, exit 0, where it was `ffi_field_type` on a field the author had spelled correctly. Widening is sound because `cstr` is read-only from the Heroes side: `const` restricts writes and the language offers none through a `cstr`. Attacked at the neighbours: `char *`, `const char *`, `signed char *` and `unsigned char *` accepted; **`void *` and `int` still refused**, so panel 083's repair is intact. The seat's suggestion — reuse `HERO_RET_CSTR` — **is not available**: field assertions are emitted at `emit_decls.hero:84` and the macros at `:86`, so the `#define` does not exist yet. The spellings therefore live in `emit_assert_spelling.CSTR_SPELLINGS`, read by both positions, because a list written twice dies in silence (§11). **Rider 2 is REFUSED, and the seat's argument for it is false.** It held that `ffi_writable_parameter` should not fire on an `@` parameter, since §4.8 makes that the address of a Heroes local rather than a lent COW interior. Measured before writing a line: `char **` lets C write THROUGH the pointer — `void evil(char **s) { (*s)[0] = 'X'; }` compiles clean and turns `ziggy` into `Xiggy` — and that local's value may be exactly a lent COW interior, which is the corruption the diagnostic exists for. The same line through `const char **` is a **C compile error**, *read-only variable is not assignable*, which is why the const form **already works today**: measured, `C filled it with: filled by C`, exit 0, and with `validated` it is complete. So the compiler's existing split is exactly right and the rider would have opened the hole. **The seat's premise was about the world** (*"an `@` parameter is not that"*) **rather than about the value in hand**, which is the shape panel 089 spent its whole sitting cataloguing | selfhost/emit_extern_field.hero · selfhost/emit_ffi_mutable.hero · docs/panel/083 | §1.12 says the boundary must be complete, and one of these two would have made it unsound instead The seat compiled all five: a result `const char*` (now sound), a `const char **` out-parameter (sound), and three that no option touches — a **`char *` struct field** is `error[ffi_field_type]` because the emitted `_Generic` asks for `const char **` and a `char *` member is a third type; a **`char **` out-parameter** is `error[ffi_writable_parameter]`; a **callback taking `const char *`** is `error[type_mismatch]`. Two have a compiled repair: `HERO_RET_CSTR`'s own six-way set applied to the *member* accepts `pw_name`, `pw_dir` and `pw_shell` from `struct passwd` — one line in `selfhost/emit_extern_field.hero:148`; and `ffi_writable_parameter` exists because `s.cstr()` lends a COW buffer's interior, which **an `@` parameter is not** (§4.8 makes it the address of a Heroes local, and `selfhost/emit_ops.hero:163` already excludes `@cstr` from the null guard *for this exact reason*) — the same narrowing is missing here | docs/panel/089 § completeness table · selfhost/emit_extern_field.hero:148 · selfhost/emit_ffi_mutable.hero | §1.12 says the boundary must be COMPLETE, and this sitting repaired the one shape that already had a working route
