- [x] **M-cleanup-verdict** | the sitting, and the count it is handed rather than guesses | **closed 2026-09-14** at the milestone's own close | **The count was taken before the sitting, as this item demanded, and it rewrote the question the sitting was convened on.** `docs/measurements/030` is the census. Of the three release obligations the tree carries, **only one is silent**: `owned <freer>` puts **no** obligation on any path — the compiler frees at the call inside a null guard, read in the emitted C and confirmed by running an early return between the acquire and the end of the function — and `lease` puts one whose miss is **LOUD**, exit 134 and *"1 lease(s) never ended"*, saying how many. **The milestone file said both put an obligation on every path; two thirds of its premise was false**, which is M-deferral-ledger's own finding of one milestone ago happening to the document that scheduled this one. The C handle is the third and the only silent one, measured twice by asking SQLite itself rather than a leak checker, because LeakSanitizer does not exist on Darwin arm64. **And the census narrowed the sitting's question from *should Heroes have `defer`* to *can a C handle join the exit sweep the language already walks*** — read in `examples/curl/main.hero`'s emitted C, the three `str?` slots each get a `_retain` and a `_release` while the `Curl` handle gets `_eq` and `_hash` and nothing else, so the sweep does not skip handles by policy, it has nothing to call. **The sitting is panel 147** and its answer is that the narrowing was still not narrow enough: the obligation is created by a **call**, so no mark on a type can carry it. **Four of the census's own numbers were wrong and are corrected underneath it**, found by the sitting's completeness critic because nobody had audited the document five judges were handed as fact: exposure is 22 paths in one file rather than 23 in two, the pair list was short by at least seven so it is ≥19 and not 12 (CL-057 — the list is a measurement too, and this one was read rather than enumerated), *"10 of 21 pairless"* is 8, and **all 22 paths end in `exit(1)` or `abort`**, so no shipped program leaks a handle and then goes on running. The class survives and the urgency does not | `docs/measurements/030-three-release-obligations-and-only-one-of-them-is-silent.md` · `docs/panel/147-the-obligation-is-created-by-a-call-and-not-by-a-type.md` · `selfhost/ir/own.hero` rule 5 | 147

- [x] **M-cleanup-verdict** | a C handle advanced by a loop is a third obligation, and neither `owned` nor `lease` reaches it | **closed 2026-09-14** at the milestone's own close, on panel 137's finding standing while the other two premises fell | **Panel 137's ffi seat was right and the milestone file's own framing around it was not.** This item said a handle is a *third* obligation beside `owned` and `lease`; the census measured that it is the **only** one — the other two impose nothing a programmer can miss silently. The shape the item filed is reproduced and the reproduction is in the record: a `break` out of a stepping loop and a `?` firing inside one both give **1 statement still open**, **`sqlite3_close` says BUSY**, **exit 0**, and the identical program in C answers the same, so the leak is the program's and not this language's. **What closes the item is the ruling rather than a repair**: panel 147 refused a releaser keyed on the handle type — it now has a design.md Part 6 row with a falsifier — and admitted a form marked on the acquiring **call**, which takes `M-marked-acquisition`, entered in the chain at 57 the same day. **The item's own witness is where the answer came from**: `examples/ledger/main.hero` holds the four `?` operators inside the loop while `finalized` sits outside it, and the spec-warden's reading of that file is what killed the type-keyed form — those statements are acquired inside `sqlite.prepared(...)`, a Heroes wrapper in another module, so the scope that acquires is not the scope that leaks and a mark on the `extern` record reaches none of them. **A third finding the item did not ask for and the sitting produced anyway**: the corpus leak in `examples/sqlite/main.hero` — a failed open still hands back a connection and that program dropped it, beside a sibling carrying seventeen lines of comment measuring exactly that on two platforms. Repaired the same day | `docs/panel/137-the-hole-was-two-operations-wide-and-the-answer-was-a-library-function.md` § Found beside the sitting · `examples/ledger/main.hero` · `examples/ledger/db/sqlite.hero` · `docs/work/milestones/M-marked-acquisition.md` | 147 (137 inherited)
