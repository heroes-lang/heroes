- [x] Covered | M-declared-thresholds | **DECIDED 2026-09-13 (author: convert the runtime's floors now; leave CLAUDE.md §5 and §6 where they are).** Two questions M-declared-thresholds carried past its own close, put with a recommendation and both answered the recommended way. **The floors converted, and the estimate that had carried them forward was WRONG** — recorded rather than quietly corrected. They were judged low risk on the ground that they count things which do not grow; `floors.check` found all three outgrown on its first run: `LEAST_FILES` 18 against **23** runtime source files, `LEAST_AT_THE_POINT` 3 against **7** allocation calls inside the single point, `LEAST_SHARED_STATE` 24 against **39** objects surviving between calls. Two of the three had more than doubled. The reason they were carried forward at all was real and is now closed: two floors sat in one `||` condition, which `floors.check` cannot weigh — split, each says its own number and each is watched for decay. **The file had already told this story about itself**: its own comment records a parenthesis that said *"three (one `malloc`, two `free`)"* and expired in six hours when `hero_grow_kept` added the fourth, *"inside the suite written to stop premises expiring in silence"* — and then its floors did the same thing for weeks. **§5 and §6 stay**, against the 179 vendored / ~238 real the warden costed for collapsing them to pointer lines. Three reasons, in order: the contract has **4886 tokens of room**, about fifty days at the measured rate, so nothing is being paid for; section numbers can never be reused (CL-069, thousands of citations and no instrument that sees them), so the saving is smaller than the figure suggests; and a section stating that a rule is SPENT is what stops a later session re-deriving it, which is the failure the contract exists to prevent. The removal stays costed and unspent, named in `docs/records/journal/052-declared-thresholds.md`, for the day the delta gate makes an amendment actually pay for itself.
