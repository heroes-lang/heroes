- [x] **`panel 112` — ratify the refusal of a `const` spelling, the wart that replaces it, and the one defect it could not repair** | the sitting sat 2026-09-05 for M-c-callbacks step 4, five seats, frozen at `aab44f9b`; the synthesis is `docs/panel/112-const-is-the-wrong-noun.md` | **The sitting refuted its own brief before it answered it, and that is the headline.** It was convened on panel 111 R4's owed item — the `c_int`/`const` vocabulary or a Part 8 wart — and the ffi seat measured **`sqlite3.h` itself, 106 callback signatures**, instead of the coordinator's eight hand-picked ones. A `const` spelling unlocks **1 of 106**. A spelling for *a pointer to a struct the header declares* unlocks **64**. **The sitting was convened on 2.6% of its own subject**, and the coordinator's count of what `const` buys was also wrong in his own favour: 2 of the 8, not 3, because `nftw` is blocked by a `struct FTW *` that is not const at all. **R1, refuse (a) and (b)** — four seats, four independent measurements: a new `Ty` case is **173 `non_exhaustive` errors across 49 files** (core, not sugar, by Part 5's own test); the cheapest spec wording is **+13** and the full vocabulary **+49** against 266 of headroom; `selfhost/` binds **zero** callbacks so Principle 0's compiler-need branch fails; and the reader given only the spec cannot reach the question at all, because nothing turns a `[i64]` into a `ptr` and nothing reads through one. Panel 083 R4's veto stands and is **stronger than in August**: then it rested on a citation whose Part 7 half expired with the fixpoint two days later, now it rests on numbers. **R2, the wart lands with the ffi seat's ordering** — Part 8 wart 19, zero spec tokens, naming pointer-to-header-struct first (102 occurrences) and `const` fourth (4), plus a fifth cause nobody had named: an out-parameter **inside** a callback is a parse error, 26 occurrences in `sqlite3.h`. **R3, two of three shipped defects repaired here**, both mine and both the same shape — a claim written without being run: a diagnostic citing a Part 8 wart that did not exist, and a needle matching `const char *` that told an author *"this cannot be written at all today"* when the one-character repair builds at exit 0. **R4, two false sentences in design.md corrected**: *"9 out of 9 fail"* (three bind) and the shim's *"how it is compiled is undecided"* (`static inline` compiles with no build rule, and it binds `sqlite3_exec` today in 28 lines). **R5, the historian's adapter is recorded, not adopted**, with its return condition; the **cast** form is refused permanently, measured lethal under all fourteen flags at exit 0 with an arbitrary permutation. **R6, the `quals` shape is recorded as the only admissible route** if the const half is ever closed — no `Ty` variant, so panel 083's veto does not fire, and **2 edits in 1 file** against 173 errors in 49. **The default the compiler is running on** while this waits: no const spelling, the wart standing, and **defect 013 open** — `atexit(f: ptr)` handed `nullptr` compiles and exits 139. **That third defect is the one the sitting could not close and the one thing here that is really a question for you**: the proposed repair (`-Wpedantic` in the probe block) fires twice on `examples/sqlite/main.hero`, which passes a deliberate NULL callback and is **correct**. Either land the flag and rewrite a correct example, or let `nullptr` inhabit a function type — a spec change that does not close the defect anyway. **The conservative resolution, recorded so you can take it instead**: adopt the wart with the brief's own wording — const-qualified pointees as the hole — and stop. It changes least, and it writes the 2.6% cause into the design document as though it were the subject | docs/panel/112-const-is-the-wrong-noun.md · docs/work/DEFECTS.md (013) · design.md Part 8 wart 19, §4.19 · docs/panel/083, 111 R4 | a sitting convened on the wrong noun found the right one by measuring a real header instead of a sample, and found three of the convener's own claims false on the way

**RATIFIED IN FULL 2026-09-05** (author, *"ratify everything and commit"*, a blanket yes recorded as one). R1 through R6 stand as adopted. **What the yes does not settle**: defect 013's route, because the sitting filed it with two prices and no recommendation — `-Wpedantic` plus rewriting a correct example, or `nullptr` inhabiting a function type, which is a spec change that does not close it anyway. That stays in `docs/work/DEFECTS.md` with both measurements, and the coordinator's recommendation is recorded there rather than defaulted into the compiler.
