- [x] **023 — the fix for `mixed_arithmetic` on a `str` reproduced the same error** | filed 2026-09-09 out of the spec-warden's steelman at panel 121, repaired the same day | **The advice was a loop, measured.** `"count " + n` gave `mixed_arithmetic` whose `fix (guess)` offered `to_f64(x)` and `to_i64(x)`; following it gave the identical diagnostic back with `str` and `f64`, and `n.to_str()` — the repair that works, and a built-in the spec lists — was never named. The title was written for two numbers of different widths and emitted unchanged when one side is text, where no `to_<width>` can help, because they answer a number and `+` on a `str` wants a `str`. **The repair is two fixes, because the reader's mistake differs**, written as an `if` EXPRESSION rather than two pushes — which is also what kept the file inside §11's threshold when the first draft put it at 301 lines of code, one over, and `layout` said so. **And the instrument that can see it is the module's own test, because no golden can**: `.expected` compares `check --brief`'s stderr, which carries no `fix (…)` line, and `suite_fixes` tests only `certain` fixes through `.fixed`, so a `guess` fix's TEXT is unwatched by every golden — the gap `docs/journal/039-closures-verdict.md` filed rather than tapped shut, and the reason the `fit_` fix could have stood indefinitely. Two tests now pin both titles WHOLE rather than by substring, since `contains` lives in `ir/mono` and importing it into a leaf every module uses would buy a cycle. 595 → 597 | `selfhost/value_errors.hero` · `docs/panel/121-the-brace-was-already-taken.md` · `docs/journal/039-closures-verdict.md` | a language whose thesis is that every plausible mistake is a compile error told a reader to do a thing that produced the same error, and the only instrument that could ever have caught it is a test somebody had to think to write
