- [x] **CLOSED 2026-08-26, author decision `/decide` answer `6a`: the instrument is built.** `tests/harness/suite_special.hero::one_bad_argument_does_not_kill_the_other_two` — three arguments, the middle one `$(printf '\377\376')`, and `args_checked` keeps 1 and 3 while naming 2 by position at exit 0, with `args()` asserted to abort at 134 beside it so the two spellings cannot collapse into one. **It found a defect on its first run and that defect is a NEW open item below**: `heroes run prog.hero -- <bad>` kills the compiler, so the case builds the binary and runs it directly. Original text: M-separate-compilation close, scoring the ledger's registered predictions | **`args_checked` bought 11 spec tokens with a prediction whose instrument was never built, and the prediction is now FALSE.** The ledger row at 3541 spent **+11** on the `args_checked() -> [str?]` clause under panel 012 branch 2 — an addition paid by a registered falsifiable prediction rather than by a removal — and the prediction reads: *"at M-separate-compilation close, a `run/` golden handed one ill-formed argument among three prints the other two and exits 0"*. Measured at the close, 2026-08-26: **no file in `tests/golden/` or `examples/` uses `args_checked` at all** (grep, zero hits), and `suite_run` has **no mechanism to pass argv** to a golden, so the promised instrument could not have existed in the shape the row names. What does exist is the opposite witness: `tests/golden/run/edges-file-args-exit.hero` uses `args()` and, handed `good.txt $'\xff\xfe' more.txt`, dies at **exit 134** with `panic: hero_str_from_bytes: not well-formed UTF-8` — which is correct, because `args()` is specified to abort. Panel 046 R2 says a falsified prediction sends its clause back here to be re-argued under the removal branch, never renewed under a new milestone name. **Two ways out, and the cheap one is cheap**: (a) build the instrument now — the right home is `suite_special` or a `suite_surface` row, not a `run/` golden, because the shell can spell an ill-formed byte with `$(printf '\377\376')` and the harness already runs arbitrary command lines; or (b) argue the clause on its merits without a prediction behind it, which is the removal branch. **The default while it waits**: `spec:186` carries a sentence whose registered evidence does not exist | docs/measurements/010-spec-budget-ledger.md (row 3541) · tests/harness/suite_run.hero · tests/harness/suite_special.hero · spec:186 | a budget whose additions are paid by promises is only as honest as the promises that get collected, and this is the first one measured uncollected
