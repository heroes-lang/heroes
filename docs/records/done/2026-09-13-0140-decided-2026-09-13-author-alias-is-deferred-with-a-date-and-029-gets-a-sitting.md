- [x] Covered | panel 135 | **DECIDED 2026-09-13 (author: ratified as adopted, nothing changed — and defect 029 gets a milestone-verdict of its own, immediately after the ledger).** The question was whether design.md Part 7 item 5, `alias`, is deferred again with a dated return condition, or ruled ENTERS or REFUSED. The author ratified the sitting's resolution **in full and unchanged**, the same night it sat: the deferral with its date, the three routes of the return condition, the three struck claims, and the settled shape of the form. **The measurement the verdict rests on**: the compiler compiles itself with zero uses of the form and `alias` is not even a reserved word, so Principle 0's first branch is unmet; and the second is unmeasurable rather than merely unmeasured — metric 3 (`heroes mutate`) is blind to a transparent alias by construction, since the alias and its target are one type and produce one kill rate, while metrics 2 and 4 have no instrument in the tree. The token case was run: one alias for the six copies of a 12-token function type in `examples/interpreter/run/expr.hero` saves **48 tokens, 1.4%** of that file, the strongest case in the whole repository, and `selfhost/`'s most repeated function type is generic and cannot be aliased at all. **The second half of the answer is a scheduling decision, taken on the assistant's recommendation and against the cheaper route of widening this milestone**: defect 029 — `sqlite3_step(db)` for `sqlite3_step(statement)` building at zero diagnostics and exiting 139, because every C pointer is one Heroes type — is not repaired at the witness and is not this ledger's to take. It gets **`M-handle-verdict`**, row 55, the ruling on whether one C handle can be told from another, in `M-closures-verdict`'s shape: a form enters with its own milestone, or it lands as a Part 8 wart or a Part 6 row naming the program that would make it wrong. The reason the ledger does not absorb it: this milestone dates Part 7's items, and a new question found beside one of them is a new question | **What the yes does not settle, said rather than implied.** It does not settle whether `alias` ever enters — the return condition decides that, and every later sitting of this ledger re-reads it: a non-generic spelling of ≥ 12 tokens at ≥ 10 sites in one file (today 6), a commit changing one structural spelling at ≥ 8 sites (Go's own warrant), or item 10's sitting at M-core-packages naming it as the C-width vocabulary's mechanism. And it does not decide 029's repair, which `M-handle-verdict` may answer with a refusal. **One consequence named at the ratification rather than discovered at the close**: `records/tagged` allows no tag over an open defect, so **M-deferral-ledger closes untagged** while 029 stands — M-anchored-spec's precedent at chain rows 45 to 47 — and its tag lands on the first commit where every list it leaves open is clean, the way `m-cstr-lifetime`'s did on 2026-09-09. The chain therefore grows to 77 rows and `ROWS` to 81 | CLAUDE.md §2, §4, §12, §14, § Verification; design.md §1.0, §1.6, §1.7, §1.12, Part 7 item 5 | 135
