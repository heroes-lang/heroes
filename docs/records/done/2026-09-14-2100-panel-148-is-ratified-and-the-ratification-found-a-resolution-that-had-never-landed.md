- [x] **panel 148** | the surface of the mark panel 147 admitted: option C refused on twelve headers of nineteen, and the mark that lands is written and names what ends the life | **ratified 2026-09-14**, all six resolutions as adopted, the robust one and not the conservative one | **The author declined the bare `acquires` at +58** recorded at R5's end, so the mark names its releaser and the completeness diagnostic stands. **The ratification came after the milestone had closed and been tagged, which made it a verdict on what SHIPPED rather than on what was proposed — and that is what found the gap.** **R1's design.md Part 6 row did not exist.** The resolution said the inferred obligation goes to Part 6 with the ffi seat's falsifier; the milestone closed; no row was written, and `grep SSL_get0_peer_certificate docs/design/design.md` was empty at the ratification. It was written then, carrying the twelve-of-nineteen count, the adjacent-lines pair in `openssl/ssl.h`, the `-Weverything` probe that shows clang cannot separate them either, the compelled-free direction that makes it §1.12 corruption rather than §1.12 leak, and the falsifier verbatim. **A resolution is not landed because a sitting adopted it**, and nothing in the close checklist asks whether the Part 6 rows a sitting orders were actually written | `docs/panel/148-the-mark-is-written-and-never-inferred-and-it-names-what-ends-the-life.md` § Author's verdict · design.md Part 6 · `docs/measurements/010` rows 154 and 155 | 148

- [x] **panel 148** | the six predictions the sitting registered, scored at the ratification | **scored 2026-09-14**, four of seven decidable today | **Two held, two missed, and both misses are about units rather than about the world.** **P2 HELD** — it is the measurement C was refused on, reproduced twice against real libraries. **P3 HELD on both halves**: `check/acquiring.hero` is **126** code lines against a 120–280 band, at the low edge, and the checker moved **+0.30% real and +0.73% user** against a compiler built from the seed at `7e3bb986` in a detached worktree, three runs each with the machine still, minimum taken, `real`/`user` at 1.005 so nothing was waiting — and the figure is conservative, because each compiler checked its OWN source and so carries step 5's growth too. **P4 MISSED on the band and HELD on the headroom**: the landing text measured **7921** where the seat predicted 7864–7896, 25 over, while leaving 271 free against the then-ceiling where 236 was asked; the seat priced the text and not the two repairs its own review then added. **P5 MISSED on the instrument it named and HELD on what it meant**: `grep` reads **five** `acquires` mark sites where it said three, and they are **three** distinct C producers, because the corpus binds SQLite twice after the author asked the shipped examples to demonstrate the form. It named three FUNCTIONS and wrote *marks*, with no unit beside either number. **P1 and P6 are not yet checkable** and stay registered, at the next binding that grows and at a Part 11 run | `docs/panel/148-...md` § Predictions to score · `docs/measurements/010` | 148

- [x] **panel 148** | R5 shipped needing a word the resolution did not name, and R6 was settled after the sitting rather than by it | **recorded 2026-09-14** at the ratification, as divergences and not as drift | **R5** adopts the ffi seat's axis — a producer of a consumed handle type must say which it is — and step 4 landed `acquires <releaser>` alone, under which a BORROWING producer has no legal spelling and the rule would refuse a correct binding. `borrows` landed at step 5 at +43 vendored, and the hole was found by building the rule rather than by reading the sitting back. **R6** the sitting returned undecided, saying its own brief had settled it by accident — both drafts ended *aborts when `main` returns*, so no seat ever saw a compile-error instrument, the comprehension veto included. The milestone's close DECIDED it on the critic's structural ground: a loud exit, because the mark is extern-only while this corpus acquires inside Heroes wrappers a module away, and a compile error would need the obligation to cross an ordinary signature that carries no mark of any kind. **Its return condition is named rather than left open**: a measured program in `examples/` that leaks a handle and CONTINUES, of which none exists today — all 22 paths end in `exit(1)` or `abort` | `docs/panel/148-...md` R5, R6 · `docs/measurements/031` § 2 and § 3 · `runtime/parts/alloc.c` | 148
