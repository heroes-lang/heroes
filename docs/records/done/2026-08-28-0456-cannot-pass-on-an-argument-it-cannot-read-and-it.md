- [x] **CLOSED 2026-08-28, answer `6a`** — `selfhost/main.hero` reads `args_checked()` instead of `args()` and refuses a malformed argument with a diagnostic naming its position, at exit 2. Measured before: `panic: hero_str_from_bytes: not well-formed UTF-8`, **exit 134** — the compiler dying on an argument the shell chose and `heroes run` was only passing along. Measured after: `error: argument 4 is not UTF-8, and a Heroes str cannot hold it (spec:52)` plus a note telling the author to build the binary and read it with `args_checked()` themselves, and the position is right. **Option (b) was refused for the reason the item already named**: a Heroes `str` IS UTF-8 (spec:52), so there is no value the compiler could hold that argument in, and machinery to carry it past the `str` boundary would be new C-boundary work for a case where refusing is honest. Exit 2 rather than 1 because the tool could not run, which is §10's contract, not because the program is wrong. Was | found 2026-08-26 by the instrument `/decide` answer `6a` built | **`heroes run` cannot pass on an argument it cannot read, and it aborts instead of saying so.** Measured: `heroes run prog.hero -- $(printf '\377\376')` is `panic: hero_str_from_bytes: not well-formed UTF-8`, **exit 134** — the COMPILER dying, not the program. `selfhost/main.hero:55` reads its own command line with `args()`, and the arguments after `--` are not its own. The consequence is exact and worth stating: **`args_checked`, the one spelling the spec provides for surviving such an argument, is unreachable through the dev loop** — only a binary built with `heroes build` can use it, which is why the new instrument builds one. **This is a limit to decide rather than a fix that was skipped**: a Heroes `str` IS UTF-8 (spec:52), so the compiler cannot represent that argument in order to hand it on. **Three ways out**: (a) `main` reads `args_checked()` and refuses a malformed argument with a diagnostic at exit 2, naming its position and telling the author to build the binary — honest, and the most `run` can ever do; (b) the same, but the refusal fires only for the compiler's OWN arguments and everything after `--` is passed through by some route that never becomes a `str` — new machinery at the C boundary; (c) leave it, and let the dev loop die on an argument the shell chose. **The default while it waits**: (c), and the abort names a runtime function rather than the author's command line | selfhost/main.hero:55 · tests/harness/suite_special.hero (the case, with the measurement in its comment) · spec:186 · docs/panel/089, 090 (the same shape one department over: the compiler could be killed by an environment variable) | §12 says a build tool's worst outcome is a wrong answer, and its second worst is dying on input it was only asked to carry
