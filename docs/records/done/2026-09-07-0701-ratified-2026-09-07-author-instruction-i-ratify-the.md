- [x] **panel 117**, 2026-09-07 | **Both sides of an `assert` when a side is an aggregate: RATIFIED, all four parts, and the ratified route is the sitting's own sixth option rather than any of the four the brief carried.** | **RATIFIED 2026-09-07** (author instruction, *"I ratify"*), the same day the sitting was convened, and after the author had already overridden one seat's wording condition in the direction of the more robust text (*"better robust than cheap"*) | **What the sitting was asked** was whether `assert` owes both sides for an aggregate and by what mechanism, on open defect 016, deliberately narrow so the wider reflection ruling stayed with M-reflection-verdict. **What reframed it was a measurement no option could survive**: no mechanism can keep `spec:202`'s promise for `{K: V}` at all, because two `==`-equal maps iterate in different orders (`{"a":1,"b":2,"c":3}` gives `b a c`, the equal `{"a":9,"a":1,"b":2,"c":3}` gives `b c a`) and sorting is refused by `runtime/parts/sort.c`, which picks a comparison by descriptor pointer identity over twelve scalar descriptors, so a map with record keys has no order to print in. It was never silence versus promise; it was narrowing honestly versus shipping a promise false by construction. **The sixth option, which no brief carried and which the historian found by reading Zig's source**: `std.testing.expectEqual` never renders the aggregate — it recurses to the differing leaf and prints two scalars, and its doc comment says the point out loud, that it shows exactly how they are not equal. It answers every objection the sitting raised **by construction**: no iteration order, so the map measurement stops being an obstacle; `eq`'s own walk, so panel 076's worklist already carries the depth; no rendering, so the third invariant clause *equal values render equal* is never owed and `selfhost/emit/structural.hero`'s seven remaining lines under §11's ceiling are not spent; no ABI event; and nothing that can go quiet, which is D's `-checkaction=context` failure mode the historian sourced with its seven-year-old open defects. **What ratification settles, part by part.** The aggregate row in `tests/harness/suite_special.hero` is a ratified tripwire, pinning today's behaviour so the day a mechanism lands it goes red. The spec sentence stands as landed at **3903** tokens rather than the spec-warden's 3876, and that seat's objection stays on the record unwithdrawn, which is what a ratified override looks like here. The leaf walk is the **ratified route** rather than a preference, so a future sitting wanting another mechanism is overturning a decision and owes the argument. And the two refusals are ratified refusals: a sixth `HeroDesc` member with the FFI seat's compiled evidence behind it (with the ABI left at 21 the seed compiles at exit 0 with zero warnings while 109 descriptors carry `render = NULL`, and the first aggregate `assert` then dies `SEGV on unknown address 0x0 (pc 0x0)`), and the per-type renderer refused *for now*, its return condition being the compiler seat's own — a deterministic `{K: V}` rendering needing no total order over non-scalar keys. **What ratification does NOT do**: it does not close the work. Defect 016 closed because a contradiction between two artifacts closed; the language still cannot render an aggregate, and the schedule for that is now the author's decision rather than the sitting's proposal | `docs/panel/117-both-sides-of-an-assert-when-a-side-is-an-aggregate.md` § Author's verdict · `docs/work/SCHEDULED.md` at M-reflection-verdict · `spec/heroes-spec.md` 187, 202 · `tests/harness/suite_special.hero` | a sitting convened on four options and ratified a fifth that came out of the room, which is the whole reason the room exists
