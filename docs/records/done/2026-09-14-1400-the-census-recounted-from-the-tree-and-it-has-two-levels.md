- [x] **M-marked-acquisition** | re-measure the census before arguing from it, because its urgency is the one number that can grow | **closed 2026-09-14**, M-marked-acquisition step 1, and the re-count is `docs/measurements/031` | **Counted from the tree with a command this time, not read off twelve function bodies, which is what CL-057 asks and what `030` broke.** The correction that matters is structural rather than arithmetic: **the census has TWO levels and `030` flattened them into one.** At the extern level the tree holds **7 acquire sites and 7 release sites**. At the wrapper level — the one `030` never counted — `examples/ledger/db/sqlite.hero` wraps all four operations in Heroes functions, because §4.19 refuses an `extern` call across a module boundary, and the rest of the program touches only those: `opened` **7** call sites, `prepared` **6**, `closed` **9**, `finalized` **6**. That is the structure that refused Route A at panel 147, and it is the structure any mark must survive: the acquiring `extern` call is one module away from every place a handle is held, so a mark **propagates** through a Heroes wrapper the way `consumes` did at 24 lines across `examples/ledger/`. **A second instrument correction is recorded rather than quietly fixed**: `030`'s handle enumeration used `[A-Za-z_]+` for a C tag, which excludes digits, so `tag sqlite3` never matched and it answered **3** where the tree holds **9**. **The critic's fourth correction is confirmed and made precise**: every failure path in `examples/ledger/main.hero` closes the database and calls `exit(1)`, so the CONNECTION is released on every path and only the STATEMENTS leak, and the process leaves at once. No shipped program leaks a handle and then continues — a fact about this corpus on this date, which the first program written here that recovers from a database error instead of exiting will end. **And the urgency did not grow**, which is what this item existed to find out. **One finding the item did not ask for**: the producing-versus-borrowing ambiguity that killed a type-keyed releaser does not appear in this tree at all — `sqlite3_db_handle` and `sqlite3_next_stmt` are bound by no binding here — so the blast radius of a mark is smaller than the sitting's argument implies, and the first binding that declares one brings it back. Filed as a premise rather than as a reassurance | `docs/measurements/031-the-census-counted-from-the-tree-and-it-has-two-levels.md` · `docs/measurements/030` § Corrected 2026-09-14 · `examples/ledger/db/sqlite.hero` · CL-057 | 147
