- [x] Covered | M-declared-thresholds | **The contract is judged on the reader's tokeniser**, closed 2026-09-12 by the milestone that filed it. `CONTRACT_CEILING` 5500 -> 12288, written as `7306 + 4982` and never as a round figure; `selfhost/measure/pinned.hero` carries a second record; `run_refresh` takes two judged documents and still refuses every other path; `measure` prints a `real` row for the contract where it printed none. The number was MEASURED by this session's own tool before it was set, which was the warden's condition: 7306, and 7402 after this milestone's own edits to the contract. **What the item could not know**: `run_refresh`'s refusal of every file but the spec was pinned by a test, so extending it had to keep the refusal for unjudged paths — `design.md` is the witness kept in the case, because §1.6 says in its own words that the compiler has no budget.
- [x] Covered | M-declared-thresholds | **The delta gate landed with the raise**, closed 2026-09-12. +50 vendored, on the document the raise disarmed, with `CONTRACT_TOKENS` as the previous level rather than `git show HEAD~1`, because a pin survives a dirty tree and a shallow checkout. It is keyless and offline, which is panel 123 R1's constraint: a key-gated check is structurally red on every fork PR forever. **It fired on this milestone's own commit**, refusing the verdict when the paragraph documenting it came to +115; the text was cut to +65 and the pin refreshed with the reason in the body. **What the item could not know**: the historian proved what a delta gate does NOT buy — Chromium runs one at 16 KiB per CL with a mandatory footer and grows ~100 KB a week — so it bounds attribution and not level, and the code says so rather than hoping otherwise.
- [x] Covered | M-declared-thresholds | **The floors are checked from both sides**, closed 2026-09-12, and the author's instruction decided the shape: the floors are the contract ceiling's case, housekeeping rather than a forcing function, so a verifier changed without a panel. `tests/harness/floors.hero` adds the other direction of the comparison the suites were already making, and eight floors were re-based to today's count on its first run — `annotations` 207->319, `canonical` 340->524, `emission` 142->225, `golden/check` 65->112, `order` 10->22 and 14->23, `layout` 140->204, `lines` 87->111. **The open design question answered itself**: the item asked whether the register should be a table, discovery, or a declaration beside each floor, and the answer is none of the three — every suite already counts its own number, so no register exists and none can go stale. **What the item could not know**: `suite_annotations`'s floor, written down as NOT MEASURED because no grep replicates its criterion, was measured from the inside on the first run by the method its own comment describes.
