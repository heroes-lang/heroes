- [x] From the author, 2026-08-25 | **DECIDED 2026-08-25 — its own milestone, `M-package-layout`, and it sits BEFORE M-package-manager** (author decision, `/decide` answer `2a`). The expectation that provoked the item is now a row rather than a misunderstanding: `docs/ROADMAP.md` order **27**, § The names, and a § What is next entry that names the deliverable — `use` paths, the qualifier, and where a program's files live. **The reasoning is CLAUDE.md §14's, applied twice**: M-separate-compilation delivers the build architecture and M-package-manager delivers `heroes add`, so directories are a *third* deliverable and take a name of their own rather than being annexed by an area that already has one. Its position is not cosmetic — `heroes add` cannot place files it has no spelling for. **The 2026-08-12 deferral is superseded, and the trade it rested on is recorded rather than dropped**: that deferral's argument was that packages supply the case which decides the qualifier, so ruling first means ruling without a distributed package in hand, and the ROADMAP entry says in so many words that the honest outcome in that event is a conservative default with a return condition, never an invented case. The `SCHEDULED.md` item **stays there** with its home rewritten (it gained a more specific milestone rather than losing one), and the panel is convened **when the milestone opens**, not now: it is surface syntax, CLAUDE.md §4. **And the item's brief cited the wrong sitting**: it named `docs/panel/031 § H11` and not `docs/panel/032`, which is the RATIFIED sitting (2026-08-12) on this exact subject — five seats, three vetoes, `S` adopted. The flat layout is a ruling, not a silence, so this milestone REOPENS it and inherits what 032 fixed: R4 already decides the landing form at **+26 measured tokens** (`use syntax/decl` binds `decl`, last parts unique), which answers the qualifier's headline and leaves root-relative against file-relative as the sitting's real subject; R5 (the C component is the whole path) and R6 (`use` is scoped to the program) hold either way; and the quoted Nim form the `SCHEDULED.md` item called an additive escape valve was **struck** by R2. Two of 032's numbers moved, measured today: **zero** of `selfhost/`'s 158 module names collide, against the rename argument that carried its veto, and the engineer's *≤ 45 files at M-selfhost-port close* was **143** at the tag and is **158** now. Neither decides the question; both change which side owes the argument. Measured today: **158 flat `.hero` modules in `selfhost/` alone**, and nesting refused by name at `selfhost/parse_use.hero:47` (`error[module_path_has_no_parts]`). Was | **A dedicated milestone for folders, namespaces and package layout?** The author expected M-separate-compilation to deliver organising `.hero` files into directories ("una sorta di package") and it does not — it delivers the build architecture (one .c per module, the per-module cache). The directories question already lives in `SCHEDULED.md`'s 2026-08-11 item (subdirectories and their spelling, deferred to M-package-manager, ratified 2026-08-12; the qualifier question is the unresolved half). The decision to take: does that work stay inside M-package-manager, or is it its own milestone (`M-<name>` per CLAUDE.md §14) covering `use` paths, the qualifier, and package layout — convened as a panel when it opens, since it is surface syntax? Recorded verbatim so the author's expectation is on the record | docs/debrief/SCHEDULED.md (the subdirectories item) · docs/panel/031 § H11 | a milestone the author expects and the table does not carry is a misunderstanding that compounds at every close
