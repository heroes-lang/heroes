- [x] **DUE NOW, moved to `DECIDE.md` 2026-08-18.** The item's own words were "nothing has been taken and nothing will be until **M-selfhost-fixpoint**", and that milestone closed today: the fixpoint is verified three ways and the seed is a tested artifact, which is design.md's v1 criterion met. It leaves this list under `SCHEDULED.md`'s own rule — the milestone it named has arrived, so it is no longer work with a home but a decision nobody has made, and the one thing missing is the one thing the assistant must not supply (`harness/tasks/README.md`: the held-out tasks must be author-written or they measure the assistant's priors). Was | (author decision 2026-08-12, taken in `/decide` over both alternatives: neither two pre-registered author-written tasks per milestone nor a local open-weights model). Nothing has been taken and nothing will be until **M-selfhost-fixpoint**. Two consequences are the decision's real content and are written here so nobody reads the silence as an oversight: the **pre-amendment numbers are gone for good** — every spec amendment from panel 013 on is now permanently unmeasurable against the version before it — and **panel 018's open colon question loses its designated tiebreaker**, so bare block headers (option F, the conservative default) stand indefinitely and become the language's answer by default rather than by measurement. If the tiebreaker is wanted after all, it is a new panel, not this item | docs/panel/011-measurement-without-an-api-key.md · docs/panel/018 § E vs F | metric 3 is primary (panel 011) and it runs on every commit; metric 2 was always the expensive one, and the honest reading is that a measurement nobody schedules is not a plan
