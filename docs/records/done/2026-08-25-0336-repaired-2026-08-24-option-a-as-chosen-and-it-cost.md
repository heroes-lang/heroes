- [x] The self-compilation cost, measured | **REPAIRED 2026-08-24, option (a) as chosen — and it cost no `#line` churn, which is why (b) was refused.** `i8[4]` against `struct dirent`'s scalar `unsigned char d_type` was `internal error: compiling the generated C failed`, **exit 2**, the compiler taking the blame for a declaration the author wrote (§7). It is now `error[ffi_field_type]` at **exit 1** with the caret on the field. **The cause, precisely**: the old assertion subscripted the member, and C type-checks EVERY operand of `&&` whether or not an earlier one is false — so three ill-formed `place[0]` subscripts broke the build instead of failing the assertion. The same fact moved the sign test off a cast at panel 085 R7; this is it one position over. **The repair asks the member's ADDRESS**: `&place` is `T (*)[N]` for an array and `T *` for a scalar, both well-formed, and each association's expression is a constant over a C **type name** rather than over the member — so a scalar lands on `default: 0` and fails cleanly. **Width and sign, never identity** (panel 064): all twelve C integer types are enumerated, and the length rides inside every association as `(*)[N]`, so the separate length conjunct is gone. **Thirteen shapes attacked and all thirteen correct**: `char`, `signed char`, `unsigned char`, `short`, `int`, `long` and a `float` array all ACCEPTED — `long` as `i64` is panel 064's own point, since identity would refuse a correct binding — and a scalar, a wrong length, a wrong sign, a wrong width, a float array declared integer and a pointer array declared integer all REFUSED at exit 1. The twelve types now live once, in `emit_assert_spelling.C_INTEGER_TYPES`, read by `HERO_C_INTEGER` too: a set written twice drifts, and it drifts in the silent direction — a real C type nobody enumerated reads as *the header disagrees* when it agrees perfectly. Was | panel 085 R7, found 2026-08-18 | **The mirror of the field-assertion hole is still exit 2, and its repair needs a decision.** A field declared as a **scalar** where the header has an array is now exit 1 (`ffi_field_type`, fixed this milestone). The **mirror** — a fixed array declared where the header has a scalar, `d_type: i8[4]` against `unsigned char` — is still `internal error`, because that branch subscripts the member (`place[0]`) and a scalar cannot be subscripted. Measured today on `dirent.h`: `i8[4]` on `d_type` and on `d_ino` both. Three candidate repairs, and the choice is a §7 question because two of them change every generated file: **(a)** enumerate C's twelve integer types as array-pointer `_Generic` associations, each row asking `sizeof`/sign of the *C type* (well-formed everywhere, target-independent, ~12 rows per integer-array field); **(b)** define the sign macro in the probe preamble unconditionally, which panel 063 measured as renumbering **56 `#line` restores across five `tests/golden/emit/` files** where `UPDATE_GOLDEN` is forbidden; **(c)** leave it, and add the shape to §7's list of what stays the compiler's. Neither `__builtin_choose_expr` nor `_Generic` protects an unselected operand from type-checking — both measured on this clang today, so the obvious route is closed | crates/heroes/src/emit/extern_field.rs (the `Ty::Fixed` integer-element branch) · tests/golden/fixedbugs/ffi-a-field-the-header-has-as-an-array.hero | the author's own `extern` is the one place §7 says the compiler must not blame itself
