- [x] Author decision 2026-09-17 | **Part 7 items 2 and 3 get their entry note, and no sitting is convened for it**: the site refresh put the deferral list in front of a reader and found File I/O and Command-line arguments reading as deferred while `read_file`, `write_file`, `args`, `args_checked` and `exit` stand in spec § 11. The answer was in the record and not missing: panel 036, author decision 2026-08-12, ruled all of them **named by the spec and delivered as Tier 2 over `hero_os.h`, not as built-ins and not through the FFI** — three judges against one, the compiler-engineer's §1.7 veto on `read_file -> str?` needing an emitter arm for a per-translation-unit struct, and the reader's vote deciding the spec half because file I/O through the FFI as specified is impossible rather than awkward. They landed at M-ffi-ladder step 2 (`6a58d47c`) and `args_checked` on 2026-08-24 (`ed249fe0`). So the two items gain a dated ENTERED note naming the sitting, the route and the commits, written underneath rather than over, and **no panel**: the recommendation put to the author was that CLAUDE.md § 4 convenes a sitting to CHANGE the language, and nothing here changes — a decision already taken and already ratified is being written into the document that failed to record it. The conservative route, a soundness-lane sitting to bless the note, was named and declined as a sitting spent on a status line that decides nothing | `docs/design/design.md` Part 7 · `docs/work/DECIDE.md` · `docs/ROADMAP.md` | the deferral ledger of 2026-09-13 gave nine items a verdict and left these two alone, because they carried no reason to rot: they carried no status line at all, and nothing reads a silence
