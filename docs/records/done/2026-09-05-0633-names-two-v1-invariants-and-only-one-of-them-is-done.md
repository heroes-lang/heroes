- [x] **M-isolated-threads** (OPEN) — the refcount half of design.md's own v1 invariant, which nothing has discharged | `docs/panel/111`, 2026-09-05; the finding is the historian's and both compiling seats measured it | **`design.md:2578-2586` names two v1 invariants and only one of them is done.** The allocator's single point landed 2026-09-05 (step 0, `23de5b5b`) with `tests/harness/suite_runtime.hero` holding it. The other — *"keep refcount operations behind a narrow, never-inlined boundary … so no inlining can smear refcount arithmetic across code that a thread-local counter would later have to change"* — is **half done**: the boundary is narrow and never inlined (measured: `inline` occurs 0 times in `runtime/heroes_runtime.h`, 6 `;`-terminated prototypes), and the counter behind it is still a plain `int64_t` in three places. `_Atomic` appears **nowhere** in `runtime/`. Measured at the sitting: a shared `str` across 32 threads gives `heap-use-after-free` or double-free in **9 of 10** ASan runs, and the refcount drifts to **6290-9785 instead of 1** without a sanitizer. **The price is measured and small**: `__atomic_fetch_add` relaxed plus `fetch_sub` acq-rel over 20M pairs is **0.88 s → 0.89 s, +1.7%**, and on the allocator's own counter the sign of the difference flips between runs. **What atomics do NOT fix, also measured**: `cow.c`'s `if (a->refcount == 1)` is a test-and-mutate, so copy-on-write stays broken; `hero_eq_queue` and 15 other process-wide buffers stay shared; and the stack guard stays main-thread-only (that half is M-thread-stacks') | design.md:2578-2586 · runtime/heroes_runtime.h:73, :295, :432 · runtime/parts/cow.c · docs/panel/111 | the document has said since day zero that this must be one edit, and step 0 proved the argument by doing the other half in six hours || **CLOSED at M-isolated-threads step 3, 2026-09-06.** The counter behind the boundary is now `HeroRefcount`, one `typedef _Atomic int64_t` carrying all three headers, so an ordinary `+= 1` on it is correct rather than wrong; `str.c`, `array.c` and `map.c` ask for the two weaker orders explicitly, `atomic_fetch_add_explicit` relaxed up and `atomic_fetch_sub_explicit` acquire-release down, and nowhere else does. `alloc.c`'s two leak counters became `_Atomic` and stayed **process-wide**, which is panel 111 **R5** and the opposite of the `_Thread_local` pair step 2 priced and refused. **The price, measured on this Mac with step 2's own two-arm protocol** — two copies of the tree differing only in these eight files, BOTH caches cleared first, five alternating runs of `heroes test selfhost/main.hero` each: control **36.76 / 35.57 / 35.08 / 35.47 / 35.70**, atomic **37.19 / 36.17 / 36.01 / 36.74 / 36.01**, so **+0.71 s and +2.0%** (+2.2% with each arm's cold first run dropped). It is signal and not noise: the control's SLOWEST warm run (35.70) is faster than the atomic's FASTEST (36.01), so the two arms do not overlap. It is the sitting's own **+1.7%** reproduced on a completely different workload — 583 compiler tests against 20M incref/decref pairs — and it is the SECOND measurement: the first one described code that did not ship, because the three `= 1` stores were relaxed afterwards. **Relaxing them recovered nothing measurable**, which is a result and is recorded as one; they stay because they say the true thing to a reader, not because they are faster. **The version skew the ABI stamp exists for was run rather than argued**: `seed/heroes.c` is generated C from BEFORE this change, it compiles clean against the new header and its 583 tests pass, so `HERO_RUNTIME_ABI` stays at **21** and the seed is not regenerated. Green on all three platforms the same day: this Mac, the Windows box (`clang` targeting `x86_64-pc-windows-msvc` takes `_Atomic`, `<stdatomic.h>` and `__atomic_always_lock_free`; smoke exit 0 with and without `--sanitize`, harness 112/112 in 14.7 s), and the Debian container. **What is NOT closed, and it is named at the line that holds it**: `runtime/parts/cow.c`'s `if (refcount == 1)` is a test and then a mutate, which an atomic makes whole without making single
