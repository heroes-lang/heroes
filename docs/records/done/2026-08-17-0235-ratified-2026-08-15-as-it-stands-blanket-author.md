- [x] panel 058 | **RATIFIED 2026-08-15 as it stands** (blanket author instruction, *"rattifica tutto"*). (i) adopted with **all five conditions binding**; (ii) and (iii) stay refused on their two vetoes each; (iv) refused; the spec takes the **+2** rewrite. **This ratification converts a resolution into a work order, which is what makes it different from the day's other three** — 057 ratified a refusal and nothing was owed afterwards, while here the yes authorises five conditions and, until they are met, **the silent corruption is reachable in a ratified language**. That is stated rather than left for the tick to imply. **Condition 1 must land first and separately** — the assertion zero becomes the bare `0` — because it is a repair on its own merits today and because the obvious alternative was built and reproduced a regression on a correct `const char **` binding. **The prediction to watch** is the ffi-pragmatist's: checkable at any time, offered by its own author as a withdrawal condition — all 28 of ncurses' plain-`char *` entry points run under the flag with zero shims, and one failure means the refusal is wrong. **On the historian's dissent**: ratifying the refusal of (iii) does **not** ratify the claim that a separate mutable-buffer type is wrong — §13 forbids it *now*, and the recorded disagreement is about the clock. Was | panel 058, 2026-08-15 | **Ratify the sitting, or overturn it — and note that nothing has landed yet, deliberately.** Full lane, five judges. **(i)** the flag plus a fifth exit-1 class is **adopted under five conditions**, and three seats set them independently with one stating that without them its verdict is `veto`; the warden's is the same rule from the spec's side — *landing the flag without the diagnostic class trades a silent corruption for an unactionable exit 2, a net §1.2 loss*. **(ii)** unshare-or-copy: **two vetoes**, on the measurement that the ownership pass's `$own` slot already holds a second reference, so the copy is unconditional (§4.10's own refutation of in-place `push`) and does not fix the unique case. **(iii)** a mutable-buffer spelling: **two vetoes**, Part 7 item 10 verbatim under §13, 400–600 lines — with the historian's dissent recorded, because four ecosystems converged on exactly that answer and OCaml's took three years, so the disagreement is about the **clock**. **(iv)** refused by every seat. **The spec takes a +2 rewrite** — *"`s.cstr()` **lends** a `str` to C to read"* — and not the +32/+55 clause, whose *"may read and never write"* **licenses** the pointer C keeps. **What is yours**: the verdict, and then whether the five conditions are worked in one step or spread. Condition 1 (the assertion zero becomes the bare `0`) is a fix on its own merits **today**, with or without the flag, because the emitter currently warns about its own generated `_Static_assert` | docs/panel/058 · crates/heroes/src/emit/extern_assert.rs · crates/heroes-cli/src/commands/flags.rs | the compiler behaves as it did before until you say otherwise, which means the silent corruption is still reachable — that is the cost of the conservative default here, and it is stated rather than hidden
