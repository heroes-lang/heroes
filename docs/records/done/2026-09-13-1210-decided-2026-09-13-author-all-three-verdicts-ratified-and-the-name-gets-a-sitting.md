- [x] Covered | panel 145 | **DECIDED 2026-09-13 (author: all three verdicts ratified as adopted; the type's name gets a sitting of its own rather than an answer).** M-handle-verdict's sitting on whether one C handle can be told from another. **029 ENTERS**: a fieldless `extern` record is a handle, a pointer to a type the header names and leaves opaque, spelled as the header's own type name starred — compiled three ways before adoption, and the decisive one is that with a nominal parameter type the checker **already** refuses the swap with `type_mismatch: expected Stmt, found Db`, no new rule, while clang refuses it too under a flag the project already carries. **030 REFUSED as a form**, its home the Part 6 borrow-checker row, **zero words added** to the specification. **031 a WART with the defect held OPEN** until a consume-mark prototype is measured — the sitting declined the cheap resolution (close 031 tonight on `--sanitize`) because §1.12 is a goal of the language and CLAUDE.md § Precedence ranks robustness above Principle 0, and the author took that rather than the cheaper one. **On the name**: panel 139 reserved the type's name for this sitting and no seat's brief carried it; the sitting said so instead of deciding by omission. The author declined both offered answers — keep `ptr`, or rename now — and **ordered a sitting of its own after step 4**, so it argues over the bare `ptr` that actually remain once the handle form has landed. Filed as the milestone's fourth item, and it may not sit before the form. **What the yes settles**: the form's shape and spelling, the refusal's home, the wart's text, and the order of the steps — the operator before the form, the form before the prototype. **What it does not settle**: the form's measured cost (step 4's diff), whether the consume mark ever enters (its prototype decides), and the name. **Ratified with three corrections against the coordinator already on the record**: the census handed to the seats was wrong four ways, all understating exposure; the engineer's before-number prediction was falsified by step 3's measurement the same evening; and the form's spelling was corrected by the completeness critic from `struct <tag> *`, which reaches one of three handle types in the tree, to the header's own name | `docs/panel/145-a-handle-is-a-pointer-with-a-name-and-the-compiler-already-reads-the-name.md` · design.md Part 6 borrow-checker row, Part 8 wart 20 · `spec/heroes-spec.md` § 13 · `docs/work/DEFECTS.md` 029, 031 | 145
