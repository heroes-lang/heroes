- [x] **reasoning 006 — what the chain lacked** | 2026-09-03, late the same evening, the session that asked whether the chain's order holds and what is missing from it | **Settled**: the order of rows 33 to 51 with a reason for every position; **all seven new milestones accepted**, one against the recommendation; the packages before the manager; the four rulings on the language moved ahead of the code they shape. **Three faults in the order, each measured and each moving a row** — the language's only scheduled ruling sat at row 41 behind the packages, the framework, QBE and both tools; closures had no row at all; the manager stood before the packages it would distribute. **Sixteen candidates were met with the rule that already ruled on each**: ten got a row or a house, five got a recorded refusal in `DESIGN-LOG.md:539` (prebuilt binaries, a REPL, a web playground, cross-compilation, typed debugging), and **one had neither** — out-of-memory, whose finding was *nothing owed*, because `runtime/parts/alloc.c:77` makes a failed `malloc` a clean `hero_panic("out of memory")`. That finding is recorded here so the candidate is not proposed again as new, which is the note's own stated reason for the table. **All six hand-offs landed.** **The number this entry exists to carry**: the first fault was a measurement, not a feeling — the one body of Heroes written against the spec today is **51,788 lines in 178 modules** (`find selfhost -name '*.hero'`, 2026-09-03), and `docs/ROADMAP.md` said *"tens of thousands"* while citing the note for the figure. Both places now carry the number. **And what *"a strong runtime"* resolved to**, in four: the threads (M-isolated-threads), the services (M-core-packages), leaks (`hero_runtime_check_leaks()`, already on every platform) and out-of-memory (a clean stop) — three of the four already had homes, and the fourth needed nothing | `git show 365fe929:docs/reasoning/006-what-the-chain-lacked.md` · DESIGN-LOG.md:539 · docs/ROADMAP.md § Who scheduled what · docs/work/SCHEDULED.md | the session that reordered the chain, cited as provenance by twelve lines of two living documents and by one comment in the net
