- [x] the last three of raylib, measured 2026-08-15 | **STRUCK 2026-08-16 — the field binds, and the item measured a mechanism panel 064 had replaced.** `kind: u32` over a C `enum` runs at exit 0, verified by the ffi-pragmatist on real SDL3 and reproduced by the coordinator on a header of its own. **97 enum fields, 95 bindable, 0 unbindable** (2 unreachable inside anonymous unions). `kind: i32` fails because SDL3's enums are **unsigned**, and refusing `i32` for an unsigned 32-bit member is the check doing its job. Panel 071 **vetoed** widening the field rule: it would loosen a sign check correct on 95 of 95 in exchange for nothing. **A false premise died with the item**: `extern_record.rs` listed *"an enum (3)"* among what the branch refuses, and clang promotes `__builtin_classify_type`'s operand so an enum answers **1** — deleted, with the measurement in its place. What survives is a **diagnostic** gap, filed separately: `ffi_field_type` says *"is not `i32`"* and never says `u32`. Was | measured 2026-08-15, same run | **An SDL3 `enum` field is unbindable, and no measurement taken on raylib could have found it.** `_Generic(&((SDL_Surface *)0)->format, int32_t *: 1, default: 0)` **fails** — a C enumerated type is a distinct type from its compatible integer type, and the address-of form sees the difference. 35 enum fields across the 57 structs SDL3's entry points name; **9 structs blocked by enums alone** and 7 more by enums together with typed pointers. raylib spells every enum-valued member `int`, which is why three sittings and four measurements missed it: gate row `2675`'s finding again, wearing a corpus with no arm for the question. Decide whether a `ptr`-style widening applies — accept any enumerated type for an integer field of the same size — or whether an enum field wants its own spelling, which is the same question the array member asks one row up | /opt/homebrew/include/SDL3/SDL.h · crates/heroes/src/emit/extern_record.rs | the corpus binds raylib and SDL3, and only one of them could have shown this
