- [x] Covered | M-deferral-ledger | **Every Part 7 item with no milestone carries a dated verdict**, closed 2026-09-13 by the milestone that filed it, at **nine of nine** — seven Part 7 items and the two watch-list entries (Part 8 warts 15 and 16) that had no home at all. Sittings 136 to 143, one per item, each with the full five seats and, from 137 on, a **completeness critic** that changed the resolution every time it ran. **Five left the list for Part 6, each with a falsifier a reader can check**: traits (panel 137 — the hole is two operations wide and the corpus wants one of them; eight cases compiled that the row said could not, including a cross-module generic `sort_by`), a variant's case as a value (138 — its justification named a parser this repository has never had, and the unification it promised would not compile even with the feature granted, because a function type cannot carry `@`), a `raw` module (140 — this document had already ruled against its premise, in this document, and the reader's seat found the sharper objection: `raw.load_u8` would *remove* a guarantee rather than fence a danger), `private` (141 — its hinge expired fourteen days after it was written, its cited ancestry is for a different form, and the direction it left to a count was left to a count that cannot decide: 49.5%, 50%, 50%), and symmetric variant syntax (142 — there is no asymmetry to restore, because nothing in this language destructures, so the proposal creates the thing it removes). **Two were deferred again with dated return conditions written as falsifiable claims**: `alias` (135) and doctests (136, its expired clock struck). **Part 6's borrow-checker row was corrected** (139) — its stated reason was false as written, and it now stands on cost and on §1.11 instead, with defects 030 and 031 as the facts that moved it. **Part 9 was corrected beneath itself** rather than rewritten (140). **Warts 15 and 16 stay, and both entries were corrected for understating what they cost** (143): the carriage return wart 15 injects erases C's own error message from the terminal, and wart 16's workaround silently reorders output using built-ins alone. **The shape the item found, nine times**: an item's *stated reason* is measured false or expired before its verdict can be written | `docs/work/milestones/M-deferral-ledger.md`; panels 135 to 143; design.md Parts 6, 7, 8, 9 | 135, 136, 137, 138, 139, 140, 141, 142, 143

- [x] Covered | M-deferral-ledger | **The ledger's widening to Part 8's homeless warts and to coverage**, closed 2026-09-13 by the milestone that filed it, at panel 144 — its last sitting. **Wart 8 stays, rewritten, and what it names changes**: not *the array you build is quadratic*, which is false for two spellings of three, but **a push reached through a path copies the array, and the same push written through `@` on the array itself grows in place**. Medians of three runs: a plain name at 1 000 000 pushes **0.02 s**, a record field in place at 50 000 **11.51 s**, the field lent to an `@` parameter at 1 000 000 **0.02 s**, hoisted into a local **0.00 s** at 200 000. The mechanical witness is `--emit-c | grep -c hero_array_push_owned` reading **1**, **0**, **1**. The repair landed in **`aacd2dbd`, 2026-08-24** and §4.10 has carried it since, so **design.md contradicted itself for twenty days**; and the cheap spelling is the one this repository applied to its own arena on 2026-08-26, `push_expr(@exprs: [Expr], node)`, taking `heroes check` from **191 s to 88 s**. **The `Builder` promise is struck** on the compiler-engineer's veto: it is core by §1.7's own test, 500-800 lines across six modules, to replace the 267-line `Writer` record of ordinary Heroes the compiler already uses. **Coverage is refused as a Part 6 row, the first whose subject is a tool**, with the column head widened to *Feature or tool* in the same edit because all **31** existing subjects are language or source forms; the reason taken is the strongest available and one no other language can make — on a §1.11 program a coverage percentage **measures the glue and reports it as the program**, measured with real `llvm-cov` over a real SQLite binding where `sqlite3` appears **0 times** and all eight FFI probe functions read **0.00%**. **Wart 5 stays with its number refreshed** (0 of 25 → **59 mutants, 0 killed, 0% over 120 programs**), **wart 11 stays**, and **warts 11 and 15's disagreeing counts are corrected** — neither method reproduces the 22 one of them claimed. **What this item could not do**: § 10's cost condition is *true and insufficient*, its replacement is drafted and measured at **+35 vendored**, and it is blocked behind the one network call the spec's content-addressed digest needs — the same key as defects 030 and 031 | `docs/work/milestones/M-deferral-ledger.md`; `docs/panel/144-the-cost-is-in-the-spelling-and-nobody-had-written-the-cheap-one.md`; design.md Part 6, Part 8 warts 5, 8, 11, 15, §4.10; `spec/heroes-spec.md` § 10 | 144 (034, 037, 088 inherited)
