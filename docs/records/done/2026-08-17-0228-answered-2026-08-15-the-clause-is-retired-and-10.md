- [x] panel 056 | **ANSWERED 2026-08-15 (`b`): the clause is retired and §10 carries the weight, with the correction kept.** It was a **duplicate of §10** (*never a second binary*) stated with a false premise, and the duplication is precisely what let the false example survive — nobody rereads a rule that appears confirmed elsewhere. §6 now ends at `style-insensitive identifiers` and says why the clause went, naming the sourced fact (nimble ships *with* Nim, so the contrast was never real) and the second failure the historian found: it is the rule a reader reaches for first to refuse a project file, and it does not reach one — **a file is not a binary**. §10 gains the corrected sentence with `heroes add`/`heroes fetch` named as subcommands (design.md:637). The record is corrected, not deleted (§14). Was | panel 056's historian, sourced 2026-08-15 | **CLAUDE.md §6's *"a separate package binary: never"* is a bare rule, and it describes nimble incorrectly.** No argument, no panel, one citation (`docs/ROADMAP.md`:171). Sourced: **nimble is bundled with Nim**, so the thing §6 names by way of contrast is not separate. Nothing in the repository mentions `nim.cfg`, `config.nims` or `nimble` anywhere else — which matters because §6 is the rule a reader would reach for first to refuse a project file, and it does not reach one: a file is not a binary. **What is yours**: give the clause a reason, or retire it and let §3.5 and §10 carry the weight | CLAUDE.md §6 · docs/ROADMAP.md:171 · docs/panel/056 § Precedent | a bare rule that is also inaccurate is the kind of thing that gets cited once and then load-bears
