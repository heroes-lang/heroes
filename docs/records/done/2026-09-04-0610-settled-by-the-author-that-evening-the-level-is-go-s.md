- [x] **reasoning 005 — packages in place of a standard library** | 2026-09-03, the session that asked for a mini standard library and then for *"every tool needed to build a web framework"* | **Settled by the author that evening**: the level is Go's and Echo's, everything explicit, because Rails, Django and FastAPI rest on what Part 6 refuses permanently; the shape is **many small packages organised as Go's tree**, composed later by a framework that is a milestone of its own; the names are `M-core-packages` — after Odin's `core:` collection, which §1.11 cites — and `M-web-framework`. **All seven hand-offs landed** in `SCHEDULED.md` and the ROADMAP: the opening sitting's six questions (i) to (vi), the callback boundary and the trampoline at M-isolated-threads, three compiler defects as step 0, the copied helpers as the first package, three design.md repairs riding the opening sitting, and the framework's brief deferred to M-core-packages' close. **One of its own settled points was superseded the same evening** by the reorder: the packages now come **before** the manager, rows 43 and 44. **What only the note held and is carried forward here**: that **FastAPI's decorators and type-driven validation need reflection Heroes has not got** — one of the three witnesses `M-reflection-verdict`'s warrant rests on, and cited by line from two living documents until 2026-09-04, which is why the sentence is in this entry rather than only in git; that `M-core-packages` is named after Odin's `core:`; two Windows facts to measure rather than predict (`tv_nsec: i64` against a 32-bit `long`, `fdopen` against `_fdopen`, and no OpenSSL `.pc` on the box); and the falsifiable form of the framework question, **whether a framework without closures reaches Echo's level or collapses into `net/http` itself**, now written into the ROADMAP's M-web-framework entry | `git show 1b5a8209:docs/reasoning/005-packages-in-place-of-a-standard-library.md` · DESIGN-LOG.md:537 · docs/ROADMAP.md § M-core-packages, § M-web-framework · docs/work/SCHEDULED.md | the session that turned a want into two milestones, and whose one uncarried sentence was holding up a milestone's warrant
