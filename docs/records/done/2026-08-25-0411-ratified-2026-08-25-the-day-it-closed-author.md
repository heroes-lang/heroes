- [x] panel 092 | **RATIFIED 2026-08-25, the day it closed** (author instruction, *"ok ratifichiamo e chiudiamo tutto"*). Three production lines, +12 spec tokens, and nothing new built — the check, the diagnostic and its fix all existed and clang could not reach them. `examples/curl`'s live defect is repaired in the same commit. What the yes does **not** settle is written into `docs/panel/092` § Author's verdict: the float half waits on `-Wdouble-promotion` under a pragma (approved today, three measurements outstanding), and the sentence naming what is unchecked is bought **once, at its final size**, rather than twice. Was | panel 092 | **Ratify P3 + S2's one row: the probe's callee is parenthesised, the discarded-result cast becomes `(void)`, and the spec gains `u64` where C says `size_t` (+12, measured, 80-column wrapping).** The sitting was convened on my claim that a parameter's width and sign are unchecked. **That was wrong, and so was my second diagnosis.** The check has been there all along — `-Werror=shorten-64-to-32` and `-Werror=sign-conversion`, shipped in `cli_flags.hero` — and the silent set was never "clang builtins": `strncmp`, `malloc`, `calloc`, `abs` and `fwrite` are builtins and are all caught. What hid the rest is **`_FORTIFY_SOURCE` macro rewriting** (`memset` → `__builtin___memset_chk`, visible in `clang -E`), which `-fno-builtin` cannot touch because a macro does the redirect. **Two production lines fix it** — `(void)(f)(…)` is not a function-like macro invocation — and the repair immediately found a **live defect in this repository**: `examples/curl/main.hero:44` declared `option: i32` where `CURLoption` is unsigned, invisible since the example was written. **P1 and P2 are vetoed by both compiling seats**: Heroes has one pointer spelling, so `spec:206`'s own `sqlite3_open` warns on a **correct** binding and P2 would stop the spec's own example from building. The historian's disjunction was **falsified by compiling it** — 16 of 25 correct bindings rejected, including the compiler's own `getenv` | docs/panel/092 · selfhost/emit_extern_probe.hero:82 · selfhost/emit_ops.hero:99 | four of five seats overturned a premise of my brief, and the answer got smaller and truer each time
