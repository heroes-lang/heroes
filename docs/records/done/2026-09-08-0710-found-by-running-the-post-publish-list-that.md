- [x] **A wrong address gets a page and a 404, where the site answered 200 with its own home** | author instruction 2026-09-08 (*"fix all of it"*), closed the same day | Found by running the post-publish list that `site/README.md` § Deployment prescribes, whose own line says a missing page answers 404: it did not, and `/pagina-che-non-esiste/` returned **200 carrying the home page**. Measured on three deployments, the live one and the two before it, so the defect is older than today's work, and the cause is Pages' documented behaviour: with no top-level `404.html` in the output it assumes a single-page application and matches every unknown path to `/`. A page per edition now, same layout and fragment shape as every other page, four links out: home, guide, examples, the whole language in one file. **Two guards, because the file is both a handler and an address**: `heroes:nested-404` renames `dist/it/404/index.html` to `dist/it/404.html`, which is the name Pages walks up the tree for, and throws if Astro ever writes it elsewhere; and `noindex, follow` from a new `BaseLayout` prop, with the sitemap filtering both pages out. **The records suite went red and was right**: its comment had left the Italian `.astro` wrappers unexempted on a 2026-09-04 measurement that none of the 23 fired, promising the entry the day one did, and `it/404.astro` is the first of 26 because a not-found page owes a sentence where every other page owes a title. **A correction to this session's own commit body**: `955131c2` says the records suite is 15 of 15 "with the new registry check live", which reads as if the fifteenth check came from the exemption registry. It did not. The fifteenth is `M-discard-refusal` step 4's, from the parallel session that closed that milestone the same afternoon, and its own DESIGN-LOG line states it | `site/src/html/404.html` · `site/src/pages/it/404.astro` · `site/astro.config.mjs` § heroes:nested-404 · `tests/harness/suite_records.hero` § ITALIAN_BY_DESIGN | a soft 404 hands a crawler an unbounded set of invented addresses that all answer 200 with the same page, and this site's whole mechanism is that a reader can check what it says
