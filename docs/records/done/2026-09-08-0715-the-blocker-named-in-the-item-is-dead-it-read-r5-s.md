- [x] **A runtime null rejection is neither a program abort nor a compiler tripwire, and design.md §1.12 had already ruled that the question does not change the answer** | filed 2026-09-08 as a panel 118 finding by the compiler seat; closed the same day by `/decide` as **STALE in both halves**, which is the check that skill exists for | **The blocker named in the item is dead.** It read *"R5's scope depends on the answer"* — R5 excluded aggregate operands from an emitter relaxation on the assumption that their `_eq` null checks were worth keeping. `docs/panel/118:373` says **R5 was REPLACED**: the relaxation was built, measured (zero warnings, four dead assignments gone, byte-identical on a second emission, `warnings` 162/0, `emission` 427/0, `determinism` 130/0, 589 compiler tests) and **thrown away**, because attacked at the shapes beside it it closed only two of four — `_ = a == b` on a `str` and on an array still warned, their assignment being a runtime call rather than an operator. What shipped declares the temporary `__attribute__((unused))`, closes all four, keeps every abort check and every runtime call, and **rests on no premise about aggregates at all**. So nothing downstream now depends on the classification. **And the question itself was answered before it was asked**, at `design.md:563-566`, §1.12's own *Defensive, and the cost is real*: the compiler and the runtime **check rather than assume**, and they check at the boundary *"even where the value is known to be good — a `cstr` from `s.cstr()` can never be null and is null-checked anyway, because the alternative is a rule that has to reason about where a value came from, and provenance is a premise about the world"*. That is the answer to *whose bug does it catch*: the principle **refuses to ask**, because the version of the rule that asks needs provenance, and one predictable branch per boundary is paid deliberately. §1.12 also carries its own falsifier at `:573-575` — a measured case where a defensive check HIDES a defect instead of surfacing it — so the recommendation the item carried, *keep them*, was not a preference to be ratified but a consequence of a principle already in the document, and `selfhost/emit/structural.hero:47-52` gives the same reason in the same words: *"'should never' is exactly the premise that goes in the loud direction"* | `design.md:563-566`, `:573-575` (§1.12) · `design.md:2374` (Part 5) · `docs/panel/118-the-missing-word-was-a-dead-variable.md:373-379` · `selfhost/emit/structural.hero:45-58` | asking the author a question whose premise the record had already overturned is the one cost `/decide` cannot pay, and this item's premise had been overturned twice — once by the sitting that filed it, once by a principle four years of this document older
