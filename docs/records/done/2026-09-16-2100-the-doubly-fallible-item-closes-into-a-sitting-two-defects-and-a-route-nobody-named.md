- [x] **M-check-completeness** | a doubly-fallible value the checker tracks and the syntax cannot write | **CLOSED 2026-09-16** — into a sitting, two defects and a route nobody had named | `docs/panel/158-the-sitting-produced-a-resolution-and-left-it-off-its-own-ballot.md`

    **Origin:** `docs/panel/110`, 2026-09-04, which refused a `T??` spec sentence
    and docketed the question the refusal rested on.

    **The four measurements this item asked for were run first**, and they
    narrowed the sitting before it sat: the representation is **stable** under
    monomorphisation (`--dump-ir` names `i64??` in full), `?` peels the outer
    level in the emitted C too, `hero_runtime_check_leaks()` is **clean** on a
    nested fallible carrying a `str`, and the library list is **one function of
    six** — only `find<A> -> A?` wraps what it bound.

    **Panel 158 then measured the footing properly and it held**: twelve shapes
    across two platforms, handles at two and three levels, real SQLite with a
    `Stmt??`, a deliberate 512-byte leak proving LeakSanitizer awake. §1.12
    **tested and not reached**, said plainly rather than manufactured. And a
    fallible has **no C ABI at any depth**, so every repair costs bindings zero.

    **Two vetoes, on two different repairs.** Flattening, vetoed on locality with
    the specification's own example line as the exhibit. Making `T??` writable,
    vetoed after the seat **built the patch**: `heroes fmt` on `v: i64?? ?` then
    prints `error: this is a compiler bug`, because the renderer emits `i64???`
    and the lexer max-munches the hole token.

    **THE ROUTE NOBODY NAMED, and it dissolves the second veto rather than
    paying it.** That collision is not intrinsic to legalising the type — it is
    intrinsic to rendering `?` next to `?`. `Prefix` has no `"(" Type ")"`
    alternative; adding one makes `(i64?)?` writable at any depth with **no lexer
    change**, and the formatter round-trips. The llm-ergonomist had recorded the
    missing alternative while hunting an escape and did not see its use.

    **What the item produced besides a resolution**: defect **050**, a test of a
    doubly-fallible value that asks the outer layer and reads as asking the inner
    one — `check` 0 and it runs, today; and defect **051**, a comment asserting an
    invariant the compiler falsifies with a design document citing it as live.

    **What is NOT closed, and the tick does not claim it.** The parenthesis route
    is **UNRUN**. Its canonicalisation duty and its tool walk are what its
    implementation owes first, and if `fmt` cannot round-trip `(i64)?` the route
    fails and the ballot returns vetoed. The specification's first sentence — the
    relation between the value § 10 promises and the spelling § 3 permits, stated
    zero times today — is owed with it.
