- [x] **028 — a record field of function type that is never called through aborts the compiler after the checker has passed the program** | **closed 2026-09-12** | **Cause: one arm that returned instead of walking.** `selfhost/emit/synth.hero::collect` returned at once for `.named` — a record or a variant — so it never descended into their fields. A function type reachable ONLY through a field therefore reached no typedef, and `ctype.func_of`'s assertion, whose comment states the premise in its own words (*"every function type is named before anything can mention one"*), found it false and aborted: `heroes check` at exit 0, then `heroes build` at **exit 134** with `assert failed: !found.is_err()` and no file, no line and no diagnostic code. **It survived only while nothing CALLED through the field** — calling it puts the type in a slot, the slot is walked, the typedef is emitted — which is why every program that holds a callback in a record and also calls it was fine, and the one that did not was the record of handlers panel 132's FFI seat wrote on its first attempt. **The repair**: `collect` walks a record's fields and a variant's case fields, guarded by a second `seen` set — a second set and not the `into` one, because `into` IS the list of function typedefs to emit and a named type in it would emit a typedef for a record; and guarded at all because a record may hold itself through `[T]` or `{K: V}` (spec § 3), so the walk has a cycle in it. **Repaired at the class and not at the witness**, with `tests/golden/fixedbugs/function-typed-field-never-called-through.hero` carrying four shapes: the direct field, one nested inside another record, one reached only as the element of an array, and one in a variant's case. **Proved both ways, run 2026-09-12**: the old compiler exits **134** on that file, the new one exits **0** and prints | `selfhost/emit/synth.hero` · `selfhost/emit/ctype.hero` · `tests/golden/fixedbugs/function-typed-field-never-called-through.hero` | the FFI seat called it blocking for any record-of-handlers form, which is the shape panel 132 refused for four other reasons — so the defect outlived the proposal that found it
