- [x] **036 — `owned <freer>` after a non-`cstr` result checked clean and made clang blame the compiler** | a freer named after an integer result silently retyped it as `str?`, emitted `h_library_validated` on the integer, and exited 2 saying the compiler failed | **repaired 2026-09-15**, and it needed no sitting | **The refusal already existed and watched only one of its two positions.** `owned_needs_cstr` has been in `check/freer.hero` since 2026-09-13, asking a marked PARAMETER whether its type is a `cstr`; `mark_refusals` called it for every parameter and never for the result, where it called only `refusal_for_mark`, which asks whether the NAME resolves. So the repair is one call site and a generalised parameter, not a new diagnostic — which is also why **CLAUDE.md § 4 does not reach it**: widening a diagnostic CLASS goes to the panel, and this class was already decided, already written and already shipping at the other position. **Three documents agreed and only the rule disagreed**: `spec § 13` gives the mark after *"a `cstr` result or a `char **` out-parameter"*, and the diagnostic's own note has always read *"it belongs on a `@x: cstr` out-parameter or on a `cstr` result"*. Measured before: `function labs(x: i64) -> i64 owned free` with `free` declared checked at exit **0** and built at exit **2**, `internal error: compiling the generated C failed`, clang reporting an integer passed where a `const char *` belongs. Measured after: exit **1**, ``error[owned_needs_cstr]: `owned` marks a `cstr`, and the result of `labs` is a `i64` ``, with the caret on the mark. **The type is asked before the name, at both positions**, which is the order the parameter branch already shipped: a mark on the wrong type stays wrong after the freer is declared, so sending the author to declare it first would send them to the wrong repair. **Nothing shipped breaks**: every `owned` result in `examples/`, `selfhost/` and `tests/` is on a `cstr`, read rather than assumed. It is a fifth member of `.claude/rules/c-boundary.md`'s four exit-1 classes, and panel 149's ffi-pragmatist filed a sibling independently — a `tag` naming a C type that needs the `struct` keyword also exits 2, which stays open. Found by panel 149's **completeness critic**, outside that sitting's question; the coordinator's first reproduction FAILED and the entry kept that, because the defect needs the freer DECLARED and `unknown_freer` catches it correctly otherwise | `selfhost/check/freer.hero` · `tests/golden/check/fixedbugs-owned-asks-the-result-too.hero` | 149
