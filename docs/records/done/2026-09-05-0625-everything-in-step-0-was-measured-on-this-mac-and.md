- [x] **M-c-callbacks** (OPEN) — the thread guard on the other two platforms, measured before the milestone closes | CLAUDE.md § Commands' three-platform rule, and panel 111 R9 | **Everything in step 0 was measured on this Mac and the guard is a runtime construct.** What is already known and is not an inference: `_Thread_local` is used by `runtime/parts/{array,drop}.c` today and CI compiles those on all three legs, so the construct itself is not new platform surface — `grep` run 2026-09-05. What is NOT known: whether a thread-local `bool` is zero on a thread created by a foreign library on **glibc** and on **Windows** (C11 7.5 says an object with no initialiser starts at zero per thread, and a *standard* saying so is not a machine saying so); and whether `pthread.h`'s `pthread_t` binds at all on the Linux leg, which panel 111's seats correctly put as a **question** rather than a premise because on glibc `pthread_t` is an `unsigned long` and not a pointer, so `@thread: ptr` may not compile there. **Linux is a container**, `docs/environment/linux/LINUX-MACHINE.md`, and an `extern` program's Linux leg runs under `--sanitize` by the same section's rule. **Windows is a paid box only the author can start**, `docs/environment/windows/WINDOWS-MACHINE.md`, and has no `pthread.h` at all — so the Windows question is the guard alone, through a callback C calls on the main thread, which `tests/golden/run/ffi-callback-c-calls-back.hero` already is | runtime/parts/thread.c · docs/environment/linux/LINUX-MACHINE.md · docs/environment/windows/WINDOWS-MACHINE.md · docs/panel/111 R9 | a guard that is silently false on one platform is worse than no guard, because the program that would have stopped runs on

**CLOSED 2026-09-05, and both machines answered the same way.** Windows first, because it is billed by the hour and the author had just started it. **Q1, the one that mattered: the guard FIRES on a foreign thread on both platforms.** A `_Thread_local bool` starts at zero on a thread `CreateThread` made on Windows Server 2025 / clang 22, and on one `pthread_create` made on glibc / clang 22 — `panic: probe.worker ran on a thread this program did not start`, exit 134 on Linux and an abort on Windows, against the home thread passing in silence on both. So the guard is not silently false anywhere, which was the outcome worth fearing: a program that should stop and runs on. **Q2 is answered and the seats were right to make it a question**: on glibc `pthread_t` is an `unsigned long` of 8 bytes, so `@thread: ptr` does NOT compile there — and the refusal is `ffi_parameter_type` at exit 1 on the author's own line, with a `guess` fix *declare it `u64`* and a note that C's `long` is the platform's word rather than a width the header chose. **A thread binds on Linux all the same**: `pthread_create(@thread: u64, …)` builds, runs, spawns, and the guard stops the worker by name from a real Heroes program. So the finding is not *"it does not bind"*, it is **two spellings and no portable one**, which is `M-core-packages`' platform-typedef question (its item (v), `clockid_t`) arriving in a second place. **Also measured on Linux, where LeakSanitizer is the only one that exists**: `tests/golden/run/ffi-callback-c-calls-back.hero` under `--sanitize` is exit 0 with the blessed output and no leak, which is CLAUDE.md § Commands' rule for an `extern` program discharged. And the two `ffi_callback_type` cases fire on both platforms, and the guard counts are **1** in the callback program and **0** in a program without one, identical on all three. **What is corrected about this item's own premise**: it said Linux was scheduled because the container had to be brought up, and Docker Desktop was already running — a failed search written as an impossibility (CLAUDE.md §1), which is why the Linux half was committed before it was measured rather than after
