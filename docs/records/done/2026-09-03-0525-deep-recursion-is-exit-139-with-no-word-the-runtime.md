- [x] **`M-robustness-guards`, step 4 — the soundness lane first (compiler-engineer + ffi-pragmatist), then the runtime** | `/decide` 2026-09-03 (author instruction as above, choosing (a) in its complete form) | **Deep recursion is exit 139 with no word; the runtime gets a guard-page handler that names the function.** Alternate signal stack + `SIGSEGV` handler on POSIX, a vectored exception handler on Windows; the handler checks the fault address against the stack bounds, prints `panic: stack exhausted` on the abort path every other runtime failure uses, exit 134, and symbolises the faulting frame (`backtrace`/`dladdr`; `CaptureStackBackTrace`/`SymFromAddr`) so the message reads `in d.down` without `--sanitize` — the mangler is deterministic, so `h_d_down` reads back. The lane prices the emitted depth counter as the fallback where symbolisation cannot name the frame, and measures the handler on the three platforms before the commit (CLAUDE.md § Commands). Measured 2026-09-03: Mac 50,000 frames exit 0 / 100,000 exit 139; Linux image 100,000 and 500,000 exit 139; `runtime/` has no signal handling at all | runtime/runtime.c (`hero_panic`) · runtime/hero_os.h · site/src/html/why.html:197 (the declared hole, to be un-declared when this lands) · design.md §1.12 | a language whose goal is that programs do not crash, crashing without a word on a five-line program

  **CLOSED 2026-09-03 (M-robustness-guards step 4), the sitting first and then the runtime, measured on the three platforms before the commit.** The sitting is `docs/panel/104-deep-recursion-stops-with-a-word.md` (soundness lane, ratified the same afternoon; (a) adopted, (b) rejected on its measured cost). The runtime part is `runtime/parts/stack.c`, installed from `hero_args_set` so the emitted C and the ABI stamp are untouched: POSIX `sigaltstack` + `SIGSEGV`/`SIGBUS` with two witnesses (the address in the guard region AND `sp` within 4 KiB of the bound), the previous disposition saved and chained, nothing installed under `--sanitize`, a frame walk through `dladdr` that turns `h_<module>_<name>` back into `module.name`; Linux asks for `_GNU_SOURCE` and every POSIX link line carries `-rdynamic` (`cli/flags.hero`, tested); Windows has a vectored exception handler registered last with `SetThreadStackGuarantee`, and names the failure rather than the function (dbghelp and a PDB are not measured on the box). The fixture is `tests/golden/surface-fixtures/deep/main.hero` at TEN MILLION frames, because the box's ratified 64 MB `/STACK` holds a hundred thousand (it printed `100000` at exit 0 there); the surface row asserts the message and not the exit code. Measured: Mac `-O0` and `-O2` `panic: stack exhausted in main.down` exit 134 (exit 139 and silence before), 551 own tests, the net 1368 passed 0 failed, the net's own 92, the run suite's 95 ASan goldens untouched; Linux the same line at `-O0` and `-O2`, 551 own tests, surface 92, run 91; Windows `panic: stack exhausted` at both levels, exit 127 as the box reports it, 551 own tests, surface 85. And one correction to the record's own reasoning: `-O2` does NOT turn this recursion into a loop — a hundred thousand printed at `-O2` on all three platforms and a million panics on the Mac — so what `-O2` buys is a smaller frame, and the depth counter the sitting rejected would have cost every program at every level. Site: `why.html`'s *two holes* is *one hole*, committed and not published.
