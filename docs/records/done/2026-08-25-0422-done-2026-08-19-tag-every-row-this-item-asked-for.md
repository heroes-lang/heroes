- [x] **DONE 2026-08-19, tag `m-bootstrap-archive`.** Every row this item asked for landed, and in the order it asked: the differential's successor is `tests/emission/` (142 programs, and the bytes were blessed while both compilers still ran — the only moment that comparison could ever be recorded); `heroes measure` is ported and agrees to the token (3440 / 3512 / spread 72, identical), with design.md:322 and :349 amended in the same commit as panel 086's ruling on where the ledger lives; the `#~` invariant's runner was already carried over at M-harness-port (`suite_annotations.hero`, verified rather than re-done); CI's four cargo steps are replaced by four that need clang alone; and `doctor` needed nothing — it has never asked about cargo, which this milestone verified rather than assumed. **Two things this item did not anticipate.** `heroes mutate` was missing too, and it matters more than `measure`: it is metric 3, the thesis's primary measurement, run on every commit (panel 011) — 538 mutants, byte-identical report, and three internal invariant suites feed off its corpus. And the harness's own self-tests named `./target/debug/heroes` in nine places, in the net built to survive that path's deletion. Was | panel 085 R4/R5, decided 2026-08-18 | **The archive itself — `M-bootstrap-archive`, and its first row is a repair rather than a move.** `crates/` -> `archive/bootstrap-rs/` cannot be the first commit: **`crates/heroes-cli/tests/differential.rs` dies with it**, and that instrument's expectation *is* the bootstrap, so the day the Rust goes away is the day nothing can ask whether the two compilers agree — three defects this milestone found were invisible to every other instrument. What the milestone owes, in order: the differential replaced by something that survives (a stored emission per program, regenerated deliberately, is the only shape left once the second compiler is gone); `heroes measure` ported and `design.md:322`/`:349` amended in the same commit, because those sentences cite `crates/heroes/src/measure/gate.rs` as live and the spec budget has no other enforcer; the `#~` invariant's runner (**86 files, 184 annotations, one enforcer at `golden.rs:344`**) carried over; CI's **four** cargo steps replaced, since `.github/workflows/ci.yml` becomes empty otherwise; and `heroes doctor`'s equivalent for a machine with no cargo. Only then the move | docs/panel/085 · design.md:82 · .github/workflows/ci.yml | design.md:82's "no third language anywhere" is an implementation claim, and every precedent examined still ships a build system in something else
