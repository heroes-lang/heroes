- [x] **Are the built-in constructors inside the mandatory-label rule, or outside it? The document shows one call that its own rule forbids** | panel 115's ergonomist hit it while writing the sitting's program from the document alone, 2026-09-06; verified by the coordinator with the compiler at `13165dc7` | **The document says both things and a reader cannot tell which.** `spec:106` — *"When two parameters in a signature share a type, named arguments are mandatory at the call site: `copy(from: a, to: b)`"*. `spec:146` presents the failure constructor as `fail(code, msg)`, and **both of its parameters are `str`**: `selfhost/check/walk.hero:1802` takes `str_ty` and `:1805` checks every argument against it, labels never consulted. Measured: `fail("negative", "n was below zero")` compiles and runs at exit 0, while the same shape on a user function is **two** diagnostics — `error[needs_label]: two of `pair`'s parameters are `str`, so every one of them is named at the call site` — each with a `certain` fix. **The honest reading, and it is why this is a question and not a defect**: `fail(code, msg)` at :146 can be read as naming the PARTS rather than showing a CALL, exactly as `ok(v)` does two words earlier. So the document does not contradict itself; it leaves the reader unable to decide, and the reader who applies the rule writes `fail(code: ..., msg: ...)` and never learns it was optional, while the reader who copies the line is fine either way. Blast radius is one compile error in the loud direction. **Recommended: leave the compiler as it is and add nothing to the spec** — the ambiguity costs a loud error and a sentence costs tokens against a headroom of 272, which panel 115 has just spent six of getting back. Worth deciding rather than leaving because §1.4's redundancy standard cuts the other way too: a rule stated once and then silently excepted is the shape that makes a reader distrust the rule | spec/heroes-spec.md:106, :146 · selfhost/check/walk.hero:1802, :1805 · docs/panel/115-the-floor-is-the-thread-that-ran-main.md | the compiler's default today is that the built-ins are exempt and nothing says so, which is a rule with a silent exception | **SETTLED 2026-09-06, and the measurement is what settled it rather than a preference.** The question assumed one of the two spellings was refused. **Both compile**: `fail("negative", "n was below zero")` runs at exit 0, and so does `fail(code: "negative", msg: "n was below zero")` — measured with the compiler at `13165dc7`. So there is no wrong program in either direction: the reader who applies `spec:106`'s rule writes the labels and is right, and the reader who copies `spec:146` omits them and is right. **The built-in constructors are outside the rule and accept it anyway**, which is the strongest of the three possible answers and the only one that costs nothing. **What is NOT done, with the reason**: the compiler is not made to demand labels, because that would break every `fail(...)` in `selfhost/` for no correctness gain; and the spec says nothing, because a sentence costs tokens against a headroom of 272 that panel 115 has just spent six of getting back, to describe a case in which nobody can be wrong. **What IS done, because this project's own rule is that a rule an instrument reads is a rule and a rule in prose is a wish**: a test pins both spellings, so the day one of them stops compiling something goes red instead of a document going stale
