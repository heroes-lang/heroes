- [x] **022 — a lend that escapes its frame is a use-after-free at exit 0** | filed 2026-09-08 from the ffi-pragmatist's finding at panel 121, ruled on by panel 122 on 2026-09-09 after the author convened it in one word, repaired the same day | **The rule is positional and never typed**, which is what two seats' vetoes forced: as a rule about the TYPE `cstr` it refused `c = getenv(name: name.cstr())` — compiled and run by the spec-warden — and `owned`, an `@` cell holding a `cstr`, and `-> cstr owned free`, all of which ship. Worded on the POSITION of the `.cstr()` expression, none of them is visible to it. **Three clauses, in `selfhost/check/lending.hero`**: a lend may stand only as an argument of a call (the extern discriminator was dropped, because two qualified calls that look identical differ in legality by what another file declares, because no language uses which KIND of function is called as a lifetime criterion, and because it refused three SOUND sites in the wrapper §4.19 prescribes); no function outside an `extern` group may answer `cstr`, which closes the two-hop laundering `return pass(c: s.cstr())` that a position rule alone blesses; and no `cstr` record field outside a group, which forced rewriting `selfhost/emit/gate.hero:172`, whose note PRESCRIBED the shape the clause refuses. **Measured**: blast radius 0 files of 638, four `tests/golden/check/fixedbugs-a-lend-*` cases whose annotations equal their diagnostics, four tests in the module, 591 → 595, seed regenerated with the fixpoint verified by `cmp`. The spec says it in a MERGED sentence at +34 real tokens, cheaper than appending because a positional rule implies the duration. **And three sentences of the diagnostic were wrong and are fixed**: it named an EXTERN call from the dropped rule, it said the bytes are released when the function returns — false in general, since the slot goes on re-execution, and false of every site where it then fired — and its note prescribed a program `extern_across_modules` refuses, so the two diagnostics pointed at each other | `docs/panel/122-the-lend-was-two-defects.md` · `selfhost/check/lending.hero` · `selfhost/ffi_errors.hero` · `spec/heroes-spec.md` § FFI · `tests/golden/check/fixedbugs-a-lend-that-leaves-its-frame.hero` | **THE DEFECT AS FILED WAS HALF A CLASS**, and the sitting's R5 split it: what is closed here is the lend that escapes its FRAME, and the lend the C side RETAINS past the call is defect 024, which no position rule can see because its discriminator is an argument of the C function
