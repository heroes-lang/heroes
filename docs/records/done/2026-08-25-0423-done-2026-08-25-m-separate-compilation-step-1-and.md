- [x] **DONE 2026-08-25, M-separate-compilation step 1 — and the item's own premise was an inference that measurement overturned.** The item read the gap as *"the port's walks happen to agree"*; measured, **every one of the four is already sorted**. Three pay `sort(keys(…))` explicitly (`emit_descriptor_set.hero`, `emit_ctype.hero` twice) and what they lacked was the MARK, not the sort — each carried the bootstrap's mark **as a quotation inside a sentence**, which is why a grep could not see it. The fourth, `ir_print.hero`, has no map at all: the port replaced the bootstrap's `BTreeSet<u32>` with a dense `[bool]`, so the walk is ascending by construction and the mark now says so. The fixpoint was green because the sorts were paid, not by luck — the shape CLAUDE.md §1 names, *a sentence whose truth comes from another sentence*. **What the gap really was**: design.md's own inventory command returned **10 hits in 8 files, four of them prose** (three quotations, one mid-sentence mention), so it reported the walks that were fine and said nothing about the ones nobody had judged. Now `# ORDER:` is a mark only as the FIRST word of a comment, the inventory is **12 files / 16 marks** against the bootstrap's 13, and three bare `keys(` walks that no reader had ever ruled on carry `# ORDER: none — <why>` (a worklist whose output is a set, a count, a bool) — a judgement recorded rather than a rule dodged. **The count reports itself at last**: `tests/harness/suite_order.hero` fails the net when a map walk in `selfhost/` carries no mark, searching the mark's own scope rather than a line window (up through the function body, across its header once, into the doc comment). Made to fire before it was believed — a mark deleted and an unsorted `keys(` added, both named with file, line and the offending text. `emit_ctype.hero` and `resolve_state.hero` are at their `suite_layout` ceilings to the line, so their marks landed line-neutral. Was | panel 086 R4, decided 2026-08-19 | **Four `ORDER:` marks in the bootstrap have no counterpart in the port, so the live inventory under-reports.** Measured independently by two seats: `# ORDER:` marks **5 files / 6 occurrences** under `selfhost/` against **10 files / 13** under `archive/bootstrap-rs/`, and the four without a marked counterpart are `ir/print.rs` and `emit/{descriptor_set,typedefs_generated,typedefs}.rs`. design.md:1349's inventory command is repointed at `selfhost/` and says so, but the sentence cannot fix the gap: each of those four walks either reaches the emitted C — where an unsorted map walk breaks a byte-for-byte comparison — or reaches `--dump` text, where it breaks a golden. **The home is the next milestone that touches ordering or emission**, because that is when the walks are open anyway; the check that would make it self-reporting is a count, and the count is here. Not urgent and not invisible: the fixpoint is green today, which means the port's walks happen to agree, and "happen to" is exactly what a mark exists to remove | design.md:1349 · docs/panel/086 R4 · selfhost/{ir_print,emit_descriptor_set,emit_ctype}.hero | panel 006's rule is that every ordering-sensitive walk owes an explicit sort, and an unmarked one is a walk nobody will remember to check
