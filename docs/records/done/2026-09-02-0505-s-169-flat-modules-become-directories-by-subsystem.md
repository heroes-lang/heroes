- [x] **M-selfhost-nesting** (id provisional until the sitting below rules — CLAUDE.md §14 forbids renaming an id once it is in the record, so this one is not tagged or cited anywhere else yet) | author instruction 2026-09-02, *"riscrivere il selfhost con le cartelle al posto dei prefissi, esempio: check/access o cli/argv"* | **`selfhost/`'s 169 flat modules become directories by subsystem — and it is BLOCKED on a language question the author has already opened.** Measured 2026-09-02: the prefix families are `emit` 49, `cli` 22, `check` 19, `ir` 13, `resolve` 10, `mutate` 5, `print` 5, `parse` 4, so **127 of 169 files nest** — and **32 of those fall into 13 last-part collision groups** that `module_last_parts_collide` refuses: `state` ×4 (`check_state`, `ir_state`, `resolve_state`, `state`), `decls` ×3, `ops` ×3, `types` ×3, `builtins` ×3, then `walk`, `access`, `ffi`, `members`, `counted`, `lower`, `table`, `layout` ×2 each. **So a mechanical nesting does not compile**, and the escape the author asked for — a file-local `as` alias — is `docs/panel/100`, convened the same day. This item waits on that verdict and inherits it; nothing here is decided by writing it down. **What is already known about the work itself**: 1255 `use` lines change, every qualified mention changes with them (`emit_ctype.foo` → `ctype.foo`), all 301 blessed emissions in `tests/emission/` change because the C component is the sanitised whole path, the seed is regenerated and the fixpoint re-verified — mechanical, once the qualifier is settled, and impossible to start before. **And one number belongs to the next sitting rather than to this item**: panel 032's spec-warden carried its veto on the rename cost (*"D renames ≥40 of 119 files"*), panel 099 recorded that cost as **zero on the flat tree** — true of flat stems — and this measurement says **32 under nesting**. Both are right; the size is a property of the layout somebody chooses, not of the tree, and that is the fact the alias sitting argues from | docs/panel/100 (the alias, convened 2026-09-02) · docs/panel/032 R4/R5 · selfhost/module/paths.hero (`last_parts_collide`) · docs/work/DONE.md (the M-package-layout closing entry carries the same 13 groups) | the compiler is the one corpus that can falsify the price of a rule this project just adopted, and it does

  **CLOSED 2026-09-02 at M-selfhost-nesting** (`811e13e`, journal `docs/journal/028-selfhost-nesting.md`). Ten directories, **134 files moved**, 37 flat. **The item's own two predictions are both scored and one is FALSE**: it said *"all 301 blessed emissions in `tests/emission/` change because the C component is the sanitised whole path"* — **zero** of them hold a symbol from a `selfhost/` module, because they are emissions of `tests/golden/` programs, so the seed is the only generated C the milestone touched. And it said 32 files fall into 13 last-part collision groups that `module_last_parts_collide` refuses: true of the global rule panel 100 replaced, and under the per-file rule that landed the real number is **41 files needing 43 bindings disambiguated, in three clusters** — of which 10 modules took `as` and 3 locals were renamed. What the item could not have known is the class the prefixes were hiding: **0** live local-vs-module collisions flat, **23** nested, which is panel 102's whole subject.
