- [x] **The Linux container cannot run the `records` suite, because the image has no git** | measured 2026-09-07 while verifying the citation repair | The suite reads commits, tags and the ignore rules, so without git it stops with *the record checks could not run* cleanly, rather than passing, which is the right failure. It was worked around for that verification with one package install inside the run, and two further things the run taught: the copy must keep `.git`, and a `tar` from this Mac carries the ignored directories IN unless they are excluded, which would have hidden the very defect under test. **A fresh `git clone` from the read-only mount is the shape a future verification should use.** Nothing is owed today: CI has git and is the judge, and the container is the hunt | `docs/environment/linux/LINUX-MACHINE.md` · `docs/environment/linux/Dockerfile` | a hunting instrument that cannot run one of the three suites is worth knowing about before somebody trusts a green from it
