- [x] **panel 131** — withdraw panel 117's sixth option, reversing the ratification of 2026-09-07 | **RATIFIED 2026-09-11** (author instruction, *yes to the withdrawal*), put as its own item precisely because it asks for a reversal and a reversal must not ride inside a longer yes | **The three things that sitting itself said were owed before it could land were run, and two of its four adoption claims failed.** The prototype exists — 160 code lines type-checking clean in the real module graph, against a realistic **350-450 across six modules and the runtime**. **The map case**: two `==`-equal left operands measured against one right operand report two different leaves, so the leaf named is a function of insertion history rather than of the value, which is panel 117's own refusal ground for map rendering reproduced inside the thing it adopted instead. **The depth case**: a leaf is reported to depth 3600 with a path 28,813 characters long, and at 3700 it is `panic: stack exhausted`, exit 134, where today's thin `assert failed: a == b` is correct at exit 0 at depth 100,000. **And the load-bearing premise is false at the source**: panel 117 adopted it because *"the recursion cliff dissolves — the walk is `eq`'s walk"*, and `HeroEqWork` (`runtime/parts/array.c:185-190`) is `{a, b, elem, len}` with no path, no parent and no index base, draining LIFO. Panel 076's veto is engaged. **The conservative variant was recorded and not adopted**: records and variants only, no recursion through a container, ~120 of the 160 lines — refused because it buys nothing for an array, and the motivating case is two `[i64]`, as are the compiler's own ASTs. `tests/harness/suite_special.hero`'s aggregate pin **stays and stays correct**, and `spec § 12`'s clause *until this language renders one* stays honest because its condition is still being worked toward | `docs/panel/117-both-sides-of-an-assert-when-a-side-is-an-aggregate.md` § Corrected 2026-09-11 · `docs/panel/131-the-refusals-were-sound-and-their-reasons-were-not.md` | the three owed measurements existed so that a decision taken unrun could be unmade, and they did exactly that: the mechanism worked as written
