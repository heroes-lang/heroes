- [x] **The three ways a rule reaches a context, measured rather than assumed** | measured 2026-09-07, while re-engineering the contract | The author's instruction asked to *make sure every context takes in its rules*, and the Claude Code documentation answers two of the three questions and leaves the third open, so all three were run. **One: path-scoped `.claude/rules/` files DO reach a non-fork subagent, lazily and correctly scoped.** A general-purpose agent was asked to read one file under `selfhost/emit/` and then report its own context: of the **seven** rules files, exactly **two** had arrived, and they are exactly the two whose `paths:` match that file. The negative control is the sharp part — `c-boundary.md`, scoped `selfhost/emit/ffi*`, sits in the same directory and did **not** load. Scoping is per file, not per directory, and the frontmatter is stripped on injection. **Two: `CLAUDE.md` reaches a subagent as a SESSION-START SNAPSHOT, and is therefore stale in any session that edits it.** The same agent held the **1139-line** version from this session's opening commit while the disk held 322 lines, five commits later. The proof is clean and does not rest on the line count: `.claude/rules/` **did not exist** at that commit, so the rules content cannot have come from that snapshot. **This strengthens the architecture rather than threatening it** — a tree-local rule in `.claude/rules/` is not only cheaper than a paragraph in the contract, it is *fresher*. What it costs is that a judge convened after a contract edit reads the old contract, which is why the `llm-ergonomist` seat is now told in its own file to ignore `CLAUDE.md` outright. **Three: hooks fire inside a subagent.** Not documented either way, and answered for free: panel 117's ergonomist seat saved its experiment as `.txt` rather than `.hero` and said in its verdict that the formatting hook had told it not to, which is the `PostToolUse` guard written that morning firing in an agent nobody had wired it into | `.claude/rules/` · `.claude/hooks/fmt_check.py` · `.claude/agents/llm-ergonomist.md` | the instruction was to be sure, and being sure about a context means asking the context rather than reading the manual
