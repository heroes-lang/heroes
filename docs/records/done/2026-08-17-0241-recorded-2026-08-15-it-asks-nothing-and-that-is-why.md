- [x] panel 059 | **RECORDED 2026-08-15 — it asks nothing, and that is why it is ticked rather than carried.** The finding is sourced and lives in `docs/panel/059` § Findings, which is where a future sitting on the C boundary will look. It is **not a proposal**: Principle 0 does not fund it, no program needs it, and this list is for decisions the compiler is waiting on. An item that poses no question is the shape this list's own first rule warns about from the other side — it costs a read on every pass and yields nothing. The record keeps the four precedents (Clang 2015, Swift 3, Zig, Ada 1995) and the fact that they were **seen and not taken**. Was | panel 059's historian, sourced 2026-08-15 | **Nullability belongs to the declaration, and four systems say so independently — it was on nobody's ballot.** Clang's `_Nonnull`/`_Nullable` with audited regions (r240156, 2015); Swift SE-0055 making `UnsafePointer<Int>?` the nullable form and mapping `_Nullable` → `Optional` at import (Swift 3); Zig spelling it in the **return type**, `std.posix.getenv(key) ?[:0]const u8`; Ada's `Null_Ptr` as a *value of the pointer type* since 1995, with `Value` raising precisely because the programmer could have tested first. Every abort-camp precedent pairs the abort with a testable null — which Heroes has, verified during the sitting. **Not a proposal**: Principle 0 does not fund it and no program needs it today. Filed so a future sitting on the C boundary starts from this shape instead of rediscovering it, and so the record shows it was seen and not taken | docs/panel/059 § Findings · design.md §4.19 | the best findings in this project have been the option that was never on the ballot, and this one arrived after the vote rather than before it
