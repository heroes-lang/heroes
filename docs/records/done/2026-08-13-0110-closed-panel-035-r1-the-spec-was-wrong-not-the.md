- [x] Covered | M6 step 3 | **CLOSED, panel 035 R1 — the SPEC was wrong, not the compiler. The print row now admits `inf`, `-inf`, `nan`, and the abort list says *integer* division by zero.** `print(0.0 / 0.0)` prints `nan`, which has neither a point nor an exponent — and spec line 155 says an `f64` always has one. NaN appears nowhere in the spec or design.md, yet it is reachable, because "division by zero aborts" is implemented for integers only. Decide: does f64 division by zero abort too, or does the spec gain NaN? | runtime/runtime.c (hero_f64_render) · crates/heroes/src/emit/ops.rs (BinOp::Div) · spec lines 130, 155 | `sort` had to decide NaN's fate this step, and the language still has not
