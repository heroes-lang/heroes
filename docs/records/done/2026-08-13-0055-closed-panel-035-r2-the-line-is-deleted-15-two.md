- [x] Covered | panel 023 | **CLOSED, panel 035 R2 — the line is DELETED, −15. Two judges reached it independently: one wrote `next: Node?` from it and terminated a list with a fabricated `fail`; the other measured that `null` is already caught loudly, so the sentence was redundant as well as misleading.** **Spec line 53 is a defect this panel did not fix and must not be credited with.** "Absence is a different type (`T?`)" invites `T?` as an option type; line 50 defines it as "a `T`, or an error". The ergonomist predicts ≥25% of tree-shaped programs reach for `next: T?` under **either** spec — a `no_size` error where the author wanted an optional child. Decide whether the spec names the distinction or `T?` genuinely doubles as an option type | spec/heroes-spec.md lines 50 and 53 · docs/panel/023 § Watch list | the second-most-likely first draft of every recursive type in the language
