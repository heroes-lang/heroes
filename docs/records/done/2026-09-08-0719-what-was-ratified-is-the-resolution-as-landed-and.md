- [x] **panel 118 RATIFIED in full, over a description of itself that the sitting had already overturned** | sitting of 2026-09-08, `M-discard-refusal` step 1, five seats; ratified 2026-09-08 (*"all the decisions as you suggested, all a"*) | **What was ratified is the resolution AS LANDED and not as adopted**, and the distinction was not pedantry: the item in `docs/work/DECIDE.md` had described this sitting for a day in the words of the ballot rather than of the file's own § Corrections, and `/decide` corrected it **before** the question was put, because ratifying a description the record contradicts settles nothing. Three facts had moved. **R1's third position was withdrawn after it was run**: `drop(risky(0 - 1))` through `function drop<A>(_: A)` prints and exits 0 with the failure gone and **no discard statement anywhere**, so refusing a type parameter relocated the hole instead of closing it, and `_ = [x]` transfers it to the position the rule exists to protect. **R5 was replaced**: the emitter relaxation for `eq`, `ne` and the three bitwise ops was built, measured green on every suite and thrown away, because attacked at the shapes beside it it closed **two of four** — `_ = a == b` on a `str` and on an array still warned, their assignment being a runtime call rather than an operator — and what shipped declares the temporary `__attribute__((unused))`, the idiom the sibling half of this class already used on 2026-08-16, closing all four and keeping every abort check. **R6 cost +62** and not the adopted +49, because the clause that landed carries the outermost-type boundary, which `args_checked() -> [str?]` makes owed: one `_ =` line on the spec's own built-in drops every non-UTF-8 failure it carries. **The two alternatives declined** are the +15 clause naming one position, and the `ignore` built-in that two seats wanted and that is blocked by `selfhost/check/builtins.hero` sitting at 374 lines of a DECIDED 374 rather than by any seat's judgement. **The robust and the cheap disagreed here** and the author's standing instruction of 2026-09-08 is to prefer robustness; the two extra positions cost zero migration sites, and the shipped repair moves no golden's bytes | `docs/panel/118-the-missing-word-was-a-dead-variable.md` § Author's verdict, § Corrections · `docs/measurements/010-spec-budget-ledger.md` row 3965 · `spec/heroes-spec.md:99` | a sitting whose own corrections section reverses three of its resolutions is a sitting whose ratification item has to be re-read rather than recalled, and this one had not been
