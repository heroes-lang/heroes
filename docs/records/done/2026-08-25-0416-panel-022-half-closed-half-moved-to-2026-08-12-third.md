- [x] panel 022 — **HALF CLOSED, HALF MOVED to `DECIDE.md` 2026-08-12** (third `/decide` session). The ROADMAP's "reference counting leaks cycles" was fixed with the panel. The `// ORDER:` half is a decision with no milestone, so `SCHEDULED.md`'s own rule sent it to `DECIDE.md` — and verifying it corrected the item twice: the tree still has zero marks, but `resolve/exprs.rs` reaches its `BTreeMap` through `resolve/resolved.rs:147` `names_in` (was `resolve/mod.rs:198`), and design.md's port note naming `types/holes.rs` as the one map walk is obsolete (it walks vectors now). Was | Two free corrections the warden found while measuring something else: `docs/ROADMAP.md` asserted "reference counting **leaks cycles**", contradicting design.md §4.10 gift 2 (fixed with this panel); and `resolve/exprs.rs` walks a `BTreeMap` into diagnostic text with **no `// ORDER:` marker**, which §4.9 requires — there are zero such markers in the tree | docs/panel/022 § Watch list · §4.9 | a rule with no instances is a rule nobody is following
