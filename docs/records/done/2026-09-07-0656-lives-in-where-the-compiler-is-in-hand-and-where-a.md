- [x] **The contract gets two instruments: its size is weighed and its section numbers resolve** | done 2026-09-07 | `spec/contract` lives in `tests/harness/suite_spec.hero`, where the compiler is in hand and where a document is already weighed on this scale. It measures the contract with `heroes measure` and fails over `CONTRACT_CEILING` **5500**, against **5127** measured. **A ceiling and not a pinned equality, which is the opposite of `SPEC_TOKENS` eleven lines above it and deliberate**: the spec is budget-governed to the token, while the contract is amended by author instruction and often by one sentence, so an exact pin would go red on a typo and be raised mechanically until nobody believed it. What it holds is unbounded regrowth, and its failure message names the two places a rule can move to rather than inviting a bigger number. `records/sections` resolves every contract section a LIVING file points at. **This is the finding that turned a renumbering into a refusal**: those section numbers are cited **3280** times, **2599** inside records §14 forbids anybody to rewrite and **346** in commit bodies, and NOTHING verified one, because the dead-citation check reads paths and discards everything from the first colon followed by a digit. A renumbering would have made every one of those pointers wrong with the whole net green. Both checks have a test that makes them fire, which §9 requires: `spec/contract` takes its ceiling as a parameter so a stub compiler can fire it, and `records/sections` is fired through a crafted map rather than by writing a broken file into the tree, because the walk would then read that file as a real offender | `tests/harness/suite_spec.hero` § contract · `tests/harness/suite_records.hero` § sections | the contract had never been measured against anything, and the only reason it stopped growing this once is that somebody was asked to look
