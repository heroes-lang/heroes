- [x] **M-separate-compilation** | **CLOSED 2026-08-26 by M-separate-compilation step 7, and the closure is MEASURED rather than read off panel 091's own page.** What closed it is the milestone's architecture landing, exactly as resolution A said it would at +0 compiler lines: the library is one module, so one `.c` per module (step 6, `c2ae7b1`) plus acceptance row 1 (step 5, `e804467`) puts every `extern` group in exactly one translation unit. **The measurement, taken in this session over the 1,026 translation units on disk**: the group's `_Static_assert`s land in **`library.c` and nowhere else** — 34 of 34, plus one `librarysource.c`, which is a program compiled FROM that module rather than a copy of it — and every `hero_ffi_probe_*` function appears only in the TU that DECLARES its group (`cliio.c`, `clishell.c`, `bind.c`, and the FFI corpus's own modules). The 138-of-155 multiplier the item was opened for is dead. **What did not close, and it is a ruling rather than a leftover**: the `#include <hero_os.h>` line itself is still in every TU — one line, not thirteen — because panel 091's ffi-pragmatist vetoed pruning the include with compiled evidence (it deletes a group record's C type, and an uncalled group holds up a used one). **And the item outlived its own closure by four days**, because the rule that moves a ticked item into the record had no executor until 2026-08-26; `/step` reads `SCHEDULED.md` on opening a milestone, which is how this was found. The original text follows, unedited.

  **M-separate-compilation** | panel 087, measured 2026-08-19 | **An `extern` group's asserts and probes are not pruned by reachability, and under one `.c` per module that cost is multiplied by 153.** Measured: `function main()` + `print(1)` emits **156 lines of C**, and **13 of them (8.3%)** exist only for the Tier-2 library's `extern "hero_os.h"` group — nine `_Static_assert`s (`HERO_OS_OK`, `HERO_OS_NOT_FOUND`, `hero_file_read`, `hero_file_write`, `hero_args_count`, `hero_args_at`, `hero_exit`) and four `hero_ffi_probe_*` functions — in a program that reads no file, writes no file and reads no arguments. The emitter **does** prune the library's Heroes functions by reachability, and `selfhost/emit.hero:201-202 (and `emit_builtins.reachable` at :89)` says why in so many words (unfiltered, `read_file -> str?` put two option structs and eight per-type functions into every translation unit in the language); the FFI half has no counterpart. **The home is this milestone because this is where it stops being cosmetic**: every one of the compiler's 153 translation units would carry those 13 lines and `#include <hero_os.h>` whether or not it touches a file, and the same pruning question decides whether a module's TU includes a header its own code never names — which is acceptance row 1's territory (panel 030 R2, panel 033). It is also what makes a new `extern constant` cost `+2` lines in every program in the language rather than nothing, which is half of panel 087's own unresolved disagreement | selfhost/emit.hero:201-202 (and `emit_builtins.reachable` at :89) · selfhost/emit_decls.hero (the prelude's extern block) · docs/panel/087 § Queued | the pruning that exists for one half of the library was never written for the other, and separate compilation multiplies the gap by the module count
