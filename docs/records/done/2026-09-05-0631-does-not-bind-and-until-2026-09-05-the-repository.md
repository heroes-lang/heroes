- [x] **M-c-callbacks** — the `const` pointee, and whether it gets a spelling or a declared hole | panel 111 R4, and **measured on this Mac 2026-09-05, in step 0, after the permission landed** | **`qsort` does not bind, and until 2026-09-05 the repository claimed in writing that it does.** `selfhost/emit/ctype.hero`'s `.function_ty` arm says *"a Heroes function value and a C callback are the same eight bytes, which is what makes qsort and every raylib callback expressible"*. Measured with the permission in place: `function qsort(base: ptr, n: u64, size: u64, compar: (function(ptr, ptr) -> i32))` is `error[ffi_parameter_type]` at exit 1, because the header says `int (* _Nonnull)(const void *, const void *)` and Heroes has **no spelling for a const pointee at all** — `cstr` is `const char *` and `ptr` is `void *`, and there is no third. The diagnostic is correct and the promise above it is false. **R4 leaves two routes and both are language changes**: the `c_int`/`const` vocabulary, or a design.md Part 8 wart naming const-qualified pointee parameters as the known hole **with its return condition** (CLAUDE.md §12 — a refusal owes the program that would make it wrong, exactly as a feature does). **The step-0 default, taken provisionally and reported to the author**: the wart, because 3 of 8 real callbacks bind today and a word invented in a hurry stays in the language forever. What the sitting must price, none of it run: how many of the 45 corpus programs and 8 named callbacks a `const` spelling would unlock against how many tokens it costs §1.2; whether `const` is a pointee QUALIFIER or a new type (`cptr`), which decides whether it can appear in a `record` field; and whether the same word is owed for `const char *` where `cstr` already stands, since two spellings for one C type is the shape §1.7 refuses | selfhost/emit/ctype.hero (the `.function_ty` arm's own comment) · selfhost/emit/ffi_narrowed.hero (the callback note, which already tells the reader the hole exists) · docs/panel/111 R4 · design.md Part 8 | a comment in this compiler promises a binding the compiler refuses, and the promise was found false by running it rather than by reading it

    **CLOSED 2026-09-05 by `docs/panel/112-const-is-the-wrong-noun.md`, ratified in full the evening it sat, and the sitting refuted its own brief before it answered it.** The question this item asked — a `const` spelling or a declared hole — is answered **declared hole**, and the reason is a count rather than a preference. The ffi-pragmatist seat refused the eight-callback sample this item was written from and measured the real `sqlite3.h`: **106 callback signatures, 153 unspellable parameter occurrences**, of which a pointer to a struct the header declares is **102 (66.7%)**, a pointer to a scalar 26 (17.0%), a pointer to a pointer 18 (11.8%), **a const pointee that is not `const char *` 4 (2.6%)** and an opaque typedef 3 (2.0%). 17 of 106 bind today; a `const` spelling makes it 18. **So this item's own framing was the wrong noun**, and its *3 of 8* was wrong in the convener's favour as well: `const` buys **2**, because `nftw` is blocked by a `struct FTW *` that is not const at all. **R1 refused (a) and (b) on four independent grounds** — a new `Ty` case is 173 `non_exhaustive` errors across 49 files; the cheapest spec wording is +13 and the full vocabulary +49 against 266 tokens of headroom; `selfhost/` binds **zero** callbacks across four extern groups, so Principle 0's compiler-need branch fails; and the seat given only the spec could not reach the question at all. **R2 landed design.md Part 8 wart 19 at zero spec tokens**, in the ffi seat's ordering rather than the brief's, with a fifth cause nobody had named: an out-parameter **inside** a callback is a parse error, 26 occurrences in that one header. **What is not refused permanently**: R6 records that `quals` on the existing `function_ty` case is the only admissible route if the const half is ever closed — 2 edits in 1 file, no `Ty` variant, so panel 083's veto does not fire. Scored at the milestone's close in `docs/journal/033-c-callbacks.md` § Predictions, scored, where the seats' four predictions on this question are measured
