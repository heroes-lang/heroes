- [x] **panel 132** — ratify the sitting, and choose the surface it deliberately left open | **RATIFIED AS ADOPTED 2026-09-11** (author instruction, *`b`, yes, yes*) | **Shape E enters, and the spelling is `Room::width`, chosen against the prototype that was already built and green.** The sitting put both diagnostics in front of the author because the surface was the one thing it would not settle: the compiler-engineer's working branch used `Point.width`, and the author took the llm-ergonomist's `::` instead, on that seat's locality argument — `.` already qualifies a module and already reads a field from a value, so a third meaning makes `Point.width` legible only to a reader who knows whether `Point` names a type or a value, which is not on that line. **The price is recorded rather than hidden**: the `.` form costs zero lexer and parser lines because the grammar already parses it and refuses it at `selfhost/check/walk.hero:337`, while `::` is not a token and must be added — a compiler-size cost taken knowingly, CLAUDE.md § Precedence putting compiler size below comprehension. **Also ratified**: three shapes refused, the briefed one on four vetoes with four distinct compiled grounds; shape B-prime **scheduled and not adopted**, because what was compiled is a hand-written simulation of what the compiler would generate rather than the generation, which is the distinction that cost panel 117 four days earlier; and the mutation operator that pays for E under design.md §1.6, two seats having converged on it separately after finding that `heroes mutate`'s 14 operators include not one that typos a string literal | `docs/panel/132-two-seats-invented-the-same-thing-and-two-others-invented-the-other.md` | what the yes does not cover is the rot, which E leaves open and only the scheduled shape closes
