- [x] **CLOSED 2026-08-26, M-separate-compilation step 9.** Repaired exactly as this item described — `take_docs` asks for the DISTANCE (line ends between two adjacent offsets, column read backwards) instead of the position, in `selfhost/text_lines.hero`. Measured before: **21,119 of 21,254 `sample` hits, 99.4%**, inside `line_col` and its three runtime calls. After: `heroes check` on the compiler's own source **88.0 s → 27.9 s**, and three further instances of the field-place push took it to about 8. The item's own analysis was correct in every particular, including which callers of `source.line_col` could wait. Moved to `docs/work/DONE.md` | original text below | **M-separate-compilation step 6** — the second quadratic, deliberately not done with the first | found 2026-08-26 while profiling the frontend | **`cursor.line_col` scans from byte 0 on every call, and `take_docs` calls it per declaration.** After the arena repair took the frontend from 191 s to 88 s, `sample` puts `h_cursor_line_col` at the top of what remains, with `hero_str_byte`/`hero_str_len` under it. The function walks the text from offset 0 counting newlines, so on the compiler's own 1.2 MB source each call is up to a million byte reads, and `cursor.take_docs` asks for one per declaration plus one per comment it examines — 1,318 declarations. **The repair needs no new machinery and no language change**: `take_docs` never wants a line NUMBER, it wants two questions answered — is this comment on the line immediately above (count the newlines BETWEEN the two offsets, which are adjacent), and does it start in the same column (scan back to the previous newline). Both are local. `source.line_col` has other callers that do want a real position (`diag_render`, `parse_use`), and those are per-diagnostic rather than per-declaration, so they can wait. Not done tonight because step 6 was the work in hand and this is a second quadratic, not the one the author asked for | selfhost/cursor.hero:50 · selfhost/cursor.hero:208 (`take_docs`, which calls it at :209 and :215) | the profile names it, so the next person does not have to find it again
