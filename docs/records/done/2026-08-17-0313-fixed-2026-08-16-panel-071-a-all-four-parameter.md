- [x] panel 062 | **FIXED 2026-08-16 (panel 071 A).** All four parameter types are exit 1 now. `ffi_narrowed.rs` matched two clang strings, both **width** diagnostics; a **class** mismatch phrases itself two other ways and both contain `" to parameter of "`, which is the whole matcher. **The gate is unchanged** — `extern_at_line` still asks whether an `extern` is declared at exactly this file and line. **The condition that made it a repair rather than a regression**: the diagnostic fires whether or not the C type has a Heroes name, and only the `Fix` consults the table — declining would return `Vec2` to exit 2, **356 of 1159** entry points. **The widening broke two things and the corpus caught both within the hour**: it swallowed panel 058's `ffi_writable_parameter` class and told an author to *"declare it `cstr`"* when they already had (repaired by naming `ffi_mutable.rs`'s own `discards qualifiers` string as that class's property — a fact about the text, not a guess about precedence), and it said C *"would convert in silence"* where C refuses. **And it found a real mistake in the corpus**: `ffi-incomplete-record.hero` declared `CheckCollisionPointRec(point: Rectangle, …)` where raylib says `Vector2`, silent for as long as a class mismatch was blamed on the compiler. Was | measured 2026-08-16, probing panel 062's follow-up as a class | **Three `extern` parameter types are exit 2 when they disagree with the header, and the repair is one phrase family in `emit/ffi_narrowed.rs`.** Measured against a real header declaring `int32_t take(int32_t)`: a parameter declared `ptr`, `cstr` or `str` gives *"internal error: compiling the generated C failed"* — the compiler blaming itself for the author's declaration, which is the exact failure §4.19's guarantee exists to prevent. A parameter declared `i64` against the same header gives the correct `ffi_parameter_type` at exit 1. **The difference is not the location gate and not the machinery** — it is that `ffi_narrowed.rs` matches exactly two clang strings, `implicit conversion loses integer precision` and `implicit conversion changes signedness`, both of which are *width* diagnostics. A **class** mismatch produces a different family: `incompatible pointer to integer conversion passing 'void *' to parameter of type 'int32_t'` and `passing 'HeroStr' to parameter of incompatible type 'int32_t'`. **The probe already errors on the author's own line** — verified, `./p.hero:2:85` with the probe's text echoed — so `extern_at_line`'s gate, which is what panel 052 made the class safe with, needs no change at all. What is owed: the new strings, the header type recovered the same way (the last quoted name, which is `int` in both families, so `spelling()` is unchanged), and a message that does **not** say *"wider than"* or *"a different sign from"* — neither is true of a pointer against an integer. Also owed is a decision the width path never had to take: whether a `Fix` is offered, since for a class mismatch the header's type may have no Heroes spelling at all. **Panel path** — it widens a diagnostic class's admission rule, which is CLAUDE.md §4, and §7's narrowing rule (*"the narrowing is `declaration()`, not whose text it is"*) is the clause that governs it | crates/heroes/src/emit/ffi_narrowed.rs:32-34 (`NARROWED`, `RESIGNED`) · crates/heroes/src/emit/extern_probe.rs · docs/panel/052 | every other way to get an `extern` signature wrong is exit 1 on the `.hero` line, and this is three ways it is not
