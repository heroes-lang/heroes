- [x] **033 — `unmarked_handle_producer` did not look inside a returned record** | an `extern` handing a handle back in a struct FIELD carried no mark, the checker said nothing, and the imbalance was caught at exit instead of at compile time | **repaired 2026-09-15**, panel 149's R1 and R2 as adopted | **A handle is now what a type REACHES, not what it IS**, through group-record fields and fixed-array elements, to any depth. **The walk was not written: it already shipped.** `check/map_keys.hero` held a 58-line reachability walk with three tests, returning a dotted path, for the rule that refuses a handle as a map key; panel 149's compiler-engineer found it. It moved to `check/reaches.hero` and `check/acquiring.hero` became its second caller, so each rule is written in one place. **Both sides, and the consumer side is the half the sitting nearly missed**: `consumed_types` asked at one level, so `UnloadFont(font: Font consumes)` left the map empty and the rule returned at its first line — a producer-only widening **stays silent on raylib's `Font`, the only real shipped-library case, while erroring on the contrived one**, which the sitting's completeness critic measured. Verified against the compiler built from `selfhost/`: four shapes that compiled in silence and aborted 134 are now compile errors carrying the route — `Pair.s`, `Outer.p.s` at two levels, `Four.a` in a fixed array, and a record through an `@` out-parameter. The marked and `borrows` forms still build and exit 0 | `selfhost/check/reaches.hero` · `selfhost/check/acquiring.hero` · `tests/golden/check/fixedbugs-a-handle-reached-through-a-field.hero` | 149

- [x] **034 — the handle counter counted CALLS and not MARKS, so it killed the correct program and blessed the leaking one** | one `extern` carrying two `acquires` marks emitted ONE increment per call, so releasing both handles aborted at 134 and releasing one exited 0 | **repaired 2026-09-15**, and it was never conditional on the sitting's resolution | **The cause was a boolean where a count belonged.** `emit/ops.hero` asked `hands_a_handle_over`, a per-DECLARATION yes-or-no, and wrote one `hero_handle_acquired();` after the call however many marks it carried. The two predicates became counts, `handles_handed_over` and `handles_taken_back`, and the emitter writes one line per mark: **+13 lines across two files and exactly two call sites in the whole tree**, as panel 149's compiler-engineer priced it. `spec § 13` had already ruled the other way — *"which the program owes it. The owing is counted"*, one owing per handle and not per call — so this was the compiler disagreeing with the specification (CLAUDE.md § 12) and it cost no spec token. **The proof is in the emitted bytes**: the new golden's C carries two `hero_handle_acquired();` and two `hero_handle_consumed();` around one call. Found by the **spec-warden** while pricing 033's spec cost, reproduced independently by the **compiler-engineer** with two parameters of ONE handle type — which is why the axis is the number of marks and never the number of types — and re-run by the coordinator with two distinct handle types before it was filed | `selfhost/handles.hero` · `selfhost/emit/ops.hero` · `tests/golden/run/fixedbugs-two-marks-on-one-call-are-two-obligations.hero` | 149

- [x] **035 — `reaches_handle` gave up at a depth bound and the giving up was silent** | past a nesting depth the walk returned absent, so a handle used as a map key was accepted with no diagnostic | **repaired 2026-09-15**, in the same commit that moved the walk, which is where the sitting required it | **The bound became a visited set** keyed on the declaration index, which terminates without giving up. It is sound for this question because whether a type reaches a handle is a property of the type and not of the path that arrived at it. **The termination argument is now written down** in `check/reaches.hero`, in two halves, because the walk crosses two structures: the interned type table is acyclic by construction, and the declaration graph is NOT and a user can write the cycle — `check/sized.hero` refuses it and **does not stop this pass**, measured. **Three constructions gave three different boundaries, and that was the finding**: the coordinator's chain refused at 15 and was accepted at 16, 17 and 18; the compiler-engineer's refused through 16; the critic's refused 15 and 16. Verified after: 15, 16, 17 and 18 all refused. **The premise had been left behind on the original.** `reaches_float` still carries the sentence licensing a bound — a missed float refusal costs a named abort because the runtime guard is still behind it — and it is TRUE there. Behind the handle rule there is no runtime guard, only an address, and `handles.hero` calls the refusal it protects *a hole nothing can close*. Found by panel 149's **compiler-engineer** while measuring the walk's termination, widened to ordinary records by its **completeness critic** | `selfhost/check/reaches.hero` · `selfhost/check/map_keys.hero` | 149
