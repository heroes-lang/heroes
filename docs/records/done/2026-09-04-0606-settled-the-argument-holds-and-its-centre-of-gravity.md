- [x] **reasoning 001 — the lineage behind §4.10, and the five alternatives it declines** | 2026-08-10, the session that asked whether value semantics survives the objection that it *"reads as amateurish"*, and what each rejected memory model would have cost | **Settled**: the argument holds, and its centre of gravity is not the lineage but the two inversions (Swift, Erlang) and the rejected-alternatives table; `design.md` recorded what value semantics **buys** and nowhere what the alternatives would have **cost**, which is exactly what §3.2 exists for on the backend axis. **All four hand-offs landed in `design.md` the same day** (`DESIGN-LOG.md:108`, *"five appendix entries promoted from names to arguments"*): the K/APL double citation is now deliberate and explained (`:2927-2934`), arena-plus-indices is named as the route for general graphs (`:1497`), the three unsourced precedents are sourced — K's superlative to kdb+'s STAC-M3 results, R/MATLAB's copy-on-write, ORC's cycle collector — and the memory-model counterpart to §3.2 is a six-row table at `:1477-1487` that exists *"so that §4.10 is not relitigated every milestone"*. No panel was needed: zero spec tokens and zero code. **What only the note held is self-superseding**: its audit of what `design.md` did **not** contain on 2026-08-10 — `atomic` 0 occurrences, `arena` 0 — and both are now non-zero because of this session. Three claims it proposed were **not** adopted, and the record is the stronger half of that (`DESIGN-LOG.md:108`) | `git show eed4584f:docs/reasoning/001-value-semantics-lineage.md` · design.md:1477-1487, :1497, :2888-2940 · DESIGN-LOG.md:108 | the one note whose whole content is now in the living document, which is what a session's leftovers are supposed to become
