- [x] **The site's promise that every code block is a real file now has an instrument** | closed 2026-09-07, in the site's own build | `site/src/lib/figures.ts`, wired into all 46 hand-written wrappers, one line each: it re-cuts every figure's slice from the file at build time and refuses the build when the two differ, naming the page, the figure's position, the file, the range and the first line that disagrees. It checks the `figcaption` too, which `site/README.md` also asked for and nothing checked. **Made to fire rather than trusted**: planting one changed character in `site/src/html/docs/maps.html` gives *"figure 1 (examples/gallery/10-maps.hero lines 7-9): first difference at line 2"* with both versions of the line. Measured over the built site, 178 pages, **522** code blocks and output panes checked against the files they name, 0 differences. **The scheduling item recommended a row in the net instead**, and said "not a build of the site, which §10 keeps out of `heroes`": that rule is about the `heroes` binary's surface and this is the site's own build checking the site's own claim, so it is not the breach the item was guarding against. The build is also the place that can stop a bad page reaching a reader, because a red build is a red deploy workflow. A harness row remains possible and would catch the same thing one commit later | `site/src/lib/figures.ts` · `site/README.md` § docs · `site/src/pages/*.astro` | the paragraph that said *"Nothing enforces it"* also said it had once credited a `check.py` that does not exist, and named an instrument nobody then wrote
