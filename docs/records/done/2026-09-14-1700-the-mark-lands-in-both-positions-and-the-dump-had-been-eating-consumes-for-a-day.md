- [x] **M-marked-acquisition** | build the counter, which is the half of the mark that does anything — and its price is unrun | **closed 2026-09-14**, M-marked-acquisition step 4, built in the tree and measured in both directions | **The mark and its counter are landed, and the sitting's resolution is in the code rather than in a document about the code.** `acquires <releaser>` parses in **both** positions — a handle result and an `@` out-parameter, which is where `owned` already rides — requires its name (`error[expected_releaser]`, with the shape in the message), round-trips through the formatter, and appears in the AST dump. The counter is the runtime's **fourth**, beside the three that were there, and it is a count rather than a header because a C handle is the one resource the runtime never allocated and can never size. **Measured in both directions, and they are two different faults.** POSITIVE accuses the program: `panic: 1 C handle(s) never given back — every call marked acquires owes one marked consumes, and this program is missing that many`, exit 134, on exactly the shape `docs/measurements/030` had measured as **exit 0 in silence**. NEGATIVE accuses the binding, and **that message exists because the first version killed a correct program**: `examples/curl/main.hero` has carried `consumes` since panel 145 landed it on 2026-09-13 and had no `acquires` to pair it, so the counter went to -1 and said *1 C handle(s) never given back* — the exact opposite of what had happened. One counter, two sentences; the repair to the corpus is **one word** on `curl_easy_init`, and it names what gives the handle back. **The corrected program is silent and the unmarked corpus is untouched**: four shipped examples run at exit 0, because the mark is opt-in and a binding that carries neither word counts nothing. **Three golden cases**, each shipping its own header so it fires on three platforms rather than where a library is installed: the pair balancing, the leak, and the unpaired binding. **Cost, measured in `suite_layout`'s own unit**: +25 code lines of surface across 7 files and +34 for the counter, plus 44 non-blank C lines in the runtime — and **two ceilings moved**, `ast.hero` 505 -> 525 and `print/fmt.hero` 1156 -> 1175, which is the budget panel 148's own critic found that five seats had not judged. **And CL-036 fired twice in one day on one form**: the formatter dropped the mark off a PARAMETER in the pricing prototype, and dropped it off a RESULT an hour later in the real tree, each time at exit 0 with no diagnostic | `spec/heroes-spec.md` § 13 · `selfhost/handles.hero` · `selfhost/parse/members.hero` · `runtime/parts/alloc.c` · `tests/golden/run/ffi-acquires-and-consumes.hero` · `docs/panel/148-the-mark-is-written-and-never-inferred-and-it-names-what-ends-the-life.md` R2 | 148

- [x] **CL-036's walk** | the AST dump had been dropping `consumes` since the day that mark shipped | **closed 2026-09-14**, M-marked-acquisition step 4, found by walking the list rather than by a suite | **`heroes parse --dump-ast` printed `function curl_easy_cleanup(handle: Curl) -> ()` for a program that says `consumes`.** The word was gone, exit 0, no diagnostic — from **2026-09-13**, the day panel 145 landed it, to 2026-09-14. **What makes it worth its own entry is the comment one line above the hole**, written before the hole existed: *"This dump is what `heroes fmt` compares its own output against, so a form missing HERE makes the formatter's self-check agree with a formatter that is deleting it — which is exactly how `as` shipped."* So the guard and the thing guarded had the same blind spot, and the file said so about a different word while being wrong about this one. **Why no suite caught it**: the dump's own test fixture carries `owned free` and `owned drop` and **neither** of the two marks, so the case that would have failed did not exist. The fixture now carries both — `function grab(h: Font consumes) -> Font acquires release` — which is what `.claude/rules/diagnostics-and-goldens.md` means by *each is owed a test rather than a reading*. **Found by walking CL-036's list for a new word and noticing the old one was missing too**, which is the whole argument for having a list instead of remembering | `selfhost/print/dump.hero` · `.claude/rules/diagnostics-and-goldens.md` § A new surface form lands in every tool · CL-036 | 148
