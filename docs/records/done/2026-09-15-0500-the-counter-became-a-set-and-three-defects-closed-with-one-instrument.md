- [x] **038 — a mark is one obligation and a fixed array of handles can be released one element at a time** | `Slot[4]` behind one `acquires`, released element by element, aborted at 134 against a runtime message naming no cause that fit | **repaired 2026-09-15**, and **the program was correct all along** | **It did not close by refusing and it did not close by counting**, which were the two routes panel 150's seats split over with two opposing vetoes. It closed by **changing the instrument**: `runtime/parts/alloc.c` now holds the SET of live handle addresses, and `check/reaches.hero`'s new `handle_suffixes` tells the emitter every handle a marked type reaches, so the four elements of a `Slot[4]` go into the set individually. **Releasing them one by one now balances and exits 0**; releasing the composite balances; doing both is a double release the set can name. The written length is the only number there is, and panel 150 measured that C's FILLED count is a runtime choice no mark can carry — with a set it need not be carried, because an unfilled element is a NULL and a NULL never enters. Measured before: `+3`, abort 134. Measured after: **exit 0** | `runtime/parts/alloc.c` · `selfhost/check/reaches.hero` · `selfhost/emit/ops.hero` | 150

- [x] **039 — `acquires` counted a failed producer's NULL, so the correct failure path aborted** | a producer returning `nullptr` on failure was counted as an acquisition, so a program that checked for null and returned aborted at 134 | **repaired 2026-09-15** | **A null is not an address, so nothing is recorded.** One line in `hero_handle_acquired`, and it is only sayable because the entry point now takes the handle: a counter was never told which value it was counting. Measured before: the program printed *handled the failure correctly* and then aborted. Measured after: **exit 0**. Found by panel 150's **ffi-pragmatist** on a `ptr` and re-run by the coordinator on a HANDLE, which is what showed it reaches the machinery panels 148 and 149 built rather than only the `ptr` gap it was noticed in | `runtime/parts/alloc.c` | 150

- [x] **040 — releasing a null discharged a real obligation, so a leak exited 0 in silence** | `slot_close(nullptr)` decremented the count, so a program holding one leaked handle and releasing one null balanced and said nothing | **repaired 2026-09-15**, and it was the worse of the pair | **The set is asked whether it HOLDS the address, and a null is not one.** C lets a program free NULL and this still does; what it no longer does is let that pay off a real obligation. Measured before: a program leaking a real handle and releasing one null **exited 0** and printed what it meant to print. Measured after: **abort 134**, naming the leaked address. **The escape from 039 and the hole in the leak check were the same construct**, which is panel 150's critic's finding and the reason neither could be repaired by loosening the other | `runtime/parts/alloc.c` | 150

- [x] **the runtime's exit message stops offering a closed list of causes** | it had been rewritten three times in two days, and panel 087 had already vetoed a closed list of aborts | **replaced 2026-09-15**, not repaired | The message now **names the address** and says what happened to it: *given back that were never taken*, or *never given back*. A closed list is a premise about the world that ages, which is CL-057's shape and panel 087's veto one level up. **The address goes LAST, and that is not a style choice**: a golden asserts a SUBSTRING, so an address in the middle splits the one stable sentence in two — measured when three shipped goldens went red on a message that was right | `runtime/parts/alloc.c` · `tests/golden/run/abort-handle-*.expected` | 150

- [x] **the runtime's shared-state sweep skipped every declaration containing `const`** | `static const void **p` is a MUTABLE pointer to const data, and the sweep read the word rather than the row | **repaired 2026-09-15**, found by the sweep failing on this sitting's own new state | **The narrowing now asks the row in hand**: a declaration with no `*` cannot be a pointer, so its `const` is the object's. `static const int64_t X = 5;` is still skipped and is still genuinely not shared state; `static const void **hero_handle_set` is now seen. **The handle set had landed exactly in the hole of the one check that exists to find it**, and the honest repair was to close the hole rather than rename the variable around it. A `T *const p` is now caught instead of skipped, which is the loud direction. Two assertions were added to the recogniser's own tests, one per side | `tests/harness/suite_runtime.hero` | 150

- [x] **041 — `unmarked_handle_producer` named no location for the declaration that armed it** | the rule is program-wide, so the refusal landed in a file whose author never wrote the `consumes` that caused it, and the message gave the function's name and no path | **repaired 2026-09-15** | **The answer already shipped and six other diagnostics already called it.** `source.elsewhere` says `line 12` when both ends are in one file and `main.hero:4` when they are not, and its own comment states the rule design.md §4.17 wants: *"the note carries the file the model would otherwise open, and when that is the file already on screen, naming it is noise."* This rule was the seventh and did not call it. `taker_of` now answers with the taker's NAME SPAN rather than a bare string, and one new function builds the refusal from the declarations so the two call sites cannot drift on what they pass. **Measured in both directions**: across files the message now reads *"`slot_close` takes one back, at …/main.hero:4"*; in one file it stays silent about the location, which the existing single-file golden asserts. The regression case is `tests/golden/surface-fixtures/handlearming/`, two modules and a header, because the defect only exists where the two ends are in different files — which is why nothing caught it | `selfhost/check/acquiring.hero` · `selfhost/source.hero` · `tests/golden/surface-fixtures/handlearming/` | 150
