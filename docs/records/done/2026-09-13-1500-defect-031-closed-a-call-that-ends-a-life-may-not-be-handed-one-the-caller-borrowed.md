- [x] **031 — one copy of a value can free what every other copy holds, at exit 0 with no diagnostic** | **closed 2026-09-13**, M-handle-verdict step 5, on panel 145's own return condition | **`consumes` on a C parameter says the call ends that value's life, and a call that ends a life may not be handed one the caller BORROWED.** The reproducer filed against this defect — `free(h.cell)` inside `closed(h: Holder)`, a function taking its `Holder` without `@` — is now `error[consumed_borrowed_handle]` on the line that frees, with a note naming the repair and a second note naming what it does not catch. **The condition panel 145 wrote is met exactly**: *a prototype of the rule at ≤ 150 code lines refusing the reproducer in `heroes check`*. It is **114 code lines** in `selfhost/check/consuming.hero`, and the rule is LOCAL — the argument's place root and what the resolver says that name is, no flow analysis, which `check/leasing.hero` states the checker does not have. **The measurement settles a contradiction between two seats of that sitting.** The compiler-engineer predicted the rule would refuse **0 of the 17** correct calls in the shipped SQLite binding; the completeness critic read the code and said **2**. Run: **2**, and they are the two the critic named — `closed(db: Db)` and `finalized(statement: Statement)`, both consuming through a parameter written without `@`, both repaired by `@` exactly as step 1 repaired `stepped`. **And a third cost neither seat counted: the mark PROPAGATES.** `@` on those two wrappers obliged every caller to hold a cell rather than a binding — **24 lines across `examples/ledger/`** for two marks: 2 signatures, 13 call sites, 7 bindings. Output byte-identical, sixteen tests green. **What this closes and what it does not, measured rather than assumed.** Repaired to `closed(@h: Holder)` the program compiles again and `--sanitize` still reports `heap-use-after-free`, because a copy made BEFORE the call still holds the freed address. That remaining class is the affine handle — core by §1.7, Part 6's borrow-checker row's territory — and design.md **Part 8 wart 20 states it in the present tense** with the measurement above written into it. The defect as FILED is closed; the class it belongs to is a stated cost, which is what a wart is for. **One consequence outside this defect**: the Part 6 borrow-checker row named its own falsifier as *an annotation with a refusal rate on `examples/` below 13 of 17*. This mark refuses **2 of 17**. The falsifier fired, and that row's ground moved from *unwritable from the headers* to *cost* the same day, in its own words. **And the formatter dropped the mark in silence** until it was taught, measured the hour the word landed: `heroes fmt` on a binding stripped the safety mark off it and said nothing, which is CL-036's whole sentence — a tool that has not learned a new form does not error, it drops it | `spec/heroes-spec.md` § 13 · `selfhost/check/consuming.hero` · design.md Part 6 borrow-checker row, Part 8 wart 20 · `examples/ledger/db/sqlite.hero` · `docs/panel/145-a-handle-is-a-pointer-with-a-name-and-the-compiler-already-reads-the-name.md` | 145 (139 inherited)
