- [x] **M-corpus-depth**, priced there and built only if the price is a file handle | measured 2026-09-03 while planning the milestone | **No program in `examples/` reads stdin, and every other language's example suite carries one** (Wren's `// stdin:` directive in `util/test.py`, verified 2026-09-03). The corpus suite has `main.args` and nothing for input: `tests/harness/shell.hero:74-92` opens stdout and stderr as files for the child and says nothing of stdin, and the spawn is the runtime's `hero_spawn` in `runtime/hero_os.h`. The shape is `main.stdin` beside `main.args`, fed to the child, and one program — a `wc`-like counter over `getchar` from `stdio.h`, which is also the commonest shape of a small C-bound program. If the redirect needs more than a handle in `hero_spawn`, this leaves the list for `DECIDE.md` with its measured price rather than being built at any cost | tests/harness/suite_corpus.hero:238-260 (`main.args`) · tests/harness/shell.hero:74-92 · runtime/hero_os.h | the commonest shape of a small program is the one the corpus cannot run

  **CLOSED 2026-09-04 (M-corpus-depth), and BUILT rather than filed because the price was measured first.** The item's own gate was *build it only if the price is a file handle*: it is one `open` plus one `dup2` in `hero_run_child` on POSIX, and one `CreateFileA` on the Windows arm where the inherited handle already carried a dead-handle probe. So `hero_run_go` takes an `in_path`, an empty one meaning inherit exactly as it already meant for the other two streams; `HERO_RUNTIME_ABI` moves **19 → 20** through the two-phase ritual (emitter first, header second, seed third, then the fixpoint); `tests/harness/shell.hero` gains `run_fed` beside `run` — a sibling and not a parameter, because `run` has 28 call sites and one of them ever wants to feed a program; and `suite_corpus.hero` reads `main.stdin` beside `main.args`, at all three levels. **The program is `examples/tally/`**, which counts lines, words and bytes of what it is fed and is checked against `wc`: `3 9 44` and a longest line of 19, identical. It is also the corpus's cheapest FFI program on purpose — one header, one function, no `link`, so it cannot skip on any platform, which matters because four of the six other FFI programs skip on Windows. **Two language facts it made visible**: nothing in Heroes reads a stream, so even `getchar` comes from C (§1.11), and nothing turns a byte value into a character, so the program carries a table and says so.
