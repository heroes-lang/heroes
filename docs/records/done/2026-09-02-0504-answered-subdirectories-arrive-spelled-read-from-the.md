- [x] **M-package-layout**, opened and answered 2026-09-02 | **ANSWERED: subdirectories arrive, spelled `use syntax/decl`, read from the directory of the file you compile, binding the last part** — `docs/panel/099` (full panel, five seats, **none for the alternative**), landed in `M-package-layout` steps 1 and 2 (`a985d28`, `c12de74`). The item asked two things and both are closed. **The spelling**: panel 032 R4's fixed form, unchanged, at the wording panel 099's spec-warden beat out of it — spec **3592 → 3630, +38 net**, gross +42 against a **−4 named removal** (", beside this file," became FALSE for any importer below the root, so removing it was mandatory rather than tidy). **The half that was genuinely open** — root-relative against file-relative — went to ROOT: the compiler-engineer measured ≈95 non-test lines against ≈180 plus a canonicaliser five identity sites must never forget, the ffi-pragmatist **compiled** the alternative's two failure shapes into `redefinition of 'h_token_next'` and an undefined-symbol link error (both exit 2 on LEGAL programs, panel 032 D1's class), the warden priced the climb operator at **+7 with three silences unclosed**, the blind ergonomist wrote the root-relative spelling unprompted, and the historian found **no language on record ever moved anchored→relative**. So `..` has no spelling at all and a module above the root belongs to another program. **What the record should carry forward**: the acceptance is a program — `examples/shapes/`, six modules over three directories, one `use geom/point` written identically from three depths, exit 0 — and the C component is the WHOLE path (`h_geompoint_span`, read out of the emitted C), which is panel 032 R5 holding as measured. **And the number the next sitting needs**: nesting `selfhost/` by prefix families would put **32 of its 169 modules into 13 last-part collision groups** (`state` ×4, `decls` ×3, `ops` ×3, `types` ×3, `builtins` ×3 …), which the new `module_last_parts_collide` refuses — so panel 032's warden was right about the SHAPE of its rename argument and wrong about the size, and the size depends on the layout chosen rather than on the tree. Panel 099's own verdict is `Pending` and its ratification item is open in `docs/work/DECIDE.md` | Was | **M-package-layout** | open question, author 2026-08-11 | **Do subdirectories ever arrive, and in what spelling? — the home changed on 2026-08-25 and is now `M-package-layout`.** The deferral to **M-package-manager** was ratified by the author on 2026-08-12 and **superseded by author decision 2026-08-25** (`/decide`, answer `2a`): this question is not a clause of the package manager, it is a milestone's whole subject, and it sits **before** the manager rather than inside it (`docs/ROADMAP.md` order row 27; `DESIGN-LOG` 2026-08-25). The decision was taken because the author expected M-separate-compilation to deliver it and that milestone delivers the build architecture instead, so CLAUDE.md §14's *name the deliverable, never the area* applies twice over. **The item stays here rather than moving to `DECIDE.md`** — SCHEDULED.md's own rule sends an item away when it loses its home, and this one gained a more specific one. Nothing below is answered by the move: the qualifier is still the open half, and the sitting that rules on it is convened when the milestone opens, not now. Today `use geom` reads `geom.hero` beside the file, and `use shapes/geom` / `use geom.shapes` are refused by name. **TWO OF THIS ITEM'S OWN PREMISES DIED THE DAY AFTER IT WAS WRITTEN, and both are corrected here rather than silently dropped** (found 2026-08-25 while taking the decision above, by reading forward in the record as CLAUDE.md §1 requires — the item had never cited `docs/panel/032`, which is the ratified sitting on its exact subject). **(1)** *"The escape valve is Nim's and it is additive — a quoted string for paths that are not identifiers"* is **struck**: panel 032 R2 killed the quoted form as dominated, because quotes are dead syntax under an ASCII-only grammar and cost one module two lexical classes. What R4 fixes in its place, at **+26 measured tokens** rather than the +38 that was tabled, is `- A `use` may be a path: `use syntax/decl` binds `decl`. Last parts are unique.` **(2)** So *"the unresolved half is the qualifier"* is now only half true: the headline answer is on the record — the module is `decl`, the last part — and what stays open is **root-relative against file-relative**, which R4 does not say and the ergonomist attached a prediction to. Two of 032's numbers have also moved, measured 2026-08-25: **zero** of `selfhost/`'s 158 module names collide (its rename argument cost nothing on today's tree, 9 stems collide across the repo's 451 `.hero` files), and the engineer's *"≤45 files at M-selfhost-port close"* was **143** at the tag and is **158** now. What survives untouched from the original item: the qualifier is what panel 031's ergonomist could not predict, and the separator is settled by precedent (`/` in the source, mapped by the compiler — Go, C's `#include`, Python). Packages still supply the sharpest case — naming a module of another library — and ordering the layout milestone first is the author's recorded trade | docs/panel/032 R2/R4/R5/R6 (ratified 2026-08-12) · docs/panel/031 § the ergonomist's H11 and ct4 · selfhost/parse_use.hero:47 (`module_path_has_no_parts`; the item cited `crates/heroes/src/syntax/decl.rs` until 2026-08-25, six days after that tree moved to `archive/bootstrap-rs/`) | the machinery for a basename collision already exists — it is what refuses `geo_m` beside `geom`
