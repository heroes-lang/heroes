- [x] **030 — the specification's promise about copies is false through a `ptr` field** | **closed 2026-09-13**, M-handle-verdict step 1, on the author's authorisation of the one network call the landing needed | **The false sentence is gone and the shipped counterexample stopped demonstrating it.** Spec § 3's *"No aliasing exists anywhere"* now reads *"No aliasing exists among the values this language owns. A `ptr` is a copied ADDRESS, wherever it sits: two copies reach one foreign thing, so a function taking one without `@` may still change, or free, what C holds. A `cstr` copies an address too, and only a group's `record` may hold one"* — panel 139's merged wording, **+75 vendored measured alone**, the *or free* clause added for defect 031. **Four acts, as the sitting ruled a sentence alone was not the repair.** (1) The sentence. (2) Its descendants: the `aliased_mutable_arguments` note and the `writes.hero` comment that quoted it, three runtime comments (`array.c`, `heroes_runtime.h`, `map-write.c`) and three golden headers (`fixedbugs-map-store-aliased`, `fixedbugs-equality-was-decided-by-an-address`, `adversarial-cow-per-step`) — the goldens corrected UNDERNEATH with the date, being records; every one of them is about a value the language owns, so what each tests is unchanged and only the quotation moved. (3) design.md's borrow-checker row, corrected at panel 139 itself. (4) **`examples/ledger/db/sqlite.hero`'s `stepped` takes `@statement`**, thirteen lines across two files as the FFI seat priced it: the marker changes nothing in C, and that is the point — the signature stops promising what it cannot keep. It is one of thirteen by-value wrappers in that file and the one that shipped the counterexample. **What paid**: § 4's restatement of the four kinds, **−43 vendored measured alone** (priced −54; the rule sentence *Every top-level line starts with its kind* stays), and CLAUDE.md §12 for the rest — a document saying something false about what a copy is manufactures the defect. Real count **7531 → 7610**, digest `196e3bfd0166e373`, 582 free. **Found in the landing and not in the sitting**: `tests/harness/suite_spec.hero` held ONE `REAL_TAKEN` date for TWO independently refreshed documents, and it went red the moment the spec was re-taken and the contract was not; `selfhost/measure/pinned.hero` had carried the two dates separately all along. Split, with the coupling named. And panel 139's spec-warden prediction — *≤ +45 real, lands ≤ 7525* — is **falsified** at +79 and 7610, for two nameable causes: the sitting adopted +75 over the warden's own +33, and the landing carries panel 144's § 10 correction beside it. **What this does NOT close**: 031, the same address freed through one copy and written through another, which the sentence now names and no rule refuses; and 029, the swapped handle. Both are M-handle-verdict's sitting | `spec/heroes-spec.md` § 3 · `examples/ledger/db/sqlite.hero` · `docs/panel/139-the-sentence-was-false-and-so-were-four-of-its-neighbours.md` · `docs/measurements/010-spec-budget-ledger.md` row 70 | 139 (144 beside it)
