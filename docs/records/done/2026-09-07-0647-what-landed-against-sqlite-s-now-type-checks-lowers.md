- [x] **M-declared-freer**, its last condition — **the `@` cell FREES, and the program panel 109 was convened for runs** | closed at M-declared-freer step 5, 2026-09-07; the item was filed at step 4b with the design worked out and the blocker named, and both held | **What landed.** `@error: cstr owned sqlite3_free` against SQLite's `char **errmsg` now type-checks, lowers, emits, runs and releases. The checker half is `check/freer.hero`'s `handed_back_param`, six lines as the item predicted, wired into `user_call`'s parameter loop and into `signature_of`. The lowering half is a **hidden `cstr` cell**: `ir/inout.hero` allocates it, null-initialises it, hands C its address, and records a `WriteBack` marked `owned`; `ir/owned_release.hero`'s new `release_cells` pays it after the call with the same four steps the result position already used — copy through the library's `validated` FIRST, null test, `cast cstr_to_ptr`, the freer by name — and only then stores the `str?` through the author's own place. **The blocker the item named was real and the fix was the one it priced**: `lower_args` and `with_receiver` gained a `callee: i64?` and a position, threaded from **nine** call sites (the item said six; the four `lower_args` sites that construct a record, a case, `ok`/`fail` and a built-in were not in its list, and each takes `fail("none", …)` because none can carry a mark). **`release_cells` runs on EVERY path and that is not tidiness**: the two marks are independent, so a cell release hung off the result mark's branch would be skipped for exactly `sqlite3_exec`, which returns an `i32`. **The emitter owed one line and only running it showed which**: the hidden cell is a `cstr`, so its address is `const char **`, and against `char **` that is `discards qualifiers in nested pointer types` under this compiler's own `-Werror` — `guard_arguments` now spells a marked `@cstr` `(char **)&cell`, which is step 4b's probe mechanism one level up and had to match it exactly. **Measured on this Mac, 50,000 refused SQLite statements, peak resident size over three runs each**: the binding that asks for the message and never frees it is **4,587,520 / 4,571,136 / 4,587,520** bytes, the marked one **2,981,888** three times — **1.60 MB apart, and the marked arm is the one that also copies every message into a Heroes `str`**. `examples/ledger/db/sqlite.hero` is back on the mark, which is panel 109's condition 8, and its 16 tests and the corpus's 54 programs are green in three configurations | `selfhost/ir/inout.hero` · `selfhost/ir/owned_release.hero` § release_cells · `selfhost/check/freer.hero` · `selfhost/emit/ops.hero` § guard_arguments · `tests/golden/ir/owned-cell.hero` · `tests/golden/run/ffi-owned-cell-is-freed.hero` · `examples/ledger/db/sqlite.hero` | the result position freed 12.8 MB per 200,000 strings and the `@` position freed nothing, and it is the `@` position `examples/ledger` needs
