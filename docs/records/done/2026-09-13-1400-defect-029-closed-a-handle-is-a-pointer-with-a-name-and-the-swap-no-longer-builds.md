- [x] **029 — a swapped opaque handle compiles at zero diagnostics and segfaults** | **closed 2026-09-13**, M-handle-verdict step 4, on panel 145's form as the author narrowed it | **A fieldless `record` inside an `extern` group is a handle: C's pointer to what the header names and leaves opaque.** `record Db tag sqlite3` is `sqlite3 *`, the tag written **verbatim** and never `struct sqlite3 *` — the completeness critic measured that `curl/curl.h` reads `typedef void CURL;` on this SDK, so the `struct <tag>` spelling every seat had proposed reached one of the three handle types in this tree. The reproducer that opened this defect, `examples/sqlite/main.hero` with line 74 reading `sqlite3_step(db)`, now fails to build with **`error[type_mismatch]: expected Stmt, found Db`** on that line; the unchanged example builds and prints `rows: 3` / `longest: 6`, byte-identical to before. **The measurement the step is judged by**: `heroes mutate --operator swap-ptr` read **15 mutants, 8 killed** before the form and **0 of the 7 that were the class**, the eight dying on `unused_binding` and `aliased_mutable_arguments`, neither about handles (`docs/measurements/029`). It now reads **15 of 15**, and the honest half **7 of 7**. The denominator did not move because the operator learned the form in the same commit — had it not, the rewrite would have hidden the sites and the form would have been scored against silence. **What landed**: `selfhost/handles.hero`, the predicate written ONCE and called by the parser, the checker, the emitter and `mutate` after five scouts proposed four definitions in four homes; the form in `parse/tails.hero`; the spelling in `emit/ctype.hero`; `nullptr` adopting a handle in `check/contextual.hero`; and `const_pointer`, because `const T *` qualifies the POINTEE when T is a pointer and clang refused `&db` at every generated `_eq`. **Zero new `Ty` cases** and `check/table.hero` untouched, which is the compiler-engineer's own prediction at panel 145, held. **Two refusals rather than implementations**, on the author's decision: a handle as a map key (directly or through a record that holds one) and two records sharing one tag. **And a third refusal was proposed and REFUSED on evidence**: a `tag` naming a type the header declares COMPLETE was to be refused, until `record File tag FILE` was compiled and run — `FILE` is complete on this SDK and the binding writes through `FILE *` correctly, so the refusal would have broken a legitimate binding. The fieldless form MEANS *the pointer*; completeness of the tag is irrelevant. **Spec**: § 13's `record` paragraph gains five lines and **its fence is rewritten to use the form**, +76 vendored net with § 13's package-reason clause removed at −17; 5722 → 5798 vendored, 7610 → 7721 real, digest `2b0304cb09af0f99`. The fence is the half panel 145 said decides everything, and `suite_special` runs it: the spec's own example is a program, and its body moved with it | `spec/heroes-spec.md` § 13 · `selfhost/handles.hero` · `examples/sqlite/main.hero`, `examples/ledger/db/sqlite.hero` · `docs/panel/145-a-handle-is-a-pointer-with-a-name-and-the-compiler-already-reads-the-name.md` · `docs/measurements/029` | 145 (135, 139 inherited)
