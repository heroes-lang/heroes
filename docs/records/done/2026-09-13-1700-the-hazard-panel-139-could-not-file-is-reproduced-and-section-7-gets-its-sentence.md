- [x] Covered | M-handle-verdict | **The hazard panel 139 could not file is reproduced, and § 7 gets its sentence**, closed 2026-09-13 (step 6). That sitting's completeness critic saw a record holding a FREED pointer compare `==` **true** to a record holding a fresh, unrelated allocation; the coordinator ran the same shape, got **false**, and filed it here as a question rather than in `DEFECTS.md`, which holds measured failures with reproducers. **Both were right, and the reason is the finding.** Re-run tonight against the shipped compiler: on **Darwin arm64 the same binary answers `true` 22 times in 40** — non-deterministic, which is why one person saw it and the other did not — and on **x86-64 Linux under glibc it answers `true` 20 times in 20**. The class is real, the reproducer is sound, and its SHAPE changes across platforms: intermittent on one, certain on the other. That is the third time this milestone met that pattern, after defect 029's segfault-on-Darwin and wrong-answer-at-exit-0-on-Linux. **The item said the answer depends on what `ptr` ends up carrying**: give it identity and the question dissolves, leave it an address and § 7 owes a sentence. Panel 145 refused identity as a form, so the sentence is what it owes and what it got: *a `ptr`, a `cstr` and a function value compare as an address **and not as an identity — free one and the next allocation may reuse that address***. **+16 vendored**, the cheaper of two drafts, the longer one refused for saying it twice. **Filed as neither a defect nor a wart, deliberately**: the behaviour now matches the document, so it is not a defect; and the item asked for a sentence in § 7, so adding a Part 8 entry beyond it would be this milestone deciding something nobody asked. The measurement lives here so the next sitting finds it rather than re-deriving it — and re-deriving it is exactly what failed the first time | **Origin:** panel 139's completeness critic, 2026-09-13, and the coordinator's failure to reproduce it, which is why it is here and not in `DEFECTS.md`. § 7 says `==` is structural equality, and that a `ptr` compares as an address. The critic measured a record holding a **freed** pointer comparing `==` **true** to a record holding a fresh, unrelated allocation, because the allocator had reused the address. The coordinator ran the same shape and got **false**. So the class is real — an address can be recycled, and equality cannot tell — but the reproducer depends on the allocator, and `docs/work/DEFECTS.md` holds measured failures with reproducers. **This sitting owns it** because the answer is whatever `ptr` ends up carrying: give it identity and the question dissolves; leave it an address and § 7 owes a sentence saying equality on one is an address test with a recycling hazard, not an identity test. | 139, 145
