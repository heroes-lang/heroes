- [x] **panel 171** | the word is `lent`, in its own slot beside `counted_by`; a lend reaches only a `lent` parameter of an `extern` function; the spec sentence is the warden's merged draft and promises nothing about a pointer C hands back; the landing is two commits with 27 marks on 19 functions across three trees | **RATIFIED 2026-09-21**, and the whole of it had already landed: commit A `83a8c92c`, commit B `abf9a17e`. The word, the rule, the sentence and the two-commit landing are confirmed rather than authorised, under CLAUDE.md § 3's rule that a milestone asked for in one `/step` decides its delegated questions with the recommended resolution as the default and says once which way it went. **The direction was never in the item**: the author ruled the default flipped on 2026-09-20 | `docs/panel/171-lent-and-the-pointer-c-hands-back.md` | 171

- [x] **panel 172** | the repair of defect 070 is a run-time report and not a word: `consumes` stays refused on a `cstr` or `ptr`, § 13 writes the limit with its falsifier, and the mechanism goes to a soundness-lane sitting before it lands | **RATIFIED 2026-09-21.** The word does not enter the language, design.md Part 6's row stands, and the conservative option the sitting recorded — route C alone, the limit written and no runtime change — was **not** taken | `docs/panel/172-the-report-comes-from-the-crash-and-not-from-the-declaration.md` | 172

- [x] **panel 173** | both seats veto the sentence, which asserts a cause the runtime cannot know and is false on six of nine measured paths; the repair is to say what was observed; the funnel, the sanitiser yield, the chaining and the frame-walk name are endorsed twice; the goldens need a harness form that does not exist | **RATIFIED 2026-09-21**, and the veto was answered before the ratification reached it: the sentence states the two facts the runtime holds, the harness gained `!sanitizer:`, and both were measured on Darwin and in the Linux container. The conservative option — report only on SIGTRAP — was **not** taken | `docs/panel/173-the-runtime-may-say-what-it-saw-and-not-why.md` | 173

    **Origin:** the author, 2026-09-21, in one act, in these words: *then ratify
    all the DECIDE items.* **Recorded as a ratification given in conversation on
    the coordinator's summaries in this session, and NOT as a reading of the
    three files, and NOT `by delegation`.** The author's standing instruction of
    2026-09-19 is that they read; the coordinator does not know whether these
    three were read, so the record says what it knows rather than the flattering
    half (CL-058). What the author was told, in plain words as each sitting
    closed: the flip's price and its two-commit landing; that the word closes
    one of seven shapes of defect 070 and not the filed one, and that three
    shapes have no parameter to mark; that both seats of the soundness lane
    vetoed the runtime's first sentence as false on six of nine paths.

    ## What the three yeses settle

    **The language does not gain a word for the freeing case**, and
    design.md:2667's Part 6 row — a mark on a parameter whose type reaches no
    handle — stands as panel 150 wrote it. What replaces it is a run-time
    report and a sentence in `spec § 13` carrying its own falsifier.

    **`lent` is the language's**, and with it the pessimistic default the author
    ruled on 2026-09-20: a C parameter is assumed to keep what it is handed, and
    a lend reaches only one declared `lent`. Defects 066 and 068 closed on that
    rule; defect 070 closed on the report.

    **`docs/work/DECIDE.md` returns to zero and `docs/work/DEFECTS.md` is at
    zero**, which is the first time both have been empty in this milestone, and
    it is what `records/tagged` reads before a tag may be written.
