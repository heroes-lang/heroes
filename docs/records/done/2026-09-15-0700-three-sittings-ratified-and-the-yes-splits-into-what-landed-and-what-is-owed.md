- [x] **panel 150** | the counter becomes a SET, a `tag` names a C type NAME, the mark is refused on a `ptr`, and the spec is corrected on both counts it is false about | **ratified 2026-09-15**, as adopted, the robust resolution and not the conservative one | **Two of four resolutions had already landed when the yes was given**, so the verdict is partly on what SHIPPED. **R1 landed** and closed defects 038, 039 and 040 together, because all three were one fact: the correct failure path went from abort 134 to exit 0, a leaked handle plus one null release went from exit 0 in silence to an abort naming the address, and a fixed array released element by element went from `+3` and an abort to exit 0 — the program had been correct all along. **R3 is owed and its shape changed**: refusing the mark on a `ptr` today would leave `getaddrinfo` unbindable rather than badly bound, because panels 151 and 152 left the author with no alternative spelling, so it waits on defect 037's remaining half. **R4 is owed at +7 real**, and panel 152's spec-warden re-ran all three probes and confirmed the document is still false at HEAD. **R2 is superseded rather than owed**: it adopted *a `tag` names a C type NAME* without a spelling, and two later sittings refused two spellings on measurement and adopted a third | `docs/panel/150-the-runtime-holds-a-number-where-it-needs-a-set.md` § Author's verdict | 150

- [x] **panel 151** | Route B refused for inverting the §4.19 probe, and the narrowing both seats compiled sent to a full sitting | **ratified 2026-09-15** | **Nothing is owed by this ratification, which is unusual and worth saying**: a sitting whose resolution is a refusal lands by not being built. What it leaves behind is the measurement that makes the refusal permanent — `record Db tag sqlite3_stmt` gives two `error[ffi_parameter_type]` today and zero under the refused route — so a later session proposing it again meets a number rather than an argument. **The route refused was the coordinator's own**, written into defect 037 an hour before the sitting, and its premise was false three ways: `handles.c_spelling` has zero callers, the existing clang dump never asks about a handle, and the emitter finishes before the ask. **The lane finding is ratified with it**: both seats said a two-seat lane could not ratify what they were asked, and the coordinator then convened three more under CL-023 rather than five | `docs/panel/151-the-probe-must-carry-the-authors-word.md` § Author's verdict | 151

- [x] **panel 152** | the `struct` qualifier read from clang's own refusal rather than guessed by a probe, an unknown tag still refused as a Heroes error, the spec at +4 real, and design.md stops saying the tag is written verbatim | **ratified 2026-09-15**, the capability yes and the generated probe no | **Half of R1 landed the same day and it is the half a program author meets.** `emit/ffi_lookup.hero` now finds a record by its TAG as well as its name, so clang's *"must use 'struct' tag"* stops being looked up under a name no declaration holds: a binding that produced `internal error: compiling the generated C failed` at exit 2 with zero Heroes diagnostics now produces `error[ffi_tag_needs_struct]` with the caret on the author's own line. **R2 landed with it**, a misspelled tag stays `error[ffi_unknown_name]`. **Owed**: the other half of R1, actually supplying `struct` from that same refusal, which is all that keeps defect 037 open; R3 at +4 real with digest `7898916124eeea2b`; and R4's design.md row, which is free. **The diagnostic that landed offers no fix and the yes ratifies that too** — its sibling can offer a `certain` one because a record with no tag can be given one, and here the tag is already correct and the repair does not exist | `docs/panel/152-the-answer-was-already-spoken-and-thrown-away.md` § Author's verdict | 152
