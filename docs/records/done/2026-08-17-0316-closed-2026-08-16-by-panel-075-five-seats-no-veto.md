- [x] panel 062 | **CLOSED 2026-08-16 by panel 075** (`docs/panel/075`), five seats, no veto against the repair. **The wording is not the ergonomist's and not the warden's**: `<` `<=` `>` `>=` with an `f32`/`f64` operand **abort** when either side is a `nan` — the answer this language already gives at `sort` and at a map key — while `==` and `!=` stay exactly as they are, so every `== 0.0` divide guard survives. **`is_nan` was vetoed twice** (four modules, two mandatory spec tests, a name reserved in every scope forever) and what its two approving seats wanted is delivered at zero new names: the spec points at **`x != x`**. Spec **+27, 3440 → 3467**, in three places rather than one, because an enumeration that names the operators and omits `sort` claims `sort` is fine. **The item's own price is corrected twice over**: it said eleven tokens, the swap measures **+5**, and the wording it priced is one candidate 2 makes false. **The item's premise held and grew** — the coordinator ran it and found the same program answering **nan** or **1.0** depending on which line the nan is on, and the ffi-pragmatist compiled two De Morgan-equivalent validators that disagree at exit 0. **Implementation owed**: +23 lines in `emit/operator.rs`, the three spec edits, a golden that exercises **both** guard sites, and the test that fires if `sort.c`'s two guards are ever deleted — because the claim that this rule subsumes them is **false**, measured three times. Was | panel 069, llm-ergonomist — **found from the spec alone, before it knew the question** | **With a `nan`, `<` and `>=` are BOTH false, so `if/else` takes the else branch in silence.** Confirmed by running it: `n < a` → `false`, `n >= a` → `false`, and the `else` arm executes with no diagnostic. IEEE requires this and every language agrees, so the compiler is not wrong — but the spec says nothing, and the seat that reads only the spec found it while doing something else, which is the strongest evidence this project's instruments produce. **The cost is bounded and worth stating**: this is a *silent branch*, not a wrong value, and the reader's model — *"one of `<` and `>=` must be true"* — is one every programmer holds. The ergonomist's own repair was **eleven tokens inside the sentence panel 069 already landed**: *"Every comparison with `nan` is false, `!=` true"*, which it argued was the best trade on the page because it closes a second bug class for the price of a clause. That wording was **not** adopted at 069 — the sitting took the warden's shorter form — so this is a live amendment with a measured price and a named alternative, not a discovery | spec § Values (the sentence panel 069 rewrote) · docs/panel/069 § Three findings nobody asked for | the seat that only reads the spec found it while doing tasks, which is the only way it could have been found
