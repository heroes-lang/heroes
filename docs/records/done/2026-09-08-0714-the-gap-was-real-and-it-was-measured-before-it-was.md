- [x] **A `certain` fix repairs the defect the diagnostic names, and one that leaves it standing is a `guess`** | filed 2026-09-08 as a panel 118 finding by the compiler seat, which ruled against certifying `.is_err()` on §1.2 and named the gap rather than inventing a rationale for it; settled by `/decide` the same day with the recommendation the item carried | **The gap was real and it was measured before it was written.** design.md §4.17 and panel 071 both answer the *inverse* case, a fix that CHANGES meaning — *"change save's signature to accept a str id"* — and say nothing about the case where meaning is preserved **and so is the bug**. `.is_err()` on a discarded fallible is behaviour-identical and keeps the swallow, so certifying it would have made `heroes check --apply` automate defeating the rule M-discard-refusal landed in the same milestone. **Compiling is not the bar, and the repository already knew it**: `_ = xs.push(4)` compiles at exit 0 and loses the element, which is why panel 071 made that fix a `guess` whenever a receiver is in hand — *"a certain fix converting a caught mistake into a silent wrong answer"*, in `value_errors.hero`'s own words. So the rule was practised in three places and written in none. **The adversarial pass, because a rule is worth what its exceptions are worth (CL-061)**: measured 2026-09-08, `selfhost/` holds **53** `Fix(` sites, **23** of which can ship `certain` — 22 unconditional, one conditional in `grammar_expr.hero` (`use while`), one forwarder in `lexer.hero`. All 23 read as compliant, and the two nearest the line obey by construction: `value_errors.hero`'s `discarded_value` de-certifies on a fallible and downgrades to a guess on a receiver, and `resolve/errors.hero`'s rename-to-`_` is gated to the `.loop_var` and `.payload` kinds alone, so it never fires for a binding — which matters, because `x = risky(1)` unused is `error[unused_binding]` with **no fix at all**, and a certain rename there would have machine-written the `_ = e` the next pass now refuses. **What is NOT claimed, and the reason is panel 118's own**: no instrument enforces this and none can — that sitting already recorded a prediction whose instrument could not exist — so the rule says plainly that it binds the writer and the reviewer, and that CI's weaker half (an applied `certain` fix must compile) is the only mechanical part | `.claude/rules/diagnostics-and-goldens.md` § Errors are a deliverable · `docs/panel/118-the-missing-word-was-a-dead-variable.md` R3 · `docs/panel/071` · `selfhost/value_errors.hero:113-129` · `selfhost/resolve/errors.hero:133-137` | `heroes check --apply` is a machine that edits programs on the compiler's word, and the one thing that must never be certifiable is a repair that silences the diagnostic and keeps the defect
