- [x] M-selfhost-probe (author decision 2026-08-12, `/decide`) | **DONE 2026-08-15, M-selfhost-probe step 1 (panel 065).** Thirteen `// ORDER:` marks across ten files, each naming the sort key and the observer; design.md §4.9's note rewritten to make the marks the inventory (`grep -rn "// ORDER:" crates/`) with the count kept only as a dated snapshot. **This item's own corrections had rotted a second time**: verified 2026-08-12 as "holes.rs walks only vectors", and `holes.rs:171` walks `resolved.top` again today (`nearby`'s reachability fix, same day) — three days, two rots, which is the argument the panel adopted for marks over prose. Two orders that reach the emitted C were pinned by no artifact at all (the double-emit test pins stability, not order — one binary emits both files); `tests/golden/emit/order-options-and-fn-typedefs` now pins both, and `tests/golden/check/nested-fixed-array-tiebreak` pins the one order a span sort cannot decide. Was | **`// ORDER:` — mark the ordering-sensitive map walks, and fix the note that points at the wrong file.** The marker is a design.md §4.9 convention with **zero** instances in the tree, and §4.9 says so itself (design.md:1233), so nothing is silently wrong today; the deferral is to the milestone that reads it, because the marker exists to tell the Heroes port where an explicit `sort` is owed and the note's last line is *"or the M-selfhost-fixpoint diff breaks"*. **Two corrections verified 2026-08-12 and owed to this item, not to be re-derived**: the port note naming `types/holes.rs` as the bootstrap's one map iteration is **obsolete** — `holes.rs` walks `resolved.locals`, `fields` and `cases`, all vectors — and the live site is user-visible, `resolve/resolved.rs:147` `names_in` (was `resolve/mod.rs:198`) iterating the `BTreeMap top` (`resolved.rs:116`) into the *did-you-mean* candidates and `--dump-scopes` (`resolve/exprs.rs:297-312`). Amending §4.9's sentence is design.md Parts 1–11, so it is a panel path (CLAUDE.md §4) even though the fact is not in dispute | design.md:1225-1236 · crates/heroes/src/resolve/resolved.rs:116,147 (was `resolve/mod.rs:167,198` until the §11 sweep moved the result types out on 2026-08-13) | the probe measures what self-hosting lacks, and an unmarked map walk is exactly a thing it cannot see by reading
