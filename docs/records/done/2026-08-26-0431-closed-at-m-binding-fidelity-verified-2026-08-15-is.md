- [x] panel 051 | **CLOSED at M-binding-fidelity** (verified 2026-08-15): `-Werror=shorten-64-to-32` is in the flag list with `-Werror=sign-conversion` beside it, and the veto was answered by building the gate rather than by arguing it — the emitter writes a never-called probe per `extern`, and `ffi_parameter_type` is exit 1 on the author's own line with the parameter's name in it. The three legs are green. **Re-verified 2026-08-26**: both flags are live at `selfhost/cli_flags.hero:35-36`; the citation this item carried named `toolchain.rs::FLAGS`, archived since 2026-08-19 | selfhost/cli_flags.hero:35-36 | the gate a veto asked for, built instead of debated
