- [x] **032 — `==` on a C handle answered a constant `true`, so every null guard in every binding was wrong, at exit 0 with no diagnostic** | **closed 2026-09-14**, M-cleanup-verdict step 1, found by this milestone's own opening census | **A handle compares as an ADDRESS, which `spec § 13` promises in as many words — *`nullptr` is its null and `==` compares the address* — and for one day the compiler generated `return true;` instead.** Five questions, four wrong answers: `a == nullptr` **true** where the handle was non-null, `a != nullptr` **false**, `a == b` on two distinct live handles **true**, and only `a == a` and `null == null` right. **What it breaks is the commonest line in a binding**: `if h == nullptr` after an init that can fail was ALWAYS taken, and a guard written the other way, `if h != nullptr`, NEVER fired — so a null handle sailed into the next C call, which is a §1.12 breach and not a cosmetic one. **The class is wider than the witness, attacked at the neighbouring shapes as § RUN IT asks**: the wrong `true` travels through a plain Heroes `record` holding a handle, through `[Db]`, and through `Db?`, because each of those asks the handle's own generated `_eq` — measured, five shapes, all five wrong, all five right after. **How it was found, and the finding is a second measurement**: this milestone's census needed to know whether a `sqlite3_stmt *` escaping a loop leaks, so a Heroes probe walked SQLite's own list of open statements and answered **`statements still open: 0`** while saying **`sqlite3_close says BUSY: true`** in the same breath — two sentences that cannot both be true. The same program written in C answered **1** and **true**. The contradiction was the compiler's, not SQLite's: `while s != nullptr` was a loop that never ran. After the repair Heroes and the C control agree line for line. **CAUSE**: `selfhost/emit/structural.hero`'s zero-field arm emitted `(void)a; (void)b; return true;` under the comment *record E is error[empty\_record] in the checker, so this is belt to those braces*. That is a premise about the WORLD, it was true the day it was written, and **M-handle-verdict killed it on 2026-09-14** by making a fieldless group `record` with a `tag` legal and giving it a pointer C type. The premise died, the comment went on reading as correct, and the fallback had been put in the QUIET direction — fifteen lines below a `partial` arm in the same function that gets it right with `hero_panic`. It is `.claude/rules/module-shape.md`'s rule failing in the one place that is hardest to see: a narrowing that asked the world rather than the value (CL-004). **WHY NOTHING CAUGHT IT, and this is the part worth keeping**: `tests/golden/run/fixedbugs-a-handle-in-a-record-field.hero`, landed the day before at M-handle-verdict step 8, asserts `print(s.handle == nullptr)` against an expected `true` — and its handle IS null, so the wrong answer and the right one are the same value. The one golden in the tree that compared a handle had picked the single case where a constant `true` is indistinguishable from an address comparison. It passed, for the wrong reason, for a day. **FIX**: `ctype.Aggregate` gains `handle: bool`, carried from `handles.is_handle_record` through `new_names` exactly as `foreign` and `partial` already travel, so the emitter asks the DECLARATION and never `c_type`'s trailing star — a star is a fact about a SPELLING, and `handles.hero` stays the one place that says what a handle is, which is the whole reason that file exists. `_eq` emits `return *a == *b;`; the not-a-handle zero-field arm becomes `hero_panic`, the loud direction its neighbour already used; and `_hash` folds the same address, because this module's stated premise is that the two walks are ONE invariant — `handle_map_key` refuses a handle as a key through containers and fields alike, measured, so the constant it returned was sound by luck rather than by construction. **The golden that separates the two answers** is `tests/golden/run/fixedbugs-a-handle-compares-as-an-address.hero`, shipping its own header so it fires on all three platforms rather than where SQLite is installed: twelve lines of output, of which the unrepaired compiler gets **seven wrong** and the repaired one all twelve, and the `.expected` was written from the specification BEFORE the program was run. **Two ceilings moved for it, and only after the comments were cut to what the emitter's own reader needs**: `emit/ctype.hero` 380 -> 395 (measured 388) and `emit/structural.hero` a new entry at 335 against §11's 300 (measured 323), with `DECIDED.len()` 18 -> 19 — the addition the net's own test exists to make somebody type, and it went red exactly as designed. `structural.hero` is not cut instead because its module doc says the seam is the one it was written to refuse: generated `eq` and generated `hash` live where one diff shows both. **Green after**: the compiler's own 646, the net's own 153, and `run` 113, `canonical` 2, `warnings` 173, `lines` 114, `determinism` 142, `layout` 2, `order` 3, `records` 23, each re-run after the last edit rather than after the last interesting one | `spec/heroes-spec.md` § 13 · `selfhost/emit/structural.hero` · `selfhost/emit/ctype.hero` · `selfhost/handles.hero` · `tests/golden/run/fixedbugs-a-handle-compares-as-an-address.hero` · `tests/harness/suite_layout.hero` · `.claude/rules/module-shape.md` | none — the compiler disagreed with the specification, so the compiler had the bug (CLAUDE.md § 12) and no sitting was owed
