- [x] **The null store before a marked `@` call cannot be falsified by any run on this Mac, and the finding corrected a comment this session had already written** | measured 2026-09-07 at M-declared-freer step 5 | **The claim, written into `tests/golden/run/ffi-owned-cell-is-freed.hero` and then run**: *delete the lowering's null store and this case is what goes red*. It is **false**. With `store $cell0 <- const nullptr` deleted and the compiler rebuilt, the case — whose `hero_owned_silent` exists precisely to be a C function that never writes the cell — prints its four lines unchanged, exit 0, at `-O0` and under `-fsanitize=address,undefined`. The generated C declares `const char * h1_cell0;` and assigns it nowhere before the call, so reading it is undefined behaviour and on this machine it held zero. **Measured across three suites**: `run` **0 failed**, `ir` **1** (`ir/owned-cell`), `emission` **3**. **So the instruments that can see the store are the structural ones**, and `tests/golden/ir/owned-cell.hero` — where `UPDATE_GOLDEN=1` is forbidden — is the one that guards it. **This is panel 116's own finding about the NULL guard arriving one level over**: that sitting measured that `fclose(NULL)` survives here and a guardless release exits 0 even under ASan, and concluded a guard no local run can falsify may never be justified by a green local run. The same sentence is now true of the store, for a different reason — an indeterminate C local rather than a tolerant libc — and the three comments that stated it wrongly say what was measured instead | `selfhost/ir/inout.hero` § argument · `tests/golden/ir/owned-cell.hero` · `tests/golden/run/ffi-owned-cell-is-freed.hero` · `docs/panel/116` | a golden whose own comment claims it is the instrument, when it is not, is worse than no comment: it tells the next reader to stop looking
