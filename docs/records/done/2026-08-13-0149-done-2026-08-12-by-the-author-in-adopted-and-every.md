- [x] Covered | reasoning 003 | **DONE 2026-08-12 by the author, in `463aaef` — adopted, and every repair this item listed was made.** Verified rather than assumed when the item came up in `/decide`: `docs/reasoning/` holds `README.md` and notes `000`–`003`; the README gained § A note on the vocabulary of 000–002 mapping `/debrief` → `/learn`+`/decide` and `M5a` → `M-scalars-run` **forward**, without rewriting the dated notes (CLAUDE.md §14); the three append-at-EOF conflicts were resolved in *date order* rather than at EOF; the directory is in `RECORD` and in §14's never-rewritten list, which is what `no_living_file_names_a_numbered_milestone` fired to ask for. And the hand-off commit's seven items were **dropped rather than appended**, each verified as already done on `main` and better — K's superlative now sourced to kdb+'s STAC-M3 results, the memory-model rejection table found to exist, both future-proofing rules landed. Appending seven stale entries would have broken this list's own verify-before-asking rule in one commit. Was: **Is `docs/reasoning/` adopted, and in what shape?** | docs/reasoning/README.md § A note on the vocabulary of 000–002 · git show 463aaef | a note written into a directory that does not exist adopts an artifact class by writing rather than by deciding, and this one did
