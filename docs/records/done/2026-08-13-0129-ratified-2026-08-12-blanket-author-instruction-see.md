- [x] Covered | panel 036 | **RATIFIED 2026-08-12** (blanket author instruction — see the panel file's § Ratification, which lists the four things a blanket yes cannot settle). Was: RATIFICATION OWED — the FFI ladder's eight riders. Ratify or overturn: the group form flattened in the parser · `<header.h>` + `-I<source dir>` · a `_Generic` `_Static_assert` per `extern` · the four edge names spec'd but delivered as Tier 2 over `hero_os.h` · `compile`/`framework` deferred (so `heroes cc` stays unbuilt **and** undecided) · search paths as the real gap · `null` as `ptr`'s only literal with `@` out-parameters · libcurl as a ladder rung (author instruction, same day). The one to read first is the third: without it §4.19's own sentence is **false**, measured | docs/panel/036-the-ffi-ladder.md § The resolution | the milestone's whole shape, and two of the riders exist only because a judge compiled the proposal instead of reading it
