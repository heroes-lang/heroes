- [x] found 2026-08-25 while running step 1's own new test cases | **ANSWERED 2026-08-25 — `a`, all three halves applied in the same session** (author, `/decide`). The net's own tests are worth a CI leg, and the number is what made it easy: this compiles the **5,606-line harness** rather than the 38,021-line compiler, so it is **13.5 s measured** against the 20m35s that made *The compiler's own tests* tags-only — **every push**, not tags. `HEROES_COMPILER` is **named** in the leg rather than searched for, which removes the whole "whichever binary is lying around" class from CI on every platform. The two repairs: `suite_emission.hero:270` now says **3** with the three named in a comment beside it (`judge` · `suite_descriptors.judge` · `orphans`), so the next check added to `sweep` is one edit and an obvious one; and `shell.hero`'s `CANDIDATES` gains **`./heroes` at the head** and loses **`./target/debug/heroes`**, the bootstrap archived at M-bootstrap-archive. **The file had been contradicting itself in place**: its own comment has said *"the path they must not name is the bootstrap's"* since 2026-08-19 with the list underneath naming it, for six days, because nothing read the file. Verified after: `heroes test tests/harness/main.hero` **81 tests, all passed** (from 2 failed), the workflow parses at 11 steps. Was | M-separate-compilation step 1, and the causes are measured rather than guessed | **The harness's own 81 `test` blocks are run by nothing, two are red, and both are the TEST being wrong.** CI runs `heroes test selfhost/main.hero` and `heroes run tests/harness/main.hero` (`ci.yml:289,297`); **neither runs the net's own cases**, so `heroes test tests/harness/main.hero` is a suite with no caller — and one of the cases it holds is called *"every check here can fire — CLAUDE.md §9, a check nobody has seen fail is a decoration"*. Both failures reproduce identically at `a4d7ca4` in a clean worktree, so they pre-date step 1, and **a first hypothesis — stale scratch state — was tested and FALSIFIED** (`rm -rf build/harness-selftest`, still red). The real causes:
  **(1) `suite_emission.hero:270` asserts a count that a later commit made wrong.** `sweep` produces THREE passes for one program — `judge`, `suite_descriptors.judge` (added inside `sweep` at M-bootstrap-archive) and `orphans` — and the self-test still says `green.must().passed == 2`. Nothing is wrong with the code; the number beside it was never updated. A self-test that counts every `keep_pass` in a function breaks whenever a check is added to that function, which is the shape, not the instance.
  **(2) `suite_run.hero:134` trusts whatever binary is lying around.** It calls `shell.some_compiler()`, whose `CANDIDATES` are `./heroes-from-seed` · `./build/heroes-seed` · `./target/debug/heroes`. **`./heroes` is not on that list** — the path every command in CLAUDE.md § Commands and the ROADMAP's *Verify it yourself* block names — while `./target/debug/heroes` **is**, and it is the bootstrap, archived at M-bootstrap-archive. So the self-test reached a `build/heroes-seed` dated **2026-08-19**, six days old, and every program it built died on `_Static_assert` against `HERO_RUNTIME_ABI 15`. **The ABI stamp fired exactly as CLAUDE.md §7 designed it** — this is version skew caught, not a defect — and `HEROES_COMPILER=./heroes heroes test tests/harness/main.hero` is **81 tests, 1 failed** against 2. The four EXTRA failures a clean worktree shows are the same cause with nothing in `build/` at all.
  **The question is what the net's own tests are worth**, because the two fixes are minutes and the CI leg is not free: **(a)** fix both, put `./heroes` at the head of `CANDIDATES`, retire the archived bootstrap path, **and** add `heroes test tests/harness/main.hero` as a CI leg — the only option under which a future red is ever seen; **(b)** the same repairs, no CI leg — the suite stays a thing somebody runs by hand, which is what it has been; **(c)** repairs plus a floor instead of a leg: `suite_records` already reads the tree, and it could assert that every `tests/harness/*.hero` self-test file is named by something that runs it | tests/harness/suite_emission.hero:270 · tests/harness/suite_run.hero:134 · tests/harness/shell.hero:250 (`CANDIDATES`) · .github/workflows/ci.yml:289,297 | the net is what stands behind the compiler, and today nothing stands behind the net
