- [x] **ANSWERED 2026-08-15 (`b`): there is no fourth slot, because there is no fourth input class.** §10 gains the rule rather than the slot, and it is written as a **recording** of what panel 056 adopted rather than as a new architectural decision — which is why it needed no second sitting. The three shapes all answer *what does this capability do to the inputs the tool already has* (a `.hero` file, argv, the environment); a per-project file is a new class, and §10 as written does not admit one. Two things are the point and both are in the amendment: the refusal is **not permanent** — 056's three return conditions are its only amendment path — and **§9's `#~` gap is evidence for the rule, not a second job**, because a new input class would arrive with its diagnostics exempt from the one invariant a regenerator cannot forge, which panel 020's historian already refused for `unsupported`. Was | panel 056, 2026-08-15 | **CLAUDE.md §10's shape table has three slots and a project file is a fourth.** The rule says a capability's shape is mechanical — a **subcommand** if it answers a different question, a **flag** if it changes how one question is answered about the same input, **nothing** if two invocations compose. A per-project file is none of the three: it is a new *input class*, and §10 does not admit one without being amended. Related and smaller: **CLAUDE.md §9's `#~` invariant cannot reach a non-`.hero` file** (`golden.rs:344-372` collects `.hero` only), so any future project file's diagnostics would be the one test class in the repository exempt from §9 — which panel 020's historian refused for `unsupported`. **What is yours**: whether §10 gains a fourth slot, or whether "no fourth input class" becomes the rule that refuses a project file more cleanly than Part 6 could | CLAUDE.md §9, §10 · docs/panel/056 § Findings | B was adopted without this, and it is the argument that would make B permanent rather than provisional
