- [x] **The hard stops get an executor, and the local settings had been pre-approving all three git refusals** | done 2026-09-07 | **Measured while it was being written**: `.claude/settings.local.json` on this machine allow-listed the three git commands CLAUDE.md refuses in capitals, so all three were pre-approved, and there were **no hooks at all**. `.claude/settings.json` also allow-listed five commands for the toolchain archived on 2026-08-19 and did NOT list `./heroes`, so the dead commands passed while the live one asked. **Now**: 12 deny rules, one `ask` on the push so the system asks the author rather than the assistant asking itself, `./heroes` allowed, cargo gone. `.claude/hooks/guard_bash.py` refuses the forbidden forms and was tested against 16 of them and 15 legitimate commands, blocking 16 and passing 15. It splits on the shell's separators, so a forbidden command after a `cd` is caught, and it reads only the arguments AFTER the git verb, because reading the whole line saw a global `-C .` flag as the forbidden dot form and refused a legitimate command. **Both hooks go through `sh` and not `python3`, and that is measured**: a PreToolUse hook exiting 2 BLOCKS the tool call, and `python3` on a file it cannot open exits 2, so naming python directly in settings would turn a missing hook file or an unset `CLAUDE_PROJECT_DIR` into a guard refusing every Bash command in the session; `sh` on a missing file exits 127, which is a warning. `.claude/hooks/fmt_check.py` notices a non-canonical `.hero` file and never rewrites one, because a suite reading the tree owns the tree until it exits. **It earned its place the same afternoon**, refusing `tests/harness/suite_records.hero` five seconds after an edit, which is the 13-minute discovery it exists to prevent | `.claude/settings.json` · `.claude/hooks/run.sh` · `.claude/hooks/guard_bash.py` · `.claude/hooks/fmt_check.py` | a rule performed by nothing is not a rule, and this repository has told that story three times about its own lists
