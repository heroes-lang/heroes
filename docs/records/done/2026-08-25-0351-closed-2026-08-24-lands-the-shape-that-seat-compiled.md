- [x] panel 089 | **CLOSED 2026-08-24: `args_checked() -> [str?]` lands, the shape that seat compiled** (author instruction, *"fai queste"*). Runtime: `const char *hero_args_raw(int64_t)` beside `hero_args_at` — **borrowed, and NULL out of range rather than a panic**, because a caller that must branch cannot branch on a panic; `hero_args_at` keeps its abort, since an out-of-range index there is the program's own mistake (§4.9). Library: `args_checked` over `validated`. `args()` keeps its signature AND its abort for the twenty-odd programs that use it. Spec **+11** (3530 → 3541), the tighter of two measured wordings. **Proven, and the proof is the design**: `prog good.txt $'\xff\xfe' more.txt` prints `arg 1: good.txt` · `arg 2: unusable (not_text)` · `arg 3: more.txt` · `survived`, exit **0** — where the whole program used to die at 134. `[str?]` and not `[str]?` is why argument 3 still ran. Six adjacent shapes attacked: no arguments, an empty argument, all three bad, a valid multi-byte one, a sequence truncated mid-character; clean under `--sanitize` with the leak gate silent. **What is NOT settled**: no sitting ruled on the spec sentence — panel 089 designed the shape and queued it, and this is the author's instruction rather than a panel's verdict, recorded here so it can be overturned as cheaply as it landed | selfhost/library_source.hero · runtime/parts/os.c · docs/panel/089 | argv is the one input a program cannot decline to receive, and it was the last of panel 087's four doors `hero_args_at` converts **eagerly** (`runtime/parts/os.c:62` → `hero_str_from_cstr`), so no `cstr` ever reaches the program and the route that rescues SQLite cannot rescue argv. Reproduced today: `./argv $'\xff\xfe'` → exit **134**. The seat compiled the shape it wants and it is **`args_checked() -> [str?]`, not `[str]?`** — one undecodable argument must not cost a program the other nine, and `heroes build good.hero $'\xff'` should *name argument 2*. It needs one added runtime function (`const char *hero_args_raw(int64_t)`, borrowed, **NULL** out of range rather than a panic, because a caller that must branch cannot branch on a panic) and **zero language change**: `args()` already lives in Heroes, so the branching goes in the library beside `validated`. `args() -> [str]` keeps its signature and its abort for the 20+ programs that use it | docs/panel/089 · runtime/parts/os.c:62 · selfhost/library_source.hero | argv is the one input a program cannot decline to receive, and it is now the only door of the four still open
