- [x] **M-declared-extents** | every prediction the milestone's ten sittings registered, scored or lapsed with its reason | **closed 2026-09-21**, at the milestone's close | Panels 166 to 173, `/step` § Close: *score every prediction whose milestone this is, and lapse the ones you cannot* | 166-173

    **Origin:** the close checklist, and panel 046 R2's rule: a prediction is
    scored, or it is marked `lapsed` in the row that spent it and the clause it
    bought goes back to be re-argued — **never renewed with a new milestone
    name**.

    **Why so many lapse, and it is not an excuse.** This milestone changed
    direction twice: the author flipped a default four sittings had priced
    against, and the last sitting's answer was a route none of the earlier ones
    had listed. A prediction conditioned on a route nobody took cannot be
    scored, and saying so is the honest reading; what it costs is that those
    seats bought their clauses with a bet nobody can settle, which is the price
    of predicting the resolution rather than the world.

    ## Panel 166 — the extent

    | seat | prediction | verdict |
    |---|---|---|
    | compiler-engineer | route C's commit shows zero lines under `selfhost/emit/` and ≥9 modified files under `selfhost/` | **FALSE, as the note beside it expected**: the adopted C-emit form wrote `emit/lend_extent.hero`. The sitting recorded the expectation and the prediction is scored against it |
    | ffi-pragmatist | no real-library `ptr` extern in `examples/` needs a clause, and `examples/sqlite/main.hero`'s emitted C is byte-identical | **CORRECT** at the time and **overtaken**: that file's emitted C moved at step 24, for the flip's `lent` marks and not for the extent |
    | spec-warden | the document lands at 8109 ± 2 real and reds exactly two `.ptr()` sites | **LAPSED**: the document has moved four times since, and the two-site figure was never re-measured against the landed text |
    | llm-ergonomist | rates on a marked write | **LAPSED**: no trial was run at this close, and the harness it names does not exist |
    | historian | clang does not implement `__attribute__((access))` | **CORRECT**, scored by the coordinator 2026-09-19, in the file |

    ## Panel 167 — the field lease

    | seat | prediction | verdict |
    |---|---|---|
    | compiler-engineer | route A as a `.lease()` overload lands with zero lines in `parse/`, `ast.hero`, `print/` | **LAPSED**: route A was never built. Panel 168 struck its allocation half and panel 172 measured the shape it was for |
    | ffi-pragmatist | under the trailing header, `sqlite3_bind_blob` needs no shim in all three modes | **LAPSED** for the same reason, and its counter-prediction — under a leading header the third mode aborts at 134 — was already **CORRECT**, run by panel 168 |
    | spec-warden | routes A, B and C each close **zero of two** reproductions when landed alone | **CORRECT**, scored by panel 168 on the day it was written, and confirmed twice since: panel 172's engineer measured route A of the WORD closing one of four and not the filed one |
    | llm-ergonomist | rates under a `keeps` mark | **LAPSED**: no `keeps` mark exists; the ffi seat of panel 172 measured that it cannot be written truthfully in either header this project binds |
    | historian | within four milestones a foreign-retention mechanism is proposed again, by a seat rather than a panel, because a program needed it | **OPEN, and it has one milestone of its four left to run**. Named here so it is not lost at the next close |

    ## Panel 168 — the allocation

    | seat | prediction | verdict |
    |---|---|---|
    | compiler-engineer | `layout` green with no new `DECIDED` row and four files at their ceilings; counter-prediction: keeping the trailing header breaks one of them | **CORRECT on its first half**: `layout` is 2 passed 0 failed at the close and no row was added for those four. The counter-prediction is **unscored**, because the trailing header was struck and never built |
    | ffi-pragmatist | §4.19 step 3 needs no shim under the leading header for the two modes Heroes can express; the third stays `type_mismatch` until a feature waives `end_lease`; and the third reserved case's buffer comes from the LIBRARY's allocator | **CORRECT on all three**, and the third is now the language's own written position: `spec § 13` says *bytes C owns come from its own allocator, with their disposer*, landed at step 28 |

    ## Panel 169 — the extent stated before it is enforced

    | seat | prediction | verdict |
    |---|---|---|
    | compiler-engineer | R2-widened refuses 0 of 752 `.hero` files and `layout` stays green with no new row | **CORRECT**: `corpus` 55, `check` 135, `run` 136 and `layout` 2 are all green at the close, and the only `DECIDED` row this milestone added is `selfhost/ast.hero 527`, which is the flip's and not R2's |
    | ffi-pragmatist | once 069 emits a C function's address, step 3 needs no shim for a retaining bind, and 066's reordering becomes unwritable | **CORRECT on the first**, measured as `docs/measurements/037`: check 0, run 0, zero sanitiser lines. **FALSE on the second**: the reordering stayed writable and was what panel 168's critic used to widen defect 066 to the lease |
    | spec-warden | R2M lands at 8170 ± 12 real; any draft off its table figure by more than 40 owes a re-price; 068 stays unreproducible through `s.cstr()`; any draft above 10180 is vetoed | **LAPSED on the first** (R2M was superseded by the flip's own sentence); **CORRECT on the fourth** (nothing came near 10180: the dearest draft measured was 8310); **FALSE on the third**: panel 172's critic reproduced 068's class through `s.cstr()` |
    | llm-ergonomist | 8 or more of 20 models answer something other than *until the call returns* | **LAPSED**: no trial of twenty was run, and this close does not fabricate one |
    | historian | a caller-side rule without a named escape has one requested within four milestones, by a program | **OPEN**, three milestones left |
    | completeness critic | a rule stated over the AST needs a fourth widening within two milestones; one over the IR does not | **OPEN**, and the register is now worth watching: the flip's rule is over the AST and has been widened once already, at panel 172's gap 1 |

    ## Panel 170 — the mark refused

    | seat | prediction | verdict |
    |---|---|---|
    | compiler-engineer | with one mark for both facts, `DEFECTS.md` still carries an open give-away defect and `check` on 070's reproducer exits 0; with two marks it exits 1 | **CORRECT on the first half and it decided the milestone**: panel 172's engineer built one mark and measured the filed reproducer at `check` 0. The two-mark half is **unscored**: no second mark was built |
    | ffi-pragmatist | `examples/sqlite/main.hero` needs no shim, no mark and no edit; `examples/ledger` pays at `db/sqlite.hero:342` | **FALSE on the first, CORRECT on the second, and both under the flip rather than under a mark**: `examples/sqlite/main.hero` took three `lent` marks at step 24, and the ledger paid exactly at `bind_text` |
    | spec-warden | a `keeps` inside the swept alternation reports `unread_mark` at exit 1 on all three reproducers, zero defects closed | **CORRECT**, measured by the coordinator at panel 172: `consumes` on a `cstr` is `error[unread_mark]` today, which is why giving it a meaning was a real change and not a widening of silence |
    | llm-ergonomist | rates under either mark | **LAPSED**: no mark shipped |
    | historian | no foreign-retention mark has ever been shipped and withdrawn, and none will be found | **CORRECT**: panel 171's and 172's historians searched eight ecosystems between them and found renames only, every one toward a plainer word |
    | completeness critic | narrowing `one_tag_one_type` at `tag void` closes 072 with zero other programs refused | **CORRECT**, measured at step 18 and green in every suite since |

    ## Panel 171 — the word

    | seat | prediction | verdict |
    |---|---|---|
    | compiler-engineer | commit B's pathspec contains `library_source.hero` and `shell.hero`; `DECIDED` reads `selfhost/ast.hero 527` (±1); `lease_errors.hero` measures 63 ± 10; the `check` form carries 8 ± 1 changed `.expected` | **CORRECT on all four.** The pathspec of `abf9a17e` carries both files; the row reads 527; `lease_errors.hero` is **69** lines by `wc`, inside the 63 ± 10 band; the `check` goldens that moved are 8 |
    | ffi-pragmatist | exactly 8 refusals in `examples/` at the lines it named; after marking and rewriting `bind_text`, the ledger matches `main.expected` and its probes are byte-identical | **CORRECT**: eight sites, and the ledger's output is unchanged with its emitted C differing only at the lease |
    | spec-warden | the landing reads 8214 ± 3 real, vendored ≤ 6172; `examples/` `.lease()` count stays 1 | **FALSE on both, and the sitting's own critic predicted the first**: the landing read **8216** real, three over the band, and the `.lease()` count went from 1 to 3 because `bind_text` and `curl_easy_setopt` took the lease route. Its P2 correction — 15 marks in `selfhost/`, not 13 — is **CORRECT** |
    | llm-ergonomist | `lent` ≥ 9/10 correct on three bindings; ≥ 8/10 on the bare grammar line | **LAPSED**: no ten-trial run was made at this close |
    | historian | no does-not-keep mark found shipped and withdrawn; the contradictory-marks shape is met in Heroes within two milestones by a program | **CORRECT on the first**; the second is **OPEN**, two milestones, and it is already filed as an open item in this milestone's own list |
    | completeness critic | the C-hands-it-back shape is met in a program within two milestones and closed by a caller lease | **OPEN**, two milestones |

    ## Panels 172 and 173

    Scored inside their own files, because both were checkable when written and
    one was already met by the time it was read: panel 172's engineer predicted
    that `lease070.hero` would still be `check` 0 at the close, which is
    **CORRECT** and is why the repair is a report; panel 173's ffi seat
    predicted that `suite_run.hero` would refuse the three reporting goldens,
    which the engineer measured as four red cases the same day, **CORRECT**, and
    which is why the harness gained `!sanitizer:`. Panel 173's engineer
    predicted `runtime` at 6 passed 2 failed without a `suite_runtime.hero`
    change — the landing made the change, so the prediction is **unscored by
    construction and its instruction was followed**, three entries and the floor
    at 49.

    ## What this leaves

    Four predictions are **OPEN with a horizon**: two historians' and the
    critics' two, all of the form *this shape will be met by a program within N
    milestones*. They are named here so the next close reads them rather than
    rediscovering them, which is what panel 036's *"score at M-ffi-ladder
    close"* failed to get and why this section exists at all.
