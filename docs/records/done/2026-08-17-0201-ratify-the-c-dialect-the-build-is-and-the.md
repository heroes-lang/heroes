- [x] found by the CI's first run | **RATIFIED 2026-08-14 as it stands** — `-std=gnu11`, all four conditions, both refused options refused; the provisional default was already green on two platforms when the yes arrived, and panel 047's own falsified prediction stays in the file rather than being tidied away. What remains open is the second half, now panel 048: whether the *spec* should say which C the FFI reaches. Was | **Ratify the C dialect: the build is `-std=gnu11` and the provisional default is already landed.** Three judges, no veto — the ffi-pragmatist holds it on ABI breakage, cross-compiled the real artifacts against real glibc/musl/FreeBSD headers, and reported *"I came to break this and could not"*: identical LLVM IR, 16 struct layouts unchanged, 63/63 `run/` goldens compiling for Linux under `gnu11` against 62/63 under `c11`. The two dialects differ by **one predefined macro**, `__STRICT_ANSI__`, measured three times independently. What the emitter writes is unchanged. Refused: feature-test macros in the generated C (127 hand-edited golden lines where `UPDATE_GOLDEN` is forbidden, the same rule in two translation units, and `_POSIX_C_SOURCE` measurably *hides* five identifiers on Darwin), and passing no `-std=` at all — which is what 3 of 3 sourceable C-emitting compilers do, declined because a byte-identical fixpoint cannot inherit clang's drifting default. **What is yours**: the yes, and one question the lane did not ask — whether the *spec* should say which C the FFI reaches. It says *"anything beyond this document comes from C libraries"*, which after this change is true on both platforms and was true on one; making it say *which* C is a different sitting with an llm-ergonomist in it | docs/panel/047 · crates/heroes-cli/src/commands/toolchain.rs:72 · CLAUDE.md §7 | the language's founding constraint is that everything comes from C, and until today half of what a program binds was invisible on half the platforms
