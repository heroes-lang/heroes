- [x] **M-reflection-verdict** | the witnesses, re-measured at the opening, plus the author's annotation question | **closed 2026-09-11 at panel 131, the milestone's opening sitting** | **The witnesses, re-measured rather than carried**: `examples/json/` is **674** lines (main 64, parse 419, value 191) where the record said 671; hand-written renderers across `examples/` are **31**, confirming the 2026-09-10 correction of the original 20; `selfhost/emit/structural.hero` is **293** code lines against §11's 300 in `suite_layout.hero`'s own unit; the spec is **5662** real tokens with headroom **482**, **422** net of the FFI floor, where the item said 706 before M-anchored-spec spent it; `decorator` is **13** hits over the named document set and still **0** in any sitting; and `suite_layout.hero`'s `DECIDED.len()` is **18**, where panel 117's own scoring condition said 16, so that condition had already expired. **The measurement that reframed the milestone**: `reflection` appears **0 times in design.md's 3660 lines** — the 2026-09-03 scheduling said *"three mentions and no ruling"*, and the count has since grown to 22 across design.md, `DESIGN-LOG.md` and `docs/panel/` while **none of them is in design.md**, because every new mention is the record talking about the absence. So the refusal was citable from no ruling at all. **The author's annotation question (iii) is RULED: refused**, unanimously, as a design.md Part 6 row — and its ground is **not** the one the brief handed the sitting. Panel 114 R7's clause was read as *a user-defined tag set cannot be closed by construction*, and the historian falsified that: the distinguishing property is not user-defined against built-in but whether the tag is a **name subject to resolution or a string**, and Java's annotations are type declarations that resolve while Go's struct tags are strings whose `govet` check, added in the same 2011-06-29 commit that introduced the convention, still after fifteen years does not notice `jsn` for `json`. The refusal therefore rests on **inertness**: in Heroes a user-defined tag is necessarily inert, because macros and templates are refused (CLAUDE.md §6), there is no compile-time execution, closures are refused (2026-09-08) so there is no decorator, run-time reflection is refused in the same sitting so there is no run-time reader, and §10 forbids a second binary so there is no plugin. A general mechanism would have made the six existing contextual words cheaper to write and **impossible to check** — `owned sqlite3_free` would become a string the compiler stores — and the FFI seat compiled the proof that today it is not: `owned sqlite3_notafunction` gives `error[unknown_freer]` with two notes naming the repair. **The falsifier the row carries**: name a reader — one consumer that exists in this tree, reads a tag the user invented, and turns an unlisted one into a compile error. Both compiling seats searched `selfhost/`, `runtime/` and the six parse sites and found none. **The author's own observation is confirmed rather than inherited**: `owned fclose` IS an annotation, and all six contextual words were verified at their cited lines, each guarded by `!in_group ||` or a position test | `docs/panel/131-the-refusals-were-sound-and-their-reasons-were-not.md` · `design.md` Part 6 · `selfhost/parse/members.hero:82`, `tails.hero:211,233`, `group.hero:167-182`, `use_line.hero:130` | what this item did NOT close is question (ii), compile-time derivation, which the sitting deliberately left unruled because its own brief put it wrongly; it is a fresh `SCHEDULED.md` item at M-reflection-verdict step 2
