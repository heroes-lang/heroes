- [x] **panel 148 R5's other half** | `borrows`, the word the completeness rule cannot exist without | **closed 2026-09-14**, M-marked-acquisition step 5, found while building R5 rather than while writing the sitting | **The synthesis adopted half of an answer to an objection and carried the objection back in with it.** R5 takes the ffi seat's axis — *in a group that consumes handle type `T`, a call handing back a `T` must say which it is, and unmarked is an error naming the call* — but what step 4 measured and landed was `acquires <releaser>` **alone**. With only that word, a producer handing back a **borrowed** handle has no legal spelling: `sqlite3_next_stmt` would be refused by the rule, and marking it `acquires` would be a lie the compiler then enforces by making the program give back what was never its own. **That is the llm-ergonomist's own veto reasoning against the inferred option**, reappearing inside the answer to it, and it was found by trying to build the rule rather than by reading the sitting back. **`borrows` costs +43 vendored and +53 real** (7921 to 7974 on `claude-opus-5`, digest `7066ff27485400b0`), a ratio of **1.23** — inside the same 1.20-1.45 band as the row before it and, again, not a factor anyone may apply. It carries **no name**, because there is nothing to name: `acquires` names a releaser because there is an obligation, and `borrows` says there is none. **Proven end to end**: two producers of one handle type in one group, one marked `acquires slot_close` and one `borrows`, with the count balancing at a single `slot_close` — `tests/golden/run/ffi-borrows-owes-nothing.hero`, exit 0 | `spec/heroes-spec.md` § 13 · `selfhost/parse/members.hero` · `tests/golden/run/ffi-borrows-owes-nothing.hero` · `docs/measurements/010` row 75 | 148

- [x] **M-marked-acquisition** | the corpus demonstrates the form, on real libraries and not only in test cases | **closed 2026-09-14**, M-marked-acquisition step 5, on the author's instruction of the same day | **Until this step exactly one shipped binding carried a mark, and it carried it by accident of history** — `examples/curl/main.hero`'s `consumes`, landed at panel 145 for a different reason. A form demonstrated only in `tests/golden/` is a form that lives in a laboratory, and the corpus is what a reader meets. **Both SQLite bindings now carry the full form**: `examples/sqlite/main.hero`, the program a reader meets first, and `examples/ledger/db/sqlite.hero`, the 370-line reference binding that panel 148 was largely decided against. Each gained `acquires <releaser>` on its two producers and `consumes` on its two releasers, with a comment saying what the words are for — and the ledger's says why the mark is on the CALL and not the TYPE, naming `sqlite3_next_stmt` and `sqlite3_db_handle` as the counter-examples it does not declare and what it will say the day it does. **Both run at exit 0**, so the counter balances on the reference binding without a line of program code changing. **And the corpus is what scored the sitting's prediction.** The spec-warden predicted *exactly three `acquires` marks and no fourth* — `sqlite3_open`, `sqlite3_prepare_v2`, `curl_easy_init`. Measured: **three producer FUNCTIONS and five DECLARATIONS**, because §4.19 forces a binding per module and the two SQLite producers are each declared twice, once in each program. **HELD on the claim and wrong on the unit**: it said *marks* and meant *producers*. `borrows` in the corpus: **zero**, exactly as predicted, because no shipped binding declares a borrowed hand-back — which `docs/measurements/031` § 1 already recorded as a fact about this corpus on this date rather than about C | `examples/sqlite/main.hero` · `examples/ledger/db/sqlite.hero` · `examples/curl/main.hero` · `docs/panel/148-the-mark-is-written-and-never-inferred-and-it-names-what-ends-the-life.md` § Predictions | 148
