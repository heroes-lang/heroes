- [x] The record | ratified 2026-08-30, author instruction *"ratifica tutto, tutti i panel, fai adesso il milestone di Windows"* | **The compiler's filesystem calls go through `hero_os.h` and the platform arm stays in C — `panel 097`, provisional, ratification pending** | **The proposal the sitting was convened on was vetoed because it does not compile**, and both seats found that independently with a compiler rather than by reading. Replacing the twelve shell calls with `extern "sys/stat.h"` / `"direct.h"` groups in `selfhost/` has no expressible form: `direct.h` is `error[ffi_missing_header]` on Darwin and Linux and Heroes has no `#if` by design (`selfhost/cli_io.hero:22-27`), while `struct stat`'s `st_mode` is 2 bytes on Darwin and 4 on glibc, so **no width passes both** — measured by cross-compiling against real glibc and mingw-w64 headers, and `error[empty_record]` means `stat` cannot be bound without naming a field. The third route, `access()`, lives in `unistd.h`: the header removed on 2026-08-24 because MSVC does not ship it, which is why `runtime/hero_os.h` exists. `grep -rn '^extern ' selfhost/*.hero` is **three groups, zero POSIX**, and that is a decision six days old. **And the proposal would not have fixed Windows anyway**: `heroes doctor` also runs `uname -s` (`cli_doctor.hero:76`) and redirects `2>/dev/null` (`:29`), neither among the twelve, and `sq()`'s POSIX single quotes reach every clang line — 47 uses across 7 modules, measured, `cmd.exe` hands clang `''/tmp/p097/t.c''`. **What both seats approve instead**: the arm goes behind `hero_os.h` in `runtime/parts/fs.c`, where `runtime/parts/os.c:21-34` already keeps one `#if defined(_WIN32)` for `_setmode`, and `selfhost/` sees one wrapper module with no platform word — DESIGN-LOG:282 applied unchanged. Seven conditions in the sitting, of which four bind hardest: ABI 16 → 17; `rename` cannot stay a bare `stdio.h` extern because the cache's publish is atomic and Windows `rename` is not; `mkdir_one` must ask `is_directory` rather than trust EEXIST, because errno gives the same 17 for a directory and for a **file** sitting where the directory should be; and `sq()` plus `cli_toolchain.hero:129`'s glob ship in the same milestone or that milestone does not claim Windows. **What is open for the author**: whether to take the work at all, and if so at which milestone — it is a runtime addition with an ABI bump, and the CI leg it repairs has been red since 2026-08-26. **The default while it waits**: Windows stays red at `heroes doctor`, Linux and Darwin are green once the `date +%s%N` repair lands, and the README says so instead of promising three platforms | docs/panel/097 · DESIGN-LOG:280, :282 · runtime/hero_os.h · runtime/parts/os.c:21-34 · selfhost/cli_io.hero:22-27 · spec:204-207, :222 · CLAUDE.md §7, §11, §12 | a Windows leg that goes green at `doctor` and red at `build` moves the failure out of the step that exists to detect it and into the cache key §7 calls the only guard against a decoy runtime
