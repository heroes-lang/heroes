- [x] **M-marked-acquisition** | price the two instruments against each other, compiled, before choosing either | **closed 2026-09-14**, M-marked-acquisition step 2, and the price is `docs/measurements/032` | **The pricing chose, and it chose by finding that the two were never alternatives.** **Instrument B, escape refusal, is refused by the same measurement that refused Route A**, and no build was needed to see it: counted from the tree, **exactly two Heroes functions in the whole repository let a handle escape the scope that acquired it** — `opened() -> Db?` and `prepared(db, sql) -> Statement?`, both in the reference SQLite binding, with **7 and 6 call sites**. A blunt escape rule refuses **2 of 2**, which is 100% of the handle-producing wrappers and the 13 call sites that depend on them. Panel 147's warden reached the same wall from the other side — 22 of 23 leaking paths acquire inside those two wrappers — so the two measurements meet: **what an escape rule forbids is exactly where the corpus acquires.** An escape can only be permitted where the obligation **propagates to the caller**, and saying a function hands its caller an obligation is precisely what instrument A's mark is for. **The ladder has one rung and the other is a clause of it.** **Instrument A's surface was BUILT rather than estimated**, in a copy, mirroring `consumes` exactly: **+25 code lines across 7 files**, in the layout suite's own unit, and it compiles, parses and runs. The compiler enumerated the work itself, one build at a time — `error[missing_fields]: FunctionDecl is built with every field, named: … acquires_result: — all of them, always` at **6 constructor sites across 5 files** — which is the property panel 132 relied on and it held. **Two files land over their ceilings and they are the two the sitting named**: `ast.hero` 505 -> 508 and `print/fmt.hero` 1156 -> 1160. **CL-036 was reproduced live inside the prototype**, not cited from a past defect: the mark was added to the AST and the parser and not to the formatter, and `@out: Db acquires` came back from `heroes fmt` as `@out: Db` — the word gone, exit 0, no diagnostic. Six lines closed it. **A prediction scored**: panel 147's compiler-engineer said both routes must touch `ast.hero` (2 lines) and `fmt.hero` (3); measured **+5 and +7**, so it is **HELD on which files and UNDER on the number, by about half**. **And what is NOT priced is said rather than left to be assumed**: the counter — an increment at the acquiring call, a decrement at the `consumes`-marked release, a fourth exit check in `runtime/parts/alloc.c` — is **unbuilt and unrun**, and it is what makes the mark an instrument rather than a word. It is filed as its own item. The prototype was thrown away, as a priced prototype is | `docs/measurements/032-the-two-instruments-priced-and-one-is-not-an-alternative.md` · `selfhost/check/consuming.hero` · `examples/ledger/db/sqlite.hero` · CL-036 · `docs/panel/147-the-obligation-is-created-by-a-call-and-not-by-a-type.md` R4 | 147
