- [x] panel 058 | **CLOSED 2026-08-15 by `docs/panel/059`**, ratified. The clause is in the spec at +25 and states three facts the reader needed and one the item did not ask for: `to_str` **copies**, a null **aborts**, and the guard is `c == nullptr` **first**. Both questions this item put are answered — the spec names `to_str`, and the ergonomist's ranking was **confirmed and sharpened**: the read direction does cost more first-try failures, and the cheap way of documenting it is worse than silence. **One claim in this item was false and is corrected rather than carried**: `print(got)` on a `cstr` does not print an address, it is `bad_operand` at exit 1 — measured by 059's warden. Was | panel 058's blind seat, 2026-08-15 | **The spec documents no `cstr` → `str`, and a reader following it cannot write `getcwd` — this is panel 048's finding with half still unrepaired.** The llm-ergonomist, reading only the spec, was blocked on the same task in **all three** variants, and not on the write rule: it holds C's answer and has no documented way to turn it into a `str`. `print(got)` may print an **address**, since the spec's `==` line says a `cstr` compares as one — a silent wrong output. Verified after the sitting: **`got.to_str()` works and prints the directory.** Panel 048 recorded exactly this class — *"the language is wider than its specification in two places at the FFI boundary"* — and the inbound `cstr` went to panel 053, which repaired `nullptr` and `==` but left `to_str` undocumented. **Two things to decide**: whether the spec names `to_str` on a `cstr` (measure it — the FFI section is where the decision is made, §1.3), and whether the ergonomist's second prediction is accepted as the ranking it implies, that the **read** direction costs more first-try failures than the write direction this panel was convened about | spec/heroes-spec.md § FFI · docs/panel/048, 053, 058 | a spec that is narrower than the language teaches a reader to guess, and guessing at the C boundary is what §1.12 is about
