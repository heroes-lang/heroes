- [x] **M-reflection-verdict** | step 3, land shape E — a field's name becomes a name the checker resolves — with the three things its landing owes and the mutation operator that pays for it | **closed 2026-09-12** | **`Point::x` is the `str` `"x"`, and `Point::witdh` does not compile.** The program that convened panel 131 now writes `{"at":{"x":0,"y":1},"name":"hall","width":3}` with **no key that is a string somebody typed**, and every byte of the output is still the program's: quoting, escapes, order, `sort`. **The cost is NOT the prototype's and the record says why.** The compiler-engineer's branch measured **+59 code lines in three files with no ceiling moved**, and it reached that by **reusing the existing field node** and distinguishing in the checker. The author chose `::` over `.` at the ratification, so the spelling has to be in the TREE for the formatter to re-print it, and that is a node of its own — which the language then charged for correctly: **68 exhaustive `match`es over `ExprKind` went red**, the compiler listed every one, and not one could be forgotten. Four files that carry those arms were already over §11's 300, so four `DECIDED` ceilings move with their reasoning: `ast.hero` 491 -> 505 (measured 500), `check/walk.hero` 1842 -> 1870 (1859), `grammar_expr.hero` 1044 -> 1085 (1075), `ir/flatten.hero` 1123 -> 1150 (1139). **Four diagnostics, three of them new**: `no_such_field` reused for the typo so the fields are listed without opening another file; `not_a_record_name` for `::` on a value; **`dot_on_a_record_name` for the near miss**, `Room.name` where `name` really is a field, which used to be a message about how records are constructed and is now the repair that was actually wanted, carrying a **certain** fix — `.fixed` beside it is what `check --apply` produces and it checks clean, which is CLAUDE.md §8's promise made executable for it; and `record_name_alone` unchanged where no such field exists, **and it has a golden for the first time** (`grep -rln record_name_alone tests/` was empty, a live diagnostic nothing judged). **CL-036's walk was done rather than read**: the formatter round-trips byte-identical, `--dump-ast` prints `::`, and **both things that COLOUR a program** learned the form — `site/src/lib/highlight.ts` (a field called `str` or `record` would otherwise have been painted as a type or a keyword) and `editors/vscode/syntaxes/heroes.tmLanguage.json`. **Spec, measured real and not estimated**: 5662 -> **5716**, **+54**, already net of panel 117's restoration clause coming out; +47 vendored, digest `35968b435ed519d7`, 428 free and **368** net of the FFI floor, ledger row 68. **And the payment landed with it**: `typo-key` is `heroes mutate`'s **fifteenth** operator, the first that typos a string literal, which two seats reached separately after finding the net held fourteen and was blind to the exact mistake this milestone exists to kill. **The honest number: the new compiler is ~1.0% slower** on `check selfhost/main.hero`, 14.05 s to 14.19 s, three runs each on a still machine with a 0.08 s spread inside each set — outside the noise, barely, and reported rather than rounded | `docs/panel/132-two-seats-invented-the-same-thing-and-two-others-invented-the-other.md` · `selfhost/check/access.hero` · `selfhost/grammar_expr.hero` · `selfhost/ir/flatten.hero` · `spec/heroes-spec.md` § 7, § 9, § 12  **And the measurement that justifies the whole milestone was RUN rather than predicted**: one program, two spellings, the same four keys. Written with bare strings, `heroes mutate --operator typo-key` plants four one-character slips and the compiler kills **0 of 4, 0%**. Written `m[Room::width]`, `typo-ident` plants five and the compiler kills **5 of 5, 100%**, because the slip lands on a name the checker resolves. Two seats predicted approximately that and neither had run it. | what it does NOT close is the rot, which is the item below and was never claimed
