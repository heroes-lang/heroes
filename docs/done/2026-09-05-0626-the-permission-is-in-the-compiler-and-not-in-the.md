- [x] **M-c-callbacks** (OPEN) — the two spec sentences R4 owes, and the budget they must fit | panel 111 R4, quoting the spec-warden's and the ergonomist's wordings | **The permission is in the compiler and not in the document.** `spec/heroes-spec.md` § FFI says what a parameter and a field may be and says nothing about a function type, so a reader of the spec alone cannot write `atexit`. R4 adopted the **spec-warden's shorter wording** over the proposal's: it states the **parameter-only** restriction, which the proposal's never mentioned, at **+16** tokens against +26, and a measured **−8** removal of `Neither may be an absolute path.` brings it to **+8 net**. Owed with it, or the restriction is under-specified: **the ergonomist's second sentence**, on the callback's own parameter widths and `(function() -> ())`, without which `atexit` is unwritable from the document alone. **Measured 2026-09-05, before this work**: `heroes measure spec/heroes-spec.md` reads **3799** of a hard 4096, headroom **297**, so +8 net is affordable and the pair must still be re-measured after it is written, never predicted. The spec-warden's own prediction, scored at M-isolated-threads close, is that the total stays **≤ 3830** | spec/heroes-spec.md § FFI · docs/panel/111 R4 · docs/measurements/010-spec-budget-ledger.md | the compiler accepts a form the language document does not describe, which is the one direction CLAUDE.md §12 says is always the compiler's bug

**CLOSED at M-c-callbacks step 3, 2026-09-05, and it lands on the predicted bound with nothing to spare.** One paragraph replaces the removal: *A callback is a **parameter**, never a result; its parameters follow the same rule and `()` is `void`: `atexit(f: (function() -> ()))`.* Two sentences, two jobs, both R4's — the spec-warden's parameter-only restriction, which the proposal's own +26 wording never mentioned, and the ergonomist's, which is the only thing that makes `atexit` writable from the document alone. **Field position is not mentioned and costs zero tokens**, because the spec's field list is closed and already refuses it in writing; the brief's *the spec is silent* was half false and the historian measured it. **Measured, not estimated, across four wordings**: 3799 → **3830**, gross +39 against a measured −8 removal, net +31, headroom **266**. The first draft read 3838 and was cut twice to reach the bound. **The removal was verified by RUNNING rather than taken from the sitting**: `extern "/usr/include/stdio.h"` and `link "/usr/lib/libz.dylib"` are both `error[machine_locked_path]` at exit 1 on the group head, and the message states the whole rule plus three repairs, so `Neither may be an absolute path.` is §1.4 redundancy the compiler pays back loudly — panel 089's shape, and the only condition under which redundancy is repayable. **The payment is a prediction that was already in the record**: panel 111's spec-warden registered *at M-isolated-threads close, `heroes measure` reads ≤ 3830*. This lands at **exactly** 3830, which is the bound and not room under it, so any further callback text falsifies it — and that is written into the ledger row rather than left to be discovered. `tests/harness/suite_spec.hero`'s `SPEC_TOKENS` and `LEDGER_ROWS` are repinned to 3830 and 52 in the same commit, which is what that instrument exists to force
