- [x] **MERGED 2026-08-28 into the soundness-lane brief that answers `2a` and `3a`, and not closed on its merits.** Both defects it names are still live and both were re-measured that day: the `f32` refusal (`clang -Wall -Wextra -Werror` on the equivalent C is exit 0 and prints `0.500000`; the warning comes from `-Wdouble-promotion`, which the project opts into) and the arity-1 `internal error` from `-Wformat-security` on the probe. **They were folded rather than kept separate because all three questions in `DECIDE.md`'s FFI corner are one subject** — an `extern` argument at a **variadic position** — and three sittings on one subject is how a panel gets contradictory conditions. **One correction the merge carries**: the coordinator offered this to the author as *"(i) a wrong message I repair now"* and that was wrong. The message does misstate the header, but the REFUSAL is what blocks a correct program, and removing a refusal is the sitting's call rather than a repair. Was | **panel 094, found on the way; the cheap repair was attempted 2026-08-26 and REVERTED, with the measurement** | **Two live defects at the C boundary, and the second one is not what it looks like.** (1) **A correct binding refused with a message that misstates the header**: `function printf(format: cstr, value: f32) -> i32` is `error[ffi_parameter_type]: value of printf is declared narrower than the header's double` at exit 1 — the header says `...`, not `double`; C's default argument promotion makes `f32` correct, and the C equivalent prints `0.500000`. (2) **A format function bound at arity 1 is exit 2, the compiler blaming itself**: `function printf(format: cstr) -> i32` gives `internal error: compiling the generated C failed` from `-Wformat-security`. **WHAT WAS TRIED AND WHY IT WAS TAKEN BACK**: silencing `-Wformat-security` inside the probe block, which looked obviously right — the probe prints nothing and is never called. Built and measured: it moves the error off the probe and onto **the author's own call site** (`(void)printf(hero_cstr_nonnull(t2));`), where the warning is **correct** and must stay. A format string the author does not control is `%n` writing into memory, which is §1.12's own subject. So the defect is not the refusal, it is the **attribution**: this belongs in CLAUDE.md §7's exit-1 family, alongside the four classes that already recover a name and blame the author's line. That is a diagnostic class, which is a panel path — and the half-repair would have cost 297 re-blessed emissions while closing nothing, so it was reverted rather than shipped. **The default while it waits**: one correct program refused, one refusal blamed on the compiler | selfhost/emit_ffi_narrowed.hero · selfhost/emit_extern_probe.hero:118-125 (the pragma block) · selfhost/cli_flags.hero:32 · docs/panel/094 | §9 says a fixed defect gets a case named after it, and these are two defects nobody had a case for because nobody had written the binding
