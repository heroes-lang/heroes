- [x] **panel 120 RATIFIED in full: the spec buys prose and refuses the signatures, and the −35 removal is refused rather than merely unspent** | sitting of 2026-09-08, opened by author instruction mid-step, four seats; ratified the same day (*"all the decisions as you suggested, all a"*) | **Option (a), the coordinator's recommendation, with the two named alternatives declined**: overturn R5 and take the removal, or overturn the sitting and let the spec stay silent with 70 free. What now stands: `spec:110-111` states *no anonymous functions* among the absences, which is the only place a reader who reads the spec alone can learn that panel 119 refused closures — that refusal lives in design.md, and design.md is not what a reader gets; `spec:187-188` carries `filter`, *keeping what the function accepts* and `find`, *the first it accepts, or an error*; `spec:160` names `not_found` beside `missing_key`. **+30 total, and the reason the whole ballot was thrown away is one sentence from the historian**: `function filter<A>(xs: [A], f: (function(A) -> bool)) -> [A]` is **byte-identical for keep and for reject**, so the four candidate drafts at +86, +69 and +63 bought the loud class, arity and return type, and bought nothing at all for the silent class the sitting was convened about. **THE PART THE RECOMMENDATION FLAGGED AS THE LIKELY OVERTURN IS RATIFIED WITH THE REST**: the mandatory-named-argument bullet at `spec:108-109` measured **−35** in isolation and in place, which would have left the spec smaller than it started, and it was refused on evidence a token count cannot see — the bullet is the conversion function from a listed signature to a written call, `write_file(path: str, text: str)` is a built-in every program uses whose call the spec never shows, and the rule governs the WRITER, so not knowing it makes `relabel_d(a: str, b: str)` compile, run correctly and be permanently worse at every call site, which the `certain` fix entrenches by repairing the call site after the signature exists. **So it is not available to the next sitting that goes shopping for a removal**, and its falsifier stays live and unmet: nobody has named a clause that needs 75 tokens and cannot fit in 40. Spec re-measured at the ratification: **3995** against a green ceiling of **4035**, **40** free | `docs/panel/120-the-signature-cannot-say-which-half-it-keeps.md` § Author's verdict · `docs/measurements/010-spec-budget-ledger.md` row 3995 and its 2026-09-08 append · `spec/heroes-spec.md:108-111`, `:160`, `:187-188` | a refusal of a language feature that a reader of the spec cannot see is a refusal that will be re-proposed by the next reader, and this is the sitting that made panel 119's visible
