- [x] **Defects 018 to 021: four diagnostics that misinformed, all four found by one sitting and repaired in one step** | filed and closed 2026-09-08, M-closures-verdict step 3 | **018 — `<` named two of the ten types it accepts.** `selfhost/check/ops.hero` accepts `.int_ty | .float_ty`, which is eight integer widths and both floats, and handed the literal `` "`i64` or `f64`" `` to `bad_operand` at three sites: the comparison, a `str` under an arithmetic operator, and unary `-`. **The repair** is *"any integer or a float"*, the vocabulary `print`'s own message already used, at all three sites plus that file's own assert. The sibling `mixed_arithmetic` had been saying the true thing all along and fires whenever the two sides DIFFER, which is why the wrong text was reached only by two sides of one non-ordered type — the shape a reader writes first, and the shape panel 119's ergonomist had predicted as the modal failure of its third task in every arm. **019 — the compiler's own fix named a family the language had withdrawn.** `mixed_arithmetic`'s `guess` fix offered `fit_<width>(x)`, and `fit_i32` is `unknown_name`: the family SHIPPED at M-sized-integers step 7 and was removed for breaking panel 017's naming rule (`design.md:923`, *"a family called `fit_` broke exactly that"*). Second site in `check/builtins.hero`'s note about a conversion that cannot fail. **The repair** names `to_f64`, which cannot fail, and `to_<width>`, which gives a `T?`. **020 — the library's `extern` constants were in every program's scope.** `print(HERO_OS_OK)` printed `0`. Panel 033 R5 had walled the library's extern FUNCTIONS and `selfhost/resolve/names.hero` grouped `.constant_decl` with kinds it assumed cannot be extern, with no guard. **The repair splits the arm and asks the declaration in hand** — a constant is extern exactly when it has a header where an ordinary one has a body — **and guards `record_decl` too, with no witness today**, because the library declares no records: the class is closed rather than the reachable case (`.claude/rules/module-shape.md`). **021 — a message showed a reader `#0`.** Six lines with an ordinary generic identity function gave *"`join` takes `[str]` and `str`, found `[#0]`"*, an index into the checker's own tables. `check/render.hero`'s module doc claimed this happens *"only where a caller outside the checker has no function in hand"*, and **that premise had a test pinning `#0`**, which is how it stayed comfortable. **The repair** is the loud honest noun, *"a type parameter"*; threading the callee's letters is the fuller repair and `check/walk.hero` already does it for two other messages, but that file stands at 1697 of a DECIDED 1700, so the thread waits for a ceiling raise. **AND THE REPAIR OF 020 INTRODUCED A WART, WHICH IS SAID RATHER THAN LEFT**: a constant now reaches a message that read *"an extern is never CALLABLE across a module boundary"*, and a constant is read rather than called. It says *"reachable"* now, the word the neighbouring module doc had used all along, and the three lines of `tests/golden/check/extern-across-modules-library.expected` that pinned the old one were **hand-edited**, never regenerated. **Verified against a compiler rebuilt from `selfhost/`** and not the seed binary, which is the trap `docs/ROADMAP.md` names first: 44.89 s, `/usr/bin/time -p`, and all four reproducers re-run against it. Four `tests/golden/check/fixedbugs-*` cases carry symptom, cause and date, with `#~` annotations written by hand — the half a regenerator cannot fake | `tests/golden/check/fixedbugs-a-comparison-names-every-numeric-width.hero` · `fixedbugs-a-guess-fix-names-a-family-that-exists.hero` · `fixedbugs-the-librarys-extern-constants-are-not-in-scope.hero` · `fixedbugs-a-message-never-shows-a-table-index.hero` · `docs/panel/120-the-signature-cannot-say-which-half-it-keeps.md` | errors are a deliverable here, and all four of these told a reader something untrue about the language while exiting 1 — the shape no suite was watching, because a wrong message is not a wrong answer
