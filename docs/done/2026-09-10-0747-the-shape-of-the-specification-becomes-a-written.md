- [x] Author instruction 2026-09-10 | **The shape of the specification becomes a written rule with an executor**: `.claude/rules/spec-shape.md` names the thirteen sections and their numbers, says that a number once assigned never changes and that every rule lives in the section of the operation it governs, and lists what is refused by earlier rulings so it is not proposed again as new. Its executors are two checks, `shape` and `anchors` in `tests/harness/suite_spec.hero`, and its pointer is one row of CLAUDE.md § Where the rest lives, measured before it was written | `.claude/rules/spec-shape.md` · `tests/harness/suite_spec.hero` · `CLAUDE.md` | a form kept in a head lasts one amendment, and CL-069 is the case: the contract's own section numbers survived a rewrite only because a rule said they must
