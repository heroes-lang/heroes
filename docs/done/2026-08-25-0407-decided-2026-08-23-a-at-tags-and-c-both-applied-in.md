- [x] panel 087 | **DECIDED 2026-08-23 — (a) at tags **and** (c), both applied in the same session** (`/decide`). The check exists now: CI's *The seed is what today's source emits* re-emits `selfhost/main.hero` from the seed-built compiler and `cmp`s it against `seed/heroes.c`, on **Linux at tags and `workflow_dispatch`** (15m41s, and Linux is the cheap leg). Two premises were verified rather than assumed before choosing that host: `seed/heroes.c` holds **zero** absolute paths and its `#line` directives carry relative `selfhost/*.hero` paths, so any checkout matches from the repository root, and `word_bits` — the one platform-varying quantity the port derives — reaches **diagnostics only**, never the emitted C (`emit_c_spellings.hero:44`, called at `cli_verbs.hero:85,108` with a literal 64). If it ever goes red on spellings alone, that is the finding and it returns here: the seed would be platform-specific, which nobody has decided. (c) landed as `seed/README.md`'s **third forcing condition** — anything `selfhost/` says that changes what the compiler *does*, with the library's own text as the case conditions 1 and 2 both miss — plus the rule of thumb that needs no instrument: if the diff touches `selfhost/`, the seed is regenerated in the same commit. And the false sentence at `suite_determinism.hero:13` now names its instrument and records that it stood for four days with nothing behind it. Verified after: `yaml` parses (10 steps), `determinism: 114 passed, 0 failed`. Was | panel 087, compiler-engineer, confirmed by a second grep | **`suite_determinism.hero:13-14` names an instrument that does not exist, so the seed can go stale in silence.** The comment reads *"this repository now has one that matters — `seed/heroes.c` is checked by `cmp` against what the seed-built compiler emits."* Two independent greps found **no such check** anywhere in `tests/harness/` or `.github/workflows/`: CI builds *from* the seed (`ci.yml:226`) and never compares it against a fresh emission. Related and worse, from the same seat: `seed/README.md:56-66` names two forcing conditions for regenerating the seed (the ABI moves; `selfhost/` uses a form the seed cannot parse) and **a library-text change is neither** — so a stale seed passes the 3.4 s clang check while CI's harness runs a compiler carrying the *old* library. The decision is whether the check is added at all: the fixpoint leg is **15m41s measured**, which is the same trade as the 90×-slower item above | tests/harness/suite_determinism.hero:13 · seed/README.md:56 · .github/workflows/ci.yml:226 | CLAUDE.md §11's exact shape — a premise that expired while the comment went on reading as correct
