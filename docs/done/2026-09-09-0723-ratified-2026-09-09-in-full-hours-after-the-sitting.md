- [x] **panel 124** | ratified 2026-09-09 in full (`1a 2a 3a 4a`), hours after the sitting closed: every candidate REFUSAL was refused and the answer is a **capability**, so §4.19 gains a fourth case it does not name, bytes Heroes holds and frees which C may read for as long as the program says | `docs/panel/124-the-remedy-was-missing-not-the-refusal.md` § The resolution · § Author's verdict | **The conservative route was on the table and was not taken.** Four rules went out and each came back with a veto, the fourth being the coordinator's own — a lend living to the frame's exit, refused at **1,441,792 bytes against 49,741,824** for a million lends, which LEAKS rather than defers because one slot holds one value. What closed the two shapes no refusal could reach was the ffi seat CORRECTING ITSELF (CL-057): it had written *"A's only remedy"* having tried `strdup`, which answers `cstr`, and not `malloc`, which answers `ptr`. With that one line, both shapes run correct and sanitizer-clean on the shipping compiler, so **024 can close without escape analysis**. It needs a WORD rather than the raw spelling because `atoi(s: ptr)` with `nullptr` is exit 139 with no message where `cstr` gives a named panic, and §1.12 forbids trading one violation for a worse one. It enters on ROBUSTNESS with Principle 0 unmet, said by the warden against its own case. **Ratifying it leaves 024 open on purpose**, so no tag until `M-held-bytes`
