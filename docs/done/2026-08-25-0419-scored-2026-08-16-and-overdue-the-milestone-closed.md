- [x] **SCORED 2026-08-16, and overdue — the milestone closed and nothing opened this item.** All six checkable predictions are in `docs/panel/036` § Predictions scored, measured **at the tag** rather than against today's tree, because three of the numbers have moved since for reasons belonging to later milestones. Result: the ffi-pragmatist **correct twice** (SQLite needs no shim — no `.c` or `.h` exists anywhere under `examples/`; `framework` unexercised, still unbuilt), its TEXT-column consequent **untested** though its premise held (`to_str()` appears 0 times in the spec at the tag); the compiler-engineer **two of three** (`ops.rs` 341→365 under 380 ✓, ABI 10 ✓, `builtins.rs` 193→211 which is +18 against a predicted +<10 ✗) and **the mechanism right, the number high** on the second (the §11 split happened — `syntax/externs.rs` at 184 with `decl.rs` down to 219, combined 403 against a predicted 420); the historian **falsified by a third option**, which is the one worth reading: neither branch of its disjunction fired, because `hero_os.h` declares `_Noreturn void hero_exit(int64_t)` and C already had the keyword. **Its own prediction still open**: the spec-warden's clang-from-Heroes cost, which M-selfhost-port has not reached — `hero_spawn` is not in `hero_os.h` yet, and the `PORT-DEBT` count to score it against is **3**, not the 6 a bare `grep` reports, because three hits are prose. Was | panel 036 | **Score at M-ffi-ladder close**: the ffi-pragmatist predicts SQLite reaches the acceptance test with no shim **but** that reading a TEXT column into a `str` needs one; the compiler-engineer predicts `emit/ops.rs` stays under 380 lines and `HERO_RUNTIME_ABI` stays 10 under the Tier-2 route; the historian predicts `exit(code)` typed as `()` either trips `-Werror=conditional-uninitialized` or forces an unproven `hero_unreachable()` | docs/panel/036 § Predictions to score | three judges predicted three different failure surfaces for the same milestone
