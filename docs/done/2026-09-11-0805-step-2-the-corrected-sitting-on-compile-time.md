- [x] **M-reflection-verdict** | step 2, the corrected sitting on compile-time derivation | **closed 2026-09-11 by panel 132**, full five seats | **Two seats invented the same fifth shape independently and two others invented a different one**, which is what the brief asked for when it gave the walls and the objections instead of the drafts. The llm-ergonomist, holding only `spec/heroes-spec.md`, wrote `Room::width`; the compiler-engineer, holding the whole compiler, wrote `Point.width`; both mean *a field's name becomes a name the checker resolves rather than a string the program types*, and the second **built it** — it compiles itself and runs at **+59 code lines** across three files with **zero** in `selfhost/emit/`, `runtime/`, the lexer, the parser, ownership and mono, 631 compiler tests and 109 `check` goldens green, `heroes fmt` and double emit byte-identical, `heroes check selfhost/main.hero` 14.41 s to **13.90 s** on a still machine, `DECIDED.len()` still 18, and **sugar rather than core** by design.md §1.7's own test because `ir/flatten.hero` erases it to an interned literal. Separately the ffi-pragmatist found that a record of handlers cannot work — **a record cannot be generic**, so its accumulator is fixed and every byte must buffer in a Heroes string before reaching C, which is §1.11's level-3 glue inverted — moved the handlers into parameters and compiled a walk driving both a Heroes string and a live `FILE *`, with field-name literals going **18 to 0**; and the historian, reading Ada's `T'Write` (ISO 1995) and `T'Put_Image` (ISO 2022), wrote that the precedent transfers *"only if the per-scalar writers are passed as ordinary function arguments"*. **The negative claim the brief asked for is FALSE**: Ada shipped compiler-derived per-component serialisation with no macro, no compile-time execution, no trait and no separate generator, twice, both in the ISO standard, the mechanism being an attribute — a compiler-known per-type slot resolved statically by name. **Three shapes refused, the briefed one on four vetoes with four different compiled grounds**, the deepest being the generic-record one above. **Two withdrawals by the historian of its own panel 131 claims**: Erlang's `record_info` does not transfer, because the path from names to values runs through the untyped tuple representation into a heterogeneous list; and its quadratic-cost warning does not reach a flat per-field emission, because every sourced report names `Generic` and `Rep`, the type-level representation, which is the candidate this sitting also refused. **What the sitting does not close and says so**: the rot — add a field and a marshaller silently never mentions it — which only the enumerate-fields shape closes, and that is scheduled rather than adopted because what was compiled is a hand-written simulation of what the compiler would generate and not the generation, **which is the distinction that cost panel 117 four days earlier** | `docs/panel/132-two-seats-invented-the-same-thing-and-two-others-invented-the-other.md` | the sitting also scored a panel 131 prediction TRUE — `examples/sqlite/main.hero` declares 0 records and 0 `partial`, so the `partial`-abort rule costs it zero diff
