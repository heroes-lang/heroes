- [x] **M-corpus-depth: panel 106's adopted resolution is BUILT — slot sharing does not land, and the exit sweep's load temporary is gone** | `docs/panel/106-the-frame-is-the-sweeps-own-temporaries.md`, soundness lane 2026-09-03, **both seats vetoed the proposal** | **The sitting refused what it was convened for and adopted what its own measurements found, and this entry is the IMPLEMENTATION of that resolution — not its ratification, which is still open in `docs/work/DECIDE.md` naming `panel 106`.** The distinction cost an hour on the day it was written: this entry first carried the ratification item's own title, `records/verdicts` went red within the minute, and the check was right — a queue entry that stands in for the author is not closed by doing the work it describes. The full argument, with the ffi-pragmatist's two compiled failures and the compiler-engineer's 0.05%, is on the panel's own page | docs/panel/106 · docs/measurements/015-the-sweeps-own-temporaries.md | the recursion ceiling of every program in this language was set by an implementation detail nobody chose

  **BUILT 2026-09-04 (M-corpus-depth), and the numbers are measurement 015.** `selfhost/ir.hero` gains `decref_slot` — an op carrying a SLOT and not a Place, so it cannot name `p.x` and release half a value; `ir/own.hero` mints one per swept slot instead of a load plus a decref; `emit/inst.hero` emits `release(&slot)` through a `reference_named` that `reference` now also goes through, so the two doors to a refcount call cannot learn a type apart; `ir/uses.hero::slots_of` reports it, which is the one line that keeps a swept slot counted as read. **Measured, before → after**: the interpreter's frames **639,792 → 294,128 bytes (−54.0%)**, its worst function **112,704 → 17,296**, its nesting ceiling **191 → 314** at `-O0`, **104 → 166** under `--sanitize`, **349 → 585** at `-O2`; the compiler's own frames **6,563,376 → 4,227,600 (−35.6%)**; the seed **846,804 → 719,955 lines (−15.0%)**; the emitted interpreter **69,052 → 54,610 lines**. **The fixpoint holds byte for byte** — the repaired compiler emitted the seed, clang built a compiler from it, that compiler re-emitted the seed and `cmp` is silent. 560 own tests, 97 harness tests, 135 of 189 emission traces re-blessed. **Twelve `tests/golden/ir/` and three `tests/golden/emit/` cases were updated BY HAND**, because §9 forbids `UPDATE_GOLDEN` in exactly those directories: every changed hunk was classified first and every one is the same pair — a load into a temporary and a decref of it, replaced by one release naming the slot. Three `suite_layout` ceilings raised with their reason and the pinned count 16 → 17. **What is NOT done and is filed**: the store's `old` temporary, 9.1% more, which genuinely needs a temporary. **And the milestone's 500-deep requirement is NOT met** — `--sanitize` tops out at 166, so the interpreter's case moves 60 → 83 and its evaluator's guard 24 → 38, both re-derived from the measurement rather than chosen. One coordinator prediction is scored **FALSE** on its own page for the useful reason: it said the case would at least triple, taking the `-O0` ceiling and stating it about a case governed by the tightest configuration.
