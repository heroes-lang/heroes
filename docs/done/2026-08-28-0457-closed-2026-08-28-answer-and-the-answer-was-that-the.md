- [x] **CLOSED 2026-08-28, answer `5a` — and the answer was that the PARSER was wrong, which `docs/defects/005` records.** The item asked which of the two artifacts was wrong and that turned out to be the whole question. Measured: `heroes parse --dump-ast` built `return (match … + "x")` and the program **PRINTED `bx` at exit 0** — a continuation at bracket depth zero, which panel 007's Amendment B (*"inside `(` `[` `{`"*) and `design.md:1794` (*"a long expression is broken inside parentheses or not at all"*) both forbid in almost the same words. So `heroes fmt` had been right to refuse for a week: **there is no canonical spelling of a form the language does not have**, and CLAUDE.md §12 settles the rest — spec beats compiler. **The repair is one predicate and one early return**: `grammar_expr.hero`'s `ends_the_expression` stops `binary()` climbing past an operand that carries a block, because parsing that block consumed the dedent that closed it, so the next token only LOOKS adjacent. Enumerated with no catch-all (panel 060's lesson). `out + match c` is untouched for a structural reason rather than a lucky one: there the control form is the RIGHT operand, which is the only side its block can be on, and `print_fmt.hero`'s `binary_control_tail` says the same thing from the printer's side — the two now agree by construction. **No new diagnostic class and so no panel**: it reuses `expected_expression`, the one the existing golden `depth-zero-continuation.hero` already asserts for the indented half of the same rule. `fmt` on that file is now **exit 1** with the diagnostic on the author's line instead of exit 2 with the compiler accusing itself. Was | **One shape `heroes fmt` still cannot spell, now loud instead of destructive** | **Measured 2026-08-26**: a control form as the **LEFT** operand with the expression continuing after its block — `return match c` / arms / `+ "x"` — is **legal Heroes** (`heroes check` exits 0) and the printer cannot produce it. Before docs/defects/003's output guard, `fmt` wrote the header alone at exit 0 and destroyed the file; now it refuses at exit 2 naming itself a compiler bug, with a test that fires on exactly that shape (`cli_syntax_cmds.hero`). **The open question is which end to repair**: teach `broken()` to print a control form that is not at the right edge, or ask whether the PARSER should accept `+ "x"` at bracket depth zero on a new line at all — panel 007's ruling is *"continuation inside brackets only"* (Amendment B), and this reads like a depth-zero continuation the grammar was never meant to allow. If the parser is the bug, the printer owes nothing | selfhost/print_fmt.hero (broken) · docs/panel/007 § Amendment B · docs/defects/003 § The class | one of these two artifacts is wrong about the same program and the guard is currently paying for both
