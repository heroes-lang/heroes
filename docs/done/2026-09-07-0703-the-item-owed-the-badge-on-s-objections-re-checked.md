- [x] **What was left of the site refresh, done by rewriting the site rather than by touching up its numbers** | closed 2026-09-07 | The item owed the badge on `index.html`, `why.html`'s objections re-checked, and every remaining number re-run. All three are done, inside a rebuild the author asked for the same day: the home is 758 words where it was 1,979, `why.html` 1,634 where it was 3,066 with six objections instead of ten, the build log 1,517 where it was 4,846, and every number on every rewritten page was re-measured or replaced by a threshold. **Four numbers were wrong rather than stale**: the fixpoint's hash on the self-hosted page (`c7c0b384…` against a measured `63a786ae…`), the specification at 237 lines against 248, the docs landing saying *twelve* chapters in one paragraph and *Thirteen* in another over a list of thirteen, and the home promising that `heroes run` works *"from any directory"*, which is false: it looks for the runtime in `./runtime` relative to the working directory. **The rule that came out of it** is `site/CLAUDE.md` § A number on the page is generated, or it is a threshold (author instruction 2026-09-07) | `site/src/html/` · `site/CLAUDE.md` | a page whose evidence was a hash had carried the wrong hash for months, and the site's whole mechanism is that a reader can check it
