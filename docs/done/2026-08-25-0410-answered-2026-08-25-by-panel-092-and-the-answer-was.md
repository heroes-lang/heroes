- [x] panel 091 | **ANSWERED 2026-08-25 by panel 092, and the answer was option (a) — now, as its own step.** The item asked *when* to close the parameter hole and offered (a) now, (b) at M-package-manager, (c) a panel first. It got (c) **and** (a) in one day: panel 092 sat, and the repair landed as M-separate-compilation step 3. **The item's own framing was corrected by the sitting**: the check was never missing — `-Werror=shorten-64-to-32` and `-Werror=sign-conversion` have shipped all along — and what the measurement had actually found was a `_FORTIFY_SOURCE` macro intercepting the probe on `memset`, `memcpy` and `strcpy`. Three lines fixed it and it found `examples/curl:44` on the way. **What is left of this item is smaller and is queued**: a **wider** by-value integer is silent under `-Weverything`, and a pointer's target cannot be separated from the benign `void **` case until Heroes has a typed pointer. Was | panel 091, found by the llm-ergonomist from the spec alone and measured by the coordinator | **`spec:204` commands a rule the compiler does not check, and the gap is a §1.12 memory-safety hole rather than a tidiness one.** The spec says *"a **parameter** and a **field** are declared at the header's own width and sign — `i32` where C says int"*. Measured three ways: `pow(x: f32, y: f64)` against `double pow(double, double)` is **exit 0** and prints `1024.0`; `memset(..., n: i32)` against `size_t` is **exit 0**; only a **kind** mismatch — `memset(s: i64, ...)` against `void *` — is exit 1. So the check sees kind, never width or sign. **The ffi-pragmatist compiled what that costs**: `getline` with its `size_t` out-parameter declared `i32`, executed, wrote `canary 0x54534554 -> 0x00000000` — four bytes of an adjacent object destroyed, **ASan and UBSan silent, exit 0**. Today the assert and probe emit a **warning** on the author's line (verified: 4 warnings, **exit 0**), which is protection but soft, and §7's own rule says a failure the author's `extern` caused is exit 1 on the `.hero` line. §12 says the compiler has the bug; §1.12 says robustness outranks everything else in CLAUDE.md. **The question for you is when, not whether**: (a) now, as its own step in this milestone; (b) at M-package-manager, where distributable bindings make it the difference between a binding that is verified and one that is merely declared; (c) a panel first, because turning today's warning into exit 1 is a diagnostic class | spec:204 · selfhost/emit_extern_probe.hero · docs/panel/091 § What this sitting found that it was not convened for | the FFI is the one place a Heroes program can reach an address nobody checked, and the author's instruction is that it be complete and bug-proof
