- [x] **CLOSED 2026-08-28, answer `4a`** — `print_fmt.hero`'s `field_line` and `case_line` now preserve the blank line below a comment, which §4.1 makes the difference between an ordinary remark and the next member's DOCUMENTATION. **The adjacent shape was fixed with it rather than after it** (CLAUDE.md §1's fourth rule): a `variant` case takes a doc comment too, so `case_line` got the same guard in the same commit — a `record` field and a `variant` case are the same witness twice. Verified: the provoking file formats to itself at exit 0, three shapes asserted in `print_fmt.hero` (remark with the blank, documentation without it, and the variant case), and **0 of 371 repository files changed**, so the repair added no churn. **AND IT COST THE GUARD ITS FIRING TEST, which is the part worth keeping**: the tree check's §9 case used this live defect to fire, so repairing the defect stopped the test testing. Both guard tests were rewritten as PREDICATE tests in the same commit — `refuse_output_the_formatter_broke` called with hand-built pairs, in both directions, including one that must stay quiet (`0xFF` -> `0xff`, a text change that is not a meaning change). A firing test that depends on a live defect stops firing the day the defect is repaired, which is the day it stops being a test. Was | **`heroes fmt` turns a remark into documentation inside a data body — live, and the guard admits it cannot see it** | **Reproduced twice 2026-08-26** (panel 095's compiler-engineer, then the coordinator with `--dump-ast`). A `record` with a comment between two fields and a blank line below the comment: `fmt` **deletes the blank**, and the tree changes from `field y: i64` to `field y: i64` + `doc # ...`. design.md §4.1 makes that blank line the difference between a remark and documentation, §4.1's last bullet has the compiler reuse doc comments in `???` output, error messages and `outline`, and the blank is implemented at `selfhost/cursor.hero:194-220` (`take_docs`, `above != wanted` ends the run). **Fourth instance of the parses-but-means-something-else class** — panel 014, panels 060/061, `docs/defects/003`, this — and the one `selfhost/cli_syntax_cmds.hero:71-75` states in its own comment that the new output guard **cannot** catch. `assert_canonical` would catch it: the dumps differ. **Two things follow**: this is `assert_canonical`'s first named `tests/golden/fixedbugs/` case whenever it is restored, and panel 095 **rejects rule 4b for data bodies** on this evidence, because *"nothing between the comment and the code"* makes an ordinary remark inside a `record`, `variant` or `extern` body unwritable | selfhost/cursor.hero:194-220 · selfhost/print_fmt.hero (comments_before) · design.md §4.1 · docs/panel/095 § The two findings | a formatter that silently changes what a comment MEANS is the worst shape this class takes, and it is the fourth time
