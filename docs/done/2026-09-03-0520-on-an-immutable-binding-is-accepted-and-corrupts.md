- [x] **`M-robustness-guards`, step 1** (chain row 32, opened 2026-09-03; ahead of everything else because CLAUDE.md §12 ranks it there) | `/decide` 2026-09-03, author instruction *"ratifica tutto"* + *"scegli le soluzioni più robuste e complete"*; panel 102's compiler-engineer found it | **`@` on an immutable binding is accepted and corrupts memory: one rule, eight witnesses.** `write_root` (`selfhost/resolve/walk.hero:231`) has one caller, `place()` at `:206`, for a write STATEMENT; a call argument's `@` never reaches it, and `marker_mismatch` (`selfhost/check/walk.hero:1491`) asks only whether the marker matches the parameter's. Measured on the seed of 2026-09-02 17:52: a by-value parameter, a `=` local, a field, an index, a nested record, a `for` variable, a `match` payload and a generic `@x: T` ALL pass `heroes check`; field 134, index **138**, nested 134 (memory corruption), local/`for`/`match` print `2` at exit 0 (an immutable mutated in silence). **Two more shapes found 2026-09-03 while opening the milestone**: `add(@make(), "x")` and `add(@Bag(items: ["q"]), "y")` — `@` on something that is not a place — pass `heroes check` at exit 0; and the LEGAL `add(@bs[0], "y")` on a cell `bs: [Bag] @ …` is **exit 138**, ASan `BUS in hero_array_incref`, because the emitted call reads `add(&h0_bs, …)` — the address of the whole array where a `Bag *` is expected, the index dropped (`@b.items` on a cell works and prints 2). The corpus holds **150** `@x.field` arguments and **zero** `@x[i]`, which is why nobody saw it; the strict `-Werror=incompatible-pointer-types` of step 3 would have refused that emission as exit 2. So step 1 is three repairs: the mutability rule at the root, the non-place argument refused, and the index path lowered as read-element / call / write-back through the indexed store the language already has. Deliver: all three, the nine programs as golden cases (eight refused, `@b.items` and `@bs[0]` on a cell running and printing the mutated value), one `fixedbugs-` case with symptom/cause/date, the three suites green, the seed regenerated | spec:88 · selfhost/resolve/walk.hero:206,:231 · selfhost/check/walk.hero:1491 · docs/work/DONE.md (the item, with the eight shapes and their exit codes) | §1.12 says a Heroes program must not corrupt memory, and today fourteen lines the spec forbids do

  **CLOSED 2026-09-03, M-robustness-guards step 1.** Three repairs, one rule, and two shapes more than the item knew. (1) `resolve/writes.hero`'s `inout_root` holds the root of every `@` argument to `write_root`'s rule — the same `not_mutable` the left of `@` gets, each binding kind with its own wording — so the eight measured shapes are refused at the argument's span; `write_root` left `resolve/walk.hero` with it (a leaf of the ring, and the second caller took the file past 300). (2) `grammar_expr.hero` refuses `@` before something that is not a place with the `not_a_place` it already gives `f(x) @ 1`, beside the same `is_place`. (3) `ir/inout.hero`, 158 lines, its own module because `ir/lower.hero` sat at its decided ceiling: an `@` argument whose place runs through an index is read into a synthetic slot before the call, the callee copies out into the slot, and `emit_call` stores it back through the indexed place after the instruction — the index values `place` lowered are reused, so `xs[f()]` calls `f` once; `ir/verify.hero` refuses an inout place that still carries an index step, and `emit/aggregate.hero`'s dead arm now spells an undeclared C identifier instead of silently stopping. **Measured on the three platforms before this commit**: ten `check/` goldens (eight shapes, the non-place, and `fixedbugs-inout-on-an-immutable-corrupted-the-heap`) byte-identical on the Mac, in the Linux image (clang 22) and on the Windows box; `run/inout-through-paths-of-a-cell` — field of a cell, index of a cell, index then field, two write-backs in argument order through two cells, `pick` printing once — identical on all three and clean under `--sanitize` on the Mac and on Linux; the seed compiler on that same program: exit 138 on the Mac, 127 on Linux, 2 on Windows. The compiler's own tests 536 → **543**. Two decided ceilings moved with dated reasons (`ir/lower.hero` 1483 → 1511, `grammar_expr.hero` 1000 → 1002); `resolve/walk.hero` 270 → 241 and `resolve/writes.hero` 91 by the split. Journal 031 § What surprised carries the emitter comment that said the crash could not happen.
