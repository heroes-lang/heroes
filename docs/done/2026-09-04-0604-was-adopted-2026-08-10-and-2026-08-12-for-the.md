- [x] **The reasoning notes are retired into the record: seven notes, thirty-two hand-offs, thirty-one of them already landed** | author instruction 2026-09-04, translated for the record (CLAUDE.md §11): *"Now add the reasoning directory to the plan too: I want to remove it and turn its content into DONE, or DECIDE plus a panel where needed, or even into ROADMAP milestones where needed. All this work goes in the direction of simplifying the docs directory"* | **`docs/reasoning/` was adopted 2026-08-10 and 2026-08-12** (`DESIGN-LOG.md:142`, `:246`) for the remainder no other artifact held: *"the path between the artifacts, and what the session left open"*. Seven notes and a README, 1455 lines. **What the audit found, note by note, before anything was deleted**: 000 → 2 hand-offs, both landed; 001 → 4, all four in `design.md`; 002 → 3 landed and **one half with no home**, now an item at M-isolated-threads; 003 → 5, all closed by panel 039 and two repairs the same day (the note's own later append re-stated one of them rather than adding a sixth, which is why the total below is 32 and not 33); 004 → 5, all closed by panel 056 and its ratification; 005 → 7, all in `SCHEDULED.md` and the ROADMAP; 006 → 6 hand-offs landed and **sixteen candidates**, of which ten got a row or a house and five a recorded refusal, leaving **out-of-memory** the only one with neither. **Where the parts went, and it is the same table the README carried**: a concept to `docs/glossary/`, a question to `docs/learn/LEARN.md`, a decision owed to `DECIDE.md`, work with a milestone to `SCHEDULED.md`, a language change to `/panel`, a decision taken to `DESIGN-LOG.md`, what a session settled to this file. The path between them is the git history, which is where it always was. **Two blocks did not go to the record, because two living documents cited them by line**: the seven-capability table (`003:82-91`) is now `docs/panel/039` § Appended 2026-09-04, and the nine-job `Cargo.toml` inventory (`004:29-39`) is now `docs/panel/056` § Appended 2026-09-04. That second move is what keeps 2026-08-15's ruling true — the note was legalised *because* it held what the panel file did not, so deleting it without moving that content would have reversed the decision (this file, `:456`) | DESIGN-LOG.md:142, :246, :301, :308, :537, :539 · docs/panel/039 § Appended · docs/panel/056 § Appended · CLAUDE.md §14 | `docs/` goes from ten directories to nine, and a session's leftovers now land in the four lists a skill is obliged to open rather than in a fifth nobody was
