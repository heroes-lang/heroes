- [x] panel 087 | **REPAIRED 2026-08-24 — and it is NOT free: the warden's `~±0` was wrong and the measurement says +8.** Seven wordings were measured before one was chosen; they ranged **+8 to +18**, and the cheapest is what landed: *"comments may contain any UTF-8, strings any but a raw carriage return"*. **The sentence was false about half of what it names, and the measurement is what showed which half**: a raw CR is `error[raw_carriage_return]` in a **string** and in a **character literal**, and **accepted in a comment** — so a repair that forbade it everywhere would have replaced a false sentence with a differently false one, stricter than the language, which is panel 081's own §12 target. Measured alongside: a raw TAB, a 3-byte star and a 4-byte character are all accepted in both. The character-literal case is deliberately left unstated: the compiler is loud at the moment of that mistake, with the rule in the message, and §1.4 spends redundancy only where a wrong guess is silent. Was | panel 087, spec-warden, found while hunting a removal | **`spec:14` is false and the repair is free.** *"Syntax is ASCII-only; strings and comments may contain any UTF-8."* U+000D is valid UTF-8, and a raw carriage return inside a string is `error[raw_carriage_return]` at exit **1** — `tests/golden/check/raw-carriage-return.expected`, confirmed by the coordinator. §12 says the compiler has the bug, but here the *diagnostic* is deliberate (it exists because a CR is invisible on screen and the next reader deletes it by retyping the line), so the document is what has to move. The warden priced the honest rewording at **~±0 tokens**, which is why it funds nothing and is its own item | spec:14 · tests/golden/check/raw-carriage-return.hero | one of the two artifacts is wrong and the cheap one to fix is the sentence
