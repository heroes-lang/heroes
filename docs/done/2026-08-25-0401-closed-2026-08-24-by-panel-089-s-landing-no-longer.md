- [x] panel 087 | **CLOSED 2026-08-24 by panel 089's landing.** `spec:229` no longer sends anybody through a null test: the sentence *"test `c == nullptr` first, because converting one aborts"* is **deleted** (−15 tokens, the named removal that funded the sitting), and the paragraph now reads *"`c.validated()` copies one back as a `str?`"*. The route it names exists and was run on ten shapes — ill-formed bytes, a null pointer, a good string, empty, 70,000 bytes, a UTF-8 sequence truncated mid-character, UFCS, `?`, `.default()` and a record field — all exit 0, clean under ASan/UBSan and the leak gate. The spec-warden's veto of the obvious extension never had to be tested: the sitting did not enumerate two paths, it **removed** the sentence and named a form that has one. Was | panel 087, found independently by two seats | **`spec:229` sends a binding author through a null test and then kills them anyway.** *"test `c == nullptr` first, because converting one aborts"* names `nullptr` as **the** reason. Measured against real `sqlite3.h` — §4.19's own ladder step 3, `:memory:`, `insert … cast(x'fffe' as text)`, `sqlite3_column_text`, **null-checked exactly as instructed**, then `.to_str()` → `panic: hero_str_from_bytes: not well-formed UTF-8`, exit **134**. The emitted C shows the path: `t37 = (const char *)sqlite3_column_text(...)` → `t44 = hero_str_from_cstr(t43)`. The llm-ergonomist reached the same sentence from the document alone. **The wording is the decision, not the fact**: the spec-warden refused the obvious extension in advance — turning a true floor into a two-path enumeration is *"row 3405's exact anti-pattern"*, +11 tokens, and panel 059 ratified the floor. So the choice is a floor that covers both reasons, or a pointer to a fallible form that does not exist yet | docs/panel/087 § The four doors · spec:229 · runtime/parts/str.c:266 | `.to_str()` is the door **every** binding uses, and §1.11 says everything comes from C
