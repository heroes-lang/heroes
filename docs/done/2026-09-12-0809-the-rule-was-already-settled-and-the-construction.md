- [x] **027 — a header field of type `char *` makes a binding unbindable, and the refusal arrives as an internal error** | **closed 2026-09-12** | **The rule was already settled and the construction did not follow it, which is the whole finding.** `cstr` emits `const char *` and a header's member is often plain `char *`; `selfhost/emit/extern_field.hero` names `struct passwd`'s `pw_name`, `pw_dir` and `pw_shell` in its own comment, because **panel 089 widened the per-field `_Static_assert` to accept both spellings** and wrote down why: *"const on the pointee restricts WRITES, and the language offers none through a `cstr`"*. So the declaration was legal and the assertion passed — and then the compound literal `(struct passwd){.pw_name = t2}` was refused by this project's own `-Werror=incompatible-pointer-types-discards-qualifiers`, at **exit 2**, as `internal error: compiling the generated C failed`. A binding the compiler had deliberately admitted could be declared and not built, and the message accused the compiler's internals and named no repair. **The repair** is `selfhost/emit/aggregate.hero::written_as`: a `cstr` value is cast to `__typeof__` of the member, for **foreign records only and `cstr` fields only** — the same form the assertion above it already uses to ask about that member, so the value is written as exactly the type the header gives and nothing new is asserted about what that type is. Everywhere else the literal's own type checking catches a wrong argument and it keeps it. **Proved both ways, run 2026-09-12**: the old compiler exits **2** on `tests/golden/fixedbugs/a-header-field-that-is-not-const.hero`, the new one exits **0** and the program prints `true` then `false`. **And the case says what it does NOT guard**: no suite clangs a `fixedbugs/` case, that directory getting `annotations`, `canonical` and `emission` while `--emit-c` never calls clang — so the file guards the emission and the build is proved by the run recorded here, which is stated in the case rather than implied, because a case that looked like a compile guard and was not would be worse than none | `selfhost/emit/aggregate.hero` · `selfhost/emit/extern_field.hero` · `tests/golden/fixedbugs/a-header-field-that-is-not-const.hero` | found by the ffi-pragmatist seat's boundary experiment at panel 131, outside every verdict, and reproduced by the coordinator before filing
