- [x] **M-closures-verdict: the count the sitting was owed, measured before it sat** | scheduled by author instruction 2026-09-03 (`DESIGN-LOG.md:539`), closed 2026-09-08 at the milestone's opening | **The instrument is the compiler and the first one was wrong.** `.func_ref` is the IR op for a function used as a value, so `heroes build <m> --dump-ir | grep funcref` is ground truth — but `--dump-ir` prints only the module named, and 158 of 308 files do not build standalone because `use` starts at the directory of the file you compile, so it speaks for **150** modules. A text census covers the rest, scoped **per file**, which is sound because the language forbids the alternative: `double = 3` under `function double` is `error[shadowed_binding]`. Validated over the 150 where both can speak: **78 sites against 78, zero missed and zero extra**, after one repair — `selfhost/library_source.hero` embeds the library's own source as string literals, so `return ok(text)` inside a `"…"` counted three function values that are text. The first version of the census pooled names across all 308 files and reported 217, most of them locals named `at`, `after`, `env` and `done`. **The answer**: 2338 top-level functions, 340 of one statement, **100** value sites, 33 distinct functions, **21 of 308 files** hold one at all; **66 of the 100 sites are `test` doubles**, and the compiler — 190 modules, 55,361 lines — passes a function as a value at **4 sites in production code**, none of them capturing. Under Part 7 item 1's own wording, helpers that exist ONLY to be passed around, §1.7's subtraction is **21 declarations, 66 lines, every one in an example program**, four of the seven files being programs whose subject IS the higher-order function; in `selfhost/` it is **0 counting every use and 1 counting production uses only**, a correction the spec-warden's seat made against the coordinator's `pieces.hero:147`, which is `is_other`'s declaration and not a call of it. **The demand side, which no note had looked for**: 18 higher-order signatures in 7 files, and the corpus's biggest instance is five functions in one evaluator threading three values by hand. **And the budget**: the spec is 3965 of a hard 4096 with 60 mortgaged, so **71 are free**, the clause that states the closure rules costs **+118** on two independent drafts against design.md's *"~60"*, and the four higher-order signatures the spec never wrote cost **+63** — the last 71 tokens buy one or the other, not both | `docs/measurements/020-four-sites-in-fifty-five-thousand-lines.md` · `docs/panel/119-the-warning-that-does-not-fit.md` · `selfhost/ir/print.hero` · `selfhost/library_source.hero` | a sitting that rules on a form nobody counted rules on a feeling, and the peer session that reached this question first got the compiler right and the file count wrong, in a claim that was in no record to read forward from
