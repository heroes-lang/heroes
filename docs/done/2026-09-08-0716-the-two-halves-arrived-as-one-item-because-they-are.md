- [x] **The spec's two silences about aborting: `.must()`'s is bought and homed, the worked diagnostic is handed to the instrument that can price it** | filed 2026-09-08 as a panel 118 finding by the llm-ergonomist, reported as an appetite rather than a request; split and homed by `/decide` the same day | **The two halves arrived as one item because they are paid from one budget, and they are separated because only one of them can be priced today.** `.must()`'s half is **bought**, and the measurement is worse than the item said: the spec writes `abort` **8 times on 7 lines** (`:71`, `:156`, `:162` twice, `:169`, `:171`, `:186`, `:197`) and defines it **nowhere**, while it says *ends the program* exactly **once** in the whole document — wrapped across `:197-198`, spent on `exit(code: i64)`'s parenthetical, the one operation nobody could misread. **The compiler is currently better documentation than the specification**: `.must()` ends the program appears at three message sites in `selfhost/value_errors.hero` (`:145`, `:165`, `:187`) plus the test that pins the third, and `selfhost/ir/verify.hero:223` opens with *an abort ends the program*, against `spec:3`'s *"This document is the whole language"*. **AND THE OPTION SET WAS WIDER THAN THE BALLOT (CL-057)**: the finding named one route, a clause at `.must()`; the measurement names two, and the second — one definition of *abort* covering all eight sites, the only route that also makes `:162`'s *recursion too deep* and `:71`'s *overflow aborts at every width* legible — **had never been priced by anybody**, so the sitting prices both against the **40** free tokens rather than buying the one that was written down. Homed at **M-interpolation-verdict**, whose agenda already carries a *the spec is silent where the runtime is specific* question, the printed float — because this file's own lesson is that *the next sitting that touches the spec* is a waiting condition and not a home. **The exhibit half is NOT deferred by a date**, which is the mechanism §1.6's record shows failing: it is handed to **M-thesis-harness**, because the claim — a language whose thesis is that every plausible mistake is a compile error shows its reader zero compile errors, so a model cannot predict what it is about to be told — is a falsifiable claim about **metric 2**, which has never run, and Principle 0 admits a form on a measured Part 11 effect. The sitting that buys or refuses it is the one that can read the number, and a refusal then owes a Part 6 row with its falsifier | `docs/work/SCHEDULED.md` § M-interpolation-verdict, § M-thesis-harness · `spec/heroes-spec.md:156`, `:197-198` · `selfhost/value_errors.hero:145` · `docs/measurements/010-spec-budget-ledger.md` row 3995 · `docs/panel/118-the-missing-word-was-a-dead-variable.md` | `.must()` is the repair a model reaches for under pressure, and the document that is the whole language describes it in three words that never say a program stops
