- [x] **Does the spec owe a sentence saying `T??` is refused, and does the refusal owe a golden?** | queued as a conservative parser reading 2026-08-04 (`DESIGN-LOG.md:48`), carried on the panel watch list until 2026-09-04, settled at `docs/panel/110` | **The sentence: NO, on two vetoes. The golden: YES, and it is written.** The spec-warden vetoed it on Principle 0 and on falsity; the compiler-engineer vetoed it under §12. The tabled wording — *"A `T?` is never fallible twice: `T??` is a compile error, `[T?]?` is not"* — is true of the **written** form and false about the language: `m: {str: i64?}` is accepted and `v = m["a"]` then `v.must().must()` runs and prints `1`, `function take(x: i64??)` is `nested_fallible`, `function take(x: i64?)` handed that value is `bad_operand`, and `spec:157` already promises `m[k]` returns `V?` unqualified. The engineer widened it by building it: a **generic** signature names the type — `function wrap<T>(x: T) -> T?` over an `i64?` gives `i64??` and nests without bound — and the producer is the §1.11 built-in `find<A>(xs: [A], f) -> A?` over `[i64?]`, which runs and prints 1. So the sentence would have made the document contradict itself one section away and booked three defects for 29 tokens. The permissive half also describes a form used **0** times in 587 `.hero` files, and the refusing half is already the loudest diagnostic in the tree. **What was owed whatever the answer, and is now paid**: `tests/golden/check/a-fallible-type-is-never-written-fallible-twice.hero`, three `#~ nested_fallible` annotations and the two legal container shapes beside them — the code had existed in exactly one place, `selfhost/parse/type.hero:55`, with one in-module test and **no golden anywhere**, so a refactor could have deleted the refusal in silence | selfhost/parse/type.hero:55 · tests/golden/check/a-fallible-type-is-never-written-fallible-twice.hero · docs/panel/110 · docs/work/SCHEDULED.md at M-check-completeness | a diagnostic class with no spec text and no golden is a rule that exists only in the parser
