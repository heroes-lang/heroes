- [x] panel 062 | **FIXED 2026-08-16 (panel 079), and it was half of a larger defect.** `types/construct.rs::field_of_function_type` matched `Ty::Named` only, while `ir/layout.rs` has carried the `Ty::Case` arm since panel 068 and design.md **§4.2:845** says a variant case **is** a small record — so the checker was the half that had not caught up, and the message was §4.17's forbidden shape: it named a repair that does not exist, and a reader acting on it edits a correct declaration. `a.g(50)` now prints **100**. **The larger half, found by composing two seats' verdicts with a measurement**: `ir/calls.rs` asked the RESOLVER where `types/ufcs.rs` asks the field first, so the two passes disagreed — `s.g(50)` printed **50007** (the free function) while the checker had typed the field's **100**, at exit 0; and with differing result types the checker typed from the field while the lowering called the function, which **clang** caught and the type system did not (*"assigning to `int64_t` from incompatible type `HeroStr`"*, exit 2). Both halves land together, the dot form moves into `ir/ufcs.rs` so they cannot drift again, and the sweep says **0 of 268** corpus files change meaning. Golden: `run/fixedbugs-a-field-wins-the-dot-over-a-free-function.hero`. Was | panel 068 F8's neighbour, measured 2026-08-16 while fixing it | **`a.g(50)` on a variant case's payload is `unknown_function`, while `h.f(21)` on a record field works.** Both hold a function value; both are a dot followed by a name and an argument list. On a record the resolver finds the field; on a case payload it reads `x.g(…)` as UFCS, looks for a top-level `g`, and reports *"`Step.apply` has no field `g`, and no function is named `g`"* — a message that says the field does not exist while the payload declares it. The two-step form works (`call = a.g` then `call(50)`, measured: prints 100), so nothing is blocked and the lowering is shared. **Three shapes**: (a) the resolver descends into the matched case's payload for a method call, making the two consistent; (b) the refusal stands and the message stops saying the field is absent, naming the two-step form instead; (c) it is left alone and this item records why. §4.11's UFCS rule and §4.13's function values meet here and the spec says nothing about the intersection | tests/golden/run/fixedbugs-function-in-a-record-field.hero (the note in its body) · crates/heroes/src/resolve/ · docs/panel/068 F8 | a diagnostic that says a declared field does not exist is the shape §4.17 exists to prevent, whichever way the question is answered
