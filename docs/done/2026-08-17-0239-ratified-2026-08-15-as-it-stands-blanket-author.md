- [x] panel 059 | **RATIFIED 2026-08-15 as it stands** (blanket author instruction, which also delegated the judges' recommendations — so the two things owed with the clause are work rather than questions and land without returning here). A stays refused on the measurement that condemned it; C stays vetoed, and ratifying that **does not rule a fallible conversion wrong** — it rules that `to_str` may not be the name carrying it, so the historian's second-name form survives unconsumed. Was | panel 059, 2026-08-15 | **Ratify the sitting, or overturn it.** Full lane, five judges. The clause lands at **+25 measured**, folded into the FFI sentence already there: *"`s.cstr()` lends a `str` to C to read and `c.to_str()` copies one back — test `c == nullptr` first, because converting one aborts."* **A (+11, name the conversion alone) is refused on a measurement** — it is worse than saying nothing, because spec:58's naming law then asserts that a null converts. **C (fallible `to_str`) took a veto** plus an objection no wording answers: `tigetstr` returns `(char *)-1`, so a `str?` promises what the boundary cannot deliver. Its reconciling form — a **second name**, Swift SE-0405's shipped shape — is recorded rather than struck. **What is yours**: the verdict, and the two things owed with the clause, which are conditions and not decoration: a **run-golden** for the inbound abort and for a guarded `sqlite3_column_text` on a SQL NULL (`tests/golden/run/` has zero coverage of the read direction today), and a **route on `bad_operand`** for a `cstr` — measured, `print(v)` stops with no `Fix` naming `to_str`, and that hard-stop-with-no-route is what produced panel 058's `static char[4096]` | docs/panel/059 · spec/heroes-spec.md § FFI · crates/heroes/src/measure/gate.rs | the clause is in the spec as of this commit, so the compiler and the document agree for the first time at this boundary
