- [x] **Two live holes at the C boundary, found by panel 096 on its way to something else, and filed separately on that seat's own condition 8** | **(1) A `size_t` out-parameter declared `i32` is ACCEPTED at exit 0.** Measured 2026-08-28 by the ffi-pragmatist on a live compiler: `extern function getline(@line: ptr, @n: i32, stream: ptr) -> i64` builds and runs, printing `warning: incompatible pointer types passing 'int32_t *' to parameter of type 'size_t *'` and **exiting 0**, because `-Wincompatible-pointer-types` is not among `cli_flags.hero`'s thirteen as `-Werror`. By C's rules the callee writes 8 bytes into a 4-byte slot. **The seat could not demonstrate corruption and said so** — the write landed in stack padding and inside malloc granularity, exit 0 under ASan and UBSan both ways — so the claim is only what was measured: accepted at exit 0, out of bounds by the standard, corruption at the allocator's discretion. **This also corrects a claim panel 091 is quoted for**: the probe is NOT *"the only thing that sees"* this — it sees it and the build does not stop. **(2) An `extern` arity mismatch is exit 2, the compiler blaming itself.** `extern function strtok(target: cstr, sep: cstr, pad: str)` gives `too many arguments to function call, expected 2, have 3` from the return `_Static_assert`, at exit 2 — panel 096's Q3 family, a different member, and it will ride whatever sitting settles Q3's attribution question | selfhost/cli/flags.hero:32 · selfhost/emit/extern_probe.hero · selfhost/emit/ffi.hero:48-122 · docs/panel/096 § Two live holes · docs/panel/091 | §1.12 makes memory safety a goal that outranks everything else in the contract, and (1) is a write out of bounds that the compiler currently waves through

  **CLOSED 2026-09-03 by splitting it, the skill's own rule for an item that is two** (author instruction 2026-09-03, *"ratifica tutto e poi dimmi cosa rimane da fare"*, then *"aspetta scegli le soluzioni più robuste e complete rispetto a quelle più economiche"* — the second sentence chose between the options wherever the first recommendation had been the cheaper one). **(1)** is the same defect as the clang-verdict item below and is decided there, in the strict direction: `@n: i32` against `size_t *` is refused by the compiler's own pointee check. Re-measured this session: `getline(@line: ptr, @n: i32, stream: ptr)` builds at **exit 0 with 5 `-Wincompatible-pointer-types` warnings**; and `@n: u64` against the same `size_t *` ALSO warns on macOS (`unsigned long long *` against `unsigned long *`), which is the spec's own advice through a pointer and is the case the strict form must keep legal. **(2)** the arity mismatch at exit 2 rides panel 096's Q3, which 096 sent to the full panel and which no list had scheduled (0 hits in `SCHEDULED.md` and `DONE.md` for a Q3 sitting, measured) — one FFI sitting now takes Q3, the arity case and the pointer verdicts together. Scheduled in `docs/work/SCHEDULED.md`.
