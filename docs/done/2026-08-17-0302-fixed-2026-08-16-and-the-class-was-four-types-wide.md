- [x] panel 062 | **FIXED 2026-08-16 — and the class was four types wide, not one.** `()` is now `ffi_parameter_type` at **exit 1** on the author's line, refused in the frontend by `types/ffi_position.rs` (new file: the seam is *where may a boundary type stand*, which is a different question from `ffi_decls.rs`'s *can C spell it*, and the shared answer is exactly what hid this). **The test for whether a refusal belongs in the frontend at all** is written into that file and is what keeps it one function long: is the fact about C's **grammar** or about some **header**? A parameter list has no spelling for an argument of type `void`, so no header anywhere can take one. **Three more types were found by probing the class rather than the case** — `str`, `ptr` and `cstr` as parameters are still exit 2 — and they are NOT the same defect: each is a legitimate C parameter type that merely disagrees with *this* header, so only clang can judge them. Their repair is widening `emit/ffi_narrowed.rs`, which today matches only *implicit conversion loses integer precision* and *changes signedness*; measured, the probe **already errors on the author's own line** (`./p.hero:2:85: incompatible pointer to integer conversion`), so the location gate needs nothing — only the phrase family does. **Queued as its own item below rather than folded in here**, because it is a different mechanism. And one refusal was written and withdrawn inside the same session: `str` looked impossible by the same argument `ffi_constant` makes, and `runtime/hero_os.h:53` takes a `HeroStr` — the compiler's own library, reported broken on the first run. Was | panel 062 follow-up, compiler-engineer | **`()` as an `extern` PARAMETER is accepted and is exit 2 on the author's line.** `crosses_the_boundary` is shared by results and parameters and `()` is a legal *result*, so `extern function abs(n: ()) -> i32` checks clean; `zero_of` then writes `abs((void *)0)` — `incompatible pointer to integer conversion`, *"internal error: compiling the generated C failed"*. No C spelling of a `()` argument exists, so the repair is a frontend refusal, which is a diagnostic class | crates/heroes/src/types/ffi_decls.rs:122 · crates/heroes/src/emit/assert_spelling.rs | every other way to get a signature wrong is exit 1 on the `.hero` line
