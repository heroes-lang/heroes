- [x] **STRUCK FROM THE 090 BALLOT 2026-08-25 as already ruled, and it is work rather than a question.** Panel 087 states a **reversal condition** in its own text — a named reader in the same commit (`cli_input.hero:24-28` widened so `heroes check <a binary>` says why) plus a golden that fires the arm. A stated condition is constructible; convening five judges on it would have been panel 088's error. It stays open **as scheduled work**, not as a decision, and the cost the sitting measured is unchanged: +2 `_Static_assert`s in every program and 291 re-blessed emissions. Was | panel 087, the two seats that compile disagreed | **Name the not-text status as its own `e.code`, or leave it collapsed into `read_failed`.** Landed provisionally: **collapsed**, because that is the resolution with the least Heroes-visible surface, and its cost is stated rather than smoothed — the message is `could not read <path>` for a file that read perfectly well. The runtime already distinguishes the two states (`HERO_OS_NOT_TEXT 3`); only `selfhost/library_source.hero:150-153` collapses them, and **one `constant` line reverses it**. **For** (ffi-pragmatist): `library_source.hero:143-145`'s own comment says the codes are distinguished *"because a caller almost always wants to"*, and a JPEG is not a failing disk; the absence of the declaration is caught loudly, `error[ffi_unknown_name]` at exit **1** on the author's own line, measured. **Against** (compiler-engineer): measured **+2 `_Static_assert`s in every program in the language** (10 asserts → 12 on a probe), **142** emissions re-blessed — and `tests/harness/suite_emission.hero:12-17` says those bytes are what *two independent implementations* agreed on, so re-blessing spends the last cross-compiler agreement now that `differential.rs` is dark; plus `heroes mutate`'s `typo-code` row is **25 mutants, 0 killed**, the table's only zero, so a new code lands in the one contract nothing checks; plus **no reader exists** — `selfhost/cli_input.hero:26` collapses every read failure into `unreadable`. Its reversal condition: **a named reader in the same commit** (`cli_input.hero:24-28` widened so `heroes check <a binary>` says why) plus a golden that fires the arm. Also refused, and not on taste: the cheap `status == 3` literal, because `library_source.hero:129-134` records that **M-header-constants** moved those numbers into the header and that *"the header is the only place they are written"* | docs/panel/087 § Where the seats disagreed · selfhost/library_source.hero:150 · runtime/hero_os.h | a status the runtime knows and the language cannot ask about is the same shape this sitting existed to fix, one level up
