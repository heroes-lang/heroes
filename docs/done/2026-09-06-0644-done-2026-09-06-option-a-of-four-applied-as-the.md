- [x] M-declared-freer step 1 | **DONE 2026-09-06, option (a) of four, applied as the provisional default under CLAUDE.md §15.** **164 live source files cited a path that had not existed since 2026-08-19**: `crates/` appeared **179 times** across **145 files under `selfhost/`** and **19 under `tests/`**, always as a module doc's `Port of crates/heroes/src/…`. The tree moved to `archive/bootstrap-rs/` at M-bootstrap-archive and nothing followed it. **The wording was sampled before the rewrite, which is what made it safe**: 118 of 128 sampled read *"Port of crates/"* and the rest are variants of the same sentence (*"lives in"*, *"cases at"*) — every one a live pointer to a file, not a dated claim about the Rust, so a mechanical rewrite is right for all of them. `docs/` holds 46 more and they stay: §14 forbids rewriting a dated record. **Verified after**: 165 distinct `archive/bootstrap-rs/…` paths are cited, **136 plain ones all exist**, 26 are brace or glob forms unresolvable by construction, and **3 did not resolve even inside the archive** — `heroes/src/diagnostics.rs` is `diagnostics/mod.rs`, and `emit/inventory.rs` and `resolve/inventory.rs` are both `builtins.rs`, whoever wrote those docs having put the HEROES module's name inside the RUST path. Those three are repaired too, so the sweep left zero dead paths where it started with 164 files of them plus three that were wrong before the tree ever moved. **The seed does not move**: an in-line substitution shifts no line, so no `#line` shifts, measured by re-emitting and `cmp`. Two reasons the instrument reported nothing — it reads `design.md` and `CLAUDE.md` only, and most of these are unbackticked — and the half of the recommendation that would fix that is still open in `docs/work/DECIDE.md` with its own measurement | found while writing four module docs for the split | `grep -rl 'crates/' selfhost/ tests/` now reports nothing
