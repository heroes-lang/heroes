- [x] **CLOSED 2026-08-28 — the sitting was held as panel 095 and all four of its stages landed.** Commits `4c69a4f` (the sitting), `aa8a372` (stage 1: `fmt` a fixpoint on all 165 modules, from 31 that were not), `a0ab809` (stage 2: `assert_canonical`), `8584d18` (stage 3: the rules), `9944209` (stage 4: the repository swept canonical). **What the author decided that the brief could not predict**: rule 3 DROPPED (no seat defended it — it teaches a reading rule 2 already owes), rule 2 NARROWED to a dedent closing a `for`/`while`/`match` and never an `if`, rule 4 SCOPED to statement bodies, and the llm-ergonomist's cheaper alternative REFUSED after measurement showed all four of its clauses were already the behaviour. Rule 5 needed no work: `blank_line` had always refused to emit a second newline, so it was enforced at the primitive. **The brief's own worst number was wrong and a judge caught it**: it said the sample was 44 lines and 'one in five' blank; measured, 47 and 29.8%, and the real figure over the swept `selfhost/` with rule 2 narrowed is **13.3%**. Was | **PANEL BRIEF, instructed 2026-08-26 by the author and not convened — the canonical form gets more air** | **Author instruction, verbatim intent**: *"voglio più aria"* — a blank line before every `for`, `while`, `if` and `match`; a blank line after a block closes and the code resumes at the outer level; a blank line before the `return` that closes a function; a blank line **before** a comment line inside a body, with **nothing** between the comment and the code it documents; and never two blank lines in a row. **What is already true, measured**: run-collapsing is current behaviour (three blank lines in, one out), so rule five is implemented — and it is also what resolves the collision the other rules create, since "after a block closes" plus "before the closing return" would otherwise emit two. **What makes this a panel path** (CLAUDE.md §4): the canonical form is surface, `design.md` §4.15 mandates exactly one correct spelling of any program, and the current policy is a recorded decision — `DESIGN-LOG:46`, *"blank lines are content — one always between top-level declarations, one survives inside a body, runs collapse"*. The rules above replace *content* with *structure* for four cases, which is the actual proposition. **The one interaction to price**: `design.md` §4.1 gives a comment adjacent to a declaration the status of its documentation, and a blank line between them declasses it to a remark — the author's rule puts the air ABOVE the comment, so §4.1 is untouched, and the brief must say so or a judge will refuse it on that ground. **The blast radius is the whole repo**: 263 of 454 `.hero` files are already non-canonical against today's rules (measured 2026-08-26), so the sweep that applies this is the same sweep either way — but it must run **once**, on the ruled-on format | design.md §4.15, §4.1 · DESIGN-LOG:46 · selfhost/print_fmt.hero (blank_line, fmt_block) | the author is the one person this project is for (§1.1) and they said the current output is hard to read; that is the objective reporting a miss, not a style preference
