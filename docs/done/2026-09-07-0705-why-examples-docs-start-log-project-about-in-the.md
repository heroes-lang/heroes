- [x] **The nav answers a visitor's questions, and three pages became one** | author instruction 2026-09-06 (*seven items, and merge self-hosted with the panel and the Zen, and the author with the thanks*), delivered 2026-09-07 | Why · Examples · Docs · Start · Log · Project · About, in the order somebody asks. It was eight items that were the project's own shelves, with neither a learning entry nor an install entry, which every one of the nine language sites surveyed has. `selfhost.html`, `panel.html` and `zen.html` are `project.html`'s three sections; `author.html` is `about.html` and `thanks.html` is its sub-page, the way `errors.html` is one of `/docs/`. **The thanks entries are copied untouched**, all 33 in both editions, because that page names living people and `site/README.md` requires every lookup re-run in the session that edits an entry: the word budget yields, the names do not. Measured in Chrome, both editions, twelve widths: the list is **394px** in English and **426px** in Italian where nine items were 517 and 540, one row of chrome above 800px, two below, never three. Old URLs answer through `site/public/_redirects`, 10 rules, and all 38 files that linked a retired page were repointed so a reader never pays the hop | `site/src/components/SiteNav.astro` · `site/public/_redirects` · `site/README.md` § the nav | a nav that lists a project's shelves asks the visitor to work out which shelf their question is on
