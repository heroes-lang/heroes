- [x] **The `.fixed` instrument is structurally blind to every module diagnostic, and that is how a non-terminating `certain` fix shipped** | panel 102, the ffi-pragmatist, unasked; reproduced twice by the coordinator | **`tests/harness/suite_fixes.hero` says in its own words *"Only `check/` cases carry `.fixed` files"*, a `check/` case is ONE file, and every diagnostic about modules needs two files to provoke.** So CLAUDE.md §8's promise — CI asserts an applied `certain` fix compiles — has never once been tested on the module class. Measured 2026-09-02: golden cases naming `needs_qualifying` anywhere in `tests/golden/` number **0**; `.fixed` siblings that exist at all number **6**. What it let through: `heroes check --apply` on `window: ptr` beside `use ffi/window` writes `window.window.destroy(handle: window)`, and applying that writes `window.window.window.destroy(...)` — **the fix reproduces itself**, because `ptr` has no fields so every module call through a shadowed handle reaches `needs_qualifying`, whose repair prepends the qualifier the line already has. Panel 101 R4 had already ruled this class (*"a certain fix that does not compile is the one thing a fix may never be"*) and it was live again one branch over, on the FFI's most natural spelling. **Panel 102's (A) does NOT close the reachable path, and the sentence that stood here said it did** (corrected 2026-09-03, measured on the seed of 2026-09-02 17:52, which carries (A): `use window` beside `window: ptr @ nullptr` and `_ = window.destroy()` gives **two** diagnostics at exit 1, `shadowed_binding` AND `needs_qualifying`, because (A) reports and then BINDS the local, so the dot still resolves through a `ptr` with no fields and falls to the module path; and `heroes check --apply` on it is **exit 0** writing `window.window.destroy()` — the fix reproduces itself today, on the FFI's most natural spelling). So this is a live defect under panel 101 R4 and not only a harness question, and the two halves of the decision below are no longer alternatives: the `certain` tag is wrong NOW whatever `.fixed` grows into. **The decision**: does `.fixed` grow to cover `surface-fixtures/`, and if so what is a multi-file case's `.fixed` (a whole directory, or one file per case), or does `needs_qualifying`'s fix drop to `guess` instead, which is the ffi-pragmatist's own condition? The first buys the whole class for the cost of a harness design question; the second buys one diagnostic for two lines. **Recommendation: the first, and price it before building it** — the class is every module diagnostic the language now has, and panel 100 and 101 both added to it in one day | docs/panel/102 § The finding that outlives the sitting · tests/harness/suite_fixes.hero (`CHECK_DIR`, `LEAST`) · tests/golden/surface-fixtures/ · selfhost/module/diagnostics.hero (`needs_qualifying`) | an instrument that cannot reach a class of case reports green over all of it, which is the sentence CLAUDE.md §9 was written to prevent

  **CLOSED 2026-09-03** (author instruction 2026-09-03, *"ratifica tutto e poi dimmi cosa rimane da fare"*, then *"aspetta scegli le soluzioni più robuste e complete rispetto a quelle più economiche"* — the second sentence chose between the options wherever the first recommendation had been the cheaper one). **Decision: both halves, the complete form.** First, `needs_qualifying`'s fix drops to `guess` NOW (`selfhost/module/diagnostics.hero:110`, and its test at `:281` flips), because the fix is live and non-terminating on the seed of 2026-09-02 17:52 — measured this session, `window.destroy()` → `window.window.destroy()` at `--apply` exit 0 — and panel 101 R4 says a certain fix that does not compile is the one thing a fix may never be. Second, `.fixed` grows to `tests/golden/surface-fixtures/`: one `X.fixed` beside each `X.hero` that carries a certain fix, the harness copies the fixture directory to scratch, swaps the file in, and requires `heroes check` exit 0 on the entry file; a floor on the count as `LEAST` is for `check/`; the first fixture is the measured `window` program. The downgrade alone (the pragmatist's condition) was declined as the cheaper half: it buys one diagnostic and leaves CLAUDE.md §8's promise untested on every module diagnostic panels 100 and 101 added. Scheduled in `docs/work/SCHEDULED.md`.
