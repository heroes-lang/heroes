- [x] **M-isolated-threads step 0** | author draft 2026-08-10, promoted to v1 invariants the same day (`DESIGN-LOG.md:110`, where they were *"verified rather than assumed"*); the half that asked for a check had never had a home until 2026-09-04 | **BOTH INVARIANTS HAVE AN INSTRUMENT NOW, AND ONE OF THE TWO WAS BROKEN.** `tests/harness/suite_runtime.hero`, four checks, run as `heroes run tests/harness/main.hero -- <compiler> runtime`, 8 s, reading files and running no compiler. **The refcount boundary held**: `inline` occurs **0** times in `runtime/heroes_runtime.h`, and all **6** entry points — `hero_str_`, `hero_array_` and `hero_map_`, increfed and decrefed — are `;`-terminated prototypes, so making the counter thread-local or atomic is still the one edit design.md promised. **The single allocation point did not**: `runtime/parts/array.c:203` grew `hero_eq_queue`, the work list that lets a deep `==` run without recursing, with **`realloc`** — a second allocation site, which is the one thing Part 7.13 says the runtime must not have, because a per-thread heap needs one place to change and a site found later is a redesign. **And the grep the invariant promised could never have found it**: `alloc.c`'s own header said *"the invariant is checkable with one grep"* and the sentence it was checking names `malloc` and `free`, while the call said `realloc`. So the suite reads the whole family — `malloc`, `calloc`, `realloc`, `reallocarray`, `aligned_alloc`, `strdup`, `strndup`, `free` — and reads **code** rather than text, because `runtime/parts/run.c:484` carries the English sentence *"the first check is free (WNOHANG, no sleep at all)"*, which a grep reports and a reader then learns to ignore. Its `code_lines` blanks every comment and every literal first, and the floor that proves it did not blank everything is the count of allocation calls **inside** `alloc.c`, which must stay at 3. **The repair**: `hero_grow_kept` in `alloc.c` — a third shape and deliberately not a pair, weighed by neither counter. Not by `hero_live_blocks`, because a buffer kept on purpose would report a leak at every exit of every program that compares two deep arrays, in the only leak instrument this platform has (panel 021: ASan has none on Darwin arm64); and not by `hero_live_scratch`, which says *borrowed and not given back*, which this is not. Plus the overflow guard §4.20 makes normative for a primitive that computes a length before allocating, which the `realloc` had never carried. **Measured** with 300 nested arrays, forcing the queue 64 -> 128 -> 256 -> 512 so the copying path runs three times: correct answers at `-O0`, at `-O2` and under `--sanitize`, exit 0 in all three, `hero_runtime_check_leaks()` clean. **The open half is answered, and the answer is no**: the two rules do NOT become CLAUDE.md §7 rules. They are written in design.md §4.20 and Part 7.13, and CLAUDE.md's own preamble says each rule is written in exactly one place and everything else cites it — a copy in §7 is the failure §7 itself records, where the flag list died 3h16m after it was made true. The precedent is exact and it is in the record: `DESIGN-LOG.md:422`, where the `ORDER:` mark stopped being a grep and became an instrument, *"no panel — the convention's shape is design.md §4.9's own sentence, obeyed rather than amended"* | tests/harness/suite_runtime.hero · runtime/parts/alloc.c · runtime/parts/array.c:186-215 · design.md:2210-2222, :2582-2588 · DESIGN-LOG.md:110, :235, :422 | an invariant nothing checks is a comment, and this one had already been false once — eleven calls across five files — with nobody noticing for two days
