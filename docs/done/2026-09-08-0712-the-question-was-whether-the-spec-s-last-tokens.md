- [x] **The spec's higher-order silences, answered by prose instead of signatures** | filed 2026-09-08 as a panel 119 finding, opened the same day by author instruction (*"as I told you, it must be opened right away then"*) and settled by panel 120 | **The question was whether the spec's last tokens should buy the four higher-order signatures it never wrote.** The answer is no, and the reason is one sentence from the historian's seat: `function filter<A>(xs: [A], f: (function(A) -> bool)) -> [A]` is **byte-identical for keep and for reject**, so every draft on the ballot — +86, +69, +63 — bought the LOUD class, arity and return type, and bought **nothing at all** for the silent class the sitting was convened about. What landed is prose at **+30**: *no anonymous functions* in the enumeration of absences the spec already carries, `filter` *keeping what the function accepts*, `find` giving *the first it accepts, or an error*, and `not_found` named beside the one failure code the document already had. The spec stands at **3995** with **40** free. **Four ways the ballot was wrong, each found by a seat**: the spec states two of the seven types and not one (`range` at `:145`); `fold`'s argument order was already written at `:116-117`, which two seats found independently and both refused to pay for twice; every draft appended prose seventy lines above the place a reader meets the names; and the fourth is the sentence above. **The removal that was priced and not taken**: the mandatory-named-argument bullet at `:108-109` measures **−35**, which would have left the spec smaller than it started, and the ergonomist overturned it on evidence a token count cannot see — that bullet is the conversion function from a listed signature to a written call, `write_file(path: str, text: str)` is a built-in every program uses whose call the spec never shows, and the rule governs how a writer NAMES parameters, which the `certain` fix entrenches by repairing the call site after the signature exists. Its own falsifier went unmet: nobody could name a clause needing 75 tokens that does not fit in 40. **And the harness caught two drafts before the suite did**: `spec_text.hero` harvests every space-free backticked fragment in the `Built-ins:` sentence as a built-in name, so a type written there fails `spec/offered` — and, silently worse, `filter` and `find` drop out of the harness's coverage inside a multi-word span. The landed wording harvests byte-identically to the previous one | `docs/panel/120-the-signature-cannot-say-which-half-it-keeps.md` · `docs/measurements/010-spec-budget-ledger.md` row 3995 · `spec/heroes-spec.md` | prose in three records and an item in none is the failure mode CLAUDE.md §3 describes about `DECIDE.md` itself, and this one was filed at the author's instruction within minutes of being written into a measurement
