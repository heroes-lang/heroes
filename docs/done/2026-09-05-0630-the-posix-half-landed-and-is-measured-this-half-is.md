- [x] **M-c-callbacks** (OPEN) — defect 013's Windows twin, eight lines, written and waiting for five minutes of the box | withheld deliberately at the repair, 2026-09-05, because the machine went off before it could be run | **The POSIX half landed and is measured; this half is written and has never been executed on a Windows machine, so by CLAUDE.md § Commands it is an inference and it did not ship.** What landed: `runtime/parts/stack.c`'s POSIX handler refuses `pc == 0` with `panic: a null function pointer was called`, measured **unique** against three adjacent fault shapes — a wild store to 0 and a wild read from 0 both give `si_addr == 0` with a NONZERO pc, a garbage function pointer gives a nonzero pc — and confirmed firing on **macOS** and on **glibc** with the same witness program, `qsort(base: buf, n: 4, size: 8, compar: nullptr)`, exit 134 on both. **What is owed**, into `hero_stack_veh` beside the `EXCEPTION_STACK_OVERFLOW` branch: `if (ep->ExceptionRecord->ExceptionCode == EXCEPTION_ACCESS_VIOLATION && (uintptr_t)ep->ExceptionRecord->ExceptionAddress == 0)`, then the same `_write(2, …)` line and `abort()`. `ExceptionAddress` is where the program was EXECUTING, which is the PC — the address it touched is `ExceptionInformation[1]` and that one is zero for three different faults, which is the whole reason the POSIX half reads the PC. `uintptr_t` is reachable: `runtime/runtime.c:96` includes `<stdint.h>` before this file. **Why it was not shipped unrun**: a compile error there is red on the Windows leg **at the next tag**, which is the moment a milestone closes, and nothing regresses meanwhile — that class dies silently on Windows today exactly as it did before. **What to run when the box is next on**: the `qsort` witness above; note that `atexit(nullptr)` is the WRONG test there, measured exit 0, because the CRT checks — the same program is a segfault on macOS, a glibc assertion on Linux and exit 0 on Windows, which is this defect's own lesson about whose property NULL-tolerance is | runtime/parts/stack.c (the marker sits where the code goes) · tests/golden/fixedbugs/ffi-a-null-function-pointer-says-so.hero · docs/environment/windows/WINDOWS-MACHINE.md | a guard that is measured on two platforms and asserted on the third is two thirds of a guard, and the record should say which third

**CLOSED THE SAME EVENING, 2026-09-05: the author turned the box back on and it took five minutes.** The branch **compiles** — seed built from C alone on Windows Server 2025 / clang 22, exit 0, zero errors — which was the whole reason it had been withheld. **And it fires**, with a witness that had to be built for the platform: `qsort` and `atexit` are BOTH validated by the Windows CRT, which terminates the process before anything reaches address zero, so neither reaches the handler there. What does is a callback that does not go through the CRT at all — `static inline void run_callback(void (*f)(void)) { f(); }` in a header the group names, which is what any third-party library looks like. That gives `panic: a null function pointer was called …` on the box. **The adjacent shapes are untouched on Windows too**: a stack overflow still says `panic: stack exhausted`, an out-of-range index still says `panic: array index out of range`. **And one thing was measured rather than assumed before the branch went in**: `runtime/runtime.c` compiled on this Mac before and after is **byte-identical**, `md5` equal, because the branch is preprocessed away here — so the net running at the time still answered for the tree. The three platforms now read: macOS panic exit 134 · glibc panic exit 134 · Windows panic, and the platform lesson is that the CRT's own validation is a fourth behaviour on top of the three this defect already had
