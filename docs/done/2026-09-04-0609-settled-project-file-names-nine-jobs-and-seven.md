- [x] **reasoning 004 — a project file, against the homes that already exist** | 2026-08-14, the session that asked whether Heroes should have a `Cargo.toml`-shaped per-project file the compiler reads | **Settled**: *"project file"* names **nine jobs** and seven already have homes with stated reasons, so the answer to the question as asked is an inventory rather than a preference about file formats; Principle 0 does not fund it, since the fixpoint invocation is locatable without one; and the nearest refusals — §3.5's *"never a Makefile"*, §6's *"a separate package binary"* — do not reach a manifest. **All five hand-offs landed 2026-08-15**: panel 056 adopted B with D as its companion and refused A and C (this file, `:446`); the `CPATH`-versus-flag line was drawn (`:448`); §6's bare clause was **retired**, its implied contrast being factually wrong because nimble ships with Nim (`CLAUDE.md:237`); the note-beside-a-panel pair was legalised narrowly (`:456`); and four stale record entries were repaired (`:460`). Panel 056's own deliverable B — the greppable §3.5 paragraph — is still unwritten and has a home at M-core-packages. **Its inventory moved to `docs/panel/056` § Appended 2026-09-04 rather than here**: the note was legalised in the first place *because* it held what the panel file did not, so moving the table is what keeps that ruling true rather than reversing it. The `.rs` citations in it were repointed to `archive/bootstrap-rs/` and to the live `selfhost/parse/decl.hero:550` | `git show 2689d606:docs/reasoning/004-a-project-file.md` · docs/panel/056-what-a-project-file-would-carry.md § Appended 2026-09-04 · DESIGN-LOG.md:301, :304, :307, :308 | the session that answered a design question by finding that seven ninths of it was already answered, and the one whose deletion had to be argued rather than performed
