- [x] panel 087 | **RATIFIED 2026-08-23**, hours after being queued (author instruction, *"ratifica tutto"* — blanket over 085, 086 and 087; `docs/panel/087` § Author's verdict says what the yes settles). The repair stands as landed: the runtime pre-check, `HERO_OS_NOT_TEXT`, +0 spec tokens, no ABI bump, the abort in `hero_str_from_bytes` untouched. **The nine queued items are untouched by the yes**, `file_not_text` included — the engineer's reversal condition (a named reader in the same commit plus a golden that fires the arm) is unchanged, and the collapsed message is accepted as a stated cost, not as a closed question. Was | panel 087, provisional 2026-08-19 — **queued 2026-08-23, and the panel file had no `## Author's verdict` section at all** (added in the same commit as this line, pointing here; 085 and 086 both carried one). Two sittings in a row could not be ratified because nothing asked | **Ratify the repair as it landed, or overturn it.** Adopted provisionally and shipped: `read_file` on bytes that are not text **fails instead of aborting** — `runtime/parts/os.c:130` pre-checks `hero_utf8_valid`, releases the buffer, sets the new `HERO_OS_NOT_TEXT` and returns the empty str, **five lines of C and +0 spec tokens**. What a yes settles: the spec was never wrong (`spec:179` already types the built-in `-> str?`), so this was a §12 repair in the one place the spec cannot see, and paying 11–21 tokens to *describe* the old behaviour would have written a defect into the document. It also settles the ffi-pragmatist's uncast veto: **the abort inside `hero_str_from_bytes` stays**, because `hero_str_chars` (`runtime/parts/text.c:87`) takes one byte off an invalid sequence and depends on it for the language-wide well-formedness invariant. What a yes does **not** settle: the eight items below, of which the first is the only one that changes what a program sees. Re-measured 2026-08-23 in one session: the five-line reproducer is `fail: read_failed` at exit **0**, and the whole net is green with a summary line where it used to be exit 134 with no summary at all. The recommendation is **yes** — this is the cheapest shape anybody found (three seats hunted for a spec removal and the warden refused all five non-zero options), and it is the only one that keeps `docs/defects/002`'s dead guard alive without weakening the invariant that the rest of the language rests on | docs/panel/087 § The resolution adopted · docs/defects/002 · runtime/parts/os.c:130 | a legal program that handled both arms of a fallible was killed before either, and the repair cost the spec nothing
