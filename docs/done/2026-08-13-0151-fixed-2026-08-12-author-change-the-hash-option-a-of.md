- [x] Covered | panel 039 / verification | **FIXED 2026-08-12 (author: change the hash, option a of four).** The mangler's typehash was FNV-1a over the rendered type arguments, above a comment saying *"the port must reproduce it exactly"* — and the port could not, three ways: the offset basis `14695981039346656037` is `int_out_of_range`, `hash * 1099511628211` is `panic: integer overflow`, and `^` is `reserved_operator`. `h_library_map_<typehash>` reaches the C of any program using a generic and the fixpoint compares that C byte for byte, so this was a wall on v1 that no list recorded. Now a polynomial modulo 2^31 − 1, intermediates fitting an `int` with three orders of magnitude of margin, and `tests/golden/run/premise-mangler-hash-in-heroes.hero` computes the same four values **in Heroes** against a Rust test that asserts the pair agrees. Chosen over a runtime primitive (would have bumped the ABI), over unsigned arithmetic (Part 7 item 10, *"never in v1"*), and over deferring to M-selfhost-probe — because **zero `emit/` goldens carried a hashed name**, so the change moved nothing today and would have cost more at every later milestone. design.md §1.0 gains the row, with the audit's blind spot named: measurement 003 is mechanical over the closure list, so a requirement absent from it is invisible — twice now, and both times the missing row was something the *compiler does* rather than something a program contains | crates/heroes/src/emit/mangle.rs · tests/golden/run/premise-mangler-hash-in-heroes.hero · design.md §1.0 | the fixpoint is the v1 finish line and this was a hard blocker on it
