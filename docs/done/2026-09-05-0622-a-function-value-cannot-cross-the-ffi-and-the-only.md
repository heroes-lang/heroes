- [x] **M-isolated-threads** (OPEN) — the callback boundary is the threads milestone's, and the program that settles it has now been run | measured 2026-09-03 while the packages milestone was planned; **re-measured and half-corrected 2026-09-05, `docs/measurements/017`** | **A function value cannot cross the FFI, and the only thing on the packages list that needs it to is a thread. What remains open is the PERMISSION, not the mechanism.** Measured 2026-09-05, four probes: pthreads binds, links and runs from Heroes (`pthread_self`/`pthread_equal` → `same thread: 1`, exit 0); the emitted signature of `function worker(arg: ptr) -> ptr` is `void * h_main_worker(void * h0_arg)`, which **is** `void *(*)(void *)`, and handing it straight to `pthread_create` with no cast compiles under `-Wall -Werror` and prints from the other thread at exit 0. **So panel 030 R6's cfront rider is answered — yes — and the deferral is not a bet.** **Two claims this item carried are corrected rather than deleted**, because both would have briefed a sitting wrong: (a) it said the emitter *"owes one generated trampoline per spawned function type"* — not for this shape, the two C types are already identical; (b) it said `selfhost/check/ffi.hero:45` refuses `.function_ty` *in every extern position* — there are **two doors** and that is one of them, because `start: ptr` is an **accepted declaration** whose call site then fails with `error[type_mismatch]: expected ptr, found (function(ptr) -> ptr)`. Its runtime line numbers were stale the same day: `_Thread_local` is at `runtime/parts/array.c:308` and `drop.c:59-61`, and the allocator's globals are `alloc.c:61` and `:64`. **What the sitting still owes**: which door opens, and the loopback HTTP server that answered `curl` from Heroes on 2026-09-03 written with one connection per thread — Part 7 item 13's first witness. **And what it must price is now measured**: four threads allocating at once give `hero_runtime_check_leaks()` **2586, 1836 and 3141 live blocks in three identical runs**, exit 134, a leak that is not one, and ThreadSanitizer names the whole race as `hero_live_blocks` at `alloc.c:113` — inside the single allocation point, which is what design.md §4.20 promised the invariant would buy | docs/measurements/017-the-thread-the-emitter-could-already-carry.md · selfhost/check/ffi.hero · selfhost/emit/ctype.hero:379 · runtime/parts/alloc.c:61, :64 · docs/panel/013, 030 R6, 104 · design.md Part 7 item 13 | a concurrency milestone with a model and no program decides nothing, and this one now has the program and the numbers

**CLOSED at M-c-callbacks step 0, 2026-09-05.** The door that opens is the **parameter** one, and it opened in the checker rather than in the backend: `crosses_the_boundary` now answers yes for a function type, and the two positions that would hand a callable INTO Heroes — an `extern`'s result and an `@` out-parameter, plus an `extern constant`, which is the third and which no seat named — are refused by position with their own repairs. Measured end to end the same day: `atexit` binds, compiles and C calls it back (`tests/golden/run/ffi-callback-c-calls-back.hero`), and `pthread_create` binds, spawns and joins with no cast and no trampoline. **Two claims in this item's own body were corrected by the work rather than by reading**: the emitter did owe something after all — `emit/extern_probe.hero`'s `c_type_of` had no `.function_ty` arm, so one callback parameter withheld the whole list and the probe was skipped for the ENTIRE extern, which is `DrawText`'s defect with a new cause; and two further gaps nobody had predicted came out only by running it — a per-module TU never joined an `extern`'s parameter types to its typedef reach (`unknown type name 'h_0fn_294870dd'`, exit 2 on a correct binding), and the probe was emitted before the typedef section against `emit.hero`'s own written invariant. **What is NOT closed here** is the loopback HTTP server with one connection per thread, which is now its own item under M-isolated-threads: the permission no longer blocks it, and panel 111 R9's guard deliberately stops every worker by name until the isolation lands
