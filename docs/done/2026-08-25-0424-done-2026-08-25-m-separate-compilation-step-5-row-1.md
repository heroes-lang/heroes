- [x] panel 091, measured 2026-08-25 | **DONE 2026-08-25, M-separate-compilation step 5 — row 1 landed, the seven sites routed, and the rule grew two gates the count had not named.** The bill was exact: re-measured before a line was written, `cli_toolchain.system` had exactly **7 qualified call sites in 4 modules**, and all seven now route through `cli_shell.shell(command: str)`, a Heroes function beside the extern (with `.cstr()` moved inside it, so the callers stopped converting by hand). The rule itself landed in the resolver as `error[extern_across_modules]`, and **the gate sits on the qualified REFERENCE, not the call** — `_ = bind.system`, the extern taken as a value, is refused identically, because taking the address needs the header exactly as calling does; `resolve_qualified.qualified()` is the one point every qualified mention passes. **Two shapes the sitting had not counted, both measured open and both closed**: the library is a module whose names are visible unqualified, and `hero_args_count()` from a user program was check-clean at exit 0 — with **zero** direct users in the whole repository (one grep hit, and it is a comment), so closing it deleted a class of program nobody had written; and UFCS (`"p".cstr().hero_file_read(@status)`) reached the same externs through the dot. Three gates, one diagnostic, the library arm's message naming the wrapper route. Group `constant`s and `record`s stay reachable qualified, deliberately: their C is compiler-written on both sides, so the header is only needed where they are declared. Verified: `wrong.hero` exit 1 on both spellings, `right.hero` runs exit 0, the compiler checks its own routed source clean, the harness needed nothing (zero qualified extern calls, already wrapped). Was | **Acceptance row 1 breaks the compiler's own source in seven places, and nobody had counted them.** `cli_toolchain.system(command: ...)` is called **qualified, across a module boundary, 7 times from 4 modules** — `cli_libraries.hero`, `cli_doctor.hero`, `cli_mutate.hero`, `cli_verbs.hero` (e.g. `cli_doctor.hero:28`, `cli_verbs.hero:159,191`) — and it compiles today. Row 1 (panel 030 R2, panel 033) says an `extern` is **never** callable across a module boundary and that a qualified call to one is a type error, with the route being a Heroes function in the declaring module. So the day row 1 lands, the compiler stops compiling itself until seven call sites route through a wrapper in `cli_toolchain.hero`. **Not a defect — the milestone's own work, priced.** Two more counts for whoever builds it: `selfhost/` holds **3 group heads over 2 distinct headers** (`cli_toolchain.hero:34` and `emit_literal.hero:40` **both** name `stdlib.h`, which matters to any group-keyed rule), and the compiler-engineer measured the shape row 1 exists to prevent, reproduced today: a calling TU with a compiler-written prototype instead of the header takes a `double`-returning C function declared `-> i64` at **exit 0** and prints `3` where the truth is `3.5`, while the declaring TU with the header is exit 1 | docs/panel/091 · docs/ROADMAP.md § M-separate-compilation row 1 · selfhost/cli_toolchain.hero:34 | the milestone's first acceptance row has a bill attached to it and this is the bill
