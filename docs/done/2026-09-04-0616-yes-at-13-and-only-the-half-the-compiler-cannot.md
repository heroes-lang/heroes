- [x] **Does the spec owe a sentence on what a `constant`'s body may contain?** | panel 039 R4, 2026-08-12, settled at `docs/panel/110` | **YES, at +13, and only the half the compiler cannot teach.** Written 2026-09-05, extending `spec:47`: *and a written body computes over literals and other constants*. **The proposed wording was vetoed by the ffi-pragmatist and separately proved false by the compiler-engineer, and both were right.** The ffi seat read the real headers on this machine with `clang -E -dM`: `HUGE_VAL` is `__builtin_huge_val()` and `NAN` is `(__builtin_nanf(""))`, both bind as `constant …: f64` and print `inf`/`nan`, so a Heroes `constant` whose value is a call already exists and works at §4.19's ladder rung 2 — and *"a `constant`'s body calls nothing"*, whose subject is the constant, presupposes a body that `spec:239` denies (*"A group's `constant` has no body"*) and would have provoked a 9-line C shim the seat wrote to price it. The engineer showed the clause false in the costly direction too: `constant_body` refuses exactly four expression kinds, `[1, 2, 3]`, `{"a": 1}`, `XS[0]` and `if`/`else` all run, and `Point(x: 0, y: 0)` is refused **as a call** — so a reader who accepts array literals and reads *"it calls nothing"* writes `constant ORIGIN: Point` and pays a round trip. **The resolution takes the warden's price and the engineer's escape at once**: state only the permissive direction, which is the silent one — `A * 3 + 1` in a body checks clean and nothing tells a reader it may — and leave the refusing direction to `error[constant_body]`, which the warden called the best diagnostic it found. A sentence that says nothing false needs no correction. **And the historian's objection is on the record rather than in the sentence**: *"a `constant` re-evaluated on every read"* argues for diagnosing an initialiser that is not a constant expression, not for forbidding calls — `len(args())` should be refused because `args()` reads the process — so the restriction stands as an **implementation limit rather than a principle**, on C23's own citation, N3018 of 2022-07-06, where WG14 kept `constexpr` to objects only *"to avoid undue burden on lightweight implementations"*, and Heroes emits C | spec/heroes-spec.md:47-48 · selfhost/resolve/cycles.hero:191 · docs/panel/110 | a `constant` whose legal contents nobody wrote down is a form a model has to guess at
