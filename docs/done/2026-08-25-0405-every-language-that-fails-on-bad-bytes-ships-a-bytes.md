- [x] **STRUCK FROM THE 090 BALLOT 2026-08-25 as already ruled.** Panel 087: *"Principle 0 has no compiler-need for it today, **measured**"*, and its own prediction names the trigger that brings it back — the first milestone whose harness must read a non-`.hero` artifact byte for byte. A settled deferral with a named return condition is not an open question, and panel 090's historian independently confirmed the shape from outside: among languages whose string carries a UTF-8 invariant, every one ships a second name, and this one's is `validated`. Was | panel 087, historian | **Every language that fails on bad bytes ships a bytes reader beside it; this one has none.** Verified across six: Rust fails (`ErrorKind::InvalidData`) **and** ships `fs::read`; Python raises **and** offers binary mode plus `surrogateescape`; Go, Nim and Elixir never ask because their string is bytes. **Zero of six abort.** PEP 383 is the record of shipping the strict half alone: Python 3.0 made a class of file unreachable and `surrogateescape` was retrofitted **one minor version later**. Measured here: the spec's only mention of bytes is `str` being *"indexed and measured in bytes"* (`:51`), and the emitter's built-in list has no byte reader — so `fail(…)` gives a program a branch it can **observe but not act on**. **The counter-argument is in the record too, and it is the provoking case**: the seat's own falsifier is *"a real program that does something useful other than reporting and exiting"*, and `tests/harness/suite_records.hero`'s `continue` is exactly that — it skips the photograph and finishes the walk. So Principle 0 has no compiler-need for a bytes reader today, measured, and the day a harness must read a non-`.hero` artifact byte-for-byte it does | docs/panel/087 § Where the seats disagreed | the escape hatch is what the precedent says you owe, and the one program that provoked the sitting does not need it
