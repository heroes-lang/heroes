- [x] **CLOSED 2026-08-28, answer `7c` — the number is rounded and the item is done.** `selfhost/print_fmt.hero`'s ceiling goes to **1100** from the measured 1083, and the reminder this item was keeping is retired. The author's call, against the coordinator's recommendation to leave it open: the item existed as a prompt to consider splitting the fmt knot, and four of the five defects found in that module across three days being one class — a measurement that disagreed with what the printer prints — was the argument FOR the split. **The counter-argument the author took**: the two repairs of 2026-08-26 made the module more sound rather than less, so splitting now is risk with no defect asking for it, and the language forbids the only seam available anyway (`print_bodies -> print_fmt -> print_bodies`, `module_cycle` on the `use` edge, §11's knot clause). **What is kept rather than dropped**: the re-baseline note in `suite_layout.hero` stands, including that the table moves in both directions — five files measured smaller and two left it — which is what says a metric change is not a way to hide growth. Was | **The size ratchet was re-baselined once and raised four times, and the four raises are the question** | **`selfhost/print_fmt.hero` 843 -> 888 -> 903 -> 996 -> 1005 -> 1083, every number measured 2026-08-26/28**: +45 for docs/defects/003, +15 for docs/defects/004, +91 for panel 095 stage 3's air rules, +9 for the comment recording `WIDTH` 88 -> 120, and +78 from the sweep formatting the file itself. **Raised, never split, and the language is why**: 003's two functions read `power` and `wrapped`, which live in `print_fmt`, so the only available cut runs `print_bodies -> print_fmt -> print_bodies` and `module_cycle` fires on the `use` edge — §11's knot clause, the same reason the module doc already gives for the file existing as one. **Separately and once, 2026-08-28, the WHOLE TABLE was re-baselined**, because two things changed under all 165 files in the same commit: `selfhost/` became canonical, and `code_lines` stopped counting blank lines (§11 says the number governs *what a reader must hold in their head*, and a blank line is the one line nobody holds — counting them would have had the ceiling punish the change made to make the files readable). That is a re-baseline rather than a raise and the table proves it: same size, 16 entries, **five files SMALLER than before** (`ast.hero` 495 -> 449, `ir_print.hero` 485 -> 460, `resolve_state.hero` 338 -> 308) and two, `cursor.hero` and `source.hero`, **leaving the table entirely**. From the next commit on, a file here that grows is red again. **What is open**: whether 1083 stands. `print_fmt.hero` is the third-largest file in the compiler and **four of the five defects found in it across these three days were one class** — a measurement or a bookkeeping fact that disagreed with what the printer prints (003's render/valued contract, 004's missing `return ` width and double indent, 004's `last_line` stopping short). That is what a knot too big to hold in one head produces, and it is the argument for the split §11's threshold exists to prompt | selfhost/print_fmt.hero · tests/harness/suite_layout.hero · docs/defects/003, 004 · docs/panel/095 | the ratchet's whole value is that moving it costs a sentence from the author; four raises nobody is asked about is a ceiling nobody has
