- [x] panel 055 | **ANSWERED 2026-08-14 — `M-struct-passing` is order row 1, ahead of `M-selfhost-probe`; ticked 2026-08-15 without being asked.** Both halves the item put to the author are decided and recorded at `DESIGN-LOG.md`:299: it is a **milestone of its own**, and it comes **before** the probe — design.md §1.12's completeness clause over Principle 0's silence, which is the one place §1.12 was written to be able to outrank Principle 0's *neither → it waits*. It is in `docs/ROADMAP.md` § The order row 1 and § Done's name table. What the yes did **not** decide, and stays live inside `M-struct-passing` rather than here: the ffi-pragmatist's prediction that the milestone will not raise raylib past **254 of 600** without it is unscored, and it is the milestone's own close that scores it. Was | panel 049 (3), still open after 055 | **Struct-by-value at the FFI boundary — the measured wall, and the next milestone's question rather than this one's.** **356 of 1159 entry points (31%)** across `sqlite3.h`, `curl/curl.h` and `raylib.h` pass or return a struct by value; raylib alone is **346 of 600**, and §4.19's ladder rung 5 promises *"real struct passing"* in ratified text. No scalar vocabulary touches it: panel 052 struck `f32` and variadics from its own agenda and this is what remained. Principle 0 does **not** fund it — the closure list binds only `hero_os.h`, which has no structs — so it enters, if it enters, on §1.12's completeness clause: *any C library must be bindable, because one Heroes cannot reach is C code the author keeps writing by hand*. The ffi-pragmatist predicts `M-binding-fidelity` will not raise raylib past 254 of 600 without it. **What is yours**: whether it is a milestone of its own, and whether it comes before or after `M-selfhost-probe` — which Principle 0 would put first | docs/panel/049, 052 · design.md §4.19's ladder, §1.12 | the FFI is this language's whole library chapter and a third of it is unreachable
