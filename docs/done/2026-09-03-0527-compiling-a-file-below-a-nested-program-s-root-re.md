- [x] **`M-robustness-guards`, step 6** | panel 099 R5, ratified 2026-09-03 with the sitting; the leaf-test hazard the verdict said it did not settle | **Compiling a file below a nested program's root re-bases every `use` path, in silence.** `heroes test selfhost/lexer.hero` is that invocation today, harmless only while the file's directory is the root. R5 states the rule; it owes a diagnostic that names the file and the root it took, and a golden fixture with two directories that fires it. Measured 2026-09-03: no list carried this (0 hits in SCHEDULED.md and DONE.md) | docs/panel/099 § Author's verdict · selfhost/modules.hero:144 · selfhost/source.hero:175 · selfhost/resolve/top.hero:147-148 | the ergonomist built a program legal under both bases that prints 2 or 3 depending on which one is in force

  **CLOSED 2026-09-03 (M-robustness-guards step 6).** The diagnostic half was already in the message since panel 099 landed (*from the directory of `<file>`, the file you compile*); what was owed was the golden, and it is `tests/golden/surface-fixtures/nested/` with both halves of the rule: from the root the program prints `2` and `20`; the leaf compiled alone is `unknown_module` naming `nested/lib/util.hero` and `nested/lib/leaf.hero`; beside a decoy `lib/util.hero` (`nested/decoy/`) the same leaf checks CLEAN against the wrong module — the ergonomist's program, legal under both bases — while the decoy's root prints `3` and `30`. Four surface rows (`verb_probes` 36 → 40) say which is the rule and which is the hazard, so the historian's standing prediction (Zig #13970's shape) has a place to be scored. Measured on the Mac (surface 96, harness tests 93, net 1372/0), in the Linux image (the four behaviours, surface 96, harness 93) and on the Windows box (the four behaviours, surface 89, harness 93); every fixture file canonical before it was added.
