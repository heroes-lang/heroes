- [x] panel 051 | **CLOSED — the clause is gone from the spec.** The warden's `−8` removal of *"`push` copies the array"* has happened: `spec:170` now reads *"`xs @ xs.push(4)` grows in place while nothing else holds `xs`"*, and the phrase appears nowhere in the document (grep, 2026-08-26). Was: warranted on its own merits — ledger row `2627`'s prediction scored **half falsified** at M-program-corpus because the reader reached for `push` anyway — but **not** spendable to fund panel 051, since the clause belongs to panel 037 and panel 041 refused that cross-funding. It needed its own decision and it got one | spec:170 · docs/measurements/010-spec-budget-ledger.md row 2627 | a removal held hostage to a funding rule it did not belong to
