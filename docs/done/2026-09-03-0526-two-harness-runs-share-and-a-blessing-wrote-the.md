- [x] **`M-robustness-guards`, step 5** | `/decide` 2026-09-03 (author instruction as above, choosing (a)+(c)); found by doing it 2026-08-28 | **Two harness runs share `build/harness` and a blessing wrote the collision to disk.** `hero_os_pid()` joins `runtime/hero_os.h` (`getpid` / `GetCurrentProcessId`; `HERO_RUNTIME_ABI` 18 → 19, ratified in advance), `tests/harness/main.hero`'s `SCRATCH` becomes `build/harness-<pid>` and is removed at exit (it is never removed today, measured), AND `UPDATE_EMISSION=1` in `suite_emission.hero` validates a captured trace before writing it — first line the emitter's header comment — refusing and naming the case otherwise. Test: two harness runs started together, both clean; a doctored capture refused | tests/harness/main.hero:88 · tests/harness/shell.hero:38-53 · tests/harness/suite_emission.hero · runtime/hero_os.h · runtime/heroes_runtime.h:37 | the emission traces are the one instrument whose source of truth no longer exists, and a race could overwrite them while reporting success

  **CLOSED 2026-09-03 (M-robustness-guards step 5), measured on the three platforms before the commit.** `hero_os_pid()` in `runtime/hero_os.h` (`parts/run.c`: `getpid` / `GetCurrentProcessId`), `HERO_RUNTIME_ABI` 18 → 19 in the header, the emitter and the seed's eighth line — a two-phase edit because the seed binary emits the old stamp: emitter first, header second, seed third (`heroes build selfhost/main.hero --emit-c -o seed/heroes.c`), then the one clang line and the fixpoint, the seed-built compiler re-emitting the seed byte for byte. `tests/harness/main.hero`'s scratch is `build/harness-<pid>`, removed on the run that reaches its verdict (a suite that cannot run exits 2 and leaves it); measured with a `layout` run beside the running net — its own directory, removed, the net's untouched. `suite_emission.hero::is_a_capture` checks a capture's first line against the program's own path exactly before a blessing writes, refusing by name otherwise; tested with a doctored stub (refused, nothing written) and a proper one (blessed); the *nothing blessed* test's stub now says the program's own header, since `from x.hero` for a program at `<tree>/x.hero` is the refused shape by construction. 178 traces re-blessed alone (every capture accepted), six `emit/` goldens by `--emit-c -o` with the diff read (the stamp only; `UPDATE_GOLDEN` does not exist, on purpose). Numbers: Mac 551 own tests, harness 93, net 1368/0, fixpoint OK, seed 840,151 lines; Linux (from the new seed) 551, 93, emission 357, run 91, scratch removed; Windows (from the new seed) 551, 93, surface 85, emission 357 after removing 172 `._*` AppleDouble files a macOS tar had shipped (seven false programs — the shipping's artifact), scratch removed.
