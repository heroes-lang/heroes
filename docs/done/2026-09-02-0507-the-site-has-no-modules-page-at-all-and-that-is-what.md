- [x] **M-documentation-site** | author instruction 2026-09-02, *"aggiornare il sito con queste ultime cose delle cartelle"* | **The site has no modules page at all, and that is what the paths feature needs written — not an edit to an existing one.** Measured 2026-09-02: `site/src/pages/docs/` holds **13 pages** (and 13 more under `it/`), and **none of them is about modules** — `use` appears only in passing prose, so a reader learning Heroes from the site never meets the module system, let alone paths, the qualifier, or the absence of `..`. Two further findings from the same measurement: the `selfhost` page's own description says **"165 modules"** and the tree holds **169** today, a number that was already stale before this milestone touched it; and the site's rule that every code block is a real file in `examples/` is satisfiable immediately, because `examples/shapes/` is six modules over three directories that compile, run and pass their own tests. **What is owed**: one new docs page in English and Italian (a file is a module · `use` · the qualifier is the last part · a path starts at the file you compile · there is no climb · last parts are unique), the module count re-measured rather than carried, and the register `site/README.md` § Style guide already fixes. **Two constraints that are not this item's to relax**: publishing anything outward is a hard stop only the author lifts (CLAUDE.md §14), and the site shares this branch with the CNAME, so the work is committed and tagged and **not pushed** without the author saying so. **And a coordination fact measured today rather than assumed**: a second session was editing `site/src/html/` while this milestone ran and committed `20c96ce` mid-session, so whoever takes this item confirms `site/` is theirs before writing in it | site/src/pages/docs/index.astro (the 13-page list with no modules entry) · site/src/pages/selfhost.astro (the 165) · site/README.md § Style guide · examples/shapes/ | the one part of the language a reader of the site cannot learn from the site is the part that decides where their files go

  **CLOSED 2026-09-02 at M-documentation-site** (journal `docs/journal/030-documentation-site.md`, tag `m-documentation-site`). Chapter 13 in both editions, 185 and 195 lines, four figures each, all eight verified against their `data-src` slices at zero drift. **This item's own complaint was already fixed and the number that was actually stale was a different one**: it said `selfhost.astro` claims *165 modules* against 169, and it says **170**, correctly, repaired before the milestone opened. What was stale is the LINE count, ten places across both editions at 49,988 against a measured **50,100**, plus the README's 44 pages against 46 and the ROADMAP's live 50,063. **What the item got exactly right**: the site had no modules page at all, and `examples/shapes/` was ready to be the source. Its two constraints are both honoured: nothing was published, because `site/` shares this branch with the CNAME and that is the author's stop; and `site/` was confirmed as this session's before a line was written, on the author's own instruction plus a measured six hours of quiet on that tree.
