- [x] **reasoning 002 — what "separate heaps" would mean, and the three rules it assumes** | 2026-08-10, the session that asked what a heap is, what a message is, and what carries it | **Settled**: the three questions have consistent answers and §4.10 is why — a value is already self-contained, so the transport can be a plain queue and a message needs no envelope. **Two copy regimes are deliberate and must not be unified** (copy-on-write inside a thread, unconditional copy at the boundary), which the note named as the part most likely to be lost. The non-atomic counter is a **consequence of the concurrency model**, not an implementation detail. **The hand-offs**: Part 7.13 was rewritten to that shape the same day (`design.md:2514-2559`, `DESIGN-LOG.md:109`) with a precedent correction the note did not have — Erlang's refcounted binaries are a cross-heap refcount — and green threads were placed in Part 7 rather than Part 6 **against the draft's wording** (this file, `:155`); the two future-proofing rules were promoted to v1 invariants (`DESIGN-LOG.md:110`) and invariant 1 was then found **false to the letter**, eleven `malloc`s across five files, which is what created `runtime/parts/alloc.c` (`:235`). The model's **width** — data parallelism alone, or the mailbox too — is a panel question at M-isolated-threads, which is open. **The one hand-off of thirty-two that had no home**: whether the two invariants become CLAUDE.md §7 rules with a check that fires. Filed 2026-09-04 in `SCHEDULED.md` at M-isolated-threads, with `suite_order.hero` as the shape | `git show 70e969e5:docs/reasoning/002-message-passing-and-separate-heaps.md` · design.md:2514-2567, :1491-1493 · DESIGN-LOG.md:109, :110, :235 · docs/work/SCHEDULED.md (M-isolated-threads) | the note that shaped Part 7.13, and the only one that left something nothing else was carrying
