- [x] **M-reflection-verdict** | panel 117's sixth option: give `assert` both sides for an aggregate by walking to the first DIFFERING LEAF | **closed 2026-09-11 by panel 131, which REFUSED it on the three measurements this item itself demanded** | The item was written on 2026-09-07 with the words *"three things owed BEFORE it lands, because it is unrun"* — a compiled prototype, the map case, the depth case. All three were run at panel 131 and **two of panel 117's four adoption claims did not survive**, which is exactly what they were owed for. **The prototype exists**: `selfhost/emit/differ.hero`, **160 code lines** in `suite_layout.hero`'s own unit, type-checking clean inside the real module graph — but 160 is a floor, not the cost, because it omits the TyId-keyed array and map satellites (`HeroDesc` carries no field names, so the runtime cannot walk into a record), the `_case_name` tables (nothing in the compiler turns a variant tag back into a case name), four runtime entry points `grep` finds zero of, and the routing through `abort.hero`, whose `builtins.to_str_entry` refuses every aggregate today; realistic total **350-450 code lines across six modules and the runtime**. **The map case**: for the yes-or-no answer the walk is order-independent and that half of panel 117 holds, but two `==`-equal left operands against one right operand give **two different messages** — `m["a"]: 1 != 10` against `m["c"]: 3 != 30` — so the leaf it names is a function of insertion history rather than of the value, which is panel 117's own refusal ground for map rendering reproduced inside the thing it adopted instead. **The depth case**: a leaf is reported to depth 3600 with a path **28,813 characters** long, and at 3700 it is `panic: stack exhausted`, **exit 134**, where today's thin `assert failed: a == b` is correct at exit 0 at depth **100,000**. **And the load-bearing premise is false at the source**: panel 117 adopted it because *"the recursion cliff dissolves — the walk is `eq`'s walk"*, and `HeroEqWork` (`runtime/parts/array.c:185-190`) is `{a, b, elem, len}` with no path, no parent and no index base, draining LIFO, so the first `false` the drain finds is not the first differing leaf in any order a reader can predict. Panel 076's compiler-engineer condition is unmet and **its veto is engaged**, because unlike 2026-08-24 this proposal does put a per-field continuation in the emitter. **What the sitting keeps of panel 117**: two claims that do hold — no `HeroDesc` member is needed, because records and variants are reached statically; and it is not a second copy of what the language is. **The conservative variant is recorded and not adopted**: records and variants only, no recursion through `[T]` or `{K: V}`, about 120 of the 160 lines, no cliff and no order dependence — refused because it buys nothing for an array, and the motivating case in panel 131's own ergonomics experiment is two `[i64]`, as are the compiler's own ASTs. `tests/harness/suite_special.hero`'s aggregate pin therefore **stays, and stays correct**; it was written to go red the day a mechanism lands, and none does | `docs/panel/131-the-refusals-were-sound-and-their-reasons-were-not.md` · `docs/panel/117-both-sides-of-an-assert-when-a-side-is-an-aggregate.md` · `runtime/parts/array.c:185-190` | the withdrawal is `provisional — author ratification pending`: it reverses something the author ratified on 2026-09-07, so it is queued as its own item in `docs/work/DECIDE.md` rather than riding inside a longer yes
