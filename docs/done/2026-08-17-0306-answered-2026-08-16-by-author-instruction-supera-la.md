- [x] panel 062 | M-selfhost-port, measured 2026-08-16 | **ANSWERED 2026-08-16 by author instruction — *"supera la regola per heroes"*: §11's threshold yields, the language rule stands.** The amendment is in CLAUDE.md §11 with the measurement and the reason: `toolchain.rs`'s argument at its limit — there a cut ran *against* a seam, here **no cut exists**, and a rule that cannot be obeyed is not a standard but a lie. The exchange the port owes is written into the rule: each knot carries a module doc naming its cycle, its entry points and its internal call graph. Was | **The grammar cannot be split the way Rust splits it, and CLAUDE.md §11 has to say what happens.** Measured both ways: Heroes' `module_cycle` fires on the **`use` edge**, whatever it carries — two modules that call each other are refused, and so are two that only name each other's types. Recursive descent has two irreducible knots in `crates/heroes/src/syntax/`: **A** (`expr`, `primary`, `control`, `stmt`, `name_stmt`) at **1025 Rust lines**, and **B** (`decl`, `data`, `externs`, `extern_members`) at **736** — 3.4× and 2.5× §11's ~300. Every other file in the directory is cycle-free and ports as its own module. **Nothing is blocked**: mutual recursion inside one module is free, so each knot ports as one long Heroes module and the grammar works. **What is owed is a rule.** The port proposes: the language rule stands (panel 031 R6 chose refusal as the reversible direction, and relaxing it spends the topological order M-separate-compilation needs — the trade 031 already priced), and **§11 yields for a grammar knot with its seam named**, exactly as it yielded for `toolchain.rs` at 400 on 2026-08-14 — where the accepted reason was that the cut ran through a seam, and here the seam is *forbidden by the language*, which is stronger. In exchange each knot carries a module doc naming its cycle, its entry points and its internal call graph, because a reader not drowning is what §11 actually protects. **Alternatives, both worse**: relax the cycle rule (spends M-separate-compilation's ordering, and 031's argument was explicitly about not doing this), or restructure the grammar to break the cycle (changing the compiler's architecture to satisfy a file-layout rule) | docs/measurements/009 § the structural finding · docs/panel/031 R6 · CLAUDE.md §11 · crates/heroes/src/syntax/ | it decides the shape of ~1,800 lines of the self-hosted compiler, and the port stops at the first knot until it is answered
