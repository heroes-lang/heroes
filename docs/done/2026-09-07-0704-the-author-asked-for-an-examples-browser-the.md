- [x] **Every program in `examples/` is a page on the site, and the corpus is the source** | author instruction 2026-09-06, delivered 2026-09-07 | The author asked for an examples browser: the programs on the site itself, never a link out to GitHub, with a hash per example so a changed one can be noticed. **178 pages** where there were 46: an index, one page per program and per gallery file in both editions, and `/examples/index.json`. Nothing is copied into `site/`: the pages are cut from `examples/` at build time, so they cannot go stale, and the fingerprint is there to be quotable rather than to detect drift. Measured: **66** examples, 54 programs and 12 gallery files, **118** `.hero` files, **18,622** lines, all computed from the model so the index cannot state a count the tree does not have; **181** code blocks compared between the two editions, 0 differences, because each file is highlighted once and both pages embed that string; **20** of the 66 declare an `extern`, which `M-online-compiler` will need and now reads from the JSON rather than from a document. The highlighter reads the keyword table from `selfhost/keywords.hero` and the type words from `spec/heroes-spec.md`, so the site holds no copy of the language, and `assertRoundTrip` requires the source back byte for byte: **630** `.hero` files and **95,478** lines pass it. Three defects were found by running it: the after-a-dot rule left `function` uncoloured after a comment ending in a full stop (70 times), the pages coloured `as` as a keyword where `selfhost/parse/use_line.hero` treats it as an identifier, and the character-literal class disagreed with the 160 figures already written | `site/src/lib/` · `site/src/html/examples/` · `examples/README.md` | the corpus was 7 times what the site showed, and the site's strongest asset was unreachable from it
