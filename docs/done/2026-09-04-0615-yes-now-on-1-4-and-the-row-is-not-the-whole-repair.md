- [x] **Does the type table owe a `()` row — now, on a §1 argument, or never?** | panel 003's watch item 2026-08-03, dormant because its condition rested on a metric that has never run, settled at `docs/panel/110` | **YES, now, on §1.4, and the row is not the whole repair.** Written 2026-09-05: *nothing: what a function with no `->` returns; `ok()` is the `()?`*, plus, at `spec:100` where the must-use rule lives, *which a `()` line refuses: it stands alone*. **+36 measured**, spec 3750 → 3786 for that half. **The item's own premise was wrong and reading the document fixed it**: it said a reader meets `()` three times, and the unit **type** appears exactly **once**, at `spec:191`'s `write_file(path: str, text: str) -> ()?` — the other two are empty *parameter* lists. The real hole is larger than the row: `ok(())` is refused because `()` is not an expression, and **`return ok()`, argument-less, is the only way to build a `()?`** while `spec:146` shows only `ok(v)`, so a reader with the document alone could not write a function of the exact shape the document displays. Principle 0 is satisfied by measurement rather than argument: `ok()` appears **19** times in `selfhost/` and **62** across 587 `.hero` files. Three seats set conditions and the wording meets all three — **result position only** (*with no `->`*, which also stops the spec teaching a spelling `heroes fmt` deletes at `print/fmt.hero:521-523`, since under §4.15 a non-canonical spelling in the spec is a contradiction), the **must-use answer** (`print("hi")` alone is legal, `_ = print("hi")` is `error[bound_unit]`), and a row that stays **76 characters**, in line with the table's other nine, with the behavioural rule where behavioural rules live. The blind llm-ergonomist measured what it buys: **7 forced guesses under the old document against 3**, its worst being a confident `str?` result that compiles and is a different API from the one asked for | spec/heroes-spec.md § Types, § Bindings · docs/panel/110 · docs/measurements/010-spec-budget-ledger.md row 3799 | the one thing a spec of 3750 tokens must not do is use a type it never defines
