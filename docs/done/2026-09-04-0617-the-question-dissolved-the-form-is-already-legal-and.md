- [x] **May a parameter list break across lines, and what shape does `heroes fmt` print for a long signature?** | queued as a conservative parser reading 2026-08-04, settled at `docs/panel/110` | **The question dissolved: the form is ALREADY LEGAL, and the item's premise was false.** Measured three ways on 2026-09-04 — by the convener, the spec-warden and the compiler-engineer independently — all three multi-line shapes (parenthesis-then-indent, hanging continuation, four-space continuation) `check` and `run` at **exit 0**, and `heroes fmt` joins them onto one line. `cursor.skip_terminators`, called four times in `parse/members.hero`, is what admits the break. The item had said *"The parser requires `,`"* and reported the newline form through `expected_params_close`; that is true of a **missing comma**, not of a line break with the comma present. So the resolution is **no change and no spec token**, and it is *not* the tabled *"refuse the form"* — which would have been a new refusal of a working one, and which §12 holds to a feature's standard. What the compiler does today is **accepted input with non-canonical output**, recorded as that. **The count the item asked for, and it answered itself**: signatures past the formatter's 120 columns are **167 of 1444 (11.6%) in `selfhost/`**, **3 of 850 (0.4%) in `examples/`**, **2 of 282 (0.7%) in the harness** — a factor of 29, so the pressure is the compiler's alone and the compiler is written, which is Principle 0's answer. The blind llm-ergonomist scored the task a **null result**, 0 of 1 answerable under either spec. And the historian could not find **one** language that forbids the break — Python exempts brackets entirely, **Nim's rule is the one CLAUDE.md §6 says to copy the surface of**, Go permits it and requires the trailing comma its semicolon insertion implies, and Elm was asked for exactly Heroes' rule in 2015 (elm/compiler#978) and kept mandatory commas — reported as a *failed search* rather than an impossibility. Its recommendation, the author's trailing comma steering the layout (`zig fmt` and Black's magic comma converged on it independently, and it preserves §4.15 because the comma **is** the textual difference), is a feature with nothing blocked, and is scheduled at M-guide-book with that seat's page-width prediction | selfhost/parse/members.hero:60-104 · selfhost/print/fmt.hero:77 · docs/panel/110 · docs/work/SCHEDULED.md at M-guide-book | the formatter is the one tool whose silence makes every diff untrustworthy (§9)
