- [x] The self-compilation cost, measured | **ALREADY FIXED, ticked 2026-08-24 without being asked** (`/decide`'s verify-before-asking rule). The repair landed 2026-08-23 as author decision `3c` — option **(c)**, this emitter's own idiom: `descriptor_set` asks the **emitted C** whether anything takes the descriptor's address, the way `emit_unread` already asks whether a slot is read, rather than asking the IR what the program reaches (`DESIGN-LOG` 2026-08-23, commit `d43684b`). Neither (a) nor (b) was taken, so the seed was not re-blessed for it and the 14m05s this item priced was never spent. Verified today by running the exact command the item names: `heroes test tests/harness/probe.hero` → **14 tests, all passed, zero clang warnings**, where it used to print `probe.c:409:23: warning: unused variable 'h_probe_Probe_desc'`. The item stayed open for one day after its own fix, which is the same gap panel 086 R7 was written about. Was | measured 2026-08-18, found by the Heroes harness | **A record's descriptor can reach clang unused, and the generated C warns.** `heroes test tests/harness/probe.hero` → `probe.c:409:23: warning: unused variable 'h_probe_Probe_desc' [-Wunused-const-variable]`, 1 warning, exit 0. The same file under `build --emit-c` defines the descriptor and its three functions (copy, drop, eq) at :249-263 and **uses none of them either** — so the difference is which translation unit clang is given rather than which C is written. CLAUDE.md §7's bar is zero warnings and `tests/harness/suite_warnings.hero` now enforces it over the goldens and the corpus; this file is neither, which is why nothing was red. Not reproduced by three smaller shapes (a scalar record, a record holding `[str]`, a record copied through `out: R @ r` with a pushed field), so the trigger is narrower than "a record with a container" and is not yet named | **The cause is located** (2026-08-18, same session): `descriptor_set.rs::generated` builds its worklist from types the program REACHES — `if !reachable.contains(&id.0) { continue }` — and reachability is not the same question as "does the emitted C write `&{prefix}_desc`". `probe.hero` interns `[Probe]` because `run_table` takes one, the type is reachable, the descriptor is emitted, and nothing in the emitted C points at it. The file's own doc already says this is the failure mode it exists to prevent (`descriptors.rs:101`, and a 2026-08-12 defect with the same shape is quoted in `descriptor_set.rs:40`) — so the worklist is asking the closest question it can ask BEFORE the bodies are written, and the true question is only answerable after. **The precedent for answering it is in this emitter**: `emit/unread.rs` decides whether a slot is read by asking the EMITTED C rather than the IR, which is exactly this shape one artifact earlier (DESIGN-LOG:385) | Three candidate repairs, and the third is this emitter's own idiom: **(a)** emit the descriptor only where something references it, which asks the emitter a question it does not currently ask; **(b)** `__attribute__((unused))` on the descriptor, which is exactly what `emit/unread.rs` already writes for a slot the C never reads and what `extern_complete.rs` writes on its probes — one line, no question about who references what. **(c)** ask the emitted C, as `unread.rs` does — the descriptors are written before the bodies, so it needs the bodies buffered first, which is the same restructuring `unread.rs` already paid for. (b) changes every generated file that declares a record, so it changes `tests/golden/emit/` expectations and **regenerates the seed** (14m05s measured), which is why it is a step of its own rather than a fix folded into another | crates/heroes/src/emit/ · tests/harness/probe.hero · CLAUDE.md §7 · panel 020's zero-warning disposition | the bar is zero because a warning nobody sees cannot falsify anything, and a warning the harness itself provokes is the first one a newcomer will meet
