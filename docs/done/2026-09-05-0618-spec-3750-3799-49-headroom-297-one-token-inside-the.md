- [x] **`docs/panel/110` — ratified 2026-09-05 by standing delegation** | the sitting on four spec silences and one diagnostic tag, which ruled on six questions and found two of them itself | **Spec 3750 → 3799, +49, headroom 297** — one token inside the spec-warden's own registered bound and **26 below the tabled proposal**, which carried two vetoed sentences. What landed, what did not, and every condition met is on the sitting's page; what belongs here is the shape of the day. **Three of the brief's five claims were false about the compiler and every one was caught by a seat rather than by the convener** — that a parameter list cannot break across lines, that `Fix`'s single span blocks a multi-part repair, and that no signature can name a doubly-fallible value — which is CLAUDE.md §1's inference-as-measurement in three fresh costumes, and the reason a full panel was the right lane. Two brief premises about the world were also wrong and the seats falsified them: Kotlin's `T??` **parses and collapses** (`T?? ≡ T?`) rather than being refused, and the historian's own first reading of the Go spec on the trailing comma, which it corrected itself. **The one payment declined is on the record with its reason**: the warden offered **−19** by deleting `spec:176`'s quadratic-concatenation cost model, and it was refused because §15's standing instruction never to slow the compiler is in capitals, the emitter is string-heavy, a concat loop draws **no diagnostic**, and the spec is that fact's only carrier — buying slack we do not need by deleting the one written warning is not a payment. The ergonomist's own offer, ~38 tokens of FFI package illustration, the warden measured at **−49** and declined on a better ground still: `error[ffi_missing_link]`'s note teaches `link` and never says `package`, so the spec's illustration is the only place in the system that tells a reader `package` exists | docs/panel/110-places-not-roots-and-the-repair-that-printed-a-third-answer.md · DESIGN-LOG.md · docs/measurements/010-spec-budget-ledger.md | §4 makes the sitting mandatory before the spec changes, and this one changed the spec twice and refused it twice
