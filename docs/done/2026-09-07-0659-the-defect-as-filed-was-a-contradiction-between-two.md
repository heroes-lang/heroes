- [x] **016 — `assert` drops both sides for every aggregate** | closed 2026-09-07 by panel 117, convened on it by author instruction (*"when in doubt, panelize"*) | **The defect as filed was a contradiction between two artifacts**: `spec/heroes-spec.md:202` promised the source expression *and both sides* with no exception, and the compiler printed neither for `[T]`, `{K: V}`, `T?`, a variant or a record, with no note that they were withheld. CLAUDE.md §12 makes the spec win, so it was the compiler's defect. **The repair is the spec's, and the reason is a measurement no option could have survived**: no mechanism can keep that promise for `{K: V}` at all, because two `==`-equal maps iterate in different orders — `{"a":1,"b":2,"c":3}` gives `b a c`, the equal `{"a":9,"a":1,"b":2,"c":3}` gives `b c a` — and sorting is refused by `runtime/parts/sort.c`, which picks a comparison by descriptor pointer identity over twelve scalar descriptors, so a map with record keys has no order to print in. A promise false for one kind **by construction** is worse than a stated limit, and the sitting's four options were all arguing about the wrong axis. **The repair.** `spec:202` now reads *"An `assert` failure shows the source expression, and both sides where `print` takes them; where a side is an aggregate it shows the expression alone, until this language renders one"*, with `print`'s own row stating the renderable set once. 3871 → **3903** tokens, net +32 against a re-measured −16 removal, `SPEC_TOKENS` repinned and the ledger's row carrying every figure. The clause *until this language renders one* is JEP 306's shape, which is why the historian read C++'s N1426 and P2186R2 as credibility rather than retreat: a narrowing with its restoration condition written down is reversible and is read as honesty. **And the instrument that should have caught this was blind exactly where the defect was**: `tests/harness/suite_special.hero` pinned `left:  4` and `right: 5` for a scalar `assert` and had **no aggregate row at all**. It has one now, pinning today's behaviour on purpose, so the day a mechanism lands it goes red and the narrowed sentence's expiry is found by a check rather than by a reader. **What is NOT closed and is scheduled rather than owed**: the mechanism itself, the sitting's own sixth option — a walk to the first differing leaf, naming the field instead of rendering the value, which the historian found in Zig's `std.testing.expectEqual` and which answers every objection the sitting raised by construction. It is an item at M-reflection-verdict, unrun, with a compiled prototype and two measurements owed before it lands. **Two options were refused with compiled evidence and the refusals are the sitting's**: a sixth `HeroDesc` member makes the first aggregate `assert` die `SEGV on unknown address 0x0 (pc 0x0)`, strictly worse than the defect; and a per-type renderer is vetoed by one seat on the ceiling while another seat compiled it and approved it, a disagreement the panel file states rather than resolves | `docs/panel/117-both-sides-of-an-assert-when-a-side-is-an-aggregate.md` · `spec/heroes-spec.md` 187, 202 · `tests/harness/suite_special.hero` · `docs/measurements/010-spec-budget-ledger.md` | the spec is the prompt, so a sentence in it that the compiler cannot honour is a wrong briefing rather than a loose promise
