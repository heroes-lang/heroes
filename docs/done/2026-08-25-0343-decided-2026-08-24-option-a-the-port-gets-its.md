- [x] The self-compilation cost, measured | **DECIDED 2026-08-24, option (a) — the port gets its Windows path and the leg covers the whole net again** (author answer `a1`, `/decide`, taken together with `3a` because (a) is what `3a` built). Every `if: runner.os != 'Windows'` is gone from `.github/workflows/ci.yml` — the seed build, `doctor`, the compiler's own tests and the net all run on the third platform, and the `::warning::` that announced what the leg had stopped covering is now a `::notice::` naming the one thing it genuinely does not: the debugger, because `lldb.exe` exits `0xC0000135` before running a command. (b) — dropping the leg — is refused because what (a) buys is measurable rather than notional: `INT64_MIN % -1` and the ASan leak detector already differ across the two legs that exist, so a third is a third instrument. (c) is what today replaced. **UNTESTED ON WINDOWS FROM HERE, and it is written into the file rather than left implied**: nobody in this project has a Windows machine, so the first tag run after 2026-08-24 is this decision's instrument. Two platform differences are predicted and handled instead of discovered — `shell: bash` on every step, because `windows-latest` defaults to `pwsh` and these are bash scripts, and a `matrix.exe` suffix on the built binary, because clang honours `-o` exactly and Windows will not execute a file without the extension. A red leg naming anything else is the finding. Verified here rather than assumed: the YAML parses and reports **10 steps**, and `grep "runner.os != 'Windows'"` returns nothing. Was | M-bootstrap-archive, 2026-08-19 | **The Windows CI leg now covers the toolchain and nothing else.** It ran the four cargo steps; they left with `crates/`, and the Heroes steps cannot replace them there because `selfhost/cli_io.hero` binds `extern "unistd.h"` — there is no self-hosted compiler on a platform without POSIX headers. The leg still runs `clang --version` and reports what is missing, and it now prints a `::warning::` saying what it stopped covering, because a green job that tests nothing is what CLAUDE.md §9 calls a decoration. **Three honest answers and the choice is yours**: (a) give the port a Windows I/O path — the externs are `read_file`/`write_file`/`getenv`/`system`, and the runtime already compiles under a Windows target (panel 049); (b) drop the leg and say in `ci.yml` that Windows is unsupported for the compiler and supported for the runtime; (c) keep it as a reporting leg, which is what it is today. What (a) would buy is measurable rather than notional: the whole net on a third platform, which is where `INT64_MIN % -1` and the ASan leak detector already differ | .github/workflows/ci.yml § the matrix note · selfhost/cli_io.hero | the third platform was added by author instruction, and the archive quietly took most of it away
