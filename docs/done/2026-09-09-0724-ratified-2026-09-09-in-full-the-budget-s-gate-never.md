- [x] **panel 123** | ratified 2026-09-09 in full (`1a 2a 3a 4a`): the budget's gate never touches the network, one pinned model id binds, `--refresh` prints for a human to paste, **the 2000-token soft line is retired** as the removal that pays the raise, and the 38 ledger rows are renamed rather than restated | `docs/panel/123-the-vendored-half-that-never-changed.md` § Resolution · § Author's verdict | **The soft line was never once slack**, which is why retiring it is stricter and not looser: on the real instrument the spec has been above 2000 since v0, so its branch was unreachable while §1.6 forbade rescaling it. The payment rule is now UNCONDITIONAL at every level. **The floor is withdrawn as a CORRECTION and not a decision** — nothing had been decided on it, and the author had been told the spec lives in a band with both ends measured when one end was a vendor's price list about a different kind of request. And the resolution had already landed: `--refresh` shipped in `757a14af` and **reproduced the hand-measured record exactly**, 5128 on `claude-opus-5`, digest `6022a8bc2f16ca0e`. This sitting's own ordering veto is satisfied, verified at `design.md:256` and `:277`
