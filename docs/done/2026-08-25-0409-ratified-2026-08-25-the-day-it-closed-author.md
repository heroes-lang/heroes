- [x] panel 091 | **RATIFIED 2026-08-25, the day it closed** (author instruction, *"ok ratifica"*). Nothing is pruned; +0 spec tokens, +0 compiler lines. The yes closes panel 087's queued half — a new `extern constant` costs **+2 lines in exactly one translation unit**, not in every program in the language — and scopes the refusal to **reachability**, never to placement: declare-site emission is already acceptance row 1's consequence and needs no new sitting. What it does not settle is written into `docs/panel/091` § Author's verdict, including the prediction that would overturn it. Was | panel 091 | **Ratify A: nothing is pruned, and the duplication dies with the architecture.** The sitting was convened on a real number — `print(1)` emits 166 lines of C, 27 for a library `extern` group it never touches, and 138 of 155 modules would carry it under one `.c` per module — and every option that prunes was vetoed with compiled evidence. What replaces them costs nothing: the library is **one module**, its externs are called only from inside it, and one `.c` per module plus acceptance row 1 puts every group in **exactly one TU**. Option B would have cost **+40 compiler lines and 146 of 146 re-blessed emissions** to buy what the milestone's close gives away, and it was separately vetoed because a runtime with `hero_file_write`'s parameters swapped and the ABI stamp unmoved is caught **by the library's 19 lines and by nothing else** (panel 037's hole). **The clause with the least precedent is named first**: no language surveyed prunes FFI verification by use, and the two that verify against real headers both emit at the declare site — so this is the majority position rather than a departure. A yes closes panel 087's queued half: **a new `extern constant` costs +2 lines in exactly one translation unit, not in every program in the language** | docs/panel/091 · docs/panel/087 § Queued | the sitting refused its own convener's premise twice and the answer got cheaper each time
