- [x]  **CLOSED 2026-09-07: it held, and it held TWICE.** Step 4 landed the checker half and the release in one commit for exactly the reason this item priced. Then the `@` position repeated it on 2026-09-07 with a different clang message — `passing 'h_0opt_… *' to parameter of type 'char **'`, exit 2 — so `handed_back_param` was written, measured, parked, and landed with `release_cells` in one commit. **The item's own prediction of the blocker was right and its count was low**: giving `lower_args` the callee touched **nine** call sites, not six; the four it missed construct a record, a case, `ok`/`fail` and a built-in, and each takes an absent callee because none can carry a mark. Was: **M-declared-freer**, at its step 4 — the two halves of the mark must land in ONE commit, measured 2026-09-06 by trying the other way | M-declared-freer step 3, while scouting step 4 | **The checker half alone makes a program that type-checks and then blames the compiler.** §4.19 says a marked `cstr` result *"hands it over as a `str?`"*, so `user_call` was made to report `str?` where the declaration says `cstr` — 6 lines, and both directions read correctly: `m.default("none")` type-checks on a marked call and is `error[bad_operand]: `default` takes a fallible value, found `cstr`` on an unmarked one. Then the same program was **built**, and clang said `assigning to 'h_0opt_…' from incompatible type 'const char *'` — `internal error`, **exit 2**, which CLAUDE.md §7 calls the compiler blaming itself for a `.hero` file. The change was parked rather than committed. **What this pins for the step**: the type change and the emission are one commit or none, and the emission is the hidden cell, the `validated` copy, the NULL branch and the freer call that panel 109's compiler seat hand-emitted as twelve lines of C and ran leak-free. The mark must reach the IR first — `ast.FunctionDecl.owned_result` and `ast.Param.owned` stop at the AST today — and `selfhost/check/freer.hero` already holds `marked_as_freer`, which is where `handed_back` belongs beside it | `selfhost/check/walk.hero` § user_call (two sites, plain call and method) · `selfhost/check/freer.hero` · `docs/panel/109` § The `cstr` half | a form that type-checks and then fails at exit 2 is worse than a form that does not exist
