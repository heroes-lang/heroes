- [x] **`records/tagged` read the substring `panel ` and so read a sitting's FINDING as its ratification** | found 2026-09-08 by `/decide` while zeroing `docs/work/DECIDE.md`, reading the executor rather than the rule; repaired the same day on author instruction (*"yes, that thing is worth it, handle it right away"*) | **The rule was never the lenient one.** CLAUDE.md § Verification, author instruction 2026-09-08, says a milestone is tagged only over *nothing open in `docs/work/DECIDE.md` that is not a `panel NNN` ratification*; the check asked `strings.contains(text: row, needle: "panel ")`, which accepts `- [ ] **panel 118 finding**`. The two are not the same thing and the check's own failure message says why: a pending sitting is waiting on the **author**, which is what § 4 means by *a panel never blocks*, while a finding is a decision the milestone could have settled and left hanging. **THE MEASUREMENT RATHER THAN THE OPINION**: `m-closures-verdict` was placed over three `panel 118 finding` items, and all three were settled by one `/decide` session — two of them without needing the author at all, and one had been answered by design.md §1.12 before it was asked. **The check now reads the ORIGIN FIELD**, `**panel NNN**` and nothing more, which is the padded spelling `DECIDE.md`'s own preamble already promises for `queued`'s sake. **And it binds only on a tag whose own tree carried the rule**, which is that neighbourhood's own comment turned into code instead of left as a sentence — *a rule cannot be applied to a commit made before it existed*, and a record is not rewritten (§14). The alternative was a constant naming the grandfathered tag, refused because this same file carries `exemptions` precisely because a dead entry sat for weeks reading like a live rule; asking the tree retires itself. **The three outcomes are kept apart deliberately**: the marker present is strict, absent is the historical reading, and the executor **unreadable** at that tag is a reported failure, because collapsing the third into the second is how a rename would weaken this check in silence. **THE FIRE TEST, RUN AND NOT INFERRED, AND IT IS A PAIR** (CL-061): a commit was built off HEAD with `git commit-tree` over a scratch `GIT_INDEX_FILE` — no branch switch, no working-tree change, nothing staged — tagged `m-zzz-probe` so it sorted newest by creatordate, and the suite run on the real tree, which CL-050 requires since a copy without `.git` cannot run this suite at all. With `**panel 118 finding**` open at that tag: **records 14 passed, 1 failed**, `records/tagged` naming the row and reporting *Applied: the strict reading*. With `**panel 121**` open at the same tag, one word different: **15 passed, 0 failed**. Tag deleted, newest back to `m-closures-verdict`, **15 passed, 0 failed** in 18.92 s. The unit half is two new tests, 122 → **124**, covering the provoking row and eight shapes beside it: no bold field, unclosed, empty, a milestone origin, `panel ` with no number, a word for a number, a signed number, and `panel 118 R5` | `tests/harness/suite_records.hero` § tagged, `carried_the_rule`, `is_ratification` · `CLAUDE.md` § Verification · `docs/work/DECIDE.md` § The shape | the contract said one thing and its executor another for as long as both existed, and the gap was invisible in the only direction that matters: the check passed
