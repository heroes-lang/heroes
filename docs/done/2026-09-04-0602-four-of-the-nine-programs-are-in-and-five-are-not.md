- [x] **M-corpus-depth**, the remaining programs of its own plan | measured 2026-09-03 at step 5's close | **Four of the nine programs are in and five are not**, and the two that need a decision are named here so the next session does not re-derive it. **In**: `fannkuch/`, `binarytrees/` (step 3, two published oracles each), `checksum/` (step 4, three documents), `interpreter/` (step 5, eleven modules). **Left**: `nbody/` and `spectral/` — both want `sqrt` from `math.h`, so both are FFI and both must run on the Mac, the Linux image and the Windows box before their commit (CLAUDE.md § Commands), and **`link "m"` is unmeasured on Windows** because `gallery/08-ffi.hero` is never run by any suite, so the box decides `nbody/`'s spelling before it is written; `query/` — 800 lines, a deterministic generator writing 5,000 CSV rows, `select … where … order by`, one join, three aggregates, copy-on-write at volume under `--sanitize`, `sort` on `bool`, a record-keyed map — needs no platform and can be written next; `ledger/` — sqlite at program scale, ≥ 15 functions bound, sqlite's aggregate against Heroes' over the same rows — needs the three platforms too. **And the milestone's own two instrument items are still open above**: the fourteenth `mutate` operator (which moves the thesis's denominator, so it is scored once against the widened corpus) and the stdin item. **One requirement of the plan was NOT met and the reason is measured**: the 500-deep expression is at 60 in `examples/interpreter/`, because 500 does not work in any configuration — the ceilings are in that program's own comment and the cause is the item above this one | docs/work/SCHEDULED.md (the milestone's brief) · examples/interpreter/main.hero (the depth comment) · docs/measurements/014-mutate-over-thirty-five.md | half a plan with the other half re-derived from scratch is how a milestone loses a week

  **CLOSED 2026-09-04 (M-corpus-depth).** The five programs it listed as left are in: `nbody/` and `spectral/`, the `math.h` pair whose `link "m"` this item said the Windows box had to decide before they were written; `query/`, which needed no platform; and `ledger/`, which needed all three. The two instrument items it pointed at closed with the milestone. **Its one open prediction was right and stays right**: the 500-deep expression is still not 500. `examples/interpreter/main.hero:154` runs at **83**, and the program's own comment carries the reason — panel 106's repair took the ceiling to 314 at `-O0` and 166 under `--sanitize`, and `--sanitize` is what tops out first. The cause this item named — every local hoisted to the prologue, so a frame is proportional to the whole body rather than to what is live — is the item filed beside it, and it is now scheduled at **M-qbe-backend**, the milestone whose own warrant is that a second backend is what turns *the IR is target-agnostic* from an assertion into a measurement.
