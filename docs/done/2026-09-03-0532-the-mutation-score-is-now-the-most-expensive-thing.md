- [x] **Which legs run `heroes mutate examples`, and on which events** | CI run 33781889835, 2026-09-03, the first full three-leg run since the step landed in `f3b0b20d` that morning | **The mutation score is now the most expensive thing in CI, and two of its three runs produce a number that is already known.** Measured in that run: Linux 1294s and Windows 1908s for the step alone, against 586s and 1474s for the whole net; the leg totals are 35m37s and 59m49s, where the last full run before the step existed was 20m. And the three tables are **identical byte for byte** — Linux's, Windows's, and one measured the same day on the author's Mac from a clean scratchpad checkout (666.37s, `/usr/bin/time`): 78 programs, 10878 mutants, 96% killed by `check` and 83% under `--permissive`, every per-operator row equal. `diff` says so on both pairs. So Windows spends 31m48s to reproduce Linux's number. **The default while this is open** is the expensive one: every tag and every `workflow_dispatch` pays ~32 minutes of Windows and ~22 of Linux for one table. **Three shapes.** (a) The step runs on **one leg only** (Linux, the cheap one) — Windows falls from 60 to 28 minutes, and nothing is lost unless the score is platform-dependent, which this run measured it is not. (b) A second `workflow_dispatch` input, `mutation`, default `no`, so a three-leg run is ~14 min Linux / ~26 min Windows and the full score is paid at a **tag**, i.e. at a milestone close where the wait costs nothing. (c) Both: one leg, and gated on tags plus an opt-in input. **Recommended: (c)**, with (a) as the half that pays for itself immediately — the identity of the three tables is the whole argument, and it is a measurement rather than a guess. A **second workflow** is refused whichever lands: it would duplicate 19 steps that then diverge in silence. **The same question in the other direction, and it belongs here rather than in a second item**: a plain push runs the net (591s) but **skips `The compiler's own tests`**, which cost 115s — a push leg is 10m54s, so the 536 tests would be 2 minutes on 11, and today a change that reddens them is invisible until somebody dispatches or tags. Recommended: add them to the push leg | `.github/workflows/ci.yml` §§ the `if:` on `The mutation score over the corpus` (:510-513) and on `The compiler's own tests` · the run's own logs · `docs/measurements/014-mutate-over-thirty-five.md` | the step that made CI's slowest leg twice as slow, measured against what it actually tells us that another leg does not

  **ANSWERED 2026-09-03 by the author, `1c` plus the push half, and applied in the same session** (M-corpus-depth step 4's decisions commit). Both halves landed in `.github/workflows/ci.yml`: a `mutation` choice input, default `no`, whose own comment carries the three measurements above; and the step's condition is now `(github.ref_type == 'tag' || inputs.mutation == 'yes') && (matrix.name == 'Linux x86-64' || (github.event_name == 'workflow_dispatch' && inputs.only != 'all'))`. **The second half of that condition is not decoration**: it keeps the one door the decision would otherwise close — a dispatch narrowed to a single leg (`only: windows`, `mutation: yes`) still scores on that leg, which is how the platform-independence this decision rests on can be re-measured the day somebody doubts it. A tag cannot take that door, because `inputs.only` is empty on a tag push and an `!= 'all'` test alone would put the step back on all three legs. And `The compiler's own tests` lost its `if:` entirely, so it runs on every event including a plain push: 115s in that CI run, against a 10m54s push leg, for **553** tests (counted on the author's Mac the same day, 113s there) that a push could not see. The count in the question above says 536, which was the number when it was written; it is left as written (CLAUDE.md §14) and corrected here.
