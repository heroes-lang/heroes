- [x] **`records/citations` asked the disk instead of git's ignore rules, CI was red for three runs, and this Mac was the one machine that could not see it** | found by the author 2026-09-07, repaired the same day | **The defect**: the check forgives a citation to a path git ignores and asked which those are with a listing of untracked-and-ignored files, which reports only the ignored paths that HAPPEN TO EXIST. Three build and cache directories exist on a machine that has built the site and made a worktree, so **twelve live citations resolved here**, while a fresh checkout has none of them and the same twelve read as dead. It is CLAUDE.md §11's narrowing rule broken inside the instrument that enforces CLAUDE.md's citations. **When**: the check read TWO documents until 2026-09-06, the widening to the whole tree landed that evening, and CI went red on that push. The commit that made the check honest is the commit that made it red, which is the check working. **The repair** is `git check-ignore`, which answers for a path that is not there, and the disk-based oracle is deleted rather than kept beside it. **Three platforms, one defect each.** Linux, in the container of the CI leg's own architecture on a tree tarred without those directories, cut twelve to FIVE, all of them bare directory names: the ignore patterns carry a trailing slash, which means *a directory only*, and git decides whether a path is a directory by looking at it. A file inside such a directory matched all along, because its path carries the directory as a component in the string itself. So every token is now asked twice, as written and as a directory. Windows then found `panic: a command line reached 256 arguments`, which is the runtime's own argument ceiling: two words per token makes 127 tokens the whole budget, and neither of the other machines had enough unresolved citations to reach it. Batches of 100 now. **Verified**: Mac records 14 of 14, Linux container 14 of 14 on a fresh-checkout shape, Windows 14 of 14 at the current commit with those directories absent | `tests/harness/suite_records.hero` § ignored_by_git · `docs/environment/linux/LINUX-MACHINE.md` · `docs/environment/windows/WINDOWS-MACHINE.md` | three CI runs red while every local instrument said green, which is the exact case the three-platform rule was written for
