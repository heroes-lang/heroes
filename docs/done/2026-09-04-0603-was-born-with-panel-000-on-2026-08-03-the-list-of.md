- [x] **The panel watch list is retired: nineteen items, ten closed, three homed, six filed** | author instruction 2026-09-04, translated for the record (CLAUDE.md §11): *"What is OPEN-QUESTIONS.md? Perhaps its items should be moved into a panel to be held. Remove that file"* | **`docs/panel/OPEN-QUESTIONS.md` was born with panel 000 on 2026-08-03** — the list of sittings awaiting the author, plus a watch list of the conditions and gaps the judges left on the table — and it was last touched 2026-08-16. Its top half was already empty: all four sittings it opened were decided 2026-08-03. **Why it goes rather than gets tidied**: it was a fifth list in a second notation, which CLAUDE.md §3 calls invisible to every count in this project, and no skill opened it — `/decide` reads `DECIDE.md`, `/step` reads `SCHEDULED.md`, the net reads this file. The proof is not theoretical: the 2026-09-03 session that gave homes to the chain's gaps read **two of its nineteen items**. **Every item was verified against the repository before it was moved** (`DECIDE.md`'s own verify-before-asking rule): **ten closed** — the spec-v0 header paragraph, false in all three of its claims (v1 landed 2026-08-04, the 53-word comment was cut in the same commit, the budget is 4096 and not 1500); `reserved-words.md`'s budget scope (panel 035 R5); o200k (author decision 2026-08-26, `8c`); the M6 closure-list audit (`docs/measurements/003`); the `ORDER:` marks, now an executing check; canonical `f64` rendering; three items already struck in the file; and panel 010, **closed as a decision and reopened as defect 011 by the probe this session ran**. **Three had homes**: the closures/C-ABI condition (M-closures-verdict, and the citation now points at `docs/panel/013:185-189`, where the sitting itself wrote it), raw string literals (M-deferral-ledger, whose `design.md:2665` was stale — wart 15 is at `:2673`), and the reserved-word collision, which got its home the same day from panel 109 — that one is now an item at M-declared-freer, since 109's `owned <C function>` is the first word in the annotation vocabulary `design.md:2120-2125` had reserved and this item had been deferred to. **Six were filed**: four questions to `docs/work/DECIDE.md` (the `T??` sentence, the `()` row, the `constant`-body sentence, the long-signature shape) and two to `SCHEDULED.md` at M-thesis-harness (panel 007's two unscored predictions, and the compiles-but-wrong-output bucket panel 009's ergonomist made a condition of its own vote). Text at `git show e5f3a828:docs/panel/OPEN-QUESTIONS.md` | docs/panel/OPEN-QUESTIONS.md (deleted) · docs/work/DECIDE.md · docs/work/SCHEDULED.md · docs/work/DEFECTS.md 011 | a list nobody is obliged to open is write-only, and this one had been write-only for nineteen days while carrying six live questions and a ratified rule the compiler never got
