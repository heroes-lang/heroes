- [x] **A mark naming a freer nobody declared was `internal error` at exit 2, on three shapes** | found and repaired 2026-09-07 at M-declared-freer step 5, while scouting the `@` cell; panel 109's condition 1 is what it implements | **The symptom, measured on all three before anything was changed**: `-> cstr owned bogus_name` (a typo), `owned my_free` naming a **Heroes** function of the same program, and `owned odd_free` naming a two-parameter `extern` its own header genuinely declares. Each one type-checked — the result became a `str?` — and then the lowering found no usable freer, fell back to a plain call, and left clang saying `assigning to 'h_0opt_…' from incompatible type 'const char *'`. **Exit 2 with `internal error`, which CLAUDE.md §7 defines as the compiler blaming itself for a mistake in a `.hero` file.** **Why the probe could not see any of them**: step 4a's repair types a freer's parameter `char *` so the header refutes a wrong one, and a probe is emitted **per `extern`** — a name no `extern` declares has no probe at all, and a two-parameter freer's probe compiles perfectly, because the probe calls it with two arguments while the release calls it with one. **The repair is `check/freer.hero`'s `mark_refusals`, called from `check/decls.hero` on every `extern` declaration**, and it reads the DECLARATIONS only: `error[unknown_freer]` when no `extern` of this module declares the name, `error[freer_arity]` when one does and takes other than one parameter. The caret is on the mark, never on the freer's own declaration, which is the same choice panel 116 made for `ffi_freer_type` and for the same reason — the declaration is usually correct and the mark is the mistake. The Heroes-function shape gets its own note, because the repair there is not *rename it* but *a freer is C's*. **THIS module and not this group**: the release is a C call emitted in the marked declaration's own unit, and that unit includes only its own groups' headers. It is a `check` case on purpose (`tests/golden/check/owned-freer-must-be-declarable.hero`), which proves the refusal rests on declarations rather than on a header being present — so it fires in a bindings module nobody has compiled | `selfhost/check/freer.hero` § mark_refusals · `selfhost/check/decls.hero` · `tests/golden/check/owned-freer-must-be-declarable.hero` · `docs/panel/109` condition 1 | three ways of mistyping one word all reached clang, and the milestone is about a mark whose whole job is to be trustworthy
