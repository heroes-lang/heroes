- [x] **M-corpus-depth, step 1 — before any program is written, because every score after it depends on it** | found 2026-09-03 by step 0's own measurement, the first `heroes mutate` over `examples/` since 2026-08-13 | **`heroes mutate` cannot score `examples/` today: it is refused at exit 2 in 2.10 s, and has been since `examples/shapes/` landed on 2026-09-02 (`c12de74`).** `mutate` walks every `.hero` under the directory and checks each one **from its own directory**, and two of them are modules below a nested program's root: `examples/shapes/geom/area.hero` (`use geom/point`) and `examples/shapes/render/ascii.hero` (`use geom/point`, `use scale`), which alone are `error[unknown_module]` while `heroes check examples/shapes/main.hero` is exit 0 — panel 099 R5's leaf hazard, met by the one caller that walks leaves by construction. Nobody saw it for a day because `mutate` over `examples/` ran in no CI leg. **The shape**: a file's program root is the nearest directory at or above it, no higher than the corpus directory, holding a `main.hero`, and a mutant of a module is checked with that root as the base of its `use` paths. Measured meanwhile as the provisional baseline: the sum of one run per directory over 35 of 36 directories, verified exact on the gallery | selfhost/cli/mutate.hero · selfhost/mutate/score.hero · docs/panel/099 § Author's verdict · docs/measurements/014-mutate-over-thirty-five.md | a metric the thesis rests on has been unable to read its own corpus for a day, and the instrument that would have said so does not exist

  **CLOSED 2026-09-03 (M-corpus-depth step 1, the same evening, with step 2 in the same commit).** `score.Program` gains `checked_as`, and `cli/mutate.hero::checked_name` computes it — the nearest directory at or above the file, no higher than the corpus directory, that holds a `main.hero`, plus the file's own name; the module's text is compiled under that stand-in, so `modules.load_text` reads its `use` paths from the root exactly as the compiler does, while the survivor listing keeps the real path. `parent_of` and `with_slash` do the walk; two unit tests run over panel 099 R5's own `tests/golden/surface-fixtures/nested/` — leaf to root, the root's own files unchanged, the **nearest** root winning on the decoy program, the walk stopping at the corpus directory, a trailing slash changing nothing, and the gallery's no-root shape. The alternative — checking the whole program with the module's text substituted — was not taken: it would change what a mutant of a flat program measures and multiply the cost by the program's size. **Verified**: `heroes check` on the leaf alone still refuses (R5 untouched); the compiler's own tests **553, all passed**; the net **1374 passed, 0 failed** (surface 96 → 98 with step 2's rows); the net's own tests **93**; the seed regenerated by the fixed compiler (42.5 s) and the fixpoint held byte for byte (840,151 → 840,806 lines), `./heroes` rebuilt from it. **And the number the step exists for**: one run over all 78 files, **679.26 s, 10,878 mutants, 10,260 killed (96%) strict, 8,872 (83%) permissive, 479 survivors listed** — measurement 014 § Appended, where the *under 680 s* prediction is scored as held by 0.74 s for a falsified reason, and where the survivors were read: 18 `shadow` are `_ = call` lines repeated, legal by `spec:128-129`, and **9 `drop-question` are a finding** — `_ = f()?` → `_ = f()` compiles — filed in `DECIDE.md`.
