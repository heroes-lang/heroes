- [x] **The seed was two repairs behind, `main` has been red since, and CI runs that leg on every push** | found 2026-09-05 while verifying M-isolated-threads step 0 against `heroes-from-seed` | **Measured, in this order.** `git log -1 -- seed/heroes.c` is `a8b6c02b`, and two commits after it changed `selfhost/`: `44930209` (defect 011, `selfhost/resolve/walk.hero` and `writes.hero`, 316 lines) and `d345e266` (defect 012, `selfhost/emit/gate.hero`). `seed/README.md` § When this file must be regenerated states the rule with no qualifier — *"if the diff touches `selfhost/`, the seed is regenerated in the same commit"* — and neither commit did. So the compiler a clean checkout builds still carried **both defects**, and the two goldens that pin them failed as the defects themselves: `check/fixedbugs-two-mutable-arguments-of-one-place` expected four `aliased_mutable_arguments` diagnostics and got **zero**, and `unsupported/fixedbugs-a-unit-parameter-reached-clang` expected `unsupported[unit_parameter]` and got clang's `argument may not have 'void' type` at exit 2. **And it is not a local-only red**: `.github/workflows/ci.yml`'s *The net, in Heroes* step (`:508-510`) carries **no `if:`**, the `test:` job carries none (`:111`), and the workflow triggers on every push to `main` (`:40-42`) — while `origin/main` is `bb8728ac`, the same commit as HEAD. A neighbouring comment (`:523-526`) calls *"the step above"* tags-only and which of the two steps above it means is ambiguous; what is not ambiguous is that neither has an `if:` today. **The repair, with the fixpoint run rather than assumed**: C1, the committed seed's own compiler, emitted today's `selfhost/` to S1 in **21.13 s**; clang built C2 from S1 in **2.68 s**; C2 emitted `selfhost/` again to S2 in **21.89 s**; `cmp` says **S1 == S2, byte for byte**, so S1 is the fixpoint and is now `seed/heroes.c` — **22,065,272 bytes against the committed 21,955,178**, +110,094, which is the two repairs. The compiler's own 572 tests pass on it, 34.82 s. **The whole fixpoint cost 46 s**, where `seed/README.md` still prices it at *"15m41s measured 2026-08-19"*: that sentence keeps its date (CLAUDE.md §14) and this number stands beside it, because it is the arithmetic that put the `cmp` on tags only. **No new instrument is added and the reason is measured rather than lazy** — it is the `DECIDE.md` item this leaves open | seed/README.md § When this file must be regenerated · .github/workflows/ci.yml:40-42, :111, :508-510 · tests/golden/check/fixedbugs-two-mutable-arguments-of-one-place.hero · tests/golden/unsupported/fixedbugs-a-unit-parameter-reached-clang.hero | the seed is the one artifact that lets a clean checkout build anything at all, and a stale one means every job after it tests a compiler nobody wrote
