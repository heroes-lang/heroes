- [x] **`compile "gfx.c"` — panel 036's deferral has found its acceptance test, and it is the author's own question** | author question 2026-09-06, *"if in future I want a graphics library wrapping three different libraries underneath, how will I do it?"*, asked while panel 114 was being written up | **The question is answered for the thin case and open for the real one, and the split is exactly where panel 036 left it.** THIN: one header the package ships, `#ifdef` inside, `static inline` wrappers — measured 2026-09-06 on macOS and on the Windows box, one `.hero` source, exit 0 on both, and `--emit-c` carries zero platform words. That is how every portable C library is already written: **SDL is literally this case** — Cocoa on macOS, X11 or Wayland on Linux, Win32 on Windows, one header, and `examples/sdl/main.hero` binds it today with no platform word anywhere in the file. The `link` half is `package`, and panel 050 said it *subsumes the platform axis panel 049 refused while putting no machine's name in any program*. REAL: a wrapper with hundreds of lines of implementation wants a `.c` file the compiler builds, and that is `compile`, which **panel 036 DEFERRED rather than vetoed** — `docs/panel/036:275`, *"not refused on their merits but deferred for want of an acceptance test. They wait on a rung that needs a shim."* Correction found by panel 114's spec-warden, 2026-09-06: the record had been read as a veto. **The author's `gfx` is that rung.** What to decide: whether `compile` lands, and if so whether it is a group clause (`extern "gfx.h" compile "gfx.c"`) or a package-level fact — panel 036 P2's own veto was of the group clause, so the second shape may be the one that was never judged. Not urgent and not this milestone: nothing in the closure list needs it, and the thin case covers everything the corpus does today | docs/panel/036-*.md:275 · docs/panel/050-*.md · docs/panel/114-the-question-was-not-which-platform.md · examples/sdl/main.hero · selfhost/cli/libraries.hero | the one question a Heroes user will ask that today has a good answer for small libraries and no answer for large ones, and the deferral that covers it has been waiting for a witness since August | **SETTLED 2026-09-06 by author instruction** (*settle every open decision with the most robust and safest solution*), and **the robust settlement was to NOT convene a sitting**, because the answer was already in the record from three hours earlier. **`docs/panel/114` R6, ratified the same day**: *"`compile "shim.c"` was deferred, not vetoed, and its acceptance test has arrived … the rung came, and the finding is that a header of `static inline` functions needs no `compile` at all. That deferral can be closed."* Panel 088 re-derived panel 037 for want of this check; CLAUDE.md § Panel says to grep the record before convening, and here the grep answered. **So the THIN case is closed with a no-form answer**: one header the package ships, `#ifdef` inside, `static inline` wrappers — measured on macOS and on the Windows box, one `.hero` source, exit 0 on both, `--emit-c` carrying zero platform words. SDL is literally that case and `examples/sdl/main.hero` binds it today. **The THICK case does not enter, and the rule that refuses it is Principle 0** (CLAUDE.md §2): a form enters v1 if the compiler needs it or it provably serves the thesis, and neither holds — nothing in the closure list calls a hand-written `.c`, and no Part 11 metric moves. *"Neither → it waits, regardless of elegance."* **What it waits FOR is named rather than left vague, which is the difference between a deferral and a shrug**: a witness. A wrapper with hundreds of lines of implementation that the corpus actually wants, which nobody has. The item is therefore re-homed as WORK to `docs/work/SCHEDULED.md` at **M-core-packages**, the milestone whose whole subject is packages that compose — if the witness appears there, panel 036's terms are on the table with panel 114 R7's shape already priced; if it does not appear there, that is itself the answer
