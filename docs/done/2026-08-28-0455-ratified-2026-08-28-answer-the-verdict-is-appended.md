- [x] **RATIFIED 2026-08-28, answer `1a`** — the verdict is appended to `docs/panel/095` § Author's verdict. All four stages had landed before the vote (`aa8a372`, `a0ab809`, `8584d18`, `9944209`) and the author read the result on real files. **What the ratification covers is the resolution as ADOPTED, not the proposal as tabled**: blank lines become structure for the named cases, rule 3 does not exist, rule 2 is narrowed to a dedent closing a `for`/`while`/`match`, rule 4 is scoped to statement bodies. **Answered outside the vote**: no spec sentence is owed (the spec names no tool at all, so its silence on `fmt` is consistent — the spec-warden's argument, taken over the llm-ergonomist's condition), and the cheaper alternative was refused because measurement showed all four of its clauses were already the behaviour. **Decided in the same session and asked of no seat**: WIDTH 88 -> 120 (`95a0214`), which very nearly cancels the air — 47,964 lines without air at 88 against 48,096 with air at 120, **132 apart on 48,000**. Was | **panel 095 — ratification pending, and the ballot has four stages rather than a yes/no** | **The author's five blank-line rules are admissible, measurably cheap, and cannot be applied today.** Four seats approve with conditions; the historian objects on precedent with no veto. Measured this session: implementation **+79 lines in one module, 0 elsewhere**; spec **3592 → 3592** and source tokens **delta 0** (`\n\n␣␣␣␣` and `\n␣␣␣␣` are one pretokeniser piece, so air before an indented line is free); emitted C **byte-identical** across three layouts of `examples/sqlite/main.hero` with `#line` stripped; **a blank line cannot split an `extern` group**, proved by deleting `|| continues` at `print_fmt.hero:182` and rebuilding. **What blocks it**: `heroes fmt` is not a fixpoint on **31 of its own 165 modules** (measured twice, independently, same number), so `--in-place` refuses them and the sweep cannot run — cause at `print_fmt.hero:655`, `arm_alignment` keying run membership on source adjacency while pass 1's 88-column break moves those lines. **The provisional resolution is an order of work**: (1) `fmt` exits 0 on all 165, alone, first; (2) `assert_canonical` exists; (3) rules 1, 2 narrowed to a dedent closing a `for`/`while`/`match`, 3, 5, with rule 4 scoped to statement bodies and excluded from `record`/`variant`/`extern` bodies; (4) the sweep, once. **What the author actually decides**: whether rule 3 survives at all — two seats condemned it independently, the llm-ergonomist because it teaches a reading it does not support (the blank is already owed by rule 2 whenever a block just closed) and the compiler-engineer because it is the only one of the four needing a new parameter on `fmt_block`; whether the spec owes an enforcement sentence, where the two seats that answer **disagree flatly** (the llm-ergonomist makes it a condition, the spec-warden would flip to object because the spec names no tool at all and the proposal *moves toward* §4.15's promise); and whether to take the llm-ergonomist's cheaper alternative instead — canonicalise only the blanks that mean nothing, insert none. **The cost of leaving it open**: the compiler stays in a form the author says is hard to read, and 31 modules stay non-fixpoints whatever is decided about the rules | docs/panel/095 · selfhost/print_fmt.hero:655, :718-727 · selfhost/cursor.hero:194-220 · tests/harness/suite_layout.hero · docs/defects/003 | the sitting's findings outrank its own question for the second time since panel 061, and one of them is a live defect that turns a remark into documentation
