- [x] panel 052 | **CLOSED — `f32` exists and the float hole is shut, verified 2026-08-26.** The finding said *"`float` parameters have no answer at all; `f32` does not exist (`error[unknown_type]`)"* and that **19 raylib entry points are bindable and silently wrong**. Both halves are dead: `f32` is a primitive (`selfhost/resolve_types.hero:142`), and M-separate-compilation step 4 landed four scoped clang errors — `double-promotion`, `implicit-float-conversion`, `float-conversion`, `implicit-int-conversion` — so a wrong float direction is `error[ffi_parameter_type]` at exit 1 with the right fix. Two goldens execute it: `tests/golden/fixedbugs/ffi-float-parameters.hero` and `ffi-int-narrowing-below-32.hero` | selfhost/resolve_types.hero:142 · selfhost/emit_extern_probe.hero | the case that could not be caught by any option the sitting considered was closed by a flag the sitting had not measured
