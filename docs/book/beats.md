# Story beats — raw material for the mini-book

One line per beat: `date | milestone | the beat`. Append-only; never polish
here — polishing happens in the book.

2026-08-03 | pre-M0 | The author arrived with a 2000-line design document written with an AI in a long conversation, and asked for a plan "senza scrivere il compilatore". The first real decision was which language would write the language.
2026-08-03 | pre-M0 | Swift won on paper (the semantic mirror of Heroes), lived for about an hour, and died to a one-line question from the author: "ma se devo usare QBE non è meglio scriverlo in C?" — which reopened the backend question instead.
2026-08-03 | pre-M0 | QBE — chosen in the design document for its learning value — was killed in one conversation. Not for speed: because with C emission, clang checks the FFI signatures against the real headers, and the FFI was the founding constraint all along. The design document had already written the escape hatch ("the design to steal from if the backend ever changes to C emission"); we just walked through it.
2026-08-03 | pre-M0 | The author, three times in one afternoon: one command like zig, everything in English (but talk to me in Italian), keep the surface tiny enough to compile itself. Constraints, not features.
2026-08-03 | pre-M0 | The plan was sent to five hostile expert reviewers before a line of code existed. Three said "object". The prediction protocol was cued (the answer written above the blank), the thesis metric had no control arm, and two whole compiler passes were missing. The plan that survived is revision 2.
2026-08-03 | M0 | Day zero produced no compiler and four hand-written C programs — the targets the compiler must hit. The machine found the first design bug before any human: clang warned that the entry label was never used, so every generated function now enters with an explicit goto.
2026-08-03 | M0 | The spec came in at ~1496 estimated tokens against a budget of 1500. The design document had said "the budget is nearly spent" — it wasn't kidding.
2026-08-03 | M0 | The uncued-essay prediction met the author's keyboard and lost. The protocol went low-typing: closed questions, clicks, dictation — retrieval kept, prose dropped. And the first prediction's divergence was the instructive kind: the predicted loop shape was the rotated form that real optimisers emit; the naive form won on purpose.
2026-08-03 | M0 | The teaching protocol died of its own weight on day two. Eight author-gates per step, rules stated in five places each — and the author asked for the keys back: "decido io dove voglio capire, preferirei farlo dopo." The simplicity judge had given the ceremony 0.85 odds of being abandoned within a month; it lasted a day. What survived is a queue: the work never waits, the understanding never expires.
2026-08-04 | M1 | The first real compiler code found the first real design bug in a day: the terminator rule cited Go but had dropped three of Go's words in transcription. The panel earned its keep on the fix — the obvious continuation rule would have silently killed every record body in the language, and the one judge who reads only code caught it. Meanwhile `let x = 5` learned to fail with the answer attached: the thesis is no longer prose.
2026-08-04 | M1 | Writing the string lexer to the letter of the design revealed the design had no letter: nobody had ever written down what `\n` meant, so it meant nothing — the language could not put a double quote inside a string at all, and `print("a\nb")` cheerfully printed four characters. The panel gave back five escapes and, more importantly, took the backslash away. Then the test found the joke in the fix: `"C:\temp"` still can't be caught, because `\t` is perfectly legal.
2026-08-04 | M1 | The project spent two days defending a token budget it had never measured, because measuring was assumed to need an API key the author refused to use. A judge sent to decide how much of the thesis to abandon came back twenty minutes later having simply counted the thing: 1989 tokens, not the 1496 everyone had been pricing against. The 1500 ceiling had been breached the day it was written, and nobody could see it. The design document had said from its first draft: count it with a real tokeniser, not by estimation.
2026-08-04 | M2 | The parser's first act was to discover that the language could not write down the type of a function. `fn` was in the registry of foreign words — an error with the repair pre-written — and also in the spec's own syntax for a function type, so `(fn(int) -> int)` failed to lex and every function-typed parameter in the language was unwritable, `map` and `fold` included. Five judges were sent the question with a recommendation attached, and four of them refused it: the project's own §4.17 lists `fn` among the mistakes the registry exists to catch, seven ancestor languages spell a function type with the word that declares a function, and the tokeniser measures the two spellings identically. The recommendation lost to the evidence, which is what the panel is for.
2026-08-04 | M2 | Then the acceptance program — the calculator in the appendix, written months of conversation before any compiler existed — was fed to the parser for the first time. It found the same class of bug from the other side: a variant case called `.var`, a name the registry had quietly reserved. Two lines later it found a form nobody had specified, `=> assert false`, used in eight places. A 317-line program that had never been compiled turned out to be the best test in the repository, because it was the only one written without the parser in mind.
2026-08-04 | M2 | The formatter turned "canonical" from a slogan into three decisions with prices. Minimal parentheses need associativity, the half of the precedence table nobody writes down. Long lines can only break inside brackets, because panel 007 left nowhere else. And the blank line between a comment and the declaration below it cannot be normalised at all — that gap is what makes the comment documentation, so a formatter that tidies it changes what the program means.
2026-08-04 | M2 debrief | The author sent the arm-body question to the panel and got back something nobody had asked for. The judge whose job is cost did not estimate the change — it *built* it in a scratch clone, and in doing so found that the status quo it was supposed to defend was shipping two disasters: `heroes fmt` printed an arm's `if` and silently deleted the branches under it, and `heroes parse` looped forever on a file that looked fine. Both had one cause, and it was exactly the special case the proposal wanted to delete. The question had been "is this form worth one spec token"; the answer was "the form is free, and the thing you were protecting was on fire".
2026-08-04 | M2 debrief | A second judge, holding nothing but the spec, killed the argument for the status quo from the other side: forbidding `=> assert false` on one line forbade nothing, because the same valueless arm was always legal one line lower inside a block. The guard everyone assumed existed had never existed. What was actually missing was a rule nobody had written — a `match` used as a value must have arms that produce values — and it belongs to the type checker, not the grammar. Two judges, opposite inputs, same hole.
2026-08-04 | M3a | The resolver's five rules went to the panel, and the answer came back with the price list inverted: four of the five cost nothing to specify, because a rule whose violation ends in a compile error is *stated by that error*. The only rule that had to be paid for was the only one that failed quietly — a file's own `map` silently replacing the language's. Two judges killed it in the same hour without seeing each other's evidence: one because the meaning of `xs.map(f)` would depend on a line you cannot see, the other because it was an exception to a sentence the spec already contained. The rule that shipped is shorter than the rule that was proposed.
2026-08-04 | M3a | One judge reasoned from the spec alone — no methods, no overloading — and proved that `x.f(y)` can be resolved with no types whatsoever. The reasoning was airtight and the conclusion was wrong: the design document says the dot looks for a *field* first, and a field can hold a function. The judge holding the actual code didn't argue, it compiled the counterexample and watched the prototype reject a legal program twice. What settled it was a test neither had proposed: report the unknown function only when no record in the whole file has a field by that name. No types, no false rejections, and it still catches the misspelling the first judge had itself made while writing Heroes from the spec.
2026-08-04 | M3a | The 317-line calculator refused to resolve, at exactly two lines: it declares its own `map` and `fold`, and the language reserves both names. Third time that program has caught a rule nobody knew they had written — a variant case called `.var`, an `assert` in an arm, and now a name. It has never been compiled and it is still the best test in the repository. Meanwhile it turned out to be calling `.str()`, a built-in that is in the design document's inventory and missing from the spec: without it the language cannot put a number into a sentence, and nobody had noticed because nobody had tried.
2026-08-04 | M3a | Making an unused parameter an error is a rule with exactly one shipped precedent, and the judge that writes Heroes for a living found the reason nobody else ships it: told "the parameter you never read is an error", its own most likely repair was to *delete the parameter* — a wrong signature that compiles, the silent bug merely relocated. Every language that enforces the rule ships the same escape hatch, `_`, and Heroes already had it by accident: `_` never binds anything. The rule that looked like a new restriction was mostly a valve that was already open.
2026-08-04 | M3b | Three ways to type a jump went to the panel, and the judge whose job is cost did not argue about them — it built all three and ran them. Typing `break` at all turned out to reject every function whose body ends in `return`, which is most functions in this compiler; Rust's own RFC had recorded that exact wrong turn a decade earlier, in the compiler's words. Then the judge who only reads precedent objected to the winner on a completely different axis — its cost is one rule per branching construct, and Heroes already has two — and named the condition under which it would withdraw: write the rule once, as a shared routine, with divergence as data. That file is 84 lines. Two judges, opposite inputs, and the design that satisfied both was proposed by neither.
2026-08-04 | M3b | And design.md was caught contradicting itself, one bullet apart: a `match`'s type comes from the arms that can produce a value, said the third bullet of §4.7; every arm must produce a value, said the fourth. The same program, legal and illegal, six commits after both sentences were written by the same panel. The rule that survived is the one with the executable evidence behind it, and the other now says "or jump".
2026-08-04 | M3c | The calculator refused to type-check on one line, and the line was in the panel record's own list of things it had fixed. Panels 002 and 006 had rewritten eleven sites to `ok(...)` "when applying the rule exhaustively"; there were twelve. Four milestones, four defects in a program that has never been run — a variant case the registry had quietly reserved, an `assert` where the grammar allowed only an expression, two library functions whose names the language reserves, and now a missing `ok`. It remains the best test in the repository for the same reason each time: it was written before there was anything to test it with.
2026-08-04 | M3d | The thesis got its first number, and the useful part is the column that shows nothing. 379 mutants, 94% caught — but the same compiler with the thesis rules switched off catches 76%, and *that* is the measurement: 18 points, concentrated in three operators. The lazy catch-all goes from 100% caught to 13%. Shadowing and dropped argument labels go to zero, because nothing else in the language looks for them. And typos score 100% in both arms, which means they are caught by names and types like anywhere else — a fact that would have been very easy not to write down.
2026-08-04 | M3d | Two mutants survived on purpose and nobody had planned for it: turning a mutable cell into a plain binding is only a mistake if something later writes to it, so where nothing does, the mutation means exactly what the original meant. A panel had asked for a meaning-preserving counter-arm to keep the catch rate honest. It turned up inside an operator, uninvited.
2026-08-04 | M3d | The spec had been telling models to write a compile error for four milestones. A panel ratified the function type's spelling at M2; the frozen spec kept the old one, which the language rejects on sight with a fix attached — and the assistant reads that spec at the start of every session. The freeze was protecting a measurement that had already been published twice. It lifts here, and the document is now held to the compiler by four tests, one of which simply asks whether the spec uses any word the lexer refuses.
2026-08-04 | M4 | The panel that decided the compiler's middle end spent its most valuable half-hour not arguing. One judge was told to write the C that each proposal implied and compile it — and it compiled fourteen programs, two of which decided the design. In the first, a Heroes function called `open` reaches C with its own name and quietly replaces the one in libc: clang says nothing, the program prints 7 instead of a file descriptor, and `open` is not on the language's list of reserved words. In the second, assigning the result of a function that returns nothing is not a warning but a hard error, and the graphics library everyone eventually binds is almost entirely made of such functions. Neither fact is in any design document. Both are now in the instruction set.
2026-08-04 | M4 | The choice between the textbook representation and the plain one was settled by the institution with the most to lose by settling it that way. LLVM — whose whole existence is single-assignment form — tells the authors of new languages not to build it: use stack slots and loads and stores, it says, and let a later pass clean up, because that is what clang itself does. QBE says phi nodes are not necessary. Rust's own middle representation chose the plain form in 2015 for the sake of error messages rather than speed and has not changed in eleven years. Every migration anyone could verify was made to produce faster machine code — the one thing this project has formally renounced. The simpler answer had a decade of evidence behind it and nobody had gone looking.
2026-08-04 | M4 | A judge was handed two spellings of the same lowered function with the labels stripped off, and asked what it returned before it was asked which it preferred. It got both right, then counted what each had cost: one re-reading for the plain form, three for the fashionable one — and the expensive one was not verbosity. A line that names a value defined by jumps elsewhere in the function cannot be read as a line at all; you have to find every jump first. It also noticed, in passing, that the sample's blocks had exits nobody had drawn: arithmetic that overflows and an index out of range both stop the program, so three of five blocks had a hidden way out. A pass written on the assumption it had read correctly would have been wrong and would have compiled.
2026-08-04 | M4 | Everyone cites the Haskell compiler as the reason to build a second tree before lowering. Read the source and the payoff was never the tree — it was the checker they could run over the small representation afterwards, which its own authors say makes it surprisingly hard to write a transformation that type-checks and means the wrong thing. So the check shipped without the tree, and it earned itself back the same afternoon: it is what now guarantees that every path out of a function writes back the arguments it promised to modify, including the one that leaves through an error. That obligation had been named four milestones earlier as the place this project would actually stall.
2026-08-04 | M4 | The loop that reads `for x in xs` becomes four blocks, and only three of them are obvious. The fourth exists because `continue` has to increment the index too — and a version that skips it produces a program which compiles, passes every test, and hangs forever on the first element it skips. No type system can see that. The 320-line calculator, which has caught a defect at every milestone since the parser, caught nothing this time: every line of it lowered and verified on the first attempt. Two bugs turned up anyway, both in the same shape, and both found by reading the dump rather than by running a test — the underscore that means "I am not using this" was quietly given a variable of its own.
2026-08-04 | M5a | The compiler produced its first binary, and the argument that took longest was about a number between zero and two. When a program is correct and the compiler simply is not finished, what should it say — and more to the point, what exit code should it use? Two says "the tool could not do its job", which is literally true. One judge was handed that message and asked what it would do next: it checked the compiler's version, ran the diagnostic command, searched the whole repository for the internal identifier it had been shown, and then told its user the toolchain looked broken — a sentence it reported afterwards as false. Another went to look for precedent and found that the C compiler everybody uses has had a special diagnostic for exactly this since 1994, printing the word "sorry", and then read one line further: it exits with the ordinary failure code. The evidence that looked like it argued for two settled the question at one.
2026-08-04 | M5a | The instruction to point generated code back at the author's file had been in the design document for months, and it was not implementable as written. The directive that does it anchors the *next* line and then the compiler counts upward on its own — so a source line that becomes five lines of generated code drifts by four, and the drift accumulates as you read down the file. The hand-written target that had frozen the shape of the whole backend advertised line 2 in its own header comment and delivered line 6, which is blank; worse, the error it printed showed the generated text underneath the name of the author's file. Nobody had noticed because nobody had made an error there on purpose.
2026-08-04 | M5a | A judge whose only job is to compile the C that a proposal implies found the hole that mattered by writing four declarations by hand. The language can already name a function from a C library, and nothing yet attaches the library's header — so the compiler would have to invent the C declaration from the Heroes one, which makes it self-consistent by construction and checked by nothing. Asking for the absolute value of a number slightly too large for 32 bits returned a wrong answer at exit zero, with one warning that is not an error; opening a database returned success in total silence, under every warning flag the compiler has. With the real header present both are hard errors. The founding constraint of the language is that everything comes from C libraries, and its guarantee turned out to live entirely in the `#include`.
2026-08-04 | M5a | A directory called `runtime` is one of the commonest names in a source tree, and the compiler looks for its own in the current one. So somebody put a decoy there: the generated program compiled with zero warnings under every diagnostic the compiler offers, ran, and printed a hexadecimal number where thirty was expected. The repair is a single line asserting a version constant that only the real header defines — after which the same program will not compile at all. The interesting part is that the search order was already written down, reviewed, and approved; what was missing was somebody willing to lie to it.
2026-08-05 | M5b | The compiler learned to count references, and the check that watches the counting refuted its own design twice in an hour. The rule was: a value that owns a string is released at the end of the block that created it. The first refutation was harmless and instructive — an assertion carries the text of what it asserted into the failure path, and that text is a string, and it crosses a block boundary, and it is perfectly safe there because a literal is never freed. The second was fatal. Ask for the half of a number and then use it in a sentence, and the sentence's first half is built in one block and finished in another, because asking for the half opens a branch in the middle of the expression. Not a corner case: that is what every expression containing a fallible call looks like. The repair was to stop letting temporaries own anything at all — put the reference in a named cell the moment it exists, and let the cells be swept. Which is exactly what choosing cells over the fashionable representation had been for, one milestone earlier, in an argument nobody had yet had to cash.
2026-08-05 | M5b | The leak detector everyone reaches for does not exist on this machine, and it took two judges measuring it independently to notice. Ask the sanitiser to look for leaks and it answers that leaks are not supported on this platform, and aborts. Do not ask, and a program that loses 999 blocks of memory exits with success in complete silence. So the milestone that introduced reference counting would have shipped behind a configuration that looked exactly like a leak gate and was not one. What replaced it is twelve lines: count the live blocks, check the count is zero when the program ends, and name the number. It caught a real bug on its first run — a plus sign between two strings reads like arithmetic and is a constructor, and the type of the result is the only thing that says so.
2026-08-05 | M5b | Two judges investigating different things produced a better answer than either had. One objected that the project must not promise "the shortest decimal that round-trips", because the smallest number a double can hold renders as sixteen digits under the chosen method and round-trips perfectly well. The other, checking a citation, found that the widely-shipped implementation of that same method starts its search at one digit rather than fifteen when the number is very small — a branch the proposal's description had dropped. Put the branch back and the counterexample evaporates: the tiny number prints as five characters, exactly as Python does. The correction to the wording stands anyway, because what is guaranteed is the round trip and not the brevity, and the difference is what a compiler shipped wrong for eighteen years.
2026-08-05 | M5b | The promise "our runtime never changes the locale" turned out to be worth nothing, and the demonstration is more convincing than the argument. Set a locale that writes decimals with a comma — which any of a dozen C libraries does on startup — and the number formatter emits a comma, then notices there is no decimal point, then appends one, producing something that is not a number in any locale. And the self-check cannot see it, because the same setting corrupts the reader as consistently as the writer. The precedent is twenty years old and exactly this shape: Python never touched the locale, the graphical toolkit it was embedded in did, and Python broke anyway.
2026-08-10 | M5c | A tree that contains itself, in a language with no pointer. There is no box type, no reference type, nothing to put in a field that stands for "somewhere else" — and yet the example the design was written around is a tree of expressions holding its own children. The answer, decided years of reading earlier, is that the *array* is the one indirection there is: an array is a pointer whatever it holds, so a shape that recurses through one has a finite size and still copies like a value. This milestone is where that stopped being a claim. The program compiles, prints fourteen, and ends with every allocation accounted for. What makes it work is not clever: freeing one node runs a function the compiler wrote for that node's type, which releases an array, which releases each element through a descriptor, which calls the function the compiler wrote — a recursion assembled from one generated function per type and one runtime that drives it. And the reason it cannot leak is a sentence about what a field may hold, not about how the freeing works: a field cannot contain a reference, so every value in the language is a tree, and a counter that frees at zero is complete.
2026-08-10 | M5c | Six bugs in one milestone and every single one was a missing row rather than wrong reasoning. The compiler is built out of exhaustive tables, so adding a capability breaks compilation until every table admits it — which works, and is why nobody wrote logic that was actually wrong. What it does not cover is adding a capability to the list of *types* and forgetting it in a walk over *fields*, because that walk already has a last arm for everything else. Three of the six landed exactly there, and the interesting part is what the last arm said. It emits a loud failure rather than doing nothing — so a container inside a variant aborted on its first run instead of quietly losing memory. The usual argument for enumerating every case is that additions become loud. The other half, which this milestone taught, is that what the *final* arm does decides the failure mode of everything the enumeration forgot.
2026-08-10 | M5c | Twice, a program printed every correct answer and was wrong once, at the end. Build a pair from two records that each hold a piece of text, print all seven lines, watch them all be right, and then watch the program free the same memory twice on the way out. Assign a new list into a field, read it back correctly, and leak exactly one block. Neither is visible in the output, at any input, ever — the answers are right because the *values* are right; only the bookkeeping is wrong, and bookkeeping has no output. Both were found by twelve lines that count live allocations and complain if the count is not zero when the program ends. The sanitiser everyone reaches for agreed with both after the fact and found neither on its own. A milestone about memory turns out to need an instrument whose subject is the balance rather than the result.
2026-08-10 | M5c | The rule that was invented to protect the generated code caught the file that invented the rule. A panel had insisted that every type's hash function must exist even where nothing hashes it, because calling a missing one crashes with no type name and no line number to go on. So the array constructor was made to refuse any type description lacking one — and the first thing it refused was the hand-written experiment that had frozen the type-description format in the first place, two milestones earlier, and had never needed a hash. The experiment was right about the format and incomplete about the rule it would later be judged by. A check with an instrument beats a check in a comment, demonstrated on the comment's own author.
2026-08-10 | M5d | The last two types in the language were entangled, and one of them may not survive. Looking a key up in a map returns "the value, or an error", so the map could not be built before that type existed — and that type turned out to be reference-counted even when it holds a plain number, because its error side carries two strings. So `int?` owns memory. The other half of the tangle is a judgement nobody has made yet: an earlier review found that the map cannot be added to, which means the compiler this language is meant to compile cannot build a symbol table with one, and deleting the whole container from the specification would give back more budget than finishing it costs. The container was built anyway, on the author's instruction, with the disagreement written down rather than smoothed over. If the audit deletes it, this work goes with it, and the record will say that was known in advance.
2026-08-10 | M5d | A rule adopted two milestones before anything could use it, and the first thing it did was reject the file that inspired it. The requirement was that every type must have a hash function even where nothing hashes anything, because calling a missing one crashes with no type name and no line number. There was no hash table in the language at the time. When one finally arrived, the rule's justification arrived with it — and in between, the constructor enforcing the rule had already refused the hand-written experiment that froze the format in the first place. Adopting a rule before its use is usually a smell. This one was right for a reason that does not depend on timing: the cost of getting it wrong is a crash with nothing in it to read, and that cost does not shrink by waiting.
2026-08-10 | M5d | A leak that only exists when the value was computed. Look a key up in a map, take the answer or a default, and one block of memory is lost — but only if the value in the map was built by joining two pieces of text, never if it was written as a literal. The reason is that a literal is laid out by the compiler with a negative reference count, so failing to release one changes nothing at all. The bug was in a single word: one table said the lookup borrows its result while the code that emitted it made a copy. What the episode really settles is how to write a test for this class: allocate on purpose. Every case in the milestone that touches counting now joins two strings rather than writing one, because the cheaper spelling would have passed.
2026-08-10 | post-M4 | The project had a home for every kind of writing except the kind it was mostly made of. Explanations had one, decisions had one, questions owed had one — and each of the three had been created, at some point, because something valuable was disappearing at the end of a conversation. What was still disappearing was the middle: not the explanation that finally worked, but the route taken to reach it, and the questions still standing when the chat ended. The request that fixed it was one sentence — «uso questa chat per capire», with the offer of a folder attached almost as an afterthought — and the first thing the new folder had to be told was what it was forbidden to become: not a second glossary, not a way to change the language without the panel, and not a record of who understood what.
2026-08-11 | M6 | Three design arguments were settled this milestone and not one of them was settled by argument. A judge was asked whether the runtime's per-type description should gain a sixth entry so that sorting could work on everything; it wrote the header, compiled the unchanged runtime against it, and found that C fills a short initialiser list with zeros — so every existing description would have carried a null pointer, silently, under every warning flag the project uses, and the call is a crash with no type name in it. Another was asked whether the language's small library could be compiled once and shipped; it wrote the only shape that is possible for a generic function compiled ahead of its uses, passed a text-handling function where a number-handling one belonged, and got no diagnostics at all and then a crash. A third was asked whether the mangled names of generic instances should be readable rather than hashed; it found two perfectly legal programs that spell the same name. Three vetoes, three compilers' worth of evidence, and none of it reachable by reading.
2026-08-11 | M6 | The specification had contradicted itself for eleven weeks and implementing one function is what found it. It says ranges are written `range(a, b)`. It says that when two parameters of a function share a type, the caller must name them. And it says `range` is written in the language itself. Any two of those are fine; all three cannot hold, because a built-in escapes the naming rule and an ordinary function does not. Nobody could see it because `range` had never had a body — the rule and the exception had never been in the same room. The resolution was to obey the rule the language already had, which turns the habit every programmer brings from elsewhere into a compile error with the repair attached.
2026-08-11 | M6 | A pass that has already decided is worse than one that declines to. The plan said the generic-expansion pass must run before the reference-counting pass because the second cannot decide anything while types are still unknown. It can, and it had: asked whether a value of an unknown type owns memory, the compiler answers no — which is right when that type turns out to be a number and a leak when it turns out to be text. The dump showed the two side by side in one function: an array getting its increment, a value of unknown type getting nothing. So the ordering in the plan was right for a reason nobody had written down, and the reason is not that the pass is helpless but that it is confidently wrong.
2026-08-11 | M6 | Adding a feature made an old defect reachable, which is a shape worth naming. Folding a list into a value requires knowing two things the specification never said: which direction, and which way round the two arguments go. Where the running total and the elements have the same type — adding numbers, joining strings, taking a maximum, which is most real uses — every combination type-checks, and four different programs are spelled identically and print different answers. It had never mattered because the function had no implementation, so nothing could run. The step that gave it one is the step that had to fix it, and the fix is nine words in the specification.
2026-08-11 | M6 | The check a panel asked for as insurance is the one that found the fire. The language's small library is appended to every program the compiler sees, so both live in one text — and a panel worried that an error inside library code would point at a line the author cannot open, and asked for a guard that refuses to report one. It fired the first time the library contained a function with local variables of its own. The library's folding function has a variable called `total`; a user program with a top-level declaration of that name was told, in effect, that their own code was shadowed by something invisible. The guard was written for a hypothetical and caught the real thing on contact.
2026-08-12 | M8a | The cheapest change the language ever made was a verb. Splitting a program into files needs rules: what happens when a file's own name collides with the name of a file it imports, and what happens when an import is never used. Both were already written down — the language says a name may not be reused while it is visible, and that anything bound and never read is an error — so the whole question was which word to use for what an import does. The panel chose *binds*, the same word the language already uses for ordinary values, and the two existing rules covered both cases with nothing added. Eleven of that session's thirteen decisions cost nothing at all; the two that cost anything bought a legal example and a rule about what an import does not give you for free.
2026-08-12 | M8a | Asked to add folders, four independent reviewers counted instead of arguing, and the count said no. The reasoning that asked for them was that a compiler written in this language would otherwise start with forty files in one directory, which sounds untidy. But a folder does not remove a single import line — it changes how a target is spelled, never whether you have to name it — and the compiler being ported has forty of its files sharing a last name with another, every one of which the folder rule would refuse and force renamed anyway. The clinching fact came from history: the Nim compiler has run with a hundred and seventy files in one directory for years. The request was to decide immediately; the answer was that the thing being feared does not happen.
2026-08-12 | M8a | One question had been standing in for three since the strings milestone, and nobody could see it while there were only two files. When the compiler wanted to know whether a piece of text belonged to the small built-in library, it was really asking three different things: may an error message point here, may the generated code claim the author's file for it, and which name should the linker see. With two files those have one answer. With four they do not — and the difference decides whether running a project's tests runs the tests in all of its files or silently only those in the one you named. The split cost two words and would have cost a whole class of missing tests.
2026-08-12 | M8a | A reviewer sent to price folders found two live bugs in the work that had just shipped, in code it was only reading as background. One made the compiler refuse a perfectly good program while blaming itself: two files with awkward names produced the same internal symbol, and the rule that was supposed to prevent it was comparing the wrong two strings — it was correct, and correct about something else. The other made every error inside an imported file point at the wrong file entirely. Neither had anything to do with folders. Both were found by someone compiling examples to answer a different question, which is the whole reason the reviews compile things.
2026-08-12 | M8a+ | Asked whether the language needed a way to hide things, the answer turned out to be that two of the three questions were not about hiding at all. Hiding a field of a record cannot work here, because building one requires naming every field, so a hidden field makes the whole thing unbuildable from outside — and because a record is a plain C structure, anybody writing the glue this language exists to write reads the hidden field at its offset, which one reviewer did, then wrote to it, then built the "hidden" case of a hidden alternative for good measure. Hiding one alternative of a choice is worse: it forces the catch-all pattern this language forbids, so whether a line compiles would depend on a declaration the reader is not allowed to see. Another language shipped exactly that, deleted it two years later as barely used, brought it back at a coarser grain five years after that, and documents the price as losing the guarantee that you handled every case. Only the third question — hiding a whole declaration from other files — was real, and it was deferred: the instrument that decides it is the port itself, which has not run.
2026-08-12 | M8a+ | Asked whether the language needed error handling like the one everybody names, the honest answer was that it had shipped it two milestones earlier and nobody had said so out loud. What it does not have is errors that carry a type rather than a spelled-out name, and the cost of that was finally measured rather than asserted: a new instrument makes one plausible slip in every error name in every example program — twenty-five of them — and the compiler catches none. It is the only zero in a table where everything else catches between three quarters and all. Then a reviewer, told nothing but the language description, wrote the cure by accident while looking for something else: put the names in constants and compare against those, and a mistyped name becomes an ordinary undefined-name error today, in the right place, naming the file it came from, for nothing at all. The sting is that the same trick makes the clever compiler check that had been proposed completely blind — it is strongest where the code is worst and silent where it is best.
2026-08-12 | M8a+ | The same source file, compiled two ways, and one of them leaked. A reviewer running the test suite from a different starting file found a failure that nobody could reproduce from the usual one: identical bytes, and memory lost only when that file was compiled by itself. The reason is that the compiler asked the wrong question about a type. To decide whether a value carrying text needs releasing, it consulted a table that is only filled in when some program somewhere *takes apart* such a value — and the file in question builds them and never takes one apart. So the release function came out as an empty switch, while the same declaration compiled next to the file holding the pattern match came out correct. Ninety test programs missed it, because catching it needs a value carrying text that is *computed* rather than written down, and never taken apart in the same compilation; every existing test that builds one also takes one apart. The fix is to ask the fields, which do not move between compilations, instead of the table, which does.
2026-08-12 | M8a+ | Two reviews in a row had been sent to answer a question about the language and had come back with a bug in code they were only skimming. The third time would have been a coincidence; twice was a pattern, so the author stopped the work and asked for the pattern to be chased instead of the bugs. The three bugs were turned into three sentences about what *kind* of mistake they were, and each sentence was handed to a search with one rule: compile it and run it, because a fault you only reasoned about is not a fault. Four searches came back with twenty. Nine of them refused perfectly ordinary programs — a record with a field that might be missing, a lookup table inside a record that anything compares, a file with no starting point — and the compiler's answer to every one of them was, in effect, *the compiler is broken*. None of them had ever been noticed, for the reason all three of the originals had not been: no program anybody had written happened to have that shape.
2026-08-12 | M8a+ | The most useful sentence of the day was a correction to one the project had already written down. After an earlier fix it had recorded the lesson as "the rule was kept by habit over one type, and the one place outside that type kept the bug". The search found that this was too kind. The habit leaks *inside* the type as well: ten error messages build a line number into their own text, and by the time anything looks at the message it is a piece of prose, so the one function whose whole job is to work out where something is cannot see them. That is why the earlier sweep had walked straight past all ten. A rule can only protect what it is asked about, and a location written into a sentence is never asked about.
2026-08-12 | M8a+ | Eighteen decisions had been sitting for a week and a half marked "awaiting the author", while the compiler had been built on every one of them since the day they were made. The author cleared them all in a single line. The interesting part is what was written down: not "ratified", but *what kind* of ratification it was — one instruction after a summary, not eighteen readings — and what it therefore cannot settle, which is anything waiting on a measurement nobody has taken yet. Saying yes to eighteen things at once is fine. Recording it as though it had been eighteen separate yeses would have been the same class of mistake the whole day had been spent hunting.
2026-08-12 | M-ffi-ladder | The promise the whole language was arranged around turned out to be false for the pair its own second example uses, and nobody could have found that by reading. The arrangement is this: when a program borrows a function from a C library, the compiler does not describe that function itself — it points at the library's own description and lets the C compiler check the two against each other. That is the reason the language emits C at all. But pointing at a description only checks what you *pass*, never what you get back, so a program that says a square-root function returns a whole number compiles without a murmur, runs, and prints 1 where it should print 1.41. Four wrong borrowings out of six passed in silence. The repair is one line per borrowing, and it costs nothing when the program runs: a question put to the C compiler in a form that asks about a call's type without ever making the call.
2026-08-12 | M-ffi-ladder | A reviewer given the language description and nothing else could not write the milestone's own acceptance program, and the reason was a gap everybody who knew the compiler had stopped being able to see. Opening a database means handing the library somewhere to write a handle, and the language had no way to write down an empty somewhere — no null, no address-of, no allocation, and a rule that everything is initialised. The people who had built the thing did not notice, because they were not trying to use it. The fix is one word, and the argument for it was a program that could not be written rather than an argument at all.
2026-08-12 | M-ffi-ladder | Asked, almost in passing, to try one more library, the author's suggestion broke the milestone's newest safety net within a minute. The net asks the C compiler what a borrowed function really returns, and it had been verified against three libraries — every correct case passing, every wrong one caught. The fourth library returns its result codes as a named set of choices rather than as plain numbers, and the question the net asks does not recognise a named set as a number at all. So it refused every function in most of the libraries anybody would want to bind. Nothing in three rungs of the ladder could have shown it, because the database library returns plain numbers; no amount of reading the C standard did either. Compiling one program did.
2026-08-12 | M-ffi-ladder | The language's three ways to say "this succeeded, and there is nothing to hand back" were all refusals, and it took six milestones and one signature to notice. Writing a file either works or fails, so its result is *nothing, or an error* — and every spelling of the success side was rejected: one because emptiness is a type and not a value, one because the constructor insisted on an argument there was nothing to give it, one because leaving it out means you forgot. A reviewer predicted the failure before the signature that needed it existed, from the description alone. The cure is that the constructor now carries as many arguments as there is something to carry: usually one, and here none.
2026-08-12 | M-header-constants | A library's settings have names and numbers, and the language could only say the numbers. To tell a download library which address to fetch, a program had to write 10002 — a number that means nothing to anybody, sitting in the file with a comment above it explaining that the library's own documentation adds two to ten thousand. The comment was arithmetic somebody had done by hand, and nothing anywhere checked either the sum or the copy. The project measured how bad that was before fixing it, by building a tool that changes one digit of such a number and seeing whether anything complains: five numbers in the example programs, and nothing complained about any of them. A changed digit produces a program that compiles, runs, asks the library for a setting nobody meant, and reports success. The cure is to stop writing the number: the program names the setting and the library's own description supplies the value, so there is no digit in the file left to be wrong. The comment explaining the arithmetic was deleted along with the number it explained, which is the part worth noticing — an explanation that only exists because something cannot be checked disappears when it can.
2026-08-12 | M-header-constants | The reviewer given only the language description, and asked about something else entirely, would not stop pointing at a different line. It was the most-copied line in the document — the one every program that talks to a database starts from — and it contradicted a rule printed four lines below it. Everyone who knew the compiler had read that page many times without seeing it, because they already knew which of the two to follow. The reviewer did not, so it copied the example, and the consequence turned out to be worse than a mistake anybody would notice: the program built cleanly, ran, opened no database at all, and told the operating system it had succeeded. Nothing in the project had ever compiled the specification's own examples. Now one of them is compiled by a test that reads it out of the document at test time, so the example and the rule beside it cannot drift apart again without something going red.
2026-08-12 | M-header-constants | The safer-looking choice was the wrong one, and only building both showed it. The compiler describes each kind of declaration as one of a handful of shapes, and the new thing — a value borrowed from another language's description — could either be a new shape of its own or a widening of the shape it most resembles. A new shape looks obviously safer: this project's whole method is that a new alternative makes the compiler complain everywhere the old ones were handled, so nothing can be silently forgotten. A reviewer implemented it and found the one place that does not complain — a single line asking "is this that kind of declaration?" in a form that answers no rather than refusing to compile. A new shape there would have quietly turned every borrowed value into a reference to a function. So the widening won: it breaks fewer places loudly, and it is right in the one place that decides the answer.
2026-08-12 | M-literal-bases | The reviewer who had only been given the language's description, and had been asked about something else, wrote down the way everyone writes file permissions — a zero, then seven, then two more zeros — and asked what it meant. Nobody had ever asked. It meant seven hundred. In every language this one borrows from, and in every example of file permissions ever written, that same run of characters means four hundred and forty-eight; here it silently meant something else, and a program using it would have compiled, run, set the wrong permissions, and passed its own tests. The whole point of the language is that a mistake like that cannot survive to run. The repair chosen was not the obvious one. The obvious one is to refuse the ambiguous form and explain; the one taken also gives the old meaning a spelling of its own, so the same three lines of description close the hole and add the notation the project wanted anyway. Three other languages each reached that answer separately, and always after being bitten.
2026-08-12 | M-literal-bases | A rule that had been true for a year was one week from becoming false without anything going red. The project measures itself by deliberately breaking its own example programs one small change at a time and counting how many the compiler catches; one of those changes moves the last digit of a named number. Finding the last digit meant asking "is this character a digit", which was a complete question while numbers were only ever written in tens. The moment numbers could also be written in sixteens, a mask ending in a letter would simply have been skipped — the tool would have reported a smaller job and a perfect score on it, and the number it reports is the one the previous milestone had made its headline. Nothing would have failed. The fix was written before the first such number existed anywhere in the project, which is the only time it is cheap.
2026-08-12 | M-sized-integers | The project had refused this for a year, in writing, in the strongest words its design document contains: never in this version, the highest-risk thing we could add, the most expensive kind of rule there is. Then the author asked for it anyway, and half the expense turned out to have been paid years earlier by an unrelated decision. What makes multiple number sizes dangerous in other languages is the invisible conversion — the rule that quietly turns one kind of number into another before you have finished reading the line. This language had banned invisible conversions from the start, for a completely different reason, so there was nothing left to write down. The feared thing arrived at roughly the size of a paragraph.
2026-08-12 | M-sized-integers | A decision that looked like a matter of taste turned out to decide whether a promise was true. The author had ruled that a number too big for its box must stop the program, at every size. Implementing that meant choosing how the compiler files the eight sizes internally — eight separate entries, or one entry with a size written on it — and the two options differ in nothing a user could ever see. A reviewer measured it instead of arguing: with eight entries, the line that decides whether to install the overflow check simply does not recognise the smaller sizes, so it silently omits the check and the program runs to completion with a wrong answer. With one entry, the same line refuses to compile until somebody handles it. The promise the author made was true only under the second filing.
2026-08-12 | M-sized-integers | The five deliberately nasty test cases every milestone owes were written last, as usual, and one of them broke the feature. The language now refuses to squeeze a number into a box too small for it — that is the entire point of the addition — and the check had been written by reasoning about which boxes need which comparisons. For boxes that hold negative numbers the reasoning had a hole: the floor was never checked at all, so a number below the floor slid in unnoticed. A silent wrong answer, inside the mechanism built to abolish silent wrong answers, found by the only tests written specifically to be unkind.
2026-08-13 | M-program-corpus | The plan was to write programs, and the programs turned out to be a way of looking at the compiler. Seven of them were written — a JSON reader, a Markdown converter, a spreadsheet, a maze solver, a small assembler with its own machine, a log summariser, a list of things to do, a game — and six faults fell out of the compiler on the way, none of which any existing test could have found and none of which anybody found by reading. The first one appeared inside a minute: the compiler simply stopped, forever, on a program whose first line named a thing the language reserves. Three of the six are the same fault wearing different clothes, and it is the one this project keeps writing rules about: a decision that was correct when it was made, resting on something about the world rather than on something about the thing in front of it, and going quietly wrong later while the sentence explaining it still reads perfectly. The sharpest of the three sits under a comment saying, in so many words, that this exact thing must never be allowed to happen — and the contradiction was twenty lines further down the same page. It had been true. It had been read. Somebody had agreed with it. What it described had stopped being the case.
2026-08-13 | M-program-corpus | A promise made a day earlier turned out to name a shape the language could not spell. A review had asked that programs stop writing error names as bare text and give them proper names instead, and it wrote out what that would look like — one program's name for an error, borrowed by another. When the corpus finally needed exactly that, it did not compile: borrowing a *function* from another file worked, borrowing a *type* worked, borrowing a **value** did not, and had never worked since the day files could refer to one another. Nobody had noticed, because nobody had written the program that needed it. Then the change was made and measured, and the measurement went the way the review had warned in advance that it might: the tool that hunts for this class of mistake did not start catching more of them, it stopped finding any to hunt. Eighty-five places moved out of an instrument that catches nothing into one that catches everything. The mistake did not become detectable — it became a different mistake, one the compiler already refused.
2026-08-13 | M-program-corpus | The tool that tidies a program's layout had been changing what programs mean. Given a division wrapped in brackets, followed by a request to turn the result into text, it removed the brackets — which moves the request onto the number underneath and produces something that is not the same program and does not even make sense. The reason is a number that was right when it was written: the tool ranks operators by how tightly they bind, and "tighter than everything" was written down as a specific rank rather than as a name. Later, six more operators were added below the middle of that scale, every rank above them moved up, and the five places that had spelled out the old number stayed where they were. It could not have been caught by the examples the project keeps for reading, because those are checked to already be in tidy form — a shape the tool would mangle had therefore never been written into one. It was caught by the first program in the whole repository that happened to divide a number and then ask for it as text.
2026-08-13 | M-program-corpus | A reader was given the language description alone — no examples, no compiler, nothing else — and asked to write three small programs, then asked afterwards how it had built each string. It had avoided the slow way, and named the sentence that stopped it: the only sentence in the whole document that mentions what anything costs, and therefore, in its words, loud. That sentence had been bought a month earlier with a promise that a future reader would do exactly this, and it is the first promise of its kind this project has ever collected on. But the same reader also noticed something nobody had asked about: it had avoided the slow way and then written the *other* slow way, in two programs out of three, without noticing until it was asked. Not because the sentence was unclear — it names both — but because in two of the three tasks the language left no other route. The warning changed which words were typed and not what the program does. A caution with no alternative behind it is a caution that moves people sideways.
2026-08-14 | M-binding-fidelity | The description of the language made a promise about the boundary with C: declare a foreign function wrongly, and the program will not build. It was half a promise. What a foreign function *hands back* had been checked since the day the boundary existed; what it *takes* had never been checked at all, and two independent readers demonstrated it the same way — a program that passes a number too large for the slot it is going into, compiles without complaint, runs, and prints a different number. Forty-two per cent of everything anybody actually binds has a slot like that. The check could not be switched on, because the trick that makes the first half free — asking a question the compiler answers without ever running the code — is exactly what stops it seeing a conversion. So the compiler now writes a small function nobody ever calls, whose only purpose is to make the call once, in a place where the C compiler is obliged to look at it, and points its line number at the line the author wrote.
2026-08-14 | M-binding-fidelity | The panel convened to add three missing things, and measurement removed two of them. The first was a 32-bit number type: a foreign function taking one had been called with a wider number, and the wide number was thought to be arriving damaged. It is not — it arrives bit-for-bit identical to what a C program would send, and the number that looked wrong was simply what a 32-bit float contains. The second was a way to declare a function that takes an open-ended list of arguments. There is a real rule saying that calling such a function through a fixed declaration is undefined, a real processor where it goes wrong, and three real projects that were broken by it. The project was not, and the reason was a decision taken a milestone earlier for an unrelated purpose: the compiler never re-states a foreign function's signature, it includes the real header and lets the call go through that. The declaration in the program is read by the front end and never reaches C. What remained after both were struck was smaller and true, and one judge left a standing veto behind it: never re-state the signature, because the version that does is silently correct on one of the two machines the project tests on.
2026-08-15 | M-struct-passing | A third of everything a real C library offers had been out of reach, because a bundle of values travelling as one thing had no way to cross. The obvious fix is to describe the bundle on both sides and then check that the two descriptions agree in size and in the position of each part. That fix is wrong, and the way it is wrong is the reason this project exists: on the machine this is being written on, two bundles can have exactly the same size, exactly the same positions, pass every check anyone could write — and still arrive as garbage, because the processor carries whole numbers and fractional numbers in two different sets of registers, and the check cannot see the difference between them. The answer was to stop describing the bundle at all. The compiler now writes nothing about it and uses the library's own description, so the arrangement is never its to get wrong. What it does check is each part, one question at a time, and the question is *is this exactly the same kind of thing* rather than *is it the same size*. Before that check existed, declaring one part as a whole number four times too wide compiled, ran, and printed the correct answer — right up until a value came along that did not fit, at which point it would have printed a different one and said nothing.
2026-08-15 | M-struct-passing | Three separate places in the compiler broke on the same day for the same reason, and none of them was a mistake anyone made. Each was a list of cases with a catch-all at the end, written when the case that now falls into it did not exist. One made the tidying tool quietly rewrite a program into a different program that still looks legal. One made a conversion produce a result meaning "this succeeded, and the answer is zero" for every input, at no complaint from anything, because the empty space it left behind happens to spell success. One made the compiler blame itself, in public, for something the author had done. The project has a written rule about decisions that rest on an assumption about the world rather than on the thing in front of them, and the rule says to write the assumption down so it can be checked later. What this day showed is that the most dangerous version is the one where nothing is written down at all: a catch-all is an assumption nobody stated, and the day it stops being true, nothing anywhere changes colour.
2026-08-15 | M-complete-structs | The count went from a hundred and ninety-one to three hundred and forty-nine in an afternoon, and not one of the steps was a new capability. Each was a refusal that had no way out. One told the author that a description was missing a part they had in fact written down — the compiler was miscounting a nested description as part of the outer one, and there was no way to write the program that would have satisfied it. Another told them to correct a field, and no correction existed: the rule was asking whether the library's own text spelled something a particular way, rather than whether the thing was what it needed to be. Both are the same mistake and both were written the same day the project had quoted, in a design record, a five-year-old bug in another language that is exactly this — an error message naming a repair that does not work. The lesson is not that refusals should be softer. It is that a refusal is a claim, and a claim that no program can satisfy is not conservative, it is simply wrong, and it is invisible from the inside because everything looks correctly strict.
2026-08-15 | M-complete-structs | A whole milestone's guarantee had been switched off by a type nobody was thinking about. The compiler checks that the values a program hands to a foreign function are the widths that function actually takes; that check is built by writing a small function nobody calls, whose parameters are the declared types. The table that spells those types had never been taught about the new kind of value, and the code that builds the list gives up entirely if any one entry is unspellable. So a single unfamiliar argument silently deleted the check for every other argument of that function — four correct-looking numbers going unverified because of a fifth. It was proved by a test file written that same morning, which passed. The tell was small and would have been visible to anyone looking for it: a sister table, answering the same question one file away, had been taught about the new kind and this one had not. Two lists that must agree, and only one of them was updated.
2026-08-15 | M-selfhost-probe | The plan said: port the first file of the compiler into the language itself, hit the wall early, write down what the language still cannot say. The wall was not there. It had been quietly removed three days earlier by a milestone that was about something else, and nobody had gone back to re-read the plan's premise — the file went from "this will fail on file one, cheaply" to a working lexer in an afternoon, in eleven small files that read like the originals. The list of things the language could not say turned out to be twelve entries long and eleven of them cost a couple of lines each. The twelfth is the good one: there is exactly one character the language refuses to spell — the invisible one that old machines put at the end of every line — and the lexer is the one program that must recognise it. The scanner shrugged and named it as a number. The test could not be written at all, because writing it needs that character inside a piece of test text, and there is no way to type it. The best find was better than a gap: the original compiler once shipped a bug because a list of "tokens that can end a line" forgot a newcomer, and the new language makes that exact bug impossible to write — leave a case out of the list and the compiler stops you on that very function. The machine that was being rebuilt refused, by design, the mistake its own builders had made the first time.
2026-08-17 | M-selfhost-port | The compiler now exists twice: once in the language it was built with, once in the language it builds. The test that says the second one is real is not a test suite — it is asking both of them to write out the same program in C and comparing the results letter by letter. Twenty million eight hundred eighty-six thousand five hundred thirty-nine letters, and not one of them different. Then the letters written by the new compiler were themselves compiled, and the compiler that came out of them was asked to write the program a third time: the same letters again. A thing that reproduces itself exactly, twice over. Getting there took a day, and the way it happened is better than the result. Pointed at its own source, the new compiler refused it — claimed a piece of its own structure contained itself, which was false. That one wrong complaint was the thread: pulling it produced five real faults in the new compiler and, more surprising, four in the old one, including a mistake where the underline that shows the reader where an error is was covering the little note written on the same line for the sole purpose of checking that underline. The child corrected the parent. And the very last thing standing between the two files being identical was a message the old compiler had written down for whoever would do this work, months earlier, in three separate places: *this list comes out in order here, and the new one will owe an explicit sort*. Three sorts, unpaid. Twenty million letters lining up behind three words.
2026-08-18 | M-selfhost-fixpoint | The plan for the day was to hand a stranger a way in and then throw away the ladder. The way in works better than anyone promised: twenty-one million letters of C sit in the project now, and one command with no options turns them into the compiler in under four seconds, on a machine that has never heard of this language. The compiler that comes out writes those same twenty-one million letters again, in the same order. Throwing away the ladder is where the day turned. The new compiler, it emerged, could not work anywhere except inside its own source folder — it had been reading its own standard library off the disk from a path pointing into the very folder that was about to be deleted, and when the file was not there it did not complain: it carried on with an empty library and told the author that *their* program was wrong. Every use of the six most ordinary helpers refused, on the author's line, at the exact moment the ladder went. The rule saying the library must live inside the compiler had been written down, argued and approved months earlier; the second copy of the compiler had quietly done the opposite, and nobody could see it, because the only place anyone ever ran it was the one place it worked. Then a second discovery, larger: the check that both compilers agree had never actually looked at the thing it was about. It compared what they *say* — messages, printed output — and the finish line is about what they *write*, a file, which no comparison had ever read except for one single program. Asked properly, for the first time, over a hundred and twenty-seven programs: twenty-six agreed. Three faults hid in that gap, and one of them made the new compiler emit a name for a value that appears nowhere else in twenty-one million letters. All three are gone and the number is now a hundred and twenty-seven out of a hundred and twenty-seven. The ladder stays up for another milestone, and the reason is the sharpest sentence of the day: the thing that catches these faults is the old compiler, so the day it is deleted is the day nothing can ask the question again.
2026-08-18 | M-harness-port | Everything that checks whether the compiler is right was written in the language the compiler is being replaced with, and all of it would have died on the same afternoon. So it was rewritten in the language itself — six hundred and eighty-eight checks, three and a half thousand lines, a program that runs the compiler the way a person does: type a command, read what comes back, compare it to what was promised. The moment it was pointed at the new compiler testing itself, it went red on two things nobody could have seen before. One was an error message with nothing in it — no code, no line, no piece of the program underneath — printed in the one situation where the compiler is allowed to say *your* mistake, so the reader was told a library was missing and not which line had asked for it. The other was stranger and better: the underline that marks where an error is was too long, and the file that recorded what the message should look like had been storing that wrong length as correct since the day it was written. No review found it. What found it was a second implementation disagreeing — the new compiler drew the underline correctly, so the old one's record of itself was exposed by being contradicted. The rest of the day was full of the same lesson in miniature. A count written down by looking at the screen instead of running the count: wrong twice, caught twice by an assertion that prints what it actually found. A rule about how long a file may be, ported straight across, would have been red on its first run for seventeen files — because in this language the tests live inside the file they test, and because some files cannot be split at all: the language forbids two modules from calling each other, so a knot of mutually recursive code is one file or nothing. The rule that cannot be obeyed is not a standard, so it became a ceiling per file: this one is this long today, and it may not get longer. And a shell command with a redirection at the end, written the obvious way, sent half its output to the terminal instead of the file — found within a minute by the net's own tests, which is the whole argument for building the net before you need it.
