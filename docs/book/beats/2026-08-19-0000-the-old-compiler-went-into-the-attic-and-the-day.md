2026-08-19 | M-bootstrap-archive | The old compiler went into the attic, and the day was spent making sure nothing important was still living in it. A compiler that can build itself has one embarrassing dependency left: the *first* compiler, written in another language, which is also — quietly — where half the project's instruments were kept. Not the tests of the language: the instruments *about* the language. The thing that counted how big the specification is, and the ledger recording what every past sentence of it had cost. The thing that takes a working program, introduces one plausible mistake, and counts how many the compiler catches — the single measurement this whole project exists to move. The thing that asked whether the two compilers agree. All of it in the attic-bound crate, and none of it noticed until somebody wrote down what the move would delete. So five successors were built first, and each one had to prove itself against the thing it replaced rather than merely compile. The counter now agrees with the old one to the token — three thousand five hundred and twelve, on the nose, out of two entirely different implementations, because the new language cannot spell a raw byte and the port had to invert the whole algorithm to work around it. The mutation instrument produces a report identical letter for letter, five hundred and thirty-eight introduced mistakes, ninety-six percent caught, the same in both columns. And the comparison of two compilers, which cannot survive having only one, was frozen into a hundred and forty-two files of generated C: the last thing two independent implementations ever agreed on, written down while both were still alive. Then the sitting about a ledger. Thirty-eight rows of history inside a source file that was about to be archived, and two sentences elsewhere calling that file the *live* home of the number. Three judges, three different measurements — the ledger weighs more than three times the document it guards — and one finding none of them was asked for: nothing had ever checked that the ledger's newest number and the number the compiler enforces were the same number. They had merely been written two lines apart, for months, and that had been mistaken for a mechanism. It is a mechanism now. The last check written that day was one that shouts when a document points at a file that is not there — and it went green on arrival, which was itself the bug: it only recognised paths that begin with a folder that exists, so the moment the folder moved, every citation of it stopped counting as a citation. Fixed, it immediately found nine dead pointers that had been wrong long before the move, in the two documents the whole project is steered by. Nobody had noticed. Nothing had looked.
