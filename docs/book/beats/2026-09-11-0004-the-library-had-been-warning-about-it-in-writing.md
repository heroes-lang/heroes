2026-09-11 | M-named-callbacks | The library had been warning about it in writing for weeks, inside its own source, where nobody but the compiler reads: give the function that combines two things an accumulator and an element of the same kind, and either order type-checks, and the program quietly returns a different answer. The council had said this was the price of a rule already written down and not a fault in it. The author said close it anyway. So the type of the combining function learned to say which of its two is the pile you have built so far and which is the next thing, and the compiler learned to check that the function you hand over agrees. Then came the measurement that decides what a rule is worth: the gentle version, which complains only if you borrow the library's own words and put them back to front, catches two of the seven real cases in this project. The strict version catches seven, and the price is that everyone who ever folds anything has to call their two parameters what the library calls them. The author read both numbers and chose the strict one. The proof that it was the right call came from a reader who had never seen the project: handed only the written language, it wrote the same two words three times in three different programs without once stopping to choose them, and said that left to itself it would have invented three different vocabularies for one function.
