# Panel 148 — spec-warden

Read `00-shared.md` first. You judge the indicator (design.md §1.2, §1.6) and
Principle 0's burden of proof. **Veto on budget breach.**

## The numbers, measured 2026-09-14 with `heroes measure`

| | vendored (`cl100k_base`) |
|---|---|
| `spec/heroes-spec.md` today | **5863** |
| A — the word `acquires` plus its production | **5927** (**+64**) |
| C — no new word, inferred from `consumes` | **5919** (**+56**) |

Live spec **7806 real** against a ceiling of **8192**; headroom 386, the FFI
floor mortgages 60, so **326 free**.

**The real delta for a draft cannot be taken** — `--refresh` is refused for a
file no ceiling judges (panel 123 R5) — and **do not convert with a ratio**:
`docs/measurements/010` says in terms *"No row is convertible: 1.275 is a
property of one day's mix of prose and code spans, not a factor."* Panel 147's
coordinator did convert and the warden of that sitting caught it. Give a range
or say unrun.

Drafts: `…/scratchpad/panel-148/spec-a.md` and `spec-c.md`. Read the diffs
rather than trusting my summary.

## What only you are asked

1. **Principle 0, per option.** Which limb — the §1.0 closure list, or a
   measured thesis effect? `selfhost/` declares **zero handle records** and has
   **one** acquire-and-release pair, which is the runtime's own and cannot carry
   either mark. Decide what that means and say it plainly.
2. **The §1.6 payment**: a named removal, or a registered prediction naming an
   instrument that EXISTS. If neither, that is a veto and you should cast it.
3. **§1.7's subtraction** for each.
4. **The eight-token gap.** A and C differ by 8 vendored tokens. Say whether
   the budget can distinguish them at all — and if it cannot, say so, because a
   seat that manufactures a distinction it did not measure is worse than a seat
   that abstains.
5. **Check the premises you are handed** rather than inheriting them. The census
   that convened the parent sitting was wrong four ways and nobody had audited
   it; finding a fifth here would be the best thing you could do.
