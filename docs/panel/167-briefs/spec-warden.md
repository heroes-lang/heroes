# Panel 167 — brief for the spec-warden

Read `00-shared.md` first.

**Measure, do not estimate.** `heroes measure spec/heroes-spec.md --refresh`
needs `ANTHROPIC_API_KEY`; source the trunk's `.env` into your copy. If the
instrument is unavailable, say **unrun** in those words and mark every figure a
lower bound — the vendored tables underprice a draft by 20 to 30%, which your
own seat measured at panel 166.

Today's baseline, run while this brief was written: vendored **6126**, real
**8154** on `claude-opus-5`, digest `249990ca1b6b2f66`, ceiling **10240**, FFI
floor **60**, so **2086 free** and 2026 net.

## What this seat is asked

**Draft and price the § 13 sentence each route needs**, one draft per route, and
say which is cheapest for the truth it carries. Route A needs a lease-shaped
sentence for the field direction; route B needs a word on `CParam` and its
production row; route C is a sentence stating the hole.

**And answer the question that is yours alone here:** § 13 already carries the
`cstr` lease's lifetime sentence. **Does the field direction need its own
sentence, or does the existing one generalise?** Read it and say — a rule that
joins a sentence already there costs less than a new one, which is your seat's
own finding at panel 122, measured across three drafts.

**Principle 0 applies and you are its keeper.** The compiler does not need any
of this to compile itself: `.ptr()` is not on the closure list. So each route
is judged on design.md §1.12 and Part 11, and an addition owes a named removal
or a registered falsifiable prediction naming an instrument that exists today.

## What would change your verdict

Name it. If a route's cost depends on a wording nobody has drafted, refuse to
rank it and say so — that is what you did at panel 166 and it was right.
