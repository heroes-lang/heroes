# DESIGN-LOG — append-only, one line per decision

Format: `date | decision | reason | design.md § | panel`

2026-08-03 | Bootstrap compiler in Rust, Cyclone-rule subset, retired at fixpoint | author preference; §3.4 comfort list; port must be mechanical | §3.4 | 000
2026-08-03 | Backend: emit C11 via clang, replacing QBE | FFI is the founding constraint; clang verifies signatures against real headers; rationale is §1.11/§4.19, NOT optimisation | §1.11, §3.1, §4.19 | 001
2026-08-03 | Keep three-address IR with explicit basic blocks; emit C from it | Part 10 step 6 is the heart; C emitted with goto+labels keeps CFG visible | Part 10 | 001
2026-08-03 | QBE becomes a scheduled second backend (post-fixpoint), not "maybe" | one backend proves nothing about IR agnosticism; preserves the register lesson | Part 7 item 14 | 001
2026-08-03 | One executable `heroes` with subcommands; never a second binary | corollary of §3.3; the zig model | §3.3 | 004
2026-08-03 | Panel mandatory on design decisions; redesigned with differentiated inputs + falsifiable predictions | author decision; rev-1 design produced correlated verdicts | — | 000
2026-08-03 | Principle 0: self-hosting is necessary-not-sufficient for a v1 form | the strict version would delete the thesis features (???, same-typed args, rich errors) | §1.1 | 000, 005 pending
2026-08-03 | Spec v0 written at M0 as the pre-amendment measurement baseline | design.md L77 "build the harness early"; spec-warden needs a measurable number | §1.6, Part 11 | 000
2026-08-03 | Two new compiler passes: type descriptors (copy/drop/eq/hash) + ownership (refcount insertion in lowering) | C has no copy ctors/dtors; §4.3 structural ==; §4.8 copy-out on every exit edge | §4.3, §4.8, §4.10 | 000
2026-08-03 | Container representation + descriptor ABI fixed by spike 04 (header + inline elements, per-type descriptor struct) | decided by hand-written ASan-clean code before any compiler code | §4.10, §4.20 | 000
2026-08-03 | Fixpoint acceptance = byte-identical generated C (diff B.c C.c), not Mach-O binaries | binary identity depends on clang/ld noise (LC_UUID, DWARF paths) | Part 0 | 000
2026-08-03 | Emitter always enters via explicit `goto bb0` | spike 02 finding: entry label is never a jump target → unused-label warning | — | journal 000
2026-08-03 | Errata applied to design.md: Part 2 heading restored; `&`→`&&` in §4.3/§4.8 examples | document already legislates && at §4.14; these were internal inconsistencies | Part 2, §4.3, §4.8 | 000

2026-08-03 | design.md is the LIVING document: every decided amendment is applied directly, history lives in git, open questions are inline OPEN QUESTION markers | author instruction | — | —
2026-08-03 | spec v0 stays FROZEN until the pre-amendment baseline run; then decided amendments produce spec v1 | preserves the only unrecoverable measurement | §1.6, Part 11 | 000
2026-08-03 | End goal declared: a mini-book on how the language came to be; story beats collected per step in docs/book/beats.md | journal/DESIGN-LOG/panel are the sources; beats keep the human texture | Part 0 | —
2026-08-03 | Showcase site stub in site/ for heroes-lang.org (domain owned); deployment deferred, ask before publishing | personality in the packaging, precision in the substrate | §"The name" | —
2026-08-03 | Part 0 teaching protocol switched to low-typing form: closed-question predictions (answers transcribed verbatim, still committed before src/), comprehension checks after each step, dictated explain-back, golden cases approved (with stated purpose) rather than authored | author instruction — typing cost was blocking the protocol; retrieval practice kept, written production dropped | Part 0 | —
2026-08-03 | Friction glossary: every comprehension gap produces a docs/glossary/NNN-<concept>.md entry (numbered in birth order = reading order) — English like all artifacts (rule 11, no exceptions), using the /where canonical analogies, origin cited, historicized, never deleted; absorbs docs/theory | author instruction — good explanations were evaporating in chat; first entry: 000-basic-block | Part 0 | —
2026-08-03 | Prediction records are private: raw answers live in git-ignored docs/journal/private/, the committed NNN-prediction.md carries the questions plus a SHA-256 seal of the raw record (precedence stays provable); journals record divergences as impersonal lessons — shapes and rules, never scores | author instruction — personal performance does not belong in a future-public history | Part 0 | —
2026-08-03 | Teaching gates attach to concepts, not steps: prediction + spot-checks fire when a new concept enters (typically once per milestone); plumbing steps chain autonomously; journal + story beat per milestone, DESIGN-LOG per decision | author feedback — the per-step ceremony felt slower than the learning it bought | Part 0 | —
2026-08-03 | `ok(x)` adopted as `fail`'s symmetric twin; both checked against the expected type (⇐ mode); no implicit T→T? ever; eleven appendix sites rewritten ok(...) | author decision, panel unanimous; ⇐ machinery already owed to §4.5; Swift SE-0230 is the implicit-promotion counter-precedent | §4.6 | 002
2026-08-03 | A non-() expression in statement position is a compile error; diagnostic ships the certain fix `_ = expr` (lowers to `(void)e;`); no extern relaxation — escape valve if ever needed is a per-extern annotation at M7 | author decision, panel unanimous; Nim/Zig hard-error precedent held; implement as ⇐-check against () sharing 002's machinery | §4.14 | 003
2026-08-03 | §1.0 ratified (stopping rule + burden of proof); closure-list audit at the M6 checkpoint with three riders: mechanical (grep, not opinion), spec-coverage with named cuts, tier assignment for file I/O/args()/exit | author decision; 4 approve + 1 abstain; historian: no precedent for self-hosting as feature freeze — deliberate departure | §1.0 | 005
2026-08-03 | Maps iterate in insertion order, overwrite keeps position, seed fixed (unobservable); print is a compiler form over str/int/f64/bool — canonical rendering, no separator, one trailing newline, deterministic locale-independent f64; Rust bootstrap marks ordering-sensitive BTreeMap walks (// ORDER:) for explicit sorts in the port | author decision; 4 approve, warden's narrow objection on (b) resolved by completing the contract; Python 3.7/ES2015 precedent, Go condemns only unspecified order | §4.9, §4.20, §1.0 | 006
2026-08-03 | Process inverted: implement-first, comprehension post-hoc via docs/debrief/QUEUE.md + /debrief skill; predictions and SHA-256 seals retired; golden ratification, failure diagnosis, drills and exit-quizzes become queue items; panel asynchronous (conservative provisional default, author ratifies later); journal 3 sections + beat per milestone; every process rule single-sourced (/step is the protocol's only home) | author instruction — the ceremony outpaced the learning it bought and its author-gates made autonomous runs impossible; the author chooses where and when to understand, default after | Part 0 | —
2026-08-03 | docs/ROADMAP.md reintroduced (revision 2 had deleted it) as the in-repo goal chain, M0–M8c with acceptance criteria | autonomous sessions need "what is next" versioned; tags only say where you are | — | —
2026-08-03 | Terminator ender-list completed (+break, +continue, +???, +postfix ?) and continuation specified: brackets suspend indentation (no Indent/Dedent inside ( [ {), terminators unchanged everywhere, unclosed opener = EOF error citing the opener, NO depth-0 continuation (parenthesize; Nim-style trailing-operator deferred to baseline evidence, 007-bis); spec v0 and the v1 package untouched | panel 007, author ratified: engineer killed the naive depth-0 clause (record/variant/else headers end in non-enders), ffi showed postfix ? ender is required by fallible externs, warden's arithmetic kept the spec sentence out; ergonomist vs warden predictions on record for the baseline | §4.15 | 007
2026-08-04 | Code-readability rule: source files short and single-concern (split before ~300 lines), module doc per file stating role + design.md §§, comments teach the invariant; lexer restructured as the exemplar (mod/token/keywords/layout/scan + tests by theme) | author instruction — "voglio poter leggere quel codice senza impazzire"; the reader is the author, not just the compiler | — | —
2026-08-04 | Escape sequences: five, split by context (Go's rule) — strings `\n \t \\ \"`, char literals `\n \t \\ \'`; backslash reserved, any other escape a compile error with a certain fix; `\r` cut; set frozen (`\0`/`\xNN`/`\u{}` reconvene the panel — ffi holds a veto: interior NUL voids §4.20's free `.cstr()`); char contract restated (one ASCII byte OR one escape) | panel 008, author ratified: without `\"` the language was INCOMPLETE (a quote inside a string was unwritable) and `"a\nb"` compiled printing four characters — a silent wrong-output trap, the anti-thesis; printf's `"%d\n"` and sqlite3_bind_text data were unexpressible (ffi compiled the proof) | §4.3, §4.15 | 008
2026-08-04 | Spec budget raised ~1500 → 2000 tokens; governance adopted: measured-only (the 1.33 tok/word heuristic retired as an authority), soft 1500 / hard 2000, the +500 is a pre-allocated purse (closure mortgage first, then the deferred sentences) not headroom, spend only where a wrong guess is SILENT, no second raise without a measurement | author decision; retro-record panel 009 — 1500 was never derived (§1.6 cites Oberon's sixteen pages, which supports a LARGER number) and was being defended with estimates spanning 1496–2058 on the same file, in contradiction of design.md Part 10's own "count it with a real tokeniser" | §1.6, §1.2, §1.0 | 009
2026-08-04 | Spec-budget governance ratified: measured-only after M3 (the word heuristic retired as an authority), soft 1500 / hard 2000, the +500 is a pre-allocated purse (closure-list mortgage first, then the deferred escape/layout/`()` sentences) and "there is headroom" is never a reason, spend only where a wrong guess is SILENT | author ratification of panel 009's proposal, debrief 2026-08-04 | §1.6, §1.2 | 009
2026-08-04 | M1's five adversarial lexer cases ratified; each now states what it guards against and the UNVERIFIED marker is dropped | author ratification, debrief 2026-08-04 — they defend one bet: whitespace carries meaning, so every "nearly right" margin is an error, never an interpretation | §4.15 | —
2026-08-04 | Two `@` arguments of one call may not share a root binding — compile error at M3c with a certain fix (pass a copy); over-rejects distinct-index element pairs by design, relaxable later; buys the emitter a non-aliasing invariant (direct-pointer lowering and `restrict` stay legal); 0 spec tokens | panel 010, adopted provisionally (author ratification queued): left legal the call's meaning depends on copy-out order, which nothing specifies (16 / 15 / 6 under the three readings) and no sourced language specifies; Ada arrived at the same rule in 2012 after 33 years of arbitrary order, Swift SE-0176 and Hylo do the same on the identical copy-in/copy-out model; E is the precondition that makes §4.10's "no aliasing exists anywhere" true | §4.8, §4.10 | 010
2026-08-04 | Spec MEASURED offline for the first time: v0 = 1989 tokens (Anthropic legacy) / 2048 (cl100k) / 2050 (o200k) — the word heuristic said 1496, low by a third. The 1500 budget was breached the day it was written; the 2000 ceiling is breached on the pessimistic bound TODAY. Budget redefined as a hard measured ceiling of 2000 = max over two vendored tokenisers (pinned by sha256, zero dependencies, no API key); Part 11's unsourced "tiktoken undercounts 15-20%" struck — cl100k measures 3% ABOVE Anthropic's own legacy tokeniser here | panel 011 — the API-free instrument the project assumed impossible took twenty minutes and revealed that every budget verdict since panel 002 rested on a wrong number | §1.6, §1.2, Part 10, Part 11 | 011
2026-08-04 | Metric 2 is NOT dead: "needs a model in the loop" is not "needs an API key" — a local open-weights model runs 20x5 unattended, and paced sampling (2 pre-registered tasks per milestone) yields ~16 samples by v1. Metric 3 becomes primary but is never pooled: per-operator, three arms (strict / permissive / C+Python transliteration) plus a meaning-preserving counter-arm, operators sealed and externally cited, "forget the @ marker" excluded from any headline (100% catch by construction = zero information) | panel 011 — metric 3 alone measures the detector, never what a model writes; the thesis keeps "measured mechanism", loses "measured claim" until metric 2 runs, and Part 11 now says so | Part 11, §1.1 | 011
2026-08-04 | Spec budget raised 2000 -> 3000 tokens MEASURED (max over two vendored tokenisers); soft/hard split rescaled to soft 2000 / hard 3000; panel 009's governance survives verbatim with the number substituted (measured-only, pre-allocated purse, spend only where a wrong guess is silent, no further raise without measurement + panel + named alternative) | author decision, panel 012 retro-record — the first budget number in the project's history set with the artifact's real size in view (v0 = 1989/2048/2050, panel 011); the named alternative was cutting SPECCED SURFACE, not deferring additions, since v0 already exceeded 2000 on the binding instrument | §1.6 | 012
2026-08-04 | Panels 010, 011 and 012 ratified by the author: repeated `@` arguments are a compile error; the spec budget is a hard MEASURED ceiling of 3000 (max over two vendored tokenisers, spread published per run); metric 3 is primary but never pooled (per-operator, three arms, meaning-preserving counter-arm, sealed and externally-cited operators); metric 2 runs via a local open-weights model or paced sampling, not an API key | author ratification | §1.6, §4.8, Part 11 | 010, 011, 012
2026-08-04 | Function type marker becomes `function`: `(function(A, B) -> C)`, parens still mandatory, `fn` keeps its reserved-word error and its certain fix in every position; arity stated (two parameters `(function(int, int) -> bool)`, none `(function() -> C)`); spec v0 untouched, v1 amendment text recorded with measured cost (spelling +0, arity +14 → 2062); design.md's five `fn(` sites amended; §4.19 gains "FFI callbacks are `ptr` until a C-width vocabulary exists" | panel 013, adopted provisionally (author ratification queued) — the recommendation put to the judges (D: `fn` as a type-position-only keyword) was overturned by their evidence: §4.17 names `fn` itself among the autopilot mistakes the registry exists to catch, §1.9 refuses familiar spelling as a tie-breaker and the tokeniser measures the two words identically, seven ancestor languages reuse the declaration word in type position and none uses a second word, and under `fn` the registry's machine-applicable fix produced a syntax error in the one position where the word was required. Found because `(fn(int) -> int)` did not lex: every function-typed parameter was unwritable | §4.12, §4.13, §4.17, §4.19, §1.9 | 013
2026-08-04 | Foreign reserved words are reserved EVERYWHERE, including as user identifiers: the registry's words cannot name a variant case, a field or a local (found by parsing the appendix, whose `Expr.var` case did not lex; renamed `.variable`). The price is now on the record — `var case union use include class try const` are unusable as names — and queued for a panel if it ever bites a real program | consequence of panel 013's "one word, one meaning, everywhere", applied to the other side of the boundary; the alternative is contextual lexing, which panel 013 rejected on four judges' evidence | §4.17, §4.2 | 013
2026-08-04 | `heroes fmt` policies fixed (§4.15's mandatory canonical formatter): minimal parentheses (associativity included, so `a - (b - c)` keeps its own); breaking at 88 columns and ONLY inside brackets, lists by newline (§4.9) and argument lists by comma, a statement with no bracket stays long; blank lines are content — one always between top-level declarations, one survives inside a body, runs collapse; `-> ()` never printed; the blank line between a comment and the declaration below it preserved exactly, because it decides documentation vs remark (§4.1); every comment kept, including the ones the tree does not hold | the formatter removes style decisions from the writer only if it makes them all; 88 is one constant, not a language rule | §4.15, §4.9, §4.1 | — |
2026-08-04 | Golden `check/` harness runs through the deepest frontend stage, which MOVES (lex at M1, parse from M2, check at M3d) while the cases stay: a case written for the lexer must keep saying the same thing when a later stage reads the same file. Each move refreshes expectations exactly once, diff read and quoted in the commit body; UPDATE_GOLDEN stays forbidden in check/ | the alternative — a case per stage — multiplies the cases and hides regressions between stages | CLAUDE.md §9 | — |
2026-08-04 | Conservative parser readings taken where the spec is silent, each queued rather than decided: parameters separated by `,` only (§4.9's newline separation is stated for literals); `T??` rejected (§4.6 refuses the level ambiguity); a `match` arm body must be an expression or a block, so `=> assert false` is an error (the appendix wanted it in 8 places — recommendation on record to unify arm bodies as blocks) | a rejection can be relaxed later, an acceptance cannot be withdrawn | §4.6, §4.7, §4.9 | — |
2026-08-04 | Panel 013 RATIFIED by the author: the function type is `(function(A, B) -> C)` and `fn` stays a reserved-word error with its certain fix in every position; a foreign reserved word is an error everywhere, including as a variant case, field or local name (the appendix's `.var` case is now `.variable`); §4.19 carries the ffi condition that FFI callbacks are `ptr` until a C-width vocabulary exists | author ratification, debrief 2026-08-04 — ratified *with the reversal read*: the recommendation put to the judges (`fn` as a type-position-only keyword) died on their evidence, because the registry's value is its uniformity and a certain fix that produces a syntax error where the word is required spends that uniformity for six characters | §4.12, §4.13, §4.17, §4.19 | 013
2026-08-04 | M2's five adversarial golden cases ratified (`fn` in both positions · depth-zero line continuation · braces as a block · signature without its `:` · trailing comma); the UNVERIFIED marker is dropped and each case states the bet it guards | author ratification, debrief 2026-08-04 — all five defend the same class: a habit imported from another language must fail loudly, not be interpreted | §4.15, §4.9, §4.2 | 013, 007
2026-08-04 | Indentation *recovery* improved: a wrong margin is rounded to the nearest level, and a jump never opens more than one level (clamped), while both diagnostics fire unchanged. Measured on tests/golden/check/indentation.hero: 2 mistakes produced **5** diagnostics, now produce **2**. The distinction is stated in the code because it will look like a contradiction later: §4.15's "errors, never interpretations" governs what the compiler *accepts* — the program is still rejected — while recovery governs only the structure handed to the next stage, and an implausible structure arrives as extra diagnostics about a mistake already reported | author instruction, debrief 2026-08-04 ("layout → migliora"), chosen over gating the parser after a layout error: gating would have hidden genuine parse errors in the rest of the file, while better recovery hides nothing. M1's two ratified adversarial cases keep their inputs and their diagnostics; their snapshots and stated rationale are updated (the jump case used to praise honouring two levels — nothing in the language opens two) | §4.15 | — |
2026-08-04 | A `match` arm's body is one statement, inline, or an indented block (panel 014, adopted provisionally — author ratification queued). §4.7 also gains the two rules the judges converged on: a jump (`break`/`continue`/`return`) is admissible as an arm body and must NOT be typed `()` (RFC 1216's recorded wrong turn), and a `match` used as a *value* requires every arm to produce a value — a checker rule at M3c, orthogonal to the spelling. R (`assert` as an expression) VETOED by the compiler-engineer: no rejection site for `x = assert y`, a §4.14 precedence row to invent, and Part 5's desugaring has no value for a three-address temp; the spec-warden's cheaper S is R's parser half and dies with it. The `?`/M4 rationale put to the judges is STRUCK from the record as false | panel 014 — Q was recorded as "0 lines" and is not: the engineer built P and measured net +10 lines in 5 frontend files (0 in checker/ownership/emitter) while finding three shipped defects with one cause, the `ArmBody::Expr` bypass — `heroes fmt` deleting an arm's `if` branches, `heroes parse` never terminating, and a doubled diagnostic. The ergonomist showed the guard Q appeared to provide never existed (the block form re-admits every valueless arm). Kotlin's grammar is P verbatim; Scala's since 2.x. v1 spec text measured: bare +1, enumerated with the "`if` is an expression" duplicate absorbed +10 (2058), the value rule a further +18 and deferred to M3c | §4.7, §1.7 | 014
2026-08-04 | Resolver rejection set fixed (panel 015, adopted provisionally — author ratification queued). **A**: unused reaches parameters (`extern` exempt, top-level declarations not covered), `_` legal and repeatable as a parameter name, and §4.16's hole suppression covers parameters too. **B**: only a *read* is a use — the initialiser is not a write, a field/element write writes the root (closing the hole Go left open at golang/go#20802, free here because §4.10 has no aliases), and a write through an `@` parameter IS a use (§4.8's copy-out is the whole observable effect). **C**: the two-tier built-in namespace is WITHDRAWN — one tier, every name in §4.20's inventory reserved everywhere including `extern`; the tier field survives as documentation of where the implementation comes from, rebuilt from §1.11/§4.20 (`range` is Tier 2, `.str()` and `sort` were missing, `panic`/`Builder` deliberately excluded). **D**: resolve UFCS names at M3a but report an unknown one only when no record or variant in the file declares a field of that name. **E**: sibling scopes may reuse a name, and the enclosing-shadow check is SEQUENTIAL — a deliberate departure from Java JLS §6.4 and C# CS0136, both order-insensitive, on §1.3 (a line below must not decide whether the line above compiles). Net spec cost 0 | panel 015: C died from two directions — the ergonomist vetoed the silent override on locality (`xs.map(f)`'s meaning would depend on a distant line) and the warden on Principle 0 (an exception to a rule the spec already states, +23/+48 measured); D's rejection half was VETOED by the engineer with a compiled counterexample (`h.cb(n)` on a function-valued field, legal under §4.13, rejected twice by its prototype) and survives only narrowed; A's escape valve was missing and three judges converged on it (Zig is the only shipped precedent and all six enforcing languages ship `_`; the `sqlite3_exec` trampoline needs ≥2 unread parameters). Measured: 4215 C headers scanned, zero collisions with the reserved list; the witness has 53 UFCS calls, not 23 | §4.4, §4.16, §4.11, §4.20, §1.11, §1.3 | 015
2026-08-04 | design.md's appendix renames its two library functions `map`→`apply` and `fold`→`reduce`; §4.20's inventory reserves both names and no file may redeclare one | consequence of panel 015's one-tier resolution, applied to the acceptance program — the third rule the appendix has caught (after `.var` at panel 013 and `=> assert false` at panel 014); the section demonstrates generics and any name demonstrates them | Appendix, §4.20 | 015
2026-08-04 | `heroes check` is the frontend command from M3a on, and golden `check/` runs through it instead of `parse`; the resolver runs only when lexing and parsing produced nothing, so a broken file reports exactly what it reported before | the stage the goldens run through moves with the deepest frontend stage (DESIGN-LOG 2026-08-04); the name now stops moving — M3b–M3d deepen `check` without renaming it | CLAUDE.md §9 | — |
2026-08-04 | Panel 015 RATIFIED in full (author instruction, `/goal` — final panel decisions ratified without a separate sitting), and M3a's five adversarial goldens ratified with it; each case now states the bet it guards and the UNVERIFIED marker is dropped. Two items stay open on their own terms because panel 015 had no standing to settle them: `print`'s reservation against PEP 3105's counter-precedent, and `+` on `str` (referred to panel 017) | author ratification | §4.4, §4.16, §4.11, §4.20 | 015
2026-08-04 | `heroes fmt` gains two policies, both of them "the layout the author chose is content": a run of consecutive single-line `match` arms **aligns its `=>`** (the run stops at a block-bodied arm and at a blank line), and a **list literal written across lines stays across lines** even when it would fit on one (§4.9 gives the two forms different separators, so joining it deletes a grouping rather than rewrapping it). A blank line between arms now survives, like a blank line between statements. design.md's appendix is reformatted to canonical form and a test now asserts it byte for byte — the reference aesthetic and the tool's output are the same artifact | author instruction (`/goal`, "comincia a pensare all'ergonomia"), applied to the formatter first because the appendix had been hand-aligning its arms since before any compiler existed. Two defects found by the new tests, both mine, both in the blank-line bookkeeping: a spurious blank line after every multi-line list, and — on the first attempt at the fix — a `last_line` reaching past a `Dedent` and stealing the next declaration's doc comment, which is the bug panel 014 had already fixed once and whose comment warned about exactly this | §4.15, §4.9, §4.1 | — |
2026-08-04 | `examples/gallery/` born: nine programs written to be read (records · variant+match · `T?` · loops · `@` copy-out · generics · strings · FFI · holes), each held to four properties by the suite — parses clean, resolves clean, canonical byte for byte, tree preserved by formatting | author instruction (`/goal`): "così comincio a dare un'occhiata estetica". All nine resolved on the first attempt, which is the datum: the resolver's rules do not fight code written without the resolver in mind. The corpus is now the widest regression surface the frontend has | §4.15, CLAUDE.md §9 | — |
2026-08-04 | Type-system edges settled (panel 017, RATIFIED same day by author instruction). **A**: a jump (`break`/`continue`/`return`) has **no type** — the engineer built all three options and measured that typing one at all rejects every function ending in `return` (RFC 1216's wrong turn, reproduced); adopted with the historian's withdrawal condition met, a **single shared join routine** (`types/join.rs`) rather than a rule per construct, and **no bottom type** in the table so M5c's descriptor pass never meets an uninhabited type. design.md §4.7's two bullets contradicted each other on `tag = match t` with a `break` arm and are amended: "every arm must produce a value **or jump**", with the value rule's payoff re-attributed to a `()`-typed arm body, where the silent program actually was. **B**: `+` joins two `str` (concatenation, no implicit conversion), and the int-to-string conversion enters the inventory as a DEFECT FIX named **`to_str`** — §4.20 had it as `.str()`, the spec omitted it entirely, and without it the language cannot render a number into a message (the ergonomist produced the string from spec v0 in 28 lines with two silent failure modes). **C**: a `()` value cannot be bound — Nim's rule verbatim, in the language CLAUDE.md §6 copies the surface of; the fix is `Guess`, not `Certain`, because a `Fix` carries one span and deleting the binding would orphan the read. **D**: a declaration as an inline arm body is rejected **in the parser**, where Rust puts it, because in the checker the resolver speaks first and the user gets two diagnostics for one mistake | panel 017 | §4.7, §4.14, §4.20, §1.11, §4.3 | 017
2026-08-04 | `heroes check` runs three stages in order — lex+parse, resolve, check — each only if the one before it said nothing; and the acceptance program now **type-checks clean**, which found a twelfth `T`-where-`T?` site panels 002/006 had missed (`tokenize`'s `return out`). Two soundness holes found by the checker's own tests: a value block ending on a *statement* was treated as if it diverged (so `x = if c` with a loop in the branch bound `x` to nothing), and binding a `()` reported twice | M3b/M3c implementation | §4.5, §4.7 | 017
2026-08-04 | `heroes` command surface fixed (panel 016, RATIFIED same day by author instruction): design.md §3.5's spellings implemented verbatim (`lex --dump-tokens`, `parse --dump-ast`, `check --dump-scopes`, `--dump-ir` reserved for M4) and every stage now prints **only when asked**; `--json` says *how*, never *what*; the mutating flag is `--in-place` (`--write`/`-w` retired, each naming its replacement, as the language does for `fn`); a three-value exit contract **0 clean / 1 the input has diagnostics / 2 the tool could not run** printed in `--help` and asserted by the golden harness; **one strict table-driven argv parser** with an `enum Tag` (a table entry with no dispatch arm is a compile error) and an unknown-flag error that *enumerates* what is accepted; `--emit-c` stays an output, outside the `--dump-*` family; `measure` states its default. The stopping rule enters CLAUDE.md §10 | panel 016: the ergonomist counted **nine guesses, four silent** in the old help text — `fmt --write` reads as "write the output somewhere", so the plausible misreading destroys a file while exiting 0 — and vetoed the status quo; the engineer vetoed merging `--emit-c` into a dump family (its bytes are pinned by the determinism diff, and no shipped compiler merges artifacts with debug dumps: rustc splits `--emit=` from `-Zunpretty=`, Swift needed `-save-*` because dump flags halt); the historian objected to the rename as unsourced and is recorded objecting, while POSIX (`grep`, `diff`), javac and ruff carry the exit codes. The CLI crate had **zero tests and 112 duplicated lines**; it now has ten tests of the contract | §3.5, CLAUDE.md §10 | 016
2026-08-04 | CLAUDE.md §7's `--no-line` is refused by panel 016's stopping rule and will not be built at M5a: it exists for debugging the emitter (the author's activity, not the tool's capability) and doubles the emitter's output space while the determinism diff can only be green in one mode. The emitter's test helper strips `#line` instead (~3 lines) | panel 016, engineer's stopping rule applied to a flag CLAUDE.md §7 currently mandates; recorded here so M5a does not build it by contract | CLAUDE.md §7, §3.1 | 016
2026-08-04 | **spec v1 applied; the freeze is over.** The pre-amendment baseline the freeze existed to protect is preserved twice over — panel 011 published v0's readings (1989 / 2048) and git holds the file — and the baseline *run* the DESIGN-LOG made the condition has now happened (metric 3, docs/measurements/001). v1 lands seven panels' amendments (007's layout, 008's five escapes, 013's `(function(A) -> B)`, 014's arm body, 015's `_` and the reserved names, 017's `+` on `str` and `to_str`/`sort`) and spends 160 measured tokens of removals, the largest being a **96-token preamble that claimed the budget was ~1500**. Measured: **2136** binding max, from 2048 — under the hard 3000 with 864 of headroom, above the soft 2000 with named removals spent and three judges' pre-registered predictions on record (panel 012's governance, both limbs) | the warden's finding, and it was not a budget one: panel 013 RATIFIED `(function(A, B) -> C)` while the spec still said `(fn(A) -> B)`, which is a reserved-word **error** in every position — so for four milestones the document CLAUDE.md §1 has the assistant read at every session start briefed a model into a compile error, and §4.20's inventory named two built-ins the spec omitted, making `sort = function:` spec-legal and resolver-rejected | §1.6, §4.13, §4.20, §4.14, §4.4 | 013, 015, 017
2026-08-04 | The spec is now tested against the compiler, not proof-read against it: every name the compiler reserves must appear in a code span of the spec, every name the spec offers as a built-in must exist, no word the foreign-word registry rejects may appear in the spec's code, and the measured ceiling is an assertion. The warden's prediction of 1998 for the v1 package is FALSIFIED at 2136, and the reason is on the record — ~30 tokens were spent stating the unused/use rule against its explicit condition 3, on the llm-ergonomist's belief probe: asked whether a write-only cell is an error, a model answered "no" from memory with 60% confidence about a proposition the document did not contain | the class of defect that shipped for four milestones is now impossible to ship silently; the budget was never the constraint (the warden's own words: "budget is not the constraint; the freeze is") | §1.6, §4.17 | 012, 015, 017
2026-08-04 | The declaration and block-header shape examined end-to-end (panel 018, worktree; the axis — which side of `=` the keyword sits on — was a first visit). **A stands, provisional**; the author's inversion (`record = Point`) rejected by every seat (its "less spec" claim measured false at +1 token; `=`-direction inversion reached independently by three seats, 0/17 shipped languages; it deletes the extern kind word clang itself names), named tests rejected (string selection captures the benefit — Jest/pytest precedent), and the session's vocabulary counter-proposals (`variable` entity, `global`, `union`, `sum`) each answered by the document or the instrument. **The candidate amendment distilled instead**: keyword-first declarations without `=`, the parameter list attached to the name (`record Point`, `function dist2(a: Point, b: Point) -> int`), and the condition loop split out as `while` (`for` keeps iteration; the trap reverses into a parser-position Certain fix). Every component carries an accepted §1-derived argument: three-plus compiler special cases deleted incl. panel 007's `at_line_start`, blind first-try predictions up (attached parens convert ~50% of the predicted error mass), precedent mainline. One sub-decision left open, pre-registered on both sides: whether every block header ends in a Python-style `:` (ergonomist + historian for — ABC's user-tested colon, 62% vs 56% pre-registered; engineer against — +60-90 lines of redundancy; first metric-2 run designated tiebreaker, conservative default bare headers). Sitting measured on v0; deltas shape-attributable, transfer to v1; adoption post-freeze amends design.md + spec v2 + consistency tests in one commit; ratification queued | panel 018 — five judges, differentiated inputs, blind label-stripped spec variants, every delta measured with both vendored tokenisers, FFI compiled against real headers, precedent sourced | §4.2, §4.4, §4.7, §1.1, §1.2, §1.7, §1.9, CLAUDE.md §9 | 018
2026-08-04 | Panel 018 RATIFIED (author instruction, option 2): the candidate core lands — keyword-first declarations without `=`, the parameter list attached to the name (`record Point`, `function dist2(a: Point, b: Point) -> int`, `constant MAX: int`, `extern function sqrt(x: f64) -> f64`), bare headers (the F default), and `while cond` / `for x in xs` replacing the one-keyword loop pair (the `while` trap reverses into a parser-position Certain `for`→`while` fix). The E colon question stays open exactly as pre-registered — first metric-2 run is the tiebreaker. The sweep touches every artifact that spells the old shape: compiler, fixtures, goldens (hand-edited, CLAUDE.md §9), gallery, design.md incl. the appendix, spec v2, reserved-words.md, editor grammar, site | author ratification of docs/panel/018 § Resolution | §4.2, §4.4, §4.7, §4.15 | 018
2026-08-04 | The IR decided end-to-end (panel 019, M4's trigger: architecture is a panel path). **Slots, not SSA** — unanimous, ~550 lines avoided, and the decisive row is LLVM's own advice to frontend authors ("we strongly recommend … alloca + load/store … unless there is an extremely good reason not to", clang does it for local mutable variables), corroborated by QBE ("phi instructions are NOT necessary") and rustc MIR, non-SSA since RFC 1211 for diagnostics rather than performance and unmigrated in eleven years; every verified migration (GCC 4.0.0, Go 1.7) bought code quality, which CLAUDE.md §13 renounces. **One pass, not two**: the sugar dies on the way into the IR, no desugared tree (~450 lines and two dense side tables saved), and GHC's real payoff — Core Lint, "an 100% independent check on the type inference engine" — is bought separately as `ir::verify`, with one `--dump-ir` golden per sugar row as the evidence the table is honoured. **Five omissions found by compiling C**, three of which make §4.19's ladder unwritable: linkage on every callee and definition (an unmangled Heroes `function open` silently replaces libc's — compiled, prints 7, no diagnostic, and `open` is not among the 23 reserved names), at most *one* destination on a call (`dst = call print(x)` is a hard clang error; raylib is almost entirely void-returning), a place — slot plus field/index path — as a store's destination (§4.8's own `l.pos @ l.pos + 1`), construction as an instruction, and an explicit `cast` so §4.3's no-implicit-conversions rule stays visible in the dump. **The dump's notation fixed by evidence, not taste**: destination always left of `=` or `<-` (the ergonomist read `store total, t0` right by namespace luck and said so — LLVM is value-first, and `t0: int @ 0` is legal Heroes), `$` on temps and synthetic slots (a character the language does not contain, which makes R1 greppable), polarity in the text (`branch $t -> bb2 else bb4`), `preds` per block, and `!` on every abort-capable instruction — three blocks of the sample had hidden exits (overflow, bounds, div-by-zero) and the ergonomist missed them on first read, which is the datum. **Block arguments lost a blind A/B** on the ground that generalises: `%3`'s meaning *is* the set of jumps elsewhere, so the line cannot be read standalone; conditional veto recorded against phi in any form. **R1 adopted as a rule**: every exit-0/exit-1 diagnostic speaks in the syntax the author wrote, IR vocabulary only in `--dump-ir` and exit-2 internal errors — already-written spec (`assert` shows the source expression, `???` reports source types), and R2's measured cost is not verbosity but two reachable repairs of which one (`.must()`) compiles and silently turns propagation into an abort | panel 019 — five judges, differentiated inputs; fourteen C programs compiled against real headers, label-stripped blind dumps, precedent sourced. Spec delta measured **0** (2139 → 2139): the cost of this milestone is not tokens but five documents that stopped being true, which is the defect class that shipped for four milestones | §1.7, Part 5, §4.12, §4.8, §4.19, §4.3, §4.17, §3.5, CLAUDE.md §7, §10 | 019
2026-08-04 | Two vetoes lifted by amendment rather than by argument. design.md **§4.12's "textual substitution on the AST before type checking" is amended to substitution on the IR after type checking** — M3 had already falsified it (the checker checks a generic body once, polymorphically) and CLAUDE.md §12 makes the unamended document the definition of a compiler bug; the pass-order invariant is now written down, monomorphisation is an IR→IR pass before the ownership pass and the emitter and both assert no `Ty::Generic` survives, because ownership cannot decide whether a `Generic(0)` slot needs a `decref`. Precedent is one-sided: rustc creates no monomorphised MIR at all, Swift has "no such thing as an instantiation-time error", and C++ — the language usually described as textual substitution — is not, two-phase name lookup reaching MSVC only in VS 2017 15.3. And **`heroes build` with no flag does not exit 0 in silence**: it lowers, verifies, and says on stderr what it did and what does not exist yet, because panel 016's watch list had already named the failure ("a model runs `heroes check` and reports success for a program that has no backend yet") and a silent `build` would promote that misreading into the tool's behaviour one milestone before the fix was scheduled | the engineer's veto was explicitly of the *unamended spec*, not of the engineering; the warden's was scoped to the silent form. Both conditions are met in the milestone's own commits, which is the mechanism CLAUDE.md §4 intends | §4.12, Part 5, §3.5, CLAUDE.md §10, §12 | 019
2026-08-04 | `Checked::written_types` is born: every *written* type node → its `TyId`, recorded by `types/lower.rs` as it resolves one. Lowering asks it for a parameter's type instead of asking the locals table | found as a defect, not designed: an `extern`'s parameters have **no locals** — there is no body to use them in, so the resolver makes none — and the IR dump printed `extern function sqrt(x: ?) -> f64`. The types matter beyond the dump: the emitter writes a C prototype from exactly them | §4.19, §4.3 | 019 |
2026-08-04 | `heroes build [--dump-ir]` lands at M4, not M5; `run` remains retired and now names `build` as its replacement | the ROADMAP gave `build --dump-ir` to M4 and `cli.rs` said M5 in two places (the `retired()` table and the help footer). The verb passes CLAUDE.md §10 because the fixpoint invocation types it (`build selfhost/heroes.hero --emit-c -o`); the *silent* no-flag form did not, and now prints one stderr line naming what it lowered and what does not exist yet — the spec-warden's scoped veto, standing on panel 016's own watch list | §3.5, CLAUDE.md §10 | 016, 019 |
2026-08-04 | `???` reaching `build`: the file lowers, `--dump-ir` prints `= ???`, and the summary says "no binary while the file has holes"; exit stays 0 | §4.16 makes a hole *not an error* ("type-checks everything else but produces no binary") and design.md says nothing about what a build does with one. Decided by default so M4 could land, and queued: the hole is the thesis's own construct and the IR is the first pass that cannot represent it | §4.16 | 019 |
2026-08-04 | An out-of-range `int` literal is a diagnostic, owned by **lowering** — the first this pass produces. A function that runs off its end is **not** a diagnostic: lowering emits the copy-out chain and a bare `return`, and `-Werror=return-type` is the net at M5a | nothing before M4 needed a literal's *value* (the lexer accepted the shape, the checker gave it a type), so the class was invisible until the pass that has to represent it. Both are new diagnostic classes, which CLAUDE.md §4 makes their own panel trigger — recorded and queued rather than smuggled | §4.3, CLAUDE.md §4, §7 | 019 |
2026-08-04 | `tests/golden/ir/` inherits `check/`'s `UPDATE_GOLDEN` ban | the compiler-engineer's condition for accepting one lowering pass with no desugared tree: with no second tree to inspect, these expectations are the only evidence Part 5's sugar table was honoured, and an expectation that can be regenerated mechanically is evidence of nothing | CLAUDE.md §9 | 019 |
2026-08-04 | The test suite gains three shapes borrowed from other implementations (author instruction: "guarda cosa hanno fatto gli altri linguaggi, coppia le loro suite"). **Negative tests for the IR verifier** — fifteen cases, one per check, each breaking one invariant and naming the message it expects; before them `verify.rs` was 257 lines that ran on every build, said nothing, and had no evidence any of it *could* speak. **Regression cases named after the defect** with symptom, cause and date in the file, so a fixed bug gets a permanent case rather than a paragraph in a journal nobody greps. **An invariant asserted over a mechanically generated corpus**: every mutant `heroes mutate` produces that the frontend *accepts* must lower to an IR the verifier passes, and must dump byte-identically twice — hundreds of programs nobody wrote, checked against an internal property rather than an expected output | the mutants already existed to measure the thesis (metric 3) and cost nothing to reuse; the verifier tests are the gap the author's instruction exposed. Two defects found immediately by the new cases: a place path lost each field's type after the first step (`g.rows[r].cells[c]` printed `store g.rows[$t1].0[$t2]`), and the nested-inline-`match` parser edge, which is queued rather than pinned because a golden expecting it would make it permanent | CLAUDE.md §9, §4.8, §4.19 | 019 |
2026-08-04 | **Every diagnostic is annotated in the source that provokes it** — `#~ <code>` for this line, `#~v <code>` for the next — in addition to the `.expected` snapshot. CLAUDE.md §9 amended. And `examples/first.hero` moves into the gallery as `00-first.hero`: it was already held to the gallery's properties by hand, and `examples/` now has one shape instead of two | rustc's rule adopted with rustc's stated reason: `tests/ui/` keeps a full `.stderr` snapshot *and* requires inline `//~ ERROR`, because "this redundancy helps avoid mistakes since the `.stderr` files are usually auto-generated" and "helps to directly see where the error spans are expected to point to by looking at one file". Four implementations converged on in-file expectations (rustc `//~ ERROR`, Go `// ERROR "regexp"` under `// errorcheck`, Zig's trailing manifest, LLVM's `; CHECK:`); QBE puts the driver and expected output in the input file; Lua ships no expectation files at all. The mechanical payoff is what §9 was enforcing by hand: a regenerator can rewrite `x.expected`, it cannot invent an annotation in `x.hero`. Retrofit evidence: all 28 cases annotated and **no `.expected` file changed by a byte** — the two that could not take a trailing comment (a line whose string swallows it; a diagnostic pointing past the last line) use the `v` form, which is why rustc has `//~^`/`//~v` too | CLAUDE.md §9, §8 | — |
2026-08-04 | Metric 3 recorded a second time (`docs/measurements/002-metric-3.md`) rather than letting 001's numbers drift: the corpus went from 10 programs to 12, so 529 mutants and 96%/79% replace 379 and 94%/76% — **not** a before/after of anything, since no rule changed. Two permissive rates moved (`swap-args` 30→44, `wildcard-variant` 13→39) and the file declines to explain them, because attributing a per-operator delta needs the mutant list and `heroes mutate` prints rates. Queued: the tool should be able to print one operator's survivors | a published measurement whose input moved underneath it is the defect class this project keeps catching in its own documents (the spec briefing models into a reserved-word error for four milestones; the ROADMAP's stale 2136). The columns that stayed flat are the ones worth reading: typos and mixed arithmetic are 100% in **both** arms, so the thesis rules add nothing there | CLAUDE.md §12, panel 011 | 011 |
2026-08-04 | **Panel 020 — the C emitter.** The unsupported-form report is a `Diagnostic` **kind** (`unsupported`), not a parallel type, and it exits **1**: GCC's `sorry()` has been a kind in `kinds.def` beside `error` since GCC 2.5.8 and exits `FATAL_EXIT_CODE` = 1, GHC's `Sorry` carries this proposal's own reasoning in a comment and travels the ordinary failure path, and CLAUDE.md §9's `#~ <code>` invariant keys off diagnostic codes — a type outside the diagnostic system would be a test class outside the invariant. The message names the **capability**, never the milestone (§4.17: nothing that resolves only in another file). A holed `build` exits 1 ending in `no binary: N hole(s)` — the ergonomist measured exit 0 producing a `build && ./artifact` loop that runs yesterday's binary | ergonomist veto on exit 0 and on `(M5b)`; historian's two independent precedents; the exit code read to the end lands on 1 from GCC's own source | §3.1, §4.16, §4.17 | 020 |
2026-08-04 | The gate refuses `extern` until **M7** — the ffi-pragmatist's veto, lifted by one table row. `extern function labs(x: int) -> int` lowers today, no header attachment exists, and an emitter-invented prototype is unverifiable by *any* clang flag: measured, `abs(-2147483649)` returned 2147483647 at exit 0 and `sqlite3_open` was accepted in total silence under `-Weverything -pedantic`, while with the real header both are `error: conflicting types` | §4.19's mechanism is the `#include`, not the declaration; shipping an unverified FFI from M5a to M7 aims the silent-wrong-answer class at the founding constraint | §1.11, §4.19 | 020 |
2026-08-04 | `--no-line` is **not born**: it fails CLAUDE.md §10's stopping rule (not in the fixpoint invocation, not typed by either harness, no measured Part 11 effect) and panel 016's watch list had already scheduled §7's amendment "at M5a, where the flag would otherwise be born". CLAUDE.md §7 and design.md §3.1 amended; the replacement is three lines in the emitter's test helper. `heroes run` survives on design.md §3.5 + CLAUDE.md §6 + the `run/` harness's -O2 leg — **not** on the fixpoint invocation, which does not contain it | the warden checked the citation the proposal offered and it was false; a capability justified by a wrong warrant is a capability with no warrant | §3.1, §3.5 | 020 |
2026-08-04 | `print`'s contract enters the spec (panel 006's decision, unlanded for four milestones): "`print` writes its values with no separator and exactly one trailing newline" — **+16 measured**, 2139 → 2155 binding, headroom 845. Panel 006's own verbatim text measures +39 against its "+15 est": the word heuristic was ~2x low for the third time. The map-insertion-order sentence (+6 measured) is deferred to **M5c**, the first milestone where map iteration is observable | the first milestone where `print`'s bytes are observable is the milestone where a model authoring `run/*.expected` cannot tell `ab\n` from `a b\n` | §1.6, §4.20 | 006, 020 |
2026-08-04 | The `@` parameter's C ABI, written down before the first emitter line: an `@` parameter is a **pointer parameter**, the prologue copies in, each `CopyOut` emits `*p_l = l;`, and two `@` parameters copy out in parameter order. `f(@x, @x)` cannot arise — panel 010's rule is what makes the convention total | `Op::CopyOut` writes the *caller's* place and the callee cannot reach it in C; M5b, M5c and M6 all inherit this shape | §4.8 | 010, 020 |
2026-08-04 | Both remaining `-W` flags land at M5a rather than being deferred with a promise: `-Werror=format` and `-Wconditional-uninitialized`, measured at **0 additional diagnostics** over the runtime, all four spikes and two hand-emitted files. Falsifiable disposition adopted: zero fires across the `run/`+`ir/` corpus at M5a close promotes the second to `-Werror=`; one fire is a lowering bug and gets a case named after it | "measure at M5a, decide at M5b" re-litigated a measurement panel 019 had already taken and shipped two milestones with the hole owned by nobody | §3.1, CLAUDE.md §7, §9 | 019, 020 |
2026-08-04 | `#line` is emitted when an instruction's line differs from the **current effective line**, not from the previous instruction's span, and a restore to the generated file carries the printer's own output line count — so a newline-counting writer is a structural requirement of the emitter, not a detail. `#line N` anchors the *next* line and C auto-increments, so a Heroes line lowering to K C lines drifts by K−1: measured on the frozen target, `tools/spike/01-first.c` advertises `00-first.hero:2` and clang reports `:6`, a blank line, with C text shown under the `.hero` name. Restores to the emitter's *own* output file appear to be original — bison has the concept (`b4_sync_start`/`b4_sync_end`) but points it at its skeletons, and disabled it as diff noise | the policy was written as "on source-line change" and that sentence is not implementable without counting output lines | §3.1 | 020 |
2026-08-04 | `missing_return` lands in the **checker**, not the lowering: the `Flow` it needs was already computed there, and a diagnostic `heroes check` cannot see is a diagnostic in the wrong pass. §4.16's **file-wide hole exemption** applies, found rather than designed — the new check fired on design.md's own appendix, whose `function simplify(e: Expr) -> Expr` has `???` for a body. The rule over-rejects `while true` exactly as Rust's does, recorded with its one-line repair rather than special-cased | the class was invisible because a function body is checked with `Want::Nothing`; the plan to let `-Werror=return-type` catch it died on two facts — modern clang calls it `-Wreturn-mismatch`, and what the author would see is a clang error about a mangled symbol in a file they did not write | §4.7, §4.16 | 020 |
2026-08-04 | The verifier's value invariant is **dominance**, not block-locality. The first wording ("a temporary is read only in the block that defines it") was falsified by the `assert` lowering the hour it landed: both sides of the comparison are computed in the test block and read in the abort block, which the test block dominates. `verify.rs` split at the seam that appeared — a block's *structure* stays there, a value's *lifetime* moves to `ir/values.rs` | with every temporary hoisted to the C prologue and uninitialised, a use its definition does not dominate is a read of an uninitialised local — caught by `-Werror=uninitialized` as a compiler bug wearing a user diagnostic (§8's wart 13) | Part 10 step 6 | 020 |
2026-08-04 | `-Wconditional-uninitialized` is promoted to `-Werror=` in the same step that measured it: **zero fires** across every `run/` and `emit/` golden at both `-O0` and `-O2`, with clang's warnings forwarded on success so that they could fire. Panel 020's falsifiable disposition, resolved by a run | a warning nobody ever sees cannot falsify anything, and "decide at M5b" would have shipped two milestones with the hole owned by nobody | CLAUDE.md §7 | 019, 020 |
2026-08-04 | A golden expectation whose last line is `!panic: <message>` asserts an abort and a non-zero exit — QBE's convention (driver and expected output in the input file). Adopted because the interesting half of §4.14 is the aborts: a program that overflows must **stop**, and the wrong emitter does not crash, it prints a wrapped number at exit 0 | that behaviour is only a golden if the harness can spell it; three of the five M5a adversarial cases need it | §4.14, CLAUDE.md §9 | 020 |
2026-08-04 | The four spikes gain checked `.expected` files and a test that compiles them against the current runtime. Their expected output had lived in C comments, so relocating print's newline collapsed spike 04's three distinguishable results to `10-42` with nothing failing | CLAUDE.md keeps calling spike 01 "the shape the emitter must produce" and spike 04 the frozen container ABI; a frozen artifact nothing checks is a remembered artifact | §4.20, CLAUDE.md §7 | 020 |
2026-08-04 | **Panel 021 — the ownership pass.** Five ownership rules written down before the pass exists, each with a compiled counterexample: a plain parameter is **borrowed** (+0, excluded from the cleanup sweep, which covers `Local`/`Synthetic` slots only) · an `@` parameter is **moved in and moved out** (copy-in takes no reference, copy-out replaces the decref) · a store increfs the new value **before** decrefing the old · a returned value is increfed before the exit sweep · an owning temporary is decrefed at the end of its defining block, made safe by a *checked* phase-`Owned` invariant rather than by a liveness pass. `incref`/`decref` are real IR instructions (Swift's SIL, and LLVM D92808 records what happens when refcount pairing lives only in the backend); **`cow_check` is struck** — it has zero call sites until M5c gives it `push` | the proposal said cleanup was "a walk over a fixed table" and it is not: owning references also live in temporaries, and the literal reading frees the caller's string — measured as a heap-use-after-free on the first program a judge compiled, and as 999 leaked blocks in a string-building loop | Part 5, §4.8, §4.10 | 021 |
2026-08-04 | `HeroStr` is `{ptr, len}` **by value**, 16 bytes, refcount and an 8-byte magic word in a heap header before the bytes. The reason is §4.19, not ergonomics: by pointer, the wrong `str`→`cstr` conversion compiles clean **with an explicit cast** and passes a refcount word to `sqlite3_open`; by value it is `error: operand of type 'HeroStr' where arithmetic or pointer type is required` — inexpressible. The magic catches a `HeroStr` fabricated from a foreign pointer, which otherwise corrupts the bound library's heap with zero warnings under `-Weverything` and ASan silent. `HERO_RUNTIME_ABI` → 2. Two corrections on the record: **spike 04 never froze the string** (it froze the array header and the descriptor; there was no `HeroStr` in the tree), and §4.20's "(ptr, len, refcount)" cannot be read literally because a by-value copy with an inline refcount diverges | a wrong FFI type must be a compile error, and the value type is what keeps it one | §4.19, §4.20 | 021 |
2026-08-04 | `hero_str_from_bytes` / `hero_str_from_cstr` enter the runtime surface — the ffi veto. Without them **no `extern function` may have return type `str`**, and §4.19's own acceptance ladder is unwritable at step 3 ("open a database, run a query, **read a result**, close"), because every C library returns strings as borrowed pointers | one primitive, and the founding constraint keeps its acceptance test | §1.11, §4.19, §4.20 | 021 |
2026-08-04 | An `f64` prints **round-trip-exact, never "shortest"**: the `%.15g`→`%.17g` ladder with gnulib's subnormal branch (precision starts at 1 below `DBL_MIN`), and the word "shortest" struck everywhere because `5e-324` renders `4.94065645841247e-324`, which round-trips and is not shortest — Java shipped `1.9999999999999998E23` for eighteen years to prove the distinction. A value with no fractional part prints `1.0`, not `1`: under `1`, `print(price * to_f64(count))` and `print(5 * 2)` emit identical bytes and an accidental int→f64 drift stays green forever, which is exactly what `1 + 2.0` being an error exists to prevent (Lua's precedent; Rust needed `Debug` to recover the distinction `Display` destroys) | the algorithm lives in design.md §4.9; the spec names the observable form only, at +34 measured | §4.9, §1.4 | 006, 021 |
2026-08-04 | Locale is answered **structurally**: a lazily-created `newlocale(…,"C",…)` with `uselocale()` around the render, which covers `snprintf` **and** `strtod` in one window. "The runtime never calls `setlocale`" is measurably not a guarantee — under `de_DE.UTF-8` the ladder prints the malformed `0,1.0` and the round-trip check **cannot see it**, since `strtod` reads the same locale and is wrong consistently. PEP 331 is the shape: CPython never called `setlocale`, GTK+ did, CPython broke anyway; llama.cpp printed `0,000000` for the same reason in 2025 | Rust, Go, Python-since-2.4 and C++17's `<charconv>` all removed the dependency structurally rather than promising | §4.9 | 021 |
2026-08-04 | `slice(from:, to:)`'s `to` is **excluded**, stated in the spec at +5 measured — the best value on the table. "Inclusive", "exclusive" and "half-open" appeared nowhere in the spec or design.md; the ergonomist called it a coin flip and named the consequence: `slice` is how a self-hosted lexer extracts every token, `"el"` and `"ell"` both compile, and the golden that would catch the error is written by the same guess that made it. `len`'s unit **waits** — derivable in one step from "indexed in bytes", +8 measured, pre-registered for M5c | a rejection can be relaxed later; a call site whose meaning is not recoverable from the line plus the signature is the locality rule failing | §4.9, §1.0 | 021 |
2026-08-04 | The verifier's phase lives on the **`Program`** (`Program { phase }` + `advance_to()` asserting monotonicity), not in `verify`'s signature: ~15 lines against 22 call-site edits, and — the reason that decides it — `verify(p, c, Lowered)` on an owned program would silently skip the new checks, which is a verifier that quietly stops checking. rustc's, LLVM's and Go's three quotations were audited verbatim and all three hold, including golang/go#22499. `--dump-ir` means the IR the emitter sees, post-ownership | the queue item's own note said "zero payoff today with one pass, total at pass two", and point 1 creates pass two | Part 10 step 6 | 019, 020, 021 |
2026-08-04 | **ASan cannot see leaks on this platform** — `detect_leaks is not supported on this platform`, exit 134, measured by two judges independently, then demonstrated: 999 leaked blocks, ASan build exit 0 in silence. So the sanitiser configuration stays for use-after-free and double-free (it caught two real bugs in the panel's own C on their first run) and **stops being described as a leak detector**; the leak gate is `hero_runtime_live()`/`hero_runtime_check_leaks()`, a live-block counter asserted at exit — 12 lines, portable, deterministic, naming a count instead of a stack. Shape: a **flag**, `--sanitize`, on `build` and `run` (same question, same input, different codegen) | the milestone that introduces reference counting would otherwise have shipped with no leak detector at all, behind a configuration that looked like one | §3.1, CLAUDE.md §9, §10 | 021 |
2026-08-05 | **The ownership pass moves, it does not release.** An owning temporary is *moved* into a synthetic slot in the block that defines it, so nothing is owned by a temporary past its own instruction, nothing owned crosses a block edge, and every release is a slot's — at a point already known. Panel 021's costing (release at the end of the defining block, made safe by a checked invariant) was refuted by that very invariant on two real programs within the hour: `.must()`, `?`, `&&` and `if`-as-an-expression all open a block in the *middle* of an expression, so `name + " scored " + got.must().to_str()` builds a concatenation in one block and consumes it in another. The consequence is a uniform reading — **every value that reaches a store is borrowed** — and the cost is over-retention, not a leak, because a loop's synthetic slot is overwritten each iteration | the alternative was liveness, which is the analysis that choosing slots over phi nodes (panel 019) existed to avoid; this cashes that decision properly | Part 5, §4.10 | 019, 021 |
2026-08-05 | `ir/asserts.rs` routes a **counted** assert operand through a synthetic slot. The `assert` lowering computes both comparison operands in the test block and reads them in the abort block — which is what lets the message quote values — and for a `str` that is a block-crossing owned reference. Panel 021 predicted the collision and scheduled the repair for M6 with `Abort::Assert`; the verifier's `Owned` invariant fired on design.md's own gallery first | two instructions on a path that is about to abort the program, against a use-after-free in the one construct whose whole job is to report accurately | §4.18 | 021 |
2026-08-05 | The check that a returning block releases every slot it owns is keyed on **position**: the sweep's load *follows* the last store to that slot, where a store's old-value load necessarily *precedes* it. Two earlier forms failed — counting `Decref`s was a proxy that stopped meaning anything when the pass changed shape, and pairing a `Decref` with a `Load` of the same slot could not tell a sweep from a store because the two pairs are byte-identical. Found by deleting a sweep decref by hand and watching the check stay silent | an invariant that cannot fail on a hand-made violation is not an invariant | Part 10 step 6, CLAUDE.md §9 | 021 |
2026-08-05 | The `f64` ladder starts at precision **1** below `DBL_MIN` (gnulib's `ftoastr` branch) and the word "shortest" is struck. Two judges' findings compose: with the branch, `5e-324` renders `5e-324` and `1e-323` renders `1e-323`, matching Python — so the spec-warden's counterexample to "shortest" dissolves, while the correction to the wording stands, because round-trip-exactness is guaranteed and brevity is not (Java shipped `1.9999999999999998E23` for eighteen years) | the historian was checking a citation and found the omission the proposal's description had dropped | §4.9 | 021 |
2026-08-05 | **Panel 022 — COW is per-step, not per-primitive.** One unshare for every array or map step of a mutated place, each writing back at its level, and the mutation primitives take `HeroArrayHeader **` rather than returning the new pointer. Measured: with a single unshare at the primitive, `h = g` then `g.rows[0].cells[0] @ 7` changes `h` too — ASan clean, leak counter zero, exit 0, a green harness on a program that violates spec line 58 ("No aliasing exists anywhere"). It is *necessarily* wrong, not accidentally: unsharing level 1 copies its elements, whose `copy` increfs level 2, so level 2 is shared exactly when level 1 was copied. The order is part of the rule — the stored value is increfed **before** the outermost unshare | nothing in this project could have caught it: the leak counter reads 0, the sanitisers are silent, and no golden read a second name after a nested mutation | §4.10, Part 5 | 022 |
2026-08-05 | **`Op::CowCheck` does not return to the IR**, and the reason is a cycle a judge actually built. As an instruction "inserted before every mutation" it can be hoisted above the argument, and in that order the unshare is a no-op and `xs` ends up reaching itself: three blocks, ASan silent, only the block counter catching it. Kept inside the runtime primitive, C's own rule that all arguments exist before the callee's first statement makes the bad order **inexpressible** — which is better than a check, because a check can be forgotten | the same instruction was struck at M5b for having no call sites; it now has one and still does not belong in the IR | Part 5, §4.10 | 021, 022 |
2026-08-05 | Records and variants are **by value** — which **confirms** `tools/spike/04-variant.c` rather than contradicting it: line 137 of the spike is already a tagged union, and the only refcounted header in it is the array's. Three judges rejected the proposal's own "deliberate contradiction" sentence. The real divergence was underneath: the spike's `h_Expr_copy` **deep**-copies while the descriptor's `copy` is **shallow + incref**, so the spike's own output changes from `1 0 -42` to `1 1 -42` — with an M5a test asserting the old value. `HeroDesc.copy` is shallow, the spike and its `.expected` are updated in the same commit, `HERO_RUNTIME_ABI` → 3, and `HeroDesc`/`HeroArrayHeader` move out of the spike into the shipped header | measured: 16 bytes and 2 heap blocks for the appendix's five-node tree, against 8+24-per-node and 7 blocks boxed, and 58 generated lines against 97 | §4.10, §4.20 | 022 |
2026-08-05 | The descriptor pass may **never** use `memcmp` or hash bytes: `record Flag { n: int, on: bool }` carries 7 padding bytes, so two `==`-equal records hash differently with no compile error and no sanitiser report. And `hash` is generated for **every** type, never null — a call through a null `hash` is `SEGV on unknown address 0x0, pc 0x0`, with no type name and no source line. Go's cheaper rule (emit `hash` only for map-key types) reintroduces the null and is rejected on that ground. A payload-free variant case is omitted from the union, because C11 has no empty struct. The reachable set is a **worklist**, not a walk over the interned arena — an unused descriptor is `-Wunused-const-variable` under this project's own flags | every one of the four was found by compiling, not by reading | §4.3, §4.20, CLAUDE.md §7 | 022 |
2026-08-05 | **Cycles are impossible, and the published reason is not the one the proposal gave.** Racordon, Shabalin, Zheng, **Abrahams** and Saeta, "Native Implementation of Mutable Value Semantics" (ICOOOLPS '21, arXiv 2106.12678), contains four of this proposal's six points in four pages — synthesized per-type copy/destroy, refcounted array blocks, a mutation-time counter check, and no cycle collector — and its guarantee is *"references cannot be stored in variables or object fields, hence all values form disjoint topological trees"*, a statement about what a field may hold rather than about copying terminating. Two exceptions named: §4.19's `ptr`/`cstr`, and a stored closure, which Heroes forbids. The paper is **stricter** than Heroes (it forbids recursive types outright, where Heroes carries finiteness through `[T]`) and the departure is recorded. Swift has shipped twelve years the same way; Nim's ORC is gated on `canFormAcycle` and motivated by closures. The queued cycle question closes by citation, and `docs/ROADMAP.md`'s "reference counting leaks cycles" is corrected | a right answer with a wrong reason is one refactor away from being a wrong answer | §4.10 gift 2 | 021, 022 |
2026-08-05 | The map's **mutation half is struck from M5c**: `hero_map_unshare` and the map's `hash`/entries machinery have zero reachable call sites, because the spec's inventory has no insert and `for x in m` is `not_iterable` today. A map at M5c is a literal, `m[k]`, `has`, `len` and structural `==`. The insertion-order sentence is re-deferred to the milestone that lands `for k in m` and re-priced at **+12 measured**, not the +6 that prices only half of it. The closure-list question goes on the record priced rather than answered: `set(m, k, v)` with `for k in m` costs **+29**, deleting `{K: V}` recovers **−57**, and one of the two must be chosen by M6's audit — 57 tokens for a container that cannot hold a symbol table is the only indefensible option | ~180 lines of C no program can reach and no golden can make fire, which is why `cow_check` was struck at M5b | §1.0, §4.9, §4.20 | 006, 021, 022 |
2026-08-05 | Three spec sentences land at **+24 measured** (2196 → 2220, headroom 780): map `==` ignores insertion order (+7), `to_int` truncates and aborts out of range (+10), and `m[k]`'s error code is `missing_key` (+7). Panel 012's rule is satisfied by its second branch — each carries a falsifiable prediction — and the named reserve stays unspent: design.md §4.6's own nomination, the `.is_err()` row, at −11. The map-equality silence was ranked first because `h_T_eq` written the cheapest way (a pairwise walk of the dense entries array) makes `{"a":1,"b":2}` and `{"b":2,"a":1}` unequal, and the ergonomist answered `true` from the spec at 80% confidence with nothing to read | a wrong guess here is silent and the golden harness would defend it | §1.2, §1.6, §4.3, §4.9 | 022 |
2026-08-10 | **The value-semantics choice is recorded with its lineage and its rejected alternatives** (§4.10), because the failure mode is not disagreement but *repair*: an implementer who reads no references, no graphs, copies everywhere as a beginner's mistake will keep trying to fix it. Six alternatives priced in a table (shared-reference RC, ARC/ORC, tracing GC, borrow checker, manual allocators, C++ RAII); five appendix entries promoted from names to arguments (APL/J/K/BQN split in **opposite directions** — against on notation, in favour on the value model, with kdb+'s 10-of-17 STAC-M3 Antuco records as the evidence that the model is not what makes a language slow; R/MATLAB as precedent *and* warning, their slowness being Heroes' own O(n)-per-mutation bill; Erlang with its motive inverted; Swift as the half taken and the half declined; Hylo as the completeness argument). Three claims in the author's draft were **not** adopted: `null`'s absence is §4.6's, not value semantics'; "no data races by construction" holds only once Part 7.13's heaps exist, so it is stated as *inexpressible after*, not *bought now*; and "nothing is ever genuinely shared" contradicts COW, whose whole premise is physical sharing — the true statement is *across threads*. Honest summary adopted verbatim: **strong at transforming data, awkward at interlinking data** | zero spec tokens and zero code: this is the section most likely to be "improved" by someone who has not read the trade | §4.10, appendix | — |
2026-08-10 | **Part 7.13 gets its shape** — isolated per-thread heaps, a mailbox per thread, and **any Heroes value as a message** with no envelope and no serialisation format, which falls straight out of §4.10. Two copy regimes that are not a contradiction (COW within a thread, unconditional at the boundary) and are not to be unified. Green threads stay in Part 7 rather than joining coroutines in Part 6, because the vertex that says no is **simplicity** — a scheduler — and this Part's own preamble makes that a deferral; the operative rule is the same either way: **do not build a scheduler**. The Erlang precedent is recorded with its correction: refc binaries over 64 bytes and literals are **not** copied between processes but shared under a reference count, so the escape hatch for large payloads is a cross-heap refcount, i.e. an atomic counter — the one cost this design avoids, to be traded consciously and not rediscovered | design intent written down so the implementation is mechanical rather than a redesign, and because two consequences bind today's code | Part 7.13, §4.20 | — |
2026-08-10 | Two **v1 invariants**, promoted from forward-looking prose because both are already true and were verified rather than assumed: allocation reaches the heap through a **single point** (today one `malloc` and one `free`, both inside the `str` primitives in `runtime/runtime.c`), and refcount operations stay behind a **narrow, never-inlined boundary** (`hero_str_incref`/`hero_str_decref` are calls into a separately compiled translation unit, so no inlining can smear refcount arithmetic across code a thread-local counter would have to change). M5c's array and map allocation is the first test of the first one | a second allocation site found later is a redesign, and a rule that is stated only as a future intention is a rule nobody checks | §4.20, Part 7.13 | — |
2026-08-10 | **Panel 023 — a type with no size.** New diagnostic class `no_size` in the **checker** (a diagnostic `heroes check` cannot see is in the wrong pass — `missing_return`'s own precedent), and the rule is a **property, not a list**: *an edge is a field C lays out by value*. Stated as "a cycle not through `[T]` or `{K: V}`" the proposal was wrong in both directions and both were compiled, not argued: `record Scope { vars: {str: Scope} }` is legal at 8 bytes, and `record R { next: R? }` is `field has incomplete type` at exit 1, because a `T?` is by value and therefore **transparent** — it propagates the edge into `T`. Two judges found the `T?` row independently from opposite ends, one reading only the spec (`next: Node?` is "the most attractive wrong answer" for a list) and one reading only the tree. The property also forced itself: the check and the emitter's `typedef` order are one relation (§3.1:461, ordered since before the backend existed), and two edge sets cannot be one walk — a map imposes **no** ordering constraint on C, measured, three records mutually recursive through arrays compiling in the worst order with zero forward declarations. So `types/sized.rs` publishes the order in `Checked` and `emit/` contains no graph code. The walk is **iterative**: rustc #84611 is a stack overflow inside this very check, Nim #13715 a SIGSEGV, Zig #21436 an undetected cycle plus a crash — the check that prevents a hang is the one that historically hangs | today all four illegal shapes exit **0**, and the day M5c deletes the gate row they become exit 2 with a path to generated C: this compiler telling the author the compiler is broken about the author's mistake | §3.1, §4.17, §4.20 | 023 |
2026-08-10 | The `no_size` diagnostic is **Swift's shape** and its fix is a **`guess`**. Swift is the only precedent in Heroes' position — a value type with no escape hatch — and its own Sema tests put the error on the **field**, not the type header, with the cycle as a named path in a note. Heroes takes one diagnostic per cycle at the closing field, one note per edge naming `Type.field` and its line, because in `A → B → A` both edges are legal repair sites and naming one silently is arbitrary advice dressed as help; Go's old `cmd/compile` printed only `invalid recursive type T2` for a mutual pair and that was **filed as a bug** (#41575). The fix stays `guess` on published precedent: rustc's `Box` suggestion carries `Applicability::HasPlaceholders`, which rustc defines as "cannot be applied automatically", and Swift emits **no** fix-it for structs at all — locally, `child: Node` → `children: [Node]` changes the field's cardinality *and* its name, so every use site moves with it | CLAUDE.md §8 machine-applies only `certain`, and this is the class where an automatic rewrite would silently change what the program means | §4.17, CLAUDE.md §8, §9 | 023 |
2026-08-10 | **The spec pays for the licence, never the prohibition — +6 measured, not +40.** The asymmetry nobody had named: a diagnostic can only teach at the moment you are wrong, and can *never* tell you something is allowed, because a legal program produces no diagnostic. So the spec buys only the half the diagnostic cannot reach, in the existing `[T]` row: `dynamic array, indices from 0; how a type contains itself` (2225 → **2231**, headroom 769). The +40 bullet was rejected twice over — it was **false** about `{K: V}`, and its "holding itself *directly*" re-licensed the mutual recursion spec line 38 explicitly invites; the corrected form measured +49 for the same falsehood. What the +6 buys is not the case the proposal was about: reading only the spec, the ergonomist put **~75% confidence on `children: [Node]` being legal at all**, because line 60's "No aliasing exists anywhere" reads as *no indirection anywhere* and the only in-spec support is line 97's `s.children`, an example whose types are never written down — predicting that 10–20% of samples hand-roll an index arena to dodge a restriction that does not exist. That is a **silent** divergence and the only failure mode in this panel no diagnostic can catch | 34 of the 40 proposed tokens bought prose, and the cheapest candidate was also the only true one | §1.2, §1.6, §4.3 | 023 |
2026-08-10 | Correction to this log, appended rather than edited because it is append-only: the panel 022 line above records "+24 measured (2196 → 2220)" and the committed spec measured **2225**. Commit `280555c`'s own body already said so — "+29, where the three deltas measured separately sum to +24". Separately measured deltas understated the joint cost by **21%**, which is the **fourth** recorded instance in this project of measured deltas not composing. Panel 023's six candidates were therefore each measured jointly against the real baseline, and the practice is now the rule | a measurement method that is wrong by a fifth is not a measurement | §1.6 | 022, 023 |
2026-08-10 | **A record is a C struct by value and a variant a tagged union by value**, and the per-type functions C cannot write are generated: `retain`/`release` (typed, so every call the emitter writes is checked) plus `eq` and `hash` walking **fields, never bytes**. A payload struct gets a real name because `Ty::Case` is a real type in the IR — `$t5: Token.num = payload $t4 .num` puts one in a temporary and a temporary needs a declaration. A payload-free case leaves the union, and when *every* case is payload-free the union goes away entirely: `union { }` compiles at `-Wall` and is `error: empty union is a GNU extension` under `-pedantic-errors`, which §4.19 schedules. The compound literal is **designated**, redundantly, because the IR already holds the arguments in order — which is exactly why: a field inserted mid-record becomes a clang error at every construction site instead of shifting every value one field along | verified by compiling the generated C with `-pedantic-errors`, so panel 023's prediction 9 is pre-empted rather than left to fire | §4.2, §4.10, §4.20 | 022, 023 |
2026-08-10 | **Ownership rule 6: an aggregate constructor retains every counted field it captures.** A record is by value, so `Pair(one: p, two: q)` copies the `str` handle inside `p` and the new value is a second owner of one block; rule 5 moves the *result* into a slot, which is about the aggregate. `ok(x)` is deliberately excluded — wrapping consumes its argument rather than sharing it — so the shapes are listed one at a time. **And rule 3 reads the stored value's type, not the root slot's**: for a whole-slot store the two questions coincide, which is why it was right for two milestones, and `p.cells @ [7, 8]` is where they part — the slot is a `Row`, counted because it *contains* an array, while the place written is the `[int]`. Both defects printed every correct answer and were wrong once, at the sweep | found by the live-block counter, twice; neither is visible in any output at any input | §4.10, Part 5 | 021, 022 |
2026-08-10 | **`push` copies, always**, and it is a correctness decision wearing a performance decision's clothes. `xs = [1, 2, 3]` leaves `xs` observable and its slot holds one reference, so a refcount of 1 means "only the slot has it" and appending in place would change what the slot sees — value semantics has no reading in which the argument is consumed. Building an array by successive push is therefore O(n²), the same bill §4.10 already declared for `s + t`. Recorded because the cheap in-place path *looks* available whenever the count is 1, and that is precisely when it is not | the IR cannot know that the result replaces the argument, and a rule that depends on knowing it is a rule that fails silently | §4.10, §4.20 | 022 |
2026-08-10 | **One `HeroDesc` serves every `[T]`**, whatever `T` is: `copy`, `drop`, `eq` and `hash` on an array value all reach the element type through the header's own `elem`, so `[[int]]` and `[[str]]` share one row. The generated set is a **reachability worklist** over the program's arrays, not a walk over declarations, because a `static const` descriptor nothing points at is `-Wunused-const-variable` — and it is emitted **before** the function definitions, since an array literal names its element's descriptor. A nested aggregate's `hash` calls its own function rather than going through a descriptor: `record Rect { corner: Point }` hashes a `Point` no array holds | three of the milestone's six defects were this class — an ordering or a spelling that clang caught, against three that only an instrument would have | §4.20 | 022 |
2026-08-10 | **Copy-on-write is one unshare per array step of the place, with write-back, and the primitives take `HeroArrayHeader **`** — the double pointer is the design, because a caller cannot forget to store a result that does not exist. `hero_array_set` unshares, releases the element that was there, and **moves** the value in, so the ownership pass increfs before the store and does not decref after. That order is the rule: the incref precedes the outermost unshare because the value may live inside the container being copied, which is what `n.children[0] @ n` builds — and what comes out is a tree, because the captured value refers to the old block and the unshare made a new one. `Op::CowCheck` stays out of the IR: inside the primitive, C's own rule that every argument is evaluated before the callee's first statement makes the hoisted order **inexpressible**, which beats a check because a check can be forgotten | panel 022's veto now runs: `h = g` then `g.rows[0].cells[0] @ 7` prints 7 and 0, where the single-unshare version printed 7 and 7 with every instrument in this project reporting success | §4.10, §4.20 | 021, 022 |
2026-08-10 | The **read-only map and `T?`'s representation are deferred out of M5c**, and the reason is Principle 0 rather than time. Panel 022 already struck the map's mutation half for having zero reachable call sites, and its R4 leaves the larger question open: `{K: V}` may be **deleted from the language** at M6's closure audit, measured at −57 spec tokens against +29 to fund `set` plus `for k in m`. Building the container's ~180 lines of C, plus a generated option struct per `T?`, before that answer is exactly what `cow_check` was struck for at M5b — with more force, because here the whole type may go | a milestone that ships a form the audit may delete has spent its budget on a decision it did not make | §1.0, §4.9, §4.20 | 022 |
2026-08-10 | **`T?` is a tagged union by value, and every one of them is reference-counted whatever `T` is** — `int?` included, because its error side is `HeroFailure`, two `str`s. `HeroFailure` ships in the runtime rather than being generated: it is the one record every `T?` in every program contains and §4.6 fixes its shape, so there is no declaration to generate it from. One `typedef` per distinct `T?`, named by **index** rather than by spelling, because `int?` and `[int]?` sanitise to the same identifier and a collision there is two types sharing one C name; the tag is a plain `int64_t`, since `Op::Tag`'s result is an `int` and lowering already compares it against `const 0`. Correction to an older note, chronological rather than a change of mind: `retains_fields` had `ok(x)` as a *wrap* that consumes its argument, on panel 021's wording — rule 5 came later and made every value reaching a constructor borrowed, so consuming one is a release the program never made | the note was written before the rule that invalidates it, which is the failure mode of a comment that records a decision instead of an invariant | §4.6, §4.20 | 021, 022, 023 |
2026-08-10 | **The map is open addressing over three parallel regions with a FIXED SEED, and `==` ignores order by construction.** Parallel regions — a state byte per bucket, then keys, then values — rather than a struct per bucket, because element sizes come from descriptors at runtime and a per-bucket layout would need padding the runtime computes. The seed is fixed because iteration order must be a function of the contents: the self-hosting fixpoint compares generated C byte for byte (panel 006), and a per-run seed makes the compiler emit two different correct outputs so the fixpoint never closes. Equality is "same length, every entry found in the other" rather than a pairwise walk of two entry arrays, which would make `{"a":1,"b":2}` and `{"b":2,"a":1}` unequal — spec line 58, and the row panel 022 ranked first among what a cheap map gets silently wrong. `m[k]` yields a `V?` and the runtime hands back an address or NULL, because it cannot build an option struct that is generated per payload type | `hash` finally has a caller, two milestones after panel 022 required it non-null | §4.9, §4.20 | 006, 022 |
2026-08-10 | `Op::MapGet` **allocates**, and it is the only one of the four read operations that does: a field, an element and a payload all hand back a borrowed view, while a lookup builds a `V?` and copies the value into it through the descriptor. Found by the leak counter on `{1: "one" + "!"}` followed by `.default(…)` — exactly one block, and **only with a computed value**, because a `str` literal has a negative refcount and the same missing decref is invisible. The consequence for every test in this project that touches counting: allocate on purpose, `"one" + "!"` rather than `"one"` | the arm's own comment had said "listed rather than defaulted so that switching one on is a decision here", and this is that decision | Part 5, §4.20 | 021, 022 |
2026-08-10 | Retaining a `T?`'s payload and retaining a record's field are **one question with one answer** (`perfn::reference_line`), and having had two is what produced a wrong argument count and then a discarded `const`: the second implementation reached for the descriptor's `copy(dst, src)`, which has no retain-in-place form. Neither diagnostic was the defect; the defect was the duplication, which `ir/layout.rs` had already priced | a repair that removes an answer rather than adding a cast | §4.20 | — |
2026-08-10 | **The gate refuses no type.** Every row that named one has retired, and the arms stay with empty bodies rather than being deleted, because the reason for enumerating was never "these are refused" but "adding a shape to the IR must be a compile error here". What is left in the subset note is built-ins — `sort`, `join`, `chars`, `range` — and `.must()`, which is an `Op::Abort` rather than a shape. Recorded because the note's own wording changed from a list of types to "every type in the language": a list that is complete stops being information and starts being something to keep in sync | four gallery programs wait on those five, and none of them waits on a type | §3.1, CLAUDE.md §10 | 020 |
2026-08-10 | The M5c deferral of the map and `T?` was **overruled by the author**, and the risk it named is unchanged: panel 022's R4 leaves `{K: V}` open, and deleting it at M6's closure audit recovers −57 spec tokens against +29 to fund `set` plus `for k in m`. If the audit deletes the container, M5d's map goes with it. `T?` is not at risk under any reading | recorded so that the audit reads a decision made with the cost known, rather than a sunk cost arguing for itself | §1.0, §4.9 | 022 |
2026-08-10 | **The spec budget's hard ceiling rises to 4096 by author decision** (panel 024, retro-record), from 3000 (panel 012), 2000 (009) and an unmeasured 1500 before that. **The soft line stays at 2000, and that is the load-bearing half**: it is not a fraction of the ceiling but the point where an addition owes a named removal or a pre-registered prediction, so rescaling 2:3 to ~2730 would put the 2231-token spec *under* it and exempt the next ~500 tokens — 16–22 amendments at the observed mean. The warden's line: **a soft line that moves whenever the document approaches it is a thermometer, not a thermostat** — and the historian found that exact shape already shipped, in Linux commit `bdc48fa11e46`, which raised checkpatch's gate to 100 columns while `coding-style` still says 80. Two numbers, deliberately: a preference that warns and a gate that fails | 2231 measured, headroom 769 → 1865; the change is a number in three files and a discipline in none | §1.6, §1.2 | 024 |
2026-08-10 | **What the raise is not backed by, recorded rather than argued away.** The mortgage — §1.0's closure items — justifies about 3181 (2231 + 950), clearing 3000 by 181, which is a precise reason to raise; 4096 leaves **915 tokens with no named claimant, 41% of the current spec**, and the 950 was itself derived *from* the old headroom (3000 − 2048) rather than costed per item. Measured over the spec's first week: **seven amendments, +183 tokens, zero removals**, while the ceiling moved +173%. And the warden refused the convener's framing — "a fourth raise in one week means the number tracks the document" — because the data refutes it: the document has been flat at ~2200 since 08-04 and never came within 700 tokens of the old ceiling, so no raise was pressure relief. **The number tracks neither the document nor a costed need**, and the historian confirms with sources that no published guidance ties a document's size to a power of two: 4096 is convention, which is a fine reason to pick it and not one to record as engineering | a ceiling raised while slack remained is not a loosened constraint but one that has stopped predicting anything | §1.6, §1.0 | 012, 024 |
2026-08-10 | **The budget's veto moves out of reach and two level-independent checks are owed** (queued, not adopted — they are architecture): a **delta gate**, failing when a commit's spec delta exceeds ~+50 tokens without a named removal or a registered prediction, and a **mortgage ledger** quoting each closure item so that 2231 + Σquotes clearing the ceiling is arithmetic a reader can check. At the observed +30 per amendment, 4096 binds after ~61 more amendments where 3000 would have bound after ~25. The precedent for why this matters more than the number is PEP 8: 79 characters is intact in the document and irrelevant in practice, because Black's 88 decides — **if the budget is not measured by `heroes` on every run, the practical budget is whatever the author's editor tolerates** | a threshold that fires after five dozen panels is not an instrument | §1.6, CLAUDE.md §10 | 024 |
2026-08-10 | **Panel 023's resolution is falsified, by its own reasoning, and the convener's claim with it.** The +6-token form — `[T] | dynamic array, indices from 0; how a type contains itself` — was landed as "the cheapest candidate and the only true one", on the argument that the spec should buy only the *licence* because a diagnostic can never say a thing is allowed. Reading the amended spec, the ergonomist ranks that clause **the worst guess in the language**: it wrote two expression evaluators, `args: [Expr]` and `lhs: Expr`, both plausible and both compiling, with nothing in the document forcing either. Its verdict on the mechanism is the general lesson: *"silence made me stop and ask; an implication made me guess **confidently**. Rules smuggled into semicolon clauses are the highest-risk text in this document."* So the form converted silence into ambiguity, which is worse than silence — true and too compressed to function. The repair is its own panel because it is a spec change | the cheapest true option can still be the wrong one, and the way to find out is to hand the amended document to a reader who has nothing else | §1.6, §4.10, spec line 48 | 023, 024 |
2026-08-10 | **Examples beat prose, found twice from opposite directions, and it cuts against this project's central instrument.** The historian's only directly relevant published result — Aycock et al., ICLR 2025 (arXiv 2409.19151), on learning a low-resource language from one grammar book — is that *"almost all improvements stem from the book's parallel examples rather than its grammatical explanations"*. The ergonomist, reading only the spec, independently made worked examples the single condition for withdrawing its objection to spending, because its worst wrong guess came *from* an example and its hardest blocked line from the absence of one. Also load-bearing and now sourced: length itself costs, before any window is approached — Levy, Jacoby & Goldberg (ACL 2024) padded the same task and found reasoning degrades **even when the padding is duplicated relevant text**. The ergonomist reached the same conclusion without the citation: **chain length, not token count, is the error predictor**, so the ceiling is safe and the *increase* is not free | and nobody has published a measurement of a specification's length against LLM code correctness, so Part 11 would be measuring something unmeasured | §1.2, §1.6, Part 11 | 024 |
2026-08-10 | **Panel 023's clause is repaired, and the reason it needed repairing is not the reason panel 024 gave.** The recursion rule leaves the `[T]` table cell (**−6**, the first measured *removal* in eight amendments) and lands as a rule of its own: a record or variant holds its fields by value, so it may contain itself **only** through `[T]` or `{K: V}`, with `children: [Node]` and `child: Node` both shown. The word doing the work is `only` — the ergonomist stopped there rather than guessing, because *"`only` converts an example into a closed set"* — and the old phrasing was not merely quieter but **restrictively wrong** about `{K: V}`, steering a reader to believe maps were excluded at 60% confidence. **But panel 024 R4's factual claim is false**: `lhs: Expr` does **not** compile, it is `no_size` at exit 1 with two notes and a naming fix. Verified by the warden and again by the convener, who had repeated the claim to the author without running it. So this lands on **§1.2's rewrite rate** — one avoided round trip at 500–2000 tokens against +47 — and **not** on silent-bug avoidance, which was never available | a change adopted for a reason that does not exist is adopted on nothing, whatever its merits | §1.2, §1.3, §1.5, spec line 61 | 023, 024, 025 |
2026-08-10 | **`/` and `%` truncate toward zero, stated: `-7 / 3` is `-2`, `-7 % 3` is `-1`** (+26, replacing the parenthetical `(integer division truncates)`). This is the **only genuinely silent gap** the panel found, and the artifact is a program rather than an argument: the same clock-arithmetic task written from the unamended spec prints `-1:0` while its own comment says it expects `23:0` — compiles, runs, wrong, no instrument firing. Worse than a missing rule, the ergonomist's diagnosis: the old spec's `%` was **extra-textual**, decided by the reader's prior language, and the document's Python-shaped surface primes Python's `2` while the arithmetic is C's. *"A worse condition than non-locality, and one I cannot veto, only measure."* Verified on all six sign combinations | §4.14 mandates specifying arithmetic edge cases in its own heading, "because unspecified means the model invents" | §4.14, spec line 129 | 025 |
2026-08-10 | **The rule this panel produced, and it outranks its own resolutions: spend spec tokens where a wrong guess is silent, spend none where the type checker already speaks.** Ranked by it, the arithmetic sentence (silent → specified) beats the recursion rule (loud retry → specified) beats any sentence about a map's iteration order (already loud, needs nothing). And the map question was not a gap at all: the other three ask about operations the document **grants**, and that one asks about an operation it does not — *"granting an operation incurs a debt: its edges."* Answering it would manufacture the feature by implication, since a reader told the order writes `for k in m`, which does not compile | a spec that answers a question about a feature it lacks creates a first-try failure out of nothing | §1.2, §1.6, §4.9 | 022, 025 |
2026-08-10 | **A category with teeth, adopted because the alternative is a drawer.** Three of the amendments state behaviour the compiler already implements; the warden argued both readings of whether that is an *addition* under §1.2 and picked "the spec discharging a debt" **narrowly**, bounded as **"discharge of an explicit design.md specification mandate"** and never as "documentation of implemented behaviour" — the second phrasing is infinitely large, drawn down one unobjectionable sentence at a time, which is §1.6's own named failure. Three teeth, all reader-checkable: **cite a mandate, not a consistency**; **decide nothing** (if design.md is silent and the panel must choose, it is a full-burden addition); and **the warden verifies on every type the sentence covers**. The third tooth is what found the defect below | the loophole and the legitimate case are the same sentence read two ways, so the bound has to be the citation | §1.0, §1.2, §1.6 | 025 |
2026-08-10 | `slice` **on an array never compiled** — `heroes check` exit 0, `heroes run` exit 2 with `passing 'HeroArrayHeader *' to parameter of incompatible type 'HeroStr'`, on a program the author is entitled to write since spec line 148 lists `slice` without restricting it to `str`. One built-in over two operand types where only one half has an entry point: `len` has the same shape and needed no gate row because both halves landed together, and the gate checked built-ins **by name**, so the array half sailed through a list that was about names. The row now splits by operand. **Found by a judge going to verify that a sentence it was pricing was true** — no test in this project looked there, and the sentence was held back rather than documenting an unreachable path | the third tooth of the category above, earning itself on its first use | CLAUDE.md §7, §9, spec line 148 | 025 |
2026-08-10 | **`{K: V}` is funded — author's verdict on panel 022 R4 — and the surface the funding buys is not the one proposed.** `set(m, k, v)` as a void call is **vetoed** (panel 026, compiler-engineer): a builtin with an in-out place needs `Arg::InOut` on a `Callee::Builtin`, which exists nowhere; `aggregate::place` extended past its `Step::Index` bail-out; and §4.8's copy-out, which lives as `Op::CopyOut` **in the callee's body** and is asserted per exit edge, hung on a callee that has no body — checker, ownership pass and emitter each grow a special case, which is **core by §1.7's own test**. The surface is **`m[k] @ v`**, a `Store` with a trailing `Step::Index`, and it costs **no IR change at all**: `ir/places.rs` already builds the step, `Step::Index` carries a `ValueId` so a `str` key fits, and `own.rs`'s `indexed` rule — incref before the store, no decref, the primitive drops the old and moves the new in — is already exactly `hero_map_set`'s contract. The divergence from arrays is named: `xs[i] @ v` **aborts** when absent, `m[k] @ v` **inserts**. Two judges reached the same conclusion from unrelated evidence — the ergonomist because the proposed `set(m, k, v)` has **no `@`**, so a statement call compiles and silently does nothing | the cheap spelling was already in the IR and the expensive one was in the proposal | §1.7, §4.8, §4.9, Part 5 | 022, 026 |
2026-08-10 | **Iteration is `keys(m) -> [K]`, not `for k in m`** — pure sugar, **zero new IR ops**, ~14 lines of C, and it composes with the `for` and `sort` that already exist, which is CLAUDE.md §10's stopping rule saying the shape is *nothing* when two invocations compose to it. It works with either data structure, so it does not force the dense-entries rewrite. And it serves the only place the bootstrap iterates a map (`types/holes.rs`), whose own comment needs **sorted** order — which `for k in m` would not have given it. The ergonomist had ranked a pair form above a key form because `for k in m` obliges an `m[k].must()` in the body whose safety is justified by the line above rather than by itself; under `keys(m)` no clause of the spec obliges an unwrap anywhere | the cheapest option was the one nobody proposed, and it is the one that needed no new form | §4.9, CLAUDE.md §10, Part 5 | 026 |
2026-08-10 | **Panel 006's insertion-order guarantee is withdrawn as FALSE, not weakened.** `HeroMapHeader` has no order field, so arrival is **not stored** and no iteration strategy can recover it; and `cap` derives from a literal's **duplicate-inclusive** pair count, so two `==`-equal maps written differently iterate differently — measured: `{"a":1,"b":2,"c":3}` iterates `b a c` at cap 8, the same map with a duplicated first key iterates `b c a` at cap 16. So design.md's "map iteration follows insertion order" and the ROADMAP's "iteration order must be a function of the contents" were false from the day the map shipped. Panel 006's *argument* was sound and is answered rather than overruled: it feared unspecified order because a model's silent default is insertion order and Go's randomization is the recorded disaster — but that hazard needs a reader who iterates a map **directly**, and there is no such form. The fixed seed stays: determinism across runs is still a fixpoint requirement. Corroborating that nothing relied on it: design.md requires `// ORDER:` at every ordering-sensitive map walk and there are **zero** in the tree | a guarantee nobody exercised, contradicted by the code that was supposed to keep it | §4.9, §4.20 | 006, 026 |
2026-08-10 | **`has(m, k)` is struck, −17 measured.** Map access returns `V?` always, so `has(m, k)` and `!m[k].is_err()` are two spellings of one predicate, and design.md's justification for it — "its absence forced a sentinel-value hack" — had already been retired by the bullet directly above it. The removal is also what **recovers the warden's panel-025 prediction** rather than falsifying it: that prediction was "net ≤ +25 **or** carries a measured removal", and the amendment at +43 carries one. Recorded with the warden's refusal to carve out an exemption for itself: *"CLAUDE.md §4 means no spec change lands without author ratification, so a funded author decision is not a subclass of amendment, it is the only class"* | a reason can outlive the thing it argued against, and the bullet above had already killed this one | §1.4, §4.9 | 025, 026 |
2026-08-10 | Two corrections to the record, both from measurement. **Panel 022's +29 is refuted, not confirmed**: it priced the *keys* shape on base 2196, that wording measures **+36** today, and the +29 now attaches to a pair form panel 022 never priced — "the agreeing digits describe a different sentence on a different base", the fourth recorded instance. The rule that comes out of it: **removal deltas transfer across spec versions, insertions must be re-measured** (`.is_err()` was −11 on base 2196 and is −11 on base 2304, because deleting a table row is local while inserting into flowing prose re-tokenises its neighbourhood). And **the compiler-needs justification for `{K: V}` is withdrawn**: of 20 `.insert(` sites, 3 are `Vec::insert` in tests, 5 are `BTreeSet` (Heroes has no set type), 6 are keyed by a dense arena index, and of the 6 genuine maps one is keyed by `(u32, u32)` (no tuples), one by `Vec<TyId>`, one by a recursive `Ty` — **exactly one** is unambiguously `{str: int}`. `resolve/scope.rs` uses an association vector because a lexical scope needs shadowing and O(1) pop, which insertion does not provide and the map has no delete for at all: *"for a symbol table the map is not unnecessary, it is the wrong structure."* So `{K: V}` enters on the **thesis** branch, where §1.0 wants a measured effect — the M6 harness, both arms, ≥2 dictionary tasks | the convener sold the decision on a reason that was inverted, and the record should say so | §1.0, §1.6 | 022, 026 |
2026-08-10 | **Reasoning sessions become an artifact class**: `docs/reasoning/NNN-<topic>.md`, one note per chat, English, committed. A reasoning session is a conversation in which the author asks questions to understand and **no file of code, spec or design is modified** — read-only by construction, and the note plus its hand-offs is everything it writes. The three existing homes keep their jobs: the reusable explanation of a concept stays in `docs/glossary/` (a note **cites** it, never restates it — that directory's "one home for every concept note" is why it already absorbed `docs/theory/`), a question owed goes to `docs/debrief/QUEUE.md`, a gap to `OPEN-QUESTIONS.md`'s watch list, and a design change still to `/panel`. The directory holds only what none of them holds: the path between the artifacts, and what the session left open | author instruction ("uso questa chat per capire ... al limite salviamo le sintesi"). The precedent is this log's own entry of 2026-08-03, which created the glossary because "good explanations were evaporating in chat" — that move saved the *explanation*, not the path to it, and a glossary entry is a concept rather than a session. **No panel**: CLAUDE.md §4 makes the teaching process amendable by author instruction. Four choices fixed by the author: a new directory over routing into the existing three; a note at the end of every qualifying chat; English, with `beats.md`'s quoted-speech carve-out (`docs/book/README.md`) covering the Italian question verbatim; one commit per note. Discoverability was the binding constraint on the shape — in `docs/` there is no general index, so a new document kind is reachable only through its own README index or a citation from QUEUE/DESIGN-LOG, and this one has both | CLAUDE.md §14, §11, §4, §1 | — |
2026-08-11 | Panel 026 implemented: **`m[k] @ v` inserts, `keys(m) -> [K]`, `has` struck** — spec 2304 → **2347 (+43 measured)**, carrying the removal that satisfies panel 012's first branch. `HERO_RUNTIME_ABI` 7. The place store needed **no new IR form**, exactly as costed: the checker gained one rule (a mutation target sheds one `Fallible` layer when the place is an index into a map — reading may fail, writing cannot, because it inserts) and the emitter one arm (a map step ends the place walk, since there is no element to descend into). `has(m, k)` became `!m[k].is_err()` in every program that used it, which is the removal's real cost and it is four lines | the divergence from arrays is now spec'd rather than implied: `xs[i] @ v` aborts when absent, `m[k] @ v` inserts | §4.9, §4.20, spec lines 70-71 | 026 |
2026-08-11 | **A defect of mine, caught by the magic word.** `hero_map_grown` was written as `memcpy` of the live entries plus `free` of the old block *without* dropping them, on the reasoning that the references pass to the new block unchanged so no descriptor should run. That reasoning is **right about the references and wrong about everything else**: it put refcount arithmetic in a second place. The first program to grow a map and then read its keys printed every answer correctly and panicked at exit with `not a Heroes string block — a str was fabricated from a foreign pointer`. The repair copies through `hero_map_put` (+1 per entry via the descriptors) and releases the old map through `hero_map_decref` (−1 via the same ones) — balanced by construction, with no arithmetic of mine in it, and shorter than the bug | the third time this milestone a counting error printed every correct answer first and failed only at the sweep | §4.20 | 022, 026 |
2026-08-11 | **Panel 026 R7 finished: the spec kept promising `has` after the compiler struck it.** M6 step 2 deleted `has` from the built-in *list* (−4) and left the sentence "`has(m, k) -> bool` tests membership" standing in § Failure — so a program written from the spec answered `error[unknown_name]: nothing named `has` is in scope`, exit 1. Removing the clause measures **2347 → 2334, −13**, and 4 + 13 is exactly R7's **−17**: what shipped was a quarter of the removal the panel resolved | a removal is not landed until the thing it removes is unfindable, and a built-in lives in two places — the inventory line and the prose that teaches it. Only one was edited, and no instrument in this project reads the spec | §4.20, spec line 128 | 026 |
2026-08-11 | **A sixth `HeroDesc` field is vetoed, and the reason was compiled rather than argued** (panel 027 R1). Adding `cmp` to the descriptor ABI leaves every one of the seven existing descriptors at `cmp = NULL` **silently under this project's own flag set** — C11 6.7.9p21 zero-fills a short initialiser list, `-Wextra` sees it and `FLAGS` does not, and `-Werror=missing-field-initializers` is blind to the designated form — with the call landing as `AddressSanitizer: SEGV (<unknown module>)`, *"can not provide additional info"*. That is panel 022's null-`hash` argument one field over. Measured further: `_Static_assert(HERO_RUNTIME_ABI == N)` does **not** catch this, because a new header against a stale `runtime.o` links at exit 0 and reads four bytes past a 40-byte object — adding a *function* is self-guarding at link time, adding a *struct field* is not | a total order over every type is one the surface cannot spell (`types/ops.rs:54` restricts `<` to `int`/`f64`), does not serve the port (the bootstrap's four sorts are `sort_by_key` over `span.start`, which a field-order `cmp` gets wrong), and over a map would be insertion-order dependent — which is why `HashMap` has no `Ord` in Rust and `cmp.Ordered` admits three types in Go | §4.20, Part 5, §4.12 | 027 |
2026-08-11 | **`sort` emits for `[int]`, `[f64]` and `[str]`; every other element type is refused by the *gate*, not by the checker** (panel 027 R1). A checker rule would contradict spec line 71 — `{Point: int}` compiles and runs today, so `keys(m)` can be `[Point]` and `for k in sort(keys(m))` is the spec's own idiom — which is the `has` failure mode four days after paying for it. `unsupported[builtin]` says "undecided", a compile error would say "forbidden" | the element-type question needs a comparator form the closure list has none of, and the M6 audit is the body that decides it: neither branch of the proposal serves the four `sort_by_key` sites the port actually has | §4.12, §4.20, spec line 71 | 027 |
2026-08-11 | **`[f64]` sorts by IEEE 754 totalOrder, not by `<`** (panel 027 R2). NaN appears in neither design.md nor the spec, and it is reachable — "division by zero aborts" is implemented for integers, so `0.0 / 0.0` is a NaN — and a `<`-based sort with one in the input yields an unspecified permutation. Rust gives `f64` `PartialOrd` and not `Ord` for exactly this reason, and Go had to **fix** `slices.Sort`, whose predecessor documented that it "may fail to sort correctly" with NaNs | determinism is an M8c fixpoint requirement, not a nicety, and totalOrder agrees with `<` everywhere the spec is observable — so it costs zero spec tokens | §4.14, Part 8 | 027 |
2026-08-11 | **The UTF-8 error moves from `chars` to the slice that splits a character** (panel 027 R4), discharging design.md:809 — *"Slicing that lands mid-sequence is an error"* — which was a mandate the compiler had never implemented: `slice("caffè", from: 0, to: 5)` **exits 0** and prints a corrupt byte. `chars` stays total: a byte that is not part of a well-formed sequence becomes a one-byte `str`, so **`join(chars(s), "") == s` for every `s`**, unconditionally | the error belongs at the operation that manufactures the broken string, one line earlier and pointing at the cause; and an aborting `chars` would kill a legitimate M7 binding — a real SQLite `text` column returned a LATIN-1 `0xEF`, because SQLite does not validate UTF-8. An invariant enforced at exactly one consumer is a landmine, and a conditional law tests nothing over `heroes mutate`'s corpus | §4.3, §4.19, design.md:809 | 027 |
2026-08-11 | **`sort` is a hand-written stable merge sort; `qsort` is refused on fixpoint grounds** (panel 027 R6). Darwin's `qsort` is not stable — measured `0/0 0/3 0/6 0/9 1/10 1/1 1/7 1/4` — so two correct hosts would emit two different byte streams and `diff B.c C.c` would never close. Panel 006's reason for the map's fixed seed, one data structure over. Independently: C11 has no `qsort_r`, glibc and Darwin disagree on argument order, and casting an `int64_t`-returning comparator is C11 6.3.2.3p8 UB that `-fsanitize=function` does not catch | performance is a non-goal (Part 2); determinism is a v1 finish-line requirement | §4.20, Part 10 | 027, 006 |
2026-08-11 | **Spec: W7, +11 measured — 2334 → 2345.** "An out-of-bounds **index or slice** aborts, **and so does a slice that splits a character**", replacing "An out-of-bounds array index aborts". Two behaviours for eleven tokens because it *replaces* a sentence, and it documents the `s[9]` abort the spec had never stated. Panel 025 R2's release condition is met in the commit that lands the behaviour, and its three teeth are discharged: the split-character half cites design.md:809 and decides nothing; the out-of-range half decides, and rides panel 012 branch 2 on a pre-registered prediction; the third tooth is five goldens — `str` index, array index, `str` slice, array slice, split character | panel 026 R8's rule holds for the fifth time: the identical slice sentence priced at +9 on base 2304 re-measures +10 on base 2334 | §4.9, spec line 129 | 027, 025, 012 |
2026-08-11 | **Three sentences of the spec cannot all be true, and implementing `range` is what made it visible** (panel 028). Spec line 113 writes `range(a, b)` positionally; line 81 says two parameters sharing a type make named arguments **mandatory** at the call site; line 152 says `range` is written in Heroes. A built-in escapes the second rule because `types/builtins.rs` checks it directly — a Heroes *function* does not. Verified: a two-`int` declaration answers `error[needs_label]` with a certain fix at every positional call site, and all seven in the repo are positional. **Resolution: obey the rule.** `range(from: a, to: b)`, spec 2345 → **2354, +9 measured** | the three exits that dodge it each cost more: an exemption is a hole in what design.md:1184 calls the most original construct in the language and panel 015 already refused one; `range(n)` has no spelling for `range(1, n + 1)`, which is the first thing a reader wrote from the spec; and making `range` a built-in decides the closure list, which is the M6 audit's job. Obeying costs a repair and turns the universal habit `range(0, n)` into a compile error carrying a machine-applicable fix | §4.11, §4.12, spec lines 81, 113 | 028 |
2026-08-11 | **`range`'s upper bound is stated, `to` excluded** (panel 028 R2). The ergonomist worked the six off-by-one directions rather than asserting them: **five of six are silent**, and the one loud case — indexing with the loop variable — is loud only because the bound happens to be `len` of the thing indexed; the hedged form `range(0, len(s) - 1)` loses even that. Silence here is also **misleading rather than neutral**: `slice` carries "(`to` excluded)" three lines away, so omission reads as deliberate contrast and argues for the inclusive reading | the measured cost is not the wrong answer but the avoidance — under doubt the judge stopped using the built-in and hand-rolled a `while` loop, "three new places to be wrong in exchange for one bound I can see" | §4.14, spec line 114 | 028 |
2026-08-11 | **The library is source, compiled with the program: P-embed** (panel 028 R3). P-ir is vetoed — `TyId` indexes a per-compilation arena, so a spliced `Program` needs a remap over 24 `Op` variants plus a serialisation format, **and it does not remove the merge**, because `emit()` still takes the `ast`, `checked` and `src`. P-c is vetoed on compiled evidence: it survives `range` and dies at the first generic, where its only prebuildable shape is type-erased — passing a `str`-typed function to map an `[int]` gave **zero diagnostics under `-Weverything`** and then SEGV. P-source buys no capability and opens a trust boundary the runtime does not have: `_Static_assert(HERO_RUNTIME_ABI)` exists so a decoy `runtime/` cannot replace the contract, and clang cannot type-check a decoy `.hero` | the historian drew the same line from outside: **generics travel as IR, never as code** — rustc's collector runs just before codegen so rlibs ship MIR, and GHC's `.hi` files carry unfoldings because cross-module specialisation needs them. Six of the seven Tier-2 functions are generic, so the boundary is not a choice | §1.11, §4.12, §4.20 | 028, 015 |
2026-08-11 | **One definition, many prototypes — the linkage rule, decided now and implemented at M8a** (panel 028 R3b). `emit/decls.rs::signature` emits no storage class, so every Heroes function has external linkage, and at M8a two modules that both call `range` are `ld: duplicate symbol` at −O0, −O2 and −flto alike — surfacing as exit 2, the compiler blaming itself for a decision nobody made. **`__attribute__((weak))` is vetoed on a measurement**: two units, one mangled symbol, *different bodies*, link and run under this project's exact flags with zero diagnostics, exit 0 and a wrong answer. `static` everywhere is refused for `-Wunused-function` noise projected at ~1200 across the port. A library or monomorphised function is emitted into exactly one deterministically chosen unit; everyone else gets a prototype | design.md covers none of this — §4.1 says "no modules in v1" and §4.12 never says which unit a monomorphised copy lands in — so it is a new rule rather than a citation, and it is made with the evidence in hand rather than under M8a's pressure | §4.12, ROADMAP M8a | 028 |
2026-08-11 | **The tier phrase stays, and the removal that would have deleted it stays available at −6** (panel 028 R6). The warden found the project's first measured removal **by asking what a sentence does rather than what it says**: `Tier` appears nowhere in the compiler outside the table defining it, and a Tier-1 name passes as a function value exactly like a Tier-2 one. The ergonomist, reading only the spec, reported that the phrase told it three things — chiefly that these names "obey the ordinary rules", which is precisely what makes §4.11 apply to `range`. The phrase is the premise the labels follow from; delete it and `range(from:, to:)` looks arbitrary | the warden's own withdrawal condition — "I withdraw the removal if anyone shows a program a reader writes differently because of the tier phrase" — was met by another judge's transcript, which is the differentiated-input design doing the thing it exists to do | §1.11, spec line 151 | 028 |
2026-08-11 | **A function value is a bare C function pointer, one `typedef` per distinct signature.** No captured environment, because v1 has no closures — which is §1.11's founding constraint paying out: a Heroes function value and a C callback are the same eight bytes, so `qsort`'s comparator and every raylib callback are expressible at M7 with no shim (design.md:1768 counts nine of ten ladder cases needing exactly this). A `typedef` rather than an inline spelling because C puts the declarator's name *inside* a function-pointer type, and one exception to `<type> <name>` would have to be threaded through the prologue, the parameter list and every temporary. A zero-parameter typedef is `(void)`, never `()`: an empty list in C means *unspecified*, which turns a wrong-arity call through the pointer from a compile error into undefined behaviour | panel 013 recorded the forward consequence and it is unchanged: the day closures arrive, a capturing one is a record plus a pointer, so the type system must distinguish capture-free **at the boundary** rather than everywhere | §1.11, §4.19, spec lines 89-93 | 013 |
2026-08-11 | **Generated C declarations are emitted in one pass in `TyId` order, options and function typedefs interleaved.** Each kind can name the other — `(function(int) -> int)?` and `(function(int) -> int?)` — so neither can be emitted wholesale first. Measured when they were two passes: a function typedef naming an option not yet declared parsed as an implicit-`int` function type, and clang reported *"'const' qualifier on function type … has no effect"* on a line about something else. **`TyId` order IS containment order**, and that is an invariant of the type table rather than luck: the checker interns a composite only after the ids it is built from | one ascending walk emits every declaration after everything it names, with no graph and no sort | §4.20 | — |
2026-08-11 | **The function typedefs are derived from the program, never walked off the interned arena** — the rule `descriptors.rs` already had, applied to a second table. The checker interns a `Ty::Func` for *every* top-level declaration, so an arena walk emitted one typedef per function in the file, almost all named by nothing; three golden `.expected` files changed for programs that use no function value at all. An unused typedef is not a warning the way an unused `static const` is, which is precisely why it had to be deliberate: nothing would ever make it fall out | the derived set restored all three goldens to byte-identical, which is the check that the noise was noise | §4.20, panel 022 | — |
2026-08-11 | **Monomorphisation runs BEFORE the ownership pass, and the reason is not the one the ROADMAP gave** (panel 029 R1). Not "ownership cannot decide with generic types" — it **has already decided, wrongly**: `is_refcounted` answers `false` for `Ty::Generic` (`types/counted.rs:89`), so `own.rs` emits `store total <- $t1` with no incref and no decref into a body that will be instantiated at `str`, while the `[A]` beside it is counted because `Ty::Array(_)` is unconditionally counted. Dumped by a judge and reproduced by the convener. `ir/phases.rs` cannot catch it, because `released_on_return` asks the same lying predicate | a pass that has committed a wrong answer is worse than one that declines to answer, and the ordering follows from the defect rather than from a preference | §4.12, Part 5 | 029 |
2026-08-11 | **The type substitution is recorded in `Checked` and never recomputed in the pass** (panel 029 R2). `types/calls.rs:260` computes the bindings, uses them and drops them; `Checked` has no table; `Op::Call` carries no type arguments. Recomputing them at IR level would duplicate `types/generics.rs::bind`, and `types/counted.rs:11-15` already ruled on that shape by name — *"two answers that disagree … is a refcount bug that reproduces once a week"*. A dense side table plus a `TyArgs` pool mirroring `Args`/`Steps` is therefore forced, not chosen | it was the compiler-engineer's stated veto condition, and it is the same rule this project has applied twice before | §4.12 | 029 |
2026-08-11 | **`Checked::counted` is extended after monomorphisation.** Its own comment says it is built last *"because nothing may intern a type after it: a `TyId` past the end of this table would read as uncounted, which is a leak that no test can see"* — and the pass interns `[str]` where only `[A]` existed. Unhandled it panics at `ir/layout.rs:87`; softened to a bounds check it becomes exactly the silent leak that comment describes. The step lands a golden instantiating one generic at `str` and at `int` in the same program, under `--sanitize`, with the leak counter green | a table whose invariant is "nothing may extend me" meets a pass whose job is to extend it; one of the two has to move, and the comment says which | §4.10, §4.20 | 029 |
2026-08-11 | **Polymorphic recursion is refused by the monomorphisation pass**, not by the checker and not by a depth limit (panel 029 R4). It type-checks clean at exit 0 today and design.md does not mention it anywhere. The checker cannot see it — it checks a generic body once, polymorphically, and never expands it; a depth limit would exit 2, *"the compiler could not run"*, for a program §4.12 makes legal. So the pass refuses structurally: instantiating `f` at `S` from within `f` at `T` where `S` **properly contains** `T` is unbounded, decidable, and exactly MLton's finiteness argument — its whole-program monomorphiser has been total for twenty-five years *because* SML bans this. Inference for it is undecidable (Henglein 1993; Kfoury–Tiuryn–Urzyczyn 1993) | Rust is the warning rather than the model: it accepts at type-check and blows up at codegen with `reached the recursion limit while instantiating`, no error code, late and unattributable — which is the failure shape this project's thesis exists to refuse | §4.12 | 029 |
2026-08-11 | **A live defect the panel found while looking for something else: `[()]`, `[ptr]` and `{str: ptr}` check clean at exit 0, build a binary, and abort with "entered unreachable code — this is a compiler bug"** (panel 029 R4b). `emit/gate.rs:146` returns unconditionally for `Ty::Array(_)` without descending into the element type, so the `ffi_type` row never fires and `Ty::Unit` returns too. Today the author must *type* `[ptr]` to reach it; **after monomorphisation they infer it** — `map(nums, print)` infers `B := ()`, `map(handles, close)` infers `A := ptr`. The gate descends into `Ty::Array`, `Ty::Map` and `Ty::Fallible`, and the pass refuses `()`, `ptr` and `cstr` as type arguments at the call site, naming the inferred argument | the generated C said so itself and nobody read it: `hero_unreachable(); /* not an array */` | §4.19, §4.20, CLAUDE.md §9 | 029 |
2026-08-11 | **The mangled suffix is a hash of the CANONICALLY RENDERED type, never of the `TyId` sequence** (panel 029 R5), with each instance emitted under a comment naming its types. Two judges constrained it from opposite ends. The historian: RFC 2603's complaint about Rust's legacy hash was information loss and *"results cannot be replicated by another compiler implementation"* — hashing the public rendering answers the second, and the comment answers the first for free. The engineer: a readable suffix is not spellable, because `render_ty`'s output carries `[ ] { } ( ) ? ,` and spaces. The ffi-pragmatist closed it by compiling both: a readable suffix is **not injective** — `pair<int, str_x>` and `pair<int_str, x>` both spell `h_m_pair_int_str_x`, so clang answers `error: conflicting types` **on a legal Heroes program** — and it buys nothing, because clang prints the `aka` expansion either way | `TyId` is an interning-order artifact, and a `TyId`-derived hash would require the Rust bootstrap and the Heroes port to intern in identical order for the M8c fixpoint to close. `mangle.rs:29-33` already applies that reasoning to `module_of` | §4.12, CLAUDE.md §7 | 029, 028 |
2026-08-11 | **Panel 028 R3b does not cover a monomorphised instance, and the amendment is M8a's** (panel 029 R5b). R3b's shape — one definition in the library's own translation unit — **does not compile**: the library TU does not know the user's types and instantiation is caller-driven. Measured across three optimisation levels: external is `ld: duplicate symbol`, `static` links but breaks function-value identity (`h_a_addr() == h_b_addr()` is false, which is new because step 5 made `eq` pointer identity), `weak` links and preserves it. **This panel records and does not decide**: panel 028 vetoed `weak` on its own measurement — two units, one symbol, different bodies, zero diagnostics, wrong answer — and that veto is not overturned by a judge wanting `weak` for a different reason. M6 has one translation unit, so nothing forces the choice now | a decision made under deadline at M8a would be worse than one made now with both measurements in the record and no obligation to pick | §4.12, ROADMAP M8a | 029, 028 |
2026-08-11 | **Spec: `fold`'s accumulator order, +19 measured (2354 → 2373)** — and the step that lands generics is what makes the defect reachable (panel 029 R6). `fold` is refused by the gate today, so its silent divergence has never been observable: with accumulator and element sharing a type, **both** callback orders satisfy `types/builtins.rs:263` and exit 0. Four well-typed programs, one source text, four outputs, zero diagnostics. `sum`, `concat`, `max`, `min` and every string builder are same-typed folds. The spec is **behind design.md, not choosing**: design.md:2415 already fixes `reduce<A, B>(xs, initial, (function(B, A) -> B))` | the language's own defence against argument-order mistakes — mandatory labels on same-typed parameters — **cannot fire at a higher-order call site**, because the call site is inside `fold` and not in the author's file. The protection is off exactly where the hazard is | §1.4, §4.12, spec line 89 | 029, 012 |
2026-08-11 | **design.md Part 5's closing sentence is amended: it is falsified by its own table.** Line 1944 says *"Everything in this table is gone in the IR: lowering erases it on the way in"*, while line 1941 of the same table says generics are erased *"on the IR, after type checking"*. One row erased by a pass rather than by lowering is fine; a table claiming otherwise is not | `ir/verify.rs:27-30` already documented the exception informally, which is how a false sentence survives — the truth was written somewhere nobody compares it against | Part 5, §1.7 | 029 |
2026-08-11 | **The six higher-order built-ins are Heroes source now, and `types/builtins.rs` lost 92 lines** (292 → 200). `map`, `filter`, `fold`, `find`, `any` and `all` are declarations in `library/source.hero`, checked by the ordinary rules; `Shape`, `higher_order` and `fold`'s bespoke checker rules are gone, and `resolve/exprs.rs` consults the top-level table before the built-in one, so no `Callee::Builtin` is produced for them. That is §4.12's own claim made good — without generics these were "seven special cases in the type checker, each with hand-written rules", which is exactly what those 92 lines were | the **by-name** gate row can no longer fire at all: every one of the 23 reserved names now has an entry point, a library body or a lowering. The row stays as the net under a mismatch, and what replaces the example is that partition asserted — an example proves one name is refused, the partition proves none can be | §1.11, §4.12 | 029 |
2026-08-11 | **The library and the author's file share one `Source` and cannot shadow each other.** Two collisions, both found by the R5 guard rather than by a test: the library's `fold` has a local `total`, and a user program with a top-level `total` was told its own declaration was shadowed by a line it cannot open; and the library's `map<A, B>` collided with a user's `record A`. The rule is one line in two places — a name declared in one region never shadows one declared in the other — and only that direction needs it, because a user local shadowing a library *declaration* is caught earlier and more precisely by `builtin_name_taken` | the guard panel 028 R5 asked for as insurance turned out to be the thing that found the defect, twice, on its first exposure to library code that has locals | §1.11, §4.4 | 028, 029 |
2026-08-11 | **Spec: `fold` folds left with the accumulator first, +9 measured (2354 → 2363)** — cheaper than either form the panel priced, and it carries *more*. The warden measured its own wording at +19 and mine at +24; both stated the argument order, and the warden's omitted the direction, which the ergonomist's withdrawal condition required in the same breath ("a signature alone fixes order but not direction; I need both"). `xs.fold(0, add)`, folding left with the accumulator first — nine tokens, on the line the example already occupies | the sentence lands in the step that makes the defect reachable, not before: `fold` had no body until now, so its silent divergence had never been observable | §1.4, spec line 89 | 029, 012 |
2026-08-11 | **`heroes test` is a subcommand and runs ONE PROCESS PER TEST.** The shape is CLAUDE.md §10's stopping rule applied mechanically — it answers a different question about the same input, and its artifact class is a verdict per test where `build`'s is a binary and `run`'s is a process. The process-per-test is not a style choice: an `assert` is a **panic** (§4.18), so a runner that called the blocks in sequence would stop at the first failure and hide every test after it, which is the one thing a test runner must not do. The binary therefore takes an index and `heroes test` executes it once per test | the titles never reach C: the index is the whole protocol, and a title is a string literal — spaces, leading digits, any UTF-8 — that no C identifier admits and that sanitising would not make injective, which is the argument the type hash already makes | §4.18, CLAUDE.md §10 | — |
2026-08-11 | **The `assert` gate row is retired, and what it was waiting for was the emission rather than the refusal.** M5d kept it "only because the step that lands `heroes test` needs the row already written. If that step slips, this row should be struck, not defended." The step did not slip: an ordinary build never reaches a `test` block and a test build emits it, so no program can make the row fire. `Op::Abort`'s arm now holds no rows rather than being deleted — adding an `Abort` kind stays a compile error there | a row kept for a named future is a debt with a due date, and this one was paid rather than rolled over | §4.18, CLAUDE.md §9 | — |
2026-08-11 | **A side an `assert` cannot render is discarded explicitly**, `(void)t13;`. Spec line 163 asks for the source expression *and both sides*, and the sides are rendered through the same `to_str` entry points `print` uses — so a record or an array has no rendering and the text-only form is used. The lowering still computes them, because asking it not to would put a backend question in the IR; the cast is what keeps `-Wunused-but-set-variable` at zero, and it says "deliberately" where silence would say "forgotten" | eight warnings on the calculator's own test build found this, which is the zero-warning rule doing the job it was adopted for (panel 020) | §4.18, CLAUDE.md §7 | 020 |
2026-08-11 | **The build order is revised: modules first, the FFI second, a probe before the port, and everything else after the fixpoint.** design.md Part 10 ordered the remainder FFI → (I/O, args, modules) → port → fixpoint; the chain is now M8a modules · M7 FFI · M8p probe · M8b port · M8c fixpoint, then M9 separate compilation, M10 concurrency, M11 QBE, M12 lsp. **M8a is the namespace, not the build architecture** — `use`, always-qualified names, one module string per declaration, and **one whole-program `.c`** exactly as today; §1.0's closure list says *modules*, never translation units, and CLAUDE.md §13 forbids compile time as the justification | three judges reached the same cut from three different inputs: the namespace slice is ≈+330 lines against +700–1000 for one `.c` per module (which also lands in the six whole-program sweeps at `emit/mod.rs:149–156`), the per-module cache is Nim's unfinished item since 2018 and why Rust shipped 1.52.1 to disable incremental, and — compiled — a wrong `extern` is 5/5 rejected in the TU holding the header and **0/5** in a calling TU that sees only a generated prototype | Part 10, §1.0, §4.19 | 030 |
2026-08-11 | **Separate compilation moves past the fixpoint (M9) and carries four acceptance rows**, which is what lifts the ffi-pragmatist's veto: the header travels with the extern into every calling TU; headers and link flags enter the cache key; every dependency's emitted interface enters it too; and one two-module FFI-shaped golden requires exit 1 in *both* TUs | measured, not argued — with a stale cache a header upgrade prints `1410065408` for `10000000000` at exit 0, and cross-TU signature skew that `-O0` and `-O2` get right returns `0.0` under `-flto` with no diagnostic anywhere. With one translation unit neither failure has a place to happen | §4.19, Part 10 | 030 |
2026-08-11 | **Measurement 003 rider 3 is corrected: file I/O, `args()` and `exit(code)` are a route M7 decides, not settled plain `extern`s.** The audit concluded "the spec gains **nothing** for them"; compiled against the real headers with the emitter's own type mapping, `extern function exit(code: int)` is `conflicting types for 'exit'` (`int64_t` vs `int`), and `fopen`/`fread` fail on `FILE *` and `size_t`. The mortgage is **≥60 spec tokens**, and it is the first entry in panel 024 R2's ledger | panel 013's 9-of-9 callback finding, reproduced on the closure list's own last three rows: the boundary lacks `c_int`, `size_t` and `const` (Part 7 item 10), and the route that *appears* to work — dropping the `#include` — is the silent-wrong-answer class §4.19 exists to prevent, since `void *fopen(const char *, const char *)` links by accident on arm64 | §4.19, §4.20, §1.6 | 030, 013 |
2026-08-11 | **Post-fixpoint order, by author instruction: packages (M10) before concurrency (M11) before QBE (M12), lsp last (M13) — and QBE's warrant is restated.** It was recorded in 2026-08-03 as "a second backend, not maybe"; the honest form is that **as long as exactly one backend exists, "the IR is target-agnostic" is an assertion no artifact tests**, and the IR could be a C pre-processor wearing an abstraction's name without anything in this repo noticing. Packages need M9 rather than M8a — without separate compilation, installing one means recompiling the world — and they are also where "a standard library that wraps C" legitimately goes: §1.11 refuses a standard library permanently, but **distributable bindings** are ordinary modules over real headers, each verified by clang against the header it names | design.md:637 already fixed the shape (`heroes add`/`heroes fetch`, inside the same binary), so this is an ordering decision rather than a design one — no panel, and design.md Part 7 is deliberately left untouched so that a milestone number does not convert a deferral into a commitment | Part 7 items 13–14, §1.11, §3.5 | — |
2026-08-11 | **Defect found while pricing that order: `-g` reaches clang only under `--sanitize`.** design.md §2 and §3.1 both claim lldb breaks on and steps through `.hero` lines via the emitted `#line` directives; `toolchain.rs:203` is the file's one `-g`, so an ordinary build carries no DWARF and the claim has never been executed. Repaired at M8b, with a golden that runs lldb in batch mode and asserts the breakpoint is hit | the `#line` emission is thoroughly tested and the sentence *about the debugger* is tested by nothing — the same class as a verifier check that never fires, and it surfaced from the question "is QBE worth more than a debugger?" | §2, §3.1, CLAUDE.md §9 | — |
2026-08-11 | **A corpus milestone (M8e) lands before the probe and the port, by author instruction: many whole programs, one directory each, all of them run in three configurations.** It is the only milestone whose deliverable is programs rather than compiler. Position: after M7, because a "whole program" that cannot read a file, take `args()` or exit with a code is not one; before M8p, because a probe that measures what the language lacks on a compiler with reachable defects measures the defects too. Programs are written **from the task**, never transcribed from a licensed tour (Rosetta Code, *Go by Example*, Nim's and Zig's sets are licensed; the problem is not) | the M6 audit found two live defects with ONE program, both reachable for three milestones with no test, "because nobody had written the program that meets them" — this milestone writes them, and CLAUDE.md §9 already says an invariant belongs over `heroes mutate`'s corpus rather than over cases somebody thought of | Part 10's golden-tests note, CLAUDE.md §9 | — |
2026-08-11 | **Four entries close the chain, by author instruction: M14 the complete VS Code extension, M15 the site, M16 the journey book, M17 the language guide — and CLAUDE.md §11 gains its one exception, because both books are written in Italian AND English.** Neither version is a machine translation of the other; the author studies from the Italian, so where they diverge the Italian is made clearer rather than the English made faithful, and both are in the plain register of the `/where` skill (zero compiler knowledge assumed) | the register is not a stylistic preference: §1.1 makes comprehension the objective and the author's stated use for the books is to study what was built, which makes plainness a requirement that outranks elegance and brevity. M14 takes the same discipline into the editor — the extension surfaces §4.17's `certain` fixes as quick fixes and never the guesses — and M15 anchors every code block to a program in `examples/`, so documentation drift becomes a test failure rather than a reading | §1.1, §4.17, CLAUDE.md §11, §12 | — |
2026-08-11 | **Licence: Apache-2.0, with a runtime exception — and both land now rather than at the publication milestone, because a repository accumulates history and history cannot be relicensed retroactively.** The exception is the load-bearing half: compiling a Heroes program emits C that `#include`s the runtime and links part of it into the binary, so read without an exception, Apache §4 would follow every program anybody ever compiles — a copy of the licence, a reproduction of `NOTICE`, and modification notices owed by the *user* of the language. GCC's Runtime Library Exception is the mechanism; the wording is this project's own and shorter. `NOTICE`, `README.md`, SPDX headers across the thirteen runtime files, and attribution for the two vendored BPE tables land with it | the author asked for "the most permissive licence, as long as paternity is attributed" and first chose MIT, then corrected to Apache — which buys the explicit patent grant and the trademark clause at the price of §4's paperwork, and §4 is exactly what the runtime exception removes from users. Attribution is owed by whoever redistributes Heroes; never by somebody who merely uses it | CLAUDE.md §14, §1.11 | — |
2026-08-11 | **Three gaps found by asking what would still be missing at publication: the bootstrap seed, the CI, and the unmeasured half of the thesis.** The seed is the sharp one — at M8c `crates/heroes` is archived, after which a newcomer has no Heroes compiler and no way to build one; the answer is the artifact the fixpoint already produces (`B.c`, the generated C, compiled by any clang, as Go shipped 1.4 and Zig ships its bootstrap), and it is now a release artifact tested from a clean checkout **before** the archive commit, not after. The CI is born at M8e because a corpus is what makes it worth having, and it runs Linux x86-64 beside Darwin arm64. Metric 2 goes to M18 as a gate | everything this project knows about its platform it learned the hard way — no ASan leak detector on Darwin arm64, `INT64_MIN % -1` not trapping there, Apple ARM64's on-stack variadics — and each is a place the other platform can diverge in silence, with nothing testing any of it today | Part 10, §1.2, CLAUDE.md §14 | — |

2026-08-11 | **Modules: the namespace, at +71 measured spec tokens** (2363 → 2434) — `use geom` binds `geom` to `geom.hero`'s declarations beside this file, written qualified, every module you name needing its own `use`, no aliases, no wildcard. **Variant B (a bare identifier) over variant A (a quoted path)**, four judges of five: the qualifier is read off the line rather than inferred from a basename no document derives; `use "geom"` + `use "shapes/geom"` is unwriteable under B rather than unresolved; and Nim's rule makes strings additive later where the reverse is breaking, while Go's quoted path is the one form with a documented, *frozen* defect (golang/go#28428; the 2024 re-spelling proposal #68151 closed not-planned). The verb is **binds**, chosen because it is the spec's own word: it makes "shadowing is a compile error" and "an unused binding is a compile error" cover a local named after a module and an unused `use`, for **zero tokens** | the proposal's own example, `geom.dist2(a, b)`, is **illegal as a qualified call** — two same-typed parameters make labels mandatory, so that text is legal only as UFCS desugaring to `dist2(geom, a, b)`. The line teaching qualification was written in the exact shape qualification forbids, and the judge wrote the correct form in its own program *against the spec's example*. Second time a spec sentence taught a wrong program, found the same way as panel 023: by writing from the document rather than reading it | §1.0 closure list, Part 7 item 4, Part 10 step 14 | 031 |
2026-08-11 | **UFCS across a module boundary is a diagnostic, not a spec sentence, and module cycles are refused with no sentence either.** `p.dist2(o)` where `dist2` is imported: the resolver fails to find it unqualified and the *error* names the module and carries a `certain` fix. Measured saving 11 tokens, and CLAUDE.md §8 requires the error to carry what is needed without opening another file. Cycles are a back-edge check on the `seen` stack discovery needs anyway (~15 lines): refusing is the reversible direction, since tightening at M9 — where the per-module cache needs a topological order — would break the port | the spec pays only for what a reader cannot get from a message they are already receiving; a rule the compiler states loudly at the exact call site is not worth a sentence in the prompt | §1.6, §4.17, CLAUDE.md §8 | 031 |
2026-08-11 | **`Source` becomes N files concatenated with a per-file table, and discovery re-uses the real lexer.** Measured: 39 `Span {` construction sites across 14 files and 271 `&Source` parameters — concatenation changes **0 of 39** and **0 of 271**, a file id inside `Span` changes all 39 and pushes it past 8 bytes. Discovery lexes each file with *the* lexer, collects its `use` names and recurses, rather than a prefix scanner licensed by a "`use` lines come first" spec rule — which refuses the compiler-engineer's condition and removes its hazard instead (two grammars that can disagree), CLAUDE.md §13 forbidding the compile-time argument that would have bought the rule | the architecture M5b chose for one special case, the appended library, turns out to be the architecture for N modules; the library becomes the last row of the table | §3.1, CLAUDE.md §5, §13 | 031 |
2026-08-11 | **Panel 030's deletion claim is withdrawn: the library becoming an ordinary module deletes ~10 lines, not a special case.** `library::misplaced` survives and is asserted by a test; `emit/builtins.rs::reachable` survives verbatim with `is_library` swapped for a module comparison, because a program that never calls `range` must not carry it into `--emit-c`; `is_library` survives as a one-line wrapper, which is what keeps its 15 non-test call sites at zero edits. M8a is priced at **+475** non-test lines against 030's ≈+330 | an estimate made while pricing something else is not a measurement, and this one was checked by grep rather than re-argued | Part 10 step 14 | 031 |
2026-08-11 | **Panel 024's delta gate lands as a test, not a command** — one test asserting the spec's measured size against a recorded constant, so any spec change must update that number in the same commit, which is exactly when panel 012's rule asks for a named removal or a registered prediction. CLAUDE.md §10's stopping rule refuses it as a subcommand or flag (it types no fixpoint invocation, no golden harness, no Part 11 harness — the refusal that struck `outline` and `explain`), and the golden harness types it as a test. Nothing pins that number today | a budget whose veto is out of reach is decoration; the cheapest instrument that makes it fire is not always a capability | §1.6, CLAUDE.md §9, §10 | 031, 024 |

2026-08-12 | **Subdirectories: refused for now, and the reason is a count rather than a preference.** Author instruction pulled the decision forward from M10 ("se no il self host parte molto disordinato"); the panel answers the *reason* instead of the request. **A path buys zero `use` lines** — 452 flat, 452 nested, because a path changes how a target is spelled and never whether a `use` is needed — and it forces the **same renames**: 40 of the port's 119 files sit in 15 last-part collision groups (`decls` ×5, `types` ×5, `exprs` ×4), every one refused by the proposal's own uniqueness clause, and repaired by `types/typedecls.hero` qualified `typedecls.foo()`, which is flat naming with directories painted on. **Nim's compiler has run 171 files in one directory for years** under the same last-component rule, and Go's compiler was 60,000 lines before it was split into packages, *after* self-hosting. The path form stays purely additive — `module_path_has_no_parts` refuses every `/` today — so waiting is free and acting pre-empts M10, where packages produce the case that decides the spelling. **If the author overrules, the form is fixed at +26** (`- A `use` may be a path: `use syntax/decl` binds `decl`. Last parts are unique.`), never the +38 tabled | Principle 0's burden is not met — subdirectories are not on the closure list, no Part 11 effect exists, and "the port would be tidier" is the shape measurement 003 rider 3 refused when this file's own ROADMAP scheduled `outline` and `explain` | §1.0, §1.3, §1.6 | 032 |
2026-08-12 | **"A directory is a module" is struck, by three independent vetoes and one opposition.** It is the only candidate in the panel's record that produces a **silent wrong program**: its wording makes `use syntax` mean one thing while `syntax.hero` is absent and another once it exists, so adding a file — a facade, a generated output, an unfinished rename — silently rebinds every call in every importer, and both versions type-check. Compiled, it also chooses a translation unit's **headers by directory membership**: an ncurses binding and a raylib binding in one `ui/` directory give 8 errors inside `raylib.h` from a library the program never calls, where the per-file form is exit 0. It buys 165 fewer `use` lines and buys them by deleting "always qualified" (panel 030 R4, panel 031) and adding a directory-enumeration primitive the closure list does not license. TypeScript ran the experiment: its namespaces are this model, its team spent 2019–2023 removing them, and the migration published a 46% smaller package and 10–25% faster builds | the conditional clause is panel 023's falsified form verbatim, and this project's own log calls a rule smuggled into a subordinate clause the highest-risk text in the document | §1.3, §4.19 | 032 |
2026-08-12 | **The C symbol component is the module's whole path, sanitised and concatenated — never its last part.** Measured over the port's 169 real paths: last-part components collide **22-way on `mod`** alone, whole-path concatenation collides **zero** times. This holds whether or not paths ever enter the language, and it is the shape `module_names_collide` must compare | the two namespaces are not the same namespace: what the author types before the dot and what reaches the linker have different collision sets, and one check was written over the wrong one | §3.1, CLAUDE.md §7 | 032 |
2026-08-12 | **`use` is scoped to the program, and the spec has to say so before there is a unit above the file.** OCaml's flat module namespace is survivable inside one program and unfixable across libraries, which is why Dune's wrapped libraries exist and why the fix had to come from the build system rather than the language. Heroes has no bundle yet; when M10 adds packages, a uniqueness rule that inherited a global scope by silence is the one that cannot be re-scoped | a rule whose scope was never stated acquires the widest one by default, and that is the scope nobody chose | §1.0, Part 7 item 4 | 032 |

2026-08-12 | **Private record fields and private variant cases are struck permanently, and move onto Part 6.** Fields were already refused by name at design.md:1142 inside §4.9's prose; the refusal now sits on the list that means *never*. Three vetoes each, from three inputs: a private field is an **opaque type wearing a field annotation's name** (`check_named_fields` demands every field, so the type becomes unconstructible from outside, and `types/access.rs:42,66` would print the hidden field's name in the message refusing it); the precedented form hides the *type* and never the field (Haskell 2010 §5.2's abstract datatypes, Ada 83's private part with no methods for twelve years, Zig issue 9909 closed not planned); and — compiled — **privacy stops at the ABI**: a §4.19-style shim read the "private" field at offset 16 of 24 bytes, wrote it, and built the "private" case, with `nm -gU` showing that case's `eq`/`hash` as global `T` symbols. For cases, Rust ran the experiment: `priv` variants shipped, were **deleted** by RFC 0026 (2014) as rarely used, returned per-*type* as `#[non_exhaustive]` in 1.40.0 (Dec 2019), and Rust's own documentation records the price — *"exhaustiveness checking cannot be used"* — with the repair lint unstable since 2021 | Heroes cannot pay that price at all: `_` is forbidden on variants, so a hidden case makes **whether a line compiles depend on a declaration the reader is forbidden to read**, and `types/patterns.rs:1-15` says that coupling is the entire reason the ban exists | §4.7, §4.9, §4.20, Part 6 | 033 |
2026-08-12 | **Declaration visibility is neither adopted nor refused: it becomes a Part 7 item whose decision instrument is M8p, and its landing form is fixed at +18, not the tabled +30.** Cost was never the objection — ~80 non-test lines, **zero** in `ir/`, `own.rs`, `emit/` and the mangler, because one whole-program translation unit gives visibility no linkage consequence; +30 is 1.8% of headroom. The burden is. It is not on §1.0's closure list, no Part 11 effect exists, and **the §1-derived argument offered was refuted by a grep**: Heroes compiles one whole program, so a declaration nobody calls is already computable — `unused_use` (`resolve/top.rs:119`) proves that shape at zero tokens, and privacy only makes the check module-local, which starts mattering at M9. The priced text is also not the justifying text: the bare licence measures **+18**, licence plus the dead-code rule **+27**, and the tabled +30 buys a prohibition clause that R1 puts on Part 6 for free. No shipped language makes an unused top-level declaration a hard error — rustc's `dead_code` is defined on visibility but warn-by-default, Go's spec stops at the function body, OCaml's warning 32 is off, Nim's is a hint whose error form crashed the compiler until 1.6.14 — and every one shipped an escape valve | ROADMAP:602's rule applies literally, *a form whose workaround compiles is a Part 7 deferral by default*, and the workaround compiles: `examples/calculator/` is four modules with zero visibility. Deciding now would also convert panel 031's registered ≥+25 prediction into one this panel fulfilled by voting for it — the warden's finding, and the reason the direction (256 `private` against 355 `export` over the port's own tree) is left to the count M8p produces | §1.0, §1.7, Part 7 | 033, 031 |
2026-08-12 | **M9's extern row gets its own rule, and it is not a visibility rule: an `extern` is never callable across a module boundary.** Panel 030 R2 row 1 offered a disjunct — the header travels into every calling TU, *or* externs are module-private and the type checker refuses the qualified call — and this was the only place the record needed visibility at all. Compiled, **neither tier delivers it**: opt-in leaves public externs as the default, and default-private keeps `export`, which is the common case for a binding module. Three shapes built against real headers: header in both TUs is exit 1 in both; header only in the declaring TU is exit 1 there and **exit 0 in the caller with the wrong signature** (`rc=0 db=open`); extern module-private with a Heroes wrapper has nothing to get wrong. `private` also cannot *mean* C `static` — `static declaration of 'sqrt' follows non-static declaration` against the real header — and the fallback of dropping the `#include` links to the real global symbol and runs | the rule is smaller than any visibility tier, spends **zero** tokens, and satisfies row 1 whether or not visibility ever ships; it belongs to M9's acceptance rows rather than to this panel, which was convened about the language | §4.19, Part 10 | 033, 030 |
2026-08-12 | **The Rust shape is already in the language, and the record now says so; what Rust has and Heroes does not is the *typed* error, which stays a Part 8 wart rather than becoming a Part 7 item.** `T?` ≈ `Result<T,E>`, `expr?` ≈ `?`, `.must()` ≈ `.unwrap()`, `.default(v)` ≈ `.unwrap_or(v)`, `match .ok/.err`, exceptions refused permanently. Typed errors need three things this language has decided against — **generics on types** (design.md:1425 is a positive rule, not an omission), traits for any conversion (Part 7 item 8), and a runtime tax — and Part 7's preamble admits only items that *"lose only on the simplicity vertex"*, so filing them there would turn a wart into a promise. Measured against real `sqlite3.h`: a typed error carrying what a C library actually reports (`sqlite3_errmsg`, copied through `hero_str_from_bytes`) is **32 bytes and refcounted**, and `ptr?SqlErr` is **40** — identical to today; the 16-byte win needs a payload-free error, which cannot carry a message. Compiled, `?` across two bindings stops being a struct copy (`error: assigning to 'h_AppErr' from incompatible type 'h_SqlErr'`) and needs one hand-written converter per pair, and `m[k]` has no error type to be | Swift shipped typed throws (SE-0413, accepted 2023-12-20, Swift 6.0) a decade after 1.0, **with** full generics, and its own proposal says *"Resist the temptation to use typed throws"*; Rust's landing place is a derive macro over an enum, which needs generics on types, traits and macros — three refusals | §4.6, §4.12, Part 8 wart 5 | 034 |
2026-08-12 | **The stringly-typed error code is a measured defect class, not acknowledged debt — and the cure is a `constant`, which the language has had since M2.** `heroes mutate`'s new `typo-code` operator reads **25 mutants, 0 killed, in both arms**, the only zero in an eleven-row table. The published magnitude: Eghbali & Pradel, ASE '20, 204 real string bugs — **42% are incorrect string literals**, a widely used analyser finds **1 of 204**, and **11%** ever surface as an error message. The proposed cure, a diagnostic refusing a comparison against a code no `fail` constructs, is **not adopted**: it is root-dependent (the same file errors rooted at `lex` and is clean rooted at `main` — compiled), it rejects the correct program `e.code == "missing_key"` written straight from spec line 133, `fail(prefix() + "key", msg)` compiles today so its escape hatch is a silent program-wide off-switch, and it cannot fire in the self-hosted compiler at all, whose codes are a record field threaded through `@diags`. Adopted instead, at zero spec tokens and zero compiler lines: **codes as `constant`s**, compared as `store.ERR_UNKNOWN_ITEM`, where a typo is `error[unknown_name]` today, locally, with the owning module named | the sting is the finding: the convention **silently defeats the diagnostic**, because there is no literal at the comparison left to check — *the rule is strongest where the code is worst and silent where it is best*. And no shipped compiler makes an *inferred* closed-world string domain a hard error: TypeScript's domain is declared, Rust's `unexpected_cfgs` is a warning because `build.rs` can widen it, Dialyzer lives outside the compiler | §4.6, §1.3, Part 11 | 034, 031 |
2026-08-12 | **A variant case payload was never released when no `match` in the same compilation bound it — the same module leaked rooted at itself and was clean rooted at its caller.** `emit/perfn.rs::owns_case` asked the type interner, and `Ty::Case(decl, case)` is interned when a `match` arm **binds** the payload, not when the case is built. `examples/calculator/lex.hero` constructs `.name(s: …)` and never destructures it, so it emitted `void h_lex_Token_release(…) { switch (v->tag) { default: break; } }` — an empty switch — while the identical declaration compiled beside `parse.hero` emitted the walking release. The retain was emitted either way. Fix: ask the **fields**, which do not move between compilations, where the interner does | 90 `run/` cases missed it because the shape needs a payload owning a *computed* `str` (a literal is static and cannot leak) **and** no `match` binding it in the same compilation — every existing case that builds a `str`-carrying variant also destructures one. No golden's emitted C changed when it was fixed, which is the same finding said twice | §4.10, §4.20, CLAUDE.md §7 | 034 |
2026-08-12 | **Eighteen panel sessions ratified in one blanket author instruction**, from 013 (the function type's marker, open since M2) through 034, plus the Part 6/7 split for green threads. Each panel file gains a `## Ratification` section that says what the instruction was — *"ratifica anche tutto quello che c'è da ratificare"*, given after a session summary rather than a clause-by-clause review — and states the limit: it settles that the provisional resolution **is** the decision and that work no longer runs on it as a default; it does not settle anything a panel keyed to a measurement that has not been taken (metric 2 above all), because a blanket yes cannot make a number arrive | the alternative was worse in both directions: eighteen sessions whose resolutions have been load-bearing since the day they landed, still labelled `provisional — author ratification pending`, is a record that understates what the compiler actually assumes — and a ratification recorded as if it were eighteen individual reviews would overstate it. Writing down which one it was costs three sentences and keeps CLAUDE.md §12's rule intact where it is easiest to break: in the project's own account of itself | Part 0, CLAUDE.md §4 | 013, 019–034 |
2026-08-12 | **Nine emitter defects, one shape, and the shape is D3's: the emitter kept asking what the program *mentioned* where the answer had to come from what a declaration *is*.** Every one refused or miscompiled an ordinary program, and every one was found by compiling rather than reading. `equality_body` had **no row** for `Ty::Map`, `Ty::Fallible` or `Ty::Func` while `hash_body` covered all three, so a record holding a map hashed fine and **aborted on `==`** — a map keyed by one inserts and then dies on lookup, which is CLAUDE.md §7's "eq and hash agree" broken in the loudest direction. `descriptors::generated()` answered "which types did the program mention inside a container" while its three callers ask "which types does the function I am about to write name": `function grab() -> P?` was `use of undeclared identifier 'h_M_P_desc'` at exit 2, and adding `[P]` *anywhere in the program* fixed it. Declared aggregates were emitted wholesale before the typedefs the emitter invents, and **each kind can contain the other** — `record Box { v: int? }` needs the option first, `function grab() -> P?` needs the record first, both ordinary. `builtins::reachable` matched `Op::Call` and never `Op::FuncRef`, so a library function passed as a **value** was referenced and never defined. The emitter's own `opt<N>`/`fn<N>` names were unreserved, so `record opt0` gave `redefinition`. And a `()` field reached clang as `void f_u;`, because the gate walks the IR and a declaration's field list is in none of it | **no golden moved and no expectation changed** — the corpus was not compiling any of these shapes, which is the same finding as D3's ninety `run/` cases and the reason each fix ships with a case named after it. The typedef repair is one containment order over both kinds, seeded with the two old orders so a program without cross-kind containment emits byte-identically; the name repair uses a **leading digit**, which `h_<component>_<name>` can carry and a Heroes identifier cannot, closing panel 031 R10's residual by construction instead of by a check | §4.2, §4.6, §4.13, §4.20, CLAUDE.md §7, §10 | 031, 034 |
2026-08-12 | **A narrowing asks the value, never the world** (author instruction, after sweep 001). Two of the sweep's twenty defects were protected by a comment that *argued correctly for a world that had changed*: `spans_lines` was narrow on purpose, with the reason written out — a reason about source **a person writes**, which stopped being true the day the 88-column rule made the formatter write source too; and `case_fields_are_counted` returned `false` under a valid argument from a premise that had died. Both had been read by reviewers who agreed with them, which is the whole finding: **a convention about comment style cannot catch this, because the comment is not wrong.** What can catch it is the shape of the question. Both repairs turned out to be the same one — ask the value's own extent, ask the declaration's own fields, instead of asking what kind of thing people write or what the program happened to mention. A fact about the value in hand cannot expire | the rule is now CLAUDE.md §11, with two obligations where a premise is unavoidable: state it as a falsifiable claim rather than a justification, and give it a **test that fires when it dies**, whose failure message names its dependants. `a_declared_type_cannot_contain_a_type_parameter` is the first — §4.12's "generics on functions only" is silently load-bearing in `emit/ctype.rs::mentions_generic`, `ir/mono.rs::contains` and `types/table.rs::poisoned`, all three of which walk `Array\|Fallible\|Map\|Func` and answer `false` for a `Ty::Named`. Nothing connected the three to the rule, and the rule itself is enforced **by accident**: a type parameter is unresolvable in a field because the parameter list lives on the function, not because anyone wrote "a declaration may not be generic". Part 8 wart 5 is the live route to killing it | §4.12, CLAUDE.md §11 | — |
2026-08-12 | **The premise rule, turned on the compiler: nine more live defects, and twenty premises that turn out to be five claims.** The worst is a **NaN as a map key** — `{f64: int}` is legal and `0.0 / 0.0` is legal, the probe rests on the key's `eq` being reflexive, and nothing said so. Inserted twice, `len` was 2; the lookup answered `missing_key`; `{nan: 1} == {nan: 1}` was false. **Exit 0, ASan clean, leak counter balanced — every instrument this project owns reported success**, which is the class §1.2 exists to eliminate, and the project had already ruled on the identical hazard for `sort`. The second is the lexer walking to `text.len()` under a doc comment saying *"one source file"*: since M8a the indent stack and the bracket list crossed the file boundary, so an unclosed `(` in `main.hero` swallowed all of `geom.hero` **and** the commonest state a file is ever in — a half-written `y =` — answered `internal error … exit 2`. The third: a user file named `library.hero` had its declarations resolvable unqualified everywhere, because four sites identify the library by module *name* while the collision check skips a pair whose modules are *equal* — a hole exactly at identity | the roots are the finding, not the count: **"the runtime may trust its caller"** carries five of them, **"one byte is one column"** three, and a premise depended on in three places is one test rather than three. `sort`'s NaN abort and the map's missing one were the same rule at two sites, decided once and applied once | §4.9, §4.15, §4.20, §1.11, CLAUDE.md §11 | 006, 007, 026 |
2026-08-12 | **The premise audit closed: twenty premises, five roots, seventeen more repairs — and one instrument that fired on its first day.** The roots are the record, not the count. *"One byte is one column"* had **three** faces (the formatter's 88-column rule, the caret's width, and the reported column itself), and the doc defending byte columns cited `#line`, which carries a file and a line and **never a column** — the premise defending the premise was dead too. *"The runtime may trust its caller"* had four, of which `hero_str_from_bytes` validating nothing is the one due to die at M7: `slice`, `chars` and `len` all rest on well-formed UTF-8 and nothing checked it, held up only by a gate that refuses `extern` today. *"Counting as a proxy for identity"* had one, and `phases.rs` had already rejected counting for the decref sweep twenty lines from the copy-out check that still counted. *"Hand-maintained lists"* had six, including `is_thesis_rule` — where a **missing** entry silently understates the number the thesis is argued from, and nothing mechanical could see it | the repair for that last one is the shape this project keeps re-deriving: pin the set of codes the golden corpus annotates to a constant, `measure::gate`'s design, so adding a diagnostic is red until somebody writes it down **and decides which side of the control arm it is on**. It fired the same afternoon, on the new `stray_carriage_return` golden — an instrument whose first act is to catch its author is the only kind worth trusting | §4.15, §4.17, §4.20, Part 11, CLAUDE.md §7, §9, §11 | 006, 011, 022 |
2026-08-12 | **Thirteen spec silences, costed as one package — and the spec got SMALLER: 2434 → 2422, net −12.** They had accumulated across eight milestones, one `Decide:` entry each, and were taken together because §1.6's budget is a single pool and thirteen separate +10s is how a document reaches its ceiling without anyone deciding it should. **Nine of the thirteen close at zero**, measured by running every draft: each wrong spelling is a loud diagnostic that prints the rule, so the tokens would buy no silence. One clause is **deleted** — *"No null. Absence is a different type (`T?`)"*, −15 — reached independently by two judges: one wrote `next: Node?` from it and terminated a list with a fabricated `fail("no_next", …)`, the other measured that `null` is already caught loudly, so the sentence was redundant *and* the misleading invitation. Four sentences land: `ok(v)` named (closure list, and its absence left `return x` in a `-> int?` function with a diagnostic carrying no note and no fix), `str` *indexed and measured in bytes* (the only genuinely silent one), the print row admitting `inf`/`-inf`/`nan`, and one word — *integer* division by zero | **the abort proposal that would have closed four silences at once was struck by three judges from three grounds, and the ffi-pragmatist compiled the counter-example in Heroes itself**: squaring a large value eleven times reaches `inf` with no division in the program, and the spec's own FFI example `extern function sqrt(x: f64) -> f64` returns NaN for negative input — so aborting makes NaN reachable *only through the door the compiler cannot check*. Zig shipped that abort and moved off it; Python traps and NumPy overrode it. The historian predicted the package would be purely additive, as every specification budget in its sample was except Oberon's, which held by removing — and this commit falsifies that prediction on purpose | §1.6, §4.6, §4.14, §4.20, spec lines 50/56/134/155 | 035, 012, 023, 030 |
2026-08-12 | **`main` may not declare a result, and the reason is that it could fail in total silence.** `function main() -> int?` returning `fail(…)` **printed nothing and exited 0** — no diagnostic, no output, and the one thing a shell can read saying the program worked. Panel 030 had reported the weaker half (a `main` that prints its error still exits 0); this is a `main` that prints none. Refused in the checker as `main_returns`, **zero spec tokens**, until M7 decides `exit(code)` — which is what a fallible `main` would have to mean | five languages, one direction, none back: C99 fixed falling off `main`'s end, Haskell treats any other failure as `exitFailure`, Rust shipped `?`-in-`main` at 1.26, Python exits 1 on an uncaught exception — and **Node.js is this exact situation**, warning and exiting 0 from 2016 until Node 15 made it fatal in 2020, four years that ended when CI could not see the error | §4.2, ROADMAP M18 | 035, 030 |
2026-08-12 | **Four live defects found beside the questions rather than in them.** `%` on two `f64` reached clang as C's `%` on two `double`s — **exit 2 on a program spec line 139 explicitly allows** — now `fmod`, which truncates toward zero exactly as line 135 already requires, so the C function and the sentence agree without either changing. A `match` as the inline body of another `match`'s arm, **open since M4**, was one accidental production: after the inner match consumed its `Dedent` the postfix loop read the next arm's leading `.` as a field, so the error landed on a line nobody had written wrong — five lines, and the port has 45 sites that want it. The build cache carried `heroes::VERSION`, which does not move when the emitter does, so **rebuilding the compiler reused the previous one's artifacts** — two debugging detours in one milestone were stale output read as real. And `hero_str_from_bytes`'s new UTF-8 validation aborted on `getenv` of a Latin-1 value, on a PNG, and on a SQLite column another program wrote: `hero_utf8_valid` is exported so a binding can branch instead of dying | the last is the sharpest, because it was **this morning's own fix**: validation was right and its *shape* was wrong — an abort on bytes the environment chose rather than bytes the program chose, which is a different thing from `to_int` aborting on a value the program computed | §4.14, §4.19, §4.20, §3.1 | 035 |
2026-08-12 | **Milestone identifiers become names, and the algorithm that assigns them is CLAUDE.md §14.** `M8a` is `M-module-namespace`, `M7` is `M-ffi-ladder`, `M8c` is `M-selfhost-fixpoint`; two words, naming the deliverable and never the area, so the area stays free for the second milestone that touches it — `M-module-namespace` and `M-separate-compilation` are both about modules, and the trap had already closed once. **The record is not rewritten**: `docs/panel/`, this file, `docs/journal/`, `docs/measurements/`, `docs/defects/`, `docs/book/beats.md`, `tests/golden/`, every commit subject and the twelve tags `m0`–`m6` and `m8a` keep the numbers they were written with, and `docs/ROADMAP.md` § The names resolves them. A numbered RFC/PEP-style series (`HEP-001`) — the scheme R7 cited approvingly — was evaluated and **refused**: the event that broke this project's numbering was a **reorder**, not an insertion (panel 030 moved M8a ahead of M7), and gapped numbering covers insertions only, while nine of sixteen rows carry `scheduled, no warrant` and may be reordered again | panel 030 R7 refused *renumbering*, where the old id still resolves to the wrong thing silently; a rename into a disjoint namespace fails **loudly** and cannot be handed to a different milestone later. R7's condition — the explicit execution-order table — survives; what goes is the nine-line paragraph above it, because a name makes no claim about position, retiring R7's own objection that this is "a rule a reader has to be told twice" | §14, Part 10 | 030 (amended) |
2026-08-12 | **The FFI emits `<header.h>` and never `"header.h"`, and every `extern` carries a `_Generic` `_Static_assert` on its return type.** Compiled, not argued: with a decoy `sqlite3.h` beside the generated unit, the quoted form read the decoy with **zero diagnostics under `-Weverything`** — panel 020's decoy-`runtime/` finding relocated to a header, where the `_Static_assert(HERO_RUNTIME_ABI)` that rescued it cannot be written. And §4.19's own sentence was false: include-only emission (Nim's `importc`) makes `extern function sqrt(x: f64) -> int` compile clean and print `1`, four wrong bindings out of six passing in silence. The assertion's controlling expression is a call C11 6.5.1.1p3 does not evaluate but does type-check, so it costs one line per `extern` and nothing at runtime — 11 of 11 correct ladder bindings pass, `size_t` turns loud | a mechanism whose guarantee is false for the pair its own second rung uses is worse than no mechanism, because the reader trusts it | §4.19, §3.1 | 036 |
2026-08-12 | **File I/O, `args()` and `exit(code)` are named by the spec and delivered as Tier 2 over a `hero_os.h`, not as built-ins.** Three judges against one: a `T?` is a per-translation-unit generated struct the C runtime cannot name, so `read_file -> str?` needs a second `print`-shaped emitter arm and an ABI bump (§1.7's mechanical test); Pascal predeclared all three of these and Kernighan's 1981 paper itemises all three as defects, and Oberon predefines none of them. The one vote for built-ins was the reader's and it decided the *spec* half: file I/O through the FFI as specified is impossible rather than awkward — no buffer type, no allocation, no `cstr`→`str` — so the four names and signatures are in the prompt and the route is not | the spec says what a reader must know; where the code lives is not something a reader can observe, and Odin's `core:os` redesign was possible only because it was a package | §1.11, §4.20, §1.7 | 036 |
2026-08-12 | **`ptr` gets exactly one literal, `null`, and a C out-parameter is an `@` parameter.** The llm-ergonomist could not write the milestone's own acceptance program: `sqlite3_open(path, &db)` needs a `ptr` value and the language produces none — no null, no address-of, no allocation, and every binding is initialised. CLAUDE.md §7 already makes an `@` parameter a pointer parameter, so the out-parameter half costs nothing new | an acceptance test that cannot be written is a milestone that cannot be finished, and the gap was invisible to everyone who already knew the compiler | §4.19, §4.8 | 036 |
2026-08-12 | **A mortgage is registered as a floor, or as an interval with the drafted text attached and measured — never as a bare point estimate.** Panel 030 R3 pre-registered ≥60 tokens, point estimate 84, and the first draft measured exactly 84; the spec-warden refused to score it, having moved the same four names between +73 and +84 by prose alone and the FFI half between +63 and +114. Panel 030 R3 is scored *floor met · point estimate withdrawn* | a quote a rewrite can move by 13% is a prediction about drafting style, not about the language, and a ledger that accepts it will read as calibrated while measuring nothing | §1.6, §1.2 | 036, 024 |
2026-08-12 | **The closure list is missing a row: the self-hosted compiler must spawn clang.** `int64_t system(const char *)` is `conflicting types for 'system'` — panel 030 R3's wall on a row nobody had quoted, found because the audit that built the list is mechanical and structurally cannot see what is absent from it. Either M-selfhost-probe prices the spawn, or the record states plainly that v1's self-hosted compiler is `--emit-c`-only and §10's `build`/`run` die with the bootstrap | a list that can only audit its own entries is not a closure list until something outside it has looked | §1.0, §3.1 | 036 |
2026-08-12 | **`ptr`'s literal is `nullptr`, not `null`, and the registry's message now names it.** Panel 036 rider 7 said `null`; `measure::spec`'s gate — which asserts the spec never uses a word the lexer rejects — turned red, because `null` is in the foreign-word registry, answered with *"there is no null in this language — absence is a fallible type: `int?`"*. The rule is right and stays; the **message** was wrong at a `ptr` site, sending a binding author to `int?` when no C out-parameter can be one | five judges read the proposal and none saw the collision, because none of them is a program that checks the spec against the lexer — which is what §1.6's instruments are for | §4.19, §1.6 | 036 |
2026-08-12 | **The reserved bitwise set moves from a spec sentence to a diagnostic, and `<<`/`>>` are lexed rather than left to the parser.** `6 & 3` used to say `` `&` is not part of the language's syntax (ASCII-only) `` — false, since `&` *is* ASCII, and the parenthetical sent the reader hunting for an encoding fault — while `1 << 2` said "expected an expression, found `<`", which names neither the set nor the reason. Both are now `reserved_operator`, naming all six spellings; `|` is reported by the parser, where an operator would have gone, because it is a real token in a `match` pattern. Pays for the spec's deleted sentence (−16) | panel 035's mechanism reused: a deletion is affordable exactly when the compiler says the sentence better than the spec did, and it was not saying it at all for four of the six | §4.14, §1.6, CLAUDE.md §8 | 036 |
2026-08-12 | **`not_fallible` carries a `certain` fix, so `--apply` can do what the message asks.** The message said "remove the `?`" while carrying no `Fix`, which is the same edit asked for twice; the fix deletes the span between the operand's end and the expression's, both extents taken from the expression in hand rather than from the `?` being the last character. Pays for the spec's deleted sentence (−15) | CLAUDE.md §8 makes a machine-applicable fix the difference between a diagnostic and a round-trip; the spec line was affordable only once the compiler could close the loop | §4.6, §4.17, CLAUDE.md §8 | 036 |
2026-08-12 | **The return-type assertion accepts every signed integer plus every unsigned one narrower than 64 bits, and the controlling expression is `+(call)`.** Found by binding libcurl (author instruction, ladder rung 4): `CURLcode` is an `enum`, `_Generic` selects on an enum's own type rather than on `int`, and the first version of the macro therefore refused **every enum-returning C function in existence** — most of libcurl, OpenSSL and raylib. Invisible against SQLite, which returns plain `int`. Unary `+` applies the integer promotions; `CURLcode` promotes to `unsigned int`, which fits `int64_t` without losing a value, while `size_t` stays unsigned 64-bit and keeps firing | a mechanism verified against one library is verified against one library, and the second rung of the ladder found in one compile what no reading of the C standard had | §4.19, §4.3 | 036 |
2026-08-12 | **`to_str` gains a `cstr` row, and it is the FFI boundary's return path.** `.cstr()` lends a `str` to C; `to_str(c)` copies one back through `hero_str_from_cstr`. §4.20 says those primitives exist for exactly this — *"without them no `extern function` may return `str` and §4.19's ladder is unwritable at step 3, read a result, because every C library returns strings as borrowed pointers"* — and panel 036's ffi-pragmatist predicted the need before the ladder reached a library that returns one. Zero spec tokens: a fifth entry point under a name the spec already lists, rather than a conversion of its own | the boundary has two directions and only one of them had a spelling; a new name would have cost tokens for a shape the three existing conversions already teach | §4.19, §4.20 | 036 |
2026-08-12 | **`ok`'s arity is derived from its payload, so `ok()` is how a `()?` succeeds.** Before this there was **no way to construct one**: `ok(())` is `expected_expression` (`()` is a type, not a value), `ok()` was `wrong_arity`, a bare `return` is `missing_value` — three spellings, three refusals, and `write_file(path, text) -> ()?` is the first signature in the language that needs one. Panel 036's spec-warden predicted the failure before the signature existed | a type the language can write but no expression can produce is a hole that only appears when something needs it, which is why it survived six milestones | §4.6, §4.3 | 036 |
2026-08-12 | **CLAUDE.md §7 gains its one named exception: a clang failure that is the author's.** The `_Generic` return assertion emitted per `extern` exists to fail when the author's declaration disagrees with the real header, so reporting it as an internal error printed C the author never wrote, named a file under `build/<hash>/`, and blamed the compiler for a mistake in a `.hero` file. It is now exit 1 and `ffi_return_type` on the `.hero` line; `emit/ffi.rs` matches only the assertion messages this emitter writes, and every other clang verdict is still the compiler's | §7's rule was written when every line of the translation unit came from the emitter, and M-ffi-ladder is the milestone that made that false — the exception is named rather than the rule loosened | CLAUDE.md §7, §4.19 | 036 |
2026-08-12 | **Panels 035 and 036 ratified by blanket author instruction, and the shape of the yes is recorded with it.** One instruction after a session summary, not a clause-by-clause reading. It settles the provisional resolutions as decisions; it settles nothing keyed to a measurement nobody has taken — panel 036's four llm-ergonomist first-try rates, the historian's five-year condition on built-in file reading, and the two deferrals (`compile`, `framework`) that wait on an acceptance test rather than on a verdict. **The 39 `# UNVERIFIED` adversarial goldens stay marked**: those are verifications, not decisions, and a blanket yes cannot make a reading happen | the project's own precedent (panels 013, 019): a record must not say more than what happened, and recording one yes as thirty-nine separate ones is the mistake the rule exists to prevent | — | 035, 036 |
2026-08-12 | **CLAUDE.md §13's non-goal is read as "never a reason", not "slowness is fine".** Author instruction: *"le prestazioni non sono un goal ma non devono essere nemmeno un limite"*. The rule forbids reaching for speed as a **justification**; it does not make a cost that stops a closure-list program from running acceptable as a **ceiling** — where it does, that is §1.0 compiler-need and the panel decides it. The live case is `push`: re-measured 2026-08-12 at 50 000 pushes 2.0 s and 100 000 pushes 7.8 s, twice the size for four times the time, in a compiler that will be written in arrays | the non-goal had been read as a prohibition on ever making anything fast, which turns a rule against motivated reasoning into a rule against noticing | §13, §1.0, §4.10 | 028 pending |
2026-08-12 | **The closure list gains a fifteenth row: spawning a process.** Author instruction — *the self-hosted compiler must be complete* — so the `--emit-c`-only v1 is refused. After the archive `heroes build` and `heroes run` are Heroes programs that invoke clang, and `int64_t system(const char *)` is `conflicting types for 'system'`. M-selfhost-probe prices it; the shape is `hero_spawn` in `hero_os.h` beside the file and argument rows | measurement 003's audit is mechanical, so it can confirm the list and can never find what is missing from it — the row was found by a judge compiling something else | §1.0, §4.19 | 036 |
2026-08-12 | **`exit` keeps its leak-gate exemption, and the corpus keeps a path that does not use it.** A program asking to stop now is not asking for an audit, and running the gate at `exit` would report a leak for every live value in every frame the exit unwinds past — but `hero_runtime_check_leaks` is the only leak instrument on Darwin arm64, so coverage is bought back in M-program-corpus rather than by making `exit` louder | the honest shape for an exemption is to name what it costs and pay for it somewhere else, not to argue it costs nothing | §4.20, ROADMAP M-program-corpus | 036 |
2026-08-12 | **`heroes run f.hero -- a b` passes arguments to the program.** A separator rather than a trailing operand: `heroes run f.hero -o x` would otherwise be unreadable and a program taking `-o` could never be run. `--` anywhere but `run` is a named refusal, because nothing else executes a program to forward to | §10's stopping rule asks whether an invocation must type it; the corpus's programs take `args()`, and a capability the language delivers that the tool cannot reach is a capability nobody can test | CLAUDE.md §10 | 036 |
2026-08-12 | **The Part 11 harness runs at M-program-corpus, not before the port.** It has not run since M-strings-ownership, and four panel predictions, `outline`/`explain`'s admissibility and panel 036's four first-try rates wait on it — but the corpus is what it measures, and a bigger corpus is a better number | a measurement scheduled where its subject is smallest buys a number nobody will trust twice | Part 11, ROADMAP M-program-corpus | 035, 036 |
2026-08-12 | **In-place `push` at refcount 1 is refused: it fires nowhere, and where it fires it corrupts.** Implemented verbatim by two judges independently. The gate never opens — `own.rs` rule 5 gives a call's result a synthetic owning slot while the named slot increfs, so every accumulator push sees refcount **2**: 50 000 of 50 000, and the curve is unchanged (9.27 s vs 8.33 s). Where it does open it is wrong: an array held only by an enclosing array's element slot has refcount 1 and a live observer, so `grid[0]` grows behind `grid`'s back at exit 0, ASan silent, leak counter green and **68 of 68 goldens byte-identical**. The sound form is a **place store** — `p @ push(p, v)` recognised at lowering, uniqueness taken from the place rather than guessed from a count, which is the shape `hero_map_set` already has: 1 000 000 pushes in **0.004 s** against 806 s. It **waits** for M-selfhost-probe, because chunked accumulation compiles today and finishes in 0.72 s | §4.10's refcount rule is written for *mutation sites*, and `push` is an expression — the proposal read a licence for one thing as a licence for another, and the corpus could not see the difference | §4.10, §4.20, CLAUDE.md §7 | 037 |
2026-08-12 | **design.md §4.10's own remedy was incomplete, and Part 8 wart 8 repeated it.** `join` was funded to remove O(n²) string building, and `join`'s argument is a `[str]` built with `push` — which is O(n²) too, so the remedy is reached through exactly the cost it removes. Corrected in place, with the measurement and the working answer (chunked accumulation, 0.5 s against 403 s on a port-scale output) | two judges found it from opposite directions while answering something else, which is the third time this record has been corrected by someone reading it rather than testing it | §4.10, Part 8 wart 8 | 037 |
2026-08-12 | **`/panel` gains a soundness lane: compiler-engineer and ffi-pragmatist only, for a proposal with no surface, no diagnostic and no spec token.** Panel 037 cost thirty minutes and four hand-built runtimes for a question two judges answered identically, and the author stopped it mid-flight. The lane's cost is stated with it: run full, that same session produced the llm-ergonomist's finding that the spec's only cost sentence implies the quadratic answer — which the lane would have lost. When in doubt, full panel | an instrument with one gear gets reached for at the wrong speed and then not reached for at all; naming the cheap lane is what keeps the expensive one available | CLAUDE.md §4 | 037 |
2026-08-12 | **Two refusals made honest.** `1e300` lexed as `1` followed by the identifier `e300`, so the reader was told `expected ')' … found a name (e300)` — a message about a parenthesis, for a number; it is now `exponent_literal` at the digits themselves, testing the characters in hand rather than what the parser expected. And `INT64_MIN % -1` no longer aborts with `integer overflow`: the remainder is 0 and overflows nothing, and what has no `int64` is the quotient C reaches it through. The guard is unchanged — arm64 traps on neither, so both stay UB the emitter refuses (CLAUDE.md §7) | the adversarial case written for exactly this had been asserting the misleading message since the day it was written, which is what a golden does when the code and the case agree and both are wrong | §4.3, §4.17, CLAUDE.md §8 | 035 |
2026-08-12 | **`/debrief` is split in two and the teaching half is renamed `/learn`, called only when the author asks.** Author instruction: one session was putting *"what does this teach you"* and *"the compiler is waiting on your answer"* in the same room, and they have opposite clocks — the second always wins. `/unblock` takes the decisions (fast, verified against the repository before asking, every answer applied in the same session); `/learn` takes the comprehension and is **never convened by the assistant** — not at a milestone close, not when the queue grows. The queue keeps its path, because every commit subject cites it and §14 does not rewrite a record | a skill that mixes urgency with curiosity teaches nothing and decides slowly, and the author noticed before the record did | CLAUDE.md §3, §9 | — |
2026-08-12 | **A `/learn` question arrives with the code on screen and a preamble long enough to make it answerable.** Author instruction, and it overrides the terser habit that came before: a queue entry is a note written by somebody who had the file open, and read out loud it is a riddle — *"which field decides it, and where is it computed?"* is a memory test on something never learnt. The setup is now: where we are, why the piece exists at all, the code pasted rather than cited, what the alternatives were, then one question with lettered options. The preamble may run twenty lines; the one thing it may never contain is the answer | retrieval is preserved by withholding the answer, not by withholding the question's subject — those had been confused | CLAUDE.md §3 | — |
2026-08-12 | **The runtime has a single allocation point, `runtime/parts/alloc.c`, and §4.20's sentence is corrected rather than left claiming it already had one.** The document said *"literally one `malloc` and one `free`, both inside the `str` primitives"*; there were **eleven calls across five files** — `str`, the array, the map, `sort`'s scratch buffer, and the file reader this milestone added. Two pairs, not one: `hero_alloc_block`/`hero_release_block` count what the language owns and feed the leak gate, `hero_alloc`/`hero_release` do not, because a scratch buffer freed inside one runtime call is not a leak and counting it invites a report of one that is not | Part 7.13's per-thread heaps need one place to change, and the reason the claim survived is that it was the *design* — a sentence describing what should be true reads as true until somebody greps | §4.20, Part 7.13 | — |
2026-08-12 | **The premise the place store rests on is now a test: a slice never shares a buffer with its source.** True today — `hero_array_slice` and `hero_str_slice` both allocate and copy — and nothing said so anywhere. Panel 037's historian found that every recorded corruption case under value semantics needs the same shape, *a second header over one buffer with its own length*: D's stomping and `assumeSafeAppend`, V's issues #119 and #6569. Heroes has none, which is exactly why the place store is sound; the day `slice` returns a view, that argument dies silently and this case is what says so | CLAUDE.md §11: where a premise is unavoidable, it owes a falsifiable claim and a test whose failure names what depends on it | §4.10, §4.20 | 037 |
2026-08-12 | **The milestone-name invariant walked into a nested git worktree and reported another tree's names as this one's.** `.claude/worktrees/<name>/` is a whole second checkout at some other commit; the walk found three aliases that do not exist here and five numbered milestones retired here. The names were real, the tree was not ours | a file-tree invariant is only as good as its idea of which tree it is in, and "everything under the repository root" stops being that the moment a checkout is nested inside another | CLAUDE.md §14 | — |
2026-08-12 | **An out-of-range `int` literal is the checker's, and `heroes check` had been exiting 0 on a program `build` refused.** The class landed in lowering, where the value is decoded — so `check`, whose whole job is *does this program have diagnostics*, never got that far and returned **0** on `print(99999999999999999999)` while `build` returned 1 on the same bytes. That is what an editor and `--apply` trust, and §10's exit-code contract says it cannot happen. The literal's text and *`int` is the only integer type* are both frontend facts. Lowering keeps its copy as a net and a test asserts it is unreachable rather than absent — a net that has never fired is indistinguishable from no net. And the code is now `int_out_of_range`: `int-literal-out-of-range` was the only hyphenated one among fifty | a diagnostic in the wrong pass is not merely late, it is invisible to the command whose contract is that it reports everything | §4.3, CLAUDE.md §10, §8 | 019 |
2026-08-12 | **CLAUDE.md §7 no longer credits the ABI stamp with catching a decoy runtime, because it does not.** A decoy that copies `HERO_RUNTIME_ABI` passes the `_Static_assert`; what protects a build against one is the **cache key**, which covers the whole runtime's contents. Panel 034 R5 named it and panel 037 measured it from the other side: two runtimes with changed *behaviour* kept the number at 10 and every generated translation unit accepted them. The stamp's real job is version skew, which the runtime search makes possible, and it is worth keeping for that | the sentence had been read as a proof for four milestones, and it was describing an intention — the same class as §4.20's single allocation point, found in the same sweep | CLAUDE.md §7 | 034, 037 |
2026-08-12 | **The queue splits into three lists, one per kind of item, and each skill is named after the list it reads.** One file held **192 open items** across three kinds — what is true, what should be true, and work with a home — and a list you have to filter before you can read it is a list nobody opens. `LEARN.md` (162) · `DECIDE.md` (20) · `SCHEDULED.md` (7); `QUEUE.md` keeps every ticked item and the index, because every commit subject cites that path (§14). The split was mechanical over the items' own text with **twenty assigned by hand**, and that map is in the commit so a misfiling is visible rather than silent — 282 items before, 282 after | a skill whose name does not match its list is one more thing to remember, and `/unblock` reading `DECIDE.md` was exactly that | CLAUDE.md §3, §14 | — |
2026-08-12 | **There is deliberately no `/scheduled` skill; `/step` § 1 is obliged to read the list instead.** A third verb reading a file `/step` already has to open when it enters a milestone answers no question the two existing ones do not — CLAUDE.md §10's stopping rule, applied to the skills instead of the CLI. What the list needed was not a caller of its own but one line in the protocol that opens it: a list nobody is *obliged* to read is write-only, and the obligation is cheaper than a command | the symmetry of three lists and three skills is the argument for the third one, and symmetry is not a warrant — it is the shape a surface grows into when nobody asks what a new verb answers | CLAUDE.md §10, §3 | — |
2026-08-12 | **A `constant` inside an `extern` group takes its value from the header, and the number leaves the file.** Panel 018 committed this spelling *"at the FFI milestone at the latest"* and M-ffi-ladder closed without it; its prediction 4 — *"the SQLite binding needs an extern-global line"* — came true and was paid in five hand-copied literals, unscored. Measured first (`docs/measurements/005`): a twelfth mutation operator moves one digit of a `constant`'s value, and **5 of 5 sites survive**, all five owned by a C header. The mechanism is the existing `HERO_RET_*` assertion plus `__builtin_constant_p` for constancy, so `stdout` and `errno` are refused rather than smuggled in as mutable globals — which no shipped language does as a language rule. The accessor is **mangled**, §7's first named exception to unmangled `extern` names: a linker name must survive, a preprocessor name must not appear | the value was never the meaning, and a literal that is not in the file cannot be wrong; the spec-warden's objection stands at 5 of 267 and the author's instruction carries the decision | §4.19, §4.2, CLAUDE.md §7 | 038 |
2026-08-12 | **§4.19's claim that `#define` constants are reachable was false, and is repaired where it is made true.** *"Macros, `inline` functions and `#define` constants are reachable because the C compiler sees the real header"* is true of the C compiler and was false of the Heroes author, who had no form to name one — the same shape as panel 036 rider 3, one milestone later, in the same section | a document that promises a capability nobody can spell teaches the reader to hand-copy, which is the mistake the section exists to prevent | §4.19 | 038 |
2026-08-12 | **The spec's own FFI example taught a program that silently does nothing.** `function sqlite3_open(path: cstr, out: ptr)` contradicted the rule four lines below it (*"a C out-parameter is an `@` parameter"*), on the most-copied line in the document. Copied verbatim it compiles, links, runs and **exits 0** printing `rc: 21` — `SQLITE_MISUSE`, no database opened, nothing said. Found by the llm-ergonomist, which reads only the spec, while it was being asked about something else; repaired at **+1 token** | the redundancy between an example and the rule beside it is only worth its tokens if something checks that they agree, and nothing did | spec § FFI | 038 |
2026-08-12 | **`emit/ffi.rs` was matching clang's echo of the source line, so §7's narrowness claim was already false.** A misspelled `extern` name reported `` does not return `int");` `` — the garbage came from the echoed line, which repeats the assertion's own message, and the diagnosis was wrong anyway: the name does not exist, and correcting the result type cannot fix that. The good path was right by *ordering*, not by matching. Now a line must also say `static assertion failed`, and an undeclared name an `extern` group declares becomes `ffi_unknown_name`, carrying clang's typo correction as a `guess` | a premise about the order of another tool's output, doing the work of a fact about the line in hand (CLAUDE.md §11) — and the class it mis-reported is the dominant FFI mistake once names outnumber signatures | §4.19, CLAUDE.md §7, §8 | 038 |
2026-08-12 | **`docs/reasoning/` is adopted, its founding branch merged, and the branch deleted** — closing the one decision that had been left open by a note written into a directory that did not exist. Author instruction, arriving as a repository cleanup ("rimuovi tutti i branch tranne main"): the notes are recovered first, so the class is adopted by decision rather than by writing. What merged is the README, notes `000`–`002`, CLAUDE.md §14's per-session duty and this log's 2026-08-10 entry; what did **not** merge is the branch's hand-off commit, whose four queue items and three watch-list entries were **each verified as already done on `main`, independently and better** — K's superlative now sourced to kdb+'s STAC-M3 results, the memory-model rejection table written, the non-atomic constraint stated with its thread condition, Part 7.13 shaped, both future-proofing rules landed. Three append-at-EOF conflicts resolved in date order, not at EOF; the three dated notes were **not** rewritten to the current vocabulary — the README gained a section mapping `/debrief` and `M5a` forward instead | a branch nobody merges is a decision nobody makes, and this one had been costing a numbering hole (`003` beside no `000`) plus an open item that re-measured its own distance from `main` every time it was read. CLAUDE.md §4 makes the teaching process amendable by instruction, so no panel; CLAUDE.md §14 makes a dated record appendable and never rewritten, which is why the repairs owed landed in the README and not in the notes | CLAUDE.md §14, §4, §11 | — |
2026-08-12 | **Metric 3's instrument stops flattering itself, and starts being attributable** (author decisions, `/decide`). Two decisions on one harness. **The corpus must compile**: `heroes mutate` checks every program before mutating anything and refuses a corpus it cannot use with exit 2, naming every offending file — the flattering direction was the live one, since `crates/heroes/src/library/` reported 77 sites and 100% killed while `heroes check` refused that same corpus, every mutant killed by a diagnostic that was already there. The check reuses `fate`, so the verdict on a base program and the verdict on its mutants are literally the same function. **And a rate is now attributable**: `--survivors` prints each accepted mutant as its site and its two lines, `--operator` narrows to one of the twelve, and the site is derived from the two texts rather than threaded out of the edits | measurement 002 recorded two per-operator rates that moved between runs and **declined to explain either**, because attributing a delta needs the mutants behind it — CLAUDE.md §12 says measurement beats opinion, and a number nobody can attribute is an opinion with a decimal point. The flag paid for itself on its first run: `swap-args` survives mostly on `fail("code", "msg")`, two `str` arguments in a position where the language's own same-type rule would demand names if `fail` were an ordinary function | Part 11, CLAUDE.md §9, §10, §12 | 011, 038 |
2026-08-12 | **A `constant` may no longer be defined in terms of itself, and the check had to be about the cycle rather than about constants** (author decision, `/decide`; reasoning note 003 found it). `constant A: int` / `B` beside `constant B: int` / `A` used to pass every gate: `check` 0, `build` 0, and a binary that **SIGSEGVs at -O0 and hangs at -O2** — one program with two failure modes chosen by a flag nobody typed. `resolve/cycles.rs` is an iterative depth-first search over "this body reads that declaration", over **every** top-level declaration, reporting a cycle only where a `constant` with a body sits on it. `constant_cycle` is not a thesis rule and both metric-3 arms keep it: rejecting a definition that has no value is what every language does, so it belongs in the control arm too | the filter is on the cycle and not on the edges because the defect escapes: `constant A` with body `f()`, where `function f() -> int` returns `A`, was accepted for the same reason and is the same defect wearing a function. Restricting the graph to constants would have fixed the reported instance and left the class. The converse rule is what keeps it honest — a cycle of functions alone is legal, because §4.2 grants mutual recursion and a function has arguments and a base case where a constant's accessor has neither, so the golden carries `even`/`odd` and a legal `START`/`CHAIN` chain as its control. The walk is iterative for `sized.rs`'s reason: rustc #84611, Nim #13715 and Zig #21436 are all crashes *inside* a cycle check. **A diagnostic class is a CLAUDE.md §4 panel path and this one landed on author instruction instead**, which is recorded rather than glossed: it is the value-side twin of `no_size`, same walk, same shape of message, and the instruction was given with the measurement on screen. No spec sentence was bought — the diagnostic says it better than a spec line would (panel 035's mechanism, run in the other direction) | §4.2, §4.17, CLAUDE.md §8, §9 | 023, 038 |
2026-08-12 | **Panels 037 and 038 ratified by author decision in `/decide`, one component at a time, and the shape of this yes is different from August's blanket one.** Both files gain a § Ratification saying which it was. 037 in block: the proposal refused on two compiled vetoes, the place store adopted as the shape and **deferred to M-selfhost-probe**, §4.10's `join` gap corrected, the soundness lane — and the queued spec clause **adopted**, the one component the ratification changed rather than confirmed. 038 in block, with the spec-warden's objection left standing: 267 int-literal sites, five reached, and what carries the decision is the instruction, not a verdict. **The spec is at 2627, +39**: *"Build a long string with `join`, not repeated `+`, and a long array in chunks: each `+` copies both sides and each `push` copies the array."* Two further closures with it — the ABI stamp is **not** extended to behaviour (the cache key already hashes the runtime's whole contents, so a second guard over the same bytes buys nothing), and the subdirectory question's deferral to M-package-manager is ratified, which moves it out of the decisions and into the scheduled work | the clause is the ergonomist's remedy at nearly twice its price, and the extra 16 tokens are the honest half: told to build the `str` with `join` and left to build the `[str]` with `push`, a reader moves the quadratic cost instead of removing it, which is what §4.10:1358 already said and the spec did not. It honours the ergonomist's pre-veto — no copy-on-write is explained, so no line's cost depends on whether some other binding is live fifty lines away. Above the soft 2000 with no removal available, so panel 012's other branch: the prediction is written into the panel file and scored at M-program-corpus — *no program in the next spec-only experiment builds a long string by repeated `+` in a loop*. And the clause is **dated**, which the panel file says and the spec does not: if M-selfhost-probe measures a need and the place store lands, this sentence becomes false, and CLAUDE.md §12 makes a false spec the compiler's bug | §1.6, §4.9, §4.10, CLAUDE.md §7, §12 | 012, 037, 038 |
2026-08-12 | **`emit/decls.rs` splits along the seam panel 038 predicted, 768 → 493, before the milestone that will read it** (author decision: now, not at M-selfhost-probe). `emit/externs.rs` takes the four walks a group's head line asks for — which headers the unit includes, which libraries the link line names, what the header says a name is, and the `_Static_assert` clang cannot make on its own — and `emit/main.rs` takes the two `int main` shims. Byte-identical output, every golden green | the reason for doing it *now* is the reason the item gave for hesitating: a split touches every caller, and doing it inside a milestone that is also changing behaviour is how a mechanical change hides a real one. So it lands in a commit that changes nothing else, and the double-emit determinism test plus 13 golden harnesses are what makes "mechanical" a measurement rather than a claim. Two smaller things fell out: `externs.rs` is the same seam `syntax/externs.rs` cut at panel 036, so one feature is now named the same thing in the two passes that own it — and the **residual is recorded as open, not closed**. 493 is over §11 by itself, the next cut (`emit/skeleton.rs`, the per-function skeleton) is obvious, and it was deliberately not taken because the ratified decision named two files. A mechanical change that grows past its instruction is how the next one stops being reviewable | CLAUDE.md §11, §4.19 | 036, 038 |
2026-08-12 | **Four verdicts that change no code, and each one closes a question by naming what it costs** (author decisions, `/decide`). **No global line budget** for the compiler (design.md §1.6): measured at 25,482 non-test lines in `crates/heroes/src` against Pascal-P4's ~4,000, and refused because the bootstrap is scaffolding with an expiry date — a number constraining it would constrain the artifact about to be archived, and "does it fit one person" is a question about the *Heroes* compiler that M-selfhost-probe answers first. **Metric 2 waits for v1**, both offered protocols refused. **Variant syntax becomes symmetric in v2** rather than the spec gaining an EBNF appendix (Part 7 item 16). **`print`'s separator gets no hedge**, measured at +17 for a sentence that refuses no program | the pattern across all four is that the cost is written down where the decision is, not left to be rediscovered. Metric 2's refusal has the sharpest consequence and it is now in `SCHEDULED.md`: the pre-amendment numbers are permanently unrecoverable, and **panel 018's colon question loses its designated tiebreaker**, so bare headers stand indefinitely as the answer by default rather than by measurement — which is a real thing to have decided, and would have been invisible if the item had simply been ticked. Part 7 item 16 does the same: construction stays unspecified until v2, so a program written from the spec alone cannot build a variant, and Part 11 should attribute that failure to the deferral rather than to the writer | §1.6, Part 7, Part 11, CLAUDE.md §11 | 011, 012, 018, 020, 035 |
2026-08-12 | **The optimisation level becomes a flag, closing panel 021's recorded asymmetry** (author decision, `/decide`). `-O0`/`-O2` on `build`, `run` and `test`, each verb keeping its default and the flag overriding it. Spelled attached, as clang spells it. **Two levels and no others** — §10's stopping rule applied to a flag rather than to a verb: the `run/` golden harness types both (every case runs at `-O0` and `-O2`) and nothing types `-O1`, `-O3` or `-Os`, so `-O1` is a strictness error that names what is accepted. Both at once is refused rather than resolved by precedence, which is the reading `build` already gives `--dump-ir --emit-c` | panel 021 left this open on the right ground and the ground moved. Its objection was that unifying is *a larger surface nothing must type* — true of a general `-O <n>`, and false of the two levels the harness runs every golden at, which is the distinction §10's rule is made of. The asymmetry it recorded was real and visible in `--help`: sanitising was a flag, the level was a subcommand, and `build` therefore could not be asked for the level the harness measures at. A tool-surface change is a §4 panel path and this landed on author instruction, with the precedent named — `heroes run f.hero -- a b` was the same shape ten days earlier | CLAUDE.md §10, §4, §3.5 | 016, 020, 021 |
2026-08-12 | **Panel 039: compile-time evaluation is examined and left *unplaced* — on neither the permanent-rejection list nor the deferred one — and Part 6's `Macros` row is repaired rather than extended.** Five judges, and **no option survived**: A refused by the spec-warden at veto grade, B vetoed by the compiler-engineer, C objected to by the ffi-pragmatist and the historian. The row now names what it actually rejects (user code that expands into other code, or extends the syntax), and a greppable paragraph states that comptime was examined, that nothing on the closure list needs it, and the three conditions that return it jointly: a measured Part 11 effect `extern constant` does not already close · a **structural** termination argument of `mono.rs:24-33`'s standard, never a quota · a named deletion, because §1.7's test is subtraction | four seats agreed by four methods that nothing needs it, and the two that decided the *wording* were the blind one and the sourced one. The historian sourced the fusion claim to its author and found it runs the other way — Zig has comptime *so that* macros are unnecessary — so "the code you read is not the code that runs" is false of `constant MAX: int / 10000 + 2` and true of `mixin`. The warden refuted a Part 6 row with a compiler fact rather than an argument: every emitted unit carries two `_Static_assert(__builtin_constant_p(…))` even with no `extern` in the program, so the one-line form Part 6 admits is falsified by this compiler's own output. The engineer vetoed Part 7 because its preamble asserts its items lose *only* on simplicity, false of a pass that duplicates 1,754 lines of runtime semantics with no `_Static_assert` between the copies. And the termination fork closed both ways: structural ⇒ folding only ⇒ zero benefit and §13 forbids speed as a reason; useful power ⇒ a quota ⇒ contradicts a refusal this compiler already enforces (sourced: Rust shipped a bound and removed it in 1.72; Zig's is bypassed by generic types into a compiler segfault; D's ate >16GB on one `std.regex` pattern) | Part 6, Part 7, §1.0, §1.7, §4.19, CLAUDE.md §1 | 039 |
2026-08-12 | **The self-hosted compiler could not have computed its own names, and the closure list gains the row that says so** (author decision, `/decide`; found while verifying a panel 039 item). `emit/mangle.rs::instance` was FNV-1a over the rendered type arguments, carrying the comment *"written out rather than taken from a crate, because the port must reproduce it exactly"* — and every ingredient is inexpressible in Heroes, measured: the offset basis `14695981039346656037` is `int_out_of_range` because `int` is signed, `hash * 1099511628211` is `panic: integer overflow` because §4.3 makes overflow an abort, and `^` is `reserved_operator`. `h_library_map_<typehash>` reaches the generated C of any program that uses a generic, and M-selfhost-fixpoint compares that C byte for byte. Now a polynomial hash modulo 2^31 − 1, and `tests/golden/run/premise-mangler-hash-in-heroes.hero` computes the same four values in Heroes with a Rust test asserting the two sides agree | three alternatives were refused for reasons worth keeping: a runtime primitive would have bumped `HERO_RUNTIME_ABI` for something the language should be able to say; unsigned or wrapping arithmetic is Part 7 item 10, which calls itself *"the highest-risk addition in the language… never in v1"*; and deferring to M-selfhost-probe was the spawn precedent but not the cheap moment — **zero `emit/` goldens carried a hashed name**, so the change moved nothing today and every later milestone would have made it dearer. A collision is loud (two C definitions of one name, clang error, exit 2), which is what makes 31 bits enough. The lesson belongs to the audit rather than to the hash: measurement 003 is mechanical over the closure list, so a requirement *absent* from the list cannot be seen — this is the second instance after `system()` at panel 036, and both times the missing row was something the **compiler itself does**, not something a Heroes program contains | §1.0, §4.3, CLAUDE.md §7, §11 | 029, 036, 039 |
2026-08-12 | **A `constant`'s body may contain no call, no `?` and no `???` — so a `constant` is constant again** (author decision, `/decide`; panel 039's llm-ergonomist vetoed the shape from the spec alone, not knowing it shipped). `constant ARGC: int` / `len(args())` printed `3` and `0` from one binary, and a body calling a printing function printed once per read. The rule is **syntactic on purpose**: a purity analysis would admit `len("abc")` and refuse `len(args())` — a better rule and a whole new pass — while the syntactic one buys the property outright, since a call-free body reads only literals and other constants and the cycle check proves those acyclic, making the value a deterministic function of the program text | measured before implementing rather than after: every `constant` body in the repository was already a literal or arithmetic over constants, so the restriction broke **zero** programs, and the only call in the corpus was a fixture written that morning. Two invariants fell out of the implementation and both got tests. **The outermost offender only** — `len(args())` is two calls and one mistake, and two diagnostics for one mistake is a defect this compiler has fixed three times. And **the cycle diagnostic is suppressed where the body was already refused**: refusing the call does not remove the edge, so both rules find `constant THROUGH` / `depends()`, and the body rule is the primary one because it names the line the author wrote. A variant case stays legal where a record construction does not, which is an asymmetry the rule *inherits* — §4.9 makes record construction a call — and which the author's v2 decision (Part 7 item 16) fixes at the source | §4.2, §4.16, CLAUDE.md §8, §9 | 035, 039 |
2026-08-12 | **The bitwise set becomes real — `& | ^ ~ << >>` on `int`, spec +48 → 2675** (author decision in `/decide`, over the narrower option of a diagnostic; `docs/panel/040` is the retro-record sitting §4 requires). The warrant is a measured silent wrong answer: with no `|`, a flag union had to be spelled `FLAG_A + FLAG_B`, right only while the bits are disjoint, and panel 039 found it twice from opposite directions — the llm-ergonomist wrote the `+` form from the spec alone and flagged its own premise, the ffi-pragmatist compiled the overlapping case (`O_RDWR|O_ACCMODE` is 3 where `+` gives 5; `S_IRWXU|S_IRUSR` is 448 where `+` gives 704). Four rules came with the implementation: `int` only · **`&` binds tighter than `==`**, so C's own recorded mistake is not inherited · `1 << 63` is `INT64_MIN` rather than an abort, because without a reachable sign bit a mask set cannot name its top flag · a shift count outside 0..63 aborts, because C6.5.7p3 makes it undefined and §7 admits no C UB. `reserved_operator` is **deleted** — the one real subtraction, and §9's rule read backwards: a check with nothing left to fire on has no test that makes it fire | five objections are on the record in the panel file rather than dissolved, and two matter. **§4.14's own sentence said "not implemented in v1"** and this overrides it; the deferral was not wrong when made, and what changed is that the FFI arrived and made the workaround silently wrong. And **it does not close the gap it was argued from**: `CURLAUTH_ANY` and `SIZE_MAX` stay unexpressible because `0xFFFFFFFFFFFFFFFF` is `int_out_of_range` whatever the operators are — a type-vocabulary gap that now has its own panel owed, from the author's own question. The sitting's real finding is what the implementation did to itself: `|` became dual-use and **reintroduced the exact class it was made to remove, for about an hour**. A literal pattern was parsed with the full expression parser, so `1 | 2 => "small"` silently became the expression `3` and matched nothing. The premise that had made that code correct — *no binary operator can follow a literal in pattern position* — was never written down, expired silently, and is CLAUDE.md §11's named failure mode for the third time this month | §4.14, §4.7, §1.6, CLAUDE.md §7, §9, §11 | 012, 036, 039, 040 |
2026-08-12 | **A refusal is now held to the same standard as a feature: a Part 6 row must be falsifiable by the compiler** (author decision, `/decide`; panel 039's spec-warden proposed it). The row must name the program or the compiler fact that would make it wrong, so a reader can check it rather than trust it — falsifier-as-program goes in `tests/golden/`, falsifier-as-measurement in `docs/measurements/`. The rule's home is Part 6's own preamble; CLAUDE.md §12 cites it, because it is "measurement beats opinion" applied to a refusal | the asymmetry is real and nothing else covered it: Principle 0 is a burden on what **enters** the language, so a *permanent rejection* was the one design act here subject to no burden of proof — the cheapest possible way to make the most expensive kind of commitment. The rule was proved to have teeth before it was written, twice in one afternoon: a proposed row rejecting compile-time evaluation was refuted by the emitted C of every Heroes program, each carrying two `_Static_assert(__builtin_constant_p(…))` with no `extern` in the source; and the preamble's own claim that *"each violates locality"* turned out **false of four of its own rows**, all cost-only refusals stating as much in their own reasons. Both halves landed together on the author's instruction, and that pairing is the point — a rule about falsifiability that left the document contradicting itself on the day it arrived would have been the very thing it exists to prevent | Part 6, §1.0, §1.1, CLAUDE.md §12 | 039 |
2026-08-12 | **`emit/decls.rs` finishes at 171 lines, from 768, and `resolve/cycles.rs` splits with it** (author decision, `/decide`: take the next cut now). Four files out of the first: `externs.rs` (the group walks and the assertion emitter), `main.rs` (the two shims), `signature.rs` (the prototype and an instance's name) and `body.rs` (the prologue, the blocks, and what is omitted from them). `cycles.rs` 343 → 241 with `constbody.rs` at 119 | the residual turned out to be **three** concerns and not the two the reopened item recorded — the unit's opening, the signature, the body — so it took two cuts; the single `emit/skeleton.rs` the item named would have landed at ~320 and stayed in breach, which is the kind of thing only measuring after the first cut shows. The two `resolve/` halves stay coupled through exactly one value, the set of constants whose bodies were already refused, and that coupling is the reason they were one file to begin with: a cycle through a refused body is a consequence rather than a finding, and reporting both would be two diagnostics for one mistake. Byte-identical emitted C both times, which is what makes "mechanical" a measurement here. **And the standing breach is reopened rather than declared closed**: twenty-two non-test files are over ~300, three of them pushed there by this session's own work (`emit/ops.rs` by the bitwise emission, `compile.rs` by `level_from`, `ir/inst.rs` by five ops), and the biggest — `perfn.rs` at 628 — no decision has ever touched. A rule twenty-two files are in breach of is a preference, and that is now its own queued decision | CLAUDE.md §11, §1.6 | 038 |
2026-08-12 | **Three decisions in the third `/decide` session, and the one that costs something is the cheap question being made to wait.** (1a) **The hexadecimal literal rides in the unsigned panel's sitting** rather than getting its own or waiting for the next FFI rung — so the sitting owed on `u64`/`size_t` now has two questions in its charge, the type vocabulary and the literal notation. (2a) **The §11 breach becomes a standing sweep with its own commit, run to zero**, over a per-milestone obligation in `/step` and over restating the number: the rule keeps saying ~300 and the tree is brought to it, one file per commit, byte-identical emitted C wherever the cut touches `emit/`. (3d) **`// ORDER:` is deferred to M-selfhost-probe** and moved out of the decisions into the scheduled work, over marking the sites now and over turning design.md's sentence into a tested claim | each verdict's cost is written into its ticked item, and two are worth stating here. **1a makes the cheap question hostage to the expensive one**: a mask stays decimal for as long as the panel is unconvened, and the notation gap costs one spec clause with no new type and no conversion rules. **3d leaves a false sentence in design.md**: §4.9's port note names `types/holes.rs` as the bootstrap's one map iteration and that is obsolete — `holes.rs` walks vectors now, and the live walk is `resolve/mod.rs:198` `names_in` over the `BTreeMap top`, reaching *did-you-mean* text and `--dump-scopes`. The correction rides in the scheduled item so the probe does not re-derive it, but until then the document misidentifies the file, which is CLAUDE.md §11's expired-premise class living in design.md instead of in a comment. The session's verification pass earned its keep before a question was put: two citations in the unsigned item were wrong (the two header constants are panel **040**'s, not 039's, and are a judge's compiled reading rather than `ffi_constant_type` transcripts), `0xFFFFFFFFFFFFFFFF` is **not** `int_out_of_range` because Heroes has **no hex literal at all** — a claim panel 040 also makes — and one scheduled item was already done in full | CLAUDE.md §11, §4, §12, §1.6, design.md §4.9, Part 7 item 10 | 022, 040 |
2026-08-12 | **Hex literals and unsigned integers both refused for now, and the sitting's finding is that the cheapest fix is not the one anybody proposed** (`docs/panel/041`, full panel, provisional — author ratification pending; convened on the author's instruction, carrying both questions by decision 1a). Q1's **bit-pattern** reading is vetoed: sixteen `f`s is `-1` and fifteen is 1152921504606846975, so `0xFFFFFFFF00000000` stops being `int_out_of_range` and silently becomes `-4294967296` — a compile error traded for a 16× wrong value. Q1's **value** reading is held rather than adopted, because panel 035's R6 refusal of exponents applies unchanged: **the closure list is a list of forms, not of types**, so "`int` is on the list" no more carries hex notation than "`f64` exists" carried exponents. What ships instead at **zero spec tokens** is R6's other half — a `hex_literal` diagnostic, because `x = 0xFFFF` today says *"expected the end of the line, found a name (`xFFFF`)"*, a message about a name for a number. Q2's `u64`-at-the-boundary is **vetoed on a measured inversion**: `extern constant SIZE_MAX: u64` plus `to_int` is a binary that **traps** where today it is a compile error | the ffi-pragmatist compiled 13 C files across six targets and moved the question: `int64_t`↔`size_t` **round-trips losslessly** at all four edges on arm64 and x86-64, so the gap was never the ABI, only the type check — and one extra pair of generic associations in the existing `_Generic` binds `strlen`, `SIZE_MAX` and `CURLAUTH_ANY` today with no new type, no spec token and no ABI move, keeping 6/6 wrong signatures as errors. It is **recorded and not adopted**, because the historian pre-refuted it without knowing it existed: `dart:ffi` is Heroes' exact integer model, shipped this shape ~7 years ago, and its filed issues say *"nobody told us the value went negative"* — and the widened assertion has no marker type at all, so it inherits that failure more exposed, not less. `u64` also fails three ways off LP64: its `_Generic` cannot be spelled with typedefs (a hard clang error), `-> u64` does not compile on i386, and wasm32 accepts a 32-bit `unsigned long` as `u64`. Three of the proposal's own claims were corrected by judges: **C# is not a bit-pattern language** (C's first-that-fits rule; `-1` needs `unchecked`), so the reading is Java-only among sourced languages; **no language enforces hex digit case** and the only precedent is rustfmt; and `~0` **already is** the all-bits mask, because a `constant`'s body may compute. The sitting's real yield is two live defects found by the judge reading only a spec: **`print(0700)` prints `700`**, and the spec's promise that *"clang checks every signature… so a wrong FFI type is a compile error"* is **false at the integer boundary** — measured, `strlen` cannot be bound at all and nothing complains about the lie you write instead | §1.0, §1.6, §4.15, §4.19, Part 6, Part 7 item 10, CLAUDE.md §11, §12 | 035, 038, 040, 041 |
2026-08-12 | **The author overturns panel 041 on both halves: all four bases land, and sized integers are built after all** (author decision, in conversation the same day the panel sat; the verdict is appended to `docs/panel/041` and CLAUDE.md §4 makes that the panel's closing act rather than a new sitting). Notation is adopted **wider than proposed** — `0x` `0o` `0b` and the `_` separator, not hexadecimal alone — on the author's ground that *"se sono interi sono interi"*: a base is how an existing value of an existing type is written, and nine languages have all four with none regretting it. The spec-warden's consistency argument from panel 035 R6 is answered rather than set aside, and the answer is the disanalogy the sitting accepted too fast: **an exponent creates values that do not otherwise exist and a base creates none**, so R6's "new syntactic form" conjunct does not reach it. The **value** reading survives the ratification, which means the one veto that changed nothing is the one that was right. Octal earns its place twice: panel 041's own D1 defect — `print(0700)` printing `700` — has two repairs and the sitting offered only the narrow one, where giving octal a real spelling closes it *and* delivers the notation. That is Python 3's answer and Rust's and Go's `0o`, arrived at because the bare leading zero is a trap | on the second half the ask is **bigger than the menu the panel judged**: `i8 i16 i32 i64` and `u8 u16 u32 u64`, first-class, the Rust shape — not option (b), which every judge who could see the repository rejected, and not option (c) as priced, which the panel described in one line and refused in one line. Half of what design.md:833 calls "the most expensive spec item that exists" is already paid, because §4.3 forbids implicit conversions outright. **Part 7 item 10 is retired, not reinterpreted** — this overrides *"never in v1"* exactly as panel 040 overrode §4.14's own sentence, and the milestone that retires it must say so. Three judge objections survive as work rather than as doubt, all measured: `Ty::Cstr` appears 11 times across 9 files with **9 of them silent `_` arms**, so every new `Ty` variant inherits nine defects under CLAUDE.md §11; `_Generic` **cannot be spelled with typedefs** (`size_t` and `uintptr_t` in one association is a hard clang error), so the FFI half must use fundamental types plus a `sizeof` companion assertion or a width-named type cannot verify its own name; and the historian's Kotlin precedent now points the other way, since the KEEP's motivation **#1** for unsigned types was hexadecimal literals overflowing the signed type — this ratification's two halves, in the order they are being built. Two milestones and deliberately not one (`M-literal-bases`, then `M-sized-integers`): decision 1a made the cheap question hostage to the expensive one that morning and said so in its own words, and folding the notation into the type system would be the same trap a third time. Four of the sitting's ten predictions become checkable early and are scored at `M-literal-bases` close; four were written about option (b), are not what is being built, and are **retired unscored** with the reason recorded rather than the numbers quietly dropped | §4.3, §4.15, §1.6, Part 7 item 10, CLAUDE.md §4, §9, §11 | 035, 040, 041 |
2026-08-12 | **Panel 042 judges the shape of the widths and returns two vetoes, neither of them about the widths** (`docs/panel/042`, provisional — four questions are the author's). The direction was ratified this morning and was not before the sitting; the four rulings that went in were judged only for consequences, and three of them turned out to have consequences the rulings did not settle. **Shape decides ruling 4's truth**: eight `Ty` variants produce **2** rustc errors and leave **18** silent sites, one `Ty::Int(IntKind)` produces 20 — and among the 18 is `emit/ops.rs:208`, `operands == Ty::Int`, which under eight variants is `false` for a `u8`, falls through, and emits `t0 = l + r;` with **no `__builtin_add_overflow`**, so *"overflow aborts at every width"* becomes silently false at exit 0. Shape B adopted, on CLAUDE.md §11's loud-fallback rule rather than on taste. **The alias cannot be transparent and satisfy §4.15 at once**, and the historian showed the proposed cure is Go's reversed design — `go/types` erased eagerly until 1.22 and its own maintainer filed the bug, repair spanning three releases; clang's `'pid_t' (aka 'int')` is the fifteen-year answer. The rule is canonicalise in the *comparison*, print what the author wrote. What remains open is which way `fmt` canonicalises, and the sitting found a third option no judge proposed: `i64 → int`, which is exactly the shape `M-literal-bases` shipped for hex digit case that morning, and which the spec-warden's numbers favour (**+21** spec tokens to rewrite, **545** `int` occurrences across 168 `.hero` files) | **ruling 4 leaves the motivating case exactly where it was**, which is the hardest finding and came from the judge that never sees the repository: FNV-1a *is* multiplication modulo 2^64, so `h @ h * 1099511628211u64` compiles and aborts on byte 1 of every input, and the limb workaround the judge then wrote **converts the abort into a silently wrong hash**. This project has met that hash — the mangler's typehash was FNV-1a and was redesigned this morning because Heroes could not express it, the single measured instance in the whole record of unsigned being needed. The author's own menu had abort-plus-explicit-`wrapping_mul` and it was not taken. **And the FFI wants a different vocabulary than the one being built**: a clang AST walk over `sqlite3.h`, `curl.h` and `raylib.h` found **1151 declarations and zero returning an 8- or 16-bit scalar**, while the 21 that hurt are `ssize_t`, `time_t`, `unsigned long`, `size_t`, `off_t` — C-width, not Heroes-width. `u64` binds `size_t` only under an LP64 premise the `.hero` source cannot write, and fires on i386 and wasm32; `c_size_t` is clean on all five targets. Part 7 item 10's amendment from this morning survived every compile rather than being overturned. Panel 041's finding 1 **is** overturned, conditionally: `SIZE_MAX` and `CURLAUTH_ANY` are named and read correctly under `u64`, but read `-1` and `-17` through today's runtime, so `u64` costs `HERO_RUNTIME_ABI` 10 → 11. The sitting also found its **third** premise-written-as-justification, and this one is a live defect rather than a future one — see the next line | §4.15, §4.19, §4.3, §1.6, Part 7 item 10, CLAUDE.md §7, §11 | 006, 036, 038, 041, 042 |
2026-08-12 | **`heroes` answers *"the compiler is wrong"* for an ordinary author mistake, whenever a bound C function returns a pointer — and the `+` that causes it was never doing the job its comment claims** (panel 042, ffi-pragmatist; reproduced twice, independently). `extern function getenv(name: cstr) -> int` gives `internal error: compiling the generated C failed … invalid argument type 'char *' to unary expression`, exit 2. Unary `+` on a pointer is a **hard clang error**, so `_Generic(+(c), …)` never reaches the point of producing the marker string `emit/ffi.rs` matches, and CLAUDE.md §7's named exception — the one place a clang failure is the *author's* error and not the compiler's — cannot fire. The mechanism panel 036 built to say *"your declaration disagrees with the header"* fails on exactly the return type most likely to be declared wrong | the premise justifying the `+` is **false today**, measured on Apple clang 21 against a synthetic enum *and* against the real `CURLcode`: `emit/externs.rs:117-123` records that without it the macro *"refused every enum-returning C function in existence"*, and a bare `_Generic` accepts both. The repair at M-ffi-ladder was two changes made together — the `+`, and widening the accepted set to `unsigned int` and its narrower siblings — and **the second alone was sufficient**, because `_Generic` on an enum selects its compatible integer type, which the widened set now contains. So the `+` has been carrying a justification for work it does not do while breaking pointer returns. It is also what the narrow widths need removed, since `+` promotes away every distinction below `int` and makes an `i8` assertion either reject the correct binding or check nothing at all. Both point the same way, which is the only reason this is a repair and not a second question. Third instance in one sitting of CLAUDE.md §11's named failure mode, after `HERO_RET_INT`'s *"exactly what fits in Heroes' `int64_t`"* and `printer/types.rs:3`'s *"this renderer has no options and no choices to make"* | §4.19, CLAUDE.md §7, §11 | 036, 042 |
2026-08-12 | **The specification may name a price and may not express a preference — and three judges reached that line from three unshared routes** (`docs/panel/043`, convened on the author's instruction over the whole of `DECIDE.md` § Open). Panel 041's spec-warden had proposed deleting the spec's two lines of cost advice as *"neither syntax, semantics nor built-in library"*; given a rehearing, it **withdrew its own proposal on the merits** rather than on the ledger it had been refused over: design.md Part 2:497 governs *choosing* (*"where a choice trades speed for simplicity, take simplicity"*) and is silent on *describing*, while §4.10:1327 already pairs both in one sentence — *"**Price, declared:** mutating a shared array copies it, O(n). Performance is not a goal, and in exchange…"*. The historian sourced the boundary from outside: C++ makes *Complexity* a **named element of a specification** binding the implementation, Scheme's proper-tail-recursion rule is the same kind, and Python states the quadratic-concatenation cost in its library reference — whereas an instruction to *prefer* a construct binds the **author** and rests on a premise about the current implementation, which is CLAUDE.md §11's world-premise. The wild failure is sourced: **C++17 removed `register`**, a spec statement whose only content was performance, and real builds broke. So the rule now has a home: **state what an operation costs; never instruct a reader to prefer one construct for speed** | the llm-ergonomist tested the clause **blind, on its own work without knowing it** — it had asked for that paragraph at panel 037 — and measured it working: `+` accumulated in a loop **0 of 4** with the clause, **2 of 4** without. It argued for removal anyway, because every move the clause caused was *correctness-neutral* (`out @ out + piece` is a correct programme, only slow) and one **cost** correctness, driving `split_lines` off a `chars()` scan into the u8/i64 hazard `M-sized-integers` had created that afternoon. Two of its claims were checked rather than relayed, and one is worse than it reported: **the array half advised the impossible.** `[i64] + [i64]` is `bad_operand` — there is no array concatenation, no `concat`, no `extend` — so *"build a long array in chunks"* had no addressee in the language it describes, for a month. The compiler-engineer priced what survives: `hero_array_push` copies **every** element with no capacity field anywhere in the runtime, measured **1.07 / 3.34 / 13.08 / 50.93 s** at n = 20k/40k/80k/160k, textbook quadratic. Net **+7**, and it is a removal wearing an addition's clothes: out go the imperative and the impossible advice, in goes `join(xs, sep)`'s shape — which the judge had to guess in **both** blind variants and named as the paragraph's real payload | §4.10, §1.6, CLAUDE.md §11, §13 | 037, 041, 043 |
2026-08-12 | **design.md said "One integer type only" for the whole of the day it stopped being true, and the panel is what caught it** (`docs/panel/043`, compiler-engineer, out of its own scope and flagged anyway). §4.3:831 read *"**One integer type only.** No `i8`, `u32`, `usize`. No choice, no width conversions… Keep them out of v1 without debate"* while design.md:1134 and :2812 — edited that same afternoon by `M-sized-integers` — called `fit_i64`. CLAUDE.md's preamble makes design.md the source of truth and §12 says spec beats compiler, so the panel was being asked to ratify a return-type rule for a family the source of truth forbade. Panel 041 had retired Part 7 item 10's *reason* and panel 042's record retired the *item*; the **normative paragraph that actually stated the rule** was never touched. The repair keeps the half that was true — conversion rules are where the errors live, which is why every one is written and none inferred — and records why the fear misfired: what makes widths expensive is C's **promotion lattice**, and §4.3 had banned implicit conversions years earlier for an unrelated reason, so there was no lattice to specify and the whole addition came to **+188** | two more defects from the same milestone, both found by this sitting and both the assistant's from that day. **A comment asserted the opposite of the code it sat on**: `resolve/builtins.rs` said *"widening returns `T?` too… the uniform rule wins"*, written at step 6 and left behind when step 7 changed the rule — CLAUDE.md §11's named failure mode, committed inside the milestone that found three instances of it elsewhere. And **`print`'s diagnostic enumerated a set it did not have**, offering *"`i64`, `f64`, `bool` or `str`"* while accepting `i16`, `i32`, `u8`, `u16` and `u64`, all measured printing correctly — in the milestone that added them. Both repaired here, with `emit/ops.rs`'s dead `"1"` branch made `hero_unreachable` and given `contains_agrees_with_range`, a test over all 64 pairs whose failure message names its three dependants. **Three of the sitting's six tree findings are defects in work committed the same day by the assistant convening the panel**, which is the instrument working rather than failing | §4.3, Part 7 item 10, CLAUDE.md §8, §11, §12 | 041, 042, 043 |
2026-08-13 | **The uniform `T?` is restored: a width conversion is fallible at every pair, including the ones that cannot fail** (author ratification, *"fai la soluzione più solida e sicura"*, closing panel 043's Q1; `M-sized-integers` step 7's bend is reverted). The bent rule returned a plain `T` where `IntKind::contains` proved the conversion safe, and it was **correct** — panel 043's compiler-engineer generated all 64 width pairs from an independent reimplementation and found no fault, showed structurally that C's usual-arithmetic conversions could not bite, and its load-bearing condition (a `certain` fix removing a needless `.must()`, a `guess` adding a missing one) was built. None of that was the question. Three things outrank the `.must()` it saved: **one rule**, so a reader knows the result's shape from the name without resolving the argument's declaration to learn its width — §1.3's locality test, measured by that judge as *"worse by exactly one write-side hop"*; **it was an invention**, the historian having searched Zig, Rust, Swift, D and Ada for one conversion name yielding a plain value for some argument types and an optional for others and found none, while Zig's `math.cast(comptime T: type, x) ?T` — `fit`'s exact shape — returns `?T` unconditionally; and **reversibility**, 8 sites today against ≥25 at M-selfhost-probe, which was the cheap moment and scores that judge's prediction 4 inside its own *"still cheaply reversible"* branch | the cost is real and is written into the code rather than hidden: `fit_i64(l.here() - '0').must()` is back at the hottest line of the calculator's lexer, a `.must()` on a conversion that can never fail, which §1.4 calls redundancy spent where errors cannot occur. **And the revert exposed an interaction the panel had itself created.** Panel 043 turned `emit/ops.rs`'s empty-test branch into `hero_unreachable()` and was right at the time — under the bent rule an early return took every widening before that point, and all 64 emissions confirmed nothing reached it. Restoring the uniform rule made it reachable again and `premise-mangler-hash-in-heroes.hero` failed inside a minute with `assigning to … from incompatible type 'void'`. It now emits an unconditional `ok`: no condition, no `else`, and no `if (1)`, which `-Wtautological-constant-out-of-range` would reject. `contains_agrees_with_range` still guards what the emptiness rests on — if `contains` and `range` ever part, a *narrowing* arrives there with no test and truncates in silence. A repair right under one rule and wrong under another, caught by a golden rather than by reading | §1.3, §1.4, §4.3, CLAUDE.md §11 | 042, 043, 044 |
2026-08-13 | **`fit_<width>` becomes `to_<width>`, and `to_i64` absorbs the `f64` conversion — one scheme for every conversion in the language** (author decision, `docs/panel/045`, retro-record with no judges convened on the author's instruction). The rule is stated once and covers conversions that do not exist yet: **`to_<T>` is fallible exactly when converting to `T` can fail** — `to_str` and `to_f64` cannot and hand back a value, every `to_<width>` can and hands back a `T?`. This is **panel 017's rule applied rather than a new one**: it renamed `.str()` to `to_str` *"so the three conversions share one scheme and a model can derive the third from the two the spec already lists"*, and `fit_` broke exactly that — a model knowing the three cannot derive `fit_u8`. Panel 044's blind judge proved it unprompted, reaching for `to_i64(text[i])` and annotating its own miss. The argument that had justified `fit_` (panel 042 Q2: `to_i64` aborts, the widths return `T?`, so two failure models under one prefix) does not survive contact with the family it defends: `to_str` and `to_f64` are total and `to_i64` aborted, so the `to_` prefix **already carried mixed behaviour** | the rename forced a consequence that was not part of the ask and is the better half of the change. `to_i64` had to serve both an `f64` and every integer width, and could not carry two failure models without being the thing panel 042 refused — so it is fallible from both, and **a runtime abort became a value**. `hero_f64_to_int` called `hero_panic`; the check is now `hero_f64_fits_int`, a *question* the emitter asks before building an `ok` or a `fail`. The predicate is untouched and is why its golden exists: `v >= -0x1p63 && v < 0x1p63`, because `v <= (double)INT64_MAX` accepts 2^63 (that cast rounds **up**) and `v > -9223372036854775809.0` rejects `INT64_MIN`, and on arm64 an unchecked cast **saturates** rather than trapping. The case is rewritten as `to-int-range.hero` and gains one nobody had — **NaN**, which satisfies no comparison and now fails as a value where it used to abort. The objection is on the record unstaged: five sites that read `to_i64(x)` now read `to_i64(x).must()`, one of them the spec's own example. **And 128-bit integers were refused in the same sitting, on C rather than taste**: `__int128` is a clang extension against §7's C11, C cannot express a literal above 2^63 at all, and `inttypes.h` has no `PRId128` — while §1.11 gives no pull, since no C header declares one in a portable API. design.md §4.3 carries the row with §12's falsifier | §4.3, §1.11, CLAUDE.md §7, §12 | 017, 042, 044, 045 |
2026-08-13 | **A promise is held to the same standard as a payment: the registered-prediction branch must name an instrument that exists on the day it is registered** (`docs/panel/046`, provisional — author ratification pending; three judges, opened from panel 043's own finding). §1.6 lets an addition above the soft line pay with a named removal **or** a pre-registered falsifiable prediction, and only one of the two was ever collectable. Measured from the ledger for this sitting: **14 rows** from 2231 to 2974, **4** paid with a removal, **6** with a prediction, and of those six, **zero scored** — because each named metric 2, which the author scheduled at M-selfhost-fixpoint. So six spec additions were bought with an instrument nobody will run before v1, and registering cost nothing. The rule adopted is the one the record already obeyed: **every prediction this project has scored named an instrument that existed when it was written** (panel 041's line count, panel 029's experiment, panel 040's `heroes mutate`), so the branch is not closed, it is re-routed to metric 3, `heroes measure`, `grep`, `wc`, a compile, a diagnostic transcript. An unscorable prediction is still **registered as an observation**; it simply pays nothing. This is CLAUDE.md §12's rule turned around: a refusal is held to the same standard as a feature, and so is a promise | **the expiry is a re-decision, not a clock, and the evidence against the clock is four regimes deep**. IETF Internet-Drafts expire at six months with a free reset and are re-posted unchanged — the draft proposing to abolish expiry *itself expired* (datatracker, 2024-07-23); sunset clauses are renewed by the inertia they were meant to overcome (*J. Reg. Econ.* 68:85-123, 1963-2022 panel); FDAAA 801's 12-month deadline, fines up to $10,000/day, statute of **2007**, produced its first notice in **2021**, **zero** penalties ever, and 41% compliance (DeVito et al., *Lancet* 2020). The one regime where expiry bites is Java's JEP 12, which uses **no clock**: a release boundary invalidates the artifact and demands a new one, and nothing previews whose implementation is incomplete. **And this project had already run the clock version and lost** — `SCHEDULED.md`'s panel-036 item says *"Score at M-ffi-ladder close"* and was still open after that milestone closed, which is why the bookkeeping is one bullet in `/step`'s close checklist and **zero compiler lines**: the gate reads no commit, has no CI and no hook, and any form that makes it invoke git is vetoed under §10 by the file's own recorded refusal. Option (b) — close the branch until a metric-2 run exists — was refused by all three: with removals unavailable at three consecutive sittings it freezes the spec until v1, including for §1.0 compiler-need items, which inverts Principle 0. Two expired premises repaired in the same commit and **neither restated as a number**: :314's *"zero removals in seven amendments"* (four of fourteen carry one) and :289's *"observed mean of +30 tokens per amendment … 4096 binds after ~61"* — the live mean is **+57.2**, so the ceiling is **~20** amendments away and the sentence calling it toothless was itself false. Both now point at the ledger, because a figure that lives in one place cannot die in another | §1.6, §1.2, CLAUDE.md §10, §11, §12, §14 | 012, 024, 036, 043, 046 |
2026-08-13 | **The §11 sweep is closed: zero non-test files over ~300 lines, and the rule stops being a preference** (author decision 2026-08-12 (2a), run to completion). Twenty-three files were in breach when the decision was taken — twenty-six counting the three the sweep itself pushed over — and twenty-two were cut in one session, one commit each, every cut along a seam that names a concern rather than at a line count. **What made "mechanical" a measurement rather than a claim**: the emitted C was **byte-identical over all 62 `run/` goldens at every single step**, shasum `adce2f13` from the first commit to the last, with `heroes mutate` unchanged at 1255 mutants / 93% / 78%, 533 tests green, clippy clean, PORT-DEBT still 4 and the spec untouched at 2974. The rule was the only thing watching the compiler's size after §1.6's global budget was refused (design.md:320), and a rule twenty-three files break is a preference | **the sweep found six defects by moving code that reading it had not found, and they are one class**: prose that outlived the code under it. Two module docs described rules the code no longer had — `types/builtins.rs` said *"a widening returns `T` rather than `T?`"* **one day** after the author's ratification restored the uniform `T?`, and `resolve/exprs.rs`'s `calls_in` took a `subst` parameter it never read under a line saying calls *"come back already substituted"*. A doc comment for `signature` sat on `extern_head`, describing a function it has nothing to do with. `run` bound a `parent` and discarded it. `to_<width>`'s emitter computed its name, target and union **twice**, the second pair shadowing the first identically. And `typo-digit` produces **0 mutants** over the whole 19-program corpus — an operator that measures nothing, §9's *"every verifier check has a test that makes it fire"* in mutation form, now queued rather than silently kept. **One cost is recorded rather than buried**: an index file grows by the act of tidying, and `types/mod.rs` went 303 → 310 on `mod` lines alone before being cut to 224. Three citations were repaired in the commits that moved their targets — including the `// ORDER:` item's own address, where leaving it would have reproduced the exact defect that item is a record of | CLAUDE.md §11, §5, §7 · design.md §1.6 | 020, 023, 029, 031, 038, 042, 043 |
2026-08-13 | **Panel 046 ratified, and the ratification's whole content is *where the date lives*** (author decision (a), `/decide`). R1–R5 stand unchanged — a registered prediction pays only if it names an instrument that exists on the day of registration, an outstanding one is re-decided and never renewed, (b) refused at its stated price of a spec frozen at 2974 until M-selfhost-fixpoint with §1.0 items included. Three things the provisional default left implicit and the yes settles. **The rule is not retroactive**: R1 governs what may be *offered* as payment, so none of the six prediction-bought rows is un-bought and no clause leaves the spec — what R2 reaches is the obligation to look. **The expiry is M-program-corpus, not M-selfhost-fixpoint**, though five of the six named metric 2: the spec-warden's condition on its own yes was that the expiry not be the instrument's arrival date, *"otherwise the expiry is the tenancy"*. **And the date is written once**, in `measure/gate.rs`'s module doc beside the rows, rather than into each of the six — six copies of one date is six places for it to rot, which is the failure this sitting was convened about, restated as a filing rule | **the sixth row is the argument, and it happened while nobody was watching**: `2675` (panel 040's bitwise set) was scored *"at the next FFI rung"*, and M-ffi-ladder closed on 2026-08-12 — the same day the row was written — unlooked-at. That is a milestone-named expiry lapsing inside three days, in a repository whose author had just been shown two others lapsing, and it is why the ledger and not the panel file is where an expiry belongs: `/step`'s close bullet greps the ledger | design.md §1.6 · CLAUDE.md §12, §14 · .claude/skills/step/SKILL.md § 5 | 046, 040, 042, 038 |
2026-08-13 | **Three expired premises about ratification itself, repaired in the sweep the ratification made** (`/decide`, verify-before-asking). Every panel in `docs/panel/` was checked against `QUEUE.md`, `DESIGN-LOG.md` and design.md before a single question was put, and the check found the record correct and its *labels* stale in three places: `docs/panel/042`'s header still read `provisional — ratification pending` while the author's verdict on all four questions sat in the same file's last section; CLAUDE.md §7 still called panel 038's mangled-accessor exception `ratification pending` while `docs/panel/038` § Ratification is dated 2026-08-12; and `docs/panel/040` carried no § Ratification at all, though the DESIGN-LOG records the author's decision. None of the three is a decision that went missing — each is a decision **recorded everywhere except in the artifact it was about** | a stale `pending` is worse than a missing one: it is a live claim that the language is still undecided somewhere, and it reads as correct forever because the argument around it never stops being valid — CLAUDE.md §11's class, applied to the process rather than to the compiler | CLAUDE.md §7 · docs/panel/040, 042 | 038, 040, 042, 046 |
2026-08-13 | **The corpus is an instrument, and it reports on the compiler: seven programs, six defects, none of them findable by reading** (M-program-corpus, `docs/journal/016`). `examples/` becomes one directory per program, found by looking rather than by list — any directory holding a `main.hero` — and each is held to six properties in three configurations, including *at least one program that never calls `exit`* so the corpus keeps a path across the leak gate the author asked for on 2026-08-12. The defects: `heroes check` **hung forever** on a variant case named `null` with a payload; `-x` on a name was read as a negative **literal**, so `x @ -x` on an `f64` was *expected `f64`, found `i64`* and `ok(-n)` was a **range** error about an expression with no value; a `constant` read **across a module** did not compile at all, refused with *"`grid` names a module, not a value"* on a line where it was followed by a dot; a dead temporary put `-Wunused-but-set-variable` on every build; `heroes fmt` **changed the meaning of a program**, printing `(at / WORDS).to_str()` as `at / WORDS.to_str()`; and the `@` marker went unchecked on a built-in, so `push(@lines, x)` claimed the call changes its argument and nothing said so | **three of the six are CLAUDE.md §11's class, and the sharpest one is under a comment forbidding it.** A narrowing resting on a premise about the world: `Unary { Neg, .. }` matched *flatly* in two of the three readers of "is this a number literal" while the third recursed; a three-name list `Call | Load | Const` presuming nothing else could produce an unread value; and the number `7` for "tighter than a postfix receiver", correct when the scale ended at `postfix = 7` and left behind when panel 040 added five bitwise levels. The precedence scale's own comment reads *"a scale that disagreed with the parser's would print a program that parses differently from the one it read"* — true, read, agreed with, and contradicted twenty lines below it in the same file. All three are repaired by naming the thing rather than the number: `contextual::number_literal`, an **exhaustive** `may_lose_its_destination` with no `_` arm, and `UNARY`/`POSTFIX`. And one race is repaired that presented as a failure **once in several runs**: the runtime cache raised its published flag while clang was still writing the file, now compiled beside the target and renamed | CLAUDE.md §9, §11 · design.md §4.5, §4.8 | 021, 034, 040, 046 |
2026-08-13 | **Error codes become `constant`s across the corpus, and the operator lost its sites rather than passing them** (panel 034 R4, discharged; `docs/measurements/006`). 61 declarations across 18 modules, **zero spec tokens and zero compiler lines**, and the measurement is the one the panel named in advance as the awkward outcome: `typo-code` 88 mutants at 0% → **3 at 0%**, `typo-ident` 3216 → 3343 at 100%. **85 sites moved from an operator that catches nothing to one that catches everything** — the mistake class did not become detectable, it became a *different* class, one the compiler already refuses. Three sites stay literal on purpose, all in `gallery/03-fallible.hero`: the gallery is one idea per file, and a `constant` in front of the code would put a second idea into the file a reader meets `fail` in | **the panel asked for a form the language did not have.** R4 was written 2026-08-12 naming `store.ERR_UNKNOWN_ITEM`, and a qualified `constant` read was refused by the resolver until step 6 of this milestone — `resolve/exprs.rs` asked the qualified-name question from the `Method` arm only, a dot *with a call*, and a dot with no call is a `Field` whose base was resolved as a value. Five of the corpus's reads are that shape. Nobody found out until a program needed it, which is this milestone's argument in one sentence | CLAUDE.md §9 · design.md §4.6, §4.17 | 034, 031 |
2026-08-13 | **The six prediction-bought spec rows are re-decided: two scored, four lapsed** (panel 046 R2's obligation, discharged at the milestone it named; `docs/measurements/007`). The split is exactly what R1 predicts — `2627` and `2675` named instruments existing on the day of registration and were collectable; `2588`, `2745`, `2768` and `2959` all named metric 2, which the author scheduled at M-selfhost-fixpoint, and are marked **`lapsed`** in the ledger with their clauses back in `DECIDE.md` under the removal branch. Nothing left the spec: R1 governs what may be *offered* as payment and is not retroactive. **None is renewed with a new milestone name**, which is the one thing R2 forbids. 312 tokens are unfunded, a quarter of the headroom, and three of the four clauses are §1.0 compiler-need | **both scored rows found what the row did not predict, and one of them sharpens R1 itself.** `2627`: a blind reader given the spec alone never built a string by repeated `+` — *"the only sentence in the document with a cost claim in it, so it is loud"* — and then wrote `push`-in-a-loop in two programs of three, *"redirected from one quadratic accumulation to the other"*. The falsifier is a language gap: with no lambda `map` cannot capture a local and with no `repeat` there is no way to make N copies of a character, so two tasks had no non-loop route. `2675`: held, and **vacuously** — none of `heroes mutate`'s twelve operators makes the substitution the prediction is about. R1 asks that the instrument **exist**; it does not ask that the instrument be able to produce the observation, and that second half is now queued. The same blind reader's careful, wrong inference about `push(@lines, x)` is what found the sixth defect | design.md §1.6 · CLAUDE.md §12 | 037, 040, 046 |
2026-08-13 | **The CI is born, on Linux x86-64 and Darwin arm64 — and writing it found three defects before it ran once** (M-program-corpus; `.github/workflows/ci.yml`). `fail-fast: false`, because neither platform's failure may hide the other's, and the three configurations are inside `cargo test` rather than beside it. The whole record of this project was made on one laptop, and every hard-won platform fact is a place the other machine can diverge in silence: ASan has **no** leak detector on Darwin arm64 and does have one on Linux; `INT64_MIN % -1` returns 0 at exit 0 on arm64 and raises SIGFPE on x86; Apple ARM64 passes variadics on the stack. Nothing had ever checked the two against each other | **the workflow found `heroes doctor` exiting 1 on Linux** (it asked `xcode-select -p` on every platform; the check is now named after what it is *for* and each platform is asked in its own words), the formatter defect, and the runtime-cache race — the last of which only ever failed under parallel test binaries on a cold cache, which is the CI's permanent state. **It has not run**: the first push starts GitHub Actions on this repository for the first time and that is the author's call, so it is recorded here as owed rather than done | CLAUDE.md §7, §14 | 021 |
2026-08-13 | **The generated C is compiled as `-std=gnu11`, and the difference between the two dialects is one predefined macro** (`docs/panel/047`, provisional — author ratification pending; three judges, opened by the CI's first run on a second machine). `-std=c11` defines `__STRICT_ANSI__`, and on glibc that is the *only* thing it does: it hides `M_PI`, `M_E`, `strdup`, `strndup`, `fileno`, `popen`, `setenv`, `unsetenv`, `newlocale`, `uselocale`, `clock_gettime` and `CLOCK_MONOTONIC` — most of what §1.11 says a program binds — while Darwin's headers do not guard them at all. So `tests/golden/run/ffi-constant.hero` passed here and failed on Linux, and **the flags rather than the types decided whether a correct binding compiled**, which is the failure §4.19 exists to prevent arriving by the one road nobody was watching. What the emitter *writes* is unchanged and still C11; only what the platform's headers *expose* moved | **the veto holder went looking for breakage with a cross-compiler and reported *"I came to break this and could not"***. The ffi-pragmatist found a real glibc/musl/FreeBSD header tree on the machine and ran the **real pipeline against real glibc headers** rather than a model of them: identical LLVM IR for the runtime and for a generated unit at both levels, 16 glibc struct layouts unchanged with three types going hidden→visible and none re-laid-out, §4.19's own `_Generic` classifier returning identical answers on ten header tokens, and **63 of 63 `run/` goldens cross-compiling for `x86_64-linux-gnu` under `gnu11` against 62 of 63 under `c11`** — the one failure being this defect. Option (b), feature-test macros in the generated C, was refuted from three directions including its own: `_POSIX_C_SOURCE` cannot expose `M_PI` on glibc (it sets `__USE_XOPEN2K8`, not `__USE_MISC`) and **measurably hides five identifiers on Darwin**, `_DEFAULT_SOURCE` alone is *exactly equivalent* to gnu11 for 127 hand-edited golden lines in a directory where `UPDATE_GOLDEN` is forbidden, and it needs the same rule written in two translation units where `toolchain::FLAGS` is already the one place both are configured. The historian's fourth option — pass no `-std=` at all, which 3 of 3 sourceable C-emitting compilers do — was **declined on this project's own criterion**: a byte-identical fixpoint cannot inherit clang's default dialect, which is `gnu17` today and was `gnu11` before Clang 11. **`-std=c11` was buying nothing it was believed to buy**: the emitter has never written portable ISO C — `__builtin_*_overflow` is mandated three sentences above it in §7, and every generated unit carries `__builtin_types_compatible_p` — so the flag narrowed the *headers* on one platform and the *output* on neither. Two premises repaired in the same commit under §11 and §12: design.md §4.19's `true`-is-`i64` sentence rested on the flag rather than on the value (the fact survives, measured on both dialects and both platforms), and the 128-bit refusal's first leg was *"§7 says C11"*, which had to be rewritten because §12 holds a refusal to a feature's burden of proof. And CLAUDE.md §7's own "Compile flags:" list did not contain `-std=` at all — both judges found that independently | CLAUDE.md §7, §11, §12 · design.md §1.11, §3.1, §4.19 | 038, 040, 046, 047 |
2026-08-14 | **Both platforms are green, and the last thing between them was a library rather than a language** (panel 047's predictions scored; CI runs 5 and 6). The dialect change was necessary and **not sufficient**: with `M_PI` visible, `tests/golden/run/ffi-constant.hero` failed one step later at the linker — `undefined reference to 'sqrt'` — because `libm` lives inside `libSystem` on Darwin and separately on Linux, and three `extern "math.h"` groups had never said `link "m"`. The language's own mechanism was the thing they were not using, so this is the same shape as the dialect one layer down: Heroes right, one platform lenient, and nobody able to tell until a second machine existed | **the panel measured the compiler's output and missed the platform's runtime.** Three judges cross-compiled 63 of 63 `run/` goldens for `x86_64-linux-gnu` and not one asked whether the programs would **link** — compiling is not linking. The ffi-pragmatist's prediction 1 is falsified on exactly that seam, and the instrument that caught it was the CI rather than any judge. `link "m"` is added only to the three groups that produce a binary; the three that stop at `check` or at the IR dump are left alone, because a `link` clause there would claim a dependency the case does not have | CLAUDE.md §7 · design.md §4.19 | 047 |
2026-08-14 | **Panel 047 ratified as it stands, and the ratification's content is that the dialect is *named*** (author decision). `-std=gnu11`, all four conditions, both refused options refused. The decision is not `gnu11` over `c11` — that followed from the measurement, one predefined macro and identical IR — it is `-std=<something>` over nothing at all, against the dominant precedent of three of three sourceable C-emitting compilers. The ground is this project's own acceptance criterion, and it now has two instances on one day: `rust-toolchain.toml` pins the Rust, `FLAGS` names the C, and a byte-identical fixpoint cannot be built on a dialect that drifts with whatever the compiler shipped that morning | **the panel's falsified prediction stays in the file.** Three judges cross-compiled 63 of 63 goldens for Linux and not one asked whether the programs would link; the yes does not tidy that away now that both platforms are green, because it is the record of what the instrument does not see. What the sitting deliberately did not ask — whether the *spec* should state which C the FFI reaches — opens as panel 048 with the two judges this lane gave up | CLAUDE.md §4, §7 | 047 |
2026-08-14 | **Panel 048 refused both clauses it was convened about, and what landed is a sentence repair and a diagnostic** (`docs/panel/048`, provisional — author ratification pending; the first full five-judge sitting in this repository). The question was whether the spec should say which C the FFI reaches. **X** — *"read as a POSIX C compiler presents them"* — is **false on glibc**: 30 probes across four libcs at the ratified `gnu11` found `strptime`, `wcswidth`, `wcwidth` and `swab` declared on Darwin, musl and FreeBSD and hidden on glibc, because glibc's gnu11 default is `_DEFAULT_SOURCE` (no `__USE_XOPEN`) while musl's `features.h` sets `_XOPEN_SOURCE 700` under the same flag; and `arc4random` is gated on `__GLIBC_MINOR__ >= 36`, so the visible set moves with the libc **version**. **Y** — *"write `link \"m\"` everywhere"* — is true about `math.h` and breaks as a rule on three rows compiled: `-lrt` **errors** on Darwin, `-liconv` is **required** there and **does not exist** on glibc, and `pthread` moved into libc at glibc **2.34**. The blind reader is the reason Y was taken seriously at all: given only the spec it wrote `link "m"` **from outside the document** — the spec's own pattern yields `link "math"` — and wrote `link "c"` twice believing a group without `link` to be ungrammatical | **three judges, judging something else, found the spec already false.** § FFI said *"A group names its header and its library"* while **13 of 20 groups name none**, including the closure list's own `extern "hero_os.h"` — so under §12 the spec made the port's own source illegal. Repaired at **+9**, paid as §1.0 compiler-need after the warden's removal search over six candidates returned **nothing** (and corrected the record: `2588` is compiler-need too, so of 312 unfunded tokens **zero** are recoverable where the record believed 28 were). The warden also **declined its veto on a dead number** — its brief grants one above 3000, design.md raised the ceiling to 4096. And the option nobody proposed, reached independently by two judges and precedented by the historian in Zig: **fix the compiler**. `ffi_missing_link` turns the linker's verdict into a diagnostic on the author's line at exit 1, **zero spec tokens**, gated on `declaration()` so a symbol nobody declared stays exit 2 and the emitter's bug. §7's "one named exception" is now four classes, and its claim that `emit/ffi.rs` *"matches only the assertion messages this emitter writes"* was **already false when written** — `unknown_name` matches clang's own — with the true narrowing being the stronger one all along | CLAUDE.md §7, §11, §12 · design.md §1.6, §1.11, §4.19 | 036, 038, 046, 047, 048 |
2026-08-14 | **The platform axis is refused, and the feature the author asked for would not have unblocked the case the author named** (`docs/panel/049`, provisional — author ratification pending; five judges, one veto). The instruction was to spend spec tokens and add an OS/platform concept for frameworks, DLLs and `.so`s so §4.19's ladder rung 5 could be climbed. Measured by four independent routes: raylib's homebrew **dylib** links on Darwin with `-I`/`-L` and **zero frameworks**; one judge **opened a window and drew from `.hero` source** with the head line unchanged and no platform token; and `heroes build` on a raylib group dies at `'raylib.h' file not found`, the **header path**, one directory before the linker exists as a question. **design.md:2058 had recorded exactly this a milestone earlier** — *"what the group head lacks is search paths, not a framework keyword"* — and panel 048's contrary sentence, which convened this sitting, is falsified and appended there. Landed instead, at **zero spec tokens**: `ffi_missing_header`, the fifth member of §7's named exception, turning exit 2 into a diagnostic on the author's line | **three findings outlive the decision.** First, the axis is wrong for its own motivating cases: panel 048 measured them per **libc** — `strptime` hidden by glibc and shown by musl, `pthread_create` moving at glibc 2.34 — and both are `linux`. Second, and larger than anything on the agenda: rung 5's own text promises *"real struct passing"*, and **383 of raylib's 600 `RLAPI` declarations pass or return a struct by value**, which no platform token changes by one line — the wall at rung 5 is the type vocabulary. Third, `windows` is unkeepable today: the runtime gives **18 errors** under mingw, all `locale_t`/`newlocale`/`uselocale` in `parts/f64.c`, and `-l` resolves differently per Windows ABI. Precedent, sourced: **0 of 11** systems put a platform keyword inside the link construct — Go reuses its *general* build-constraint vocabulary and its chronology shows the fusion sliding within ten months of shipping platform-blind, then needing a CVE-driven allow-list; clang module maps spell `link framework "Cocoa"` with no platform word; Cabal ignores `frameworks:` off Darwin; Rust makes an untagged `framework` a compile error. And **pkg-config already solves rung 5 with one universal spelling**, which §10 very likely refuses — but §12 holds a refusal to a feature's burden of proof, so that refusal is owed in writing | CLAUDE.md §7, §10, §11, §12 · design.md §1.0, §1.3, §4.19 | 036, 038, 046, 047, 048, 049 |
2026-08-14 | **`package` — the machine is asked where a library is, instead of the program being made to say** (author decision, `docs/panel/050`; retro-record, the evidence being panels 047–049). +123 spec tokens on the author's instruction to spare no expense on the FFI, and it **subsumes the platform axis panel 049 refused at +144** while putting no machine's name in any program: `pkg-config` answers with `-framework Cocoa` where that is the answer and `-lGL -lX11 -lm -ldl` where that is, from one spelling. And it says the thing no proposal in three sittings could say at all — `-I` and `-L` — which four independent measurements had found to be the actual blocker at §4.19's ladder rung 5, and which design.md:2058 had recorded a milestone before anyone looked. **The rung is climbed**: `examples/raylib/main.hero` opens a window in a clean environment with no platform token | **the allow-list is the feature, and it is here on the first day rather than after a disclosure.** A `.pc` file is input this program did not write; Go shipped the same idea without a filter and it became **CVE-2018-6574**, a repository shipping `attack.so` beside `#cgo CFLAGS: -fplugin=attack.so` and `go get` loading it into the host compiler. Only `-I`, `-L`, `-l`, `-F` and `-framework` pass, the list is named in the spec because the list *is* the safety, and a package that answers otherwise is `ffi_package` at exit 1 on the author's line. **Windows was asked for and was therefore built rather than named**: the 18 mingw errors panel 049 measured were all in `runtime/parts/f64.c`, and the port makes the code better rather than merely portable — `uselocale` switches a thread's locale and must be switched back, while `_snprintf_s_l`/`_strtod_l` take it as an argument and remove the window. A whole Heroes program now cross-compiles to `PE32+`, and `windows-latest` is the third CI leg. Two POSIX assumptions fell with it, and the second was a defect the first had hidden: `/tmp` in two `.hero` files existed because `golden.rs` ran one leg from the crate directory and two from the root, so a relative path was a different file in each — the workaround had outlived its cause and the disagreement it hid was still live | CLAUDE.md §7, §10, §11 · design.md §1.11, §4.19 | 036, 047, 048, 049, 050 |
2026-08-14 | **`-lm` is not a fact about the program, so the driver answers it rather than the author** (author instruction 2026-08-14, *"I want Windows too"*; the class is panel 047's and the instrument is panel 049's historian's). Windows was the fourth platform to hide `M_PI` and it hid it for a fourth reason — MSVC gates `math.h`'s constants behind `_USE_MATH_DEFINES`, which nothing switches on — so the flag joins `-std=gnu11` in `FLAGS`, the platform's own documented switch named rather than discovered. Then the same file failed to *link*: `link "m"` is required on glibc and FreeBSD, a POSIX-mandated no-op on musl and Darwin, and on MSVC it is `LNK1181: cannot open input file 'm.lib'`, because there is no separate maths library to open. The answer is **`in_the_c_runtime`**, precedented in Zig's `isLibCLibName` (panel 049's historian sourced it): the driver drops the `-l` names *this* platform's C runtime already contains, so **the universal spelling always works** and a library that is genuinely absent still fails with `ffi_missing_link` | **this is panel 049's refusal holding, not being worked around.** 049 vetoed a platform axis in the *language*; the same problem is answered here in the *driver*, where it is a fact about the machine that can be measured on the machine, rather than a word the author has to write into a program that then means nothing on the machines it does not name. Six of the run cases had been correct on two platforms for a full day. The third leg is what disagreed | CLAUDE.md §7, §10 | 047, 049, 050 |
2026-08-14 | **The assertion asks what an `extern` returns, and never what it accepts** (`docs/panel/051`, provisional — author ratification pending; five judges, three vetoes, one option refused by measurement after the sitting proposed it). § FFI said *"clang checks every signature and constant against that header, so a wrong FFI type is a compile error"*, and two judges falsified it with running programs — `putchar(c: i64)` given `4294967361` prints `A` at exit 0, a C `int narrow(int)` given `4294967301` prints `5` — while a third counted the exposure: **267 of 643 bindable entry points across `sqlite3.h`, `curl/curl.h` and `raylib.h`, 42%, take a parameter that is not 64 bits**. Under §12 the compiler has the bug, so the repair is §1.0 compiler-need: **+34**, *"a result may be wider than C's. A parameter is declared at the header's own width — `i32` where C says int — because clang narrows a wide one in silence"* | **the asymmetry was found by moving the two examples, not by reasoning, and it is principled.** `examples/sqlite/` moved in two edits and no shim; `examples/curl/` moved its parameters and **could not move its results**, because `CURLcode`'s compatible type is unsigned — `HERO_RET_INT` accepts every unsigned type narrower than 64 bits since they all fit an `i64`, and `HERO_RET_I32` refuses correctly. The direction of the conversion is what decides it. **The option with the only shipped precedent was refused by measurement**: a function-pointer probe is an error on clang ≥16 with no flag at all, finds the same four true positives, and then rejects an **enum parameter at every width** — which is panel 042's defect ( *"refused every enum-returning C function in existence"* ) arriving on the other side. And the warden's refusal of the widths rests on a design.md Part 7 row whose own falsifier a second judge **ran**: `strlen(s: cstr) -> u64` prints 5, so the row is unsupported under §12/panel 039 and is queued | CLAUDE.md §7, §9, §11, §12 · design.md §1.6, §1.11, §4.19, Part 7 | 042, 047, 048, 049, 050, 051 |
2026-08-14 | **What an `extern` accepts is checked, and the mechanism had to be built rather than switched on** (`docs/panel/052`, provisional — author ratification pending; five judges, one veto declined by measurement, one judge re-convened after its first run was cut off). Panel 051 left §4.19 half a guarantee; the reason it could not simply be completed is that `_Static_assert` over a `_Generic` leaves its controlling expression **unevaluated** (C11 6.5.1.1p3), and clang runs no conversion diagnostics inside one — measured in both the folding and the non-folding form. So `emit/extern_probe.rs` emits one never-called function per `extern` with a parameter, declared types in and the call in the body, under a `#line` at the author's declaration; `-Werror=shorten-64-to-32` and `-Werror=sign-conversion` make it an error, and `ffi_parameter_type` makes that exit 1 naming the parameter **by the author's own name**. Spec **+1**: *"at the header's own width and sign"*, the second half arriving from `examples/curl/` the day the first landed, `CURLoption` being signed and `CURLcode` unsigned in one header | **the sitting's two headline claims were both false, and both were the chair's.** A C `float` parameter bound `f64` is **bit-exact** — `0.10000000149011612` is what a `float` holds — so the 19 raylib entry points were never wrong, and the genuine hole is a `float *` out-parameter, a *pointer* problem no float type fixes. And variadics do not "work by accident": measured on arm64, the same call is `66` through the header's prototype and `8393946705` through a re-declared one, **correct on x86-64 either way** — so §4.19's refusal to re-declare is load-bearing ABI machinery, and the pragmatist holds a standing veto against any implementation that would "fix" it. The compiler-engineer's veto on the probe was **declined with a measurement**: strip the probes and the same defect is exit 2 with generated C on screen, because the probe's line is the only report carrying a declaration. Its four other findings landed as fixes, and the best of them was outside its brief — `-Werror=sign-conversion` made a **three-line program with no FFI** fail, exposing that `print(n)` and `print(n.to_str())` had printed **different numbers for the same `u64`** since panel 042: two readers of one decision, one exhaustive with the reasoning and one with a `_` arm that swallowed it. Also closed: a struct-returning C function bound as `ptr` was exit 2 and is now exit 1, which is **356 of 1159 entry points across three real headers**, by one line — `__builtin_classify_type(c) == 5` where a `_Generic` negative list could not see a struct at all | CLAUDE.md §7, §9, §11, §12 · design.md §1.11, §4.19, Part 7 | 042, 047, 048, 049, 050, 051, 052 |
2026-08-14 | **Panels 048, 049, 051 and 052 ratified together, as they stand** (author decision). Four sittings over two days, every provisional default already in the tree and green on three platforms when the yes arrived — which is the arrangement CLAUDE.md §4 describes, now exercised at scale rather than once. Nothing in any resolution changes: panel 048's two candidate clauses stay refused and its **repair** of an already-false sentence stays landed; panel 049's **platform axis stays refused**; panel 051's asymmetry stays — *a result may be wider than C's, and a parameter may not* — with **B** and **E** refused; panel 052's **D** and **C** stay landed and its **A** and **B** stay waiting | **what the batch settles is a pattern none of the four settles alone: measurement removed more than it added, and that is the sitting's result rather than its failure.** 048 was convened to add two clauses and landed a repair, because three judges found the sentence already there to be false — 13 of 20 `extern` groups name no library, one of them the closure list's own, so under §12 the spec made the port illegal. 049 was convened to add a platform axis on the author's own instruction and refused it; panel 050 then climbed the same ladder rung the next day at less cost, and the `-lm` question Windows raised a day after *that* was answered in the **driver** on exactly the ground 049 stated. 052 was instructed to implement `f32`, `size_t` and varargs and struck two of the three: a C `float` parameter bound `f64` is **bit-exact**, and a variadic bound with a fixed arity is **correct**, because §4.19 refuses to re-declare and the call goes through the header's own prototype. What survived is smaller and true. The ffi-pragmatist's **standing veto** is ratified with the rest — no variadic implementation may emit a re-declared prototype, because that version is silently correct on x86-64 and garbage on arm64, and the Linux leg would not see it | CLAUDE.md §4, §7, §11, §12 · design.md §1.0, §1.11, §4.19, Part 7 | 047, 048, 049, 050, 051, 052 |
2026-08-14 | **A prediction pays only if the instrument has an arm for the question** (author decision, `/decide`; panel 046 R1 amended, panel 040 prediction 2 re-scored). R1 as adopted asked that a prediction name an instrument that **exists**. `docs/measurements/007` found what that leaves open: panel 040's prediction 2 named `heroes mutate --survivors`, which existed, and asked about a substitution **none of the twelve operators could make** — so it scored `held` over 892 survivor lines that could not have falsified it. The thirteenth operator, `boolean-twin`, was built the same day and writes `&` for `&&` and `|` for `||`; over `examples/` it produces **132 mutants and kills 132, 100%**, at `check` and under `--permissive` alike. Prediction 2 is now held **and measured** | **this is R2's own failure wearing the other face, which is why it belongs in R1 rather than beside it.** R2 catches a renewal that only moves a date; nothing caught a prediction that only names a tool. Both are a payment that cannot come due, and panel 046 was convened about exactly that. What the re-score buys is not the number but that the number could have come out otherwise: a language whose boolean and bitwise families shared a type would show survivors here, and §4.14's refusal to let them overlap — `&&`/`||` `bool` only, the bitwise set `i64` only — is what kills all 132 before anything else looks at them. The amendment carries its own falsifier per §12: a prediction whose instrument plainly has the arm, unscoreable because building the *corpus* is the real cost, would mean R1's second half pushes work from registration onto scoring | CLAUDE.md §11 · design.md Part 11 | 011, 040, 046 |
2026-08-14 | **Robustness is a goal of the language, and it wins** (author instruction: *"uno dei motivi per cui lo voglio fare è che produca codice che non abbia problemi di segmentation fault o di memoria — tutte le scelte in quella direzione vincono su altri criteri"*). A Heroes program must not segfault and must not corrupt memory. Where a choice runs toward that, it beats every other criterion in CLAUDE.md: elegance, token cost, ergonomics, the size of the compiler, and speed. Written into §12 because it is a **precedence** rule and not a new permission — it does not suspend Principle 0, which still binds what *enters*; it decides the case where two admissible forms disagree and one of them can be made to crash. It is the mirror of §13's performance line rather than an exception to it: performance is not a goal *and* not a licence, safety is a goal *and* a tie-break. The instruction's second half is that the FFI be **complete and bug-proof** — complete because a library Heroes cannot bind is a library the author must keep C around for, bug-proof because a binding is the one place a Heroes program can reach an address nobody checked | **panel 053 was sitting when the rule arrived, and it re-ranked the sitting.** Four options were on the table about a nullable inbound `cstr`, and the ffi-pragmatist measured that three of them are blind to the only case that actually crashes: `to_str` on a null `cstr` is a **clean abort** — `runtime/parts/str.c:215` guards NULL before `strlen`, exit 134, identical at `-O0`, `-O2` and `--sanitize` — while a null `cstr` handed **straight to another C function** is `AddressSanitizer: SEGV in libsystem_platform.dylib`, and `to_str` is never called on that path. Under the old weighing that was a footnote about an unlisted case; under this rule it is the sitting's subject. The measurement that says a premise is false is worth more than the four options built on it | CLAUDE.md §12, §13 · design.md §1.11, §4.19 | 021, 052, 053 |
2026-08-14 | **A null `cstr` on its way into C is an abort with a name, and `nullptr` takes the type its context asks for** (`docs/panel/053`, provisional — author ratification pending; five judges, and a criterion that arrived mid-sitting and re-ranked it). The sitting was convened on a verified gap: `v == nullptr` on a `cstr` was `expected cstr, found ptr`, and declaring the binding `-> ptr` bought the comparison at the cost of ever reading the string — **you had to choose between checking for null and reading it**. Four options were proposed and **the sitting's own premise was false**: `to_str` on a null `cstr` never dereferenced anything, because `runtime/parts/str.c` guards it and panics with its own name, identical at `-O0`, `-O2` and `--sanitize`. That path was a *diagnostic* gap. **The safety gap was a null `cstr` handed straight back to C** — `strstr(haystack: getenv(UNSET), needle: "y")` — where `to_str` is never called and the pointer reaches libsystem unexamined: `AddressSanitizer: SEGV on unknown address 0x0`. Three of the four options were structurally blind to it. What landed is **E**, which nobody put on the ballot, and **A**: `hero_cstr_nonnull` on every `cstr` argument of an `extern` call, and `nullptr` joining the contextual forms at +19 in two files | **E is the strongest argument of the sitting because it is not a language change at all.** It is a backend obligation of exactly the class CLAUDE.md §7 already states — *"Arithmetic aborts via `__builtin_*_overflow` — never C UB — and `%` is guarded like `/`"* — so `hero_cstr_nonnull` is to a null `cstr` what `__builtin_mul_overflow` is to signed overflow: zero spec tokens, no new type, no new surface. The narrowing is the callee's **declared parameter type**, deliberately not provenance, because *"this value came from an extern"* is a premise about where a value has been and this compiler has no flow-sensitive narrowing to rest one on (CLAUDE.md §11); the price is that a `cstr` from `s.cstr()`, which cannot be null, is checked too — one predictable branch, against §12's rule that safety outranks speed. **A moved from cheapest to necessary** under that same rule: without it the null test is unwritable, so the guard would fire on programs whose authors had no means to prevent it. A makes the check writable, E makes its omission survivable. And A's own refusal half was **broken for one afternoon and caught by its golden**: recording the expectation unconditionally made `s: str @ nullptr` compile, which is why `adopts` is asked rather than assumed | CLAUDE.md §7, §11, §12 · design.md §1.11, §4.19, §4.20 | 021, 042, 048, 051, 052, 053 |
2026-08-14 | **Eleven compilers measured against one instrument, and CLAUDE.md §6's Nim figure stops being an assumption** (`docs/measurements/008`; panel 005's side finding, discharged). The author asked how the bootstrap compares in size, so eleven trees were cloned and counted rather than recalled — the same `wc`/`awk` split over all of them, exclusions declared per tree. Heroes is **fourth from the bottom at ~30,100 non-test lines**, above harec (20,446) and below tinycc (68,619). Nim's `compiler/` measures **113,168** in 175 files, with the stdlib a separate 133,427, against the "~150k" the historian could not verify at panel 005; the remembered number looks like the two conflated. §6's rule — copy the surface, never the implementation — depends on no magnitude, so it stands unchanged and **amending its parenthetical is the author's call** | **the anomaly the table exposes is not size but the comment ratio: 0.51, against a 0.15 median, Go's 0.27 as runner-up, and Zig and harec at 0.04.** §11's *code is written to be read* is this codebase's most unusual property measured against its peers, and it is the only one of the three things the extra lines buy that the table can see; `types/` outweighing `emit/` is the second (a compiler whose type checker beats its code generator has decided errors matter more than output, which is §4.17 as a line count), and diagnostics-with-fixes plus 163 goldens are the third, invisible because no other tree carries the equivalent to compare against. **The uncomfortable comparison is tinycc**: 68k for *all* of C with x86-64/ARM/RISC-V backends and its own linker, against 30k for a subset that delegates every backend question to clang — per unit of language covered this compiler is expensive, and the number that decides whether that was worth it is not in the table but at M-selfhost-fixpoint, where design.md's 5–8k port estimate meets 18,700 lines of Rust logic. Two circulating figures are also refuted: Go's `cmd/compile` is 630,247 non-test lines of which **440,108 are generated**, a human wrote 190,139; and rustc's 861,366 is 78 crates, no one of which is a compiler | CLAUDE.md §6, §11 · design.md §1.0, §4.17 | 005, 032 |
2026-08-14 | **`==` on a `cstr` compares addresses, and the spec said it compares values** (`docs/panel/053`'s spec-warden, landed as a §1.0 truth repair, +17). Measured: `a = "abc"`, `b = "ab" + "c"`, then `a == b` is **true** and `a.cstr() == b.cstr()` is **false**, at exit 0 with no diagnostic. The sentence read *"structural equality on everything"*. That is panel 048's exact class — a spec describing something the compiler does not do — arriving in the **silent** direction: it compiles, it runs, and it answers wrong | **which side has the bug was decided by the robustness rule, not by taste.** §12 says the spec beats the compiler, so the repair could have gone either way — make `==` on a `cstr` a `strcmp`, or say that a pointer compares as a pointer. A structural `==` would have to **dereference**, so a null `cstr` compared structurally is exactly the crash CLAUDE.md §12 now forbids, while an address comparison is total and is the question `v == nullptr` actually asks. The false sentence became load-bearing the same day: panel 053 landed `nullptr` taking its context's type, so the operator a reader checks nullability *with* was described wrongly in the one document that describes it. Paid as compiler-need with no removal and no prediction, on row `2983`'s precedent | CLAUDE.md §12 · design.md §4.19 · spec § Types | 048, 053 |
2026-08-14 | **Clause X returns, at the wording that is true** (author decision, `/decide` item 6; `docs/panel/048`). Panel 048 refused *"the headers are read as a POSIX C compiler presents them"* because its ffi-pragmatist compiled 30 probes across four libcs and found it **false on glibc**: `strptime`, `wcswidth`, `wcwidth` and `swab` are declared on Darwin, musl and FreeBSD and hidden on glibc under the ratified `gnu11`, because glibc's default is `_DEFAULT_SOURCE` while musl's `features.h` sets `_XOPEN_SOURCE 700` under the same flag — and `arc4random` is gated on `__GLIBC_MINOR__ >= 36`, so the visible set moves with the libc **version**. What lands is that judge's own counter-wording, the only version true 4-for-4: *"A header shows more than ISO C's names — `M_PI`, `strdup` and `fileno` are usually there. How much more is the platform's answer, not this language's."* **+45, against panel 052's R2 removal at −12, net +33** | **the repair is that it states a floor instead of an identity**, and that distinction is the whole difference between a sentence a reader can rely on and one that is wrong on the platform with the largest installed base. The removal was verified unspent and is spent here: `to_i64` appeared **twice in one line** of Built-ins and its truncation is already stated in § Types, so the list was saying a thing the rules had already said. Panel 051's separate −8 stays **unspent**, on panel 053's warden's advice rather than for want of a use — a blind reader quoted that very sentence as the only cost claim in the document, and spending another sitting's live measurement is what panel 041 refused. Registered under R1 **as amended earlier today**: the instrument must have an arm for the question | CLAUDE.md §12 · design.md §1.6, §1.11 · spec § FFI | 041, 046, 048, 052 |
2026-08-14 | **The four lapsed clauses are re-decided, not renewed** (author decision, `/decide` item 4). `2588` (+28), `2745` (+70), `2768` (+23) and `2959` (+191) were each bought with a prediction naming **metric 2**, which waits for the fixpoint, and panel 046 R2 forbids a renewal that differs from its registration only in a date or a milestone name. So each is re-registered with a **different claim and a different answerer**: an `extern constant` count beside a grep for the same names spelled as literals; the corpus's 44 non-decimal literals holding at ≥44 with no decimal mask; `int` staying at 0 while `i64` stays ≥374; and — the one that matters — **≥40 of the corpus's 50 narrow-width annotations sitting at an FFI boundary**, each of which would be a silent truncation without the widths | **`2959`'s re-registration is the first time its question has had an instrument, and that is R1's second half arriving on its own.** The original prediction named metric 2 because on the day it was written nothing else could see the widths at all; `ffi_parameter_type`, built at M-binding-fidelity **because of** the widths, can see them now — so the re-registration is not a rewording but a question becoming answerable. Three of the four are kept as §1.0 compiler-need with a falsifier attached, which is what `DECIDE.md` had said the honest starting position was; `2588` is the one that is not, and its prediction is written to be able to fail. **And the clippy pin is settled in the same session**: the upstream search found no report of the class, and found instead that current `alloc` builds `vec!` through `Box` deliberately (`write_box_via_move`), so the lint is reading a real `Box` and it is **this project's rule that is too broad** rather than clippy that is wrong | CLAUDE.md §5, §12 · design.md §1.6 | 012, 040, 041, 046, 051, 052 |
2026-08-14 | **The abort flake has a cause, and the cause was fixed before it was diagnosed** (author decision, `/decide` item 10 — *chase it now*). `tests/golden/run/abort-slice-splits-a-character.hero` failed once on Linux at `-O2`, printing nothing where `caff\n` was expected, and never again. Three checks: every abort path in the runtime **flushes stdout** before writing to stderr and calling `abort()`, so buffering is out; 1000 runs of the `-O2` binary on Darwin — through a pipe, through a file, and across eight parallel workers — gave an empty stdout zero times; and the timestamps settle it. Compile-beside-and-rename for the **binary** landed at 08-13 **23:32**, and the seven CI runs of that day ran between **22:54 and 23:28** | **so runs 3 and 4 executed on a tree that still wrote the binary in place, while `cargo test` runs its binaries in parallel.** One process truncating a binary another is about to exec gives a program that starts and prints nothing — the symptom exactly — and the `-O2` leg has its own cache path and therefore its own race window. The fix was made a day later for an unrelated reason (Windows refuses to rename over a running executable), which is the second time this week a repair has landed ahead of the diagnosis it answers. It gets **no case of its own**: a test for two processes racing on one path is not deterministic, and what it would pin is already asserted in the `publish` helper's doc. The diagnosis carries its own falsifier — a recurrence after 23:32 makes it wrong | CLAUDE.md §9, §12 · design.md §4.14 | 021 |
2026-08-14 | **Robustness and a complete C boundary become a principle, design.md §1.12** (author instruction: *"metti nei principi del progetto la robustezza, il fatto che possa chiamare completamente qualsiasi programma C, e che sia molto robusto, quindi anche difensivo"*). It sits beside §1.11 because it is that constraint's **consequence** and not a separate wish: if everything a program touches comes from C, the C boundary is where this language's guarantees end, and a guarantee that ends quietly is not one. Two halves. *A Heroes program must not segfault and must not corrupt memory* — a goal of the **language**, not a quality of its implementation. And the boundary is **complete**: any C library must be bindable, because a library Heroes cannot reach is C code the author has to keep writing by hand, which is §1.11 failing at the one thing it exists to do. §1.1's hierarchy gains a fourth force above tokens; CLAUDE.md §12 keeps the operational rule and now cites §1.12 as its argument | **no panel, and the reason is worth stating**: a panel judges proposals *against* the project's goals and cannot judge the goals themselves — Principle 0 is an author decision for the same reason. What the principle does **not** do is suspend §1.0: *"it would be safer"* is not an entry ticket any more than *"it would be elegant"*, and what §1.12 decides is the shape of a form already admitted. The **defensive** half is the one that costs: the compiler checks at the boundary even where the value is known to be good — a `cstr` from `s.cstr()` cannot be null and is null-checked anyway — because the alternative reasons about where a value came from, and provenance is a premise about the world (§11). The precedent is this project's own §7, which already makes arithmetic abort through `__builtin_*_overflow` rather than reach C's undefined behaviour; §1.12 states that obligation as a principle so the next boundary needing one is not argued from scratch. It carries its own falsifier: a measured case where a defensive check **hides** a defect rather than surfacing it | CLAUDE.md §7, §11, §12 · design.md §1.0, §1.1, §1.11, §1.12 | 039, 052, 053 |
2026-08-14 | **`repeat(s: str, n: u64)`, and a cost clause that finally names an exit** (`docs/panel/054`, provisional — author ratification pending; four judges reporting, one stopped). `docs/measurements/007` measured a blind reader avoiding the quadratic `+`, naming the clause that stopped it, and then writing `push`-in-a-loop in **two of three** programs — because the clause names `join` as the linear escape and `push`, the only way to build the array `join` needs, as the trap. **The escape hatch and the trap were the same sentence.** The rule adopted: a cost claim is admissible only where the same paragraph names a construct the reader can reach for. +14 against the sitting's own −5 removal, net **+9**; the runtime primitive is 13 lines and adds no allocation site | **the count is a `u64`, and that is the decision.** Three answers were on the table for a negative count — return `""`, consistent with `range(from: 0, to: -1)`; abort at runtime; or refuse it in the type — and design.md §1.12, three hours old, is what chose the third: *a defensive check must surface a defect, not hide it*. Returning `""` hides the subtraction that went negative, aborting names `repeat` rather than the subtraction, and a `u64` puts the diagnostic on the value that is wrong. Go takes a signed count and oh-my-posh #4135 is the shipped crash that follows, from a renderer's padding subtraction — which is exactly what `repeat(" ", col)` is. **And the multiply had to be guarded by a division**: `4 * (2^62+2)` wraps *positive to 8*, so eight bytes are allocated and 2^64 copied — exit 138, SIGBUS, no message — while `hero_str_alloc`'s `len < 0` catches only the half that wraps negative. Rust shipped that class as CVE-2018-1000810. §4.20 gains one normative sentence so the next primitive does not rediscover it. **`repeat` is not compiler-need and the record says so**: two judges checked the port's own diagnostic renderer and found its padding is a tab-preserving per-character map, not `repeat(" ", col)` — writing it as `repeat` would re-break a guarded invariant. It is here for the banner, the one program with no route. **The corpus had already written it the slow way**, in `examples/markdown/render.hero`: `push` in a loop, then `join`, exactly the program the measurement predicted | CLAUDE.md §9, §12, §13 · design.md §1.0, §1.12, §4.20 · spec § Strings | 037, 041, 043, 046, 048, 054 |
2026-08-14 | **A group head names what the machine has, it does not say where this machine keeps it** (`docs/panel/055`, provisional — author ratification pending; five judges, all building in copies). The sitting **inverted twice, both times on a measurement rather than an argument**. It was convened to decide whether to *add* a way to name a search path, and found one **already there**: `extern "/opt/foo/include/foo.h"` compiled and ran, because the string goes to `#include` and C accepts a path — a slot **inherited** from Nim's `header` pragma, which CLAUDE.md §6 says to copy, so §6 worked and bit in one act. `link` had the worse version: GNU `ld` reads `-l:filename` as a *file*. Then it found the language's other door shut by this project's own hand — **SDL2 was bindable through neither clause**, `package` refusing `-D_THREAD_SAFE` and `link` unable to find the header, each diagnostic sending the author to the other | **panel 050 cited Go's allow-list and adopted one narrower than Go's**, which documents `-D`, `-U`, `-I`, `-l`: measured, **28 of 285 `.pc` files (10%)** on one machine answer with a flag it rejected — `sdl2`, `ncurses`, `readline`, `x264`, all twelve `Qt6*`. `-D` and `-U` join it, on the property the list is actually about: neither names a file, loads anything or writes anything. `-Wl,-framework,<name>` joins in **that exact form only**, because it is `-framework X` spelled the long way and four packages of 285 need it; a blanket `-Wl,` would pass anything to the linker. **A is refused on four independent grounds**: +222/−34 across 14 files reaching the **lexer**, where it must evict `include`'s reserved word and its `certain` fix; `heroes fmt` would **merge** two groups with different paths, the defect class M-program-corpus already paid for; **219 of 273** `.pc` include answers embed a version number, so a path in a source file is stale at the next upgrade; and it is the shape that can **skew** — `CPATH=v1/include LIBRARY_PATH=v2/lib` compiles clean, links clean, exits 0 and prints the wrong number, which under §1.12 loses to `package`, the shape that cannot. **C is queued rather than refused** and the split is recorded: §10's composition test says *nothing* — `CPATH`, `LIBRARY_PATH` and `PKG_CONFIG_PATH` are all live because `Command` inherits the environment — while the judge who built it says an environment variable is not an artifact a program can carry. And design.md:2100 is repaired twice over: its premise expired at panel 050, and its **validation rule is incomplete**, because clang expands `@file` before parsing options and a `-` check cannot see one | CLAUDE.md §6, §9, §10, §11, §12 · design.md §1.7, §1.12, §4.19 · spec § FFI, § Operators | 036, 048, 049, 050, 054, 055 |
2026-08-14 | **Panels 053, 054 and 055 ratified together, as they stand** (author decision). Three sittings in one day, every provisional default already in the tree and green on three platforms when the yes arrived. Nothing in any resolution changes: 053's call-site null guard and context-typed `nullptr` stay landed with B vetoed and C refused; 054's `repeat(s: str, n: u64)` stays with its division-guarded multiply and the cost clause repaired rather than deleted; 055's refusal of a path clause stays, with `-D`/`-U` on the allow-list, `machine_locked_path`, and `ffi_missing_library` as §7's sixth named exception | **what the three settle together is a pattern none settles alone: a sitting may discover its own question was the wrong one, and that is the result rather than the failure.** 053 was convened about a nullable inbound `cstr` and found its premise false — `to_str` on a null already aborted cleanly — while the real crash was a null handed **straight back to C**, which three of its four options could not see; the answer was on nobody's ballot. 054 was convened to add `repeat` and found the clause that motivated it had a defect no rule covered: it named `join` as the linear escape and `push`, the only way to build the array `join` needs, as the trap — **the escape hatch and the trap were the same sentence**. 055 was convened to add a search-path clause and found one **already present and unnamed**, inherited from Nim's `header` pragma that §6 says to copy, and then found the language's other door shut by this project's own allow-list, which cited Go's precedent and adopted a narrower list than Go's. **design.md §1.12 decided a question in each of the three within hours of being written** — the `u64` count, the call-site guard, and `package` as the shape that cannot skew — which is the strongest evidence a principle can give that it earns its place | CLAUDE.md §4, §6, §7, §10, §11, §12 · design.md §1.0, §1.7, §1.11, §1.12, §4.19, §4.20 | 043, 046, 048, 049, 050, 051, 052, 053, 054, 055 |
2026-08-14 | **`--include` and `--library` land, settling the split panel 055 recorded** (author decision, `/decide`). Three judges held that CLAUDE.md §10's stopping rule yields *nothing* — `CPATH`, `C_INCLUDE_PATH`, `LIBRARY_PATH` and `PKG_CONFIG_PATH` are all live because `Command` inherits the environment, measured four ways — and one had **built** them: +105/−6, 75 code lines, all in `heroes-cli`, **zero spec tokens**, on the argument that an environment variable is *not an artifact a program can carry*, so a model asked for a program that binds SDL2 produces something that cannot be built and the missing half is an unversioned shell line. The author's yes buys that half | **three conditions were measured before the yes and all three are in the commit.** (1) The paths are **in the cache key**: the judge who built it found its own prototype serving one artifact to two configurations, because `dir_for` hashed the source, the level, the sanitiser and the runtime and nothing else — the same silent staleness the level key exists to prevent, through a new door. (2) The flags are **repeatable**, and `cli.rs` used to drop a repeat *silently*, which for `-o` is right and for a search path is a **wrong build** rather than a failed one (§1.12). (3) **No allow-list**, and the reason is written as a falsifiable claim with a test that fires when it dies: panel 050's list exists because a `.pc` hands back one string split on whitespace and read as flags — the splitting **is** the vector — while a search path is one argv word clang consumes as a path, with no shell, a parser that refuses a `-`-leading value, and a clang that reports *"ignoring nonexistent directory"* rather than obeying. `a_search_path_reaches_clang_as_one_argv_word` checks all three. What the yes costs is on the record too: `-I` and `-L` named **independently** are the only way to produce panel 055's measured skew — header v1, library v2, exit 0, wrong number — so `package` stays the shape to reach for first | CLAUDE.md §5, §10, §11, §12 · design.md §1.12 · docs/panel/055 | 050, 055 |
2026-08-14 | **`M-struct-passing` becomes the next milestone, ahead of `M-selfhost-probe`** (author decision, `/decide`). **356 of 1159 entry points (31%)** across `sqlite3.h`, `curl/curl.h` and `raylib.h` pass or return a struct by value; raylib alone is **346 of 600**; and §4.19's ladder rung 5 promises *"real struct passing"* in ratified text that the language cannot keep. Today the wall is **honest rather than silent** — `error[ffi_type]: \`Vec2\` cannot cross the FFI boundary` at exit 1 — so nobody gets a wrong program, they get a *cannot* | **Principle 0 does not fund it and the ROADMAP row says so.** The closure list binds only `hero_os.h`, which has no structs, so §1.0's first branch is empty; it enters on design.md §1.12's completeness clause — *any C library must be bindable, because one Heroes cannot reach is C code the author keeps writing by hand* — which is the author's own principle of this morning doing the work its second half was written for. The order is the author's over the assistant's reading, which put the probe first on Principle 0's silence; what the yes settles is that **a third of the boundary being unreachable outranks measuring the port a milestone earlier**. The name follows §14: two words, the deliverable named, and the FFI *area* left free for the milestone after | CLAUDE.md §14 · design.md §1.0, §1.12, §4.19 · docs/ROADMAP.md § the order table | 049, 052, 055 |
2026-08-14 | **The corpus gets a timeout, `-Wl,-rpath,<dir>` joins the allow-list, and the SDL example is SDL3** (author instruction: *"usa la SDL 2???? DUE"*, which was the right question). `examples/sdl/` bound SDL2 and could not be a corpus program for two reasons neither of which was a defect in the program: `pkg-config --libs sdl2` answers `-lSDL2main`, and **`SDL2main` replaces the program's `main`** with a Cocoa one, so eleven corpus programs in three configurations each became windowed applications and filled the author's dock; and that machine's `libSDL2` is **`sdl2-compat`**, a shim that loads SDL3 at run time and, failing, opens a **modal dialog** and waits for a click — two processes at 0% CPU for ninety minutes. **SDL3 has neither**: the `SDL3main` library does not exist and there is no shim. The corpus runs the SDL3 example in **30 seconds** | **the general repair is the timeout, and it is the one that matters**: two minutes, chosen against the slowest thing the corpus actually does, so it is the line between *slow* and *never* rather than a performance budget in disguise (§13). A hang on an unattended harness burns the whole job where a failure costs one case, and the message it panics with names the likely cause — a C library waiting on a window, a display or a dialog the machine cannot show. **And the allow-list was asked the same question twice in one hour.** Its property test is *does this word name a file, load anything, or write anything* — `SDL2main` does none of the three and replaces the program's entry point; `-Wl,-rpath,<dir>` does none of the three and changes what the loader picks. Both act at **run** time, and the test is about **build** time. The rpath is allowed in that exact form because SDL3's own `.pc` needs it, with its escalation written in the code rather than waved past: a hostile `.pc` saying `-L/tmp/x -lfoo` already chooses the library, so this is not a new capability — but it lets the choice be swapped **after** the build by anyone who can write to that directory. The question itself is queued, because it is one question and not two | CLAUDE.md §9, §12, §13 · design.md §1.12, §4.19 | 050, 055 |
2026-08-15 | **A project file is examined and deliberately *unplaced* — neither Part 6 nor Part 7** (panel 056, provisional, author ratification pending). Convened from `docs/reasoning/004` to decide *whether* Heroes should have a `Cargo.toml`-shaped artifact; it found the question is not whether the file exists but **what it is allowed to say**. **A refused** — one veto, four objections: the falsifier a Part 6 row owes is constructible the day `heroes add` exists, and §3.5 already promises that verb, so a document cannot permanently reject the object of a verb it commits to shipping. **C refused** — two vetoes (spec-warden, ffi-pragmatist), on §1.12 rather than on cost, the compiler-engineer explicitly declining to inflate its own seat (*"§1.7 does not carry an objection here and I will not pretend it does"*, at a measured +247/−13 across 8 files and **0 lines** in `crates/heroes/src/`). **B adopted**, zero spec tokens, with **D** as its companion: the return conditions live in the ROADMAP's M-package-manager entry, not Part 7, and are re-decided rather than renewed | **Three judges converged on one rule from three unshared inputs**: the file may say *where to look*, never *what to find*, and never in a shape that can disagree with itself — the ergonomist from the spec alone (no string in it may also appear in a `.hero`; a bare `extern "H"` is *already* a valid program meaning "these symbols need no library", so a stanza carrying `link` repurposes a well-formed construct instead of adding syntax), the engineer from the tree (**one key, not two** — a `prefix <dir>` deriving both `-I` and `-L` makes the skew unrepresentable at six extra lines), the pragmatist from C that compiles (record the **question**, never the **answer**). And Principle 0's burden went undischarged: **no library on this machine is bindable with a project file and not without**, giflib included — no `.pc`, header off the default path, bound by `link` + `--include`, exit 0. What the file adds over the flag is durability, and that is the objection: DESIGN-LOG:298 accepted the skew for the flags with the mitigation *"`package` stays the shape to reach for first"*, which is **a person, typing, per invocation** — a project file deletes the person | §1.0, §1.3, §1.11, §1.12, §3.5, §12 · CLAUDE.md §9, §10, §11 | 056, and 039's precedent for *unplaced*
2026-08-15 | **Panels 014, 039 and 056 ratified in one blanket author instruction**, and four stale records repaired in the same session (author instruction, `/decide`: *"le ratifiche ratifica tutto per me"*). The three were what a full scan of `docs/panel/` found genuinely open — 014 (an arm's body is one statement, inline, or an indented block; outside the 2026-08-12 blanket's `013, 019–034` range by how that range was written rather than by any judgement, and the shipped rule since M4), 039 (comptime **examined and unplaced**, the Macros row's reason narrowed, the word made greppable) and 056 (a project file: B adopted with D, A refused, C refused on two vetoes). The ordering is the finding: **056 stands on 039's precedent**, so ratifying 056 alone would have left a decision leaning on a provisional one. Every other file in `docs/panel/` was verified as already carrying a verdict, a `DECIDED` status, or a retro-record | the blanket's limit is the one the 2026-08-12 instruction recorded and it is restated in each of the three files rather than assumed: a yes settles that the provisional resolution **is** the decision and that work no longer runs on it as a default; it settles nothing keyed to a measurement nobody has taken, and eight of the three sittings' predictions name instruments that do not exist. The repairs are the same rule pointed at the record: `ROADMAP` § Status was two milestones behind, `panel/020`:5 said *pending* against its own § Ratification, and **CLAUDE.md §7 said `panel 047, ratification pending` while carrying a flag list four flags shorter than `FLAGS`** — a contract stating a fact about the compiler that the compiler had stopped agreeing with | CLAUDE.md §4, §14 | 014, 039, 056 |
2026-08-15 | **A C width stops being a constant: `long` and `unsigned long` are derived from the target, and `malloc` stops blaming the compiler** (author decision, `/decide`). `extern function malloc(size: i64) -> ptr` — the first thing anybody binds — exited **2** under *"internal error: compiling the generated C failed"* with raw clang output and a path under `build/<hash>/`, because `emit/ffi_narrowed.rs` mapped C types to Heroes widths through a **table** and `long`/`unsigned long` were deliberately absent: they are 64 bits on Darwin and Linux and 32 on Windows, so no tabled string is right on three legs. That correction was right about the table and wrong about the question — a missing row makes the class decline, and declining hands the failure to §7's exit-2 rule | the answer is that a width is not a constant, it is a question about the target: `word_width_bits()` reads `size_of::<c_ulong>()`, Rust's name for the same C type clang is about to compile, on the same machine for the same target, which is a fact about the value in hand rather than a premise about which platforms exist (CLAUDE.md §11). A **second note** carries the portability half, because without it the diagnostic would tell a reader on Windows to write `u64` for a 32-bit parameter — the silent widening the class exists to prevent, arriving through the fix. The premise that this compiler and clang agree is asserted by `the_word_width_this_compiler_claims_is_the_one_clang_uses`, which goes red **in the milestone that adds a `--target` flag** | CLAUDE.md §7, §11 · design.md §4.19 | 056 |
2026-08-15 | **A search path that names no directory is refused at the argv layer** (author decision, `/decide`; panel 056's finding). `clang -Wall -I/no/such/dir` says **nothing at all**, so an invocation pinning one machine's prefix and committed to a script or a CI file builds clean on another machine **against a different header than the declarations were checked against** — §4.19's oracle swapped rather than weakened, and the half of panel 055's skew that survives being committed. Exit **2**, §10's *a bad flag*, before clang is invoked | three routes were measured and two refused. `-Werror=missing-include-dirs` exits 2 through §7's *"the compiler is wrong"* path on a program with **no `extern` at all**; a fifth exit-1 class would break §7's narrowing, which is that every exit-1 class recovers a name *this program declared* `extern`, and a directory is not a declaration. **The constructor's name was half the defect**: `repeated` made repeatability the identity of a search path and its being a directory invisible. It is `search_path` now, and both properties follow from the thing it names. What this does not reach is `CPATH`, and that is the same line panel 055 drew adopting these flags — an environment variable is the machine's configuration, a flag is the artifact a program carries | CLAUDE.md §7, §10 · design.md §4.19 | 055, 056 |
2026-08-15 | **The library allow-list is not extended to what a library does at run time** (author decision, `/decide`, closing panel 055's open finding). `pkg-config --libs sdl2` answers `-lSDL2main`, and `SDL2main` **replaces the program's `main`** with one that initialises Cocoa — eleven corpus programs in three configurations each became windowed applications. The flag names no file, loads nothing and writes nothing at build time, so every question the list asks returns *safe*, correctly | the list is not weak on this class, it is **blind** to it: what a library does after linking is not a property of the flag string and no allow-list reading flags can recover it. That is CLAUDE.md §7's argument about the ABI stamp and a decoy, in a second place — a guard is not widened to cover a class it cannot see, because the widening buys nothing and makes the guard read as though it did. Refusing `*main` by name is refused on §11: a rule keyed on a naming convention is a premise about the world. **Falsifier** (§12, a refusal carries a feature's burden): a library whose linked-in behaviour breaks a corpus program *and* that the corpus timeout does not catch | CLAUDE.md §7, §11, §12 | 050, 055 |
2026-08-15 | **CLAUDE.md §10 gains no fourth slot, because there is no fourth input class** (author decision, `/decide`; recording what panel 056 adopted). The three shapes — subcommand, flag, nothing — all answer *what does this capability do to the inputs the tool already has*: a `.hero` file, argv, the environment. A per-project file is none of them; it is a new **input class**, and §10 as written does not admit one | this is the argument that makes panel 056's B permanent rather than provisional, and it refuses a project file more cleanly than design.md Part 6 could — 056 refused the Part 6 row because the falsifier such a row owes becomes constructible the day `heroes add` exists, which §3.5 already promises. The refusal is **conditional**: 056's three return conditions are its only amendment path. And §9's `#~` gap is **evidence for the rule rather than a second job** — the golden harness collects `.hero` only, so a new input class would arrive with its diagnostics exempt from the one invariant a regenerator cannot forge, which panel 020's historian refused for `unsupported` | CLAUDE.md §9, §10 · design.md §3.5 | 020, 056 |
2026-08-15 | **CLAUDE.md §6's *"a separate package binary: never"* is retired** (author decision, `/decide`; panel 056's historian). One citation, no argument, and its implied contrast was **factually wrong** — nimble ships *with* Nim, so the thing it named as separate is not | it was a **duplicate of §10** (*never a second binary*) carrying a false premise, and the duplication is what let the premise survive: nobody rereads a rule that appears confirmed elsewhere. The second failure is worse than the first — it is the rule a reader reaches for first to refuse a project file, and it does not reach one, because **a file is not a binary**. §10 keeps the weight and gains the corrected sentence with `heroes add`/`heroes fetch` named as subcommands (design.md:637). The record is corrected, not deleted (§14) | CLAUDE.md §6, §10 | 056 |
2026-08-15 | **A reasoning note may accompany a panel when it holds what the panel file does not** (author decision, `/decide`, from `docs/reasoning/004`). `docs/reasoning/README.md` said a `/panel` session does not qualify, *"because each already has its artifact… and recording it twice is noise"*. Session 004 produced both and they hold different things: the note holds the nine-job inventory and the two-decisions-in-one-day tension that *produced* the proposal, the panel file holds the proposal and the verdict | the exception is bounded by the reason for the rule rather than by the shape of the session: the note must hold **neither the proposal nor the verdict**, and the test is mechanical and reads the two artifacts rather than the session's intent — if the note can be deleted without losing anything the panel file does not already say, it should not have been written. The rule's reason was never *one session, one artifact*; it was *do not record the same thing twice* | docs/reasoning/README.md | 056 |
2026-08-15 | **`/panel` step 3 gains `rm -rf target build`, and the working-tree freeze binds the coordinator** (author decision, `/decide`; panel 056's process notes). A copied `target/` leaves `env!("CARGO_MANIFEST_DIR")` pointing at the **real** repository, so a judge's golden run silently measures the tree it was told not to touch; panel 056's compiler-engineer found it in its own first run, discarded that run and rebuilt, which is the only reason it is known rather than a wrong number inside a verdict | **the copy is not sufficient on its own and this is the second payment for the same lesson** — panel 054 bought the copy rule, and 056 found what the copy leaves behind. The second half is the one that is easy to file as a postscript: the coordinator put `-Werror=missing-include-dirs` into `FLAGS` between 23:37 and 23:45 while judges were measuring, and one observed the effect from inside its own run. Nothing landed wrong, but a rule that binds judges and not the seat that convened them has its hole exactly where the most privileged actor stands. The tree is frozen from the briefs going out until the synthesis is written | .claude/skills/panel/SKILL.md · CLAUDE.md §4 | 054, 056 |
2026-08-15 | **The path pair is refused, and the proposal that lost was the coordinator's own** (`docs/panel/057`, provisional — author ratification pending; five judges, all building in copies, working tree frozen for the sitting). **Three vetoes from three sections and no seat in favour of the pair.** Principle 0 is the ground: `path_join` was conceded to have no closure-list claim, and *"the inverse of the function that does"* is the elegance argument §1.0 exists to refuse — while `path_parent`'s claim was `directory_text`, **and it does not compute it**, measured twice. What killed it was execution rather than reading. The llm-ergonomist, blind on the spec, wrote `path_join(path_parent(a[0]), "defaults.txt")` as its first draft and produced **`/defaults.txt`** for a bare filename — an absolute path at the filesystem root, compiling and type-checking — while **its program written with no built-in at all was correct**, because writing the scan by hand made it return `"."`. The compiler-engineer executed the pair and got `examples\calculator/lex.hero`, one directory spelled two ways, which is the defect the third CI leg found on 2026-08-14 and which was fixed by *not joining* | **the `\`-as-separator clause is refused outright and that half has a falsifier that already fired**: the ffi-pragmatist ran §4.19 rung 3 against a real POSIX directory named `back\` and opened **two different databases**, both `SQLITE_OK`, no diagnostic anywhere — §1.12's class, a correctly-formed string naming a different object, handed to every path-taking C function the language reaches. The historian sourced the same hazard shipped in both directions (filebrowser GHSA-83xp-526h-j3ww manufactured a POSIX traversal by rewriting `\`; Git's CVE-2019-1354 fix then broke repositories holding legal backslash names), and Zig and Node both split POSIX from Windows for it. **The C alternative is closed on §12**: `dirname(3)` is `char *` on all four libcs checked, faults `SIGSEGV` on a literal's read-only `.cstr()`, and `libgen.h` does not exist under `-target x86_64-windows-msvc`. **§1.11 had already settled the class** — a `T?`-returning edge is Tier 2, not a built-in — and the pragmatist reproduced the argument by failing to write `path_parent -> str?` as a runtime entry point at all. Nothing lands; the port writes the scan once in `modules/` at 0 spec tokens, and the Tier-2 route is measured at **zero compiler lines, zero runtime lines, no ABI bump** and left available | design.md §1.0, §1.11, §1.12 · CLAUDE.md §11, §12 | 036, 053, 057 |
2026-08-15 | **Panel 057 ratified as it stands** (author instruction, *"io ratifico tutto"*, given after reading the synthesis rather than clause by clause — the third blanket ratification of the day and recorded as that rather than as five individual reviews). The refusal holds in full: neither built-in enters, the port writes the scan once in `modules/` at 0 spec tokens, the `\`-as-separator clause is refused outright, the Tier-2 route stays measured at zero compiler cost and available | **a yes to a refusal is not an answer to the questions the refusal raised**, and three are named in the panel file so they cannot be read as settled: `path_dir` — up to and including the last separator, `""` on absence, +41 — was never on the ballot and is **not** refused by this ratification, so path work that ever returns starts there rather than from the pair; the `.cstr()`-handed-to-a-writing-C-function hazard is about the FFI boundary and is queued separately; and `directory_text` still carries the refused premise, which makes it the tree's one instance of a premise a panel has ruled against. The limit is the 2026-08-12 blanket's limit and it binds harder here than usual — **four of the five predictions name `M-selfhost-probe` or a model harness that does not exist**, and R2's *re-decided, never renewed* governs them unchanged. The one to watch is the llm-ergonomist's, because it carries a counter-intuitive clause its own author offered as the falsifier: that the language with no built-in beats both proposed variants | CLAUDE.md §4 · design.md §1.0, §1.11, §1.12 | 057 |
2026-08-15 | **`directory_text` keeps the `\` premise panel 057 refused everywhere else, written as a claim that can die** (author decision, `/decide`). The premise is real and the counterexample was created on this machine before the decision: `a\b.hero` is **one legal file** on Darwin and Linux, and this function reports its directory as `a\`, which does not exist. It is kept because a path reaching it was **typed by the author** on the command line and may use the Windows separator whatever the platform, and spelling a sibling module the way the author spelled the root is worth more than that case | **what makes it survivable is scope, not luck, and scope is what the test pins**: nothing opens a file with this result — `load_text` reaches the filesystem through `PathBuf::parent`, which is separator-correct, and this text reaches only `InputFile::name`, what a diagnostic prints. `a_backslash_name_is_wrong_here_and_right_where_files_are_opened` asserts both halves *and* that they disagree, so it fires the day the wrong answer reaches a real path — which is the only way the cost stops being cosmetic. The third option this item offered, *record an exemption because the display-name case is harmless*, was **struck by the measurement**: the printed name is wrong when the file exists, so there is nothing harmless to record | CLAUDE.md §11 · crates/heroes/src/modules/mod.rs | 057 |
2026-08-15 | **§11's per-file ceiling becomes a test, and it went red on four files the moment it existed** (author decision, `/decide`). The 2026-08-12 sweep took the tree to zero breaches in one run; ordinary work put **four** files back over it within three days, unnoticed, because nothing was watching. `crates/heroes-cli/tests/layout.rs` walks `crates/`, excludes tests **by where they sit** rather than by a name list, and carries a `DECIDED` table whose load-bearing column is the **reason** — a bare exception list decays into the breach list it replaces | it is a **test** and not a `/step` checklist line because a checklist is discipline and discipline is what already failed twice, and not a `heroes` subcommand because §10's stopping rule says *nothing if two existing invocations already compose to it* and `find crates -name '*.rs' \| xargs wc -l \| sort -rn` is that composition — what a subcommand would add is not the measurement but the **obligation**, and an obligation is what a test already is. Same shape as `emit/tests/gate.rs` and `clippy.toml`. **An entry in `DECIDED` is a ceiling, not a licence**, which is how `toolchain.rs` at **425** against a 400 settled by name became visible at all. Four cuts, each along a seam that names a concern: `emit/unread.rs` (what the emitted C **reads** — `inst.rs`'s per-op half and `body.rs`'s per-function half, one question in two places), `syntax/extern_members.rs` (what a group **declares**, against `externs.rs`'s what it **names**), `commands/flags.rs` (what clang **enforces**, which knows no path — deliberately *not* the cache-key seam the author's 2026-08-14 decision refused). Emitted C **byte-identical over 66 goldens** at every step (`f990db898b5a`) | CLAUDE.md §11, §10 · design.md §1.6 | — |
2026-08-15 | **The outbound mutable `cstr`: the flag is adopted under five conditions, unshare and a new spelling are vetoed twice each** (`docs/panel/058`, provisional — author ratification pending; five judges, four building in copies, working tree frozen). `.cstr()` is zero-copy over a **refcounted copy-on-write** buffer (§4.20), so a C function writing through it mutates values never handed to C — `b = a` then `strtok(a.cstr(), …)` changes both at exit 0, and a literal's `.cstr()` is `exit 138, SIGBUS`. **(ii) unshare-or-copy is vetoed on a measurement**: `--dump-ir` shows the ownership pass's `$own` slot already holds a second reference, so every heap `str` has refcount 2 at a call and *unshare-if-shared* copies at **100%** of call sites — §4.10's own recorded refutation of in-place `push`, and it deletes §4.20's *"single highest-return decision"*; it also does not fix the unique case, where `len` still lies. The historian supplied the reason from two languages of Heroes' exact shape: CPython and Erlang license a foreign write on **provenance** (*"just created"*, *"only mutable after `enif_alloc_binary`"*), never on uniqueness. **(iii) a mutable-buffer spelling is vetoed as Part 7 item 10 verbatim** under §13, priced at 400–600 lines across 13 non-test modules — with the historian's dissent recorded: four ecosystems converged on a separate type, and OCaml's `bytes` took 2014→2017 to land, so the disagreement is about the **clock** and not the destination | **the sitting's own framing was wrong twice.** The two spec clauses on the ballot cost +32 and +55 measured; the sentence saying the same thing — *"`s.cstr()` **lends** a `str` to C to read"* — costs **+2**, and the expensive one is unsafe, because *"may read and never write"* **licenses** the C function that keeps the pointer, which is the use-after-free design.md:2064 defers. Two seats reached that hole independently. And the blind seat reports the **read** direction is the larger hole: it could not write `getcwd` in any variant because the spec documents no `cstr` → `str` at all — panel 048's finding with half still unrepaired, while `to_str()` on an inbound `cstr` works. **What decides (i) is that the flag deletes a spelling and not a function**: 186 plain `char *` parameters across 17 AST-dumped headers, **159 of them (85%) genuinely write**, the historian's named counterexamples `execv` and `iconv` are `char **` and outside the flag's reach (compiled, clean with the flag live), and the correct spelling — `ptr` with a C-owned buffer — already runs, ncurses `tigetnum` returning 80 against the real library with zero shims | design.md §1.12, §4.10, §4.19, §4.20, Part 5, Part 7 item 10 · CLAUDE.md §7, §12, §13 | 048, 053, 058 |
2026-08-15 | **Panel 058 ratified, and the yes is a work order rather than a full stop** (author instruction, *"rattifica tutto"*, after reading the synthesis — the fourth blanket ratification of the day, recorded as that and not as five reviews). (i) the flag plus a fifth exit-1 class is adopted with **all five conditions binding**; (ii) unshare-or-copy and (iii) a mutable-buffer spelling stay refused on two vetoes each; (iv) refused; the spec takes the **+2** rewrite — *"`s.cstr()` lends a `str` to C to read"* — and not the +32 or +55 clause whose *"may read and never write"* licenses the pointer C keeps | **this is where it differs from the day's other three ratifications**: 057 ratified a *refusal*, so nothing was owed afterwards. Here the yes authorises work, and until the five conditions are met the compiler behaves exactly as before — **the silent corruption is reachable in a ratified language**, which is a state worth naming rather than letting a tick imply otherwise. Three seats made the conditions the price of their yes and one said that without them its verdict is `veto`. **Condition 1 lands first and separately** — the assertion zero becomes the bare `0` (C11 6.3.2.3p3) rather than a cast — because the emitter warns about its own generated `_Static_assert` today, and because the obvious alternative, `(char *)0`, was built and **reproduced a regression** on a correct `const char **` out-parameter binding. Four of the five predictions name milestones or a harness that does not exist and R2 governs them unchanged; the exception is the ffi-pragmatist's, checkable now and offered as its own withdrawal condition. And ratifying the refusal of (iii) does **not** ratify that a separate mutable-buffer type is wrong: four ecosystems reached it independently, OCaml's took three years, and §13 forbids it *now* — the dissent is the argument that starts Part 7 item 10 if it is ever taken up | CLAUDE.md §4, §12, §13 · design.md §1.12, §4.19, Part 7 item 10 | 058 |
2026-08-15 | **Panel 058's remaining four conditions land: `ffi_writable_parameter` at exit 1, the spec at +2, and the wart named in Part 8** (author instruction, `/decide`). `-Werror=incompatible-pointer-types-discards-qualifiers` joins `FLAGS` as the twelfth, `emit/ffi_mutable.rs` is the **fifth** exit-1 class under panel 048's `declaration()` narrowing, and the hardest case works: `extern function strtok(s: cstr, sep: cstr)` **declared and never called** is now exit 1 on the author's own line rather than exit 2 with generated C — which it is only because condition 1's bare `0` had already stopped the emitter tripping over its own `_Static_assert` | **the diagnostic's repair is not "add `const`"**, which is the header's to choose and not this program's: it is *"declare it `ptr` and give it a buffer C owns"*, and the parameter's declared type is read **from the value** rather than asserted to be `cstr` (CLAUDE.md §11). The spec takes the warden's two-word rewrite — `s.cstr()` **lends** a `str` to C **to read** — ledger row `3210`, and the two clauses that were on the ballot at +32 and +55 are not taken, the second because it *licenses* the pointer C keeps. Part 8 gains item 17 with the falsifier §12 requires, and it is a **wart rather than a Part 6 row** on purpose: three repairs were priced and refused, so what is left is a real cost that is named instead of presented as a design. **What would make the row wrong**: a binding the closure list or §4.19's ladder needs that requires a buffer whose *length Heroes knows* — `malloc` + `ptr` returns none, so `snprintf`'s truncation and `readlink`'s no-NUL result have no correct spelling; one on the critical path and the mutable-buffer type stops being a Part 7 wish. Measured against that bar today: the closure list's only outbound `cstr` are `hero_file_read` and `hero_file_write`, both `const char *` | design.md §1.12, §4.19, §4.20, Part 8 · CLAUDE.md §7, §11, §12 | 058 |
2026-08-15 | **The read direction is documented, and the ballot's cheap option was the dangerous one** (`docs/panel/059`, provisional — author ratification pending; five judges, four building in copies, tree frozen). The spec documented `s.cstr()` outbound and never said how a `cstr` comes **back**, though `to_str` works and aborts cleanly on null — panel 048's finding with half unrepaired after 053 fixed `nullptr` and `==`. **The +11 option, naming the conversion alone, is refused on a measurement**: the llm-ergonomist wrote `getenv(…).to_str()` then `len(v) == 0` *first and confidently*, because spec:58 says *"the name says whether it can fail: `to_str` and `to_f64` cannot"* — naming the conversion inside that law positively asserts that a null converts, and the ignorance it removes is exactly what had been sending readers to find `nullptr` in the first place. **The fallible form took a veto** (+37 honest, since spec:58 must be repaired with it) and an objection no wording answers: ncurses `tigetstr` returns `(char *)-1`, so a `str?` advertises a guarantee the boundary cannot honour — measured, SIGSEGV with the null guard passing — and all four `to_str(cstr)` sites in the repo are on functions that cannot return NULL, so 4 of 4 would pay `.must()` for nothing | **what every seat agreed on was not on the ballot: `to_str` copies**, and that is what makes the read direction sound. Proven against `ctime` returning **one pointer** twice with different content while both Heroes strings stayed intact, and the copy deletes a class rather than a surprise — `getenv` then `setenv` is `heap-use-after-free` in C and ASan-clean at exit 0 in Heroes. design.md §4.20 already said it and the spec did not. The clause lands at **+25**, folded into the sentence already there, buying three facts and an escape: it copies, a null aborts, and the guard is `c == nullptr` **first** — the last eight tokens over the warden's own +17 wording, and they are the difference between a clause every seat adopts and one that fails three stated conditions. The abort is stated as a **floor and not an identity**, because there are two paths (null, malformed UTF-8) and panel 048's shape is the precedent. Paid as a **§12 repair with no removal** on rows `2983`/`3141`/`3158`: `types/decls.rs:237` is a shipped diagnostic naming `to_str`, so the compiler documented what the spec did not. **Two seats that compile disagreed about an ABI bump and both were right** — they priced different implementations, and the lesson is §1.11's: the composition arm is pre-paid by `emit/convert.rs`, a new runtime entry point is not | design.md §4.19, §4.20, §1.11 · CLAUDE.md §12 | 048, 053, 059 |
2026-08-15 | **Panel 059 ratified, and the two things owed with the clause land as work rather than questions** (author instruction: *"ratifico tutto e finisci tutto prima del prossimo step, accetto le decisioni degli esperti senza chiedere a me"*, which delegates the judges' own recommendations). The read direction gains its first coverage — `tests/golden/run/` had **zero**, the only null case being the *outbound* one, which is why the sitting had to measure the abort by hand — and `bad_operand` on a `cstr` gains a route | **the run-golden asserts the relationship between two strings rather than either one's contents**, which is what makes it portable: `strerror`'s text differs across libcs (`Unknown error: 9999` on Darwin, `Unknown error 9999` on glibc) and `HOME` is unset on the Windows leg, so it pins `first == second` being **false** — a borrow would make them one string — and picks a variable no machine has. A golden that pins a libc's wording goes red on a CI leg for a reason that is not the compiler's. The route is a **note and not a `Fix`**, because building `c.to_str()` needs the operand's own source text, which the diagnostics module does not have, and a `Fix` with a span and no text is worse than none (§8). It carries both halves — the conversion *and* the `nullptr` guard — since naming the conversion alone is what panel 059 measured as worse than silence. `tests/golden/check/` runs `--brief`, one line per diagnostic, so it cannot see a note: the golden pins the code and a **surface test** makes the route fire, which is the pair §9 asks for | design.md §4.17 · CLAUDE.md §8, §9 | 058, 059 |

2026-08-15 | **Panel 060: a struct crosses the FFI boundary as the header's own type, never as one this compiler declares** (`M-struct-passing` step 1, provisional — author ratification pending). A `record` may be declared inside an `extern` group; the emitter writes **no typedef** and uses the header's name unmangled, and each field is checked by `_Generic` on the field's **address**. The alternative — a Heroes struct plus `_Static_assert` on `sizeof`/`offsetof` — is **vetoed** on a measurement: `{float; float}` against `{int32_t; int32_t}` is `Vector2`, has identical size and identical offsets, passes every assertion that mechanism can emit, and arrives as `(0.0, -1.13e35)` on arm64 because the two travel in **different register files** (aapcs64 §5.10.5.1, HFA). The field list is **complete**, and the record states that the reason is Heroes-side — construction, `==`, `hash`, `perfn.rs`'s unconditional emission — and **not ABI-side, since partial-plus-by-value was measured safe to read**; partial is queued rather than refused, so a later panel does not inherit a reason that does not exist | **`f32` stays struck and the milestone still delivers, because under this mechanism the layout is never ours**: a field declared `f64` over a header `float` is ABI-safe, measured against real raylib (`CheckCollisionPointRec`, `GetSplinePointLinear`, exact), so `f32` becomes a **precision** question rather than a **reachability** one and is deferred to its own sitting instead of crippling the milestone. Raylib goes from 252 of 601 bindable to **589** — 337 of the 349 by-value crossings, the missing 12 blocked by **array fields** rather than floats. The clause lands at the spec-warden's wording, **+56** against the proposal's +137, paying for itself with a **−22 removal that is also a §12 repair**: `spec:207`'s *"only `-I`, `-L`, `-l`, `-F` and `-framework` are accepted back"* has been false since panel 055 — `libraries.rs:138` accepts nine spellings — and read under §12 the spec makes `package "sdl2"` illegal. Principle 0's compiler-need branch is **empty** (`hero_os.h` declares no struct); this enters on §1.12's completeness clause, and the record says so rather than dressing a reach count as a Part 11 metric | §1.0, §1.11, §1.12, §4.19, §1.6, §1.7, Part 5 · CLAUDE.md §7, §10, §11, §12 | 060

2026-08-15 | **`f32` enters, by author instruction overturning panel 060's own deferral** (*"e aggiungi il tipo f32 basta storie"*). Two panels had struck it and both were right on their own evidence: a C `float` **parameter** bound `f64` is bit-exact, because clang converts against the real prototype (panel 052, measured), and panel 060 then showed the milestone could ship without it, because under the adopted mechanism the layout is never ours — an `f64` field over a header `float` compiled and returned exact values against real raylib. What neither sitting could fund, and what the author's instruction settles, is that **inside a struct there is no prototype**: `Vector2` is two `float`s and nothing widens them, so the width that is a convenience at a signature is the layout at a field | **The shape is the panel's measurement, not the instruction's.** There is no `Ty::F32`; there is **`Ty::Float(FloatKind)`**, because a bare variant beside `Ty::F64` was measured at **2 rustc errors and 19 silent sites across 15 files** — the same numbers `types/widths.rs`'s module doc records for the eight integer widths, the same trap, and the same answer. Confirmed on landing: the rename produced exactly 2 exhaustiveness errors, so nineteen sites were sitting inside a `_`, and two of them would have been silently wrong at exit 0 — `crosses_the_boundary` answering *"an `f32` cannot cross"* and `structural.rs` generating an `==` that skips the field. **`HERO_RUNTIME_ABI` 13 → 14**, which invalidates every cache key and every golden, as panel 060's compiler-engineer priced it. Spec **+56** for the width and what it obliges — the table row (+22), `to_f32` in a closed built-ins list that must not contradict the prose, the sentence saying nothing fails to fit a float, and printing; panel 052's own warden had called the +22 *"understated"* for the second of those | spec § Types · design.md §4.3, §4.19 · crates/heroes/src/types/floats.rs · CLAUDE.md §11 | 052, 060

2026-08-15 | **The `f64`-over-`float` field clause is removed the day it landed, under §12** (`M-struct-passing` step 3). Panel 060 wrote *"a C `float` field is `f64`, read exactly and written rounded"* into the spec as the thing that made deferring `f32` survivable — measured and true, because the emitter declares no struct and C converts at the designator. `f32` then landed by author decision and the field assertion asks **type identity**, so the compiler began refusing `x: f64` over a C `float`. A spec that permits what the compiler refuses is a false spec | **The clause was scaffolding for a deferral that no longer exists**, and §12 does not care which of the two is younger: spec and compiler disagreed, and here the spec had the bug because the sentence had outlived its reason rather than because the check was wrong. `SPEC_TOKENS` **3347 → 3329**, and the ledger row now says the clause entered and left the same day and why — a row that recorded only the net would teach the wrong lesson. This is the **sixth** false-sentence repair in this document's history and the fifth in the silent direction; what is new is that it was found by the compiler contradicting the spec within hours rather than by a reader months later | spec § FFI · design.md §4.19 · crates/heroes/src/measure/gate.rs · CLAUDE.md §12 | 060

2026-08-15 | **Panel 061: the partial field list is adopted, and the sitting found it was already shipping** (author instruction *"ok rendili legabili"*, adopting what panel 060 queued; provisional — ratification pending). Spelling **B**, `record Font partial`; `==`, `hash` and a map key refused **transitively**; **construction permitted**; `_eq`/`_hash` still emitted for a partial record as `hero_panic` bodies so no `HeroDesc` holds a null. Q3 splits — construction **per-declaration**, comparison **transitive** — because the transitive default would make raylib's `RenderTexture { Texture texture; }` unbuildable, which is the milestone's own use case | **`spec:211` says *"all its fields"* and nothing enforces it**: `SDL_Event` binds with 1 member of 41 and runs at exit 0 today, so panel 060's *"Q2 = complete"* ratified a rule that was never implemented. The cost is a **silent wrong answer shipped this morning** — three of raylib's four `Color` fields, every `_Static_assert` green, zero warnings, exit 0, `0xFF000000` where C gives `0xFF0000FF`. So Q1 never enabled partial; its real job is making **complete** checkable, and the answer is a mechanism no ballot option contained and two judges compiled independently: a never-called **positional** probe, `Font f = {0,0};`, which `-Wmissing-field-initializers` names by field at **zero spec tokens**, while the designated form the emitter writes warns nothing even under `-Wextra`. **E was refused on evidence rather than by vote**: the llm-ergonomist wanted Nim's polarity because the *unmarked* record is the dangerous one — correct, and its own programs proved it — but the historian read `sizealignoffsetimpl.nim` and found Nim's default-incomplete governs **`typ.size` only** and refuses no construction, and the compiler-engineer found that under E a default program carries **fewer** `_Static_assert`s and the author opts in to being checked, which is the wrong direction for a robustness rule and is invisible from the spec alone. **A is vetoed** (spec-warden): its own sentence declares the spec's `Point(x: 3, y: 4)` a compile error, and it costs a lexer token | spec § FFI · design.md §1.0, §1.7, §1.11, §1.12, §4.19 · CLAUDE.md §7, §11, §12 | 060, 061

2026-08-15 | **Five call sites survived panel 060's `of`/`satellite` split, and the order of work is now part of panel 061's resolution.** `emit/aggregate.rs:92,240,251` and `emit/descriptors.rs:229,262` read the C **type** table where they need the generated-function **prefix**, so on any group `record` — complete or not — `a == b` is `call to undeclared function 'Color_eq'` at exit 2, and `[Color]` emits `static const HeroDesc Color_desc`, a **global unmangled name beside the library** (CLAUDE.md §7). Found independently by the spec-warden and the ffi-pragmatist, three sites and two, on their first programs | **The consequence outranks the bug**: the two operations panel 061 spends spec tokens forbidding cannot be performed on any group record at all, so the refusal's control arm is unwritable and a golden asserting it would pass **vacuously** — gate row `2675`'s finding for the fourth time. Both compiling judges made the sweep a condition of their verdicts, so the resolution lands it **before** partial rather than with it. Two further items ride the same commit: a group whose members are only `record`s emits **no `#include`**, and `heroes fmt` **deletes the word `partial`** while `printer/dump.rs` drops it too — so `assert_canonical`'s *"fmt changed the tree"* check is blind in the same place and **two blind spots cancel into a green test**. That is panel 060's `fmt_extern.rs` hoisting one milestone later, in the same file family, with the round-trip guard complicit | crates/heroes/src/emit/aggregate.rs · crates/heroes/src/emit/descriptors.rs · crates/heroes/src/printer/dump.rs · CLAUDE.md §7, §9 | 060, 061

2026-08-15 | **`partial` lands, and the audit it triggered found the parameter check switched off** (panel 061 ratified; `spec:64`'s seventh false sentence repaired at **0 tokens** in the same commit). Spelling **B**, a **contextual keyword** — `record partial` with a field named `partial` still compiles, so the program's namespace pays nothing for one line's grammar. The word buys exactly one thing, `extern_record.rs`'s completeness probe stops asking; everything else it does is a refusal — `==`, `hash`, a map key — **transitively** through arrays, maps, record fields and a **variant case's payload**, while construction stays **per-declaration** because a transitive rule there makes raylib's own `RenderTexture { Texture texture; }` unbuildable. Spec **3329 → 3360**, +35 against a −4 §12-shaped dedup: *"at the header's own width and sign"* was stated twice fifteen lines apart and is now stated once for **a parameter and a field** | **The audit is the entry, and its three silent findings outrank the feature.** (1) `extern_probe.rs::c_type_of` had no `Ty::Named` arm, and `parameter_list` collects into `Option<Vec<_>>` — so **one** struct parameter deleted the probe for the **entire function**, and with it `ffi_parameter_type` and `ffi_writable_parameter`. M-binding-fidelity's whole deliverable was **off for every signature touching a struct**, proven live by a golden shipped that morning; `DrawText(text: cstr, posX: i64, posY: i64, fontSize: i32, color: Color)` lost the width check on all four `i64`s because of the fifth parameter. The tell was that the sibling table `assert_spelling.rs` **was** widened: two tables answering one question, one grown. (2) `types/partial.rs::map_keys` — written by the seat that commissioned the audit, twenty minutes after writing its brief — walked written type **nodes**, so a map **literal** interned `Ty::Map` with no `TypeKind::Map` and the refusal never saw it. Its own doc said *"the hazard is in the type"* and it implemented a rule about the **syntax**. It was caught by `structural.rs`'s `hero_panic` body rather than by a test — `heroes check` at exit 0, then a **named abort** — which is the compiler-engineer's condition 3 doing exactly what it was asked to: a hole in the containment walk costs an abort with a name instead of two C structs colliding as one map key at exit 0. (3) `printer/dump.rs` still elides a record's `header`/`library`, so half of panel 060's cancelling pair survives: `fmt` prints a record's group head and `dump` does not, and `assert_canonical` is green about a hoist | spec § FFI · design.md §4.19, §1.12 · CLAUDE.md §7, §11 · crates/heroes/src/emit/extern_probe.rs · crates/heroes/src/types/partial.rs | 060, 061

2026-08-15 | **`M-complete-structs` becomes a milestone of its own, after the fact** (author decision, *"A"*, choosing a new id over recording the work as post-close). Nine commits landed after `m-struct-passing` was tagged — `partial` (panel 061), a C array member `i32[4]` (panel 062), the completeness probe, and the repairs those sittings found — and raylib went from **191 claimed** to **349 measured** by-value crossings with zero `partial`. §14's rule decides it: *an id is never renamed once it is in the record, and a milestone that changes shape gets a new one*. The two deliverables are different sentences — *a struct can cross the boundary* against *every struct a C header writes can be named* | **The alternative was a paragraph in journal 018 saying what arrived later, and it was refused on what a reader does**: the work after the tag is larger than the milestone before it, and a journal that does not name it sends the reader to the commit log. The name follows §14 — two words, the **deliverable** rather than the area, and the FFI area left free for the milestone after; `struct` recurs as a noun the way `module` does across `M-module-namespace` and `M-separate-compilation`, which §14 gives as its own precedent. What the id does **not** do is reopen `m-struct-passing`: its tag, its journal and its numbers stay as they were on the day, and journal 019 states the corrections rather than editing them | CLAUDE.md §14 · docs/ROADMAP.md § The names · docs/journal/019-complete-structs.md | 060, 061, 062

2026-08-15 | **A struct result crosses the boundary unchecked, and the failure is exit 2 blaming the compiler rather than a wrong answer** (panel 063, soundness lane, both judges `approve`). `assert_spelling.rs::return_check` answered `None` for `Ty::Named` above the comment *"No other type crosses the boundary"*, false since panel 060 — so `spec:194`'s *"clang checks every result type"* shipped two of three. **113 of raylib's 600** entry points return a struct by value (the milestone's 349 is the union: 294 take one, 113 return one, 58 do both); SDL3 is **4 of 1248**. The mechanism is `__builtin_types_compatible_p(__typeof__(call), T)` in an **unevaluated operand with no probe** — the parameter check needed a *conversion* diagnostic, which is silent when unevaluated, while this needs a *type-identity* fact, which `__typeof__` gives exactly. `_Generic` is refused although it works: an association type must be **complete**, and an incomplete one is a C2y extension Apple clang accepts **silently** at this project's flags — the `HERO_RET_UNIT` defect that survived two milestones here. `sizeof` is refused for accepting panel 060's veto case | **The correction to the premise is the finding**: there is no pair of structs for which a wrong declaration compiles and runs, because C struct assignment is **nominal** (C11 6.2.7p1) — `Vector4` declared as `Rectangle`, identical size, alignment and members, is refused. So the class is **misattributed exit 2, never silent-wrong-answer**, which is worse for the thesis: a silent wrong answer is a bug in a program, while *"internal error: compiling the generated C failed"* is the compiler asserting **it** is broken when the author's `.hero` line is — CLAUDE.md §7's named exception, defeated. Cost **+109/−45, three files, zero golden churn** — conditional on emitting the macro only where a record result exists, because unconditionally it renumbers **56 `#line` restores, 112 lines in 5 files**, in an `UPDATE_GOLDEN`-forbidden directory. Two things are owed and are queued rather than absorbed: the struct-`constant` hole (`__builtin_constant_p` answers **0 for every struct**, so §4.2's back door stays open for struct constants and the fix makes it *look* closed), and `()` as an `extern` parameter, whose 6-line repair both judges built and which goes to a **full** panel because CLAUDE.md §4's trigger is path-based and it lives in `types/` | crates/heroes/src/emit/assert_spelling.rs · crates/heroes/src/emit/extern_assert.rs · spec/heroes-spec.md:194 · CLAUDE.md §7 | 060, 062, 063

2026-08-15 | **The C-width row's falsifier was produced at a position the row does not name, and the answer is a mechanism rather than a vocabulary** (panel 064, full lane; four `object`, one **`veto`**). The row (design.md:2444-2449 — **Part 7 item 10, not a Part 6 row**, and both the proposal and the brief misfiled it) demands *"a C `long`, `size_t` or `unsigned long` **parameter** whose correct spelling is the same on all three CI legs"*. At **parameter** position nothing is produced and there is no hole: `FLAGS`' `-Werror=shorten-64-to-32` makes `fseek(offset: i64)` **exit 1 with `error[ffi_parameter_type]`** on Windows and i686, so the llm-ergonomist's *"1 of 14 silently wrong"* is **0 of 14** — measured on four targets by both compiling judges. At **field** position the row is falsified outright: Darwin's `int64_t` is `long long` while `time_t` is `long`, so panel 060's identity assertion refuses **9 of 24 POSIX fields** (`timespec`, `timeval`, `ldiv_t`, `rusage`, `clock_t`) and **`z_stream.total_in/total_out/adler`**, the struct zlib cannot be used without. `partial` does not rescue it — the field becomes unnameable, which is bug-proof and **not complete** (CLAUDE.md §12 asks for both) | **The vocabulary is vetoed and 9 lines replace it.** `clong` is core under §1.7 (checker *and* lowering *and* backend, erased nowhere), it is Part 5 item 7, it is **not on the closure list** (`library/source.hero:102-115` has one `extern` group of `cstr`/`str`/`i64`; `grep -n long runtime/heroes_runtime.h` returns 0), and the ergonomist's *"legal only as a parameter or a field"* is **not one rule** but 3 loud sites plus **20 silent allow-list gates rustc does not flag**. What lands instead relaxes the **integer** field assertion from type identity to `__builtin_classify_type == 1 && sizeof && sign` — 208 → 217 lines in one file, **559/559 green**, and **strictly monotone**, so it cannot regress an accepted program. It is right and not merely cheap: C's *integer* ABI is width and sign alone, while for floats, pointers, bools and enums identity **is** the ABI, and the branch keeps them there — panel 060's `f64`-over-`float` case still refused. Deliberately **not** built on `__builtin_types_compatible_p` as instructed: that would need an enumeration of which C types are 64-bit-signed here, a premise about the world (§11), where class+size+sign asks the value in hand. Three things die with it: the coordinator's own `_Generic`-on-a-function's-address probe, **vetoed by both compiling seats** — C cannot name parameter *k*, so the assertion covers the whole signature, and `ptr` is `void *` by design, so a binding with **every width correct** is refused (`void *` never matches `FILE *`; every SQLite, curl and stdio entry point), while casting to fit makes it vacuous; the claim that clang destroys `size_t`'s name, which survives on real headers and is lost only on clang's **builtin** libc prototypes (8 of 10 bare, **8/8 recovered under `-fno-builtin`**), so the historian's 3-line repair fixes `memcpy` and not `malloc` and a caveat right per-function is worse than one uniformly wrong; and the premise `size_t = u64`, true of **three 64-bit CI legs** and refused on **4 of 7** targets clang reaches. **What the mechanism does not buy is one spelling for three legs** — it buys *bindable on the machine in front of you* — and the amendment settles that reading the narrow way | crates/heroes/src/emit/extern_record.rs · design.md:2444-2449 · CLAUDE.md §2, §11, §12 | 060, 061, 062, 063, 064
2026-08-15 | **§4.9's port note stops asserting a count and the marks become the inventory** (`docs/panel/065`, provisional — author ratification pending; soundness lane, two approve-with-conditions, no veto). The note's claim "the bootstrap iterates a map in exactly one place (`types/holes.rs`)" is false by sweep: **16 direct `BTreeMap`/`BTreeSet` iteration sites in non-test code, 13 of them observable** — 3 into the emitted C, the rest into diagnostic text and `--dump` output — and `holes.rs` itself had *regained* a map walk after the SCHEDULED item recording the correction was verified, so the fact rotted twice in three days. Each of the 13 now carries `// ORDER:` at the write site naming the sort key and the observer; the live inventory is `grep -rn "// ORDER:" crates/`; the prose keeps the number only as a dated snapshot | two findings outrank the correction itself. **The double-emit determinism test pins stability, not order** (both emissions come from one binary, so a deterministic reorder passes), and two of the three emitted-C orders — the per-option function order and the `fn{N}` typedef numbering, where the suffix is the walk position so an unsorted walk renames symbols file-wide — were pinned by **no checked-in artifact**; an emit golden now pins both. And the two break classes are stated separately, because they were being conflated: an unsorted emitted-C walk breaks the M-selfhost-fixpoint diff at its first seam (sorted order vs fixed-seed hash order — two deterministic functions of the same data), while an unsorted diagnostic/dump walk breaks the golden diff **only once the harness runs under the ported binary**, which M-selfhost-port therefore must do | design.md §4.9 · crates/heroes/src (13 sites, 10 files) · tests/golden/check, tests/golden/emit | 065 |
2026-08-15 | **`private` stays Part 7 item 14 — the probe hit no blockage shaped like visibility** (decided by the ROADMAP's own rule at M-selfhost-probe, the milestone panels 033/034 assigned it to: *"a blockage there puts it on the closure list, a wish does not"*). Eleven modules ported, every cross-module read intended, zero moments where a field or function wanted hiding | the rule's two arms were written in advance precisely so this close could be mechanical: nothing needed arguing, only reporting. The pre-fixed price (+18) is unchanged and the item waits with its warrant intact | docs/ROADMAP.md § two milestones asked for · docs/measurements/009 | — |
2026-08-15 | **Panel 065 ratified as it stands** (author instruction, *"ratifica tutto"*, after the M-selfhost-probe closing recap — a blanket ratification recorded as that, the fourth of its kind on this date). The rewritten §4.9 port note, the thirteen `// ORDER:` marks, the two order-pinning goldens and the dated snapshot all move from provisional to final | the sitting's one disagreement — whether a dated count may live in prose — resolves to the adopted resolution: the compiler-engineer's reading stands, the number stays dated and panel-cited, and `grep -rn "// ORDER:" crates/` stays the only live inventory | docs/panel/065 · design.md §4.9 | 065 |
2026-08-16 | **`\r` enters as the sixth escape** (`docs/panel/066`, provisional — author ratification pending; full lane, 2 approve + 3 approve-with-conditions, and the sitting's one veto **lifted by its own judge on a second ruling**). The proposal's own premise died in the sitting — no Rust unit test writes `\r`; the shared golden already gave the fixpoint its firing test; the coordinator had briefed a false premise that two seats caught independently — and the verdict rests instead on what the seats executed: byte 13 is **unspellable by any composition of the inventory**; `"…\r\n"` errors with a **`certain` fix that repairs the correct program toward wire garbage** (`\\r`); a **raw CR pasted mid-literal compiles silently, survives `fmt`, renders invisibly**, and retyping the visible text drops the byte and still compiles — the thesis inverted, executed live by the warden itself. Ten sourced escape sets contain `\r` without exception, and the bootstrap precedent is Thompson 1984 adding `\v` with hardcoded-11 as the interim workaround — the port's `CR: u8 = 13` exactly | measured +3 (3374 → 3377), paid with the warden's registered prediction (scored at M-selfhost-port close, lapse-with-refund if the escape finds no consumer); the landing carries the coherence condition — **a raw CR inside a literal becomes a diagnostic with the certain fix `\r`**, because the grounds that admit the escape condemn the invisible second spelling — plus the `ir/print.rs` re-encoder arm (dumps stop carrying raw bytes), the enlarged silent trap on the record (`"C:\results"` joins `"C:\temp"`), and panel 008 appended, reaffirmed for the NUL class it ruled on and reopened for the question it never heard | spec § escapes · design.md §4 · docs/panel/008, 066 · measurement 009 gaps 7+10 | 066 |
2026-08-16 | **CLAUDE.md §11's ~300-line threshold yields entirely where the language forbids the seam** (author instruction, *"supera la regola per heroes"*, answering the port's own measurement). Heroes refuses module cycles — `module_cycle` fires on the **`use` edge, whatever it carries**, measured both ways on 2026-08-16 — and a recursive-descent grammar is mutually recursive by construction, so its knots cannot be split at all: `expr`+`primary`+`control`+`stmt`+`name_stmt` is **1025 lines** and `decl`+`data`+`externs`+`extern_members` is **736**, each one module or nothing (call sites read across all 20 files of `crates/heroes/src/syntax/`; every other file there is cycle-free and ports separately) | this is `toolchain.rs`'s reason at its limit rather than a new exception: there the cut merely ran **against** a seam and the file stayed at 400, here **no cut exists**, and a rule that cannot be obeyed is not a standard but a lie. **The language rule is the one that stands**, and panel 031 R6 is why: it chose the cycle refusal as *"the reversible direction"* and priced relaxing it against M-separate-compilation's topological order, so the port supplies the other half of that trade — the cost is two long files, not a wall. **The exchange is written into the rule**: a knot carries a module doc naming its cycle, its entry points and which function calls which, because a reader not drowning is what the ~300 actually protects | CLAUDE.md §11 · docs/measurements/009 § the structural finding · docs/panel/031 R6 | — |
2026-08-16 | **Nothing enters the language: naming every case stays the rule** (`docs/panel/067`, provisional — author ratification pending; full lane, **five seats, no seat in favour of any form**). Asked because the coordinator told the author the port was *longer* than its Rust original and named a 4.7% ergonomic cost. **Both numbers were wrong**: the port is **21% shorter** (812 code lines against 1025 — the earlier figure compared code+tests to code-only), and the price of a legal `_` is **0.77% of the tree** in lines (because `_ => 0` is itself a line, so the price is the *difference*) or **1.10% port-wide in tokens**, which is the unit §1.2's formula actually uses | the decisive findings are three, and none is about the rule. **The site that motivated the complaint is a porting regression**: Rust's `binary_op` returns operator *and* power from one function; the port split it, so 50 non-operator tokens are enumerated twice and the second table invents `.add` — and changing `.percent => .rem` to `.percent => .add` leaves `heroes check` at exit 0 with **46/46 tests passing** and `%` parsing as `+`. *"§4.7's ban did not cost those lines; the split did."* **The arithmetic kills it**: the lifetime saving over the whole self-hosted compiler is **840 tokens**, between 0.4 and 1.7 correction round-trips, against +94 spec tokens paid on every inference — and §1.3 is breached outright, since `else` means *"and every case added later, forever"*, a meaning that lives **elsewhere in time**. **The remedy already shipped and Heroes shipped it first**: 26 of 28 literal-bodied arms already use `|`, carrying 136 names, and GHC's Or Patterns (9.12.1, 2024-12-16) give Heroes' motivation verbatim while Java's own exhaustiveness note answers the question the other way for one reason — separately-compiled novel values — that Heroes does not have. The refusal is falsifiable on the warden's three conditions | design.md §4.7 · spec:117 · docs/panel/067 | 067 |
2026-08-16 | **Two defects the sitting was not convened for, both queued** (`docs/panel/067` § what it found). **`ptr` and `cstr` cannot go in any container**: `[ptr]`, `[cstr]`, `{i64: ptr}`, `{i64: cstr}`, `{ptr: i64}`, `{cstr: i64}` all pass `heroes check` at **exit 0** and abort at **exit 134** — *"entered unreachable code — this is a compiler bug"*. Cause: `emit/descriptors.rs`'s `_ => None`, whose doc says *"which `gate.rs` has already refused"*, **a dead premise** — the gate refuses only `Ty::Unit`. Panel 029 filed three types and the fix covered one | CLAUDE.md §11's class and §1.12's severity, and the **same shape** as the option-payload defect this port fixed hours earlier: a narrowing resting on a claim about the world rather than about the value. The second defect is its own test: the case named after it carries `[ptr]`, `{str: ptr}` and `[[ptr]]`, but its `[()]` line fires first and stops the build, so **the live half is never reached** — §9's `#~` invariant proves where a diagnostic points and cannot prove that the lines after the first firing one ran | crates/heroes/src/emit/descriptors.rs · tests/golden/unsupported/fixedbugs-container-element-reached-clang.hero | 029, 067 |
2026-08-16 | **Panel 067 ratified as it stands** (author instruction, after the recap). Naming every case remains the rule: no catch-all on a variant, no `rest` arm, no lookup-with-default, no named case-groups — five seats, none in favour. The refusal keeps its three return conditions and is falsifiable rather than permanent | the sitting's own products are ratified with it: option D (the port's two operator tables are one function again — the split, not the ban, is what produced the padding that prompted the question), the sweep that found one further instance in its weak form, and the two defects the FFI seat found while answering something else. **The `???`-arms diagnostic stays queued and unlanded**, which is the one place the seats disagreed: the ergonomist proposed it at ~30 spec tokens and the warden never priced it, so landing it on one seat's estimate is the discipline this sitting caught its own brief failing | docs/panel/067 · spec:117 · design.md §4.7 | 067 |
2026-08-16 | **The CI matrix is chosen by what the push is, not by which platforms exist** (author decision, no panel — CLAUDE.md §4's triggers are the language, a diagnostic class and the tool surface, and a workflow file is none of the three). Linux runs on every push, a docs-only one included; **Darwin arm64 and Windows run on `m-*` tags and `workflow_dispatch` only**. Measured over 2026-08-13..16 across **94 runs and 269 jobs**: Darwin was **26% of the minutes and 70% of the money** — 136 min of 524, $10.88 of $15.49, billed at $0.08/min against Windows' $0.016 and Linux' $0.008 — and the account's spending limit stopped **every** job from 08-15 21:44Z, so 21 runs since then are red without having started one step | the narrowing is not *test less*, it is **stop paying to re-read what is already read**: Darwin is the author's own machine, so every commit ran that leg locally before it was pushed, and it is the only leg billed at ten Linux minutes to the minute. Linux is the opposite trade and so runs on everything — cheapest leg, the only ASan carrying a leak detector to check `hero_runtime_check_leaks()` against (panel 021), and the x86 side of `INT64_MIN % -1`. It stays on docs-only pushes because `crates/heroes-cli/tests/milestones.rs` reads `docs/`, so a malformed record **is** a red `cargo test` — and **119 of the 183 commits since 08-13 touched `docs/` alone**, which is what accelerated the bill rather than any milestone's code. What Windows and Darwin now trade is read-at-the-tag for read-at-the-commit, which is CLAUDE.md §14's own per-milestone unit | .github/workflows/ci.yml | — |
2026-08-16 | **`sort` keeps its scalar-and-`str` domain: the spec had the bug, not the compiler** (`docs/panel/068`, provisional — author ratification pending; full lane, **five seats, five vetoes or rejections of A**, five approvals of B). `spec:85` promised *"`for k in sort(keys(m))` walks in order"* while `sort` on a record dies in the emitter, and a compound map key must be a record because there are no tuples. §12 asks which artifact is wrong, and the answer is the document: **panel 027 R1 already put `sort` at the scalars at +0 spec tokens**, design.md:2232 carries it normatively on compiled evidence, and *"the compiler is not lagging a decision; it **is** the decision"* — what lagged was the spec, never amended to match | the veto of A is measured rather than argued. **`==` is total over Heroes types and `<` cannot be**: a record may hold a map (no canonical order without sorting its own keys, i.e. allocation inside a comparator) and a function value (a link-time address, which C11 6.5.8p5 leaves undefined and which **moves with the linker**) — so A must ship a refusal rule anyway, *"and that refusal rule is B. A is B plus ~250 emitter lines plus several invented orders. It is strictly dominated."* The ffi seat **ran** it: a sixth `HeroDesc` field compiles the unchanged runtime at **exit 0 with 16 descriptors silently NULL** (panel 027 measured 7), and under version skew the stale slot reads **`0x4`, not NULL**, so the runtime's own guard says PROCEED and the program **SIGSEGVs** — design.md §4.20's ratified asymmetry (*"adding a function is self-guarding, adding a struct field is not"*) naming exactly this act. The historian found A has **no precedent in its stated form**: Haskell, Rust and Swift all make declaration-order ordering **opt-in per type**, Rust classes a field reorder as semver-incompatible, and Swift declined `Comparable` for structs on *"all fields are reorderable"* — *"A is the OCaml/Python-2 choice wearing Haskell's clothes"* | spec:85, spec:168 · design.md §4.20 · docs/panel/027, 068 | 027, 068 |
2026-08-16 | **The seventh shape of the container defect, and the convener's third wrong brief** (`docs/panel/068`). `ptr`/`cstr` reached **through a record field** where the record is a **map key** passes `heroes check` at exit 0 and aborts at 134 — panel 067's fix covered six shapes with the pointer as the *direct* element, and `emit/gate_types.rs::check_element` does not descend into a record's fields. Only the key position fires, because only there is `hash` called. **The project already owns the correct transitive walk** (`types/partial.rs::reaches`, written for `partial` records) and it was not reused | and the brief for this sitting asserted *"sort.c already solved NaN ordering once (totalOrder)"* — **false**: sort.c rejects totalOrder in writing and **aborts**, and `DESIGN-LOG:205` had superseded `:148` four days after it was written. Three sittings, three wrong premises from the convening seat, three catches by the judges (066's dead test, 067's line-count-against-a-token-formula, 068's dead NaN premise). The instrument is working; the seat that convenes it is the one that keeps needing correction | crates/heroes/src/emit/gate_types.rs · crates/heroes/src/types/partial.rs · runtime/parts/sort.c | 029, 067, 068 |
2026-08-16 | **CLAUDE.md §1 gains a fourth bullet: a number or a repository fact is measured in the session that writes it** (author instruction, after three sittings in a row were briefed with something false). The instances are named in the rule rather than summarised: panel 066's Rust test that never existed, panel 067's line count against a token-denominated formula plus a "longer" that compared code+tests to code-only, panel 068's NaN premise superseded four days after it was written, and a line count typed from memory | the rule's home is §1 because §1 is already *"what never to trust from memory"* — it binds `design.md` (reach it by grep) and the spec (re-read in full), and the gap was that **nothing bound what goes into a brief or a measurement**. Three obligations, each matched to an instance that would have died under it: run the measurement, match the unit to the rule that will judge it, and read a record forward to its end because DESIGN-LOG and the panels are append-only. **The judges caught all three**, which is the instrument working — and the seat that convenes it is the one that kept needing correction | CLAUDE.md §1 · docs/panel/066, 067, 068 | — |
2026-08-16 | **Panel 068 ratified as it stands** (author instruction, after the recap). `sort` keeps its scalar-and-`str` domain; the spec repair stands at 3399 with both ledger rows; A returns only by amending design.md §4.20's *"gains no sixth"* by name, in a sitting that re-runs the `0x4` measurement; `sort_by` is refused-today-and-available at +47…+55 with the warden's prediction as its return condition | **one half is owed and is named rather than assumed done**: B's *move* — the refusal from the emitter to the checker. The transitive walk landed with the seventh-shape fix (a §1.12 crash both the engineer and the pragmatist made condition 1 regardless of which option won), but the relocation, the `bool` admission and the re-wording did not | docs/panel/068 · spec:85, spec:168 | 068 |

2026-08-16 | **Panel 060 ratified as it stands, and the yes deliberately excludes two of its own clauses** (author instruction *"ratifica e sistema tutto"*, blanket). The mechanism stands — a group's `record` **is** the header's struct, no typedef emitted, the name unmangled, each field checked by `_Generic` on the field's **address** — with the complete field list and the −22 §12 repair that funded the clause | **What is not ratified is what the author already overturned, and re-asserting it in a ratification would rewrite the record.** 060's *"no `f32`"* died on 2026-08-15 by instruction (*"e aggiungi il tipo f32 basta storie"*), landing as `Ty::Float(FloatKind)` on the panel's own measurement — 2 rustc errors, 19 silent sites, of which two were wrong at exit 0 — with `HERO_RUNTIME_ABI` 13 → 14; and the `f64`-over-`float` field clause, which 060 itself called *"the one clause that makes the last two survivable"*, was removed the same day under §12, because once `f32` exists the field assertion asks type identity and the spec was permitting what the compiler refuses. A sitting can be ratified minus a deferral its own author overturned, and saying which half died is the difference between a record and a summary | §1.11, §1.12, §4.19 · CLAUDE.md §4, §12 | 060
2026-08-16 | **Panel 066 ratified in full** (same blanket instruction): `\r` is the sixth escape, `raw_carriage_return` refuses the invisible second spelling with a certain fix, and the port mirrors both across four `selfhost/` files | **The process note is the durable half.** The spec-warden's veto adjudicated only the coordinator's own false parity premise and was lifted on that seat's second ruling — after it verified the other seats' grounds itself, including executing the raw-CR trap live. A veto that investigates its own basis and withdraws is the panel mechanism working rather than failing, and it is recorded here because the three sittings around it (066, 067, 068) were each briefed with something false and each caught it | design.md § escapes · spec § escapes | 066
2026-08-16 | **Panel 068 R2 landed in full: `sort`'s element rule is the checker's, and `bool` joins the ordered set** | The ratification's owed half. `types/ordering.rs` holds what may be ordered; `unordered_element` fires at exit 1 on the author's line with the **twelve** ordered types named — the emitter's *"`i64`, `f64` or `str`"* had been stale since panel 042 — never saying *"has no `<`"*, which is false of `str`, and worded so `sort_by` stays addable. The transitive walk builds a **dotted path** (`Line.inner.m` is a map), threaded downward because composing on the return produced a sentence with two subjects and no path. `bool` costs one comparator and one dispatch row: `hero_cmp_for` is `static`, so no ABI moves. **One shape stays in the emitter and the reason is structural**: inside a generic the element is not yet a type, `first([3, 1, 2])` runs today, and the instantiation is visible only after monomorphisation — which is in the IR, which `heroes check` never reaches. Queued as architecture rather than smuggled in | §4.20 · spec § Strings, arrays, maps · CLAUDE.md §12 | 068
2026-08-16 | **A header beside the program was found by one spelling of its path and not the other** (defect, no panel — a driver bug, not a language question). `heroes build prog.hero` did not pass the program's own directory to clang; `heroes build ./prog.hero` did | `Path::new("prog.hero").parent()` is `Some("")`, and `compile.rs` dropped an empty answer — a narrowing resting on a premise about the world (*"no parent component means no directory to add"*) rather than on the value in hand, which is CLAUDE.md §11's named shape. It survived because it fails as `ffi_missing_header`, a diagnostic that reads as correct: the header really was not on the include path. Every FFI test in the tree passes an explicit `--include` or a path with a directory component, so the invocation people actually type was the one nothing watched | CLAUDE.md §11 · panel 036 | —
2026-08-16 | **Panel 036's predictions scored at last, and the scoring was overdue by the whole of M-ffi-ladder's close** | Six checkable predictions, measured **at the tag** rather than against today's tree, because three of the numbers moved afterwards for reasons belonging to later milestones — scoring against a later state answers a different question. The ffi-pragmatist was **correct twice**, the compiler-engineer **two of three** on one and right about the mechanism while high on the number on the other, and **the historian was falsified by a third option nobody listed**: `exit(code)` typed as `()` neither tripped `-Werror=conditional-uninitialized` nor forced an emitter special case, because `hero_os.h` declares `_Noreturn void hero_exit(int64_t)` and clang's flow analysis never runs on a call it knows does not return. A disjunction over the two places *this compiler* could have solved it was a claim that the problem was the compiler's; it was C's, and C already had the keyword | **And the scoring corrected its own instrument**: the `PORT-DEBT` count is **3**, not the 6 a bare `grep -rn PORT-DEBT crates/` reports — three hits are prose in module docs. CLAUDE.md §1's second obligation is to match the unit to the rule that will judge it, and the spec-warden's still-open prediction is denominated in markers | CLAUDE.md §1, §5 · docs/panel/036 | 036
2026-08-16 | **Panel 069: `==` was decided by an address, and the shortcut that did it was not about floats** (provisional — author ratification pending). `runtime/parts/array.c:112` and `map.c:201` each carried an uncommented `if (a == b) return true;`; copy-on-write makes `b = a` share a header, so with a nan inside, `a == b` was **true** and `a == c` was **false** for identical contents, at exit 0. Both lines are deleted. The spec is repaired at `spec:70` by **deleting the enumeration rather than extending it** — 3399 → **3405**, +16 against a −10 named removal — because an enumeration is how a spec makes a silent claim about what it left out, and floats were not the only omission: **function values compare by address** (`emit/structural.rs:118`, panel 029) while the sentence named only `ptr` and `cstr`. Naming floats alone would have been the **fifth** repair of that exact shape in this document | **The sitting turned on a program with no float in it.** The compiler-engineer showed the shortcut silently defeats panel 061's deliberate `hero_panic`: shared operands exit 0 with `true`, distinct ones abort loudly — an optimisation inverting CLAUDE.md §11's loud direction, on a line carrying no comment at all. The two defences it has everywhere else both fail here, measured: **speed** is forbidden as a reason by §13 and the harness got **3% faster** without it; **termination on cycles** is Python's reason and design.md:2855 makes cycles unconstructible, verified two ways. Cost: **two deleted lines, zero compiler lines**, `HERO_RUNTIME_ABI` unchanged at 14, **566 tests green with zero goldens changed**. Precedent: Swift ships the identical bug (SR-6181, open since 2017-10-18) and swift-foundation **added the same fast path to `Data` in May 2025**; no language has removed a container-level one | spec § Values · design.md §4.10, §1.7 · CLAUDE.md §11, §13 | 069
2026-08-16 | **The `spec-warden` seat's own definition carried a ceiling three panels out of date, and a golden control had never controlled anything** — both found by panel 069's judges, both repaired with it | `.claude/agents/spec-warden.md` said **3000 tokens since panel 012**; design.md §1.6 says **4096 since panel 024** (author decision 2026-08-10). The seat reached design.md by grep as CLAUDE.md §1 requires, found the contradiction, and said what it cost: *"Had I held the 3000 line I would have vetoed the unmodified spec at 3399"* — a veto on nothing anyone proposed. **A wrong brief poisons one sitting; a wrong agent definition poisons every future one, silently**, so the repair adds the instruction to grep §1.6 rather than trust the file. Separately, `tests/golden/run/fixedbugs-a-map-key-that-is-not-itself.hero:31` wrote `m[0.0 - 0.0] @ 2` under a comment calling it *"the `-0.0` control"* — and `0.0 - 0.0` is `+0.0`, so it compared positive zero with positive zero for four days. `0.0 * (0.0 - 1.0)` is the expression that produces the sign; the output is unchanged, because what the control claimed is true and it simply never checked it | CLAUDE.md §1, §9 | 069
2026-08-16 | **Panel 070: a deep value SEGFAULTED on release, silently, and the release path is iterative now** (provisional — author ratification pending). Nine lines of Heroes — a `record Node { children: [Node] }` chain, no `==` anywhere — died at **exit 139 with no output**: `hero_array_decref` → the element's `drop` → the generated `_release` → `hero_array_decref`, one C frame per level, at depth **74642** (-O0) and **174003** (-O2) on an 8 MB main thread. design.md §1.12 names that outcome as forbidden. `runtime/parts/drop.c` now threads a worklist through the **dead `refcount` of a doomed block** — zero allocation, layout unchanged, `HERO_RUNTIME_ABI` still 14 — with **two lists and one shared flag**, because a value may alternate arrays and maps and two flags would let the second kind recurse. Measured after: depth **5,000,000** at -O0 exit 0, the alternating chain at 300000 exit 0, `--sanitize` clean, 566 tests green | **`-O2` was never a fix and the brief said it was** — the fourth false premise from the convening seat in five sittings, and the compiler-engineer refuted it with measurements: the recursion depth is **identical at both levels** (20001 at depth 20000, zero calls eliminated), only the frame shrinks, and the call sits inside a `for` followed by `hero_release_block`, so **no C compiler is permitted** to make it a tail call. The convener believed it because of a **second defect the same seat found**: `commands/run.rs:59` returned a hardcoded `134` for every signal under a comment claiming `128 + signal`, so `heroes run` could not tell a clean `hero_panic` from a segfault — repaired in the same commit, and it is what hid this from the person holding the instrument. **Still live and recorded rather than closed**: `==` and `hash` recurse too and crash **lower**, at 52043; they need a continuation in the emitter (vetoed on §1.7) or a stack probe (vetoed on portability), so the hole is on `DECIDE.md` | design.md §1.12, §1.7 · CLAUDE.md §7, §12 | 070
2026-08-16 | **Panel 071: the compiler's own `certain` fix was turning a caught mistake into a silent wrong answer, and `--in-place` was about to start writing it into people's files** (provisional — author ratification pending). `discarded_value` offered `_ = …` as machine-applicable, and CLAUDE.md §8 defines `certain` as *a fix that preserves meaning and the compiler knows it does*. For `xs.push(4)` it does not: `push` returns the new array and leaves `xs` alone, so `_ = xs.push(4)` compiles at **exit 0** and `len(xs)` prints **3** where the author meant 4 — and §8's CI rule, *"the applied fix compiles"*, passes. Now: where the discarded expression's type equals the type of the name it is written on, **both** repairs are offered and **neither is `certain`**, so `--apply` declines the line rather than picking the losing one | **The rule is a fact about the value, not a guess about intent** (CLAUDE.md §11): it does not ask whether the author meant to assign, it asks whether the two types coincide — and where they do, the compiler genuinely cannot tell the readings apart, which is §8's own test for a `guess`. Where they differ (`twice(3)` discarded) the `certain` fix stands, unchanged. **The test that should have caught this was written on the broken example and certified it**: `a_discarded_value_is_an_error_with_a_certain_fix` used `xs.push(4)` and asserted `Certain`. It now pins **both** shapes, because the pair is what makes a regression visible. Landed with `--in-place`, which parsed, was documented in the argv table, and **never wrote the file** — CLAUDE.md §10 names it as *the* mutating flag, and a mutating flag reporting success while mutating nothing is the surface lying about itself; its report is now the **difference** rather than the diagnostic count, since a file can now have diagnostics and no applicable repair | CLAUDE.md §8, §10, §11 · design.md §1.12, §4.17 | 071
2026-08-16 | **Panel 071's spec half: two rows, and a rule the document had never stated** (provisional — author ratification pending). **3405 → 3440**, +35 against a **−32 named removal**, net +3. `ptr` and `cstr` enter § Types **and `spec:222`'s definition clause leaves** — the warden showed the table is a **glossary, not a predicate** (nothing in the document reads off it; `spec:214`'s field list names both types itself), so rows close no hole and what was wrong was **placement**: `p: ptr @ nullptr` compiles at exit 0 **with no `extern` group in the file**, so ordinary types were filed under FFI. And `spec:222` gave `nullptr` to `ptr` alone while `c: cstr @ nullptr` compiles — repaired free. The second amendment states the rule that **a line computing a value must use it**, which this document had never carried: measured, six shapes all exit 1 `discarded_value`, `design.md:1718` calls it *"the highest-frequency LLM habit error under value semantics"*, and grep for *"discard"* returned only the escape hatch. Panel 068's row again — design.md carries it, the compiler **is** the decision, the spec never caught up | **Two ballot wordings were measured FALSE, and pricing them is what caught it**: both said *"`ptr` and `cstr` are § FFI's"*, at +31 and +15, and both are false because neither type needs an `extern` group. That would have been the **ninth** silent-direction sentence here. **Adopted at zero tokens with them**: no sentence in this spec may refer to *"this document's types"* — the phrasing that nearly cost panel 060 130 of 192 raylib entry points, and grep-enforceable. `push`'s shape is **folded into the existing cost sentence** rather than added (+25 unpaid as its own), and the same day proved that sentence load-bearing: `discarded_value`'s `certain` fix wrote `_ = xs.push(4)`, exit 0, element lost. The warden predicted E alone at **3338 / 3409** and it landed **exact to the token**; the full package it put at 3438 against 3440 measured, the 2 from line wrapping | spec § Types, § Bindings, § Strings · design.md §1.6 | 071
2026-08-16 | **Panel 071's emitter half: a flexible array member is exit 1, and a false premise about enums is deleted** (provisional — author ratification pending). The field assertion's second conjunct was `sizeof({place}) == sizeof({elem}[{n}])`, and a flexible array member made it a **hard error** — *"invalid application of `sizeof` to an incomplete type"* — so clang could not evaluate the assertion, nothing carried `heroes-ffi-field`, §7's named exception did not reach it, and the build was **exit 2, the compiler blaming itself**. It is now a **tail measurement**: `sizeof(T) − __builtin_offsetof(T, m) >= n * sizeof(elem)`, defined for both shapes, **0** for a flexible member and at least `n * sizeof(elem)` for every sized one — measured by the ffi-pragmatist across the padded `char[1]` and over-aligned cases. The assertion now **fails** rather than failing to compile, so it carries its marker and the author gets exit 1 on their own line. Verified: correct sized arrays still bind (including the padded case), a wrong length is still refused | **`__builtin_types_compatible_p` was tried first and cannot do it**: clang answers **1** for `char[4]` against `char[]`, so the two are indistinguishable that way; and matching clang's prose was refused on the reason `ffi_narrowed.rs` already quotes from cgo. **A second false premise died with it**: `extern_record.rs` listed *"an enum (3)"* among what the integer branch refuses, and clang applies the default argument promotions to `__builtin_classify_type`, so an enum field answers **1** — the refusal that sentence described never happened. What actually refuses a wrong enum field is the **sign** conjunct, correctly: SDL3's enums are unsigned, `kind: i32` fails and `kind: u32` binds, measured end-to-end. **The sitting's own item B was struck on that measurement** — 97 SDL3 enum fields, 95 bindable, 0 unbindable, against a brief claiming 35 unbindable | design.md §4.19 · CLAUDE.md §7, §11 | 071
2026-08-16 | **Panel 071 item A: three `extern` parameter types stop being exit 2, and the widening caught a latent error in the corpus itself** (provisional — author ratification pending). A parameter declared `ptr`, `cstr` or `str` against a header that says otherwise gave *"internal error: compiling the generated C failed"* — the compiler blaming itself — while the same parameter declared `i64` gave the correct `ffi_parameter_type` at exit 1. The cause was narrow: `ffi_narrowed.rs` matched exactly two clang strings, both **width** diagnostics. A **class** mismatch phrases itself two other ways and both contain `" to parameter of "`, which is the whole matcher. **The gate is unchanged** — `extern_at_line` still asks whether an `extern` is declared at exactly this file and line, which is what panel 052 made the class safe with; only the phrase set widened. All four types are exit 1 now, measured | **The diagnostic fires whether or not the C type has a Heroes name, and only the `Fix` consults the table** — the ffi-pragmatist made that a condition, and it is the difference between a repair and a regression: the types with no Heroes name are exactly the interesting ones, and declining would return `Vec2` to exit 2, which is **356 of 1159** entry points by panel 052's count. A `Vec2` parameter's note now says *declare it as a `record` inside this same group*, a sentence rather than a type name. **Two things the widening broke and one it found**: it swallowed panel 058's `ffi_writable_parameter` class, whose clang text also contains the phrase, and then told an author to *"declare it `cstr`"* when they already had — repaired by naming `ffi_mutable.rs`'s own `discards qualifiers` string as that class's property, a fact about the text rather than a guess about precedence. And it surfaced a **real mistake sitting in the corpus**: `ffi-incomplete-record.hero` declared `CheckCollisionPointRec(point: Rectangle, …)` where raylib says `Vector2`, silent for as long as a class mismatch fell through to exit 2. `ffi_narrowed.rs` split at 348 lines — `c_spellings.rs` holds the C-type table, a lookup that knows nothing about diagnostics | design.md §4.19 · CLAUDE.md §7, §11 | 071
2026-08-16 | **Panel 072: a tag-only C `struct` is unbindable, the shape of the answer is fixed, and the answer itself is NOT adopted** (provisional — author ratification pending, and deliberately narrower than the sitting's own conclusion). `struct TagOnly { … };` with no `typedef` is **exit 2** — the compiler blaming itself — while both other shapes C permits bind at exit 0. Refused: an emitter construct that never chooses (**four compiles**: no C11 spelling names a struct under both shapes, and the helper `typedef struct X X;` — legal C11, accepted silently under all thirteen flags — dies on `typedef struct stat stat;`, because `<sys/stat.h>` declares the **function** `stat` too, so the most-bound struct in POSIX refutes it); and a probe that emits `struct <name>` when the bare name fails, on **soundness** rather than cost — `struct Foo` beside a typedef `Foo` over a *different* struct is legal C11, so a probe can bind the wrong type and every field assertion then passes against it | **The shape is a marker on the group's `record`, and it must carry a C NAME rather than be a boolean** — the sitting's real finding, and it came from binding the real thing instead of modelling it. C has four namespaces (6.2.3) and Heroes has one: **4 of 155 real tags across 19 headers are also a function or object** (`stat`, `sigaction`, `sigvec`, `timezone`), so a boolean leaves `struct stat` unbindable **beside the call that fills it**, measured as `declared_twice` and reproduced by the coordinator. With a rename it binds end to end — 18 fields, no `partial`, four nested tag-only `struct timespec`, `@buf` as an `@` parameter rather than a `ptr`, exit 0. Scale: tag-only over total tags is **POSIX 46/47, curl 43/47, zlib 14/15, sqlite3 3/23, raylib 0/35** — *"the corpus that never hit this is precisely the part of C that does not have the problem"*, and the Linux kernel style guide calling a struct typedef *a mistake* is why. **Not adopted, because a new surface word is CLAUDE.md §4's full-panel trigger and the two seats that judge one did not sit**: the sitting may say the mechanism is this and no other, and may not put a word in the language on two seats. **Precedent, one-sided**: every project that let a *rule* pick the namespace has an open bug on this seam — cgo 2017, c2nim 2018, Zig 2024, bindgen 2025, none closed, and Zig is moving translation out of the language — while Nim, which lets the binding say it, has none | design.md §1.11, §4.19 · CLAUDE.md §4, §7, §10, §11 | 072
2026-08-16 | **Panel 074: the word is `tag`, and the sentence folds rather than appends** (provisional — author ratification pending). The two seats panel 072 could not seat — llm-ergonomist and spec-warden — converged from disjoint inputs. The free-form string `is "struct stat"` takes **two vetoes**: the blind seat because whether the declared thing is a value or a pointer would be decided by characters inside a string Heroes does not parse (*"the spec closed exactly this channel for widths, with exactly this reason, one paragraph earlier"*), the warden on §1.0's smallest self-consistent surface at +39. `as` is objected to because every language spends it on introducing the **new** name (`import numpy as np`) and `spec:9` already says *"No aliases"*; the warden reports **no token difference** between `tag` and `as` (both 3462) and declines to break the tie, leaving it to the seat whose brief it is. **The spec sentence is a fold, not an addition**: appending leaves `spec:217`'s *"the same name"* contradicted four lines above its own example, so the clause is rewritten — *"all its fields, and the same name unless `tag` gives C's"* — at **+26 against a −4 named removal, net +22, 3440 → 3462**, twelve tokens cheaper than the cheapest appended form. **Not compiler-need and the ledger says so**: `selfhost/` has **no `extern` group at all**, and the shim route works today (measured, exit 0, 137 tokens across two files against 96) — what pays under §1.2 is that the repair *"go write a C header"* cannot be expressed as a `Fix` at all, while `tag stat` is `Certainty::Certain`. **Both seats make the same condition**: `emit/ffi.rs:127`'s `declaration()` matches Function and Constant and drops `record` through `_ => return None`, so a record this program declared `extern` never reaches exit 1 — CLAUDE.md §7's own rule, unimplemented for records, and the approval is void without it. Measured this session over six real header sets: **323 struct tags, 109 tag-only**, and **6 of 328 tags are also a function or an object** (`flock`, `sigaction`, `sigvec`, `stat`, `timezone`, `wait`), which is why the marker carries a C name rather than being a boolean. `docs/panel/074`
2026-08-16 | **Panel 073: a C `union` bound as a `record` gives a wrong value at exit 0, and what gets refused is the construction rather than the type** (provisional — author ratification pending). `UDef(i: 1, f: 1.0)` prints **1065353216** — the bits of `1.0f` — while clang emits `-Wexcess-initializers` and `-Winitializer-overrides` on the author's own line and the compiler throws both away. **The author's `variant` proposal is refused on three vetoes**, and its premise was corrected before the sitting: `SDL_Event`'s `type` is a member **of** the union, overlapping every arm's first field (C11 6.5.2.3p6), not a sibling — and the arm↔value mapping is **124 enumerators over 38 arms, stated only in doc comments**, so the author would declare what clang cannot refute, inverting §4.19. The historian supplied the precedent: it is **Modula-2's variant record, which Wirth deleted from Oberon** (*"mostly being misused to breach the typing concept… the true sin was that this tag could be omitted"*, HOPL-3 p.13), **Ada standardised it only by making the tag size-zero, the checks suppressed, punning erroneous and `=` a `Program_Error`** (RM B.3.3), and **rust-bindgen refused the same mapping in 2021** for this brief's own reason. The warden priced it at **+131…+186**, *"31% of the real remaining budget for a construct that binds 2 types across 6 header sets"*, with **no admissible payment** — `heroes mutate`'s 13 operators have no arm for a discriminant mapping. **The repair costs +0 spec tokens** because `spec:217` already says a group's `record` **is the header's struct** and a union is not a struct: CLAUDE.md §12, the compiler has the bug. Detector: `_Static_assert(sizeof(T) >= Σ sizeof(field))` in the generated unit — the **sum-of-sizes** form rather than all-pairs `offsetof`, because the pragmatist's attack found the pairwise form calls a struct of two GNU **zero-sized** members a union (measured firing) where the sum form passes it (measured, exit 0), and because it is O(n) where a 16-field record emits **120** conjuncts pairwise. **What it gates is construction and `eq`/`hash`, not the declaration** — the historian's correction against the brief (*"no language in the record does that"*: Rust, D, Zig, Swift and Nim's C++ backend all restrict the construction **expression** to one member; Go and Fortran restricted the type and their users routed around it, Go's route-around defeating its own runtime pointer check) and the pragmatist's measurement, which is that a two-field read-only binding, a one-member binding and a **four-arm SDL3 event loop** all run correctly today. Classified as a **fifth CLAUDE.md §7 class**, `ffi_union_field`, exit 1 with both field names, one `#~` golden — the warden's condition, on panel 020's rule. `docs/panel/073`
2026-08-16 | **Panel 075: an ordered comparison with a `nan` aborts, `is_nan` is refused, and the spec says the test out loud** (provisional — author ratification pending). Five seats. The defect: `<` and `>=` are both false at a `nan`, so an `if/else` takes the `else` arm in silence. **The measurement that carries it**: `smallest([nan, 1.0, 2.0])` prints **nan** and `smallest([1.0, nan, 2.0])` prints **1.0** — the same six numbers, the same program, and the answer decided by which line the `nan` sits on; the ffi-pragmatist reached the same class by compiling two De Morgan-equivalent validators that **disagree on a `nan` at exit 0**. **Where the `nan` comes from is not the author**: 21 of 29 probed C expressions return one, `strtod` and `atof` are **required** to accept the three characters `nan` (C99 7.22.1.3p3), and neither `raylib.h` nor `sqlite3.h` says the word once. **§1.12 reaches only through the boundary and the record keeps both readings**: the compiler-engineer refuses the citation inside Heroes (a wrong answer is not a corruption, and *"a record citing §1.12 for a non-memory-safety property will be quoted back to license something §1.12 never granted"*), while the ffi-pragmatist compiled the other half — `(int)nan` is `0` on arm64 and **`-2147483648` on x86_64**, so a bound float a C callee casts to `int` indexes memory Heroes cannot see. Cost: **+23 lines in ONE file** (`emit/operator.rs`), `--dump-ir` **byte-identical** over all 78 run goldens (so the checker never sees it), 566/566, `HERO_RUNTIME_ABI` unmoved at 14, **zero guards emitted across the 34-file corpus**, 5 instructions against 3 at `-O2`. **`is_nan` is vetoed twice** — +19 lines across four modules, two mandatory spec-enforcement tests and a name reserved in every scope forever, for what `x != x` already spells — and what its two approving seats asked for is delivered instead: **the spec points at `x != x`**. **The brief's claim that this removes `sort.c`'s special case is struck**: three seats falsified it, two by deleting the guards and running — `sort` returned `1.0 2.0 3.0 5.0 nan 4.0` at exit 0, because `hero_cmp_f64` is runtime C reached through a function pointer and the emitter never writes that comparison. So it is **one rule at two sites in two languages**, and the test that fires when that premise dies is the compiler-engineer's price for approving (CLAUDE.md §11). Spec **+27, 3440 → 3467**, in three places: the operator table, `sort`'s own row — *"an enumeration is how a spec makes a silent claim about what it left out"*, row `3405`'s lesson — and the `==` clause. Payment may **not** name `heroes mutate`: it decides Killed/Survived from `resolve`+`check` and **never runs the mutant**, so it has no arm for a runtime abort; the golden **run** harness has had one since `abort-sort-nan`. **The historian's fifth shape is filed, not adopted**: Erlang has no `nan` at all and Zig **closed** the comparison-abort proposal in favour of float types that cannot hold one — and integer division by zero aborts here while `1.0 / 0.0` returns `inf` at exit 0, measured. `docs/panel/075`
2026-08-16 | **Panel 076: `==` is a conjunction, so the fix nobody could afford turned out to need no emitter line at all** (provisional — author ratification pending). A deep value SEGFAULTed on `==` and `hash` at exit 139 with no output. **Two seats prototyped the same repair independently and measured the same result**: a worklist inside `hero_array_eq` that returns `true` **provisionally** — because `true` is the identity of `&&`, so a nested comparison can defer and let the outermost call drain the queue and AND the answer in. The generated `_eq` never learns it was deferred, so `emit/structural.rs` is untouched and **the Heroes port reproduces nothing** — which is precisely what the compiler-engineer's standing veto was about, and that veto **stands and is simply not engaged**. Depth **1 000 000 at `-O0` exits 0** against exit 139 unpatched, 569 tests green, `HERO_RUNTIME_ABI` unmoved at 14, zero goldens touched. It is sound because the checker makes it so: `record Node { child: Node }`, `child: Node?` and a self-referencing variant are all `error[no_size]`, so **every unbounded descent goes through `[T]` or `{K: V}`, and both are runtime C**. `hash` takes the **opposite** fix — a depth cap — because `hash` may lose information where `eq` may not, and a cap on it is a fact about the value rather than about the machine. **The depth bound is refused on a measurement rather than an argument**: the same binary at depth 52 200 exits 0 in a plain environment and **139 with one 900 KB environment variable set**, because the environment is copied onto the stack; `ulimit` moves it too, and `-O` moves it 2.6×. The warden priced the rewrite such a bound would teach at **+210 program tokens (130 → 340)** — and it still exits 139, because the reader's own walk recurses. **The signal handler is refused as this sitting's answer and kept alive with its conditions**: measured, a bound C library that installs its own `SIGSEGV` handler **un-installs the guarantee with no compile error** (§1.11 makes that the normal case), the handler **blinds ASan** from a 262-line symbolised report to 2 lines, a second fault inside it **hangs at 100 % CPU**, and the Darwin/Linux inversion compiles clean under all 13 flags and is indistinguishable from doing nothing. The historian's contradiction is on the record: **OCaml ran this experiment for twenty years and abandoned it**, moving the check into the compiler's own prologue at OCaml 5, and Heroes owns its emitter. Three numbers were corrected in the record, two of them by the seats that had written them: the closure list's release depth is **7, not 3**; `DECIDE.md:338`'s cliff of 52043 exits 0 today; and `runtime/heroes_runtime.h:19` still says *"HERO_RUNTIME_ABI is 3"* eleven bumps after it became 14. **+0 spec tokens**: `spec:76` invites the tree and `spec:144` promises recursive `==` with no bound, and neither sentence is false — CLAUDE.md §12, the compiler had the bug. `docs/panel/076`
2026-08-16 | **Panel 077: the sitting refuted its own brief, and the flagship union was still printing a false number three hours after the panel that repaired it** (provisional — author ratification pending). The coordinator's brief asserted that C cannot detect a union naming one member, having checked `__is_union` (C++ only), `sizeof` equality and the positional probe. **Two seats that compile found the answer independently, in a builtin this emitter already calls on line 10 of every generated FFI unit**: `__builtin_classify_type(*(T *)0)` is **13 for a union and 12 for a struct**, in a `_Static_assert`, on a one-member union. So panel 073's `sizeof(T) >= Σ sizeof(field)` is replaced: it is **incomplete for a padded union** — `union { int32_t i; float f; char pad[128]; }` passes `128 >= 8` and prints **1065353216**, the same false number panel 073's own commit body cites as the defect it killed — and **`SDL_Event` is a padded union**. Four more defects land with it, three of them introduced or left by that morning's own commit: the trigger gains the **map key** (`hash` was reachable where `==` was refused, which is worse than refusing neither); a **tagged** union was **exit 2** because `typedefs.rs` spells `struct` unconditionally, which C11 6.7.2.3#1a (DR 251) makes a constraint violation clang already diagnoses and even names the right word for; `extern_union.rs`'s marker carried `c_type` where panel 072 rider 1 established it must carry the **declaration's** name, one file over from where that was fixed the same morning; and `extern_complete.rs` emits `T v = {0};`, which **clang exempts from `-Wmissing-field-initializers`** — so a one-field record over any multi-field struct silently claimed completeness, and `record FileStat tag stat` naming `st_size` said **two different files, different inodes, different mtimes, were equal at exit 0**. That last one has **no union in it**, is panel 061's transparent-colour defect reborn in the one-field case, and the repair is **two characters**: `(0)`. A **premise-death control** ships with the predicate (CLAUDE.md §11): two assertions stating this clang still answers 13 and 12, because clang answered 12 for unions before 2019 and a regression would turn every union check into a silent pass. The spec says **the size and not the construction** (+12…+14 measured): construction's wrong guesses are all exit 1 with the rule in the message (panel 035 R4), while the size has no diagnostic **because there is no mistake** — the reader who guesses wrong gets a correct program, and the cost is a binding **never written**, which two blind seats demonstrated by refusing to write one. The historian supplied what pays for it: **every FFI that let the field list decide the size shipped a wrong number** — Nim's `dirent` 272 vs 1, Rust's `libc` `struct stat` 136 vs 120 — and every one that got it right documents the rule. Blast radius measured over five real libraries: **12 of 382 named structs (3.1 %)**, SQLite **0**, raylib **0**. And the contradiction stays on the record: **Go has shipped `==` over C-union bytes for fifteen years with no public bug report**. `docs/panel/077`
2026-08-16 | **Panel 078: the question had been answered four days earlier, and finding that out is what the sitting bought** (provisional — author ratification pending). Spec **unchanged**. Two seats reported before the author stopped the sitting; it did not need the other three. **The word `integer` at `spec:149` is panel 035's own artifact** — commit `ec5558b`, 2026-08-12, three seats, a one-word diff recorded at `DESIGN-LOG.md:207` — so the document is silent **by decision** rather than by oversight, and panel 035's historian had already found the precedent running the other way (*"Zig shipped that abort and moved off it; Python traps and NumPy overrode it"*). The coordinator's brief asserted the opposite, having taken `DECIDE.md:352`'s framing without checking whether the silence was a ruling: CLAUDE.md §1's third obligation says to read forward from a record, and the coordinator read forward in the **spec** and not in the **decision**. Two more brief claims measured false: the `nan` routes are **not** mostly division (`inf` is reachable four ways with none — a 400-digit literal, `to_f32`, eleven squarings; `nan` five, including `extern sqrt(x: -1.0)` at exit 0), and candidate 2 owed `spec:61` no repair because that bullet is about **conversions** (+9 priced for nothing). **The spec-warden then vetoed the option its own indicator made free**: deleting the word measures **−1 token**, and it refused it on design.md §1.1's warning that the token count is *"frequently a misleading one"* — the program cost moves the other way, **+25** in the callee and **+6** per call site. Candidates priced **+14 / +25 / −1 / +12 / +31**; the cheapest was the refuted one. The llm-ergonomist found what the sitting was really about: **the document answers *"what does a degenerate input do"* twice, in two sections, differently** — `xs[0]` on an empty list aborts while `mean([])` is a value, and neither sentence mentions the other. What this sitting bought is **CLAUDE.md §1's fourth obligation**, landed the same day: a silence in a document and a silence in the record are opposite things. `docs/panel/078`
2026-08-16 | **Panel 079: the checker and the lowering read a dot differently, and neither item on the ballot had noticed** (provisional — author ratification pending). Two seats reported before the sitting was stopped. **Defect A was ruled a §12 bug rather than an addition**, on two design.md lines the spec-warden quoted: **§4.2:845** — *"a variant case **is** a small record, so it is written as one — no special case"* — and **§4.11:1534** — *"the resolver, on seeing a dot, **first looks for a field**"*. `types/construct.rs` matched `Ty::Named` only while `ir/layout.rs` had carried the `Ty::Case` arm since panel 068, so `a.g(50)` on a matched payload said *"`Step.apply` has no field `g`"* about a payload declaring `g` two lines up — §4.17's forbidden shape, a message naming a repair that does not exist. **The larger half came from composing the two seats with a measurement**: the llm-ergonomist made a collision clause its condition (*"a value with both a field `g` and a top-level `g` must be a compile error, never a silent preference"*), the warden quoted the line that orders it and asked to be shown the program — and the program **already shipped, resolving the wrong way**. `s.g(50)` printed **50007**, the free function, while the checker had typed the field's **100**, at exit 0 with no diagnostic; and where the two result types differ, the checker typed from the field while the lowering called the function, so **clang caught a type confusion the type system did not** (*"assigning to `int64_t` from incompatible type `HeroStr`"*, exit 2, the compiler blaming itself). Cause: `ir/calls.rs` asked the **resolver**, which answers `Ref::Top` whenever a free function of that name exists, field or no field. Both halves land together, the dot form moves into **`ir/ufcs.rs`** — the seam `types/calls.rs` was cut on one pass earlier — so the two answers cannot drift again, and the sweep says **0 of 268** corpus files change meaning. **The collision clause is deliberately NOT adopted as a new refusal**: it was asked for as protection against a hazard shape 1 would introduce, and the hazard was already there — repairing the precedence removes it rather than refusing it. Defect B is ruled **message-only at +0 spec tokens** and a spec sentence is **vetoed**: a diagnostic is spec business only where a program can read it or where the message is the feature, and the route B must name is already `spec:222`'s `partial`. Its wording stays owed, and the ffi-pragmatist's panel-071 condition with it. `docs/panel/079`
2026-08-16 | **Panel 080: `heroes this`, the egg that cannot hide** (retro-record — author instruction 2026-08-16, verdict given before the sitting). A `this` subcommand prints "The Zen of Heroes" — twenty ASCII laws, eight Bowie songs verified 8/8 by the historian, laws 5–6 and 10–11 built as the Zen of Python's own contradiction pairs — to stdout at exit 0. **It is a one-off author exception to §10's stopping rule**, carried under the compiler-engineer's containment sentence verbatim: the class `--version` already occupies — zero input, constant output, exit 0 — never citable for any capability that reads a file, a flag, or the environment. Two seats sat (panel 037's gear lesson; the other three briefs were empty: spec delta measured **0**, no `.hero` surface, no C). The historian corrected the coordinator's brief — the CPython egg **predates** PEP 20 by two years (`this.py` 2002-02-08, Fred Drake, "Python 10 was a success, commemorate it!"; PEP 20 is 2004) — and found the closer precedent: **`zig zen` is a listed subcommand**, so Heroes is the third instance of a proven form, and the one-table design (panel 016) forces the listed side anyway: hiding the egg would add a third pre-table special case. Python stores its Zen ROT13-encoded; this one is plain text, because its own law 4 says what the eye can see is all there is. ~60 lines, all in `crates/heroes-cli`, zero in `crates/heroes/src/`. `docs/panel/080`
2026-08-16 | **A step off the last statement of any function left the author's file** (defect, no panel — the emitter breaking its own stated rule, not a language question). `#line N` anchors the *next* line and C auto-increments, so a Heroes statement lowering to K lines of C claimed one source line and let the other K−1 walk forward one each. `emit/writer.rs` tracked what clang **believes** and honoured it only at an `at_file` call — so the drift its own module doc diagnosed in its opening paragraph was never actually prevented. Measured: **550 C lines across 81 of 81 `run/` goldens** pointing at a blank line, at the next declaration, or past the end of the file; lldb showed `two.hero:7` then `two.hero:8` stepping off the last line of a **six-line** program. design.md §2 promises lldb "breaks on **and steps through** `.hero` source lines" — the breaking half had `lldb_breaks_on_a_hero_line` and the stepping half had nothing, which is the width of the defect | Fix: `intent` beside `claim` — what was **asked for** against what clang **believes** — with `line` restoring the first when the second has aged out. The asymmetry that makes it cheap: auto-increment is correct while the claim names the generated file and wrong while it names a `.hero`, so a restore is emitted only against a source claim. Cost **+1.03%** generated C lines, **zero** content lines changed (proved: strip every `#line` from the six `emit/` goldens, old and new are byte-identical). Invariant over the corpus rather than over cases somebody thought of (§9): `no_generated_line_points_past_or_between_the_authors_lines` scans every `run/` golden and reports **550** against the unrepaired tree. Found while answering a `DECIDE.md` question about whether `#line` was worth measuring on the Windows CI leg — the leg that could not measure it was not the one that was broken | CLAUDE.md §7 · §9 · design.md §2, §3.1 | —
2026-08-16 | **`sort` orders a `bool`, and the spec said it did not** (§12 repair, no panel — panel 068's ratification catching up, not a new decision). +4, 3506 → 3510. Panel 068's verdict reads *"B's remaining half is owed: the refusal moves from the emitter to the checker, transitive, **admitting `bool`**"*, and that half landed at **06:06** today. The `sort` built-in row was then edited by panel 075 at **12:52** — six hours and forty-six minutes later, by a sitting reading that exact sentence — and still said *"a number or `str`"* | Measured: `sort([true, false, true])` exits 0 and prints `false true true`, while the compiler's own `unordered_element` already named *"numbers, `str` and `bool`"*. Two artifacts contradicting each other, and §12 says the spec is the one that is wrong; §1.6 makes it expensive rather than untidy, because the spec **is** the prompt and a reader was being told a working program is illegal. **One row, not two**: `false < true` and `"a" < "b"` are both `bad_operand`, so the operator table's *"a number only"* is true and stands — `sort`'s domain and the ordered operators' are different sets, which row 3399 established. Found independently by the coordinator and by panel 081's spec-warden in the same sitting | spec:173 · CLAUDE.md §12 · design.md §1.6 · `measure/gate.rs` row 3510 | 068
2026-08-16 | **Panel 081: the two silences, and what was under one of them** (provisional — author ratification pending). Five judges, five briefs. **Clause A lands**: *"a `bool` prints `true` or `false`"*, +12 against a **−10 named removal**, net **+2** → 3512. **Clause B does not, and is not refused either — it is unripe**, held behind the surface it describes. **Numbered 081 and briefed as 080**, because a parallel session took 080 mid-sitting; two seats independently reported the tree unfrozen, which is how it was caught, and the freeze rule now has a failure mode nobody had listed — a second session with the same author's credentials | Clause A is §1.4 exactly: the blind seat answered `true` **from no sentence** at confidence 5/10 and recorded that it nearly wrote `1`, and the wrong guess **compiles and prints**. The compiler-engineer attacked eleven rendering shapes and found the clause exactly true; the historian's survey of nine systems found the rendering stated normatively wherever a printing primitive is documented, and left to library *source* only where the reader has a source file to open — Heroes has neither, so the sentence has no other home. Clause B was offered as a phrasing question and three seats went and **used** the surface: `heroes check` exits 0 and the build dies in **four positions** (`r = a.reserved`, `reserved: a.reserved`, `w.reserved @ [...]` at exit 2; `[a.reserved]` at exit 134), only indexing works. **The brief's own argument was falsified by the seat that approved B** — the misreading is *silent*, not loud, because *"therefore `partial`"* exits 0 with fields absent — and the historian's counter-inference was falsified by running it: `partial` is memory-safe, zero-filled by C11 §6.7.9p21. **And one rung lower**: `char[N]` binds at none of the eight widths (26 of 81 real fixed-array fields; `uname()` unreachable), because the field probe asks type identity for an array element where it asks class+width+sign for a scalar. **R5 refines panel 035 R4 with the case it never had**: loud at **exit 2 with the compiler's own name on it** is not a covered mistake, it is a defect, paid in compiler lines and never in spec tokens | docs/panel/081 · spec:173,177 · CLAUDE.md §7, §12 · design.md §1.4, §1.11, §4.19 | 081
2026-08-16 | **Panel 082: the note that told the author not to try** (provisional — author ratification pending). Five judges. **The coordinator's brief was false and a judge measured it rather than arguing**: it claimed the frontend cannot see a generic's instantiation, so *"there is no third place to put it that `check` reaches"* — CLAUDE.md §1's named shape, an inference presented as a measurement, with the tell in the connective. `types/apply.rs:139-143` writes `checked.instantiations` **inside `types::check`**, keyed by the **call-site** span, both edges present for a generic calling a generic. Option (a) as briefed could not even catch its own example: `mono::run` pushes only polymorphic recursion | **R1 lands now**: *"no change to this file will fix this"* moves off the `Diagnostic::unsupported` constructor and becomes the **row's** decision, because it was false on **three of the five rows** measured — `[ptr]` runs the moment the pointer is held in a one-field record (exit 0 against real SQLite 3.51.0, §4.19 ladder step 3's own shape), `[()]` runs as `[i64]`, `unit_field` runs with the field typed, and `sort` in a generic runs when the call instantiates it at an ordered type. Only `missing` earns the old note. GCC's taxonomy is the sourced line — `DIAGNOSTIC_LEVEL_SORRY` is for input that is **valid** — and `emit/builtins.rs` had **already written down** that the note was false here and left it. **R2 refuses (a)** on the compiler-engineer's veto: 57 lines, `check.rs` past §11's ceiling, 1.3–2.2× on every keystroke, `--permissive` contaminating Part 11's control arm, a `heroes mutate`/`heroes check` split, and a §4.16 hole both compiling seats found independently. **R3 queues (b)** at ~90–110 lines as the only option answering both vetoes' substance — the error lands on the call the author can edit, which the IR route provably cannot produce. **R4 corrects two shipping module docs** that asserted the false claim. **R6**: the invariant is *check accepts ⇒ **build** succeeds*, never *⇒ run succeeds* — `sqrt(-1.0)` is a legal `f64` and `sort.c:51,57` is the standing counterexample, shown by deleting the guards and getting **four different answers at exit 0** | docs/panel/082 · CLAUDE.md §8, §1 · design.md §4.17 | 082
2026-08-16 | **Panel 081 R3 lands: four fixed-array shapes stop being the compiler's fault** (defect class, ruled by panel 081). Every one was `heroes check` **exit 0** and then died: `r = a.reserved` and `w.reserved @ [...]` at **exit 2** with `use of undeclared identifier`, `reserved: a.reserved` at **exit 2** with `incompatible pointer to integer conversion`, and `[a.reserved]` at **exit 134** in `hero_unreachable`. Now all four are exit 1 on the author's own line, and the four shapes that worked — reading by index, passing the record by value, `==`, a map value — still exit 0, verified case by case | One cause for all four: a `T[N]` has no storage in C (`float t[4]; t = …;` compiles at no optimisation level), so `emit/storageless.rs` renders one **where it is used**, and that works in exactly the two positions consuming `fixed_text` — a subscript base and a construction argument. Nothing refused the rest. Two new rows: `fixed_flow` in `gate_ops.rs` (one `Op::Store` covers the binding *and* the place store; the `Construct` arm distinguishes a **written literal**, which renders `{a, b, c}` and C accepts, from a **field read**, which renders `v.member` and C refuses as an initialiser) and `fixed_element` in `gate_types.rs`, a fourth kind of non-element beside `()`, `ptr` and `cstr`. **A refusal and not a lowering**, on Principle 0: `selfhost/` declares zero fixed-array fields, measured by two seats, so the widening waits. Golden `unsupported/fixed-array-flow` fires all three rows with its header **beside it** rather than from a package, because a skip is a pass and these were crashes. Both codes registered as **soundness, not thesis** — `--permissive`'s control arm has no correct program to compare against a segfault | docs/panel/081 R3 · CLAUDE.md §7, §9, §12 · design.md §1.12 | 081
2026-08-16 | **Panel 081 R4 lands: `char[N]` binds, and it is panel 064's own argument arriving one type late** (§1.11/§4.19 completeness defect, ruled by panel 081). A C `char[N]` struct member could be bound at **none** of the eight integer widths, so `uname()` was unreachable at any price — and its `partial` escape hatch is refused as `empty_record`, because `struct utsname` is nothing but `char` arrays. Scale, from a clang AST walk over eleven POSIX headers plus raylib, SDL3, sqlite3, zlib and curl: **26 of 81 fixed-array struct fields (32%) are `char[N]`** | Cause: `emit/extern_field.rs` asked **class, width and sign** of a scalar integer (panel 064, whose own doc says *"C's integer ABI is width and sign, and C's type identity is finer than its ABI"*) and **`_Generic` type identity** of an array element — and C's `char` is a **third type**, distinct from `signed char` and `unsigned char`, so `&t.name` is `char (*)[4]` and matches `int8_t (*)[4]` never. Nobody carried the argument across to the element. The repair asks panel 064's rule of `{place}[0]` and keeps the length **exact and sign-agnostic** by asking it of the field's own element type — `__builtin_types_compatible_p(__typeof__(place), __typeof__(place[0])[n])` decides the count and never re-litigates the element. **Four refusals verified to survive** before the golden was written: `u8[4]` (wrong sign — `char` is signed here), `i16[4]` (wrong width), `i8[8]` and `i8[2]` (wrong length both ways); and a flexible array member keeps panel 079's own `ffi_flexible_array_member` rather than being told to correct a length that does not exist, because the tail measurement is still asked first. Golden `run/ffi-a-char-array-member` prints **68**. **`ffi_decls.rs`'s `_` arm repaired in the same commit**: *"no C header can declare a field of this type"* is CLAUDE.md §11's claim about the world and is false — `tiffio.h` declares `float d_mat[3][3]` — so it now names what *this group form* has no spelling for | docs/panel/081 R4 · CLAUDE.md §11, §12 · design.md §1.11, §4.19 · panel 064 | 081
2026-08-16 | **An `assert` failure showed the expression with its brackets eaten** (defect, no panel — §12, the spec beats the compiler). `assert (1 == 1) == (1 == 2)` reported `assert failed: 1 == 1) == (1 == 2`, and `spec:190` says the failure *"shows the source expression"* | Cause: a parenthesised operand's span covers what is **inside** the brackets, so the outer `==`'s span ran from the left operand's first token to the right operand's last and stepped over both on the way. The expression node was the wrong thing to slice, because what the reader is looking at is a line of text rather than a tree. The lowering takes the **statement** now and strips the keyword, which cannot lose a character the author typed; the trailing trim keeps a comment out, since a statement's span reaches one. The existing test could not see it — `twice(2) == 5` has no top-level parentheses — so a second one was added with both shapes | crates/heroes/src/ir/asserts.rs · spec:190 · CLAUDE.md §12 | —
2026-08-16 | **`#line` on Windows: the question was two questions, and only one of them was about Windows** (author decision, `/decide`). The item asked whether `#line` was worth measuring on the third CI leg and assumed the debugger was the only instrument. **The mapping is now measured on all three legs** by `no_generated_line_points_past_or_between_the_authors_lines`, which reads the emitted C and needs no debugger — and it was written for a different reason and found the mapping **broken on all three**, 550 drifted lines. **The debugger leg stays uncovered on Windows and is recorded as such**: honest, free, and not a defect — `lldb.exe` exits `0xC0000135` on a Python DLL, while `-g` writes a 7.7 MB `.pdb` that is exactly where Windows keeps CodeView | The way back in is named and **deliberately not asserted**: `llvm-pdbutil` reads CodeView and, unlike `lldb.exe`, has no reason to link Python — but whether the runner carries it is a claim about the world nobody here can check, so it goes to the machine as a question rather than into a document as a premise (CLAUDE.md §1). `Report the toolchain` now prints whether `llvm-pdbutil` and `lldb` are on PATH, so the next run answers it | .github/workflows/ci.yml · crates/heroes-cli/tests/golden.rs · design.md §2 | —
2026-08-16 | **Panels 081 and 082 ratified** (author instruction, *"ratifica tutto"* — blanket, recorded as one; each file's own § Author's verdict says what the yes does and does not settle). 081: R1, R3, R4 were already landed; **R5 becomes a standing rule** — panel 035 R4's test gains its third case, so *loud* means exit 1 with a message the author can act on, and loud at **exit 2 with the compiler's own name on it** is a defect paid in compiler lines rather than a silence bought with spec tokens. 082: R1 landed, R2 refuses option (a) without making the refusal permanent, R4 and R6 corrective | **Two things a blanket yes explicitly does not do, written here so neither is re-derived as an oversight.** Clause B's veto condition **is now met** — the spec-warden's *"exit 0 **or exit 1**"* was satisfied by R3 the same day, re-measured after ratification — so B is **eligible** and still unwritten, because the sitting's other condition holds: the wording is chosen against what is true *now*, which the four measurements changed (a fixed array is written as a literal where its record is built and read by index; it cannot be named, copied or stored). Pricing that wording is a fresh measurement, queued rather than smuggled in — adopting a clause the sitting declined to adopt would read the yes wider than it was given. And 082's **llm-ergonomist veto stands**: option (b) moves the error to the call site without making the generic body's line decidable from the line, so lifting it needs constraints on generics, which Principle 0 refuses today | docs/panel/081 § Author's verdict · docs/panel/082 § Author's verdict | 081, 082
2026-08-16 | **The three open decisions close, and none of them closed the way it was written** (author instruction, *"completa anche le tre voci aperte … suggeriscimi tu la soluzione più robusta"*). `DECIDE.md` goes from three open items to **zero**, and the two that were not questions leave the list by becoming something else | **(1) Clause B lands after its repair**, +13 against a **−11 named removal**, net +2 → 3514, ledger row `3514`. Its veto condition was met by R3 the same day and re-measured before the sentence was written; the wording **shows** the form (`build one with `[a, b, c, d]``) rather than naming it, on the historian's only measured discriminator — the document held exactly one array literal and it was the empty one, and C's brace is the reader's prior while `{}` is this language's map. **The payment is a §11 expiry the sitting's own spec-warden did not find**: *"because clang converts a wrong one in silence"* was true when written, and this compiler falsified **both** halves — a wrong parameter is `ffi_parameter_type` and a wrong field is `ffi_field_type`, both exit 1, measured today against a real header. §1.4 makes it a removal rather than a rewrite. **(2) Panel 082's option (b) moves to `SCHEDULED.md`** — it is no longer a question but work with a home, and building it would contradict the ratification it came from; it travels with a **trigger rather than a date**, `grep -rnE '^function [a-z_]+<' selfhost/` returning a generic whose body contains `sort(`. **(3) Panel 058's standing reopen condition now FIRES** rather than waiting to be re-read: `no_binding_needs_a_length_back_from_c` states it as the falsifiable claim it always was, over ten named contracts, and was verified to go red on a `readlink` probe naming the file and the line and green again when it was removed. A deliberate proxy — it fires on the binding rather than on the need, and early is the correct direction | spec:200,220 · docs/panel/058, 081, 082 · crates/heroes-cli/tests/corpus.rs · CLAUDE.md §11 · design.md §1.4 | 081, 082
2026-08-16 | **Panel 083: the pointer that could not be read, and the one that read too much** (provisional — author ratification pending). Convened on a C member declared **pointer-to-const** being `heroes check` exit 0 and `heroes build` **exit 2** — the compiler blaming itself for the author's `extern`, CLAUDE.md §7's named exception failing. **The sitting found worse on the way**: `-> cstr` accepted **any pointer at all**, so a four-byte blob read **ten bytes** — six past the object — at exit 0, no diagnostic, no abort, and `--sanitize` silent because the read stays inside the containing allocation. Every instrument this project owns reported success. Cause: `emit/assert_spelling.rs` sent both `Ty::Ptr` and `Ty::Cstr` to `HERO_RET_PTR`, `__builtin_classify_type(c) == 5`, blind to the pointee — so §4.19's *"clang checks every result type"* was not being kept for that row | **R1** an explicit `(void *)` at the two places a `ptr` is materialised from C (a group-record field read, an `extern` result): C11 6.3.2.3p7 makes it a conversion instead of 6.5.16.1's constraint violation. **R2** `cstr` gets `HERO_RET_CSTR` over the six character-pointer spellings — six because C's `char` is distinct from both signed forms and real headers return all three; `const void *` is now exit 1 on the author's line. **R3** panel 058's write guard verified untouched — `ffi_writable_parameter` still fires; the flag was adopted for the write direction and the read was collateral, the tell being that the designed firing has a diagnostic and the accidental one had *"internal error"*. **R4** no `Ty` variant, no keyword, **zero spec tokens** — §4.19:2098 already files `const` under Part 7 item 10. **(a), refusing, was VETOED**: it costs 25 bindings including `sqlite3_column_blob` and `sqlite3_column_text` — rung 3's own *"read a result"* — and buys nothing, because the hazard is already reachable three ways, one of them a **shipped example** passing `void **` where the header says `const char **`. **The coordinator's brief was wrong three times** and the panel file says so: a substring bug counted `sqlite3_index_constraint` as const, the class was scoped to fields when results are the bigger half, and it did not know `const char *` already binds. Four counts were offered across two sittings and all four were wrong | docs/panel/083 · CLAUDE.md §7, §12 · design.md §1.11, §1.12, §4.19 | 083
2026-08-16 | **Panels 083 and 084 ratified** (author instruction, *"approva tutto"* — blanket, each file's § Author's verdict recording what the yes does and does not settle). 083: R1, R2, R4, R5 final; **R2 read as the result rather than a footnote** — a four-byte object was being read ten bytes deep at exit 0 with every instrument reporting success; R3 ratified as a *verification* (panel 058's guard measured intact, not re-opened); the compiler-engineer's veto **stays armed** against any future `Ty` variant or spec type name for `const`. 084: R1, R2, R4, R5 final | **Two things a blanket yes must not be read as doing.** 084's R3 is a **non-decision**: constraints are refused by design.md §4.12:1593, ratified long before the sitting, and this panel did not decide it — the warden's veto was *"on presenting a ratified ruling as an open question, not on the author's right to change it"*, so a later amendment of §4.12 must be its own act and cannot inherit this yes. And **panel 082's standing veto is DISCHARGED by 084 R1**, which is what closes the thread these four sittings were pulling: the blind seat named *"refuse early"* as one of the two wordings that would lift it, R1 is that wording, the line `sort(xs)` is now decidable from itself, and the capability survives at `sort_by<T>(xs, less)` — measured running at `i64` and at a user record before the line changed. Six defects stay filed with located causes, and the ratification says so rather than implying they closed | docs/panel/083 § Author's verdict · docs/panel/084 § Author's verdict · docs/panel/082 | 083, 084
2026-08-16 | **A generic that calls a generic now compiles** (defect, no panel — panel 084 filed it with its cause located; this is the repair). It was `heroes check` exit 0 and `heroes build` **exit 2**, `too many arguments to function call, expected 0, have 1`, pointing at `t2 = hero_unreachable(t1);` — a line the compiler wrote | Two halves, because the defect had two. **The keying**: `Checked::instantiations` is keyed by `span.start`, exact at check time because a span occurs once, and stale after monomorphisation because the same span then occurs once **per copy of the enclosing body** while the table still holds the template's unsubstituted `[#0]`. The answer moves onto the thing that is per-copy: `Function::call_instances`, substituted by `mono_subst`, asked before the checker's table and falling back to it for an ordinary function where the two agree. **The fallback**: a missed lookup emitted the literal *name* `hero_unreachable`, so arguments went to a zero-argument `_Noreturn` — CLAUDE.md §11's quiet direction where the loud one was owed. It emits `hero_unreachable()` now and says *compiler bug*. **Why it survived**: of the four goldens with two or more generics, every call was from `main` — nothing composed, and §1.11's Tier 2 is a library that composes. Attacked around it: three levels at three element types, one generic reaching two generics at two types, and polymorphic recursion, still refused | crates/heroes/src/ir/mod.rs · ir/mono_subst.rs · emit/inst.rs · emit/ops.rs · CLAUDE.md §11 | 084
2026-08-16 | **`_ = v` stops making the generated C warn** (defect, no panel — the emitter not saying in C what the author said in Heroes). `-Wunused-but-set-variable` fired on `int64_t h0_v;` for §4.4's own discard idiom, and the corpus harness holds a zero-warning bar, so the one spelling the spec provides for *"I deliberately do not use this"* was the one that could not meet it | **The first attempt failed and the reason is the finding**: `_ = v` lowers to a **load** of the slot whose destination is then discarded, so the slot is read in the IR and not in the emitted C — a rule that asks the IR answers *read* for every discard and catches nothing. The question had to be asked of the emitted C, which is exactly what `emit/unread.rs` already answers for temporaries, so the third question went beside the other two. A slot nothing reads in C is declared `__attribute__((unused))`, the idiom `extern_complete.rs` already writes on its probes. **Not a suppression**: an unused binding is a compile error (§4.4), so a slot written and never read exists precisely when the author wrote the discard — clang is right about the C, the C is right about the program, and what was missing was the deliberateness. **A refcounted slot never gets it and that falls out rather than being carved out**: the exit sweep loads it to `decref`, so it is read by construction and never warned. Verified on all four shapes — scalar discard, refcounted discard, `_ = f(x)`, and a binding that is used | crates/heroes/src/emit/unread.rs · emit/body.rs · CLAUDE.md §7 | —
2026-08-17 | **THE FIXPOINT — the compiler compiles itself, byte for byte** (M-selfhost-port close; the measurement M-selfhost-fixpoint's row asks for). `heroes build selfhost/main.hero --emit-c` from the Rust bootstrap and from the Heroes port produce **one hash**: `373f454482831a9fc23942b8675cfc68e450a61cb49f34f45b7dbeac81173e63`, 20,886,539 bytes, 724,245 lines, over a 34,812-line source in 143 modules. Apple clang 21.0.0, arm64-apple-darwin25.5.0 | **Verified three ways, because `cmp` on two files is not enough for a claim this size.** The C the port emitted **compiles** under the full thirteen-flag set with every `-Werror` in it, into a compiler that answers `heroes 0.0.1`; **that** compiler passes the whole differential (five commands over 257 corpus inputs, both streams, 2,570 byte-exact matches, zero divergences); and the fixpoint is **stable** — the stage-2 compiler's own emission is the same hash again. **Nine defects were found getting here and every one was measured rather than reasoned**: five in the port, of which five were one class (a `continue` in a match arm is a loop jump, so it skips a hand-kept index — a shape the Rust cannot have, since it writes `.iter().enumerate()`), and **four in the bootstrap**, of which three were a caret repair that had reached statements and not the call, the method, or a fixed-array type beside them — one of them underlining the `#~` annotation §9 requires to check that caret. The last obstacle was a note the bootstrap had written *to the port*: three `// ORDER: ascending TyId — so the Heroes port owes an explicit sort` marks, unpaid, which left 20.8 MB of C whose lines matched exactly and whose sequence did not | docs/journal/021-selfhost-port.md · selfhost/ · docs/ROADMAP.md § M-selfhost-fixpoint | —
2026-08-18 | **Panel 085: the seed is proven and the archive is blocked — by a path the port wrote into itself** (provisional — author ratification pending; four seats, the blind one not convened because both halves change **zero** spec tokens, measured). The seed works and works better than claimed: `clang -I runtime seed.c runtime/runtime.c` under **six dialects**, `-O2`, `-pedantic-errors` and `-Wall -Werror`, all clean, and the compiler it makes **re-emits the fixpoint byte for byte** — sha256 `373f4544…`, 17m02s at `-O0`, 12m29s at `-O2`. **A1 adopted** (raw C committed, refreshed in the commit that breaks it): **A2 vetoed on a measurement the brief had not made** — git deltas plain text and cannot delta a compressed stream, so gzip's 8% saving on copy one becomes **5.78 MiB against 2.74 MiB by copy three**; A3 is CVE-2024-3094's shape (the tarball that differs from the repository); A4 costs 152 seed reviews per milestone. **B4 adopted**: the archive leaves this milestone | **What blocks it is `selfhost/main.hero:45`** — `LIBRARY_PATH` is `"crates/heroes/src/library/source.hero"`, read at `:77` through `.default("")`, and the Rust it replaces is `include_str!`. The port reversed that decision silently, so **the self-hosted compiler is already unusable outside the repository root**: `range`, `map`, `filter`, `fold`, `read_file`, `args` vanish and the diagnostic **blames the author's line** (`builtin_shape`, exit 1, reproduced three ways). Archiving `crates/` would make it permanent. **And the emitted C of the two compilers had never been compared**: the M-selfhost-port differential compares *streams*, `--emit-c -o` writes an *artifact*, so the comparison had run on exactly one input. Over **127 programs** — 26 identical, **81** differing only in a `#line` file name the port spells raw where it writes it sanitised on disk, **19** by float spelling (`DECIDE.md:367`), **3** where the port emits an **undeclared identifier** and `heroes build` is exit 2, cause named in the port's own unpaid `# PORT note` at `emit_container.hero:77`. Three seats' judgement on B3 was unanimous — the `#~` invariant (86 files, 184 annotations, one enforcer at `golden.rs:344`), the spec-budget gate (`measure` is **not** in the port, `main.hero:65-67` exits 2), and **zero precedent**: no self-hosting project in Go, Rust, Zig, Nim, GCC, LLVM, CHICKEN or Guile deleted its harness at the archive, and Go's own gap between the two acts was **six years** | docs/panel/085 · selfhost/main.hero:45,77 · selfhost/emit_container.hero:77 · docs/ROADMAP.md:105 · CLAUDE.md §9, §10, §12 | 085
2026-08-18 | **THE SEED — a stranger with a C compiler can build Heroes, and the archive is postponed by what looking for the seed found** (M-selfhost-fixpoint close; v1). `seed/heroes.c` is committed **raw**, 21,008,434 bytes, and `clang -I runtime seed/heroes.c runtime/runtime.c -o heroes` is the whole build — **3.7 s, no flags**, clean under six dialects, `-O2`, `-pedantic-errors` and `-Wall -Werror`. The compiler it makes re-emits that same file byte for byte: sha256 `8c87d43023e2b0a1d2824624fd5429a2cf9baf7a6de4eb041b4b6ec93335c481`, 11m06s at `-O2`. Tested from **`git archive HEAD`** rather than the working tree, because a `clang` run in a dirty directory proves nothing about a *checkout*; the test also reads the seed's `HERO_RUNTIME_ABI` stamp against the runtime's as numbers, and runs a program end to end. **2.50 MiB of pack** (6.56 → 9.06 MiB, measured before and after), and raw rather than compressed because git deltas plain text and cannot delta a compressed stream — gzip saves 0.20 MiB once and costs 1–2.4 MiB per refresh | **The archive of `crates/` did not happen, and each of the three reasons was measured rather than argued** (panel 085 B4, provisional). (1) `selfhost/main.hero:45` read the standard library from `crates/` at run time through `.default("")` — panel 028 R3's ruling reversed in silence — so the self-hosted compiler was **already** unusable outside this repository and refused `range`, `map`, `filter`, `fold`, `read_file` and `args` **on the author's own line**. (2) **The emitted C of the two compilers had never been compared**: M-selfhost-port's differential compares *streams* and `--emit-c -o` writes an *artifact*, so the finish line's own output had been checked on exactly one input — **26 of 127 programs agreed**, in three classes (81 on a `#line` file name the port spelled raw while writing it sanitised on disk, 19 on float spelling, 3 where the port emitted an **undeclared identifier** at exit 2), and after the repairs **127 of 127**, pinned by `tests/differential.rs`, whose expectation *is* the other compiler and which **dies with the bootstrap**. (3) `heroes measure` is not in the port while design.md §1.6 cites its gate as live. Two more defects fell out: `x: f64 @ 1_0.5` printed **0.0** at exit 0 in the bootstrap (`parse::<f64>().unwrap_or(0.0)` under the comment *"the parse cannot fail"*, and the lexer accepts `_` on both sides of the point), and a field declared scalar where the header has an array was the compiler's **exit 2** because `((__typeof__(place))-1 < 0)` is ill-formed rather than false — C type-checks every operand of `&&` regardless. The float divergence closed on a **fourth** way the filed item had not listed: §1.11 says everything comes from C, and C owns a correctly-rounded `atof`. `M-harness-port` and `M-bootstrap-archive` are the new chain | seed/README.md · docs/journal/022-selfhost-fixpoint.md · docs/panel/085 · CLAUDE.md §7, §9, §10, §11 | 085
2026-08-18 | **A directory walk that needs no extern — panel 085 R4's "no sound cheap route" was a failed search, not an impossibility** (M-harness-port step 1; no panel — this is a program's plumbing, no spec token, no surface, no diagnostic class). R4 measured `ls > file` + `read_file` splitting a filename that contains a newline into **two entries at exit 0** — silently wrong, which §1.12 forbids — and named `popen`/`fgets`/`pclose` or `readdir` as the routes, the second blocked by `DECIDE.md:462`'s construction question | **`find -print0` and the language as it stands.** NUL separates the names, `read_file` carries that byte into a `str`, and byte indexing splits on it — no literal can spell `\0` (spec:68 has six escapes), so the separator is passed as a `u8` and `strings.split_on` takes a byte rather than a `str`. Measured on the hostile directory R4's own falsifier describes (a name holding a newline · a name holding an apostrophe · one plain name): **3 entries for 3 files, the newline name whole, all three readable**, byte-identical from the Rust bootstrap and from the seed-built self-hosted compiler. Kept as `shell.hero`'s third test, so the route's death is a red test rather than a discovery. **The generalisable half is the shape of the error**: two routes found and written as *"the only sound routes"* is a negative claim resting on the searcher's vocabulary — CLAUDE.md §1's rule about a failed search, applied to a panel's own brief | tests/harness/shell.hero · tests/harness/strings.hero · docs/journal/023-harness-port.md · docs/panel/085 R4 · CLAUDE.md §1, §12 | —
2026-08-18 | **The port printed a diagnostic with no code, no position, no snippet and no notes — and the Heroes harness is what saw it** (defect, no panel — the port not saying in a diagnostic what the bootstrap says; M-harness-port step 2 found it, step 3 repairs it). `heroes build tests/golden/unsupported/ffi-records-only-group-cannot-claim-its-package.hero` on the self-hosted compiler wrote `heroes-ffi-package `…` is not installed on this machine` plus four bare lines, where the bootstrap writes `error[ffi_package]`, `at …hero:25:5`, the source line with its caret, and four `note:` lines. Every part of §4.17 at once, in the **one** class §7 allows to blame the author's own `extern` — so a reader was told a package is missing and not which line named it | **The two compilers ask `resolve_packages` from different places.** `toolchain.rs:219` asks it from **inside** `link`, so its marker text reaches `emit_ffi::explain` with every other clang failure and comes back as a `Diagnostic`; `selfhost/cli_verbs.hero:71` asks it one call **earlier**, in the driver, where the only thing left to do with a `fail` is print it. The repair sends it through `emit_ffi.explain` exactly as the clang branch twelve lines below already did — the port's own mapper (`emit_ffi_build.package_problem`, tested, byte-identical logic) had been reachable by nothing. **Verified against the expectation rather than by eye**: `diff` is silent, and the whole harness goes 100/102 → **102/102** with the self-hosted compiler testing itself. **Why nothing caught it before**: `tests/differential.rs` compares the two compilers over the corpus and over five commands' *streams*, and nothing ran `build` over `tests/golden/unsupported/` on the port — panel 085 R4's argument for porting the net **before** the archive, arriving three hours after the harness existed | selfhost/cli_verbs.hero:71 · seed/heroes.c (regenerated, panel 085 A1) · tests/harness/ · docs/journal/023-harness-port.md | —
2026-08-18 | **A caret that swallowed a comment: the 2026-08-12 repair had reached statements and not declarations** (defect, no panel — same class as the three the fixpoint found, `docs/journal/021`). `tests/golden/unsupported/ffi-writable-parameter.expected` underlined **71** columns of `    function strtok(s1: cstr, s2: cstr) -> cstr   #~ ffi_writable_parameter` — the declaration **and** the case's own annotation — and had been pinning that as correct since the case was written | **The span of a declaration depended on whether somebody had written a comment beside it.** `decl.rs:207` closed an `extern` member's extent with `previous_span()`, which is the last token *consumed*: `body_check` skips terminators looking for an indented block, and a trailing comment is a token, so the extent reached past it. `previous_significant_span()` — written 2026-08-12 for this exact defect on statements, with `f() # a note` underlined 23 columns wide for a three-column statement — was never applied here. One-line repair, and the measurement is the evidence: **71 columns with the comment, 43 without**, on two files differing by that comment alone; **43 in both** after. Nine other `#~`-carrying goldens all stop at their construct (measured), so this declaration was the only one. **What exposed it was a second implementation disagreeing** — the port never had the defect — which is the argument for keeping a differential past the archive, and the reason `M-bootstrap-archive`'s entry already asks for one that survives its own oracle. `cargo test` 576 green; the one expectation changed by a **single line**, read and quoted | crates/heroes/src/syntax/decl.rs:207 · crates/heroes/src/syntax/cursor.rs:133 · tests/golden/unsupported/ffi-writable-parameter.expected · CLAUDE.md §9 | —
2026-08-18 | **THE NET, IN HEROES — every suite the Rust harness carries now has a successor, and the first thing it did was find two defects nothing else could see** (M-harness-port close). `heroes run tests/harness/main.hero -- <compiler>` is **688 checks in 3m35s**: check 65 · ir 20 · emit 6 · unsupported 11 · run 87 · annotations 76 · determinism 113 · fixes 6 · lines 87 · corpus 14 · warnings 107 · records 5 · surface 81 · special 9 · layout 1. **3,630 lines of Heroes against the 4,711 of Rust they replace** — 0.77 of the denominator, inside panel 085 R4's predicted 2,500–3,760 and just under its ~3,200 centre. **No compiler line and no new surface**, which is what §10's stopping rule required: `heroes run` and the compiler under test already compose to it, so B2's `heroes golden` subcommand stays refused by its own text | **Two defects, and the second is the one worth keeping.** (1) The port printed `error[ffi_package]` with **no code, no position, no snippet and no notes** — `selfhost/cli_verbs.hero:71` asks `resolve_packages` from the driver while `toolchain.rs:219` asks it from inside `link`, so the port's own mapper (`emit_ffi_build.package_problem`, tested) was **reachable by nothing**. (2) `ffi-writable-parameter.expected` pinned a caret **71 columns** wide where the declaration is **43** — `decl.rs:207` closed an `extern` member's extent with `previous_span()` instead of `previous_significant_span()`, the repair written 2026-08-12 for this defect on *statements* and never applied to a *declaration*. Measured both ways: 71 with the trailing comment, 43 without, on two files differing by that comment alone. **The golden had been recording the defect as correct since the case was written, and what exposed it was a second implementation disagreeing** — the argument for keeping a differential past the archive, which `M-bootstrap-archive`'s entry already asks for. Three more findings are the milestone's own shape: the directory walk needs **zero externs** (`find -print0`, DESIGN-LOG:389, falsifying R4's "no sound cheap route"); `layout` is a **rewrite**, because a Heroes module holds its own tests (**30 of 144** files pass 300 lines counted whole, **17** counted as code) and because the language forbids splitting a mutually recursive knot, so the check holds a ceiling **per file** rather than a rule nobody can obey; and one defect is recorded rather than repaired (`DECIDE.md:466`, cause located in `descriptor_set.rs::generated`) | tests/harness/ · docs/journal/023-harness-port.md · docs/ROADMAP.md § Status · docs/debrief/{SCHEDULED,LEARN,DECIDE}.md · CLAUDE.md §9, §10, §11 | 085
2026-08-19 | **The differential's successor is the bytes it blessed — `tests/emission/`, 142 programs, and BOTH compilers green against them** (M-bootstrap-archive step 1). The one test whose oracle is a second implementation cannot be ported, so what survives it is the agreement itself, frozen while both compilers still existed: 5.4 MB, 162,878 lines of generated C, written by the self-hosted compiler and verified identical from the bootstrap, with `cargo test --test differential` green in the same tree. Four guards, each a way an oracle stops being one: the floor (142, the measured count, no slack), collision (two programs mapping to one file would leave one uncompared), orphans (a renamed case leaves bytes nobody compares — reported, never deleted), and the skip floor. Blessing is deliberate and typed: `UPDATE_EMISSION=1` | **The whole C rather than a hash, and the argument is what a failure can SAY.** 142 hashes catch the same changes for 142 lines and cannot say what moved — after the archive nothing can reconstruct the previous emission except an old checkout and a rebuild, so §4.17's rule for a diagnostic decides it. Precedent inside the repository at four times the cost: `seed/heroes.c` is 22 MB of generated C for one program. Emission is machine-independent, checked before storing: zero hits for `/Users`, `/opt/homebrew`, `__APPLE__`, `aarch64`, `x86_64` across all 142, and the emitter carries no platform conditional | tests/harness/suite_emission.hero · tests/emission/README.md · CLAUDE.md §9 | 085
2026-08-19 | **`heroes measure` in Heroes, and it agrees with the bootstrap to the token** (M-bootstrap-archive step 2). 3440 / 3512 / spread 72 / headroom 584, identical from both compilers, and the three published BPE vectors match too (2 · 6 · 7). The spec-warden's veto at panel 085 was that §1.6's budget had one live enforcer and it was about to be archived; this is the port, and the only difference in the whole output is the path line, because the Rust resolves the repository root at COMPILE time and a Heroes program cannot | **The language forced a better shape.** Heroes cannot build a `str` out of arbitrary bytes, so the BPE table cannot be decoded — the port keeps each token's base64 spelling as the key and encodes the LOOKUP, canonical and padded, which is measured to be how both vendored tables spell every token. Second departure, written as a claim that can die: a letter is `A-Z a-z` where Rust asks Unicode, sound only while the spec's non-ASCII characters are punctuation — 53 occurrences, 4 distinct (`·` `—` `→` `…`) — and `suite_spec.hero` pins that SET so a new letter is loud while another em dash is not. A missing vendored table is exit 2 in the tool's own name, never a silent empty count (panel 085 R5's lesson) | selfhost/measure_{pieces,bpe}.hero · selfhost/cli_measure.hero · design.md §1.6 | 085
2026-08-19 | **The spec's own gate moves into the net — 7 checks, green against both compilers** (M-bootstrap-archive step 3). `measure/gate.rs` and `measure/spec.rs` are 435 lines and every check in them is `cfg(test)`, so the budget, the built-in inventory and the reserved-word rule would have died leaving nothing: the spec measures 3512 and the registry 916 or the number moves in a commit that says why; the spec names every built-in the compiler reserves and offers none it lacks; no word the lexer refuses appears in the spec's code; the built-in list still ends at `range` | **The compiler's side is read out of the compiler's own source, not copied** — `builtins.hero`'s 37 rows and `keywords.hero`'s 23 match-arm words, each extraction floored, because an extractor reading the wrong shape reads nothing and turns two checks into decorations. Three of those arms carry several words (`"null" | "nil" | "None"`), which a one-per-line reader would have missed. Every check has a test that makes it fire (CLAUDE.md §9): five failure injections and three stub compilers. **What broke**: the first draft walked bytes and sliced them one at a time — the obvious port of `char_indices` — and died on the spec's own em dash, `panic: string slice splits a character`. The abort is the language keeping a promise; the module now takes `[str]` from `chars()` | tests/harness/{suite_spec,spec_text}.hero · CLAUDE.md §9 | 085
2026-08-19 | **`heroes mutate` in Heroes, and the gallery diff is EMPTY** (M-bootstrap-archive step 4). 538 mutants, 512 killed (96%) strict and 414 (78%) permissive, all thirteen operators, every rate, `--survivors`, and the unknown-operator message — byte for byte the bootstrap's. The thesis's primary metric (panel 011: metric 3 runs on every commit) had no successor, and three internal invariant suites feed off the same corpus. Six modules, 1,258 lines against 1,079 code-only lines of Rust — **1.02×**, the port's closest ratio | **Two rounding-and-walking decisions measured rather than assumed.** The rate is `{:.0}`'s round-half-to-EVEN reproduced in integer arithmetic, because Heroes has no float precision format — the empty diff over thirteen operators is what says the two agree. The corpus walk is `find -print0`: `ls > file` splits a filename holding a newline into two entries at exit 0 (§12 forbids it), NUL is the byte a filename cannot hold, and the self-test builds one through the only door the language leaves open. **And one finding**: `edits_typo.rs`'s doc said the slip turns `total` into `totl`; the code drops index `len/2` and produces `toal`, measured on both compilers. The comment is corrected, the code is not — changing it would move every number in measurements 002 and 007, the operator is data and the illustration is prose | selfhost/mutate_{sites,edits,typo,ops,score}.hero · selfhost/cli_mutate.hero · crates/heroes/src/mutate/edits_typo.rs | 085, 040, 041
2026-08-19 | **The spec-budget ledger becomes a dated record, and gets the check it never had** (panel 086, provisional — author ratification pending). The 38 rows move verbatim from `crates/heroes/src/measure/gate.rs`'s doc comment to `docs/measurements/010-spec-budget-ledger.md`; `tests/harness/suite_spec.hero` gains `LEDGER_ROWS` and a three-way lock — the record's row count against the pin, the newest row's figure against `SPEC_TOKENS`, and `SPEC_TOKENS` against what `heroes measure` reports today (`spec: 8 passed`, and the failure injections are in the module). design.md:322 and :349 now point at both halves and say which is which; :1349's `ORDER:` inventory is repointed at `selfhost/`; a forwarding pointer stays in the archived file (CPython's `Misc/stable_abi.toml` practice); and `suite_records.hero` gains the dead-citation check in its anchored form | **Three seats converged from three measurements, and all three named the same hole.** The ledger is 48,996 bytes and **12,757 tokens — 3.6× the spec it guards** — so CLAUDE.md §11's test relaxation ("a case is read one at a time") does not reach it, which kills the put-it-in-the-harness option; `docs/measurements/` is the only candidate both on §14's never-rewritten list and already outside the records walk; and five of six enforced-budget precedents split figure from history (CPython, Go's `api/go1.*.txt`, Rust's `tidy`, TeX's `errorlog.tex`, Linux's checkpatch), while leaving the citation pointing into an archive is the failure Linux built `make refcheckdocs` to catch. **The missing mechanism was adjacency**: nothing ever checked the newest row against the constant two lines below it, and the base rate for that is `REGISTRY_TOKENS` drifting +85 across eight milestones unnoticed. The warden also narrowed :349 rather than repealing it — its subject is *this paragraph*, so the rule is **a number may live in a second place only if it is checked or dated**: a row is dated, a pin is checked, prose is neither. The dead-citation rule is anchored because the naive form was measured at **52% false positives** (145 occurrences, 44 missing) against the anchored form's **2**, both of which prefix matching accepts with no allow-list | docs/panel/086 · docs/measurements/010-spec-budget-ledger.md · tests/harness/{suite_spec,suite_records}.hero · design.md:322, :349, :1349 | 086
2026-08-19 | **THE THIRD LANGUAGE IS GONE — `crates/` is `archive/bootstrap-rs/`, and design.md:82 is a fact rather than a plan** (M-bootstrap-archive close, tag `m-bootstrap-archive`). The way in is one clang line over `seed/heroes.c` (3.4 s), what runs the tests is the one command, and CI holds four steps of which none is Rust. **Five successors landed before the move and each was measured against the thing it replaced**: `tests/emission/` — 142 programs, 5.4 MB, both compilers green against the same bytes, which is the differential's claim recorded while a second implementation still existed to make it; `heroes measure` in Heroes at 3440 / 3512 / spread 72, **identical to the bootstrap's**, plus the three published BPE vectors; `tests/harness/suite_spec.hero`, 8 checks, replacing 435 lines of Rust in which every check was `cfg(test)`; `heroes mutate` at 538 mutants and 512 (96%) / 414 (78%), **byte-identical report including `--survivors`**; and the seed regenerated with both verbs inside it, fixpoint re-verified byte for byte (22,025,792 bytes, 15m41s). The port is **37,137 lines of Heroes across 153 files** against 25,482 non-test lines of Rust | **What the milestone found is worth more than what it moved.** The net was still holding the bootstrap's hand in nine self-tests that named `./target/debug/heroes` — in the harness built to survive that path's deletion; both record walks read a **gitignored TLS private key** and reported it as a milestone identifier, so they now ask `git ls-files` (a list of exclusions is a premise about which junk exists, git's answer is a fact about the project); the Heroes `numbered_in` **aborted** where its Rust twin reaches for `from_utf8_lossy`, which is the language refusing to guess; and panel 086 R7's dead-citation check went green on arrival — its own defect, because anchoring on existing roots stops recognising a path the moment its root moves — then found **nine dead citations that predate the archive** in the two documents this project is steered by. Three process texts and one README were edited rather than queued (`DECIDE.md` carries the revert list); CLAUDE.md §4's trigger paths and the Windows CI leg, which the archive left covering the toolchain and nothing else, are the author's | docs/journal/024-bootstrap-archive.md · archive/bootstrap-rs/README.md · docs/ROADMAP.md § Status · .github/workflows/ci.yml · docs/debrief/{DECIDE,SCHEDULED,LEARN}.md | 085, 086
2026-08-19 | **`read_file` on a file that is not text fails instead of killing the process — and the panel shrank the repair from a language change to five lines of C** (panel 087, provisional — author ratification pending). Spec **unchanged, +0 tokens**, which was not the proposal's plan: the spec-warden refused all five non-zero options on §12, because `spec:179` already types `read_file` fallible, so the document is right and the runtime is what lies — paying 11–21 tokens to *describe* a defect inverts the rule. `runtime/parts/os.c` now asks `hero_utf8_valid` before `hero_str_from_bytes` and returns the new `HERO_OS_NOT_TEXT`; the abort inside `hero_str_from_bytes` is **untouched**, because `hero_str_chars` takes one byte off an invalid sequence and depends on it (the ffi-pragmatist's uncast veto). Measured after landing: seed rebuild 3.29 s, the reproducer `fail: read_failed` exit **0** where it was `panic … 134`, `heroes check <a jpeg>` exit **2** where it was 134 — **which was not even a verdict in §10's contract** — `records` 6/6, hello-world's emitted C **byte-identical**, **0** emissions re-blessed, ABI stays 14. The Tier-2 wrapper still collapses the status into `read_failed`: naming it `file_not_text` is **queued**, because the two seats that compile disagreed — the ffi-pragmatist wants it (a JPEG and a failing disk are not the same program state, and `library_source.hero:143` says codes exist to be told apart), the compiler-engineer refuses it on Principle 0 (+2 `_Static_assert`s in **every program in the language**, 142 emissions re-blessed spending the last cross-compiler agreement, and `heroes mutate`'s `typo-code` row is 25 mutants **0 killed**, the table's only zero). What the sitting really bought is the **class**: four doors, and this shuts one — `args()` on non-UTF-8 argv is 134 with no error channel to widen, and `.to_str()` on a real `sqlite3_column_text` is 134 **after the null test `spec:229` tells you to write**, so a binding that obeys the spec to the letter still dies. A null-safety predicate is categorically unbindable, because the emitter wraps every `cstr` in `hero_cstr_nonnull` — a §1.12 completeness failure in §1.12's own words. The llm-ergonomist's counter-option (one sentence closing the abort list) was **vetoed and falsified by a running program**: a `{K: i64}` map built inside a generic and keyed at `nan` is `check` 0 and `run` 134, one of ~12 reachable aborts the spec never names, so the sentence would have been false the day it shipped and §12 would have convicted the compiler of a dozen bugs by fiat. | robustness at the C boundary; the failure a fallible built-in could not report | §1.12, §1.11, §4.19, §12 | docs/panel/087
2026-08-23 | **Panels 085, 086 and 087 ratified** (author instruction, *"ratifica tutto"* — blanket, recorded as one; each file's own § Author's verdict says what the yes does and does not settle). **085**: the seed committed **raw** and the archive as its own milestone are final, and the yes carries an execution of R1's claim rather than a memory of it — `clang -I runtime seed/heroes.c runtime/runtime.c -o heroes` in **3.75 s**, and the compiler that line produces then ran the whole net at **845 passed, 0 failed, exit 0**, four days after the tag. The compiler-engineer's two vetoes keep their lift conditions (three refreshes where gzip beats raw; a harness capability `heroes run` cannot express) and R7's mirror — a fixed array declared where the header has a scalar, still `internal error` at exit **2** — stays open. **086**: R1–R7 final as landed, each re-verified in the ratifying session: the 38 rows at `docs/measurements/010-spec-budget-ledger.md`, the three-way lock green inside the net (`spec: 8 passed, 0 failed`), `heroes measure` at **maximum 3512**, the forwarding pointer at `archive/bootstrap-rs/heroes/src/measure/gate.rs:100-101`, the anchored dead-citation check at `tests/harness/suite_records.hero:235`. R4's four unmarked `ORDER:` walks stay in `SCHEDULED.md`; R6's CLAUDE.md:112 stays the author's, now with the same rot found in `.claude/skills/{panel,step}/SKILL.md`. **087**: the five-line runtime repair is final at **+0 spec tokens**, with the abort inside `hero_str_from_bytes` deliberately untouched because `hero_str_chars` depends on it — and **none of the nine queued items is settled by the yes**: `file_not_text` keeps the engineer's reversal condition (a named reader in the same commit plus a golden that fires the arm), and the collapsed `read_failed` is accepted as a stated cost rather than a closed question. **The process failure travels with the ratification**: 087 shipped with no § Author's verdict section and no `DECIDE.md` item, and 086 had the section pointing at an item that did not exist — so for four days two sittings could not be ratified by anybody, and neither absence was visible from either end. Both were repaired in `a6e4205`, hours before this line | docs/panel/085 § Author's verdict · docs/panel/086 § Author's verdict · docs/panel/087 § Author's verdict
2026-08-23 | **Three decisions taken in `/decide`, all applied in the same session** (author answers `1a`, `2b`, `4 (a) at tags + (c)`; the fourth of that batch, the unused descriptor, is `3c` and is its own commit). **(1a) The panel trigger stops pointing into the archive.** CLAUDE.md §4's parenthesis named `crates/heroes/src/{lexer,syntax,types}/` four days after that tree became `archive/bootstrap-rs/`, and so did the brief that convenes those panels — which is how four of panel 087's five seats died on a watchdog rebuilding a compiler that had moved. §4 now names the *behaviour* (*"what `selfhost/`'s lexer, grammar and checker DO, not how they do it"*), and dropping the braces is not cosmetic: the brace form is one of the two citation shapes `suite_records.hero`'s dead-citation check cannot read (panel 086 R7), so the sentence is now checked by an instrument instead of by memory. `.claude/skills/panel/SKILL.md` sends the engineer to `selfhost/` + `runtime/` with the 3.4 s seed route in hand; `.claude/skills/step/SKILL.md` sends prediction-scoring to `docs/measurements/010`. **(2b) The compiler's own 482 tests move to tags**, the net stays on every push. Re-measured in the deciding session: **20m35s / 1219 s user**, against the ~16 minutes the item recorded five days earlier and the 13 s the archived Rust bootstrap took — the price of self-hosting, not a regression, and CLAUDE.md §13's second clause (performance is not a licence either) is why it is a decision rather than a wait nobody chose. What the trade gives up is the 482 in-module assertions against 845 net checks over the same compiler. **(4a+c) The seed gets the check its own comment claimed it had.** `suite_determinism.hero:13` said `seed/heroes.c` "is checked by `cmp` against what the seed-built compiler emits" and two greps found no such check: CI built *from* the seed and never compared it, so a change to the **library text** — which moves neither the ABI assert nor the grammar — left a stale seed passing the 3.4 s build while every later job ran a compiler carrying yesterday's library. CI now re-emits and `cmp`s, Linux at tags (15m41s), and the host was chosen on two verified premises rather than one assumption: zero absolute paths in the seed with relative `#line` paths, and `word_bits` reaching diagnostics only, never the C. `seed/README.md` gains a **third forcing condition** and the rule of thumb behind it — if the diff touches `selfhost/`, the seed is regenerated in the same commit | CLAUDE.md §4 · .claude/skills/{panel,step}/SKILL.md · .github/workflows/ci.yml · seed/README.md · tests/harness/suite_determinism.hero:13
2026-08-23 | **A descriptor is emitted because the C takes its address, not because a signature mentioned the type** (defect, no panel — the emitter not saying in C what the program does, which is `emit_unread`'s class one artifact earlier; author decision `3c` in `/decide`). `heroes test tests/harness/probe.hero` printed `probe.c:401:23: warning: unused variable 'h_probe_Probe_desc'` against CLAUDE.md §7's zero-warning bar, on the harness's own file: `[Probe]` is interned because a function takes one, so the type was reachable, the descriptor was emitted, and nothing pointed at it | **The author chose the exact answer over the cheap one and the exact one had to be paid for twice.** Asking the emitted C — writing the bodies into a throwaway writer first and reading the names out of them — works and was measured: **+39%** on emission (the net's own C, 20.97 s → 29.03 s, two runs each) for bytes that were **identical** on every program without a dead descriptor, so it was refused on the author's instruction that the compiler must not get slower. The structural answer was then wrong on the first attempt, loudly: pruning by *"which instruction takes a descriptor's address"* missed `through_pointer` ← `hash_call` ← every aggregate's `_hash`, and `examples/tree` stopped compiling with `use of undeclared identifier 'h_main_0opt0_desc'` at exit 2 — the compiler blaming itself for a correct program, which is the direction `emit_descriptor_set`'s own module doc warns about. **The landed repair is one condition in one place**: `generated`'s first source (the type-table walk) now requires that an instruction actually **construct** the container before its element, key or value descriptor is queued; the other two sources — every option's payload, every `T?` field of every aggregate — stay reachability-driven and must, because `emit_perfn` writes an option's `_eq`/`_hash` whether or not any instruction builds one. `m[k]` needs no rule: the value it copies through a descriptor is the result option's payload, already queued. **Cost: zero** — the `constructed` map rides the walk that already computes `reached`, measured at 22.04/22.50 s against 22.66/23.11 s, inside the same binary's own run-to-run spread. **And the textual question moved to where it costs nothing**: `tests/harness/suite_descriptors.hero` asks the blessed C **and the seed** whether every descriptor defined is pointed at and every one pointed at is defined — both directions, 145 files, no compiler run — plus the same check on every **fresh** emission inside `suite_emission`, one rule with two callers. It fired on exactly the two files an independent shell scan had found. Net **1132 passed, 0 failed**; 2 emissions re-blessed at −17 lines of dead C each; seed regenerated (22,036,674 bytes) and byte-identical between the seed-built compiler and the source-built one | selfhost/emit_descriptor_set.hero · selfhost/emit.hero · tests/harness/suite_descriptors.hero · docs/debrief/DECIDE.md
2026-08-23 | **Panel 088: the sitting that had already been held** (provisional — author ratification pending). Convened within the hour on `xs @ xs.push(v)` being quadratic (`runtime/parts/array.c:110` allocates `len+1` and copies every element per push; 25k/50k/100k pushes = 0.9/3.0/11.7 s of work, measurement 011) — and **panel 037, twelve days earlier, is titled *"Array growth, and the optimisation that fires nowhere and corrupts where it fires"***, with `design.md:1531-1537` carrying the same ruling and the sound form's name. CLAUDE.md §1 prescribes the grep that finds them; it was not run, and panel 084 already carries this exact class. **What the sitting added, and it was owed**: the measurement 037 assigned to M-selfhost-probe, which closed 2026-08-15 without taking it, and a working prototype nobody had | **The headline died of its own measurement.** Removing **99%** of push's element traffic (1,796,724,220 → 18,398,012) moved the harness emission **12.29 s → 11.46 s: 1.07×** — so push is 7% of the 46× that measurement 011 measured and deliberately did not attribute. **The gate is now measured impossible rather than argued**: `pushes=100000 rc1=0 rc2=100000`, `ys = xs.push(2)` and `xs @ xs.push(2)` both report rc=2 because the count is of slots and not of liveness (`ir_own.hero:14-16`, `:26-40`), and loosening to `rc<=2` prints `zs[10]=99` for 77 at **exit 0, clean under ASan+UBSan**. O3 is inside the noise on two independent seats. **O4 is vetoed** (ffi-pragmatist, §1.11: bulk C data reaches Heroes only as `str → [str]` through push). The sound form was **built**: `hero_array_push_owned(HeroArrayHeader **slot, …)`, 69 lines, ABI unchanged, 100k pushes **22.34 s → 0.00 s**, seven adversarial shapes byte-identical and sanitizer-clean — and two seats' prototypes differed exactly at `n.kids @ n.kids.push(n)`, where one exited 138 and the one whose guard re-reads the refcount **across** `elem->copy` returned a clean snapshot. **The spec's fear was inverted**: `spec:167-169` teaches the quadratic cost, so this was never a §12 compiler bug, and the *true* wording measures **3508 against 3512 — a 4-token refund**; leaving the sentence false is inadmissible (it has a dependant at `selfhost/measure_bpe.hero:51`, inside the tool that scores §1.6) and deleting it was already refused by panels 043 and 054. The ergonomist found the sentence **already doing harm**: warned about arrays and told nothing about maps, it wrote a counter-keyed map used as a list, which loses ordering at exit 0 and is **15× faster** (0.75 s against 11.7 s). Resolution: **nothing lands**, the place store stays adopted and waiting on 037's own two triggers, five conditions ride with it, and **the 46× becomes its own question** — with `emit_writer.hero:82` (`w.out @ w.out + text + "\n"`, 724,245 times, and `spec:167` says `+` copies both sides) measured at 25k/50k/100k lines = 0.45/1.2/8.4 s as the leading suspect. Three of the sitting's four process errors are the coordinator's, including a "frozen" worktree shared by five seats in which one instrumented the runtime another was timing | docs/panel/088 · docs/panel/037 · design.md:1531 · docs/measurements/011
2026-08-24 | **The 46× has its answer, and it was two lines: the writer and the loader** (measurement 012, closing the question panel 088 opened; both repairs landed the same session, no panel — module internals, no surface, no diagnostic, no spec token, output byte-identical by `cmp` at every step, seed regenerated in the same commit). `emit_writer.hero` accumulates in chunks — §4.10's own remedy, 128 lines per chunk, one `join` at `finish` — worth **407.16 s of the 944.76 s build (43.1%)**, which is R5's marked inference (~440 s) measured true. The sampler then found what nobody had suspected: `source.hero:156` re-ran `starts_of` over the whole accumulated text once per file to compute `lines_before`, so a 153-module compilation re-read its own concatenation ~76 times before any phase began — an incremental newline count over the appended segment is worth another **112.6 s**. Build **944.76 → 424.67 s (2.22×)**, tests **20m35s → 446.08 s (2.77×, 488 green)**, the net 1132/1132. The phase profile is now readable — load+lex 205.53 · parse 124.66 · check 34.52 · emit 54.33 — which **exonerates the check phase and the O(types) walks** (the brief's other two candidates) and leaves **78% of the build in `p @ push(p, v)` accumulators** that chunking cannot reach: there is no array `join` (panel 043), so the token stream and the AST arrays wait on the place store, and DECIDE R2 now carries the number its trigger was missing | the profiler R5 proposed (the split subcommands) was measuring the defect rather than the phases — the loader's quadratic sat inside every command, which is why subtraction said "check is 8m03s" while the checker costs 35 s; `sample` against a live run is what found both culprits, and the discard rule this leaves is in the measurement file: a timed run is `real ≈ user` or it is not a measurement. One more instrument fired exactly as designed: the loader repair grew `source.hero` past its frozen ceiling (304 → 326) and the net went red 1131/1132, so the module split along the seam its own headings name — `source_extent.hero` takes the root's-extent trio, chosen because its five users are the ones under their ceilings, where the Names seam would have forced a `use` line onto two files frozen at exactly their measured size | §4.10, §3.1, §3.5, CLAUDE.md §11 | 037, 043, 088 |
2026-08-24 | **The place store lands — the author's one-sentence trigger, pulled: "approvo panel 037"** (closing panel 037 item 2 and panel 088 R2; measurement 013). One IR op (`push_owned(place, value)`), one pass between mono and own (`selfhost/ir_place_store.hero` — on the IR every safety clause is a fact about the value: bare place, load and call read once, one block, no write to the root between them, so `qs @ qs.push(bump(@qs))` refuses itself and keeps the classic order), one runtime entry (`hero_array_push_owned(**slot)`, refcount-1-with-room appends, anything else copies into a doubled block, the copy-guard re-reads the count across `elem->copy`), ABI 14 → 15 honoured, spec amended in the same commit at **3506** (predicted 3508, falsifier ≥3512; ledger row 39). The compiler building itself: **944.76 s (2026-08-23) → 188.51 s (5.01×)**; its suite **20m35s → 198.72 s (6.2×, 492 green)**; fixpoint byte-identical over a three-link chain, because the repair changes IR the previous binary did not emit | **landing exposed a defect that had been masked for eleven milestones, and the landing's own named case caught it**: a borrowed load whose value outlives an `@` call on its root was alive only because rule 5's synthetic slots held every accumulator at refcount 2 — honest counts made it a use-after-free (`place-store-c5` case 5, "array with no element descriptor"; ASan on the previous compiler measured CLEAN, so the accident was verified rather than inferred). Repaired where it belongs, in the ownership pass: `load_survives_write`, an incref and rule 5's own slot machinery, cost inside run-to-run spread. Every frozen-ceiling file stayed at its size except ir_print (+1, the op's dump line — exhaustiveness growth, the DECIDED row moved with its reason) | §4.10, §4.20, Part 5, CLAUDE.md §7, §12 | 037, 088 |
2026-08-24 | **Nine decisions taken in `/decide`, all applied in the same session** (author answers `1a 2a 3a+a1 4a 5b 6a 7b 8b 9a`; every one of the nineteen candidates reproduced or measured before it was put to the author, and four of them turned out to need no question at all). **(1a) Panel 088 ratified** — the last of four sittings that could not be ratified because no list carried the item; the resolution stands, and what it settles is the sitting's own contribution (the measurement panel 037 asked for) rather than the landing, which was its own already-ratified row. **(2a) The four process texts confirmed**, nothing overturned; verified rather than trusted — `grep -rn "cargo" .claude/skills/` returns only the two historical notes, so no live instruction names a dead command. **(8b) Metric 2 deferred to M-guide-book** and moved back to `SCHEDULED.md` with a named home, because that milestone's own work IS the author writing programs, and `harness/tasks/` holds zero of the fifteen. **(7b, the refusing half) `5.` gets no diagnostic** — reproduced (`error[unused_binding]`, the number never named), refused because `5.` is not an intention in a language where a writer who means 5.0 writes `5.0`, and given a falsifier that was itself measured in both directions before it was written down. **(4a 5b 6a 7b 9a) One sitting instead of six**: eleven rows assembled into the 089 brief, handed over and NOT convened — `/decide` stops at a panel question by its own rule | a decision left open goes on shaping the code by default, and nineteen of them were; the four that needed no author were the cost this skill exists to avoid paying twice | CLAUDE.md §3, §12, §14 | 088 ratified; 089 briefed, not convened |
2026-08-24 | **The compiler stops binding POSIX: `write(2)` becomes `hero_write_err` in the runtime** (author decision `3a`, `/decide`; no panel — runtime C plus one compiler module, no surface, no diagnostic, no spec token, no ABI move). `selfhost/cli_io.hero` reached `write(fd: 2, …)` through `extern "unistd.h"`, which put `#include <unistd.h>` into `seed/heroes.c:5` — so the **self-hosted compiler could not be built on Windows at all**, and nothing caught it because the Rust bootstrap had no such dependency and the third CI leg was testing a compiler that did not have the problem. The route is not a platform arm (this language has no `#if`, by design) but `fwrite(…, stderr)` in `runtime/parts/os.c`, which is C89: the platform question stays in the one file in this project that is allowed to know what machine it is on, and the seed loses its only POSIX include. **The cheap-looking alternative was refused on a measurement**: `extern "stdio.h" constant stdout: ptr` is `error[ffi_not_constant]` at exit 1 — reproduced, and the diagnostic's own note says why — so ANY stdio route needs a C function on the other side and "one line in `cli_io.hero`" never existed. **It closes a §1.12 hole rather than only moving an include**: `write(2)` may write fewer bytes than it was given, and both shipping call sites discarded the count, so a truncated diagnostic was indistinguishable from a whole one; `hero_write_err` returns **nothing** and loops until the blob is out or the process has no error channel left. No `fflush`, and that is measured rather than assumed (C11 7.21.3p7, plus `fwrite`-then-`abort()` verified through a file and a pipe). No ABI bump: adding an entry point is a **link error** on a stale runtime, which is louder than the stamp, where the stamp exists to catch a wrong ANSWER | a platform the port cannot build is a platform Heroes does not have, and after the archive there is no other compiler | §1.11, §1.12, §4.19, CLAUDE.md §7 | none — no surface, no diagnostic, no spec token |
2026-08-24 | **Text from C stops killing the program: `validated(c: cstr) -> str?` lands as a Tier-2 library function** (`docs/panel/089`, full lane, five judges, provisional — author ratification pending; author instruction *"scegli la soluzione più robusta anche se implica usare più token"*, which names §12's tie-break). Measured at HEAD before the sitting: a program that null-tests exactly as `spec:229` instructed still died at `panic: hero_str_from_bytes: not well-formed UTF-8`, exit 134, on real C bytes — and **the shipping compiler could be killed by an environment variable**, `HEROES_RUNTIME=$'\xff\xfe' heroes doctor`, exit 134 (found by the compiler-engineer seat, reproduced by the coordinator, now exit 0). **The resolution is not new**: panel 059 refused `to_str` as the carrier on 2026-08-15 and RESERVED the second-name form as *"the shape any future sitting starts from"* — this sitting starts from it. Runtime: `hero_str_try_from_cstr(p, *status)` written BESIDE `hero_str_from_bytes` over the static `hero_str_alloc`, **one** validation walk — composed on top it measured **1.96x** on 16 MiB, beside it **0.87x**, because validation dominates the copy 25:1 (ffi-pragmatist condition 1). ABI held at **15**: adding a function is self-guarding, and bumping to 16 **breaks the seed** because `emit_decls.hero:77` hardcodes it. Library: zero checker lines, zero emitter lines (design.md:476-480's own §1.7 test, already run on `read_file`). Spec **3506 -> 3530, +24 net**, gross +39 against a **-15 named removal** — `spec:229`'s *"test `c == nullptr` first"*, redundant under §1.4 because the compiler is loud in both directions. **THE BRIEF WAS WRONG FOUR TIMES AND THE SEATS FOUND ALL FOUR** (`089` records them first): a ratified ruling read as a silence — the coordinator's own pre-convening grep listed `panel/059` and the coordinator read the filename instead of the file; a failed search written as an impossibility, the ffi-pragmatist compiling the whole SQLite case at HEAD with no language change; a runtime mechanism described wrongly; and the framing itself, since the measured question was **171 tokens against 51**, ergonomics under §4.19, not possibility under §12 | every language whose string carries a UTF-8 invariant ships two or more names at this boundary and not one aborts; the one that made it infallible is JNI, whose runtime kills the VM | §1.11, §1.12, §4.19, §1.7, spec:59, spec:181, spec:228 | 089; 059 as its starting point |
2026-08-24 | **Panel 089 ratified, and with it a correction to CLAUDE.md that the sitting's own payment rested on** (author instruction, *"ok a tutte le ratifiche e alla modifica al CLAUDE.md"*, hours after the sitting closed). The resolution stands as landed — `validated(c: cstr) -> str?` as a Tier-2 library function over a runtime entry point that validates once, ABI at 15, spec 3530 — and the yes was verified rather than assumed: net **1146 passed, 0 failed**, the compiler's own tests **491 passed**, the seed **identical** to what today's source emits, and the environment-variable kill **exit 0** where it was 134. **CLAUDE.md §12's SEGV claim is corrected, not deleted**: *"a null `cstr` handed straight to another C function is a SEGV in libsystem"* was true when panel 053 measured it on 2026-08-14, and `guard_cstr_arguments` closed it since — the same program is a named abort at exit 134 today, measured twice on 2026-08-24. The sentence keeps its date and gains its closure, and it gains the reason it is load-bearing: panel 089 deleted `spec:229`'s null-test advice as a **−15 named removal**, and §1.4 only permits that because the compiler is loud in both directions. Under the stale sentence the same edit would have read as deleting a warning. **This is CLAUDE.md §11's own shape found inside CLAUDE.md** — a premise that expired in silence while the sentence went on reading as correct — and the instrument that found it was a panel seat hunting for a token removal, not a review of the contract | a contract that states a danger the compiler has already shut will fund the wrong decision next time | CLAUDE.md §12, §1.4, §1.11, §1.12 | 089 ratified; 053's measurement dated rather than overturned |
2026-08-24 | **The last three unratified sittings are ratified: 022, 063 and 064** (author instruction, *"ok ratifica anche quelli"*, after being shown a two-line summary of each and told which one deserved a second's thought). **Every one was verified live before the yes was written down, not read off its own file** — six weeks is long enough for a resolution to have been quietly overturned. **022**: two records with seven bytes of padding compare equal and one finds the other as a map key, and `grep memcmp seed/heroes.c` is **0**, so `eq` and `hash` walk fields — R1's requirement, whose failure mode the sitting called *"a silent wrong answer generator"*. Its `HERO_RUNTIME_ABI → 3` is superseded by 15 and the rule behind it stands; §14 leaves the 3 on the page. **063**: verified in the PORT rather than in the archived Rust it measured — `selfhost/emit_extern_assert.hero:95` emits `HERO_RET_RECORD` over `__builtin_types_compatible_p`, conditionally, exactly as R1 and R5 resolved; Q4 stays queued to a full panel and the lane's unpaid costs stay unpaid. **064**: `clong`/`culong`/`csize` are **0** in the spec and **0** in `selfhost/`, and the 9-line integer-field relaxation is still what stands in their place. **A correction the coordinator made to itself in front of the author**: 064 was put to them as a permanent refusal that ratification would harden, and it is not — the spec-warden's own finding is that the row is **Part 7 item 10, not a Part 6 row**, so it is a deferral until the closure list compiles itself and owes no §12 falsifier. **Process note**: finding these three took three attempts, because two crude greps reported 51 and then 7 sittings as unratified. Both were artifacts — `## Author's verdict` is a recent convention and older sittings were ratified by DESIGN-LOG lines that do not repeat the panel number — and 028, 030, 032 and 033 carry `RATIFIED 2026-08-12` in their own status lines. **And the sentence that stood here for ten minutes — *"no sitting in `docs/panel/` is unratified from here"* — was FALSE and is corrected in the same commit that wrote it.** A fifth check, this time with a control that the earlier ones lacked (053 and 089, both known ratified, must register), found **eleven sittings from 2026-08-16 — 069 through 080 — whose own status lines still read `ratification pending` and which carry no yes anywhere**. They have `DECIDE.md` items, so they were queued; the ratification never came. That is the same gap panels 085–087 sat in for four days, one run of eleven instead of three, and it is now its own queued item. The lesson is not about those eleven: **this question cannot be answered by grepping**, because a ratification is recorded in at least four vocabularies across three files, and four of my five attempts returned a different number — 51, 7, 16, and eleven — each wrong in its own way, the second because *"ratification pending"* contains the string *"ratif"* | a ratification taken from a six-week-old file is a ratification taken from memory, which is the one thing CLAUDE.md §1 forbids outright | §1.7, §1.11, §4.19, Part 7, CLAUDE.md §12, §14 | 022, 063, 064 |
2026-08-24 | **All eleven unratified sittings closed in one batch: panels 069-079** (author instruction, *"ratifica anche quelle 11"*, chosen over reading them first). Each file gains a `## Author's verdict` naming what its own yes settles — the batch is recorded as eleven verdicts and not one stamp — with a shared clause that **a yes over a resolution is not a yes over the questions that resolution deferred**: every reserved veto, return condition and queued follow-up inside them stands. **080 was in my count and should not have been**: it is a `retro-record` adopted on author instruction given in advance of its own sitting, and its text says *"no ratification pending"* — the phrase my checker was matching on. Verified after, with a control the earlier checks lacked (053 and 089 must register as ratified): **no sitting in `docs/panel/` is unratified.** **What the eleven cost was eight days of nobody asking, and the fix for that is not this commit** — five attempts to count them by hand returned 51, 7, 16, 12 and 11, because a yes is recorded in four vocabularies across four files and because the string `ratif` appears inside the words `ratification pending`. The check that would work asks the **value in hand** rather than the vocabulary — every panel file carries exactly one `## Author's verdict` and it does not say `Pending` — which is CLAUDE.md §11's own distinction and is queued for `suite_records.hero`, where panel 086 R7 put the dead-citation check for the same class of rot | a contract that says a decision is not real until the author ratifies it owes an instrument that can name the unratified; without one the gap is found by accident, eight days late | CLAUDE.md §11, §14 | 069-079 ratified; 080 was never pending |
2026-08-24 | **The record can now say which decisions are waiting for the author, and it caught two of the coordinator's own mistakes on its first run** (author instruction, *"aggiungilo"*; no panel — a harness check, no surface, no diagnostic, no spec token). `records/verdicts` requires every sitting from **063** up to carry exactly one `## Author's verdict` that does not open with `Pending`. **The design decision is which question it asks**: five greps that day returned 51, 7, 16, 12 and 11 unratified sittings, because each asked about the VOCABULARY of the record — and a yes is written four ways across four files, the string `ratif` lives inside the words `ratification pending`, and `080`'s own *"no ratification pending"* read as a positive. This asks a fact about the FILE: one section, not `Pending`. CLAUDE.md §11's distinction, applied to the record instead of to the code. **Proven by making it fail** (§9): a `_Pending._` verdict and a deleted section each turn it red with a different message, and both restore green. **First run: 59 flagged, two real** — `063` and `064` carried two sections each because the coordinator had appended a verdict to files that already held a stub, and `064`'s naive repair would have taken its scored-predictions section with it. The other 57 predate the convention and are ratified elsewhere, so the floor is 63, exact rather than round, and documented as movable only DOWNWARD — raising it would hide what the check is for. `080` gained a verdict section describing what a retro-record is, so the rule has no exception | a contract that says a decision is not real until the author ratifies it owed an instrument that can name the unratified; without one, an eight-day gap over eleven sittings was found by accident | CLAUDE.md §4, §9, §11, §14 | none — a harness check |
2026-08-24 | **`args_checked() -> [str?]` lands: the last of panel 087's four doors closes, and argv stops being able to kill a program** (author instruction *"fai queste"*, over the shape panel 089's ffi-pragmatist compiled and queued; no sitting ruled on the spec sentence and this line says so). `hero_args_raw` joins the runtime beside `hero_args_at` — **borrowed, and NULL out of range rather than a panic**, because a caller that must branch cannot branch on a panic, while `hero_args_at` keeps its abort since an out-of-range index there is the program's own mistake (§4.9). `args()` keeps its signature and its abort for the twenty-odd programs that use it. Spec **3530 → 3541, +11**, the tighter of two measured wordings, paid by a registered prediction naming live instruments. **The design is `[str?]` and not `[str]?`, and the measurement is what shows why**: `prog good.txt $'\xff\xfe' more.txt` prints `arg 1: good.txt`, `arg 2: unusable (not_text)`, `arg 3: more.txt`, `survived`, exit **0** — one undecodable argument no longer costs the program the other two, where before the whole thing died at 134 | argv is the one input a program cannot decline to receive, and `hero_args_at` converted eagerly so no `cstr` ever reached the program and `validated` could not rescue it | §1.11, §1.12, §4.19 | 089's queued rider, landed by author instruction |
2026-08-24 | **A `cstr` field accepts every spelling C gives a string; the matching relaxation for a `char **` parameter is REFUSED, and the refusal is the finding** (author instruction *"fai queste"*, over panel 089's two compiled riders; the sitting queued both and one of them is wrong). **Rider 1**: a `cstr` field asked the member's ADDRESS to be `const char **`, so `struct passwd`'s `pw_name` — a plain `char *` — was `ffi_field_type` on a field the author had spelled correctly. It now asks the six spellings, and `void *` and `int` stay refused, so panel 083's repair is intact. The seat's own suggestion, reuse `HERO_RET_CSTR`, **is not available**: field assertions are emitted at `emit_decls.hero:84` and the macros at `:86`. The set therefore lives once, in `emit_assert_spelling.CSTR_SPELLINGS`, read by both positions — §11's rule that a list written twice dies in silence. **Rider 2 is refused on a measurement taken before a line was written.** The seat argued `ffi_writable_parameter` should not fire on an `@` parameter because §4.8 makes it the address of a local rather than a lent COW interior. But `char **` lets C write THROUGH the pointer — `void evil(char **s) { (*s)[0] = 'X'; }` compiles clean and turns `ziggy` into `Xiggy` — and that local's value may be exactly a lent interior. The same line through `const char **` is a C **compile error**, which is why the const form already works today (measured, exit 0, complete with `validated`). The compiler's existing split is right and the rider would have opened the hole it was meant to close | a panel judge's compiled recommendation rested on a premise about the WORLD — *"an `@` parameter is not that"* — rather than about the value in hand, which is the shape panel 089 spent its sitting cataloguing, found this time in the sitting's own output | §1.12, §4.8, §4.19, §4.20, CLAUDE.md §11 | 089's riders: one landed, one refused |
2026-08-24 | **Two sentences in which the specification contradicted itself, repaired — and the second one is now EXECUTED** (author instruction to work the queue with robustness as the criterion; §12 truth repairs, +12 together, 3541 → 3553). **(1)** § Tests showed `dist2(Point(...), Point(...))` while § Functions, ninety lines above, makes labels mandatory when two parameters share a type: `error[needs_label]` twice, exit 1, **identically on both compilers**, for seven days. The repair is two labels; **the payment is an instrument** — `tests/golden/run/spec-the-documents-own-example.hero` carries the example byte-for-byte and runs it, so the next divergence turns a test red instead of misleading a reader. An example nobody executes is §11's premise that expires in silence, and this one did. **(2)** `spec:14` claimed *strings and comments may contain any UTF-8*; measured, a raw carriage return is refused in a **string** and a **character literal** and **accepted in a comment**, so the sentence was false about exactly half of what it named — and a repair forbidding it everywhere would have been stricter than the language, panel 081's own §12 target. **Seven wordings measured, +8 to +18**; the cheapest true-in-both-directions one landed. The spec-warden's estimate for this row was *~±0 tokens* and was wrong by at least eight | a document that contradicts itself teaches the contradiction, and the compiler had been refusing the document's own example since 2026-08-17 | spec:14, spec:186, §1.4, §12 | 087's rows 7 and 8 |
2026-08-24 | **A field declared as an array where the header has a scalar stops being the compiler's fault** (author's chosen option (a), panel 085 R7's mirror). `i8[4]` against `struct dirent`'s `unsigned char d_type` was `internal error: compiling the generated C failed`, **exit 2** — §7's line about a failure the author's own `extern` caused being exit 1 — and is now `error[ffi_field_type]` at **exit 1** with the caret on the field. **The cause is C's own rule and it has now bitten twice in the same file**: every operand of `&&` is type-checked whether or not an earlier one is false, so three ill-formed `place[0]` subscripts broke the build instead of failing the assertion. Panel 085 R7 moved the SIGN test off a cast for that reason; this is the same fact one position over. The repair asks the member's **address** — `&place` is `T (*)[N]` for an array and `T *` for a scalar, both well-formed — with each association's expression a constant over a C **type name**, so a scalar lands on `default: 0` and fails cleanly. Width and sign, never identity (panel 064): twelve C integer types enumerated, the length riding inside every association as `(*)[N]`, and the separate length conjunct deleted. **Thirteen shapes attacked, thirteen correct**: `char`, `signed char`, `unsigned char`, `short`, `int`, `long` and a `float` array accepted — `long` as `i64` is exactly why identity is the wrong question — and a scalar, a wrong length, a wrong sign, a wrong width, a float array declared integer and a pointer array declared integer all refused at exit 1. Option (b) was refused on its measured cost: it renumbers 56 `#line` restores in five goldens where `UPDATE_GOLDEN` is forbidden. This cost **3 re-blessed emissions**, not 145. The twelve types now live once, in `emit_assert_spelling.C_INTEGER_TYPES`, shared with `HERO_C_INTEGER` | §11: a set written twice drifts, and here it would drift silently — a real C type nobody enumerated reads as *the header disagrees* when it agrees perfectly | §7, §4.19, CLAUDE.md §11 | 085 R7's mirror, closed |
2026-08-24 | **`readdir` works, and the hypothesis that constructibility was blocking it is falsified by running it** (panel 085's could-not-reproduce row). Four shapes measured: `struct dirent` **is** unconstructible (`fixed_array_length`; 1,024 literal elements is not a program anybody writes) — confirmed; declaring `readdir` to return the record by value is correctly `ffi_return_type`; binding it as `ptr` is accepted and the fields are simply unreachable; **and a real directory lists today** — `entries: 6`, `has heroes_runtime.h: true`, exit 0 — through a two-line C accessor plus `validated`. So constructibility blocks nothing. What stops a program reading `d_name` is that C returns a POINTER to a struct and this language has no dereference for an opaque `ptr` — which is precisely the pointer question the item said this was not | the item asked whether design.md Part 6's struct row was the wrong suspect; measured, it is the right one, and the completeness position is that a returned struct pointer needs its accessor written in C, which is §1.11's founding premise rather than a hole | §1.11, §1.12, §4.19, Part 6 | 085, closed by measurement |
2026-08-25 | **Panel 090: `cstr.to_str()` is retired, the spec names argv's abort, and the sentence that set the `continue` trap is replaced** (`docs/panel/090`, full lane, five judges, provisional — author ratification pending; author instruction *"panel su tutte"* with *"il linguaggio deve essere super solido"* as the criterion). **Two of the five queued questions never reached a judge**, because the record already held their answers — a bytes reader (panel 087: *"Principle 0 has no compiler-need today, measured"*, with the trigger that returns it) and the not-text `e.code` (panel 087 states a reversal condition, so it is work rather than a question). That filter is what panel 088 did not run. **Q1 — retirement.** The ffi-pragmatist went looking for a program retirement makes worse and found the opposite: real SQLite, a real `x'fffe4142'` blob, **guarding `cell == nullptr` exactly as the spec once instructed** — exit 134; the same program with `validated` — exit 0. *The null guard does not save you, because the failure is not nullness.* And the mechanism question closed by a sweep rather than an argument: C's only way to promise a non-null return, `returns_nonnull`, occurs **2 times in 3120 SDK headers** and **0 times in sqlite3.h, curl.h, raylib.h, SDL2, SDL3, string.h and stdlib.h** — no real library gives a program anything to know with, so every *"I know this one is safe"* is a §11 world-premise. Cost, measured twice independently: **~25 lines / 5 files, 11 sites, 0 spec tokens, ABI unmoved, +15% per conversion**; the warden proved no compiler-need by APPLYING the retirement in a worktree and running `check selfhost/main.hero` clean. The historian found **no precedent anywhere** for removing an aborting FFI conversion, and the warden answered it: Swift kept its trap for source compatibility with a shipped ecosystem, and Heroes has eleven call sites and no users — the precedent's *reason* does not transfer. **Q2 — both ballot options refused, and the convener's own wording asserted something FALSE**: `q2-full` said a float too large to render aborts, and `inf` prints at exit 0 (measured by the warden, reproduced by the coordinator). The abort that mattered was not on the ballot and both compiling seats found it independently — `args()`, a correct program killed by an argument the *user* typed, passing every test the author runs because the author's argv is UTF-8. **Q3 — `pass` vetoed twice** (§1.7: 27 exhaustive-match sites and it reduces to nothing; Principle 0: the compiler writes the `x @ x` no-op **296 times** and compiles today) and the proposed checker diagnostic vetoed on a measurement: it fires on **3 of 3** `while`-enclosed `continue` arms in the compiler and **all three are correct** — 100% false positives. **124 of 127 `=> continue` arms are in a `for`, where the same shape terminates** | the blind seat wrote the hanging program itself, rated its own confidence 7/10, and explained the funnel: `_` forbidden as a variant catch-all, a computed value must be used, and *"a jump is a valid arm body"* — doors close until `continue` is the only blessed spelling and `continue` skips the counter | §1.7, §1.11, §1.12, §4.19, spec:60, spec:130, spec:183, CLAUDE.md §12 | 090; 059 and 087 as its starting points |
2026-08-25 | **The spec package lands five tokens BELOW where it started: 3553 → 3548** (panel 090). Two clauses in, one sentence out. **In**: *"An arm that does nothing is a block holding `_ = 0` — `continue` is not one"* (+24), which **replaces** the sentence that set the trap rather than appending a warning to it — the blind seat's objection to warning-only was the *vacuum*, and naming the route fills it, which is also PHP's shape after it withdrew its own removal RFC and shipped a warning naming the alternative; and *"`args()` … one that is not UTF-8 aborts … `args_checked()` … which does not"* (+16), which repairs a second defect for free by giving `args_checked` the reason §1.6 says it needs. **Out**: `spec:227-228`'s `M_PI`/`strdup`/`fileno` sentence, **−45**, the only passage that is platform commentary rather than a language rule and the only one whose wrong guess is harmless — a reader who does not know `M_PI` is bindable writes the digits and gets a correct program. The warden is explicit that this is NOT panel 081's or 084's shape: the sentence is true and non-redundant, merely low-value, so it is a design act the author ratifies rather than a free sweep. **Two removals it found and REFUSED to spend**: `Point(3, 4)` (−12), the sentence that stops the likeliest error in this language from being written, and the NaN idiom (−12), §1.4's canonical silent bug | a warden that only counts tokens is doing half the job | §1.2, §1.4, §1.6, panel 012, panel 035 R4 | 090 |
2026-08-25 | **A real C name declared under the wrong header stops being the compiler's fault** (defect, no panel — one marker in the ffi error mapper; found by the coordinator's own sloppy test during panel 090). `extern "stdlib.h" function strstr(...)` was `internal error: compiling the generated C failed`, **exit 2**, while a name NO header declares was already `ffi_unknown_name` at **exit 1**. Same author mistake, two verdicts. The cause: **clang has three words for it and the mapper knew two** — `call to undeclared function '` and `use of undeclared identifier '` were matched, and a function clang recognises as a C library name produces `call to undeclared LIBRARY function '`, a different string. Now `error[ffi_unknown_name]: `stdlib.h` declares no `strstr`` with the note that names the header that does | §7 says a failure the author's own `extern` caused is exit 1 on the author's line; this was the fourth shape of that rule found in one week | CLAUDE.md §7, §4.19 | none |
2026-08-25 | **Panel 090 ratified the night it closed** (author instruction, *"ratifica 090"*). The resolution stands: `cstr.to_str()` retired, argv's abort named, the `continue` sentence replaced, spec at **3548** — five tokens below where the package started, +40 in against a −45 named removal. **The clause with the least precedent is named first in the verdict rather than buried**: no language in seven surveyed has ever removed an aborting FFI conversion, and the yes adopts the warden's answer — Swift kept its trap for source compatibility with a shipped ecosystem, Heroes had eleven sites and no users, so the precedent's reason does not transfer while §1.12's tie-break does. What the yes does not settle is written down: the `cut-header` removal is a design act, not a free sweep, and the two removals the warden found and refused to spend stay unspent | the sitting refused two of the convener's three proposals and caught one of its wordings asserting something false; the filter that struck two questions before any judge saw them, and the four corrections, are the part that transfers | §1.12, §1.6, §1.7, CLAUDE.md §12 | 090 ratified |
2026-08-25 | **The `ORDER:` mark stops being a grep and becomes an instrument** (M-separate-compilation step 1, closing `SCHEDULED.md`'s panel 086 R4 item; no panel — the convention's shape is design.md §4.9's own sentence, obeyed rather than amended). A map walk whose order somebody can see owes an explicit `sort` and a mark naming the key and the observer; the inventory was `grep -rn "ORDER:" selfhost/`, and **a grep cannot tell a mark from a sentence quoting one** — measured today it returned **10 hits in 8 files, four of them prose** (three quotations of a bootstrap mark, one mid-sentence mention). **The item's own premise fell to the measurement**: it read the four unmarked walks as *"the port's walks happen to agree"*, and all four are **already sorted** — three pay `sort(keys(…))` and lacked only the mark, while `ir_print.hero` has no map at all (the port replaced the bootstrap's `BTreeSet<u32>` with a dense `[bool]`, so the walk is ascending by construction). The fixpoint was green because the sorts were paid, not by luck — CLAUDE.md §1's *sentence whose truth comes from another sentence*, found in a record rather than in a brief. What lands: `# ORDER:` counts only as the **first word** of a comment, so prose can no longer inflate the count; the inventory is **12 files / 16 marks** against the bootstrap's 13; three bare `keys(` walks nobody had ever ruled on carry `# ORDER: none — <why>` (a worklist whose output is a set, a count, a bool), which is a judgement recorded rather than a rule dodged; and `tests/harness/suite_order.hero` fails the net when a walk in `selfhost/` carries no mark. The check searches **the mark's own scope** rather than a line window — up through the function body, across its header once, into the doc comment — because a real mark sits 25 lines above its sort (`resolve_cycles.hero:177`) and a window wide enough for that would reach into the declaration above. **Made to fire before it was believed**: a mark deleted and an unsorted `keys(` added, both reported with file, line and the offending text | a convention nothing executes decays into prose, and the decay is invisible because the grep keeps returning hits — the cost of the unpaid version of this sort was 20.8 MB of C whose LINES matched and whose SEQUENCE did not, the last thing between the port and the fixpoint (2026-08-17) | §4.9, CLAUDE.md §1, §9, §11 | 086 R4, closed |
2026-08-25 | **The net's own tests get a caller, and both reds turn out to be the test being wrong** (author decision, `/decide` answer `a`, taken and applied the same session; found while running step 1's own new cases). CI drove the compiler two ways and **never asked whether the thing driving it was right**: `heroes test tests/harness/main.hero` had no caller, and while nobody asked, **two of its 81 blocks sat red** — reproduced identically at `a4d7ca4` in a clean worktree, so they long pre-date the step that found them. **A first hypothesis was tested and falsified** (stale scratch state; `rm -rf build/harness-selftest`, still red), which is what turned a description into a cause. **(1)** `suite_emission.hero:270` asserted `passed == 2` while `sweep` had grown a **third** check (`suite_descriptors.judge`, added inside it at M-bootstrap-archive) — the code right, the number beside it never updated; it now says 3 with the three named, so the next check added to that function is one obvious edit. **(2)** `suite_run.hero:134` asked `shell.some_compiler()`, whose candidate list held `./heroes-from-seed` · `./build/heroes-seed` · `./target/debug/heroes` — **`./heroes` was not on it** (the path every command in CLAUDE.md § Commands names) and the **archived bootstrap's was**. So the self-test reached a `build/heroes-seed` dated 2026-08-19 and every program it built died on `_Static_assert` against `HERO_RUNTIME_ABI 15`: **the stamp doing exactly its job** (CLAUDE.md §7's version skew), against a test that trusted whatever binary was lying around. **The file had been contradicting itself in place for six days** — its own comment says *"the path they must not name is the bootstrap's"* with the list underneath naming it — which is the argument for the leg rather than an aside about it. The leg runs on **every push**, not tags, and the number is why: 5,606 lines of harness rather than 38,021 of compiler, **13.5 s measured** against the 20m35s that made *The compiler's own tests* tags-only. `HEROES_COMPILER` is named in the leg rather than searched for, so CI never walks the candidate list at all | a suite with no caller decays exactly like a comment with no instrument, and at the same speed: the count broke the day a check was added, the list broke the day the bootstrap was archived, and both went on reading as correct | CLAUDE.md §7, §9, §13 | none — a decision, not a design change |
2026-08-25 | **Panel 091: the `extern` block is NOT pruned by reachability, because the architecture deletes the duplication for free** (`docs/panel/091`, full lane, five judges, provisional — author ratification pending; opening M-separate-compilation step 2 on the author's *"panel ok"*). The sitting was convened on a real number — a `print(1)` program emits **166 lines** of C, **27** of them for a library `extern "hero_os.h"` group it never touches, and **138 of 155** selfhost modules would carry that under one `.c` per module — and the answer is **A, +0 spec tokens and +0 compiler lines**. Three measured facts compose it: the library is **one module** (`source.hero:109-110`), its externs are called only from inside it, and the milestone delivers one `.c` per module — so with acceptance row 1 landed every group is emitted in **exactly one TU** and the multiplier dies with nothing written. Option B would have cost **+40 lines and 146 of 146 re-blessed emissions** to buy what the close gives away. **The ballot died on the seat that compiles**: seven experiments, four decisive — pruning the `#include` deletes a group record's C type (12 errors, `incomplete type 'struct timespec'`; the engineer reproduced the class independently as `unknown type name 'Widget'` at exit 2 on a legal program); an **uncalled** group holds up a used one (`<stdio.h>` before `<jpeglib.h>`, else exit 2 `internal error` on a correct program); `libraries()` walks the same list as `headers()`, so pruning takes the `-l` with the declaration and a constructor library silently stops running at **exit 0**; and **the probe is a memory-safety instrument** — `getline`'s `size_t` out-parameter declared `i32`, never called, is seen by the probe alone, and executing that conversion wrote four bytes into an adjacent object with **ASan and UBSan silent at exit 0** (§1.12, which CLAUDE.md §12 says outranks tokens). B was refused too: a runtime with `hero_file_write`'s parameters swapped and `HERO_RUNTIME_ABI` still 15 is caught by the library's 19 lines **and by nothing else** — panel 037's measured hole, closed by what B would delete. Precedent agrees and adds the shape: **nobody prunes FFI checking by use**, and the two systems that verify against real headers (GHC `CApiFFI`, cffi API mode) both emit per declaration at the **declare site** — which is what row 1 already decided, so it is a no-op today rather than a sixth option | **four of five seats corrected the convener, and the worst error was the convener's own §1 violation**: the brief said *"the mechanism already exists"* because `reachable` is called one function away, and `emit_builtins.hero:171-187` discards `.extern_fn` in **both** arms — the walk cannot see an extern at all, which is 30 of B's 40 lines. An inference from reading one call site, written into a brief as a measurement, by the seat whose job is to police exactly that. Also: a spec delta quoted without its line wrapping is not reproducible (+4/+5 claimed, +2/+3 measured), and a line count with no unit had two right answers (19 asserts+probes, 27 with the `#line` directives and the `#include`) | §1.1, §1.6, §1.11, §1.12, §4.19, spec:203, CLAUDE.md §1, §7, §12, §13 | 091; 030 R2, 033, 037, 061 and 087 as its starting points |
2026-08-25 | **The ratification check could not have found anything, and it said so by firing on the sitting it was built for** (defect, no panel; found by `heroes run tests/harness/main.hero -- ./heroes records` refusing to commit `docs/panel/091`). `suite_records.hero`'s `queued()` asks whether an OPEN item in `DECIDE.md` names a pending sitting — the second half of the check, added **hours** after the first on 2026-08-25 because a check that fails on every freshly-held sitting contradicts CLAUDE.md §4's *a panel never blocks*. It built its needle as `"panel " + number.to_str()`, and `sitting_number` parses `091-*.md` to the **integer 91** — so it hunted for `panel 91` while every reference in this repository writes `panel 091`: measured that day, the padded form appears 30-odd times in `DECIDE.md` and the unpadded form **zero**. `queued()` therefore returned false for every sitting in the entire live range, 063 through 099, and the correction was **inert from the hour it landed**. It never showed because the only pending sitting since was ratified the same night. The repair is `sitting_digits()` — the digits **as the filename spells them** — with the integer kept for the floor comparison that needs one, and both forms accepted at the queue because a human may write either and neither names a different sitting | CLAUDE.md §11 in one line: the file's own digits are a fact about the VALUE IN HAND, while *how this repository spells a number* is a premise about the world — and it expired in silence inside the instrument whose whole job is to catch that. The check found its own defect by being run against a real pending verdict for the first time, which is §9's *a check nobody has seen fail is a decoration* paying out twice in one day | CLAUDE.md §4, §9, §11, §14 | none |
2026-08-25 | **Panel 091 ratified the day it closed** (author instruction, *"ok ratifica"*). The resolution stands: the `extern` block is **not pruned**, +0 spec tokens and +0 compiler lines, because the milestone's own architecture deletes the duplication — the library is one module, one `.c` per module plus acceptance row 1 puts every group in exactly one TU, and option B would have cost +40 compiler lines and 146 re-blessed emissions to buy what the close gives away. **What the yes closes** is panel 087's queued half: a new `extern constant` costs **+2 lines in one translation unit**, not in every program in the language, so the objection that an FFI addition taxes the whole language is answered by WHERE the block lands rather than by whether it is written. **What it deliberately does not close**: the refusal is scoped to *reachability*, never to *placement* — declare-site emission is acceptance row 1's own consequence and needs no second sitting, with `F-restricted`'s boundary already named (the `#include` and the `-l` stay in every unit that needs them, unconditional) | the sitting refused every option its convener put on the ballot, corrected the convener four times, and the answer got cheaper at each correction — from +40 lines and 146 re-blessings to nothing at all | §1.12, §4.19, CLAUDE.md §4, §12 | 091 ratified |
2026-08-25 | **Panel 092: the parameter check was there all along, and a `_FORTIFY_SOURCE` macro was hiding it from half of libc** (`docs/panel/092`, full lane, five judges, provisional — author ratification pending; author instruction *"per la spec fai la cosa più robusta a costo di prendere token di spec"*). **The sitting was convened on a false premise and every seat said so.** The convener claimed a parameter's width and sign are unchecked; measured, `sqlite3_sleep(ms: i64)` is exit 1 *"wider than the header's `int`"* and `(ms: u32)` is exit 1 *"a different sign"* — `-Werror=shorten-64-to-32` and `-Werror=sign-conversion` have shipped in `cli_flags.hero` all along, and the convener had measured with `-Wall -Wextra`. **The second diagnosis was wrong too**: not clang builtins — `strncmp`, `malloc`, `calloc`, `abs` and `fwrite` are builtins and are all caught. What hid the rest is **`_FORTIFY_SOURCE` macro rewriting**, `clang -E` showing `(void)__builtin___memset_chk(a0, a1, a2, __builtin_object_size(a0, 0))`, which `-fno-builtin` cannot touch because a **macro** does the redirect. **The repair is two production lines**: `(void)(f)(…)` is not a function-like macro invocation, so `memset(n: i32)` against `size_t` goes **exit 0 → exit 1** with the full diagnostic and its fix, while the correct `u64` binding stays green. It **immediately found a live defect in this repository** — `examples/curl/main.hero:44` declared `option: i32` where `CURLoption` is unsigned, invisible since the example was written, and repairing it surfaced the C fact underneath: an enumeration **constant** has type `int` while the enumeration **type** may be unsigned, so the constant is `i32`, the parameter is `u32`, and the call converts. A third line repairs a defect found on the way: `emit_ops.hero`'s `(void *)`/`(const char *)` result cast is emitted **only where there is an assignment** now, because with the result discarded it became a cast expression whose value is unused — the **compiler's** warning, on a **correct** program, routed to the **author's** line. **P1 and P2 vetoed by both compiling seats**: Heroes has one pointer spelling, so `spec:206`'s own `sqlite3_open` warns on a correct binding and `-Werror=incompatible-pointer-types` would stop the spec's own example from building — *"not a strict compiler, an unbindable library"* — which is design.md Part 6 row 10's own named falsifier arriving as a program. **The historian's disjunction was falsified by the compiler-engineer compiling it**: 16 of 25 correct bindings rejected, including the compiler's own `getenv` | **four of five seats overturned a premise of the brief and the answer got smaller and truer at every correction** — from a proposed new mechanism, to a flag, to two lines that were always available. The convener's own error is CLAUDE.md §1's named shape twice in one sitting: generalising from a sample (`-Wall -Wextra`, then builtins) to a class, and relaying it to two seats mid-flight before it was checked | §1.4, §1.11, §1.12, §4.19, spec:204, CLAUDE.md §1, §7, §9, §12 | 092; 037, 047, 064, 091 and Part 6 row 10 as its starting points |
2026-08-25 | **The spec gains one row and it is the row a reader cannot derive: `u64` where C says `size_t`** (+12, measured with `./heroes measure` at 80-column greedy wrapping; 3548 → 3560, headroom 536; panel 092, provisional). **Paid by a registered prediction rather than a removal** (panel 012 branch 2 as amended by panel 046), scored at M-separate-compilation close by two instruments that exist today. **Why one row and not the three that were proposed**: `int`→`i32` is already at `spec:205` and `f32` is C's `float` is already at `spec:50`, and this file's preamble says a rule is written in exactly one place. `size_t` appears **nowhere** in the document, its width is not in its name, and the llm-ergonomist — given the spec and nothing else — guessed it at **5/10 confidence**, *"cited from nothing"*, then wrote a binding that the measured compiler builds, runs, and kills at exit 137 when the value is negative. **The sentence naming what is NOT checked was deliberately not bought** (+24 measured): the compiler-engineer approves `-Wdouble-promotion` under a `#pragma clang diagnostic error` today given three named measurements, and that closes the float half — so writing the sentence now means writing it twice, which is the second payment under §1.2 the spec-warden warned against. It is queued to be written once, at its final size | the seat that reads only the document found the hole the seats with the repository could not see, and the seat that counts refused two thirds of what it asked for — on the ground that the tokens were already spent elsewhere | §1.2, §1.4, §1.6, spec:204, spec:50 | 092 |
2026-08-25 | **Panel 092 ratified the day it closed, and the decision queue goes to zero** (author instruction, *"ok ratifichiamo e chiudiamo tutto"*). The resolution stands: the probe's callee parenthesised, the discarded-result cast `(void)`, the spec **+12** at 3560. **What the yes settles is that nothing was built** — the check, the diagnostic and its fix all existed, and three production lines let clang reach them; the robust answer turned out to be the small one, and it was small because four seats refused the large ones. Reachability (panel 091), `-Werror` on the pointer families, and whole-type identity are now all closed **with a compiled counter-example rather than an argument**, so a future sitting that wants any of the three must overturn a measurement. **What it does not settle is queued rather than implied**: `-Wdouble-promotion` under a `#pragma clang diagnostic error` closes the float half and is approved *today* pending three named measurements, and the spec sentence naming what is unchecked is bought **once, at its final size** — a wider by-value integer and a pointer's target — rather than twice at two sizes | §1.2, §1.4, §4.19, spec:204, CLAUDE.md §1, §7 | 092 ratified |
2026-08-25 | **CLAUDE.md §7's flag citation is repaired, on the third failure of one sentence** (author instruction, *"chiudiamo tutto"*; found by panel 092's historian — a seat with **no `grep`**, reading the contract as a stranger would, and confirmed by the compiler-engineer and the coordinator). It named `commands/flags.rs::FLAGS`, a path that lives only in `archive/bootstrap-rs/` and that nothing builds; the live list is `selfhost/cli_flags.hero::flags()`. **The shape is the finding**: the paragraph immediately below says *"the copy that used to stand here is deleted rather than corrected a third time"* — the copy WAS deleted, and its **replacement citation** then rotted, which is the same failure one level up. A pointer is a premise about the world and expires exactly like the counts that sentence has already been wrong about twice. Landed with it, from the same sitting: `__typeof__` and `__builtin_types_compatible_p` are **clang extensions that work under `-std=c11`**, measured, so `-std=gnu11` is load-bearing for `M_PI`/`strdup`/`fileno` (panel 047) and for nothing else — which stops a future cleanup from reading the `gnu` as load-bearing for a checking mechanism it does not carry | §11's expiring premise, found in the contract that states the rule; and the instrument that could have caught it reads these two files for milestone NAMES, not for PATHS — recorded as an observation, not built | CLAUDE.md §7, §11, §14 | 092 |
2026-08-25 | **The float half closes with four flags where the sitting expected one, and the probe block becomes the place where a conversion is always a verdict** (M-separate-compilation step 4, landing panel 092's queued half; no new sitting — the mechanism, the scope and the sentence are 092's own ratified rows, executed after the measurements its engineer named). **The queued premise fell to its own protocol**: `-Wdouble-promotion` closes a quarter of the float half, measured before a line was written — `f64` against a header's `float` still demoted in silence under it, a float against a header's INTEGER dropped its fraction in silence, and `-Wconversion` cannot see the promotion at all because it is value-exact. Attacking the adjacent shapes (CLAUDE.md §1's rule for repairs) found the integer family open below the 64→32 window too: `i64` against `short`, `i32` against `short`, `i16` against `char`, all silent. The landed pragma carries four errors — `double-promotion`, `implicit-float-conversion` (which brings `implicit-int-float`), `float-conversion`, `implicit-int-conversion` — each with a witness made to fire first. **The scope is load-bearing and now measured, not argued**: enabled globally the four fire on **19 of 146** blessed emissions of correct programs (`s[i]` into a `u8`, `f32` literals, one float→int the emitter writes deliberately), so none of them can ever enter `flags()`; inside the block a probe's arguments are exactly the declared types, so any implicit conversion IS a declaration disagreeing with the header. The engineer's other two measurements: runtime exit 0, zero warnings under all four; all four wrong directions exit 1 on the author's line with the right fix (`f64`, `f32`, `i32`), and the corpus's correct float bindings (raylib `f32`, gallery `f64`) run untouched. The mapper learned three needles and got the fourth free — clang files the sub-32 narrowings under the SAME text NARROWED matches. `emit_c_spellings` gains its two float rows (`double`→`f64`, `float`→`f32`), so the fix names the repair instead of declining; `long double` is the new live decline. **The spec sentence landed once, at its final size, +32** (3560 → 3592, ledger row 45, paid by a registered prediction scored at this milestone's close): a parameter that disagrees is refused, except what C converts exactly (`i16` against int — bare, because `spec/rejected` refuses spec code the lexer rejects, and it caught the backticked form the minute it was written) and what a pointer points at. Cost elsewhere: 146 emissions re-blessed +2 new (the diff is the pragma block and the `#line` renumbering under it, no content line), six `emit/` goldens regenerated with their diffs read, seed regenerated, fixpoint byte-identical | a repair shipped without its adjacent shapes is CLAUDE.md §1's named failure, and this one had four neighbours — the sitting's flag was necessary and not sufficient, and only running the shapes said so | §1.12, §4.19, spec:205-207, CLAUDE.md §1, §7, §9, §12 | 092's queued half, closed
2026-08-25 | **Acceptance row 1 lands: an `extern` is never reachable across a module boundary, and the rule grew two gates the ruling had not named** (M-separate-compilation step 5, executing panel 033 R5 — ratified 2026-08-12, queued by that sitting for this milestone — at panel 091's measured price; no new sitting, the class was ruled and the step obeys it). The bill was exact: **7 qualified call sites in 4 modules**, re-measured before a line was written, all routed through `cli_shell.shell(command: str)` — a Heroes function beside the extern, compiled in the one TU that carries stdlib.h's checks, with `.cstr()` moved inside so the callers stopped converting by hand. **The gate sits on the qualified REFERENCE, not the call**: `resolve_qualified.qualified()` is the one point every qualified mention passes, so `_ = bind.system` — the extern taken as a value — is refused identically, because taking the address needs the header exactly as calling does. **Two open shapes the ruling had not counted, both measured and both closed the same hour**: the library is a module whose names are visible unqualified (top_visible's fallback), and `hero_args_count()` from a user program was check-clean at exit 0 — with ZERO direct users in the repository, one grep hit and it is a comment, so the closure deleted a class of program nobody had written; and UFCS reached the same externs through the dot (`"p".cstr().hero_file_read(@status)`). Three gates — qualified, value-position name, method name — one diagnostic, `error[extern_across_modules]`, whose library arm names the route a reader can take (the wrapper) rather than one they cannot (editing the library). Group `constant`s and `record`s stay reachable qualified, deliberately and in the comment: their C is compiler-written on both sides, so the header matters only where they are declared. Verified both directions: `surface-fixtures/externroute/wrong.hero` exit 1 on call and value, `right.hero` builds and runs exit 0 through the wrapper, `check/extern-across-modules-library.hero` pins all three library spellings, the compiler checks its own routed source clean, and the harness needed nothing — its extern was already wrapped, zero qualified calls. **The layout instrument fired on the landing and the answer is a split, not a squeeze**: resolve_walk.hero was frozen AT its 366-line pin, so the two gates took it to 386 and the net went red — the seam that names a concern is the LOOKUP (how a bare or dotted mention finds its declaration), and it left as `selfhost/resolve_names.hero` (141 lines, module doc carrying the why), with the walk down to ~250 code lines, under the default ceiling for the first time since the port | the ruled rule was one sentence and the landed rule is three gates, because "callable" undercounts the ways a name crosses a boundary — the value and the dot are the adjacent shapes CLAUDE.md §1 says a repair owes, found by reading the resolver rather than by a defect | §1.11, §4.19, CLAUDE.md §1, §8, §9 | 033 R5 executed; 091's bill paid
2026-08-25 | **Panel 093: per-module TUs, and where a monomorphised instance lives** (`docs/panel/093`, SOUNDNESS lane — compiler-engineer + ffi-pragmatist, both compiling; provisional — author ratification pending; opening M-separate-compilation step 6 on panel 029 R5b's deferred question). **Both seats hand-built the architecture before judging it.** The pragmatist split a real SQLite binding over two TUs (works, ASan-clean, wrong declaration exit 1 in the declaring TU) and **made the new boundary bleed**: a cross-TU prototype disagreeing with its definition compiled clean and printed `4317207840` at exit 0 — the first compiler-written C clang never verifies — with the fix verified (the declaring TU includes the interface text it exports → `conflicting types` where the author can act). It also measured the emitted C **link-blind** (`link "sqlite3"` → `"NOTALIB"`, byte-identical output), which kills any cache key derived from emitted C. The engineer mapped the split (~480–720 non-test lines, ~700–1000 with tests — design.md:2788's pricing holds), hand-split a two-module program and got **byte-identical behaviour** through cross-TU equality, map hashing computed by two different static `hash` copies, and the leak gate; found the per-TU reachability trim MANDATORY (11+13 unused-function warnings without it); scored panel 030 prediction 8 in advance as **false** (placement near-zero; the cost migrated to key/interface machinery); and built the falsifier that decides the interface question — reordering two empty variant cases changes only the tag enum, so a curated interface list ships a warm-cache consumer that **prints the wrong case at exit 0**. **Two corrections to the convener's brief, recorded first**: define-site placement does NOT die on nameability (it links and runs — the cache arrow kills it: the defining TU's text would depend on its dependents), and "descriptor externs" named an import the architecture abolishes (descriptors were already static). Resolution R1–R6 adopted provisionally: two paths one emitter · static-per-TU placement with the trim · walk-rendered interface text included by its own declaring TU · source-text-based keys with resolved package flags · row 4 re-read (every dirty TU compiles before any verdict) · TU-stable typedef and string names. Four instruments owed with the landing | the sitting corrected its convener twice and no seat needed the other's evidence to converge on the same two repairs — the key never reads emitted C, and nothing curated stands between a dependency's bytes and its users | §1.11, §3.1, §4.19, design.md:787, :2788, :2806, CLAUDE.md §1, §7 | 093; 029 R5b, 030 R1/R2, 033, 091 as its starting points
2026-08-25 | **Panel 093 ratified: per-module TUs, the static instance, and a cache key that never reads emitted C** (`docs/panel/093` § Author's verdict; author decision, `/decide` answer `1a`, the day the sitting opened). All six resolutions stand, nothing deferred, no seat overturned. **One of the six was already delivered when the yes arrived**: R6's name stability landed as M-separate-compilation steps 6a (`809d64a`) and 6b (`995c4de`), so option typedefs, function-type typedefs and string statics are now named by content alone — identical across translation units, builds and programs, which is the ground a dependency's interface text stands on. What the yes settles beyond R1–R6 as written: the key takes **source text**, never the emitted unit (the pragmatist's `link "sqlite3"` → `"NOTALIB"` byte-identical falsifier); a compiler-written prototype is verified by clang or it is not written (R3's declaring-TU include, against the measured exit-0 `4317207840`); and the per-TU reachability trim is **mandatory** for §7's zero-warning bar rather than an optimisation (11+13 unused-function warnings without it). What it does **not** settle, recorded so no silence reads as a ruling: the four instruments are owed **with the landing** — per-TU double-emit determinism, the warm-cache staleness golden, the prototype-agreement mechanism, the descriptor-reference pin — and every prediction stays scored rather than assumed, the sharpest being that a key on emitted C would link the old library at exit 0 after a `link` edit | ratifying on the day a step is already building on the provisional default is the cheapest an overturn will ever be, and two resolutions had turned into commits before the question was asked | §1.11, §3.1, §4.19, CLAUDE.md §4, §7 | 093 ratified; 029 R5b, 030 R1/R2, 033, 091
2026-08-25 | **`M-package-layout` is a milestone: `use` paths, the qualifier, and where a program's files live** (author decision, `/decide` answer `2a`; `docs/ROADMAP.md` order row 27, § The names, and the § What is next entry). The decision was taken **mid-M-separate-compilation and because of an expectation that did not match**: the author expected that milestone to deliver organising `.hero` files into directories, and it delivers the build architecture — one `.c` per module, the per-module cache. Under CLAUDE.md §14 that makes directories a **third deliverable** rather than an area annexed by a milestone that already exists, so it takes a name of its own instead of riding M-package-manager, where it had been scheduled since 2026-08-12. It sits **before** the manager: `heroes add` cannot place files it has no spelling for. Today nesting is refused by name (`selfhost/parse_use.hero:47`, `error[module_path_has_no_parts]`) across **158 flat `.hero` modules** in `selfhost/` alone, measured today. The sitting it owes is convened **when the milestone opens**, not now — surface syntax, CLAUDE.md §4 — and it **REOPENS a ruling rather than filling a silence**, which the decision's own brief had missed: it cited `docs/panel/031 § H11` and not **`docs/panel/032`, ratified 2026-08-12**, five seats, three vetoes on `C`, `A` struck, `S` (stay flat) adopted. So the sitting inherits what 032 fixed instead of re-deriving it: **R4 already decides the landing form** at +26 measured tokens (`use syntax/decl` binds `decl`, last parts unique), so the qualifier's headline is on the record and what stays open is root-relative against file-relative, with the ergonomist's prediction attached; **R5** (the C name component is the whole path — last parts collided 22-way on `mod` over 169 paths, whole paths zero times) and **R6** (`use` is scoped to the program, and must not inherit a global scope by silence when packages arrive) hold either way; and the quoted Nim form that `SCHEDULED.md` had called an additive escape valve was **struck by R2** as dominated, which is a premise that died the day after that item was written and is corrected in it today. **Two of 032's own numbers have moved, measured 2026-08-25**: its rename argument — *D renames ≥40 of 119 files to satisfy last-part uniqueness* — costs **zero** on today's tree, where none of `selfhost/`'s 158 module names collide (9 stems collide across the repo's 451 `.hero` files), and the compiler-engineer's *≤ 45 files at M8b close* (M-selfhost-port — 032's own vocabulary, CLAUDE.md §14) was **143** measured at the `m-selfhost-port` tag and is **158** today, falsified by more than 3× along with the *a flat tree stays small* premise under `S`. Neither fact decides the question; both change which side owes the argument. And panel 031's ergonomist **declined to use a subdirectory at all** because it could not predict the qualifier, which is the measured cost of the silence. **The trade is on the page**: the 2026-08-12 deferral rested on packages supplying the case that decides the qualifier, so ruling first means ruling without a distributed package in hand — and if the sitting cannot, it owes a conservative default with a return condition rather than an invented case | a milestone the author expects and the table does not carry is a misunderstanding that compounds at every close, and the fix is a row, not an argument | CLAUDE.md §14, §4, design.md:637 | 031 § the ergonomist's H11; 056; SCHEDULED.md's 2026-08-11 subdirectories item
2026-08-26 | **M-separate-compilation step 6: one `.c` per module lands, and the four errors it took to get there were one cause said four ways** (panel 093 R1–R6 executed; no new sitting — the architecture is the one the author ratified 2026-08-25). The compiler now **builds itself as 157 translation units**, links, and the binary is correct: it checks its own source at exit 0, runs `examples/calculator` correctly through 5 TUs, and re-emits all **297** blessed emissions byte for byte. **What the first per-module self-build actually did was fail — 20 clang errors — and that is the finding**: every SECTION of a TU was answering "what do I need" for itself, and four answers cannot share one file. Descriptors came from the whole program's aggregates while typedefs came from the module's reach (`use of undeclared identifier 'h_token_Span'`); prototypes came from all 1,318 functions while the types they name came from the module (`h_source_InputFile`); a wanted record's `_hash` called its FIELD's `_hash`, which `check_table.contained` never reaches because it stops at a declared type (`h_diag_Kind_hash`); and a wanted `T?` took its payload's descriptor address where the descriptor set had a narrower reach (`h_clitoolchain_Toolchain_desc`). **One set now feeds all four**, closed the way C reads a declaration rather than the way the arena holds a type — fields, cases' fields, and a FIXED array's element, which `contained` also stops at and which the FFI corpus found (`h_ffiacarraymember_Matrix_hash`, 6 errors, the net's own `ffi-a-c-array-member`). Two consequences worth their own line: a TU prototypes **what it defines and what it calls**, not the program, which is R3's guarantee unchanged (the declaring TU still compiles its own prototype beside its definition) and 1,318 prototypes per TU deleted; and the fused unit is no longer emitted on the per-module path at all, where it was printed and dropped — 785,323 lines per build | five defects and every one of them was two sets where the architecture needs one; the emitted-C sections are not independent and the compiler cannot pretend they are | §3.1, §4.19, §4.20, CLAUDE.md §7 | 093 R1-R6; 030 R2; 033
2026-08-26 | **The place store reaches the compiler's own arena, and the frontend halves: 191 s -> 88 s** (panel 037's shape, landed 2026-08-24 as `push_owned`; author instruction 2026-08-25, *"la forma giusta è il place store"*). **The repair was three lines and no language change.** `ir_place_store` fires on `p @ p.push(v)` where p is a BARE place, and the compiler's hottest push is not one: `ast.push_expr(@a: Ast, node)` writes `a.exprs @ a.exprs.push(node)`, a FIELD place, so every push copied the whole expression arena — measured in the emitted C (`hero_array_push`, the classic shape) and in `sample`, where `hero_array_push` -> `Expr_desc_copy` -> `retain` was the top of the parse. The arena's three entry points now take the ARRAY (`push_expr(@exprs: [Expr], node)`) and their 56 call sites pass `@a.exprs`, which makes the parameter a bare place and the place store fire. **Measured, all in this session**: `heroes check` on the compiler's own source **191 s -> 88 s**; `heroes test selfhost/main.hero` **3m39s -> 2m15s**; the seed's own emission **224 s -> 120 s**. **What was wrong in my own account of it, corrected here because the record must not carry it**: I reported the place store firing "0 times" in the compiler — the grep was for `hero_array_append`, the name in panel 037's PROPOSAL, and the landed primitive is `hero_array_push_owned`. It fires **483** times against 717 classic. The author sent me back to the panels rather than accepting the claim, which is CLAUDE.md §1 working from the other side of the conversation | panel 037's deferral said *soundness without need still waits* and the need arrived as the compiler itself: a repair that landed for the provoking shape and not for the adjacent one is CLAUDE.md §1's own catalogue, and a FIELD place is the adjacent shape | §4.10, CLAUDE.md §1, §12, §13 | 037, 088; docs/measurements/011, 012
2026-08-26 | **The lists are re-cut by what an item IS rather than where it was written, and the rule that should have kept them clean is given an executor at last** (author instruction, three decisions taken in one sitting: the record is `DONE.md`, `LEARN.md` leaves the work, the directory is `docs/work/`). `docs/debrief/` becomes `docs/work/{DECIDE,SCHEDULED,DONE}.md` plus `docs/learn/LEARN.md`. **Measured before anything moved: of 671,146 bytes across the four lists, 481,677 (71.8%) was closed work and 19,493 (2.9%) was live** — `DECIDE.md` held **138 ticked items and ZERO open ones** under eighteen headings still titled `## Open`, and the record had been untouched since 2026-08-18. The move-to-the-record rule was written in CLAUDE.md §3, `DESIGN-LOG:240` and the record's own preamble, and **performed by no skill**: `/decide` step 5 ticked in place. It is now a numbered step in `/decide` and a sentence in `/step`, because a rule stated in prose about a skill is not a rule the skill runs. Three more defects fell out of the same read: `/panel` told a sitting to append its ratification item to the record while `suite_records.hero::verdicts` reads `DECIDE.md` — a panel that obeyed the skill was invisible to the check built to catch it, which is how 069–079 sat eight days; `/where` ran `cat QUEUE.md` under the comment *"open items = everything owed to the author"* on a file with zero open items since 2026-08-12; and `design.md:43` named `/debrief`, a skill split and deleted on 2026-08-12. **One notation now, `- [ ]` / `- [x]`, one header per file and no `##` sections**: nine live FFI findings had been sitting under two panel headings in a bare-bullet notation no count could see, and reading them found **five already closed**, two of those measurably false (`f32` *"does not exist"* is a primitive; a `size_t` sign error is `error[ffi_parameter_type]` with a fix) | a list that keeps its own dead stops being a list of what is owed, and the reason given for the old name — *"every commit subject cites this path"* — was measured false at **24 of 792, 3.0%**, last used 463 commits earlier | CLAUDE.md §3, §4, §14 | — |
2026-08-26 | **The ROADMAP gets one order for the whole file, and six pointers that named a section nobody has are repaired** (author instruction, *"metti in alto la tabella di sintesi e poi sotto tutti gli step in ordine"*; `docs/ROADMAP.md`, plus `CLAUDE.md` §14, `.claude/skills/step/SKILL.md`, `.claude/skills/where/SKILL.md`, `README.md`, `docs/journal/README.md`). The 2026-08-25 pass merged three tables into one and left the **prose** in four sections that did not share an order — a milestone's row in § The chain, the open one's story between the summary table and that chain (**154 lines of the past before the first line of the future**, the exact shape the journal split exists to prevent), the scheduled ones under *what is next*, and four closed ones under *what the closed milestones settled*. Now every milestone that still has something to say has **one** section and the sections run in the chain's order; the two verification blocks, which sat 700 lines apart, are one section under the summary table, because two copies of the same duty at opposite ends of a file is how one of them rots. **Nothing was dropped, and that is a measurement rather than a promise**: every 8-word window of the old file was searched for in the new one — **36 missing of 8,201 words**, every one a seam between two blocks that used to be adjacent or a line deliberately re-measured — and all 24 command lines and all 40 `` | `M- `` rows survive byte-identical (`records` 7/7, harness **1168/1168**). **Three things the reorganisation found by measuring what it moved**: § Where we are said *"step 6 lands"* while the body three hundred lines below said it had landed; *"13 assigned in `SCHEDULED.md`"* measured **10** and *"the decision queue is at zero"* measured **6**, all six opened the same day; and *"92 panels, every one ratified"* is **checkable by no instrument in this repository** — panel 093 records ratification as `**Status:** RATIFIED`, 091 as a body line under a header still reading `provisional`, and 7 of 92 files never use the word at all — so the claim is dropped rather than re-asserted (CLAUDE.md §1: a fact goes in only if it was measured in the session that writes it). **The pointers were the same failure one level up**: `§ Status` and `§ The order` have not existed since 2026-08-25 and six live sites still named them — including `/where`'s `head -40`, a line count that had stopped reaching the very table its own comment named. Both of that skill's ROADMAP commands are pattern ranges now, which cannot rot that way, and CLAUDE.md §14 names § The chain and says why no other heading may repeat the order.
2026-08-26 | **The per-module build cache stops serving an object built against a header that has since changed** (M-separate-compilation step 7; acceptance row 2's second half, panel 093 R4; `selfhost/cli_deps.hero` new, `selfhost/cli_units.hero`, `selfhost/cli_toolchain.hero`, `selfhost/cli_verbs.hero`, `tests/harness/suite_cache.hero` new). **The defect was measured before the repair and it was a wrong answer at exit 0**: a two-module program whose `extern "conf.h"` group declares `constant CONF_LIMIT` printed `1` with the header on disk saying `2`, and then `1` again with it saying `7`. The cause is structural rather than an oversight — the accessor emits `return CONF_LIMIT;` whatever the header holds, so the emitted C is byte-identical across the edit and a key that contains the whole emitted C is blind to it **by construction**. §12's robustness rule puts this ahead of everything else in the file: a build system's worst outcome is a wrong answer that looks like a right one. **The mechanism is clang's own dependency listing** (`-MD -MT dep -MF`), riding the compile that is already happening: every TU records what it actually opened, transitively, with a content digest per file, and a warm object is served only when every one of them still hashes the same. Three alternatives were weighed and the numbers decided it — hashing the header names an `extern` writes down misses the file one level below and means re-implementing the include search; preprocessing first so the contents can enter the key literally costs a second clang pass per TU on 157 units; what landed gives R4's guarantee (a stale object is never reused) and gives up only directory retention, which costs a recompile and never an answer. **No sitting was convened**: this executes an acceptance row panel 093 already ruled, with no surface, no diagnostic class and no spec token — the choice of mechanism is recorded here so it can be overruled rather than discovered. **Three things fell out of the work and each is a measurement.** `runtime_text` was being asked once per translation unit — 157 shell `cat` invocations and 25 MB of hashing per build of the compiler (162 KB × 157, at this compiler's measured ~1.2 MB/s) for an answer that cannot change during a build; it is now a `Toolchain` field computed once at `locate_toolchain`. The record deliberately does **not** carry what lives under the key's own directory, asked of the path rather than assumed of its position: the first entry of every listing is the generated `.c`, ~90 KB per module, which would be 14 MB of hashing per build to re-answer a question the directory name already settled. And a memo threaded through the build's loop means the 39 headers a trivial program reaches (797 KB) are read and hashed once instead of 157 times — 125 MB and about 104 s, which is the difference between this repair and an unusable one. | a cache key that cannot see the input it was built from is not a key; measured both directions, and the instrument that finds it again is `suite_cache` | §3.1, §4.19 | 093 R4/R5
2026-08-26 | **A comment in `cli_units.hero` claimed the fused path had the same blindness and it was FALSE** (found and corrected in the same step, by running the witness both ways). The sentence read *"the fused path has the same gap today, so nothing regresses here"* — an inference presented as a measurement, CLAUDE.md §1's first named shape. The fused path never caches its unit object (`cli_toolchain.link` recompiles the `.c` on every call), so it caught the header edit and the per-module path was the only one that missed it. The corrected sentence stays in the file with the falsification beside it rather than being quietly deleted, because the reason it was wrong is the reusable part. | a claim about a second code path is a claim, and this one cost nothing to run and would have cost a wrong architecture to believe | §3.1 | —
2026-08-26 | **The same repair is extended to the runtime's object, which is the adjacent shape, and the language decided where the code lives** (M-separate-compilation step 7, second half; `selfhost/cli_digest.hero` new, `selfhost/cli_toolchain.hero`, `selfhost/cli_deps.hero`). This compiler caches **two** objects and only one of them was repaired by the entry above. The runtime's object is keyed on the runtime's own sources and on nothing else, so `runtime.c` including `<stdio.h>` had the identical hole — **and its failure is worse than the one that provoked the work**: every translation unit rebuilds against changed system headers while this object does not, which is a libc ABI mismatch inside a single binary, reached with nobody touching a line of Heroes. CLAUDE.md §1's fourth shape says a repair is attacked at the shapes next to the one that provoked it *before* the commit, and this is that clause spent rather than quoted. **The language then refused the obvious arrangement**: `cli_toolchain` must ask `cli_deps` whether an object is fresh, `cli_deps` must hash a file, and `digest` lived in `cli_toolchain` — a module cycle, which Heroes refuses on the `use` edge whatever it carries (CLAUDE.md §11). So `digest`/`hex8` moved to `selfhost/cli_digest.hero`, which is where they belonged under §11's single-concern rule anyway: hashing bytes is not toolchain business, and nothing in that file knows what clang is. The cycle refusal chose the seam, which is panel 031 R6's *"reversible direction"* paying for itself in the small. **A third thing fell out and it was not the point**: `Toolchain.fingerprint` is the HEROES version (`0.0.1`) and never clang's, so a clang upgrade alone never moved any cache key — and now it does, because clang's builtin headers live under a versioned path (`…/clang/21/include/stdbool.h`) that every dependency listing records, so a major bump makes that path vanish and everything rebuilds. A side effect of asking the right question rather than a design, and written down as such. | two caches, one defect class, and closing it in one place would have been a repair shipped without its adjacent shape | §3.1 | —
2026-08-26 | **What step 7 costs, measured back to back — and one thing it was believed to save that it does not** (M-separate-compilation step 7, third entry; four timings taken within fifteen minutes on one machine and one source tree, the pre-step-7 binary rebuilt from the committed seed as the control). Per-module self-build of the compiler: **cold, from an empty `build/`, 142.29 s before and 145.53 s after — +3.2 s, +2.3%**; **fully warm, nothing edited, 133.08 s before and 132.68 s after — unchanged**. So the freshness check, which is the half that runs on every warm build, costs nothing measurable, and the cold build pays about three seconds for 157 depfile writes plus one pass over the 797 KB of headers a TU reaches. The author's rule is that a change must not slow the compiler down; this is a **measured 2.3% on cold only**, spent to stop a build that returns a wrong answer at exit 0, which §12 puts ahead of speed by name. **And the correction: taking `runtime_text` out of the per-TU loop does NOT show up in wall clock.** It removes 157 shell `cat` invocations and 25 MB of hashing per build — that count is real and re-measured — and the two warm numbers are the same to within 0.4 s, so the work removed was not where the seconds were. It stays because it is less work for the same answer, not because it bought time. **A third measurement, recorded because a decision rests on it**: `SCHEDULED.md`'s open item on per-module slowness carried **cold 261 s / warm 250 s** from 2026-08-25, and those are **not reproducible on today's tree** — the same command with the pre-step-7 binary gives 142 s / 133 s. The old pair is left in the record with its date (§14) and the item now carries both, because the author's decision on that item was about to be taken on numbers 1.9× too large. No cause is asserted for the difference: none was measured. | a change is not fast because the work it removed was large, and yesterday's number is not today's; both halves of that cost a measurement each and both were run | §3.1, §12, §13 | —

2026-08-26 | **The four instruments panel 093 owed with the landing arrive, and each one was made to fail before it was believed** (M-separate-compilation step 8; `tests/harness/suite_units.hero` new, `tests/harness/suite_cache.hero`, `tests/harness/main.hero`). The sitting's resolution ends with a line naming what the architecture owes in tests — per-TU double-emit determinism, a warm-cache staleness golden, the prototype-agreement mechanism, a pin that runtime descriptors are referenced and never copied — and steps 6 and 7 landed the mechanisms while that line stayed open. **What each one catches is a failure with no diagnostic and no crash.** A wobbling TU is worse on this path than on the fused one, because the emitted text IS the cache key: two spellings of one module are two directories and a cache that can never hit. A caller compiling a prototype the definition does not have links, runs and returns garbage at exit 0 (the 093 pragmatist's measured shape). A copied runtime descriptor gives one type two identities inside one binary, and identity is what `==` and `hash` walk through. **The tag-reorder witness was measured to have teeth rather than assumed to**: three orderings of a three-case variant put `h_hue_Colour_tag_blue` at **2, then 0, then 1** in the CALLER's own emitted C, each under a different cache key — so a key blind to the tag enum hands the second build an object switching on 2 while the module now says 0, and the program prints `red` at exit 0. **And the determinism case carries a guard against itself.** A warm build writes no `.c` at all, so the check has to delete the cached objects before each of its two emissions; if that deletion ever stopped working the suite would compare a file with itself and report three green cases having asked nothing. The forget is now asserted between the two builds rather than assumed, and falsifying it turns all three red. **Every check was run in its failing direction before the commit** (§9's rule that a verifier check owes a test that makes it fire): the forget disabled → 3 of 3 red; the descriptor prefix pointed at a name a TU really does define → the exact line, quoted; the crossing function renamed to one that does not exist → all three of its findings printed. The divergence the prototype check exists for cannot be produced by any live tree — the emitter writes both lines from one walk — so it is a test block over two hand-written lines, which is the only way to see the case at all. Net **1171 → 1175**, all green | a mechanism that has never been seen to fail is a mechanism nobody has tested, and four of them had been carrying an architecture for two steps | §3.1, CLAUDE.md §7, §9 | 093 R1/R2/R3/R6, § Instruments owed

2026-08-26 | **The frontend was one function, and `heroes check` on the compiler's own source goes from 88 s to about 8** (M-separate-compilation step 9, author instruction *"rendi il più ottimizzato possibile il frontend"*; `selfhost/text_lines.hero` new, `selfhost/resolve_binding.hero` new, `selfhost/cursor.hero`, `selfhost/resolve_state.hero`, `selfhost/state.hero`, `selfhost/parse_use.hero`, `selfhost/resolve_{walk,decls,qualified}.hero`). The author's answer to `SCHEDULED.md`'s open item was *make the frontend incremental*; **the profile said the frontend was not slow, it was quadratic, and no architecture change was needed to find that out.** Measured with `sample` over `heroes check selfhost/main.hero`: **21,119 of 21,254 samples — 99.4% — inside `cursor.line_col` and the three runtime calls it makes.** `line_col` answers with a line NUMBER, so it walks from byte 0; `take_docs` asked it once per declaration and once per comment it examined; the parse runs over the whole concatenated Source (§1.11 appends the library), so each call read up to a million bytes of this compiler's own 1.2 MB source. **The rule the repair records: ask for the DISTANCE you need, not for the position you could derive it from.** `take_docs` never wanted a line number — it wanted *is this comment on the line immediately above* (count line ends between two ADJACENT offsets) and *does it start in the same column* (scan back to the previous line end). Both are local; both read tens of bytes. **88.0 s -> 27.9 s.** Then the profile named the next one and it was **the same shape as the day before**: `new_resolver` wrote `out.uses @ out.uses.push(.unresolved)` once per expression in the program, and `out.uses` is a FIELD place, so the in-place store never fired and every push copied the whole arena — CLAUDE.md §1's fourth clause (a repair is attacked at the shapes NEXT to the one that provoked it) collected in arrears, one department over from `ast.push_expr`. Built in a bare local and moved in once: **27.9 -> 11.2 s.** Then the same shape three more times, found by a systematic grep for `x.y @ x.y.push(` rather than by waiting for the profile: `state.emit` appended every TOKEN through a field place (quadratic in the token count), and `resolve_state.declare` did it twice per binding. Through an array parameter, which is what makes it a bare place: **11.2 -> 5.2 s.** **What this cost in files, and it is a seam rather than a squeeze**: the layout check fired on both grown files, so `line_col` and its three cheap siblings became `text_lines.hero` (it imports nothing, which is the tell that the concern is its own) and `Binding` plus its five pure constructors became `resolve_binding.hero`. **One inference was written and then falsified before it reached this record**: that the split would cost speed because clang can no longer inline across a TU boundary. Measured both ways on one source — fused **9.90 s**, per-module **10.29 s** — and the difference is inside this machine's noise. **The noise is reported rather than hidden**: repeated runs on this machine vary from 5.2 s to 12.6 s for the SAME binary and input, with the first run of a block fastest, so every number above is a minimum over its repetitions and the step-to-step deltas are singles. The end-to-end claim survives it by an order of magnitude: **88.0 s (min of 3) -> 7.9 s (min of 9)**. Fixpoint verified: the compiler emits **793,526 lines** of C, rebuilt from that C it emits the same file byte for byte. `heroes test selfhost/main.hero`: **512** tests, all passed | the item asked which architecture to change and the answer was that no architecture was wrong: four instances of one language-level shape were, and the profile named every one of them | §4.10, CLAUDE.md §1, §11, §13 | 093; 037, 088; docs/measurements/011, 012

2026-08-26 | **Acceptance row 4 lands in the shape the sitting re-read it into, and annotating it found 19 decorations** (M-separate-compilation step 10; `tests/golden/surface-fixtures/externsignature/` new — four files and a README, `tests/harness/suite_cache.hero`, `tests/harness/suite_surface.hero`). The row was tabled as *"one two-module FFI-shaped golden with a wrong `extern` signature: exit 1 in BOTH TUs"* and panel 093 R5 measured that unsatisfiable — the caller is **correct**, the mistake is one module over, so there is no second failing unit to demand. R5's three claims replace it and all three are now instruments. **Two are a golden pair**: `math.h` declares `double sqrt(double)`, `bind.hero` says `i64` both ways, and the verdict is `error[ffi_return_type]` at **exit 1 on `bind.hero:13:5`** — the author's own line, in the module that declared the `extern`, not whichever TU happened to be compiling; the corrected twin runs and prints `4.0`, so the case cannot pass by refusing every program forever. **The third cannot be a golden at all** and is why it lives in `suite_cache`: it is about the SECOND invocation. Three builds in a row must each exit 1 with the same code and the same file named, with every healthy object warm — because **a build that goes green on its second run is the worst outcome a build system has, since nobody re-reads a green build** — and then a repair must reach exit 0. Falsified before it was believed: with the failing program replaced by the correct one, every branch of the check reported separately (exit code, missing code, wrong file blamed, three attempts each). **What annotating the fixture turned up is a defect in the invariant itself, and it is filed rather than fixed**: `suite_annotations` is §9's one unforgeable check and sweeps exactly two directories, because its mechanism compares a case's `.hero` with its `.expected`. Measured: **15 annotations across 11 files in `tests/golden/fixedbugs/` and 4 across 3 in `surface-fixtures/` sit outside it**, against 181 inside — and `fixedbugs/` has **zero** `.expected` for 16 `.hero`, so the mechanism cannot reach it. The suite's own failure message names this state — *"annotations that stop being checked are the shape §9 calls a decoration"* — while measuring a floor it can only see inside its own two directories. Three ways out, all with a cost, so it is `DECIDE.md`'s and not mine | the row that could not be satisfied as written is satisfied as ruled, and the annotation that documents it turned out to be checked by nothing, in the directory §9 created for defects that already shipped | §3.1, §4.19, CLAUDE.md §9 | 093 R5; 030 R2; 033 R5

2026-08-26 | **§9's unforgeable check stops being unforgeable in name only: 181 pinned diagnostics become 207, and the sweep finds three unannotated cases plus a defect in its own parser** (author decision `/decide` answer `5a`; `tests/harness/suite_annotations.hero`, `tests/harness/main.hero`, three `tests/golden/fixedbugs/` cases annotated). The check compares a case's `.hero` with its `.expected`, so it could only ever reach cases that HAVE one — and **19 annotations lived outside it**, 15 across 11 files in `fixedbugs/`, which has **zero** `.expected` for 16 `.hero`. The second half RUNS the compiler and reads what it prints, and three things it had to get right were each measured on the real tree rather than assumed: **which command provokes** (check for a type error, build for anything FFI, because the probe only reaches clang on a build — check-then-build resolves all fourteen), **which file to run** (a case here is a DIRECTORY: some of these are modules with no `main` whose diagnostic a neighbour provokes), and **that the comparison carries the FILE** (`externsignature/` forced it — the annotation is in `bind.hero` and the `main` is in `wrong.hero`, which is the general shape rather than an oddity). **It found two real things on its first run.** Three `fixedbugs/` cases — `ffi-missing-link`, `ffi-pointer-return`, `ffi-return-type` — produced a diagnostic and carried no mark at all, in the directory §9 created for defects that already shipped once; they are annotated now. And **the parser could not tell a mark from a sentence quoting one**, which is the second time this project has paid for that exact distinction: step 1 of this milestone found the `ORDER:` inventory was a grep returning *10 hits in 8 files, four of them prose*, and `claimed_by` had the same hole — `externsignature/wrong.hero` explains in a comment where its annotation lives, and every word after the two characters was read as a code (`annotation`, `belongs`, `the`, `own`). **The fix is about position, not vocabulary**: a row whose first non-blank character is `#` is prose unless the mark is the very next thing. Filtering on what a code looks like would not have worked, because `annotation` and `belongs` are both perfectly good snake_case. The floor moves 181 -> **207**, and the number was read out of the suite rather than added up: the floor was set impossibly high so the failure message would print the true count, which is this file's own rule applied to itself | an invariant that covers part of its subject is a convention wearing an invariant's name, and the part it missed was the directory that exists for defects that already got through once | CLAUDE.md §9, §11 | —

2026-08-26 | **The `size_t` mapping row bought less hesitation, not different code — the experiment its +12 tokens registered was finally run, in two arms, and the prediction is FALSE** (author decision `/decide` answer `9a`; panel 092's llm-ergonomist prediction, `docs/measurements/010-spec-budget-ledger.md` row 3560). The prediction: *"with the mapping named, generated callers convert at a size argument (`to_u64`/`to_u32`) in a majority of samples, **against near zero today**"*. Two llm-ergonomist seats, each given ONE document and nothing else — the live spec for arm B, and a copy with `u64` where it says `size_t` deleted for arm A — and each asked for the same ten programs binding the same ten C functions. **Measured by counting the programs, not by reading the seats' summaries**: arm A converts in **10 of 10** (`to_u64(len(s)).must()`) and arm B in **9 of 10** (`len(s).to_u64().must()`); both declare the parameter `u64` (17 and 18 occurrences). The second half of the prediction is simply not true: without the row the callers convert anyway, and the row changed the SPELLING (call form against UFCS) rather than the behaviour. **What the row measurably did buy is stated instead of the claim that failed**: declared `UNDECIDABLE` points drop from **9 to 5**, and arm A names the cost in its own words — without the row a wrong width *"would be blamed on the compiler (exit 2), not on me"*, which is a misattribution the row prevents. Whether that is worth 12 tokens is a spec question and goes to the sitting, not here. **Two findings the experiment was not asked for and which are worth more than it was.** The seat's own prediction — *at least 5 of the 10 will not compile* — is **false, measured: 8 of 10 build**, and one of the two that fail is `qsort`, whose comparator *"cannot be written: no dereference, and `<` is a number only"*. And the silent-error surface it names is one thing repeated five times: **a program cannot ask what a type's size is**, so every byte count is a hand-typed literal (`n * 8`, `size: 8`, `(count + 1) * 4`) — write 4 where 8 belongs and it compiles and overruns the heap, which is §1.12's own subject | a registered prediction is only worth the tokens it bought if somebody comes back for it, and this one had been carried as evidence for ten days without being run | §1.2, §1.6, §4.19 | 092; 011, 046 R2

2026-08-26 | **The word width stops being a literal and becomes a question asked of C, and the runtime ABI moves to 16** (author decision `/decide` answer `4a`; `runtime/hero_os.h`, `runtime/parts/os.c`, `runtime/heroes_runtime.h`, `selfhost/cli_io.hero`, `selfhost/cli_verbs.hero`, `selfhost/emit_decls.hero`, 148 blessed emissions re-blessed, the seed regenerated). `emit_c_spellings.hero` has said in prose since the port that *"a width is not a constant to be tabled; it is a question about the target"* — and then **every live call site passed the literal `64`**, which is the one option that same comment names as forbidden. The bootstrap derived it from `size_of::<c_ulong>()`; Heroes has no such thing, and an `extern constant` cannot carry it either, because declaring `ULONG_MAX` at one width fails the width assertion on the other platform. So `hero_word_bits()` is one line of C — `sizeof(long) * CHAR_BIT`, `sizeof` rather than a preprocessor guess about the platform, because the question is what THIS compiler does on THIS target. **The wrong answer was never loud**: nothing crashes, the author is simply told `i64` where a Windows header means `i32`, on the one platform this project has a CI leg for and no machine. **The derivation was proved live rather than asserted**: with `hero_word_bits` forced to return 32 and the compiler rebuilt, `ffi-word-width.hero`'s diagnostic reads *"32 bits on this machine"*; restored, it reads 64, and C on this machine says 64. **What the bump costs, measured**: 148 emissions re-blessed, every one a single line `== 15` to `== 16` and nothing else (the diff was read and deduplicated before it was believed, §9), the seed regenerated at **793,541** lines, and the fixpoint verified byte for byte from `clang -I runtime seed/heroes.c`. **And one procedure worth writing down, because it will be needed again**: an ABI bump cannot be built in one pass. The stamp exists so that a `runtime/` from another milestone is a compile error, and that is exactly what the current compiler hits — it emits `== 15` against a header that now says 16. The way through is two steps: put the number back, build the new source with the old compiler to get one that emits the new number, then raise the number and build again with that. The stamp doing its job to the compiler that is changing it is the stamp working, not a defect | a rule stated in a comment and contradicted by every call site under it is not a rule, and this one had been that way since the port | §4.19, CLAUDE.md §7, §11 | 071; 030 R3

2026-08-26 | **Panel 094 refuses both halves of the variadic proposal, and refuses the second one for a different reason than it was proposed** (five judges; `docs/panel/094-c-variadics-and-the-name-apart.md`; provisional, author ratification pending). The author answered `1c` (grow the language until `...` can be written) and `2a` (a way to spell the C name apart from the Heroes name). **`...` drew four vetoes and one objection on three independent grounds.** The dispositive one is the ffi-pragmatist's, compiled against the real header: `curl_easy_setopt` is a **function-like macro nailed at three arguments**, so a `...` binding can name only the fixed prefix and the generated unit's return assertion is `error: too few arguments provided to function-like macro invocation` — **§4.19's own acceptance rung 4 becomes unbindable**. The seat records, against its own interest, that its standing panel-052 veto does **not** trip here: a probe over the fixed prefix compiles and is not a re-declaration, and writing that as an impossibility would have been CLAUDE.md §1's second named shape. **The historian reframed the whole question and it is the finding worth keeping**: every project that broke on variadics — Terra #508, CPython #92892, cranelift #1451, frida-gum #439, Ada's `Convention => C` — **synthesised the call itself**, while Heroes routes through the header, which is precisely the design GHC added `CApiFFI` to reach after `ccall` proved unsound and what OCaml's ctypes tells users to switch to. **Heroes already is `capi`**, so `...` buys ergonomics rather than correctness and imports the one hole `capi` does not have. **THREE CORRECTIONS TO THE CONVENER'S BRIEF, TWO OF THEM CLAUDE.md §1'S NAMED SHAPES, AND THE BRIEF WAS MINE.** *"One C symbol carries one arity"* is **false** — two modules bind one symbol at two arities today, built independently by the spec-warden on `printf`, by the ffi-pragmatist on `curl_easy_setopt` with real `curl/curl.h`, and reproduced a third time by the coordinator: a failed search for a **syntax** written as an impossibility about a **capability**, which is the whole stated case for half 2. And *"one `_Static_assert` separates a variadic bound at fixed arity from an ordinary binding, so the refusal costs one line"* is **false on real bindings** — 4 false fires of 5 measured by the engineer, 3 of 3 re-measured by the coordinator, because §4.19 *mandates* that the Heroes spelling differ from the header's and a whole-type comparison cannot isolate variadicity from that difference; the original measurement had used the header's exact signatures, which no real binding writes. **Two live defects fell out and are queued regardless of the verdict**: `printf(format: cstr, value: f32)` is a **correct** binding refused at exit 1 with a message that says the header declares `double` when it declares `...` (the C equivalent prints `0.500000`), and `printf(format: cstr)` at arity 1 is **exit 2, the compiler blaming itself**, from `-Wformat-security` firing on the probe — which CLAUDE.md §7 says must be exit 1 on the author's line | a sitting that overturns its own convener's premises twice is the instrument working; the cost of the brief being wrong was five judges' time, and the cost of not convening would have been a language form nobody needed | §4.19, §1.6, §1.7, CLAUDE.md §1, §4 | 094; 051, 052

2026-08-26 | **Panel 094 ratified: neither half lands, the two-module route becomes a running program, and the one repair worth taking is measured and queued rather than slipped in** (author instruction *"fai la cosa migliore e piu robusta"*; `docs/panel/094`, `tests/golden/surface-fixtures/twoarity/` new, `tests/harness/suite_surface.hero`). **The robust reading is the refusal, and it is §1.12's own argument**: `...` removes the declared width that makes a wrong argument a compile error, in the one call form where C punishes hardest, so it moves the wrong way on the axis the contract ranks above ergonomics. The rename saves three lines against **+23 permanent spec tokens** and a second notation for an idea the document already spells `tag`. **R4 executed the same day**: `twoarity/` is a running program with a `suite_surface` row and a README naming the claim it refutes, because that claim was rediscovered three times in one sitting after a brief asserted it impossible — the fixture exists so the fourth person reads a program instead of re-deriving it. **AND THE CHEAP REPAIR WAS ATTEMPTED, MEASURED AND TAKEN BACK, which is the part worth recording.** Silencing `-Wformat-security` inside the probe block looked obviously right — the probe prints nothing and is never called. Built: it moves the error off the probe and onto **the author's own call site**, where the warning is **correct and must stay**, because a format string the author does not control is `%n` writing into memory. So the defect is not the refusal, it is the attribution, and the fix belongs in CLAUDE.md §7's exit-1 family — a diagnostic class, which is a panel path. The half-repair would have cost **297 re-blessed emissions** and closed nothing. **What the sitting did point at, now queued with its numbers**: `printf(format: "%d\n".cstr(), value: 4294967303)` declared `i64` prints **`7`** at exit 0, and `-Wformat` cannot see it because the emitter passes the format as `hero_cstr_nonnull(t2)` and never as a C literal. `-Wformat-nonliteral` was tried and is **not** the lever — it fires on every Heroes call to a format function, correct or not, because the format is never a literal by construction. What does work, compiled: the same call with a literal gives `error: format specifies type 'int' but the argument has type 'int64_t'` under the project's own `-Werror=format`, and names the fix. Blast radius measured at **19 call sites and 18 blessed emissions**; it changes emitted C and can turn building programs into compile errors, which is the point and also why it is a soundness-lane sitting rather than something to slip in | the most robust move was to refuse the language growth, execute the one resolution that costs nothing, and price the one repair that would move a silent wrong answer into the loud column — then leave it for the lane that judges emitted C | §1.12, §4.19, CLAUDE.md §4, §7 | 094

2026-08-26 | **`heroes fmt` now reads back what it wrote, because it deleted seven arms out of a compiler source file at exit 0** (author instruction *"possiamo fare adesso il mega fmt"*; found by the sweep itself, which was reverted; `docs/defects/003`). Two repairs in one commit. `print_fmt.hero`'s `valued()` grows a `.binary` arm: `binary_control_tail` finds a control form at a binary chain's right edge and `binary_prefix` renders everything left of it as the **head** it is printed under — the path that already knew how to put a block under a head. And `cli_syntax_cmds.hero` grows `refuse_output_the_formatter_broke`: the formatted text is re-parsed and re-formatted, and anything that does not parse or is not a fixpoint is **exit 2 with the author's file untouched** (§7 — the compiler's error class, never the author's). Measured: 9 probe shapes, 6 of them destructive before and 8 of 9 correct after; the 9th (a control form as the LEFT operand, legal Heroes per `heroes check`) now refuses loudly and has the test that fires. 514 selfhost tests, 1184 harness checks, fixpoint byte-for-byte in 80 s | **`render()`'s comment stated an obligation and nothing checked it was met**: *"Headers only: valued() prints the blocks under them"* was true when the control form WAS the value and false when the value CONTAINED it, so `out @ out + match c` printed the header and dropped the body. This is CLAUDE.md §11's premise rule holding between two functions instead of inside one, and it is the **third** instance of the class — panel 014 (arm body), panels 060/061 (`record` hoisted, `partial` deleted), this — with all three found by a person and none by an instrument. The guard that was supposed to find them, `assert_canonical`, **exists only as a sentence in `print_dump.hero:12`**; the port never brought it over, and the output guard is deliberately the weaker property (parses, not means-the-same) with that written into its own comment so nobody mistakes one for the other. Queued in `docs/work/DECIDE.md` with the ratchet raise it forced (`print_fmt.hero` 843 -> 888, no split available: the cut runs `print_bodies -> print_fmt -> print_bodies` and the language refuses module cycles) | design.md §4.15 · CLAUDE.md §7, §9, §11 | 014, 060, 061

2026-08-26 | **Panel 095: the author's blank-line rules are admissible and cannot be applied, because `heroes fmt` is not a fixpoint on 31 of its own 165 modules** (author instruction *"voglio più aria"*; `docs/panel/095`; provisional, ratification pending). Four seats approve with conditions, the historian objects on precedent and holds no veto. The resolution is an **order of work rather than a refusal**: stage 1, `fmt` exits 0 on all 165 `selfhost/*.hero` in its own commit; stage 2, `assert_canonical` exists; stage 3, rules 1, 2 (narrowed to a dedent closing a `for`/`while`/`match`, never an `if`), 3 and 5 land with rule 4 scoped to statement bodies and excluded from `record`, `variant` and `extern` bodies; stage 4, the sweep, once. Measured: implementation **+79 lines in one module** and **0 elsewhere**; spec **3592 → 3592** and source tokens **delta 0**, because `\n\n␣␣␣␣` and `\n␣␣␣␣` are one pretokeniser piece; emitted C **byte-identical** across three layouts with `#line` stripped; ratchet **+31.7% code lines** and **44 of 165 files red**; reader backtracks **4 → 1** on the sample, with a **confident error** at the one dedent whose wrong answer compiles | **The sitting's two findings outrank its own question, which is the second time that has happened after panel 061.** (1) `heroes fmt` refuses **31 of 165** selfhost modules as non-fixpoints — measured by the compiler-engineer and **independently re-measured by the coordinator at the same number** — visible only because of the same day's output guard (`docs/defects/003`), which made a four-month-old defect loud rather than causing it. Cause: `arm_alignment` keys run membership on **source adjacency** (`print_fmt.hero:655`) while pass 1's own 88-column break moves those lines, which is defect D4's premise in a second symptom, and `spans_lines:718-727` already carries the lesson. **The morning's reverted sweep wrote non-fixpoint output into 118 files silently.** And the result that surprised the sitting: the rules-1-4 prototype refuses **7**, a strict subset — the proposal **removes 24 non-fixpoints and adds none**. (2) `heroes fmt` **deletes the blank line under a record-field remark and `--dump-ast` shows a doc comment appear** — a remark becomes documentation, which §4.1 makes a difference in meaning and the compiler reuses in error text. Reproduced by the coordinator. **Fourth instance of the parses-but-means-something-else class** (panels 014, 060/061, defect 003), and the one `cli_syntax_cmds.hero:71-75` states in its own comment that the new guard cannot catch — so rule **4b is rejected for data bodies**, because it would make an ordinary remark inside a `record` unwritable. The precedent the historian sourced is the strongest argument against and it may not apply: Black shipped rules 1-3's family in 2018 and **removed it 24 days after the first complaint**, gofmt refused the equivalent the day it was filed, rule 1 has **zero precedent** in any canonical formatter's default — and every one of those decisions is about a **human author's intent**, while §1.1's thesis is about model error rates | design.md §4.15, §4.1, §1.2, §1.6, §1.11 · CLAUDE.md §11 | 095

2026-08-26 | **`assert_canonical` exists in the selfhost compiler for the first time, and its first catch was a live defect the other two guards pass** (panel 095 condition 2; stage 2 of that sitting's order of work). `heroes fmt` now runs THREE checks on its own output, and they are three different properties: it must PARSE, it must be a FIXPOINT in text, and it must hold the same TREE. The third is `dump(text) == dump(fmt(text))`, which the Rust bootstrap had, the port dropped, and `selfhost/print_dump.hero`'s module doc described as live for a week while it did not exist. **The obstacle that made it look impossible, and how it was got round**: the input reaches `run_fmt` parsed with the whole module graph plus the appended library while the output is one file, so the two dumps are not comparable — the guard therefore re-parses the ROOT FILE'S OWN TEXT standalone (`source_extent.user_text`) and compares that against the output parsed standalone, one file against one file, both named `path` so `dump_ast`'s first line agrees. Two things make the dump invariant under legitimate formatting and both were measured rather than assumed: `print_bodies.hero:130` runs an integer literal through `canonical_int`, so `fmt`'s one literal rewrite (`0xFF` -> `0xff`) dumps identically, and the dump carries names, structure and doc comments but never spans or columns. Verified: **0 of 165 `selfhost` modules refused with all three checks on**, 515 selfhost tests, 1185 harness checks, seed fixpoint byte-for-byte | **The first thing it caught is the argument for it.** A remark inside a `record`, with the blank line below it that design.md §4.1 makes the difference between a remark and that field's documentation, is silently promoted to documentation — and that output **parses** and **is a fixpoint**, so the two guards added earlier the same day both pass it. `--dump-ast` shows the `doc` appear. It is the fourth instance of the parses-but-means-something-else class (panels 014, 060/061, docs/defects/003) and the first one an instrument caught rather than a person. §9's named case is in `cli_syntax_cmds.hero` and asserts both halves: that `run_fmt` refuses at exit 2 leaving the file byte-for-byte untouched, AND that the squashed output really does parse and really is a fixpoint — so the case cannot pass for the wrong reason. `print_dump.hero`'s comment now cites the live function and states what its own expiry would mean, because a comment describing a guard that does not exist is worse than no comment (CLAUDE.md §11) | design.md §4.1, §4.15 · CLAUDE.md §9, §11 | 095, 060, 061, 062

2026-08-28 | **The canonical line width is 120, from 88** (author instruction, this session; `selfhost/print_fmt.hero`'s `WIDTH`). No panel: the code's own comment has always said *"not a language rule — one constant"*, `DESIGN-LOG:46` fixed the 88 with `—` in the panel column, and panel 007 ruled on WHERE a line may break — inside brackets only — never on how wide. Measured over all 165 `selfhost` modules, and the isolation is the point: the canonical form at 88 with panel 095's air is **+30.2%** longer than today's source, at 120 it is **+20.0%**, so the width alone gives back **4071 lines (−7.8%)**. Blank lines are **identical at both widths (6946)**, which is the two axes proved independent rather than assumed. **And the two changes very nearly cancel**: the canonical form as it stood this morning — no air, 88 — was 47964 lines; with the air and at 120 it is 48096. **132 lines apart on 48000, 0.3%** | **What the width buys is not only shorter files, it is fewer places for the formatter to be wrong.** Every non-fixpoint in `docs/defects/004` began with a line this constant decided to break: the break made a value span lines, `spans_lines` then answered a question about *"source a person writes"* with source the formatter had written, the arm left its alignment run, and the padding changed. Raising the constant does not repair that — 004 did — but it shrinks the population the mechanism can reach, and the census is 0 of 165 at both widths, so the repair is what is holding rather than the number. Verified at 120: 515 selfhost tests, 1185 harness checks, seed regenerated, fixpoint byte-for-byte, and 0 of 165 modules refused by any of the three output guards | design.md §4.15, §4.9 · CLAUDE.md §11 | 007, 095

2026-08-28 | **The repository is canonical: 308 `.hero` files swept through `heroes fmt --in-place`, and the program is byte-identical** (panel 095 stage 4, author instruction *"sweep"*; `selfhost/` 163 files, `tests/harness/` 26, `examples/` 44, `tests/golden/{run,ir,emit,fixedbugs}` 78, plus 76 re-blessed `tests/emission/` traces and the seed). §4.15's second purpose — *"it makes any textual difference between two versions semantic"* — is true of this repository for the first time: **0 files are non-canonical**, where 263 of 454 were this morning. **THE GATE, and it is the whole reason this could be trusted**: `seed/heroes.c` **is** what `selfhost/` emits, so the "before" C was free. After the sweep the same compiler re-emitted, and `grep -v '^#line'` of the two is **byte-identical** — the reformatting changed nothing the compiler does, and the raw files differ, so the `#line` numbers moved as they must. Verified end to end: 515 selfhost tests, **1185 harness checks, 0 failed**, seed regenerated, fixpoint byte-for-byte. Nothing was lost: across all 308 files, **comments unchanged except where hand-edited, `#~` annotations unchanged (0), and `=>`/`else` occurrences unchanged** — the one file that looked like a gain, `keywords.hero` 42 -> 43, is `grep` counting LINES where a message string containing *"`.name => expr`"* moved onto its own line when its arm broke; occurrences are 43 both sides. `tests/golden/check/` and `unsupported/` were excluded by author decision and are **untouched (0 files)**: those programs are the test's data, 23 of them cannot be parsed by construction, and 40 `.expected` files cite line, column and a caret run whose length is the span | **Three things broke and each was a premise about text rather than about a value** (CLAUDE.md §11's rule, three live instances in one afternoon). (1) `tests/harness/suite_spec.hero`'s extractor read `=> ok(ForeignWord(` on one line; at WIDTH 120 the arm breaks and `ok(` ends the line, so it found **0 refused words out of 23** — exactly what its own failure message had predicted in those words, which is why the floor was there. Repaired by resting on the arm (its value names the type, its pattern is before the nearest `=>` at or above) rather than on the layout; the obvious wider marker `=> ok(` would have matched every `"record" => ok(.kw_record)` and reported real keywords as foreign, a FALSE POSITIVE worse than the red. (2) The three `tests/golden/emit/*.expected` and 76 `tests/emission/*.c` differ **only** in `#line` — 38, 72 and 46 changed lines and **zero** with the directives stripped, and 0 of 76 traces has a non-`#line` difference, which is §9's *read and quote the diff* discharged as a measurement instead of a promise. (3) The ratchet: **36 files past their ceiling counting blank lines, 10 without**. `code_lines` stopped counting them, because §11's number governs *what a reader must hold in their head* and a blank line is the one line nobody holds — it is what makes the others holdable. Counting them was harmless while the formatter only preserved the author's blanks and became perverse the day panel 095 had it insert them. The table was re-baselined ONCE, and it moves in both directions, which is what says the metric change is not a way to hide growth: same size (16), five files smaller, two leaving. **What the sweep cost, isolated**: +20.0% lines over `selfhost/`, of which the air rules are +8.8% and every one of their 4203 added lines IS a blank; the rest is the WIDTH break and arm alignment, canonical since 2026-08-04 and owed the day the repo was formatted at all | design.md §4.15, §4.9, §4.1 · CLAUDE.md §9, §11 | 095, 007

2026-08-28 | **`heroes fmt` reads the file it was named and nothing else, and the repository now has a check that keeps itself canonical** (author instruction *"sistemare FMT"*; `selfhost/main.hero`, `selfhost/cli_input.hero::read_root_only`, `tests/harness/suite_canonical.hero`). `fmt` came off `cli_input`'s one-read list, which its own doc says "keeps lex, parse, check, build, run, test and fmt looking at the same text" — it is the one command of the seven whose answer cannot depend on another file, because `print_fmt.format_file` keeps root-file declarations and root-file comments and discards everything else. **Measured: formatting all 165 `selfhost` modules went 188 s → 8 s, the whole repository 194 s → 12 s, and `main.hero` alone 4 s → 0.** The new suite walks 371 files in seven directories, compares `heroes fmt <file>` against the file, and reports exit 2 separately because that is the formatter accusing itself (§7) rather than the file being crooked. No new flag: §10's stopping rule admits a capability only when something must type it, and the harness already runs the binary and reads files, so a `--check` would be surface bought for nothing | **A number I had measured that morning was stale by the afternoon, and it changed the recommendation.** `fmt` on `main.hero` was 156 s when panel 095 was briefed and is 4 s now, because M-separate-compilation closed in between and took the frontend from 88 s to about 8 — so the honest problem was never an hour, it was three minutes. The repair still stood but landed somewhere else than the brief said: `lex main.hero` and `parse main.hero` both cost 4 s, so the price was never the parse, it was upstream in reading and lexing 48000 lines to print one file. **A measurement is a fact about a moment**, and CLAUDE.md §1's rule about re-measuring in the session that writes it does not save you when the world moves between two sessions of the same day. Two more things this found: the first draft of the suite used `list_entries` and walked **325 files of 371**, caught by its own floor — `examples/` keeps its programs one directory down — which is why a floor is not decoration; and the ROADMAP's summary table had **five wrong numbers**, two rotted before today (ABI 15 against 16, panels 92 against 94) and three moved by the sweep, of which the compiler's line count needed its reason written beside it: **39,739 → 48,164 is a layout change and not 8,425 new lines of code** | design.md §4.15 · CLAUDE.md §7, §9, §10 | 095

2026-08-28 | **Panel 095 RATIFIED, and six of the nine open decisions closed in one sitting of `/decide`** (answers `1a 2a 3a 4a 5a 6a 7c 8a 9a`; `docs/panel/095` § Author's verdict, `docs/defects/005`). What landed: the air rules and their order of work ratified as ADOPTED rather than as tabled; `print_fmt.hero`'s ceiling rounded to 1150 and the split question retired (`7c`, against the coordinator's recommendation); `field_line` and `case_line` preserve §4.1's discriminator so a remark inside a `record` or a `variant` stops being promoted to documentation; `grammar_expr.hero` refuses a continuation at bracket depth zero; and `main.hero` reads `args_checked()` so the compiler stops dying on an argument it was only carrying. Three items stay open and all three are panel briefs the author has instructed — one soundness lane carrying three questions, two full panels re-instructed for the second time | **Two of the answers were WRONG WHEN OFFERED and verifying changed them, which is what the skill's step 2 is for.** Answer `3a` was offered as *"(i) a wrong message I repair now"*: measured, `clang -Wall -Wextra -Werror` compiles the equivalent C at **exit 0** printing `0.500000`, and the warning Heroes refuses on comes from **`-Wdouble-promotion`, which is not in `-Wall -Wextra`** — the project opts into it. So it is a REFUSAL blocking a correct program, not a message to reword, and it went to the sitting instead of being half-repaired. Answer `5a` asked which of two artifacts was wrong and the answer was the one nobody had suspected for a week: `--dump-ast` built `return (match … + "x")` and the program **printed `bx` at exit 0**, so `heroes fmt` had been right to refuse — there is no canonical spelling of a form panel 007's Amendment B and `design.md:1794` both forbid, and §12 makes that the compiler's bug. **AND BOTH REPAIRS COST THE OUTPUT GUARD ITS §9 FIRING TESTS**, which is the lesson of the day: each test made a guard fire by handing it a live defect, so repairing the defect stopped the test testing. Both were rewritten as PREDICATE tests over `refuse_output_the_formatter_broke`, in both directions including one that must stay quiet — `0xFF` -> `0xff`, a text change that is not a meaning change. A firing test that depends on a live defect stops firing the day the defect is repaired, which is the day it stops being a test | design.md §4.1, §4.15, §1794 · CLAUDE.md §7, §9, §12 | 095, 007, 094

2026-08-28 | **Panel 096: three defects at a variadic `extern` argument, and the proposal the coordinator wrote would have shipped a SIGBUS** (soundness lane, author instruction `/decide` `2a`+`3a`; `docs/panel/096`; provisional, ratification pending). **Q2 lands REPAIRED rather than removed** — removal is refused by both seats on one measurement, that clang's variadic and fixed messages are **byte-identical**, so dropping `-Wdouble-promotion` would trade a false refusal (`printf(format: cstr, value: f32)`, where the promotion is the C ABI and exact) for a true one (`pow(x: f32, y: f32)`, where the header really says `double`). The resolution takes the ffi-pragmatist's mechanism over the engineer's: `HERO_VARIADIC` separates the **position** with `__builtin_types_compatible_p` against an *unprototyped* function type of the callee's own return type, spelling no parameter and no return type at all — **9 of 9 correct with 0 false fires on the exact five panel 094 R6 measured false** — usable in one compile through `__builtin_choose_expr`, one promotion entry instead of six, and **no ABI bump**, because the probe is `__attribute__((unused)) static` and never emitted. **Q3 goes to the full panel**: closing it mints a 21st `ffi_*` code and §4 puts a diagnostic class outside this lane. **Q1 does not land** | **THE FINDING IS THAT THE BRIEF'S OWN PROPOSAL WAS A MEMORY-SAFETY REGRESSION, and it was the coordinator's brief.** In C a string literal has type `char[N]`, not `const char[N]` (C11 6.4.5p6), so emitting a bare literal — as proposed — lets it reach a `char *` parameter with nothing complaining: reproduced independently, `strtok("a,b", ",")` compiles **clean** under the project's own thirteen flags and **runs to exit 138, SIGBUS**, while `(const char *)"a,b"` is a compile error from the thirteenth flag, the one `cli_flags.hero:21-22` calls *"panel 058's cstr-write refusal at the C level"*. The case is already a fixture in the tree one level of quoting down (`emit_ffi_mutable.hero:111`) and nothing tests it as an emission. **The seats then disagreed on Q1 and the disagreement is the sitting's one open question**: the engineer vetoed the mechanism on `design.md:563-567`, which forbids *"a rule that has to reason about **where a value came from**"* by name in the paragraph about `cstr` at the boundary, and measured the rule defeated by one line — `f = "%d\n"` then `f.cstr()` still prints `7` — with the tree's one real instance out of reach behind `extern_across_modules`. The ffi seat answered that its narrowing is *"in this expression"*, a fact about the value in hand, and measured that **keeping `hero_cstr_nonnull` makes `-Wformat` blind**, so the guard must go on that one argument, with the author's own 2026-08-15 `@cstr` exclusion as precedent. Two seats, the same two rules, opposite answers, neither careless. **Three of the brief's numbers did not reproduce** and one of the seats' corrections was itself wrong — `tests/golden/*.expected` has 0 `hero_str_cstr` and `tests/emission/` has **18 files**, so Q1 *does* have blessed-C churn — plus one false hypothesis (`putenv` is already refused) and one defect misread as a message fix. The ffi seat also **falsified its own prediction from panel 094** before being asked: `-Wformat-nonliteral` was *"a lever nobody has pulled"* and is 100% false positives | design.md §1.11, §1.12, §4.19 · CLAUDE.md §4, §8, §9, §11 | 096, 094, 091, 058

2026-08-28 | **Panel 096's Q2 was implemented in full and then reverted in full, because the mechanism the sitting ratified refuses a correct program** (author instruction *"sistemiamo due, tre cose"* before a talk; `docs/panel/096` § Q2's mechanism was built and does not work, appended after ratification; `tests/golden/fixedbugs/ffi-f32-against-a-float-header-is-correct.hero`). What was built: `HERO_VARIADIC` in the runtime, ABI 16 -> 17, the one-entry promotion table, `probe_line` emitting `__builtin_choose_expr`, `argument_columns` rebuilt, all five consumers rewritten, 148 emission traces and 6 emit goldens re-blessed, 516 selfhost tests green. What stopped it: the net named `examples/raylib/main.hero:80`, where `function ColorAlpha(color: Color, alpha: f32) -> Color` was **refused** against raylib's own `float alpha` — a correct binding, accepted since it was written. **Cause in nine lines of C**: `__builtin_choose_expr` forms and diagnoses BOTH arms, because clang's conversion warnings are emitted at expression formation rather than after the choice, so the `(double)` cast the variadic case needs is itself a `double`-to-`float` error against every non-variadic callee whose header says `float`. The cast moved inside the argument gives the same error. **What survives**: removal is still refused on its own measurement, `HERO_VARIADIC` is sound as a predicate (9 of 9, re-verified), and what is left of Q2 is the MESSAGE — *"the header's `double`"* where the header says `...` — which is what the coordinator first offered the author and the investigation then called insufficient. It is now the only sound half | **Neither seat attacked the shape that was already correct, and that is the generalisable half.** Both reported adjacent shapes 7/7; both attacked `f32` at a variadic slot and `f32` against a `double` header. `f32` against a `float` header on a non-variadic callee — the binding that needs no repair — is the only shape the promotion can break, and `examples/raylib/` holds one. CLAUDE.md §1's fourth rule read from the other side: **the class can include the case that is already right**, and a repair is attacked at the shapes next to the one that provoked it including the ones nobody is complaining about. Two more things this session found and neither is folded in. **The two DECIDE items that were about to go to five judges each had a dead premise**: `to_i64` asked the author to undo their own ratification of 2026-08-13 (`DESIGN-LOG:266`, panel 043 Q1, *"fai la soluzione più solida e sicura"*) without citing it, and its one new argument — 251 call sites — measures **38**; the float item's witness was superseded on 2026-08-05, since `5e-324` renders `5e-324` and Java's `1.9999999999999998e23` prints `2e+23`, the shortest round-tripping form, so the example proves the opposite of what it was cited for. Both were rewritten rather than convened or closed. **And two harness runs at once corrupted three blessed emission traces**, spliced program stdout into the C and mangled a first line, because both use `build/harness` as scratch and one of them was blessing — reverted, reproduced clean when run alone, filed. Those traces are the one instrument here whose source of truth no longer exists | design.md §4.19 · CLAUDE.md §1, §8, §9 | 096, 094, 043

2026-08-28 | **The site gets a deploy path, a gate that keeps it shut, and an engine: Cloudflare Pages by Direct Upload, and 44 hand-written pages become an Astro build** (author instruction *"copia tutto ciò che riguarda la pipeline di pubblicazione su Cloudflare"* and *"allinea le due tecnologie"*; `.github/workflows/deploy-site.yml`, `.github/workflows/release-site.yml`, `site/functions/[[path]].js`, `site/astro.config.mjs`, `site/src/`, `site/README.md` § Deployment). `site/README.md` had named GitHub Pages in one sentence and never wired it; the route is now the one the author's other site already runs, `npm ci` + `npm run build` on GitHub's runners and `wrangler pages deploy dist`, which is a Direct Upload and spends none of the Pages Free plan's 500 builds a month. **Deployed is not published**: a catch-all Pages Function shadows every URL with a holding page, 200 at `/` and 404 elsewhere, both `noindex`, with the real site uploaded underneath — CLAUDE.md §14's hard stop stays a hard stop, and opening the site is deleting two files. The port keeps every URL: `build: { format: 'file' }` means `src/pages/docs/maps.astro` writes `dist/docs/maps.html`, so the 44 canonicals, the 132 hreflang alternates and every relative link survive untouched. What stops being copied: the head, eighteen tags carrying **176 absolute URLs across 44 files**, and the nav, of which there were **44 distinct blocks and no two alike**. Deliberate divergence from the other site: no Tailwind, because this one's 45 KB hand-written stylesheet is the art direction, and no MDX, because the content is HTML | **A GREEN COMPARISON MEANT NOTHING FOR ONE BUILD, AND THAT IS THE FINDING.** The port was checked by a tool that reads every page four ways — prose word for word, `<pre>` byte for byte, markup modulo whitespace, and the document's own structure — and it answered 44/44 while **three pages had never been ported at all**: Astro copies `public/` into `dist/`, the old files were still there, and for `docs/index.html`, `it/index.html` and `it/docs/index.html` the copy won. Emptying `public/` turned the same run into 41/44 and named them **MISSING**. The cause underneath was worth more than the symptom: under `format: 'file'`, `pages/docs/index.astro` is the route `/docs` and writes `dist/docs.html`, so **the Italian edition's landing had been built at `/it.html`** — a dead link from the language switch of all 22 Italian pages, with the build log still reporting 44. The fix is a route that ends in `/index`, which means the file goes one directory deeper. **The checker itself had to be tested in both directions before any of it was believed**, and the first version failed: a probe meant to delete one paragraph produced a near-duplicate page, 23,335 bytes becoming 43,751, and every question answered "identical" because they all read the FIRST `<main>` and the duplicate sat past `</html>`. STRUCTURE exists because of that hour. **And the lockfile broke a records check on a rule about base64**: `opens_at` excludes a letter, a digit, `_` and `-` before an `M`, which is what makes `sha512-M3…` safe, but `+` and `/` are in the base64 alphabet — 4 occurrences in 232,880 bytes, and `M93B` was reported as a milestone identifier. A `GENERATED` list answers it, and beside it a new check, `records/exemptions`, fails when any entry of any of the three lists selects no tracked file. It found `site/log.html` dead on arrival — 0 matches, and 0 numbered identifiers in either edition of the log, so it was deleted rather than repointed | CLAUDE.md §9, §10, §11, §14 | —

2026-08-29 | **The site's URLs end in a slash, because that is the only shape its host serves, and Astro moves to 7** (author decision, options put with the measurement; `site/astro.config.mjs`, `site/README.md` § The URLs, and why they end in a slash). Measured live against this project's own Pages deployment rather than read anywhere: `/why/`, `/docs/`, `/docs/maps/` and `/it/` answer **200**, while `/why.html`, `/index.html`, `/docs/index.html` answer **308** with the extension stripped, and `/why`, `/docs`, `/it` answer **308** with the slash added. Cloudflare Pages removes a `.html` extension whether the site wants it to or not, so the ported site was advertising 44 canonicals, 132 hreflang alternates, 44 `og:url`s, 44 sitemap entries and every internal link at URLs that REDIRECT, each page's canonical pointing at the URL that redirects away from it. `build.format: 'directory'` + `trailingSlash: 'always'` now, which is also the pair the author's other site uses. Internal links became **absolute** in the same move (314 of them, across 44 files): relative links and directory URLs are a bad pair because every page sits one level deeper than its file did, and the change deleted the site's most confusing detail, the TWO different depths — one for the stylesheet, one for the nav — that existed because `/it/` is one directory below the root and at the top of its own edition at once. Astro 5 -> 7 in the same sitting: `npm audit` reported 3 findings, 2 high, 8 of them Astro XSS advisories; 271 packages instead of 356, `npm ci` 3.9 s -> 2.0 s, build 474 ms -> 277 ms, audit 3 -> 0 | **THE CAREFUL-LOOKING CHOICE WAS THE WRONG ONE, AND IT WAS CHOSEN FOR THE RIGHT REASON.** `format: 'file'` was picked deliberately so the port would preserve every existing URL and be a move of files rather than a move of URLs — and it preserved names the host refuses to serve. Preserving an artifact is only conservative when something else depends on it; here nothing did, because the site has never been published, and the same correction after a launch costs redirects forever. The advisories were the mirror image: every one of the eight is unreachable on a site with no server, no islands and no input, and the exposure really is zero — which is exactly the kind of argument that is true today and silently false the day somebody adds a component, so it went to the version with no advisories rather than to the reasoning. **Neither of these was found by thinking about it.** The URL shape was found by asking the server, after the deploy, what it does with `/why.html`; the version was found by reading what `npm ci` printed on a CI runner. Two facts about the world that no amount of reading the repository would have produced | CLAUDE.md §11, §12 | —

2026-08-30 | **The repository moves to an organisation and takes the name the language has: `giuseppearici/heroes-lang` becomes `heroes-lang/heroes`** (author decision, options put with the measurement; one `gh api .../transfer` call carrying `new_name`; `CLAUDE.md` §14, `site/README.md` § Launch order). Twenty-two lines carried the old address and every one of them was in the site or in a document — 20 across `site/src/` and `site/public/llms.txt`, one in `CLAUDE.md`, one in `site/README.md` — while **nothing in `.github/`, the compiler, `docs/`, `spec/` or any package metadata names an owner at all**, so the three workflows needed no edit. Verified after the move rather than assumed: 26 tags, 285 workflow runs, both Cloudflare secrets with their original timestamps, admin rights, three workflows active, `git fetch` clean | **What made this cheap is that GitHub Pages was never in play.** Half of what GitHub documents about a transfer is about Pages — a private repository on a free plan loses it, the site is unpublished, an unverified custom domain becomes claimable by a stranger — and this site ships to Cloudflare Pages by Direct Upload, so `heroes-lang.org`, its DNS and `site/public/CNAME` sit outside GitHub's reach entirely. Two premises were checked before the move instead of after. The name-retirement rule (>100 clones or Actions runs in the week prior) **does not fire**: 35 and 33, measured on the seven-day window — a first reading said 205 because that is the 14-day window the traffic API returns by default, and the same figure had one unique cloner behind it. So the old name stays creatable, which makes recreating it a standing way to delete every redirect by hand, and §14 now says never. And the organisation's `members_can_fork_private_repositories` is **false**, which is what actually blocks a personal fork — not the name, which was the thing being worried about. The history was scanned before anything moved: `gitleaks` over 830 commits and 124.87 MB reported 11 findings, all 11 were opened, all 11 are false positives in two files (8 C declarations in `seed/heroes.c`, 3 base64 rows of the vendored BPE table). The one real exposure is not a secret and has no remedy: `g.arici@codermine.com` is in 831 of 836 commits, and an email lives inside the commit object the SHA is computed over, so removing it rewrites every SHA the records cite. Left as it is, by author decision | CLAUDE.md §14 | —

2026-08-30 | **The compiler's filesystem calls stay out of `selfhost/`: the platform arm belongs to the runtime, and the proposal convened to move it was vetoed because it does not compile** (`docs/panel/097`, SOUNDNESS lane, provisional — author ratification pending; two judges, one veto, both compiling). What provoked it: the first `workflow_dispatch` since the `m-separate-compilation` tag measured all three CI legs, and Windows dies at `heroes doctor` because `mkdir -p build` reaches `cmd.exe`, which creates a directory named `-p`. The proposal was to replace the twelve filesystem shell calls with `extern` groups — `stdio.h`'s `remove`/`rename`, `sys/stat.h`'s `mkdir` on POSIX and `direct.h`'s `_mkdir` on Windows. **It has no expressible form.** `direct.h` is `error[ffi_missing_header]` on Darwin and Linux, and this language has no `#if` by design (`selfhost/cli_io.hero:22-27`); `struct stat`'s `st_mode` is **2 bytes on Darwin and 4 on glibc**, cross-compiled against real headers, so no width passes both and `empty_record` forbids naming none; `access()`, the third route, lives in the `unistd.h` removed on 2026-08-24 because MSVC does not ship it — which is why `runtime/hero_os.h` exists, and why `grep -rn '^extern ' selfhost/*.hero` is three groups and zero POSIX. Adopted instead, conservatively: the arm goes behind `hero_os.h` in `runtime/parts/fs.c`, beside the `#if defined(_WIN32)` that `runtime/parts/os.c:21-34` already keeps for `_setmode`, and `selfhost/` sees one wrapper with no platform word — DESIGN-LOG:282 applied unchanged. Seven conditions, ABI 16 → 17 among them | **the proposal would not have fixed Windows, and that is the finding that outranks the veto.** `heroes doctor` also runs `uname -s` and redirects `2>/dev/null`, neither among the twelve, and `cli_shell.sq()`'s POSIX single quotes reach **every clang line** — 47 uses across 7 modules, and executed as `cmd.exe` would, clang answers `no such file or directory: ''/tmp/p097/t.c''`. Twelve already-safe sites removed, six broken ones kept, **three of them silent** — including `cli_toolchain.hero:129-135`, which builds the runtime cache key with `cat 'dir'/*.c 2>/dev/null`, discards the status with `_ =` and returns `""`, so the key becomes `digest("")` and CLAUDE.md §7's only guard against a decoy runtime ships void. Second finding, from the other seat: `mkdir` answers **EEXIST for a file and for a directory alike**, so errno cannot say whether `build/` is the directory it wants or a file sitting where it should be, and the retry must ask `is_directory`. Third, and it is the thesis working on a machine nobody here has: cross-compiled to mingw, the all-extern version produced exactly one error class, on the `.hero` line — `too many arguments to function call, expected 1, have 2`, because Windows' `_mkdir` takes no mode | CLAUDE.md §7, §11, §12 · design.md §1.11, §1.12 · spec:204-207, :222 | 097, 049, 057 |

2026-08-30 | **The shell stops being the compiler's boundary with the operating system, and the argument list turns out to be the thing that cannot cross** (`docs/panel/098`, SOUNDNESS lane, provisional — author ratification pending; two judges, no veto on the direction, the same veto on the spelling from both). **The decision this answers was taken revocable and named this alternative by name**: `docs/work/DONE.md:760-768`, at M-selfhost-port — *"taken (revocable): bind C's own `system()` … The alternative (a runtime entry point wrapping posix_spawn with argv separation) is safer against quoting but grows the runtime; system() + careful quoting of the few paths we pass **is v1**. Veto or confirm."* v1 was reached 2026-08-18 and the clause expired on its own terms. What is adopted: `hero_run_reset` / `hero_run_arg` / `hero_run_go` behind `hero_os.h`, in its own runtime part, the Windows arm in C beside it, `sq()` deleted from `selfhost/`, the cache-key glob replaced by a readdir that returns a status. Thirteen conditions across the two seats, of which three are load-bearing: the single-call form is dropped, `posix_spawn` never appears as an `extern` in `selfhost/`, and the Heroes side branches on `status` before `code` | **the briefed shape does not compile, and the wall is higher than panel 097's**: `argv: [str]` is `error[ffi_type]`, the NUL-packed escape is `error[unknown_escape]`, and `posix_spawn_file_actions_t` has **no struct tag on either platform** — `typedef void *` on Darwin, an anonymous typedef on glibc, **8 bytes against 80**, bisected with `_Static_assert` under `zig cc`. 097's `st_mode` is refused on exactly one of {Darwin, glibc}; this is refused on both. **What the measurement bought, beyond Windows**: the same argument `a b 'c' $HOME` is **14 bytes** through argv and **20** (`a b c /Users/joseph`) through a shell without `sq()`, so the class is deleted rather than defended; `system()` answers 32512 for a missing program, decoding to **127 and colliding with a program that legitimately exits 127**, which is why `heroes build` on a machine with no clang says *"internal error: the runtime did not compile"* — the compiler blaming itself for the machine — and a program killed by SIGKILL decodes to `WEXITSTATUS=0`, reading as success. The cache-key glob's replacement is **163,739 bytes both ways, byte-identical**, and CLAUDE.md §7's guard gets a status where it had silence. Across all 22 sites the shell provides four features: two `>`, two `2>`, two `2>/dev/null`, one glob — zero pipes, zero `&&`, zero `$(...)`. **The honest cost**: Windows has no argv at the OS level, so the quoting moves into C and changes algorithm rather than vanishing. **And the coordinator's own number was wrong**: `sq(` repo-wide is **189, not 68**, with **121 in `tests/harness/`**, which carries its own `system()` and its own decode — so the author is choosing a milestone's size, not only its direction. One finding outlives the sitting: a leak in a new runtime part is **invisible to both instruments** — the gate counts `hero_alloc_block` only and ASan has no leak detector on Darwin arm64, measured by deleting a `free` and getting exit 0 with a silent gate | CLAUDE.md §7, §12 · design.md §1.7, §1.11, §4.19 · spec:204-207, :222 | 098, 097, 049 |

2026-08-30 | **Three sittings ratified together, and the Windows work opens as `M-argv-execution`** (author instruction, *"ratifica tutto, tutti i panel, fai adesso il milestone di Windows e sistema anche il problema della memoria"*). **096**: its first ratification of 2026-08-28 was overturned in part by a measurement on the code it asked for, so what is ratified now is the residue — the **message** is what survives of Q2, removal stays refused on its own measurement, Q1 does not land, Q3 goes to the full panel; the sitting's own open question (a fact about the value against a premise about provenance) is explicitly NOT closed by this yes and returns when Q1 does. **097**: the resolution stands as written, all seven conditions binding, and the wider question it flagged is answered — the work is taken now. **098**: all thirteen conditions binding, and the size question is answered **wide**, so `tests/harness/`'s 121 `sq(` uses and its own `system()` and wait-status decode are in scope; `docs/work/DONE.md:766`'s revocable clause, which named this alternative by name and said `system()` *"is v1"*, is answered **confirm** — twelve days after v1 made it checkable | **a ratification is not a rubber stamp when the record has already contradicted the sitting, and 096 is the case that proves it.** Ratifying its brief as written would have re-approved `HERO_VARIADIC`, which was built in full and reverted in full four days ago because `__builtin_choose_expr` diagnoses both arms and refused a correct raylib binding — so the yes had to be written against the record's later entry rather than against the sitting's own page. CLAUDE.md §1's *read forward to the end of that record* applies to ratification, not only to briefing. Two bookkeeping consequences, both instruments rather than notes: the alias table gains `M-argv-execution` and `tests/harness/suite_records.hero`'s `ROWS` moves 40 → 41, which is the check firing exactly where its own comment says it should — *"a new milestone reaches this number before it reaches a tag"* — and the chain renumbers 27 through 36 by one | CLAUDE.md §1, §4, §14 | 096, 097, 098 |

2026-08-31 | **A failed directory scan holds nothing, array included, and that is an invariant rather than a courtesy** (M-argv-execution step 9). `hero_dir_scan` answering -1 used to call `hero_dir_reset()`, which frees the names and keeps the block that holds them; `hero_dir_remove_tree` did the same at its exit. One live scratch buffer per call, invisible to every caller — and caught by the leak gate this same milestone added hours earlier for panel 098's condition 6, in a suite that had never been run: `heroes test tests/harness/main.hero` was 83 of 90 while the other two suites were green. The repair went to both shapes rather than to the one that fired (CLAUDE.md §1): five call sites in `tests/harness/shell.hero` answer a -1 by returning their own failure, and not one of them could have been expected to remember a release. The rule is now the return value's: **-1 means the runtime holds nothing.** Journal `docs/journal/026-argv-execution.md`.

2026-08-31 | **A probe whose silence cannot be told from success measures nothing, and three rounds of Windows diagnosis rested on one** (M-argv-execution step 10). The CI probe ran `build --emit-c -o path` and printed both streams; `-o path` writes to the path and says nothing, so the log was empty on Darwin and Linux too, where the emission works. Read as evidence for three rounds. What replaces it asks for the exit code and for the file's existence, which are the two things an answer can be read off, and a second step asks the smallest version of the question first — `heroes test` on a three-line program — before anything the size of a compiler module. The general rule is the one CLAUDE.md §1 already states about negative claims, applied to instruments: an instrument is admissible only if a failure and a success look different through it.

2026-08-31 | **M-argv-execution closes: the shell is out of the compiler, and Windows is a first-class leg** (26 steps, journal `docs/journal/026-argv-execution.md`). The decisions that outlive the day, each provisional under §4 with its item in `docs/work/DECIDE.md`: the watchdog is a runtime call (`hero_run_limit`, 124 on a kill — three probes could not tell coreutils' `timeout` from Windows' pause utility, so no probe decides any more), the runtime ABI sits at 18 and a bump propagates over THREE seed generations, and a Windows binary links with `-Wl,/STACK:67108864 -Wl,/INCREMENTAL:NO` because 1 MB of main-thread stack kills a recursive-descent compiler on its own modules, measured with two seeds one flag apart. Close: the net 1166/0 and the harness self-tests 92/92 on Windows, Darwin and Linux green in CI on the same commit.

2026-09-02 | **A `use` path starts at the directory of the file you compile, and five seats said so from five inputs** (`docs/panel/099`, full panel, provisional — author ratification pending; opening M-package-layout). The sitting **reopened a ratified ruling rather than filling a silence**: panel 032 adopted `S` (stay flat) on 2026-08-12 and fixed R4 as the landing form if the author overruled, and the author scheduled this milestone on 2026-08-25 to rule on it, so R4 (the form and the qualifier), R5 (the C component is the whole path) and R6 (`use` is scoped to the program) were inherited and only R4's one open half was argued: **ROOT** (the compiled file's directory, the same from every file) against **FILE** (the writing file's directory, `..` to climb). **Nobody sat for FILE.** The engineer found the answer already half-built: `selfhost/modules.hero:144` opens every module at *the compiled file's directory + name*, so "beside the file that names it" coincides with the root only because the tree is flat — ROOT is **≈95 non-test lines in five frontend files with zero in the mangler, the TU cache and the emitter**, FILE ≈180 plus a canonicaliser that five identity-comparison sites must never forget, and a missed one loads one disk file as two modules at exit 0. The pragmatist **compiled both of FILE's failure shapes on legal programs** — `error: redefinition of 'h_token_next'` and an undefined-symbol link error, both exit 2, panel 032 D1's class — and `nm`-diffed the SQLite binding into a directory: **zero of the 7 linker-resolved symbols move**, only the mangled accessor (panel 038's exception, behaving as designed). The warden beat both tabled wordings: **`w-root-min`, measured 3630 (+38)**, which *deletes* ", beside this file," from the `use geom` bullet because under ROOT that clause is **false** for every importer below the root — keeping it would write a contradiction §12 then enforces — and priced the climb operator alone at **+7 with three silences unclosed**, so **`..` gets no spelling at all** and a module above the root is a module of another program. The ergonomist's **blind phase 0** wrote the root-relative spelling unprompted and then built the sitting's sharpest artifact: a program **legal under both variants that prints 2 under one and 3 under the other**, which is why the base cannot stay unsaid. The historian found the asymmetry one-way — no language on record ever moved anchored→relative, every documented removal (PEP 328, Rust RFC 2126, Go's module-mode ban) killed a spelling with two meanings, and Node's relief valve for `../../` chains was **a manifest**, which §10 refuses. **The one hazard is named rather than smoothed**: compiling a file *below* a nested program's root re-bases its paths, `heroes test selfhost/lexer.hero` is that invocation today, and it is harmless only while the tree is flat — R5 makes it a stated rule owing a diagnostic and a golden, with the historian's Zig #13970 prediction standing | a base-less path rule is not a smaller spec but a wrong one, and this section has already charged the project one declined subdirectory for the same silence (panel 031's ergonomist) | §1.2, §1.3, §1.6, §1.7, §4.19, CLAUDE.md §4, §10, §11 | 099, 032, 031, 038

2026-09-02 | **Panel 032's ergonomist prediction is scored on its own page, four milestones after it was registered** (`docs/panel/032` § Scoring, appended with today's date; CLAUDE.md §14 — the record is appended to, never rewritten). It predicted *≥50% of `use` lines from a subdirectory to a peer take the root-relative spelling, and under a file-relative compiler a 6-file 3-directory program first-try-compiles ~0% of the time*. Panel 099's blind phase 0 **is** that run at n=1 seat: the one subdirectory-to-peer line it wrote was `use lex/token` from `syntax/parse.hero` — root-relative, 1 of 1 — and its program is illegal under file-relative in two `use` lines, a 0% first try. **Both halves confirmed on this sample, and the sample is one seat**, which is why 099 registers the harness-scale version rather than closing the question. This is `/step`'s close checklist doing what the bullet exists for: panel 036's *"score at M-ffi-ladder close"* survived that milestone untouched, which is the failure the rule was written against | a prediction nobody scores is a prediction that bought a clause for free | Part 11, CLAUDE.md §1 | 099, 032, 036

2026-09-02 | **A `use` may rename what it binds, and "unique" stops meaning "across a program"** (`docs/panel/100`, full panel, **RATIFIED the same day by author instruction** — *"se sei in dubbio forzo io l'AS locale"*, and that overrule is the spec-warden's own third lift clause rather than a way round its veto). The sitting was convened hours after panel 099 landed paths, because the author asked for the compiler's own 169 modules to be grouped by subsystem and **that tree does not compile** under *"Last parts are unique"*: measured, and the size is a property of the mapping rather than of the tree — families of ≥2 give **15 groups over 36 files**, ≥4 give **13 over 32**, and `state` ×4, `decls` ×3, `ops` ×3, `types` ×3, `builtins` ×3 collide under every mapping. **The sitting's product is that every wording on the table was wrong.** The ffi-pragmatist **implemented** the tabled sentence and found *"Last parts are unique unless renamed"* unimplementable where the check lives — `last_parts_collide` walks the file SET and never sees a `use` line, so two packages both shipping `client`, **both renamed**, are still exit 1 — and the historian, reading precedent and no code, named the same repair: **Modula-3 scopes uniqueness per compilation unit** and defines `IMPORT I` as sugar for `IMPORT I AS I`. Two seats, opposite inputs, one structural answer, and it answers the warden's §1.3 objection instead of dodging it: under a per-file rule a qualifier means what **this file's own `use` lines** say, three lines up. **What the C sees is nothing**: the bindings TU is **md5-identical** aliased and unaliased, `#include` order follows the use-line order and not the binding name, and the alias token appears **zero** times in the emitted C. **The compiler-engineer objected without vetoing and its measurement is the one that reframes the case**: the alias's exclusive contribution to nesting this compiler is **39 `use` lines in 37 of 154 files, 36 of them the single word `state`**, and **five renames take it to zero** — because the **4065** qualified mentions are rewritten by the nesting either way, so the alias never saved the bulk edit. So the feature's recorded warrant is **not** this tree but the case nobody can rename: two published modules ending in `client`, whose authors own neither the other's files nor a manifest to put a rename in (§10 refuses one). It also found a defect the convener would have shipped — a `token.Span?` alias field stops `ast.Use` being POD and puts refcounting into **84 of 170** translation units, so R8 makes the binding **total**. The landing form is the **dearest** of six measured candidates, **3663, +33**, by author instruction *"non mi importano i token"*: it shows `use syntax/decl as sd` instead of glossing it, which is how the document teaches everywhere else. **TWO CORRECTIONS AGAINST THE CONVENER, both on this page.** The brief told five seats that an alias reproduces the two-lexical-classes defect panel 032 killed the quoted form for — **false on the point that matters**: under the quoted form the *path text itself* differed between importers, while under an alias every importer writes `use check/state` identically and only the local qualifier varies. And the first draft of the verdict table carried a compiler-engineer row **with numbers in it while that seat was still running** — an unrun measurement written as run, in the judge's voice, in a document whose whole value is that its numbers were measured | a rule adopted in the morning met the only corpus that can price it by the afternoon, and the corpus won | §1.0, §1.3, §1.6, §1.7, §4.19, Part 7 item 4, CLAUDE.md §1, §4, §10 | 100, 099, 032, 074, 056

2026-09-02 | **A path keeps binding its last part, and the sitting that refused the alternative measured it as the better one on three counts out of four** (`docs/panel/101`, full panel, **RATIFIED the same day by author instruction** — *"nel dubbio seguiamo la storia"*). The author asked whether the binding could be the whole path **joined** — `use check/state` binding `check_state` — and the numbers were extraordinary: on the compiler's own tree **127 of 169 modules would bind exactly the name their call sites already write, 100% of them, with zero collisions**, so nesting `selfhost/` becomes a **558-line `use` edit with none of its 4065 qualified mentions touched**, against 4623 edits plus five renames. **Three seats approved it and the fourth's argument won**: the llm-ergonomist **VETOED the rule that now stands**, from the spec alone, on §1.3's own test — `state.enter(x)` is not determined by its line plus the enclosing signature when three modules end in `state`, *"and the wrong determination compiles"*; the spec-warden proved a **theorem and then ran it** (because panel 032 R5 sanitises the whole path, every joined collision is already a **component** collision, so under the joined rule `as` and the uniqueness sentence both become unnecessary and the spec **shrinks to 3624, −39**); and the ffi-pragmatist enumerated **162,165 path pairs** — joined-collides-while-component-does-not **zero**, last-part-collides-while-component-does-not **1970** — and found the packages case decisive, since under the last-part rule the collision fires *"on code the consumer did not write and cannot rename"*. **What decided it is the one argument no other seat touched: the last-part rule is REVERSIBLE and the joined rule is not.** Adding Idris-style unambiguous-suffix qualification later is additive; changing the default afterwards rewrites every call site in every program ever written. The historian put that beside a search that came back **empty** — no language binds a whole path joined by a separator that does not survive as structure, the only instance being Dune's `Wmo__Cumulus`, a build system with a **double** separator — and beside the two languages that do bind whole paths and **spent that uniqueness on renaming immediately**: Ada ships `Text_IO renames Ada.Text_IO` in its **obsolescent** annex, and Haskell was offered implicit last-part aliasing in **2001** (Wallace, with this exact objection) and declined it. **THE VETO IS OUTSTANDING AND SAYS SO ON THE PAGE**, with its falsifier registered rather than argued: if a read-and-extend harness task measures a non-zero silent wrong-module rate under the last-part rule, this resolution is wrong and the joined rule is the answer. The refusal is therefore **conditional**, on three named returns — that measurement, a real package ecosystem where the collision fires on files nobody may rename, and `as` usage in `selfhost/` above one line in five (measured by the convener at the historian's request, which it could not run: **39 of 558, 7.0%**, under its own 20% bar and so evidence FOR the refusal). **Three corrections the sitting produced against documents rather than judges**: **Go does not bind a path's last element** — it binds the name in the imported package's own `package` clause, so `gopkg.in/yaml.v2` binds `yaml`, and the convener's brief said otherwise; Python's whole-path form is documented as a **cache invariant**, not an ergonomic choice; and **Erlang's package removal, which panel 100 leaned on, does not adjudicate this question at all** — it killed hierarchy whose tooling did not understand it, and Erlang packages already had the short form | the compiler is the only corpus that can price a rule this project adopts, and twice in one day it priced one against its author's expectation | §1.2, §1.3, §1.6, §4.19, CLAUDE.md §1, §4 | 101, 100, 099, 032

2026-09-02 | **M-package-layout closes: a `use` may be a path, and a file may rename what it binds** (3 steps, 3 sittings, journal `docs/journal/027-package-layout.md`). `use syntax/decl` binds `decl` and reads `syntax/decl.hero` **from the directory of the file you compile**, the same from every file; `as` renames what one file calls a module; a file may not bind one name twice; there is no `..`, because a module above that directory belongs to a different program. Spec **3592 → 3663**, headroom 433, two ledger rows with their payments. **What outlives the day, beyond the feature**: CLAUDE.md §9 gains a rule by author instruction — a new surface form lands in **every tool that reads the language** — and it was written because `as` reached six consumers one at a time, with `heroes fmt` **deleting** the alias from a working program and the guard that watches the formatter **blind in the same place**, because it compares two trees by dumping them with the printer that had the same hole. One omission, two consumers, and the second was the instrument watching the first. **Four predictions came due and two lapsed**: panel 099's engineer holds on the emitter (zero changed lines in `emit_*`, `cli_units.hero` untouched) and is **false by 4.4×** on its ≤120 lines (measured 417 for the path work alone, a number that seat later quoted against itself); ledger row 3630's property **holds** — `use geom/point` appears three times character for character at two depths and nothing spells one target two ways — but it lives in the corpus rather than the `run/` golden it named, because `run/` is flat; and panel 100's warden and ffi-pragmatist predictions are **lapsed**, the first because nothing nested (nesting became chain row 29) and the second because `examples/sqlite` was never split. **The llm-ergonomist's veto of the rule that stands is OUTSTANDING**, with its falsifier registered and three return conditions on panel 101's page: this milestone's central decision is one measurement away from being wrong, and the record says so rather than implying otherwise | a milestone that decides a rule in the morning and is priced by its own compiler in the afternoon should expect to be asked the question twice, and both times the corpus answered | §1.2, §1.3, §1.6, §4.15, CLAUDE.md §4, §9, §11, §14 | 099, 100, 101, 032

2026-09-02 | **The net's own suite was red for six commits, and the cause is a command nothing is obliged to run** (M-selfhost-nesting step 1). `heroes test tests/harness/main.hero` answered **92 tests, 1 failed** on `main` at `d08062f`: `ceiling_for("selfhost/resolve_state.hero") == 308` against a table that says **315**. `9599d97` re-baselined the table and the assert did not follow — and **that commit's own diff states both new numbers in prose**, two screens above the assert (*"`resolve_state.hero` 308 -> 315 ... `parse_use.hero` is a NEW entry at 310"*), so the knowledge was present and only the execution was missing. The same assert was last repaired in `d9442ab`, whose subject is *"CI goes green again: three pins a later commit made false"*. **A SECOND dead number sat in the same case and nobody had seen it**, because Heroes stops a case at its first failed assert: `DECIDED.len() == 15` against a table of **16**. One stale number hid another, which is its own lesson about a case that pins several independent facts. **`DESIGN-LOG` had recorded this exact failure on 2026-08-31** — *"a suite that had never been run"*, 83 of 90 while the other two were green — and left the lesson without a caller: the command lived in `docs/ROADMAP.md`'s verify block, which nothing obliges anybody to open. **So the repair is the executor, not the asserts.** The third suite is named now in CLAUDE.md § Commands and in `/step` § 2, both of which said *"the two"*; the witness the case uses is the table's entry nearest `CEILING` by lookup rather than by name, which is what its own comment always claimed it was; and the two counts are re-measured (92/92, 16 entries) rather than nudged | a rule written in one document and performed by nothing is exactly the shape CLAUDE.md §3 already tells at length about `DECIDE.md` — and it turned up FOUR times in one evening, in four departments: this suite, panel 102's missing shadow stage, §8's never-run assertion about `certain` fixes, and a comment in the resolver asserting a check that did not exist | CLAUDE.md §1, §3, §9, §11, §14 | —

2026-09-02 | **A local may not take a module's name, and the ruling that says so is four months old** (`docs/panel/102`, full panel, five seats, **RATIFIED the same day by author instruction** — *"non preoccuparti troppo dei token, abbassa i suoi veti"*, which lowers the spec-warden's two vetoes and overrules its zero-token clause). Found by M-selfhost-nesting the moment the prefixes came off: a local named `state` beside `use check/state` made `state.value` mean the local record's **field** in one function and the module's **function** in another, one file, **exit 0 with no diagnostic** — both readings type-check because a field of `i64` and a function returning `i64` are indistinguishable at the use site. **The brief told five seats this was an open question and that was false**: the spec-warden found panel 031 R3, ratified, at `DESIGN-LOG:185`, which chose the verb *binds* precisely so `spec:99`'s shadowing ban would cover *"a local named after a module"* **for zero tokens**, and whose page carries this defect as its own reductio (*"otherwise `p = geom.Point(…)` followed by `geom = 3` is legal and `geom.f()` becomes ambiguous"*). So this is CLAUDE.md §12 — the spec was right and the compiler had the bug — and the convener's grep for `shadowed_binding` and `module_binding_taken` missed a ruling written in other words, which is §1's negative-claim rule applied to the record instead of to the world. **The compiler asserted the missing check in two comments and a test asserted its absence as correct**: `selfhost/resolve_qualified.hero:96-98` states it as an accomplished fact (*"a local named after a module is the shadowing error, reported where the local is bound"*) and `:226`'s case *"a local wearing the module's name turns the dot back into UFCS"* pinned the fallback. What lands is **(A) file-wide reservation**: a fourth arm on `resolve_state.declare`, standing **first** of the four because a `use` line is always in this file, reusing the code `shadowed_binding`, its fix a **`guess`** (panel 101 R4). **It reports and then BINDS, which is the compiler-engineer's condition and a measurement** — returning a failure leaves the name meaning the module, which is not a value, so one mistake became **five** diagnostics on `examples/json/parse.hero`; binding anyway takes it to one and leaves the pre-existing test at `:226` passing untouched. Spec **3663 → 3685, +22** at `:99`, the dearest of three measured wordings, paid by a registered prediction on an instrument that exists (ledger row 3685) rather than by the **−11** removal the warden found and declined — `spec:11`'s uniqueness clause is panel 100's four-day-old deliberate purchase, and cashing it to fund prose is a reversal, not a payment. `resolve_state.hero`'s ceiling 315 → **330** against a measured 325, with the seat's own argument on the record: a reader holding the first three arms holds the fourth for free | **(B) was vetoed by two seats on measured grounds and the argument that settled it came from the seat with no veto.** The historian found that **every language shipping (B) also ships `::`** — C++, C#, Nim — and that **Swift is adding it right now**, SE-0491 in 6.3, whose motivation is this defect verbatim in shape; Swift had also moved the other way in 2018 (swiftlang/swift#21378), replacing an ambiguity error with shadowing plus qualification because the error broke working code. Heroes has no `::`, so under (B) the repair is still renaming the local: (A)'s cost moved from the binder to every use site, priced by the warden at **+21** against (A)'s **+0**. It **struck both of the brief's precedents** — Modula-3 §2.1 resolves by smallest enclosing scope and Ada 8.4(10) hides silently — and found the real ones in **Zig**, closest in existence because `@import` binds a value read with `.` and which closed the identical order-dependent hole as a **bug** (#9194), in **C#** (CS0135/CS0136, twenty years, preserved through a compiler rewrite), and in **ISO 7185 Pascal** §6.2.2, in Heroes' own ancestry. Heroes' asymmetry turns out to be **Go's asymmetry exactly** — file block against package block — and nobody has ever defended it in writing, while `go vet -shadow` was **withdrawn in Go 1.12** for imprecision, which argues for an exact rule over a heuristic one. **And the sitting's most useful finding is not about shadowing at all**: `needs_qualifying`'s `certain` fix **does not terminate** — on `window: ptr` beside `use ffi/window` it writes `window.window.destroy`, then `window.window.window.destroy` — because CLAUDE.md §8's promise that CI checks an applied `certain` fix has **never once run on the module class**: `.fixed` files live only in `check/`, a `check/` case is one file, and every module diagnostic needs two. Golden cases naming `needs_qualifying`: **0**. Plus a live **heap-use-after-free** on `main`, unrelated to the sitting, minimised to 14 lines by the engineer and reproduced by the coordinator: `@` on an immutable by-value parameter is accepted, and `spec:90` already forbids it | CLAUDE.md §1, §8, §11, §12 · design.md §1.3, §1.4, §1.11, §4.19 · spec:11, :90, :99 | 102, 031, 101, 100, 011

2026-09-02 | **M-selfhost-nesting closes: 134 modules move into ten directories, and four written rules turn out to have no executor** (2 steps, one sitting, journal `docs/journal/028-selfhost-nesting.md`). `selfhost/` is ten directories and 37 flat files; a qualifier is a module's last part unless the file renamed it, and **41 of 1267 `use` lines do**. Four modules were renamed to the word their own module doc already used (`ir/containers`, `module/diagnostics`, `inventory`, `parse/use_line` — the last one forced, because `use` is a keyword and cannot be a binding), ten kept their prefix through `as` because their last part is a word this compiler uses for a **value**, and three locals were renamed because their own names were the wrong ones. **The finding worth more than the feature: the prefixes were load-bearing.** They were not only organising the tree, they were keeping module qualifiers out of the namespace where values live — nobody names a local `emit_decls`, everybody names one `decls` — and the pair that proves it is **0** live collisions flat against **23** nested, same code. **Nesting changes no emitted C at all**: 803,134 seed lines before and after, and of 7018 non-`#line` differing lines, 6670 name a renamed module's symbol, 288 are anonymous-type hashes containing one, 60 are the three local renames, and **zero** are anything else — because the mangler sanitises the module component to `[A-Za-z0-9]`, so `check_state` and `check/state` are the same symbol. Close: `heroes check` 0, the compiler's own tests **536 all passed**, the net **1202/0**, the net's own tests **92/92**, the seed regenerated and the fixpoint verified. **Six predictions came due**: panel 100's engineer FALSE at 41 against >300, its historian FALSE at 0 and false *by choice* (aliasing a module in every file that binds it, not only where the collision bites), panel 101's engineer FALSE by 3429 on the source diff and **TRUE** on the emitted C — the half about the backend, held for the seat's own reason — panel 101's 20% return condition unmet at **3.2%**, and panel 102's warden **HOLDS** at exactly one rename in 501 files | **four rules with no executor, in four departments, in one evening** — panel 031 R3's shadowing coverage that the compiler never delivered while two of its own comments claimed it had; CLAUDE.md §8's promise that CI checks an applied `certain` fix, never once run on the module class because `.fixed` is single-file by design and every module diagnostic needs two; `heroes test tests/harness/main.hero` red for six commits over a number the comment above it promises to keep in step, while the command lived in one document and in neither of the two anybody follows; and a second dead number hidden inside that same case because Heroes stops at the first failed assert. CLAUDE.md §3 already tells this story about `DECIDE.md`. What each repair adds is the caller, not the rule | CLAUDE.md §1, §3, §8, §9, §11, §12, §14 · design.md §4.9, §4.15 · spec:11, :90, :99 | 102, 101, 100, 099, 031, 011

2026-09-02 | **M-corpus-coverage closes: the first program that reached the library broke the compiler** (6 steps, journal `docs/journal/029-corpus-coverage.md`). Twenty programs, 21 modules, 3762 lines, **156 `test` blocks**; `examples/` goes 15 programs to **35**, 55 modules to 78, 6426 lines to **10,286**. The first ten were chosen by a coverage measurement re-taken over **56 language forms** — the scheduling item's own figure was wrong twice, saying 39 of 46 exercised and 7 not, where the real count was **12** and two of its "thin at one use" forms were at **zero**, having counted occurrences inside comments — and the rest by the shape of what they can get wrong. **The item's central claim paid off on the first program**: *"a corpus that does not reach the library cannot find a defect in the library"*, and `examples/readings/` produced `heroes check` 0 with `heroes run` 2 on its first run. **The defect**: one generic call chained directly into another through UFCS — `xs.map(to_n).fold(0, add)`, the spelling `spec:113` teaches the feature with — monomorphised `map` at `fold`'s type arguments, because both tables keyed each call by its span **START** and `grammar_expr.hero:322` gives a method-call node its start from its **receiver**, so a chain's inner and outer calls begin on the same byte. The premise was asserted in TWO files, `check/state.hero` as *"a span is a byte range and two calls cannot share one"* and `ir/containers.hero` as *"at check time a span occurs once"*, and the second copy is what made the first look checked. **Both are keyed by the END now**, which rests on the other half of the same asymmetry: a call ends at its own closing parenthesis, so an outer call always ends strictly after the inner one. Nine sites, one word each, and CLAUDE.md §11's two obligations both paid — the claim rewritten as falsifiable, and `tests/golden/run/fixedbugs-a-ufcs-chain-gave-two-calls-one-key.hero` verified to PASS fixed and FAIL unfixed, its blessed emission recording **three** distinct `map` instances where the unfixed compiler emitted one. Close: the net **1309/0**, the compiler's own tests 536, corpus floor 15 → 35 re-measured six times | **the corpus corrected the person writing it SEVEN times, and a checklist of features would have produced none of them**: a generic cannot sit on a record (`spec:116`); two parameters of one type make labels mandatory (`spec:104`, hit by five of the twenty); `heroes fmt` deleted the C parentheses around `mode & MASK == 0` because `spec:166` binds `&` TIGHTER than a comparison, the opposite of C where that line is the oldest bug in the language; `if c a else b` on one line does not exist, so *"there is no ternary"* means there is no inline conditional either; a multi-line `match` cannot sit inside `ok(...)`; an accumulator bound with `=` cannot be mutated; and the seventh was found by a **failing test** rather than the compiler — an escape rule that protected `{{` and not `}}`, so `{{name}}` refused. **And the sweep of the adjacent shapes produced the general rule** (CLAUDE.md §1): the compiler's other two span-keyed tables key on a `name.start` and are sound, because the start of a TOKEN is a fact while the start of an EXPRESSION is inherited from a child | CLAUDE.md §1, §9, §11, §12 · design.md §1.2 · spec:104, :113, :116, :155, :166, :167 | 102, 011

2026-09-02 | **M-documentation-site closes: the site can now teach where a program's files go** (journal `docs/journal/030-documentation-site.md`). Chapter 13, `Modules and files` / `I moduli e i file`, in both editions, neither a translation of the other; the docs go 13 pages per edition to **14** and the site 44 built pages to **46**. It is the last chapter rather than one in the middle for an arithmetic reason and not a pedagogical one: inserting it would renumber twelve pages and rewrite every crumb and previous/next link, so `the-c-boundary` gains a forward link instead. **Every code block is a slice of `examples/shapes/`** — the six-module program in three directories, eight tests green — and that buys the page something the site's slice rule was not designed for: the program prints `10` for the root's `scale` and `3` for the neighbour's, so if path resolution ever flips, `main.expected` changes and the corpus goes red. **The page cannot quietly stop being true.** The three diagnostics are real output produced in the session that wrote the page (`module_path_climbs`, `module_binding_taken`, `shadowed_binding`), and the third is panel 102's rule, ratified the same day: the chapter can teach it because the compiler enforces it, and the compiler enforces it because a corpus program found the hole that afternoon | **the item's own complaint was already fixed and the number that was actually stale was a different one.** It said `selfhost.astro` claims *"165 modules"* against 169; it says **170**, correctly, repaired before this milestone opened. What was stale is the LINE count — ten places across both editions said 49,988 against a measured **50,100**, moved by this evening's own repair to the generic-instantiation key — plus `site/README.md`'s 44 pages against 46 and `docs/ROADMAP.md`'s live 50,063, correct at the last close and overtaken since. Four stale numbers, and the one the item named was not among them. **Verified rather than reviewed, because the site's rules are written as things a script can check**: zero em dashes in the prose of either edition with `<pre>` excluded, eight figures diffed against their `data-src` slices at zero drift, captions checked against the same attributes, sixteen links resolved against `dist/` at zero dead. The one slip a script could not catch was Italian prose: *"togliata"*, which is not a word | CLAUDE.md §11, §14 · site/CLAUDE.md · site/README.md | 102, 100, 099

2026-09-02 | **String interpolation gets a milestone of its own, and what it delivers is the RULING** (`M-interpolation-verdict`, chain row 37, **author instruction**). design.md Part 7 item 7 has read *"deferred; `print` takes multiple arguments"* since 2026-08-03 and nothing has asked the question since: measured this session, `grep -rn -i interpolat` over `docs/panel/`, `docs/journal/` and `docs/measurements/` returns **zero hits**, so the entire repository says two things about it and both are in design.md. The name is the verdict rather than the feature because the milestone must be able to close with a **refusal** — and under CLAUDE.md §12 a refusal owes the same proof as a feature, so a no lands as a Part 6 row with its falsifier, never as a second deferral. **The sitting is full five seats** (surface, spec tokens, a diagnostic class) and the llm-ergonomist's is the deciding seat, because Part 7 item 7 is **not on the closure list** — the compiler self-hosts with none of it — which leaves a measured Part 11 effect as the only available warrant. Ordered after the tools and before the two books: a form that lands after M-guide-book rewrites chapters in two languages, one that lands after M-vscode-extension adds a rule to a grammar file | **Part 7's preamble stopped forbidding the question at the fixpoint (2026-08-18) and nobody noticed** — *"nothing on this list is considered until the closure list compiles itself"*, which CLAUDE.md §13 repeats as a place not to go, has been satisfied for fifteen days. What the sitting must not re-derive is that **this trade was already made in the other direction**: `design.md:1403` records that `print`'s variadic-looking form *"was bought by trading away string interpolation"* (panel 006), so the question is whether to buy back something already sold, and the proposer owes the other side of that trade. Two more constraints are already on the record and are not the sitting's to invent: panel 008 R2 reserves the backslash (*"any other character after `\` is a compile error"*) with R3 stating a new escape **reconvenes that panel**, which prices Swift's `\(name)`; and `{` already means a map, so a brace spelling owes the doubling escape `examples/template/main.hero` implements. **What its absence costs, measured 2026-09-02**: **945** lines of `selfhost/` hold `" + ` and **146** hold `to_str()` — this compiler's own diagnostics assembled by hand — **118** `print(` calls in `examples/` take the multi-argument form, and `examples/template/main.hero` (235 lines) already interpolates at run time with `{key}`, doubling as its escape, refusing an unknown key, and with a corpus-found defect in that very rule (journal 029). Spec **3685** of a hard 4096, headroom **411**, spread 79 (`heroes measure`, this session) | Part 7 item 7, Part 6, §1.2 · CLAUDE.md §9, §12, §13, §14 | — (006 and 008 are what it inherits)

2026-09-03 | **The ROADMAP's chain runs closed-then-scheduled, and the table stops carrying prose** (author instruction, *"riordina roadmap con tutto quello che è stato fatto prima di quello che deve essere fatto, in più tutte le note spostale dalla tabella … la tabella la voglio pulita"*; `docs/ROADMAP.md`, third reorganisation, after 2026-08-25's and 2026-08-26's). Two changes. **The order**: rows 1–31 are the closed milestones in the order they closed, rows 32–40 what is next, so `M-documentation-site` — done 2026-09-02 and sitting at position 36, below five open milestones, because the table was recording the plan as well as the outcome — moves to 31 and the five it overtook keep their turn among themselves. That is the journal split's own rule (a reader meets the past before the first line of the future) applied to the table instead of to the prose. **The cells**: every scheduling note, panel ratification and author decision that lived inside a row is now a line in a new § Who scheduled what, **keyed by name rather than by row number**, because a reorder moves a number and never a name (CLAUDE.md §14). Nothing was dropped; the longest cell falls from 383 characters to 91, the second longest from 243 to 85, both measured. Row 27 is still `M-argv-execution` and row 29 still `M-selfhost-nesting`, so the three live citations of a chain row by number — `docs/environment/windows/WINDOWS-MACHINE.md`, `docs/journal/027-package-layout.md` and this log's own row-37 entry — all still resolve, checked rather than assumed. **And the reorder found two sections describing shipped work in the future tense**: `M-package-layout`, closed the day before, and `M-documentation-site`. Both now carry *(closed …)* in the heading like `M-separate-compilation`, with their record named and their brief kept and marked as a brief — and reading the first one to write that framing turned up a live stale premise in the compiler, `selfhost/parse/use_line.hero`'s comment on the `use` declaration, which still said *"there is no nesting"* one day after `M-package-layout` landed the slash. CLAUDE.md §11's shape exactly: the argument stays valid while the premise dies in silence. **One number left the table rather than being carried a third time**: row 29 said *"134 modules move into ten directories"*, and `find selfhost -mindepth 2 -name '*.hero'` counts **133** nested of **170**, 37 flat, in 10 directories — the same row's own § The names entry already records it having said 169 where the tree held 170, so the cell now names the deliverable and the journal keeps the count. Records suite 9 of 9, the compiler's 536, the net, and the net's own 92.

2026-09-03 | **Ten decisions in one sitting, and the criterion that chose them: robust over cheap** (`/decide`, twelve items read, every one verified against the repository before it was put to the author; author instruction *"ratifica tutto e poi dimmi cosa rimane da fare"*, then *"aspetta scegli le soluzioni più robuste e complete rispetto a quelle più economiche"*, which reversed one recommendation and strengthened three; and then, when a plan was asked for, three more sentences that bind every item this entry schedules — *"privilegia il consolidamento e le soluzioni migliori non le scorciatoie"*, *"più storia e meno attenzione al token"*, *"più robustezza su tutte le piattaforme, non silenziare errori"* — so the two sittings weigh precedent above the token count, every landing is measured on the Mac, the Linux image and the Windows box before its commit, and no `-Wno-` flag is on the table anywhere). **Two items closed by verification and were never asked**: `to_i64` on `i32` — nothing reopens `DESIGN-LOG:266`, 61 fallible conversions re-measured against the item's 38 and the ruling's 8, the corpus grew and the order of magnitude did not; and panel 102's ratification, already written into its file the day before, kept alive by a premise about `suite_records` that the check's own line refutes (an open item is owed only while a verdict reads `Pending`). **Three ratifications**: `hero_run_limit` as a runtime call at ABI 18; `-Wl,/STACK:67108864` and `-Wl,/INCREMENTAL:NO` on Windows link lines; and panel 099, ROOT as the base of every `use` path, whose verdict section now reads *Ratified* — and whose one unsettled half, R5's leaf-test hazard, turned out to be scheduled nowhere (0 hits in every list) and is now. **Five decisions, each taken in its complete form**: `@` on an immutable binding is fixed as the first step of what remains, and the fix is one rule with **eight measured witnesses** — a by-value parameter, a `=` local, a field, an index, a nested record, a `for` variable, a `match` payload and a generic all pass `heroes check` today; field 134, index **138**, nested 134, and three silent mutations printing `2` at exit 0 — because CLAUDE.md §1 says a repair is attacked at the shapes next to the one that provoked it and here they were attacked before the repair; `needs_qualifying`'s `certain` fix drops to `guess` AND `.fixed` grows to the multi-file fixtures, because the item's claim that panel 102 had closed the path was **measured false on the seed of 2026-09-02 17:52** — `window: ptr` beside `use window` gives two diagnostics and `--apply` writes `window.window.destroy()` at exit 0, the fix reproducing itself on the FFI's most natural spelling; the FFI pointer verdict goes **strict**, `-Werror=incompatible-pointer-types` for every clang with the emitter passing `(void *)&x` for a `ptr` out-parameter and the compiler checking a numeric out-parameter's pointee by width and sign itself, which reverses the convener's `-Wno-error` recommendation — uniform by loosening is uniform by blinding — and costs the compiler learning the header's parameter types, the mechanism the sitting owes (measured: clang 22 in the Linux image errors by default on `void **` → `char **` and `int *` → `long *`, Apple 21 warns; `@n: i32` against `size_t *` builds at exit 0 with five warnings; and `@n: u64` against `size_t *` **warns on macOS too**, `unsigned long long *` against `unsigned long *`, so the spec's own advice through a pointer is the case the strict form must keep legal); deep recursion gets a guard-page handler that **names the function** through symbolisation rather than a bare `panic: stack exhausted`, soundness lane first (measured: Mac 50,000 frames exit 0 and 100,000 exit 139 — the item's 500,000 was a smaller frame — Linux 100,000 exit 139; `runtime/` has no signal handling at all); and the harness scratch carries the pid AND the blessing validates a trace before writing it, so no corruption from any cause is ever blessed. **Two routings**: what `print` guarantees a float becomes a question `M-interpolation-verdict` must answer with a sentence or a Part 6 row (measured: `5e-324` → `5e-324`, Java's `1.9999999999999998e23` → `2e+23`; `runtime/parts/f64.c:26` had cited the dead example for a month, corrected at `830e416`); and panel 096's Q3, the `strtok` arity case and the pointer verdicts share one FFI sitting, because Q3 had been sent to the full panel on 2026-08-28 and scheduled by nobody. **Where the work went**: `docs/work/SCHEDULED.md`, seven items, six of them under a milestone proposed as `M-robustness-guards` and not yet named by the author — the id enters the ROADMAP with its first step (CLAUDE.md §14: an id is never renamed once it is in the record, so it is not written into one until it is chosen). `DECIDE.md` holds zero open items. Records suite 9 of 9, the net's own tests 92 | CLAUDE.md §1, §3, §4, §7, §8, §9, §12, §14 · design.md §1.11, §1.12, §4.19 · spec:88, :184-185, :211, :215 | 099, 096, 102, 101, 097, 047, 043

2026-09-03 | **The FFI pointer verdict goes strict, and the compiler learns the header's parameter types** (`docs/panel/103`, full panel, five seats, **provisional — author ratification pending**; M-robustness-guards step 3). The question the `/decide` sitting of the same morning put to the author — a verdict that differs by host clang, and `getline(@n: i32)` against `size_t *` accepted at exit 0 while C writes 8 bytes into 4 — and the author's answer, robust over cheap, never a `-Wno-` flag. **Five seats, none for doing nothing, and the two that compile corrected the brief on the same point**: the strict flag alone is uniform by platform lottery, because `long *` and `long long *` are the same 8 bytes and different types on macOS — `time(@t: i64)` and `getline(@n: u64)` would be refused on the Mac and accepted on Linux and Windows, the engineer measuring a FALSE `ffi_parameter_type` on the Mac for a correct program. So the cast is on **every** `@` argument to an `extern`, `(void *)&x`, in all three spellings (call, probe, `_Static_assert`), and the pointee check is the compiler's own, width AND sign, because the flag never checks sign (`-Wpointer-sign` warns on all three clangs). **The mechanism, tried three ways and measured on clang 18, 21 and 22**: pure C11 has no parameter-position `__typeof__`; a `transparent_union` redeclaration is free and uniform but `conflicting types` the moment any parameter is `ptr`; DWARF is cgo's shape and the historian's preference, but the CI's Linux leg has no `llvm-dwarfdump` and Windows emits CodeView. What lands is one clang process per cold unit — `extern __typeof__(name) hero_ty_name;` per numeric-`@` extern, `-ast-dump=json -ast-dump-filter=hero_ty_`, 0.02 s, 6–8 KB, the same `qualType` shape on all three clangs — and two `_Static_assert`s in the probe block at the declaration's `#line`, so the failure is claimed by `extern_at_line` and is `ffi_parameter_type` at exit 1 everywhere, by the header's own text and never a width table. **The spec**: w5 at **3718, +33 net** — the warden's wording, the clause's third size in nine days (+12, +32, +33), and the sentence at `:237` folded beside the example that uses it; w3 vetoed as false (`cstr` is a pointer that is not `ptr`), the ergonomist's `ptr` or a `cstr` refused on a measurement (a `cstr`'s pointee is already checked, `ffi_writable_parameter`, exit 1 today). It lands only in the same commit as the check, never before (panel 060's class). **Q3 rides**: a clang complaint about the CALL of an author-declared extern is exit 1 on the `.hero` line, the 21st `ffi_*` code minted for two texts (arity; `-Wformat-security`), cheaper than panel 096 priced because the probe reproduces the mistake at the declaration line; a variadic bound at fixed arity stays green. **Q4 does not land**: `-Werror` on all generated C is free today at `-Wall` and the record — GCC 14 and Clang 22.1.0, each after a distribution-wide rebuild — tightens one named diagnostic at a time. **Two author instructions arrived mid-sitting and bind**: *"se serve puoi richiedere una versione minima di clang recente a piacere"* — the floor is **clang 18**, the oldest measured (the CI's Ubuntu leg), enforced by `heroes doctor` and the build at exit 2, chosen from measured versions and not from a preference; and the `-Wno-` exclusion. The historian's sourced record: upstream PR #157364 (2025-09-15) made the diagnostic an error in Clang 22.1.0 (2026-02-24), GCC 14 in 2024 after Fedora fixed 381 packages; cffi's API mode is the one FFI that checks a user-declared pointer against the header; Microsoft's 64-bit checklist names the cast on an out-parameter as the way a width bug passes a compiler — which is why the cast is confined to what the compiler has checked | CLAUDE.md §1, §4, §7, §12 · design.md §1.11, §1.12, §4.19 · spec:209-211, :215, :237 | 103, 096, 092, 060, 058, 099

2026-09-03 | **Deep recursion stops with a word: a guard-page handler that names the function, and no depth counter** (`docs/panel/104`, soundness lane, compiler-engineer and ffi-pragmatist, **provisional — author ratification pending**; M-robustness-guards step 4). Measured before the sitting: the five-line recursion is exit 139 with silence on the Mac at 100,000 frames and on Linux, and **exit 127 with silence on Windows** at 1,000,000 — the number `hero_run_go` also uses for *program not found*; `runtime/` has no signal handling at all. **Both seats approve (a) with conditions and reject (b) for the same reason from two sides**: the engineer measured the depth counter at +55–60% per call at `-O0` and at 0.81 s against 0.000 s at `-O2`, because a counter with a side effect forbids the transformation that turns the recursion into a loop; the pragmatist showed that where the handler is blind — a library's own thread, a library overwriting the handler after `main` — the counter is blind too, since those frames are C. **The engineer's prototype works and moves nothing in the compiler**: `runtime/parts/stack.c`, 156 essential lines, installed from `hero_args_set` so the emitted `main` and the ABI stamp do not change; `panic: stack exhausted in d100000.down`, exit 134, on the Mac and in the Linux image; a frame walk names the function even when the fault lands inside `__vfprintf`. **Three findings corrected the brief**: *restore the default and return* is a compiled defect — a library that installed its own handler in a constructor loses its write-barrier at exit 138 — so the previous disposition is saved and **chained** (measured: 134 with the library recovering); on `si_addr` alone a wild store 8 KiB under the stack was reported *stack exhausted*, a false abort, so the handler needs **two witnesses**, the address in the guard region AND `sp` within 4 KiB of the bound; and `SIGSTKSZ` is 8,192 on glibc 2.41, so the alternate stack is sized in bytes, `mmap`ed with a `PROT_NONE` page below. Under `--sanitize` nothing is installed — ASan's report names the `.hero` line and is the better one. Linux needs `_GNU_SOURCE` and `-rdynamic` for the name; Windows gets `AddVectoredExceptionHandler` and is measured on the box before the commit or the step does not close. **Threads are a recorded hole, not a fix**: a C library's own thread that overflows dies 138 today and 132 with the handler; M-isolated-threads owns the per-thread stack. Real SDL2 and SDL3 install no SIGSEGV/SIGBUS handler after `SDL_Init`, which is the pragmatist's registered prediction | CLAUDE.md §1, §4, §12, §13 · design.md §1.11, §1.12, §4.19 | 104, 097, 070

2026-09-03 | **Panels 103 and 104 ratified** (author instruction, *"confermo la ratifica dei 2 in decide"*, given the same afternoon: the FFI sitting while its step was being built, the stack sitting before its step opened). Both verdict sections read *Ratified*, the two items leave `docs/work/DECIDE.md` for the record, and nothing in either resolution changes — the predictions stay predictions, scored at the milestone's close. The clang 18 floor and the `-Wno-` exclusion, both given mid-sitting, are ratified with them | CLAUDE.md §4 | 103, 104

2026-09-03 | **M-robustness-guards closes the day it opened, and four decisions taken inside it are recorded here rather than only in commit bodies.** (1) **`-rdynamic` goes on every POSIX link line, not Linux's alone** (`selfhost/cli/flags.hero::link_flags`): glibc's `dladdr` reads `.dynsym` only, so without it the stack guard's message loses the function's name; Darwin names the symbol either way and accepts the flag at exit 0 with no diagnostic, so the flag is POSIX's and `selfhost/` keeps asking the machine one question, `exe_suffix()`, instead of gaining a second platform axis. (2) **The deep-recursion fixture recurses ten million times**, because panel 058's ratified 64 MB `/STACK` on Windows holds a hundred thousand frames (`100000` at exit 0 on the box, measured) and a fixture that overflows on two platforms out of three tests the platform; and the surface row asserts the message and not the exit code — `abort()` is 134 on POSIX and the box reports 127. What the measurement also overturned: `-O2` does not turn the recursion into a loop, it makes the frame smaller — a hundred thousand printed at `-O2` on all three, a million panics — and the sitting's *0.000 s at `-O2`* was one depth read as a property of the optimiser. (3) **A blessing checks its capture's first line against the program's own path exactly** (`suite_emission.hero::is_a_capture`): the emitter's header names the file it compiled, a capture of another program is the race the pid directory closes, and the blessed traces have no other source of truth. (4) **Panel 099 R5's hazard is pinned as a rule by a fixture, not repaired**: a leaf compiled below its root re-bases its `use` paths; with no file there it refuses and names the directory and the file that decided it, beside a decoy it checks clean against the wrong module, and both rows say which is the rule and which is the hazard. Two seat predictions are falsified at this close (`docs/panel/103`, `104` § scored): `stack.c` is 316 lines by `wc -l` against a predicted ≤ 200 — the essential lines plus the comments that carry each measured reason — and step 3 added 840 code lines under `emit/`+`cli/` against a predicted 350–550 | CLAUDE.md §1, §9, §12; design.md §1.12, §4.19 | 103, 104, 099, 058

2026-09-03 | **M-corpus-depth is scheduled at chain row 34, and its step 0 — the measurement before any program — finds that `heroes mutate` has been unable to read `examples/` for a day** (author instruction *"ragiona sulla possibilità di aggiungere ulteriori esempi anche alcuni più complessi per avere una rete più ampia, guarda anche cosa hanno fatto altri linguaggi"*; plan measured and approved the same day; placed after M-robustness-guards by the author's choice, which its close that afternoon satisfied, so it stands behind the open M-isolated-threads). **What the planning measured**: form coverage of `examples/` is at zero unexercised (journal 029) and the corpus is thin in **shape** — the six `extern` programs are 65–192 lines and single-module with no `variant`, nested container, generic or `@` structure, so FFI and "real program" are disjoint sets; generics are declared in three programs and never cross a `use`; direct self-recursion in three, shallow; one program (`sieve/`) checks an answer somebody else wrote down; one (`todo/`) writes a file; `i8`/`i16`/`u16`/`u64` declared nowhere; `exit(0)` never; and between `json/` at 671 lines and the compiler at 50,452 there is nothing, where Nim keeps `tests/manyloc/`, Zig `test/standalone/` and Rust `rustc-perf`'s crates — nine languages surveyed, twelve recurring example-suite patterns, ten already here, the two missing being that rung and a docs-drift instrument (`site/README.md:889-893` admits the site's `data-src` promise has none). **Nine programs, three families**: five with public oracles (`nbody/`, `spectral/`, `fannkuch/`, `binarytrees/`, `checksum/`), two large (`interpreter/` ≥ 1,500 lines in three directories, `query/` over 5,000 generated CSV rows), one FFI at program scale (`ledger/`, sqlite, ≥ 15 functions). **Step 0's finding**: `heroes mutate` over `examples/` is refused at exit 2 in 2.10 s — `examples/shapes/geom/area.hero` and `render/ascii.hero` are `unknown_module` when checked from their own directories (panel 099 R5's leaf hazard, the rule step 6 of M-robustness-guards pinned), and `mutate` checks every file from its own directory; broken since `c12de74` (2026-09-02), unseen because the tool runs over `examples/` in no CI leg. The fix is M-corpus-depth step 1, ahead of every program; the CI question is in `DECIDE.md`. **The provisional score**, 35 directories summed after the sum was verified equal to one run on the gallery (538 | 6 | 514 | 416 in every column): **10,711 mutants, 10,099 killed (96%) strict, 8,782 (83%) permissive** — 1.9× measurement 006's mutants at the same rates; `typo-code` 0/34 and `typo-digit` 0/29; `swap-args` 80%, the weakest live operator; 680 s for the 36 runs, six programs half of it. **The corpus leg alone**: 203.91 s and 188.02 s, 35/35, the "before" the milestone's +1:15 is checked against. **Three record faults repaired in the same commit**: `examples/README.md` cited `tests/corpus.rs` (archived 2026-08-19) and listed 14 of 35 programs; `SCHEDULED.md` held M-corpus-coverage open a day after its close; `DONE.md:924` ticked the fourteenth `mutate` operator with the twenty-programs closure under it — the operator is re-opened, the record keeps its words. `docs/measurements/014-mutate-over-thirty-five.md` | CLAUDE.md §1, §3, §9, §14; design.md §1.1, Part 11 | 102, 099, 011

2026-09-03 | **The frame is the exit sweep's own temporaries, not the slots — and the sitting convened to share slots vetoed itself twice** (`docs/panel/106`, soundness lane, compiler-engineer and ffi-pragmatist, **provisional — author ratification pending**). Convened by M-corpus-depth because the recursion ceiling of `examples/interpreter/` is 104 under `--sanitize`, 191 at `-O0` and 349 at `-O2` — measured by binary search, all three stopping at exit 134 with the guard naming the function — which is why the milestone's 500-deep expression is at 60 and that program's own script guard at 24. **Both seats refused the proposal, on different halves.** The ffi-pragmatist refused it as unsound at the C boundary, with compiled programs rather than reasoning: a `str` lent to SQLite with a null destructor answers **`length: 0` at exit 0** when its slot is shared, and **`--sanitize` hides it** because the read happens inside an uninstrumented system library; a buffer whose address `setvbuf` retains becomes **SIGSEGV exit 139**, with panel 104's stack guard correctly silent because it is not a stack fault. Sizes, alignments, by-value group records, `i32[4]`, `partial` and the ABI were all checked and all unaffected. The compiler-engineer refused it as **vacuous**: slots are 9.6% of declared frame bytes and 98.2% of slot bytes are refcounted, and `ir/own.hero:257-262` reads every swept slot at every returning block, so no two of them are ever disjoint — merging a pair anyway is a **heap-use-after-free at exit 134** under ASan, and making them mergeable means the liveness-directed sweep panel 021 R3 refused, quoted in the module the proposal edits. What is left after the exclusions is **0.05%** for ~200 lines including the compiler's first CFG fixpoint. **What the same seat then found is the answer**: `own.hero:259` mints a load temporary per swept slot per returning block, every one address-taken so clang can never promote it, and they are **58.5% of all declared frame bytes**. Removing them, measured by rewriting the emitted C and rebuilding: interpreter frames **639,792 → 305,008 at `-O0` (−52.3%)** and **349,776 → 130,304 at `-O2` (−62.7%)**, `h_runexpr_combined` **112,704 → 17,744**, the nesting ceiling **191 → 311** and **347 → 581**, the compiler's own frames **−28.3%**, the seed **846,804 → 786,062 lines**; output byte-identical, ASan and UBSan clean, zero warnings under the full flag set, and a compiler built from the rewritten C **emits byte-identical C** (30,371 pairs across 2,907 functions, fixpoint preserved). And clang cannot do it: at `-O2` it recovers 45.3% by register promotion but coalesces nothing address-taken at top scope — the witness compiles to 192 bytes at top scope and **96 at block scope, at both levels**. **The resolution is the robust one and not the conservative one, by author instruction given mid-sitting** (*use always the most robust and safe solution, never the more economical and compromise one*): the conservative resolution was *do nothing*, since both seats vetoed. Adopted: slot sharing does not land in any form, not even gated; `decref_place` lands as M-corpus-depth's next step; the store's `old` temporary (9.1% more) is filed rather than done, because it genuinely needs a temporary; the interpreter's two low limits are re-derived from the ceilings measured after the repair; and panel 021 R3 stands untouched | CLAUDE.md §4, §12; design.md §1.7, §1.11, §1.12, §4.19 | 106, 021, 104

2026-09-03 | **M-corpus-depth opens ahead of M-isolated-threads, and its first two steps repair the instrument before any program is measured by it** (author instruction the same evening — *"anticipare quei due passi subito, prima di M-isolated-threads"*, and *"cancello su ogni ramo + punteggio pieno sui tag"* for the CI question; **two milestones are open at once, in two sessions**, and the chain table says so at rows 33 and 34 rather than hiding one). **Step 1**: `heroes mutate` compiles a module below a nested program's root under a stand-in name beside that root's `main.hero`, with the module's own text, so its `use` paths are read from the root exactly as the compiler reads them (panel 099 R1) — `cli/mutate.hero::checked_name`, `mutate/score.hero::Program.checked_as` — while `heroes check` on the leaf alone still refuses, R5's rule untouched. The alternative, checking the whole program with the module's text substituted, was not taken: it would change what a mutant of a flat program measures and multiply the cost by the program's size. **Step 2**: the gate on every leg — `suite_surface` rows `mutate examples --operator typo-digit` and `mutate examples/shapes`, exit 0, `verb_probes` 40 → 42 — and the full score printed and never asserted on the tag-and-dispatch CI run (panel 011: a per-operator rate is the measurement). **Measured with the fixed compiler, one run over all 78 files: 679.26 s, 10,878 mutants, 10,260 killed (96%) strict, 8,872 (83%) permissive, 479 survivors** — the per-directory sum of step 0 was exact (`shapes/` adds 167 mutants, 161 and 90 killed), and the *under 680 s* prediction held by 0.74 s for a falsified reason: the cost is the mutants, not the invocations. **The survivors were read, and nine are a finding**: `_ = f()?` → `_ = f()` compiles, because a discard accepts a `T?` — the one place a `?` can be forgotten without a type error — filed in `DECIDE.md` with three shapes and (a), refuse it, recommended; the 18 `shadow` survivors are `_ = call` lines repeated, legal by `spec:128-129`; the rest are `swap-args` and `positional-named`, the price of a language without distinct types for same-typed things, and `typo-code`/`typo-digit`, a different string or number being a different valid program. Seed regenerated and fixpoint held (840,151 → 840,806 lines); own tests 553, net 1374/0, net's own 93 | CLAUDE.md §1, §4, §9, §12; design.md §1.1, Part 11; spec:97-98, 128-129 | 099, 011, 102

2026-09-03 | **The want for a standard library becomes two milestones: small packages organised as Go's tree, and the web framework that composes them** (`M-core-packages`, chain row 36, and `M-web-framework`, row 37, both **author instruction**, out of a reasoning session the same evening — `docs/reasoning/005-packages-in-place-of-a-standard-library.md`). The author's words: *"vorrei avere tutti gli strumenti necessari a costruire un web framework alla Rails o Django o anche più sottile, tipo Go, Echo o FastAPI"*, then, when one toolkit was proposed, *"mi immagino più pacchetti che si combinano e poi quello web che li usa tutti; il modello è Go come organizzazione"*. **What the record already said, read forward before anything was proposed**: §1.11 refuses a standard library permanently (`design.md:443-445`) and panel 097 condition 5 keeps `selfhost/library_source.hero` shut; `DESIGN-LOG:178` and the ROADMAP's M-package-manager entry route *"a standard library that wraps C"* to distributable bindings; Part 9 says of the web that JSON *"is a library you would write yourself, like everything else"*; and a package written in Heroes alone is ruled on by nothing — no Part 6 row, no Part 7 item, deliberately (`DESIGN-LOG:178`). **Three decisions**: after M-package-manager, because `heroes fetch` is what makes a package a thing you distribute; Go's tree as the organisation, which `use net/http` already spells since M-package-layout (`spec:10-13`); Go's and Echo's level, everything explicit, because the Rails and Django shape rests on Part 6's own rows (`design.md:2382-2404`, `:2395`). **The sitting sits at the opening, not now** (M-interpolation-verdict's precedent), with six questions — the §1.11 boundary for a pure-Heroes package; where packages live (measured: a module that `use`s a sibling package compiles only from the program's root, so `heroes fetch` places a tree under the root); the byte buffer's three routes; `net` as a runtime part; Part 7 item 10 widened to *width or sign*; and, added by the author the same night, **conditional compilation** — five shapes surveyed (C's `#if`; Nim's `when`, D's `version`, Odin's `when`, Swift's `#if`; Rust's `cfg`; Go's one file per platform; nothing, Ada and Oberon, which is where Heroes stands), against panel 049's veto of the platform axis, panel 097's arm in the runtime, panel 039's unplaced comptime, §4.15, and the CI leg that `cmp`s the seed against what the compiler emits on every platform. **Measured before scheduling, on the Mac and in the Linux image**: a single-threaded HTTP server written in Heroes answers `curl` today with no language change — `socket`/`bind`/`listen`/`accept` as `extern`s, the address as `record SockAddr tag sockaddr` with the port bytes computed in Heroes, `fdopen`+`fgets` in, `write` out, about 100 lines, three record lines different on Linux; `hero_str_from_bytes` brings a `ptr` of known length back as a `str` (10 MB in 0.32 s); SHA-256 and HMAC over `package "libcrypto"` run on both; a libcurl client with no callback fetched from that server. The callback boundary — `selfhost/check/ffi.hero:45` refuses `.function_ty` while `selfhost/emit/ctype.hero:375-380` already emits a function value as a C function pointer — is needed by **no package on the list** and by threads, so it is filed under M-isolated-threads with the loopback server as that sitting's program. **What blocks a distributable `net`**: `sockaddr` differs between Darwin and glibc (`ffi_field_type` on the other platform, both ways) and Windows is winsock; `clockid_t` is `u32` on Darwin and `i32` on glibc. **Two compiler defects found by the probes and filed, not fixed**: `xs @ xs.push(c.to_u8().must())` copies the array on every push — 100,000 pushes in 14.78 s against 0.00 s with the value bound first, `selfhost/ir/place_store.hero:100` — and `@value: i32` against a `const void *` pointee is `internal error` at exit 2 on both platforms, panel 103's pointee assertion writing `sizeof(const void)`. **The corpus, measured**: 78 files, 10,356 lines, 35 programs and the gallery, 6 files with an `extern`, zero socket calls, zero thread creation; `split_lines` copied in 8 files, `is_space` 8, `trimmed` 7, `index_of` 4, and `tests/harness/strings.hero` a further `trimmed`. **Records moved**: rows 36–37 inserted and 38–44 renumbered, `ROWS` 46 → 48, five items in `SCHEDULED.md`, the ROADMAP's `design.md:637` corrected to `:772`, and panel 056's deliverable D — the three return conditions in the M-package-manager entry — discharged nineteen days after its ratification; deliverable B, the two stale inventories in design.md and Part 7 item 4's *"no aliases"* wait for the sitting, because they are Parts 1–11 | design.md §1.11, §4.15, §4.19, Part 6, Part 7 items 4, 10, 13, Part 9 · CLAUDE.md §1, §4, §9, §12, §14 · spec:10-13 | — (013, 028, 032, 035, 036, 039, 049, 056, 057, 087, 092, 094, 097, 099, 103 are what it inherits)

2026-09-03 | **The chain is re-read for what it lacked, seven milestones enter, and the language's rulings move ahead of the code that would otherwise be written in their absence** (**author instruction**, late the same evening — *"facciamo un grosso ragionamento sulla roadmap e anche sulle cose in decide e scheduled … valutiamo se ci sono degli step che non abbiamo considerato finora … poi le riordiniamo tutte in un ordine molto logico"* — out of a reasoning session, `docs/reasoning/006-what-the-chain-lacked.md`; every item was put to the author with a recommendation, and all seven were accepted, one against it). **Three faults in the order, each measured**: M-interpolation-verdict, the language's only scheduled ruling, sat at row 41 behind the packages, the framework, QBE and both tools, its ordering paragraph weighing the books as the expensive consumer and not the tens of thousands of lines of Heroes the packages are; Part 7 item 1, closures, *"v1.5, immediately after the first running program"* (2026-08-04), had no row while M-web-framework's entry planned middleware around its absence; and M-package-manager stood ahead of M-core-packages, whose own step 13 was `heroes fetch`. **What enters**: `M-closures-verdict` (35); `M-reflection-verdict` (37 — the record names reflection three times, `docs/panel/018:99`, `docs/reasoning/003:89`, `docs/reasoning/005:194`, and rules on it nowhere, while `examples/json/` is 671 lines and 20 renderers are hand-written across `examples/`); `M-deferral-ledger` (38 — seven Part 7 items and two watch-list entries with no date); `M-doc-generator` (42, **accepted against the recommendation** — `heroes doc` fails §10's stopping rule today as `outline` and `explain` did at measurement 003 rider 3, and its entry makes that rule the first question of its opening); `M-panic-location` (43 — `hero_panic` prints `panic: <msg>` and aborts with no line, `runtime/parts/panic.c:21-25`, while the emitted C already carries `#line`); `M-thesis-harness` (44 — metrics 2 and 4 have never run, and the instrument is a Heroes program over the packages' HTTP client; **the author decides that a small seed of tasks is written at its opening, the bulk staying at M-guide-book as decided 2026-08-24**); `M-install-channels` (50 — a tap, a manifest, a flake and an image, all from the seed's one clang line because `heroes` without clang compiles nothing, plus a version scheme: `heroes --version` is `heroes 0.0.1` and the tags are names). **What moves**: M-interpolation-verdict 41 → 36, beside the other rulings and before M-core-packages; M-core-packages ahead of M-package-manager (39, 40), the `fetch` step going with the manager, and the manager's sitting owing an answer on pinning without a per-project file; M-qbe-backend 38 → 47, after the tools, since every ruling above it may move the IR; the incremental frontend finds its home in M-lsp-server, closing the `SCHEDULED.md` question *which milestone does it*. Rows 44 → 51 in § The chain, `ROWS` 48 → 55, the sections under § The milestones put into the table's order (word frequency identical before and after) and their preamble's count of closed ones corrected from seven to eight, and one stale citation corrected: the QBE cells said *Part 7.14* where item 14 is visibility and QBE is 15 (`design.md:2562`, `:2579`). **Considered and not entered, each with the rule that refused it**: a REPL (Part 6's interpreter shapes; `heroes run` is the loop), a web playground (Part 2 and Part 9: wasm breaks the FFI premise), cross-compilation (the three platforms are measured on real machines by rule), typed debugging (Part 2's stated non-goal, not urgent), prebuilt binaries (a decoy without clang). Spec headroom that evening **378** (3718 of 4096), and no panel: scheduling changes no language | CLAUDE.md §1, §4, §9, §10, §12, §14; design.md §1.1, §1.12, §3.5, Part 2, Part 6 (`:2395`, `:2400`), Part 7 items 1, 5–9, 11, 12, 14–16, Part 8 warts 1, 15, 16, Part 9, Part 11 | — (013, 016, 018, 030, 033, 035, 039 are what it inherits)

2026-09-03 | **Three decisions the first full three-leg CI run put on the table, answered the same evening and applied in the same session** (author, `/decide` answers `1c` + the push half, `2a`, `3a`). **(1) The mutation score runs on ONE leg, gated on tags plus an opt-in `mutation` input, and the compiler's own tests move to EVERY event.** The argument is a measurement and not a preference: in run 33781889835 the score cost 1294 s on Linux and 1908 s on Windows against 586 s and 1474 s for the whole net, taking the legs to 35m37s and 59m49s — and the three tables it produces are **identical byte for byte** (Linux, Windows, and the author's Mac at 666.37 s: 78 programs, 10878 mutants, 96% strict, 83% permissive, `diff` clean on both pairs), so Windows was spending 31m48s to reproduce Linux's number. The condition keeps one door open deliberately — a dispatch narrowed to a single leg still scores on it, which is how platform-independence gets re-measured the day somebody doubts it — while a tag cannot take that door. The other half is the mirror: `The compiler's own tests` had a tag-and-dispatch `if:` and cost 115 s against a 10m54s push leg, so **553** tests were invisible to a push. **(2) The Darwin leg moves `macos-14` → `macos-26`**, because it was reporting *nothing*: it died in 16 s at `heroes doctor`, step 7 of 18, and the ten steps after it were skipped. Not a compiler defect — that image carries Apple clang 15.0.0 against the floor of 18 set the same morning, `macos-15` carries 17.0.0 and is still under, and `macos-26` carries 21.0.0, the version the author's own Mac reports; CI's macOS toolchain becomes the one the project is developed on. Still owed: a run, since the label has never been executed for this repository. **(3) `_ =` on a fallible value becomes a compile error** — the verdict is the author's, the work is not done, and it does not land on this answer alone because both halves are language (a sentence at `spec:97-98`, a diagnostic class), which is CLAUDE.md §4's panel path; filed as `M-discard-refusal` in `SCHEDULED.md` | **What the third decision's cost is, measured exactly before it was filed, and the method is the finding**: removing `_ = ` from a line makes the compiler refuse it *and name the type*, so a copy of the tree with every discard stripped is a census. `selfhost/` holds **237** discard lines, all 237 reported by one `heroes check`, of which **54 discard a fallible value** (28 `()?`, 24 `i64?`, 2 `Exit?`); `tests/harness/` 14 and **6**; `examples/` 18 and **1**. **61 lines stop compiling on the day it lands, the compiler's own source included.** And they are not one shape: `_ = process.mkdir_all(dir)` is a deliberate best-effort discard that wants the `.is_err()` escape hatch the decision already names, while `_ = write_file(path: watched, …)` inside `cli/deps.hero`'s own test is a hole that would let the test pass for the wrong reason. **A second interaction the sitting cannot skip**: `error[discarded_value]` offers `_ = …` as its **certain** fix (panel 071), so on the day `_ =` refuses a fallible value that fix writes a program that does not compile — and CLAUDE.md §8's CI rule asserts an applied certain fix compiles. Measured on this Mac: a bare `risky(3)` line is exit 1 with `fix (certain): discard it explicitly` today. No `.fixed` golden carries the shape yet (6 exist, none with `_ = `), so nothing is red — which is exactly why it needs a case rather than a repair | CLAUDE.md §4, §8, §9, §14; design.md §1.1, §1.12, Part 11; spec:97-98 | 071 (the certain fix), 103 (the clang floor), 011 (the score is a measurement, never an assertion)

2026-09-03 | **M-corpus-depth steps 3-5: four programs of the nine, and the fifth one found two compiler defects by being written** (corpus material, CLAUDE.md §9; no design change, so no sitting). **Step 3** — `examples/fannkuch/` and `examples/binarytrees/`, each against a published oracle fetched that day with its sha256 and quoted with its URL: the benchmarks game's `228` / `Pfannkuchen(7) = 16` at N=7, and OEIS A000375's eight terms from a source that never heard of the benchmark, which agree; the binary-trees `main.expected` **is** the downloaded file. The checksum's dependence on the permutation ORDER was measured before a line was written — dictionary order gives **-502** where the published answer is 228, so a guessed generator would have printed a wrong number that looked right. **Step 4** — `examples/checksum/`, four modules against three documents (the CRC catalogue's `check=0xcbf43926`, RFC 1950's two sums, RFC 4648 § 10's seven vectors both ways), every number cross-checked against Python's zlib and base64 the same day, and its printed output `diff`-identical to the RFC's own table once the RFC's indent is removed. It is where `u8`/`u16`/`u32`/`u64` first appear in the corpus as TYPES rather than as conversions, because the bitwise operators are `i64` only and a 32-bit register therefore cannot exist. **Step 5** — `examples/interpreter/`, eleven modules in four directories, 57 tests: the rung between a program and the compiler, and the first program here whose shape is the compiler's own | **What the fifth program found, and neither was found by reading.** (1) `docs/defects/006`: a type parameter appearing only in the RETURN type is accepted by `heroes check` at exit 0, carried through the IR as `??`, and then either refused by the verifier (*returns a value of the wrong type*, exit 2, when the call is a `return`) or — as a `match` arm's value — **aborted on at exit 134 with `assert failed: false`**, naming no file, no line and no expression. (2) `docs/defects/007`, the more serious: **`heroes check` exits 139 in SILENCE on a legal program** nested 440 parentheses deep, while `heroes parse` on the same file names the fault (`panic: stack exhausted in grammarexpr.postfix`, exit 134) and `heroes lex` never fails at any depth. The guard is not absent — `down(1000000)` is caught at both `-O0` and `-O2` — so it is a coverage gap in one shape, and §1.12 says a Heroes program must not segfault. The cause of the low ceiling is measured rather than guessed: `--emit-c` shows **247 hoisted locals in one parser function's prologue** (§7 hoists every local), so a recursive frame is proportional to the whole body and not to the live set; ceilings for the new interpreter are **120 under `--sanitize`, 190 at `-O0`, 340 at `-O2`**. **The plan's 500-deep requirement is therefore NOT met and says so**: that case stands at 60, half the tightest measured limit, with all three ceilings in its comment — scaling a requirement down is the author's call and it is on the record rather than quietly absent. Four items filed with homes (the two defects, the hoisted-frame question as architecture, and the milestone's five remaining programs). Also repaired: `suite_corpus`'s `module_texts` read one directory and now reads the tree, which mattered the moment a program kept most of its modules two levels down | CLAUDE.md §1, §7, §9, §12; design.md §1.12; spec § Operators, § Types, § Failure | 104 (the guard), 082 R3 (*check accepts ⇒ build succeeds*), 011 (the corpus is measured, not asserted)

2026-09-03 | **`docs/defects/007` repaired the evening it was filed: the stack guard's second witness has a second shape, and the compiler no longer dies in silence on a legal program** (runtime only, `runtime/parts/stack.c`; no surface, no diagnostic class, no spec token, so no sitting — panel 104's own resolution as amended by a measurement). The cause was read under `lldb` before anything was changed, and it was not the mechanism the defect note hypothesised: clang's prologue calls `___chkstk_darwin` for a frame larger than a page, and that probe walks the frame's pages while `sp` still stands where the caller left it, so `si_addr` was in the guard and `sp` was 6,472 bytes above it (frame 8,576, `grammarexpr.primary`). Panel 104's *"`sp` within 4 KiB of the low bound"* — written against a frame touched after `sp` moves — said no, and the fault went to `SIG_DFL` at 139. The witness now accepts a probe as well: `addr < sp && sp - addr < HERO_STACK_WINDOW`. The falsifier that sitting's engineer built (a wild store 8 KiB under the stack from a shallow frame) was re-run against both runtimes on the Mac and in the Linux container: 139 before and after, as it must be. The 440/500/2000-parenthesis files now say `panic: stack exhausted in grammarexpr.postfix` at 134; the fixture `tests/golden/surface-fixtures/wideframe/main.hero` (a 32 KiB frame, probed) is 139-silent on the old runtime and named on the new one at `-O0` and `-O2`, two rows in `suite_surface`. Linux never had the defect — no probe is emitted there — measured, not inferred; Windows is untouched by construction and unmeasured, the box being off. The recursion CEILING is unchanged and stays the hoisted-frame architecture item in `docs/work/SCHEDULED.md`; the defect was the silence, not the number.

2026-09-03 | **A type parameter takes the type the context asks for** (`docs/panel/105`, full panel, five seats, **provisional — author ratification pending**; the defect is `docs/defects/006`, found the same day by M-corpus-depth step 5). The spec has said *"always inferred"* since the beginning and never said from WHERE; design.md §4.12 said *"look at argument types, deduce `T`"*, and a type parameter that appears only in a function's result — `function wanted<A>(what: str) -> A?` — passed `check` at exit 0 and died in `build` at exit 134 with `assert failed: false`, because `substitute` turned the unbound parameter into the silent error id and nothing reported it. Measured by the pragmatist over 21 adjacent shapes: 0 refused by `check`, 3 caught by the verifier, 12 aborting at build, **3 building clean and panicking at run time**. Two rulings were on the table and the seats split 3–1–1 for A: infer from the context (the annotation, the declared result reaching a `return` or an arm, a non-generic callee's parameter), refuse a call with no context AT the call, refuse a parameter that appears nowhere in the signature at the declaration. The ergonomist's argument carried it — with no constraints a result-only parameter can only produce a failure, an empty container or a hole, so whatever type the context picks the value is the same, and the spec's own `fail`, `[]`, `{}` and `nullptr` are already context-typed; the engineer measured that B's premise (*"no call could ever bind it"*) is false because `check(expected)` binds it at every ⇐ position, so a Part 6 refusal would have no falsifier; the historian found that every language that refuses ships an outlet Heroes lacks by design, and that Go's objection is to the NESTED case this checker already refuses. The warden objected on cost (+32 tokens against a burden no program in the repository meets: 52 of 52 signatures bind from arguments) and found the shape nobody had named — a parameter appearing NOWHERE compiles today. The author's instruction the same evening, robust over cheap, decided the tie. Two repairs land under any ruling: the verifier's `no_error_survives` (mono and owned, under *no holes*), and the emitter's bare asserts given words. Spec 3718 → 3750 when the sentence lands, paid by six registered predictions.

2026-09-03 | **Panel 105 ratified** (author instruction, *"Va bene, la ratifico subito"*, minutes after the synthesis, after asking whether A was the most robust and the safest and being answered yes on both, with the reasons). The verdict section of `docs/panel/105` reads *Ratified*; the item leaves `docs/work/DECIDE.md` for the record; the resolution is unchanged and lands in the same session, complete.

2026-09-03 | **Panel 105 lands, complete, the evening it sat: a type parameter the arguments leave unbound takes the type the context asks for, and defect 006 is repaired** (`docs/panel/105` § Resolution, every condition of the approving seats; `docs/work/DONE.md` carries the defect with its repair). The checker hands the expectation it already holds past the one line that dropped it (`check()`'s `.call` arm, and the `.method` arm that never had it); `bind_missing` fills what the arguments left and never reports; a call with no context is `cannot_infer` at the call, with a `guess` fix that names the two ways to give it context and never a type argument; a letter that appears nowhere in the signature is refused once, at its letter. The verifier refuses any unresolved type before the emitter (`no_error_survives`, under *no holes* — measured over 134 hole-free corpus files by the engineer, 0 false refusals), and the emitter's two bare asserts say what they saw. Instances carry the call that made them, and the gate reports a boundary type bound from context at that call, whichever order the file declares things in. Measured with the rebuilt compiler: the reproducer prints `7` and `true`; of 21 adjacent shapes the four with no context are exit 1 at the call and the other seventeen build and run, three of which used to panic at run time; two instances for one `empty_of` at two types, `??` 0, `_3f` 0. Spec **3718 → 3750**, ledger row 50, paid by six registered predictions. **Two of the engineer's landing predictions are already falsified and recorded as such in the panel file**: the diff adds more than 100 non-test lines (the comments are long, and the rule is that a comment teaches the invariant) and it touches `ir/` and `emit/` beyond the two repairs — one new field on the IR's `Function`, `instantiated_at`, for the pragmatist's condition 4, and its 32 constructor sites. The engineer's veto condition (touching `mono.apply`, `phases` or `ctype`'s switch for A itself) is not met.

2026-09-03 | **`read_file` on bytes that are not text says `not_text`, the code `validated` already gives a `cstr` in the same state** (`selfhost/library_source.hero`, `runtime/hero_os.h`'s comment, the golden `fixedbugs-read-file-on-bytes-that-are-not-text` re-pinned; the decision panel 087 left to the author, 2026-08-19, closed the robust way under the author's instruction of the evening — robust over cheap, *"non transigo"* — while the seven defects were being attacked, defect 002's one open door). For two weeks the Tier-2 wrapper collapsed `HERO_OS_NOT_TEXT` into `read_failed`, and *could not read* was false of a file that read perfectly well: the five-line reproducer over the repository's one tracked JPEG said `fail: read_failed`. Three program states have three codes now — `file_not_found`, `read_failed`, `not_text` — and a walk over a directory the program did not create can branch on the third by name instead of guessing from the second. No spec token: the spec types `read_file` as `str?` and names no code for it, and `not_text` is not a new string in the language. The third door of panel 087 — a *"is this pointer safe"* predicate — stays categorically unbindable and is not touched.

2026-09-04 | **The repository's working text becomes English, and the doctrine that let Italian in is retired in the three places that stated it** (author instruction, *"I do not want anything in Italian … I speak Italian to you because it is comfortable for me, but the texts are all in English, since this is a project that will become open source"*; `CLAUDE.md` §11, `tests/harness/suite_records.hero`'s new `records/english`). §11's first line has said *everything written is English* since it was written, and **measured this session, 241 Italian quotations sat in the tree, 19 of them inside §11's own section**. They were not careless: `docs/book/README.md` stated a doctrine in one line — *quoted speech is not an artifact* — `docs/reasoning/README.md` cited it by name for its own carve-out, and `docs/glossary/README.md` sent the Italian analogies to `/where`. Every rule in the contract is anchored to the author's words because a quotation binds where a summary does not, and those words arrive in Italian, so the loophole was load-bearing rather than incidental. **The doctrine is retired in all three files**: quoted speech is an artifact like any other, and an instruction is written down for what it MEANT, in English. **55 Italian spans were translated across ten living files** — `CLAUDE.md` 19, `docs/ROADMAP.md` 16, `site/README.md` 9 (four of them behind `&ldquo;` entities, which is why an earlier count said five), `site/CLAUDE.md` 3, and one or two each in `docs/work/DEFECTS.md`, `docs/work/SCHEDULED.md`, `docs/reasoning/README.md`, `docs/environment/linux/LINUX-MACHINE.md`, and **the compiler's own source**, `selfhost/grammar_expr.hero` and `selfhost/cli/clang_floor.hero`, whose module docs quoted the author in Italian against §11's plainest clause (comments-only diffs, 0 non-comment lines changed, so the emitted C is byte-identical). **The dated records are NOT translated** and that is the ruling, not an omission: §14 forbids rewriting a record, about 210 of the 241 live in them, and **40 commit bodies of 971 carry the same words and cannot be edited at all**, so a translated record would disagree with the commit that made it. **§11's "one declared exception" was measured false**: the Italian edition of the site was born 2026-08-18, seven days after that sentence, and had been **46 tracked files for 17 days** — the exceptions are a class now, *a translation that is itself a deliverable*, holding the two bilingual books and the site's edition, with three files named as Italian data rather than Italian prose (`site/CLAUDE.md`'s spelling rule, `SiteNav.astro`'s labels, and `/where`'s vocabulary, which moved out of `SKILL.md` into `.claude/skills/where/italian-vocabulary.md` so the skill itself reads English). **The rule now has an instrument**: `records/english` reports any line of six or more words where four of them, and a quarter of them, are Italian function words, with inline code stripped first so `site/CLAUDE.md`'s row of backticked Italian spellings is ruled ON rather than read AS. Thresholds measured before they were chosen: over every tracked `.md`, `.hero`, `.c`, `.h`, `.html`, `.astro` and `.txt` file outside the records, **23 files fire and all 23 are declared**, zero false positives. **It was written against the suite's `living` list first, and that was the defect worth the whole session**: `living` is `run_all`'s walk minus the records *and minus `quoting`*, and `quoting` drops `CLAUDE.md`, `docs/ROADMAP.md` and `docs/work/DONE.md` because they quote the retired milestone spelling — so the check would have run green over **the two files holding 35 of the 55 spans it exists for**. It takes the whole walk now and prunes it itself, against `RECORD`, `GENERATED`, a new `APPENDED` (`DONE.md` is the record with no record directory) and a new `QUOTES_THE_ITALIAN` naming this one module, which must hold Italian in order to refuse it — `QUOTES_THE_OLD_SPELLING`'s argument, in the same file, for the same reason. Made to fire on purpose twice (§9): on `docs/work/DEFECTS.md`, and then on `CLAUDE.md` itself to prove the repair, both `records: 9 passed, 1 failed` with the file and the line named, both files restored byte-identical. Green after: the third suite 96 tests 14.7 s, `records` 10 checks, `layout`, `lines` and `units` clean.

2026-09-04 | **The English check was measured in one direction only, and in the other one it caught 10 of 42** (`tests/harness/suite_records.hero`, `records/english`, second pass the same day). The check landed at `ed88c02a` with a number beside it — 23 files fire, all 23 declared, zero false positives — and that number is still true and was never the question. **What was never measured is what it MISSES**, and the answer arrived an hour later from a peer session: a paragraph landed in §4 at `c3a5fdf` quoting the author in Italian, the check read `CLAUDE.md` on the very next run, and it passed. Replaying the 42 Italian spans this repository actually held, out of `d7c2661b`: the per-line rule found **10**. It sees an Italian *page*, whose every line is Italian; it cannot see the shape this repository is made of, six Italian words quoted inside an English sentence — `*"convoca il panel e finisci"*` is five words on a line of thirty, and no per-line density will ever reach it. **The repair is a second rule that reads the quotation instead of the line**: every `*"…"*` span, three words or more, two of them and a fifth of them Italian, which catches **41 of the 42**. It needs a wider word list than the line rule — `i`, `e`, `di`, `per` are what make a five-word quotation legible — and that list **cannot** be used on a line: measured the same session, with it `function step(@m: Machine, opcode: i64, a: i64, b: i64) -> i64?` reads as Italian, along with 46 other lines of real code, so there are two lists and the comment says which question each answers. **The floor is stated rather than left to be found**: `*"continua senza chiedermelo"*` is three words carrying one function word and is not distinguishable from English by counting, so an Italian instruction of three words or fewer still walks past, and the test asserts that bound so nobody rediscovers it as a surprise. Two real spans were caught by the new rule the moment it ran: the peer's §4 quotation, and *"usa questa immagine duotonata nella pagina autore"* in `site/README.md` — **a span the morning's sweep had missed**, which is the second thing a check is for. Both translated. Green: 97 tests, `records` 10 checks, and the peer's paragraph put back on purpose to watch it fail.

2026-09-04 | **The exit sweep stops minting a temporary, and half of every frame goes with it** (M-corpus-depth, building panel 106's adopted resolution; `docs/measurements/015-the-sweeps-own-temporaries.md`). `selfhost/ir.hero` gains `decref_slot`, an `OpKind` case carrying a **slot** and not a Place — so it cannot name `p.x` and release half a value, and the verifier does not have to check what cannot be spelled. `ir/own.hero` mints one per swept slot where it used to emit a load into a fresh temporary followed by a decref of it; `emit/inst.hero` emits `release(&slot)` through a `reference_named` that the value door now also goes through, so the two ways to reach a refcount call cannot learn a type apart; `ir/uses.hero::slots_of` reports it, which is the one line that keeps a swept slot counted as read rather than gaining `__attribute__((unused))`. **The file that had asserted the old rule for a year is the file that withdraws it in writing**: `ir.hero`'s own comment said releasing a slot *is* a load followed by a decref, *"which keeps one form instead of two and makes the dominance check cover it free"* — both halves true, and the price was 58.5% of all declared frame bytes. **Measured before and after on this Mac, `-fstack-usage` over the emitted C**: the interpreter's frames **639,792 → 294,128 bytes (−54.0%)**, its worst function **112,704 → 17,296**, one nesting level of its grammar **36,896 → 20,608**; the compiler's own **6,563,376 → 4,227,600 (−35.6%)**; the seed **846,804 → 719,955 lines (−15.0%)**; the emitted interpreter **69,052 → 54,610**. **The ceilings, binary-searched, all stopping with the guard naming the function**: nesting **104 → 166** under `--sanitize`, **191 → 314** at `-O0`, **349 → 585** at `-O2`; script recursion **40 → 76**, **50 → 107**, **80 → 239**. **The fixpoint holds byte for byte.** 560 own tests, 97 harness tests, 135 of 189 emission traces re-blessed, and **twelve `tests/golden/ir/` plus three `tests/golden/emit/` cases updated BY HAND** because §9 forbids `UPDATE_GOLDEN` in exactly those two directories — every hunk classified before anything was written, and every one is the same pair replaced by one release naming the slot. Three `suite_layout` ceilings raised with their reason, the pinned table count 16 → 17. **What the milestone asked for and did NOT get**: a 500-deep expression. `--sanitize` tops out at 166 and the corpus runs all three configurations, so the interpreter's case moves 60 → **83** and its evaluator's guard 24 → **38**, both re-derived from the measurement. A coordinator prediction is scored **FALSE** on `docs/panel/106`: it said the case would at least triple, taking the `-O0` number and stating it about a case the tightest configuration governs | CLAUDE.md §7, §9, §12; design.md §1.7, §1.12, Part 5 | 106, 021, 104

2026-09-04 | **The last of M-corpus-depth's nine programs lands, and it found three things in the compiler on its first run** (`examples/ledger/`; the milestone's third family, FFI at program scale). Four modules, **eighteen** SQLite functions and seven of its constants bound, one prepared statement reused across ten inserts inside a transaction, three column types read through a `variant` that cannot forget SQLite's fifth, and a join. It closes the gap the milestone was scheduled for outright: measured before it, the six `extern` directories in this corpus were 65 to 192 lines, every one single-module, and not one had a `variant`, a nested container, a generic or an `@`-parameter structure — FFI and *a real program* were disjoint sets. **How it is checked is the part worth copying**: there is no published answer for a book somebody invented, so every total is computed twice by routes that share no line — SQLite's own `sum … group by` against the same total in Heroes over the same rows — and the program prints `agree` only if they match cent for cent. Money is an integer number of cents and never an `f64`; the one float in it, SQLite's `avg`, is compared with a tolerance that says so. **Three findings.** (1) **An `extern` is not callable across a module boundary** — only the module carrying the header has it `#include`d, so a caller elsewhere would be a call nothing verified; the module wraps every one of them, which also keeps `cstr`, raw result codes and one-based column indices out of the rest of the program. (2) **A `ptr` may not be the element of a `T?`** (`unsupported[pointer_element]`), so a handle is a one-field record — the diagnostic's own note, and it improves the signature besides, since `Db` and `Statement` are two types the compiler will not let a caller confuse where two `ptr`s are one. (3) That record emitted C clang warned about, which is **defect 009**, repaired the same hour: `hash_body` declared `const T *v = elem;` unconditionally, and a record whose every field is unhashable — a `ptr` has no structural hash — left it with nothing reading it, against `suite_warnings`'s zero-warning rule over the whole corpus. **The first repair for 009 was wrong and the instrument caught it in one blessing**: it asked whether `descriptors.hash_call` answers for a field's type, a premise about what the loop would emit, and a FIXED ARRAY field answers no there because the loop handles it element by element — so `VrStereoConfig`, whose only field is a `Matrix[2]`, lost its declaration while its lines went on saying `&v->projection[0]`, and its golden was `error: use of undeclared identifier 'v'`. CLAUDE.md §11's own shape, inside the repair for a defect. The second collects the lines it is about to write and declares `v` only if one says `&v->`, which is a fact about the lines rather than a premise about the loop. Seed re-baselined 719,955 → **720,377** lines with the fixpoint holding byte for byte; 560 own tests; measured on the Mac and in the Linux image, and on the Windows box it SKIPS with `error[ffi_missing_header]`, which is the skip rule working rather than a gap | CLAUDE.md §9, §11; design.md §1.11, §4.19 | 106

2026-09-04 | **The corpus can be fed, and the fourteenth mutation operator gives panel 102's refusal a number** (M-corpus-depth, its two remaining instrument items; `docs/measurements/016-the-fourteenth-operator.md`). **The operator**: `local-takes-a-module` rewrites a binding's name to a name a `use` in the same file bound — the mistake the sitting's refusal exists for, and one no habit from another language warns anybody about. It waited on purpose, because adding an operator moves the DENOMINATOR of metric 3, so the discontinuity is spent once against a corpus wide enough for the number to be worth having. Measured over 107 files under 44 directories in 22:09: **1,700 mutants, 1,700 killed (100%) strict, 1,636 (96%) permissive** — and the 64-mutant gap is what panel 102 was owed, since `--permissive` drops the thesis rules and those 64 are killed by that rule and by nothing else. Sixty-four programs that would compile, run and be wrong. Whole corpus: **19,411 mutants, 18,246 killed (95%), 15,788 (83%)**, and **the rates did not move** from measurement 014's 96%/83% over a corpus nine programs smaller — which is what a corpus is supposed to do to a rate; one that moved would say the earlier number measured the corpus rather than the language. `typo-digit` is non-zero for the first time, 0% → 4%, and both kills are in the oracle-checked programs that arrived since. `swap-args` stays the weakest live operator at 80% strict. **The stdin**: nothing in `examples/` read its standard input, measured while the milestone was planned, and every other language's example suite has something that does. The scheduling item's gate was *build it only if the price is a file handle*, and it is — one `open` plus one `dup2` on POSIX, one `CreateFileA` on the Windows arm where the inherited handle already carried a dead-handle probe. So `hero_run_go` takes an `in_path` (empty means inherit, exactly as for the other two streams), **`HERO_RUNTIME_ABI` moves 19 → 20** through the two-phase ritual, `shell.hero` gains `run_fed` beside `run` — a sibling rather than a parameter, because `run` has 28 call sites and one of them ever feeds a program — and `suite_corpus.hero` reads `main.stdin` beside `main.args` at all three levels. The program is `examples/tally/`, checked against `wc`: `3 9 44` and a longest line of 19, identical; the corpus's cheapest FFI program on purpose, since `stdio.h` is the one header no platform lacks, so it cannot skip where four of the six others do. Two language facts it made visible: nothing here reads a stream, so even `getchar` comes from C (§1.11), and nothing turns a byte value into a character, so it carries a table and says so. Fixpoint holds byte for byte at ABI 20; 560 own tests, the net **1474 passed 0 failed**, 97 harness tests; six `tests/golden/emit/` cases updated after verifying every changed line was the stamp and nothing else | CLAUDE.md §7, §9; design.md §1.11, Part 11 | 102, 011

2026-09-04 | **Panel 106 is ratified, and the ratification exposes what the recursion ceiling actually is** (author instruction, *"ratify and push"*, given after the repair was built, measured and shipped). The resolution stands as the sitting wrote it: slot sharing refused permanently and in every form, `decref_slot` the shape this IR has, the store's `old` temporary (9.1% more of declared bytes) and the liveness question filed, panel 021 R3 untouched. What the yes settles is that the repair was RIGHT rather than whether to do it — frames **−54.0%** on `examples/interpreter/` and **−35.6%** on the compiler itself, the nesting ceiling **191 → 314** at `-O0`, the seed **846,804 → 721,238** lines, the fixpoint byte for byte, 560 own tests and **1,474** net checks green. **And it exposed a question the sitting could not have answered, because it was never about the frames.** M-corpus-depth's 500-deep requirement is unmet at 83, and the reason is not the frames panel 106 halved: it is that `selfhost/cli/flags.hero::link_flags` passes `-Wl,/STACK:67108864` — 64 MB, ratified by panel 058 in August — on **Windows and on no other platform**, so a program on macOS or Linux gets the operating system's answer, 8 MB here. Measured the same day, the same emitted C linked twice on this Mac: with the default, **314** levels at `-O0` and **166** under `--sanitize`; with the same 64 MB Windows already gets, **2,537** and **1,347**. So the deepest a Heroes program can call itself is a link-line decision this compiler makes once out of three times, half ruling and half default, and design.md §1.12 makes not crashing a goal — a promise that currently means something different on each platform. That is a language question rather than a repair, so it goes to `docs/work/DECIDE.md` with three shapes and (a) recommended: pass the same number everywhere | CLAUDE.md §4; design.md §1.12 | 106, 058

2026-09-04 | **The verification rule stops naming a suite and starts naming the reasoning that skips one** (author instruction, `CLAUDE.md` § Commands). The block above it had carried *there are THREE suites* since 2026-09-02, and the lesson that paragraph leaves is **the suite that was missed last time**. Two sessions working this tree the same afternoon each missed a different one, and neither was short of instruments: one rewrote prose in fifteen files, ran the third suite plus `records`, `layout`, `lines` and `units`, and skipped `canonical` on the reasoning that prose cannot break a formatter — `tests/harness/suite_records.hero` had one blank line the formatter removes, and design.md §4.15's instrument was itself non-canonical inside the harness; the other landed a large repair, ran the whole net, and that is what found the first failure **and** its own `tests/harness/suite_layout.hero` pin, `DECIDED` at 17 entries with the assert reading 16 (an addition is the direction that assert notices least loudly: a removal takes the table below the number, a raise leaves it alone). The third shape is an edit *after* a green run — `records` green fifteen minutes before an item left `docs/work/DECIDE.md`, the recap already written, and `records/verdicts` correctly red because `docs/panel/106-the-frame-is-the-sweeps-own-temporaries.md` still said its ratification was queued while nothing named it. So the rule is **the suite you did not expect to move, after the last edit rather than after the last interesting edit**, and it carries the corollary the third failure taught: a ticked item is a question standing in for the author, not a task, so a list reaching zero open items is a symptom to check rather than an achievement to report — the work being built is not the work being ratified. *"My change cannot have touched that"* is an inference, so §1 binds it: run it, or write it down as a guess.

2026-09-04 | **The number cannot be made uniform on any of these platforms; the abort can — panel 107 refuses all three mechanisms it was convened to choose between** (full panel, five judges, author instruction *"option (a), with a panel"*; `docs/panel/107-the-number-cannot-be-uniform-the-abort-can.md`). The question was whether `link_flags` should pass the same 64 MB everywhere instead of on Windows alone, and **two seats vetoed while a third vetoed one half of it**. **The Linux arm does not exist**: GNU ld answers `warning: -z stacksize=0x4000000 ignored`, lld answers `unknown -z value`, `readelf -l` shows `PT_GNU_STACK` `p_memsz` at **0x0 in both binaries**, and the measured depth is **388 with the flag and 388 without** — while `selfhost/cli/toolchain.hero:106-109` forwards a *successful* clang's stderr and `selfhost/cli/units.hero` re-links on every build, so that warning would print on every Linux build of every program forever, for zero bytes: `/INCREMENTAL:NO`'s own defect rebuilt deliberately ten lines from where it is documented. **Running `main` on a created thread — rustc's answer, and the compiler seat's own best alternative — breaks a program in this corpus**: `SDL_Init(VIDEO) failed: No available video device` on a created thread, window and exit 0 on the real main thread, so the FFI seat vetoed the compiler seat's condition 3 with a measurement, and the veto wins because a resolution that breaks `examples/sdl/` is not one. **Stating a depth in the spec would be false the day it landed**: the warden compiled a legal Heroes program whose frame is **263,488 bytes**, which reaches **248** levels at 64 MB and aborts at 300, and this compiler's own worst frame is **65,936** bytes — 1,016 frames, **1.6% from breaching its own spec** — while §12 makes the spec beat the compiler, so every such abort would become an unfixable compiler bug. The historian found the corroborating shape and it is unanimous across the industry: Go picks 1 GB, GHC 80% of RAM, rustc 17 MB on a spawned thread, Java 1–2 MB, and **no language specification states a number** — the JVMS refuses a size, Scheme states a property, and Python's number described the wrong stack and broke working code in 3.12. **What the sitting adopted is larger than what it was asked**, per CLAUDE.md §4's robust-not-conservative rule: the conservative resolution satisfied every veto by changing nothing and would have left the actual §1.12 defect standing, which both compiling seats found independently — `hero_stack_lo/hi` (`runtime/parts/stack.c:107-108`) are the **main thread's**, set once, so overflow inside SDL's audio callback is **exit 132 with an empty stderr** where the main thread says `panic: stack exhausted in deep.down` at 134, and the proposal would have widened that gap from **16:1 to 128:1** with the loud side being the side already safe. So the bounds and the alt-stack become **per thread**, and the uniformity a program is given is a named abort on every thread of every platform rather than a number. **The number cannot be uniform and that is measured, not conceded**: the flag is inert on Linux, Darwin's libpthread pins library threads at 524,288 bytes whatever is done, `ulimit -s unlimited` on Linux gives *less* than a finite value (exit 139 where 64 MB is exit 0), the flag **overrides the operator** under `ulimit -s 1024` and is granted **above the hard rlimit**, and the same 64 MB is 248 levels for one legal program and 2,537 for another. **Principle 0 has no ticket for the size**: `./heroes check selfhost/main.hero` over 178 modules is exit 0 at **896 KB**, so the deepest Heroes program in existence needs under 1 MB against POSIX's 8, and no Part 11 metric moves. The direction that does have a ticket is the **emitter** — `h_runexpr_joined` declares **344 locals** and occupies **21,888 bytes**, about 26 KB per nesting level, and 314 × 26 KB is the whole 8 MB, so the eightfold asymmetry is this compiler's temporaries rather than the operating system's stinginess, which is panel 106's direction with journal 032's next 9.1% already carrying its number. **Two corrections the sitting forced.** `flags.hero:74-78` said a large reserve *"costs nothing real"*: true for a program that stays inside its stack (max RSS 2,768,896 against 2,752,512, binary 525,312 both) and **false for the case the flag exists for**, where max RSS goes **9,814,016 → 68,534,272** — CLAUDE.md §11's expiring premise, corrected in the same commit with its number. And **the entry above this one credits the 64 MB to panel 058, which is wrong**: the provenance is `docs/work/DONE.md:961`, a `/decide` ratification of 2026-09-03 from M-argv-execution whose stated reason was that Windows' 1 MB kills the compiler on its own modules — a repair for one platform, so nobody had ever decided this question on any platform. The record is append-only (§14), so the correction stands here rather than there. A spec sentence stating the *property* is queued at **+16 to +23** against a headroom of **346** (spec re-measured this session: 3750 max, 3669 claude-legacy, spread 81) and the warden holds it **unpaid** — deferring it until a removal or an instrument exists is §1.6 working, and the ergonomist's finding is why it is worth paying for: of four guessing points a reader of the spec alone can hit, three fail loudly and **depth is the only silent one**, so a stated number would have readers sizing a guard from "1,000 calls" while their parser burns two frames a level and the guard never fires | CLAUDE.md §4, §7, §11, §12; design.md §1.6, §1.11, §1.12, §4.19 | 107, 106, 104, 102, 058

2026-09-04 | **Panel 107 is ratified, and what the yes settles is a refusal** (author instruction, *"ratify and push"*, given immediately after the synthesis was read). Three mechanisms are refused permanently and by name, each carrying the measurement that produced it rather than an argument: `-Wl,-z,stacksize=` is never shipped on Linux, `main` never runs on a created thread, and the spec never states a recursion depth. **A refusal is the rarer kind of ratification here and design.md Part 6's preamble holds it to a feature's burden of proof**, which is why each one has a stated return condition in the panel file — a later sitting that wants to reopen one has to falsify a number. **Authorised**: the stack guard's bounds and alt-stack become per thread, filed in `docs/work/SCHEDULED.md` with its cheap half and its unsolved half kept apart, so the yes says the direction is right without pretending the alt-stack-on-a-foreign-thread question is answered on any of the three platforms. **Unchanged, deliberately**: the stack size everywhere, Windows' `/STACK:67108864` included, and the 16 MB option argued from this compiler's own frames stays recorded and available as the author's choice rather than taken. **And the record keeps the fact that the recommendation was wrong**: the `/decide` item that convened the sitting recommended passing the flag everywhere, on the reasoning that the only thing keeping it from the other two platforms was that nobody had asked. Nobody asking was not the reason — two of three platforms cannot take it and the third takes it by overriding its own operator — and the recommendation was honest, made from a measurement of one machine. That is the sitting's cost stated as what it bought: finding this out before the flag shipped instead of after. `docs/work/DECIDE.md` reaches zero open items, which CLAUDE.md § Commands says is a symptom to check rather than an achievement to report; checked, and it is correct here, because the item WAS the ratification and the work it authorises is in `SCHEDULED.md` where work belongs | CLAUDE.md §4, §12; design.md §1.12, Part 6 | 107

2026-09-04 | **The unread cell is not the leak — panel 108 refuses the rule it was convened for, in both shapes, and names what the question was actually about** (full panel, five judges, no vetoes; convened from `/decide` on the author's question *"shall we put a rule in the compiler to avoid these leaks?"* after CI's Linux leg found `examples/ledger/` leaking 40 bytes per refused statement; `docs/panel/108-the-unread-cell-is-not-the-leak.md`). The proposal: a cell passed as `@` and never read by the program itself becomes `unused_binding`, `_ = x` the escape — `selfhost/resolve/writes.hero:72-79` counts the pass as a read (copy-in) and the rule would drop that read; panel 015 had ruled only the callee side (DESIGN-LOG:53, :56). **The two seats that counted both object**: a real prototype (+13 −2 lines, rebuilt, run over `selfhost/` + 44 programs + the harness) fires **54 times, 0 defects** — 44 in the compiler's own `test` blocks, the `parse_expr(@c, @a)`-then-assert idiom §4.8 exists for, 8 in functions threading a `memo` cache or a `seen` set; an independent scan of 458 `@`-passed cells says **55 (12.0%), 0 defects**. The compiler would need 48 `_ = x` lines to compile itself; `tests/golden/check/fixedbugs-at-marker-on-a-builtin.hero`, which records a real LLM mistake, loses four `certain`-fixable `marker_mismatch` lines for two `unused_binding` (§4.17 regression); the `extern`-only variant fires twice, both harmless, and has a measured UFCS hole (`"42".cstr().strtol(@end, 10)` escapes because the callee resolves after the arguments, `walk.hero:145`). **The two seats that approve measured other things and conceded the point in their own words**: the ergonomist wrote the leak's shape **3 of 3 times from the spec alone** (`spec:213` reads as a rule, and the word "free" does not occur in the document), and the FFI seat's argument says *"the leak was an ownership fact, `_ = error` compiles it clean"* — measured, `leak_escaped.hero` exit 0, and its prediction is that the Linux leg would still report 40 bytes leaked with `@error` plus `_ = error` reinstated while `unused_binding` stays silent. **The historian found the exact precedent and why it is not one**: GNAT `-gnatw.o` (since GCC 4.5, 2010) warns on an Ada `out`-mode actual never read afterwards and by its own text does not cover `in out`; Heroes' `@` is copy-in copy-out, and every single-mode language — Swift `inout` (`RK_Read|RK_Written` in its checker), Go `&x`, Zig — counts the pass as a read. **One message was refuted by measurement**: the compiler seat's condition wanted *"pass `nullptr` if the value is not wanted"*; the FFI seat ran thirteen out-parameter shapes on both platforms and `nullptr` is a fault or a crash in 5 of 9 — `stat(path, NULL)` is a silent wrong program on macOS and **a segfault, exit 139, on the Linux leg** — while `_ = x` is safe in all thirteen. **Resolution, provisional**: the rule is refused with its falsifier named per Part 6's preamble (a measured real defect among the 54, or an out-only `extern` parameter mode, after which the rule is GNAT's and right for that mode); the ownership fact goes to a §4.19 sitting of its own, for which `design.md:2120-2125` already reserves a keyword, with the historian's out-only mode recorded as its likely carrier; the instrument that catches the leak today is LeakSanitizer on the Linux leg, blind on Darwin arm64, so *an FFI program's pre-commit Linux run includes `--sanitize`* is proposed to the author as a one-flag sharpening of CLAUDE.md § Commands. Here the conservative resolution and the robust one coincide, and the file says so. **Found beside the question**: a compiler defect — `@value: i32` against C's `void *optval` emits `_Static_assert(sizeof(void) == …)`, clang refuses it, and the compiler says `internal error`, exit 2, where §7 owes exit 1 on the `.hero` line (`docs/work/DEFECTS.md`); the spec never shows a named `@` argument; `selfhost/emit/body.hero:141-143`'s premise that `_ = v` is the only way to reach a written-never-read slot is false and corrected. **And the `/decide` item that convened this recommended the rule**, on a count of the four sites that provoked it rather than the 458 it would judge — the same shape as panel 107's item the same day, and the reason both went to a sitting instead of a commit | CLAUDE.md §4, §7, §11, §12; design.md §1.2, §4.8, §4.17, §4.19, Part 6 | 108, 015, 107

2026-09-04 | **Panel 108 is ratified — the second refusal of the day, and the second time a sitting overturned the item that convened it** (author instruction, *"ratify 108"*, given while the synthesis commit's CI run was in flight). What the yes settles: a cell passed as `@` and never read is **not** `unused_binding`, in either the full or the `extern`-only shape, with the falsifier named per Part 6's preamble — a measured real defect among the 54 sites the rule fires on today (0 found by two independent seats), or an out-only `extern` parameter mode, after which the rule is GNAT `-gnatw.o`'s and correct for that mode. What it authorises: the §4.19 ownership sitting carries the author's word that it is the right question — an `extern` declaration stating what C hands the caller to free, the historian's out-only mode as its likely carrier, `design.md:2120-2125`'s reserved keyword as its budget line — and defect 010 (`void *` bound as a sized `@` pointee, `internal error` at exit 2 where §7 owes exit 1 on the `.hero` line) is the author's to see repaired. What it leaves to the author's own hand: the CLAUDE.md § Commands sharpening that an FFI program's pre-commit Linux run includes `--sanitize`, because LeakSanitizer exists only there and is what found the defect; the contract is amended by author instruction (§4), so the sentence was proposed with a recommendation and the author writes it. **The process fact worth keeping**: panels 107 and 108, one day, each convened by a `/decide` item whose recommendation was honest, measured, and wrong — on a sample of one machine (the stack flag works on this Mac) and of four sites (the four `@` cells that provoked the leak, against 458 the rule would judge). Neither error was catchable by a commit; both were caught by the sitting the item asked for. That is the cost of a sitting stated twice as what it bought | CLAUDE.md §4, §12; design.md §4.8, §4.19, Part 6 | 108, 107

2026-09-04 | **Defect 010 repaired the day it was found, as a class of two shapes and a cache that hid the first** (panel 108's ffi-pragmatist found it compiling `getsockopt`'s `void *optval` bound as `@value: i32`; `docs/work/DONE.md`). The pointee check (panel 103) asserts `sizeof(H) == sizeof(D)` and the sign of `(H)-1` against the header's own type; for `H = void` both are hard errors — `invalid operands to binary expression ('void' and 'int')` — and for an incomplete struct so is `sizeof`, so neither produced the `static assertion failed` line `emit/ffi.hero`'s readers know, and the compiler said `internal error … clang refused the generated C`, exit 2, where CLAUDE.md §7 owes exit 1 on the author's line. **Three C facts were measured before the shape was chosen**: `__builtin_choose_expr` does not skip the type-check of its unchosen branch, so no single assertion can be made void-safe; `__builtin_types_compatible_p(H, void)` in a `_Static_assert` fails as a proper assertion and ignores qualifiers, so `const void` is caught; and an incomplete type has no builtin that sizes it, so its line will always name the TYPE and never the parameter. The repair follows those facts: a **guard assertion** before every width line, carrying its own marker (`heroes-ffi-pointee-void`) and read by `pointee_void`; and, for the incomplete shape, `cli/pointee.hero` now hands `explain` what the header said each ask points at (`pointee_ask.Answered`), so `pointee_unsized` finds the parameter from the type clang names. Both land as `ffi_parameter_type` with the repair that is not another width — `ptr`, and no `@` — as a `guess` fix spanning from the `@`, which is found backwards over the spaces the grammar allows because `ast.Param` keeps no span for it and `@ n` compiles (measured). **The cache hid the repair from its own reproducer**: the pointee verdict was keyed by `t.fingerprint`, which is `VERSION`, a constant, so the repaired compiler replayed the old compiler's verdict on the old unit and answered exit 2 until `build/pointee-*` was cleared; the verdict is now named by the digest of the unit clang judged, so a compiler that writes the assertion differently is asked again and an unchanged one costs a `test -e`. Verified cold and warm on the reproducer, the two new `fixedbugs` goldens (`memcpy` for `void` — C89, every leg — and an opaque struct with its own header beside it), the old width golden and a correct `frexp`; fixpoint byte for byte, seed 721,238 → 725,213 lines. **The ceiling caught the threading**: `cli/produce.hero` went 297 → 307 against §11's 300 and came back to exactly 300 along two seams the file already had — the two explain sites that were the same eleven lines twice are one `blamed`, and two byte-identical failure-text helpers are one — rather than by a raised row or a trimmed comment. The language corrected the repair once: two `str` parameters at `verdict_path` were `needs_label` at both call sites | CLAUDE.md §1, §7, §11; design.md §4.17, §4.19 | 108, 103

2026-09-04 | **Panel 109 splits the ownership mark in two, and the line it cuts on is core versus sugar** (full panel, five judges, one veto; convened by author instruction on the §4.19 question panel 108 queued; `docs/panel/109-the-string-c-hands-you-and-the-handle-it-keeps.md`). The fact under the sitting was measured three ways: **a caller-owned C string can be freed or read today, never both** — `@error: cstr` against `char **` is `error[ffi_writable_parameter]` (panel 058), `@error: ptr` then `.validated()` is `type_mismatch`, and from the spec alone the ergonomist found no `cstr`→`ptr` route to `free(void *)` — so the `sqlite3_exec` leak that convened panel 108 was not carelessness but **a program the language cannot express**, and `fa01ab20`'s `nullptr` was the only legal one. **Adopted, provisional**: `owned <C function>` after a `cstr` result or a `@` parameter the header spells `char **`; the compiler frees it **by name** in a generated per-freer release (twelve real freers compile by name under all fourteen flags, three of seven are refused through a generic `void (*)(void*)` — `sqlite3_close`, `fclose`, `LocalFree` — and a cast is UB); hands it over as a `str?`; guards NULL itself (`fclose(NULL)` segfaults on glibc, nine of ten freers are no-ops); makes the `@` cell out-only (panel 108's unnamed mode, now with a reason); and **refuses the program's own call of the freer on an owned value** — the sentence the ergonomist, the warden, the FFI seat and Racket's `(deallocator)` each wrote independently, because without it a program that frees by hand and is then freed by the compiler double-releases at exit 0 in silence on both platforms (measured). A borrowed pointer marked owned is loud: 20 of 20 runs abort or trip ASan. The compiler seat hand-emitted the lowering and ran it in the Linux image under the sanitizers: **0 bytes where the control leaks 40, in twelve lines of C**; the same seat built the parse and both printers (+63 lines, fixpoint holds) and priced the half at ~270 lines through three files with no room — so `ir/lower.hero` (1511/1511) and `parse/decl.hero` (638/638) are **split first**, `DECIDED` rows lowered. Spec: the three facts floor at +49 (panel 108's registered ≤ +25 is FALSE); the complete wording Wt2 is +70, paid by the `spec:225-227` platform-commentary removal (−28) to **+42 net** against the ledger's +31, the eleven being the two clauses two seats made conditions; `spec:239` gains *"for that call"* (+3). **Refused, veto standing**: `ptr owned` as a counted value — a new `Ty` case touching 179 exhaustive arms in 49 files, five more ceilings, an ABI move or a descriptor reuse, i.e. checker and lowering and backend, §1.7's core — wanted by four seats (the FFI seat built `HeroOwned` against the runtime on both platforms and found Darwin's first leak detector for C handles in it, `panic: 1 heap blocks still live`) and unaffordable by the one whose mandate is cost; it returns with a compiled prototype giving shared release **without a new `Ty` case in ≤ 120 lines and no ABI move**, plus a corpus program that cannot release its handle explicitly — and the warden found the program to aim that at: `examples/ledger/main.hero` has five `?` between `prepared` and `finalized`, so a failed `bind` never finalizes and `sqlite3_close` answers 5 at exit 0, measured. Two hazards travel with the half: the emitter's declaration-order drop leaks 62,464 bytes with `sqlite3_close` and none with `sqlite3_close_v2`; and every precedent that refcounts an opaque handle leaves *C keeps it* to the programmer (historian). **Queued**: the third case — C keeping a lent buffer, panel 106's `SQLITE_STATIC` — whose C-side spelling (`constant SQLITE_TRANSIENT: ptr`) compiles today; a Heroes-side refusal needs a lifetime across two calls that a language without references cannot state. Precedent: GCC 11's `malloc(deallocator)` names the freer for return values only; GObject-Introspection's `(transfer full)` covers the out slot by type; nobody names the freer per slot on an out-parameter, so `@error: cstr owned sqlite3_free` is a composition and not a catch-up; .NET's hard-coded `CoTaskMemFree` is the recorded crash class this design fixes | CLAUDE.md §4, §12; design.md §1.2, §1.6, §1.7, §4.19, §4.20 | 109, 108, 058, 106

2026-09-04 | **Panel 109 is ratified, and it is the day's one sitting that adds rather than refuses** (author instruction, *"I ratify"*). Three sittings and three ratifications in one day: 107 refused a stack number that cannot be uniform on three platforms, 108 refused a rule that could not see the leak it was convened for, and **109 admits `owned <C function>` to the FFI because the correct program cannot be written without it** — a caller-owned C string is freeable or readable and never both, measured three ways, so §1.11's *everything comes from C* had a hole no program could route around. What the yes authorises, in order: **the two splits first** (`selfhost/ir/lower.hero` at 1511 of 1511 and `selfhost/parse/decl.hero` at 638 of 638, along seams that name a concern, their `DECIDED` rows lowered rather than raised), then the mark with all eight conditions, then `examples/ledger/db/sqlite.hero` returning to `@error: cstr owned sqlite3_free` with the Linux `--sanitize` leg green on it. **None of the eight is negotiable at implementation time** — each is a seat's condition, and the one four seats wrote independently (a call of the declared freer on an owned value is a compile error) is what turns a silent double release at exit 0 on both platforms into a diagnostic. **What the ratification does not do**: overturn the compiler-engineer's veto on the `ptr owned` half, which it was not asked to; that half returns only through its own measured conditions (shared release with no new `Ty` case, ≤ 120 lines, no ABI move, plus a corpus program that cannot release its handle explicitly), and `examples/ledger/main.hero`'s five error paths that never finalize are the program those conditions point at. The third case — C keeping a lent buffer — stays queued, its C-side spelling (`constant SQLITE_TRANSIENT: ptr`) compiling today. **The process fact of the day**: 107 and 108 were convened by `/decide` items whose recommendations the sittings overturned, on samples of one machine and of four sites; 109 was convened by the author on a question a sitting had queued, and it is the one that changes the language | CLAUDE.md §4; design.md §1.0, §1.7, §1.11, §4.19 | 109, 108, 107

2026-09-04 | **Every item in `docs/work/SCHEDULED.md` names a milestone that has a chain row and is not closed, and twelve of twenty-nine did not** (author instruction, *"fix SCHEDULED too, so that every item is attached to a roadmap step that is still to be done; if you cannot find one, create it"*; the rule is in that file's preamble, what it cost is in `docs/ROADMAP.md` § Who scheduled what). Measured against the chain before a word was written: **nine items named a waiting condition instead of a milestone** — *"the next panel that touches emission"*, *"trigger, not a milestone"*, *"the milestone that makes the stack guard per-thread"* — and **three named a milestone that had closed**, two of them M-corpus-depth, closed that morning, and one M-ffi-ladder, closed 2026-08-12 and filed at the day before out of panel 109's own ratified sitting. **A closed home and no home read identically in this list**: the item looks scheduled either way, which is why the rule binds the first field rather than the intention. **Four rows entered the chain, 51 → 55.** `M-declared-freer` — panel 109's `owned` mark; a new id because §14 never reopens one, and deliberately **not** `M-foreign-ownership`, since `ptr owned` stands refused under a veto and an id may claim nothing a later milestone can falsify (the runner-up is in `suite_records.hero`'s `REFUSED` beside `M-language`). `M-thread-stacks` — panel 107's per-thread guard **together with** the Windows `-Wl,/STACK:67108864` divergence that hid defect 008, because how much stack there is and what is said when it runs out are one subject; plural because 107 refused a uniform number on six measurements. `M-discard-refusal` — an alias row since 2026-09-03 and no chain row, which its own item said in its first line. `M-check-completeness` — *check accepts ⇒ build succeeds*, scheduled with **no warrant** because Principle 0 holds it and its two items had been parked on a `grep` since 2026-08-16. **Three homes were found rather than created**, which is the cheaper half of the instruction: §4.19's include-order sentence rides M-declared-freer's sitting, the macro-only probe case is already M-core-packages step 0's own witness (c), and the hoisted-frame question goes to M-qbe-backend, whose warrant is that a second backend turns a slot's lifetime from the C emitter's convention into a decision somebody must make twice. **The two M-corpus-depth items went to the record**: nine programs in, both instrument items closed, `LEAST` at 44, `suite_corpus` walking with `list_tree` — and the half that did **not** close is back as an item of its own, measured that day: `examples/gallery/` holds 12 files and no `main.hero`, `00-first.hero` is named 31 times under `tests/`, `09-holes.hero` 5, `01-points.hero` once, and **the other nine are named nowhere**, while 148 of the site's 160 code slices come out of that directory. **And the list stopped carrying row numbers**: twelve items cited one, §14 puts the order in the chain table and nowhere else, and four new rows would have falsified every one of them in the same commit that added them. **And the rule ships with its executor**, because CLAUDE.md §3 says twice what happens to one without: `records/homes` reads the first field of every open item, looks the name up in § The chain and fails on three shapes — no milestone, no row, a closed row. **The naive reader would have passed the very item it was written for**: § Where we are opens `| **Last closed** | **M-corpus-depth**, 2026-09-04 |`, which looks like a chain row and carries no `done` cell, so the check requires the first cell to be a row NUMBER and a test asserts both summary rows are refused. Falsified rather than assumed — three bad items appended to the real list, all three reported, the file restored byte-identical. `records` is 11 checks; the harness's own tests are 100. `records/names` fired twice during the work, once on the refused runner-up written down in prose and once inside that falsification | a list whose items point at finished work reads as scheduled and blocks nothing, which is `DECIDE.md`'s 2026-08-26 failure one department over — and there the symptom was 138 ticked items, here it is a home that closed under the item | CLAUDE.md §3, §14 | 107, 109

2026-09-04 | **The panel watch list is retired, and what it was carrying was six live questions, three homeless conditions and a ratified rule the compiler never got** (author instruction, *"What is OPEN-QUESTIONS.md? Perhaps its items should be moved into a panel to be held. Remove that file"*; the audit is in `docs/work/DONE.md`, that entry). `docs/panel/OPEN-QUESTIONS.md` was born with panel 000 on 2026-08-03 and last touched 2026-08-16: a list of sittings awaiting the author, empty since 2026-08-03, over a watch list of the conditions the judges left behind. **It goes because it was a fifth list in a second notation** — CLAUDE.md §3's *"invisible to every count in this project"* — which no skill opened, and the proof is measured rather than argued: the 2026-09-03 session that gave homes to the chain's gaps read **two of its nineteen items**. Verified item by item before anything moved: **ten closed**, including its own header paragraph, false in all three of its claims, and panel 010, whose rule was ratified 2026-08-04 and **is not in the compiler** — defect 011, found by the probe this session ran, four shapes at exit 0 where `design.md:1287` promises a compile error. **Three had homes** and their citations now point at the sittings that wrote them; two of those citations were stale and are corrected. **Six were filed**: four questions to `docs/work/DECIDE.md`, which had reached zero the same day, and two to `SCHEDULED.md` at M-thesis-harness — panel 007's two contradictory predictions, unscorable until metric 2 runs, and the compiles-but-wrong-output bucket panel 009's ergonomist made a condition of its own vote, which exists nowhere in the instrument | a watch list nobody is obliged to open is write-only, and every rule this project keeps is now read by a skill or by the net | §1.6, §4.6, §4.8, §4.9, Part 11 | 003, 007, 009, 010, 013, 039, 071

2026-09-04 | **The reasoning notes retire into the record: thirty-one of their thirty-two hand-offs had already landed, and the two blocks that had not were being cited by line** (author instruction, *"I want to remove it and turn its content into DONE, or DECIDE plus a panel where needed, or even into ROADMAP milestones where needed. All this work goes in the direction of simplifying the docs directory"*; seven entries in `docs/work/DONE.md`, one per note, plus the retirement). `docs/reasoning/` was adopted at `:142` and `:246` for the remainder nothing else held — *"the path between the artifacts, and what the session left open"* — and the audit is what retires it: the parts had gone where they were always meant to go, and the path is the git history. **CLAUDE.md §14 now says a reasoning session leaves no note of its own**, and names the seven destinations that absorbed one. **Two blocks moved into sittings rather than into the record**, because living documents cited them by line: the seven-capability table to `docs/panel/039` § Appended, which `design.md`'s own Part 6 paragraph made necessary by arguing that an answer reachable only from a session note is uncitable, and the nine-job `Cargo.toml` inventory to `docs/panel/056` § Appended — that second one is what keeps `:308`'s ruling true, since the note was legalised **because** it held what the panel file did not, so deleting it without moving the table would have reversed the decision. **The exception at `:308` lapses with the directory.** Two things only these notes were carrying are now in the documents that needed them: the chain's first fault was a measurement of **51,788 lines in 178 modules** where the ROADMAP said *"tens of thousands"* and cited the note for the figure, and out-of-memory — the one of sixteen candidates with no row and no recorded refusal — is on the record as *nothing owed*, `runtime/parts/alloc.c:77` making a failed `malloc` a clean panic. `docs/` goes from ten directories to nine | a directory whose job was the path between artifacts is a fifth place to look, and §3 says the lists are four | §4.10, §4.12, §4.19, Part 6, Part 7.13 | 039, 056, 109

2026-09-05 | **Panel 110: two of four spec silences bought, one refused by two vetoes, one measured out of existence — and the sitting corrected three of the brief's own claims** (full panel, five seats, sat 2026-09-04, ratified 2026-09-05 by the standing delegation quoted on the page; `docs/panel/110-places-not-roots-and-the-repair-that-printed-a-third-answer.md`). Spec **3750 → 3799, +49**, headroom **297** — 26 tokens below the tabled proposal, and one token inside the spec-warden's own registered bound. **What landed**: a `()` row (*nothing: what a function with no `->` returns; `ok()` is the `()?`*) plus, at `spec:100`, *which a `()` line refuses: it stands alone*; and the permissive half of the `constant` body rule, subject the **written body**. The row's warrant is not the missing row: the unit type appears in the document **once**, `ok(())` is refused because `()` is not an expression, and **`return ok()` argument-less is the only way to build a `()?`** while `spec:146` shows only `ok(v)` — so a reader with the document alone could not write a function of the shape `spec:191` displays, and `ok()` appears **19 times in `selfhost/` and 62 across 587 `.hero` files**. **What did not land**: *"`T??` is a compile error"*, vetoed twice and false as tabled — `m: {str: i64?}` is accepted, `v = m["a"]` then `v.must().must()` prints 1, a **generic** signature names the type and nests without bound, and the §1.11 built-in `find` over `[i64?]` produces it, while `spec:157` promises `V?` unqualified; docketed at M-check-completeness, which `docs/ROADMAP.md:363` recorded as the only scheduled row **with no warrant** and which now has one. And *"refuse a multi-line parameter list"* did not land because **the premise was false**: all three shapes `check` and `run` at exit 0 and `heroes fmt` joins them, measured three times independently, so the resolution is accepted input with non-canonical output. **Three of the brief's five claims were wrong about the compiler and every one was caught by a seat** — CLAUDE.md §1's inference-as-measurement in three fresh costumes, and the reason the full lane was right. The declined payment is on the record: **−19** by deleting `spec:176`'s quadratic-concatenation cost model, refused because §15's *never slow the compiler* is a standing instruction in capitals and the spec is that fact's only carrier, a concat loop drawing no diagnostic at all.

2026-09-05 | **Defect 011 repaired, and the rule compares PLACES rather than roots because a root test refuses two working programs** (`docs/panel/110`; `selfhost/resolve/writes.hero`, `selfhost/resolve/walk.hero`). `shift(a: @n, b: @n)` had given `check` 0, `run` 0 and printed **10** since 2026-08-04, on a form `design.md:1287` names as a compile error — the wrong-answer-at-exit-0 class this language exists to delete, hidden by two accidents: design.md's own examples were in a spelling `misplaced_mutable_marker` now refuses, and the unlabelled call fires `needs_label` first, so the alias question is reached only once both labels are written. `writes.refuse_aliased_places` is called once from `arguments`, the single funnel for all four call shapes, and needs no types because every legal `@` root is a `.local`. **design.md:1298's *"comparing roots is a complete alias test"* is corrected**: `shift(a: @p.x, b: @p.y)` and `shift(a: @xs[0], b: @xs[1])` reach disjoint memory, print `1 10`, and are clean under `--sanitize` **with the value shared** — measured, because the first unshare takes the refcount to 1 and the second moves nothing — so the walk stops at the first decidably different step (a field name, or an index in plain decimal digits) and treats a variable index, an undecoded base and a prefix as overlapping. `f(a: @xs[i], b: @xs[j])` is still refused, which is the over-rejection `design.md:1299` always named. **And the repair ships as a `guess`, not the `certain` design.md promised for a month**: the engineer built it and it prints **1** — a third answer beside the program's 10 and the reference reading's 11 — while CLAUDE.md §9 asks an applied `certain` fix only to compile, so `--apply` would have rewritten a 10-program into a 1-program with CI green. **Defect 012 with it**: `function f(_: ())` reached clang as `void h0_`, exit 2, and the FIELD position of that same hole was closed 2026-08-12 twelve lines away — `unsupported[unit_parameter]` now sits beside `unit_field` and `unit_element`, and `_` is the one spelling that got through, since a named `()` parameter is already `unused_binding` or `bound_unit`. `emit/gate.hero`'s pinned ceiling was at **341 against a measured 341**, which is what forced the count to be read at all.

2026-09-05 | **Part 7.13's two v1 invariants stop being prose and become `tests/harness/suite_runtime.hero`, and one of them was broken** (M-isolated-threads step 0; no panel — the shape is `DESIGN-LOG:422`'s, where the `ORDER:` mark stopped being a grep, *"the convention's shape is design.md's own sentence, obeyed rather than amended"*). The refcount boundary held: **0** `inline` in `runtime/heroes_runtime.h`, **6** `;`-terminated refcount prototypes. The single allocation point did not: `runtime/parts/array.c:203` grew `hero_eq_queue` with **`realloc`**, a second allocation site, which §4.20 (`design.md:2210`) and Part 7.13 (`:2582`) both forbid because a per-thread heap needs one place to change. **The promised grep could not have found it** — `alloc.c` said *"checkable with one grep"* and the sentence names `malloc` and `free` — so the suite reads the whole allocation family and reads code rather than text, `parts/run.c:484` having the English words *"the first check is free ("* in a comment. Repaired with `hero_grow_kept`, a third shape in `alloc.c` and deliberately not a pair, weighed by neither counter: the leak gate would report a false leak on a buffer kept on purpose, and `hero_live_scratch` means borrowed-and-not-returned, which it is not; §4.20's normative length guard added, which the `realloc` never carried. Measured over 300 nested arrays (queue 64->512): correct at `-O0`, `-O2` and `--sanitize`, exit 0, leak gate clean. **And the open half of `docs/work/DONE.md:157` is answered no**: the two rules stay in design.md and are NOT copied into CLAUDE.md §7, whose own flag-list paragraph records what a second home costs. | §4.20, Part 7.13 | —

2026-09-05 | **Panel 030 R6's cfront rider is answered, and the answer is yes: the C11 backend can express Part 7.13's concurrency model, measured rather than argued** (M-isolated-threads step 1; `docs/measurements/017-the-thread-the-emitter-could-already-carry.md`). The rider has been owed since 2026-08-03 — *"before M8c the record states whether the C11 backend can express the intended concurrency model at all (stack switching in the runtime, or a CPS/state-machine transform) … if the answer is 'not yet known', the deferral is a bet and is logged as one"*. It is not a bet. Stack switching and CPS are what green threads and coroutines need, and Part 7.13 refuses both by name (`design.md:2566-2573`); an OS thread is a C function call. Measured: pthreads binds, links and runs from Heroes at exit 0; `function worker(arg: ptr) -> ptr` emits `void * h_main_worker(void * h0_arg)`, which **is** `void *(*)(void *)`, and passing it straight to `pthread_create` with **no cast and no trampoline** compiles under `-Wall -Werror` and prints from the other thread at exit 0. So the scheduling item's *"the emitter owes one generated trampoline per spawned function type"* is false for this shape, and what is missing is a **permission**: the compiler refuses to declare what it already knows how to print (`selfhost/emit/ctype.hero:379`, whose own comment says the two are the same eight bytes). **What the model does cost is the runtime**: four threads allocating at once give `hero_runtime_check_leaks()` 2586, 1836 and 3141 live blocks in three identical runs, exit 134, a leak that is not one, and ThreadSanitizer names the entire race as `hero_live_blocks` at `alloc.c:113` — **inside the single allocation point**, which is exactly what §4.20 said the invariant would buy, six hours after step 0 made it true and gave it an instrument. No race in the drop queue: panel 070's ffi-pragmatist required `_Thread_local` when it was written. | §4.20, Part 7.13 | 030 R6, 070

2026-09-05 | **The cheap repair to the thread race is priced, and on its own it is a regression: `_Thread_local` counters trade a wrong answer for a blind one** (M-isolated-threads step 2; `docs/measurements/017` § 5). Two words on `runtime/parts/alloc.c:61` and `:64`. **Cost: nothing measurable** — two copies of the tree differing only in those words, each built from the seed and run twice, give control 35.90/34.49 s against 35.75/34.92 s, so the arms differ by +0.43 s warm and −0.15 s cold while the same arm varies by 1.41 s between its own runs. **Effect: the whole measured race goes** — the four-thread probe's 2586/1836/3141 phantom blocks become `the leak gate is happy`, exit 0, three of three, and ThreadSanitizer reports nothing at all. **And the trap, measured rather than reasoned**: a worker thread that allocates a `str` and never decrefs it is `panic: 1 heap blocks still live at exit`, exit 134, under today's shared counters, and **exit 0 in silence** under the thread-local ones, because `hero_runtime_check_leaks()` runs on the main thread and reads one counter. ASan has no leak detector on Darwin arm64 (panel 021), so that silence is total. The two words are therefore not a repair on their own: what goes with them — a per-thread exit check, an atomic, or the per-thread heap that makes the question disappear — is the sitting's, and design.md §4.20's single point is what keeps it one edit in one file. | §4.20, Part 7.13, §1.12 | 021, 030 R6

2026-09-05 | **Panel 111 refuses the proposal it was convened to adopt, and M-isolated-threads is re-scoped around what the two vetoes measured** (`docs/panel/111-the-emitter-could-already-carry-it-the-runtime-could-not.md`; provisional, ratification queued). **The backend was never the problem**: a Heroes function emits `void * f(void *)`, which IS `void *(*)(void *)`, and goes to `pthread_create` with no cast, no thunk, exit 0 — so panel 030 R6's cfront rider is answered yes and the deferral was not a bet. **The runtime was the problem, and two seats built the programs**: a shared `str` across 32 threads is `heap-use-after-free` or double-free in **9 of 10** ASan runs with the refcount drifting to 6290-9785 instead of 1; `a == b` on nested arrays is exit 139 with an empty stderr and a `heap-buffer-overflow` in `hero_eq_push`; copy-on-write double-frees through `cow.c`'s `if (a->refcount == 1)`, **which atomics do not fix**; and the stack guard is main-thread-only, so a worker overflow is exit 132 in silence. Four classes, each from one line of ordinary Heroes. **The permission is cheap (+32/-1, one file) and insufficient**: it binds 3 of 8 real callbacks, because §4.19's own sentence was right that *"what the boundary lacks is `c_int` and `const`, not a marker"* — `qsort` does not bind at all against `const void *`, while `emit/ctype.hero:376` promises in writing that it does — and it **silently switches off the extern's whole parameter probe**, so `qsort`'s other three parameters stop being checked against `stdlib.h`. **Adopted**: the milestone delivers Part 7.13's isolation or nothing (R2); the permission leaves it for an id of its own (R4); the thread-local counter plan does not land in any form (R5) — its mechanism reads **0** inside a `pthread_key_t` destructor on Darwin arm64 and it named 2 of 18 shared mutable objects; the refcount half of design.md's own v1 invariant is open work (R6); and `design.md:2536`'s *"no aliasing means no data race is expressible"* is corrected, being true of Heroes values and false of the refcount (R7). Five brief premises were false and every one was caught by a seat. | §1.12, §4.19, Part 7.13 | 111

2026-09-05 | **Panel 111 ratified in full, and `M-c-callbacks` is placed at chain row 34, ahead of the milestone it was split out of** (author, the same day: *"and I also ratify R1 to R8"*, and *"can we put M-c-callbacks in right away? I would not wait too long to do it"*). R1 through R8 stand as adopted, the refusal of the proposal the sitting was convened for included. **The one half a panel may not default was answered in the same breath**: `docs/work/SCHEDULED.md` puts a new id's position with the author, and this is that placing — the chain renumbered **34..55 to 35..56**, and two `SCHEDULED.md` items that carried a row number lost it, which is CLAUDE.md §14's own reason for keeping the order in one table and nowhere else. **The order taken is callbacks first, isolation second**: the permission is unblocked and measured, while the isolation is architectural and wants the C boundary settled and the refcount safe before it starts — and R8 binds them anyway, since the FFI refusal is today the only thing between a Heroes program and `hero_str_incref`'s race, so the permission does not land before the counter is atomic. M-isolated-threads stays open at 35 with its new scope: Part 7.13's isolation, or nothing. | §1.11, §1.12, §4.19, Part 7.13 | 111

2026-09-05 | **The thread guard is emitted into the callback, not installed in the runtime, and three placements were built and raced to find that out** (`docs/measurements/018`; panel 111 R9, approved by the author separately from the R1-R8 ratification). R8 said the permission must not land before the refcount is atomic; R9 keeps the rule and replaces the mechanism, because atomics repair class 1 and leave classes 2, 3 and 4 open while the handshake makes all four **unreachable**. **Measured on `heroes test selfhost/main.hero`, four separate tree copies identical but for `runtime/`, each built from the same seed and run twice**: control 34.36/35.21 s; the guard at every str/array/map through a `_Thread_local` **37.19/37.00 s, +6.6%**; the same through `pthread_self()` **42.04/40.68 s, +19%**; at the allocator alone 35.67/34.65 s, **inside the noise**. **The +19% is a coordinator prediction that was wrong by a factor of three**: the brief said `pthread_self()` on arm64 is a register read and a `_Thread_local` access can cost a call, and the opposite is true — building both is the only reason it is known. **And the answer was in none of the four**: the guard belongs at the entry of the EMITTED callback, because a foreign thread can arrive only through an address Heroes handed to C, and the compiler knows exactly which functions those are. A program that passes no callback — the compiler and all 45 corpus programs — pays nothing, since the check is not emitted into its binary at all. Four of panel 111's five witnesses then stop with a message naming what they touched, against `heap-use-after-free` in ten ASan runs of ten; the fifth, a recursive function that allocates nothing, still dies at exit 132 in silence and is M-thread-stacks' half by name. | §1.12, §4.19, §4.20 | 111 R9

2026-09-05 | **Panel 111 R4 lands: a function type crosses the FFI as a parameter, and three defects came out that no seat had predicted** (M-c-callbacks step 0). **The permission is one line in the checker** — `crosses_the_boundary` answers yes for `.function_ty` — because C spells a function pointer everywhere it spells a type, so the vocabulary question could never have been the one that says where a callback may stand. The positions are refused separately and there are **three**, not the two R4 named: an `extern`'s **result** (`signal` is the header every reader meets first), an **`@` out-parameter** (`void (**)(void)` is the same case wearing C's other direction), and an **`extern constant`**, which no seat named and which reaches the same danger through the one door that does not go through a signature. The argument for all three is one sentence and it is the permission's own mirror: an address passed OUT is one this compiler emitted, so C calls a body this compiler type-checked; an address handed BACK is a body nothing here has seen. **Measured end to end**: `atexit` binds and C calls it back at exit 0, `pthread_create` binds, spawns and joins with no cast and no trampoline. **Three defects, and every one was found by running rather than by reading.** (1) `emit/extern_probe.hero`'s `c_type_of` had no `.function_ty` arm, and one unspellable parameter withholds the whole list, so binding a callback would have switched the header check off for **every other parameter of that extern**, in silence — `DrawText`'s defect with a new cause, and R4 predicted this one. (2) A per-module TU never joined an `extern`'s parameter types to its typedef reach, because an extern is in neither `defined` nor `needed`: `unknown type name 'h_0fn_294870dd'`, exit 2 on a correct binding. (3) The probe was emitted in the prelude, **before** the typedef section, against `emit.hero`'s own written invariant *"types before anything that can mention one"*; both emitters now write it after `typeorder.definitions`, and the six blessed emissions moved by exactly that block — verified as the **same multiset of lines and the same line count**, so nothing was added or dropped. Gaps 2 and 3 were latent from the day the probe existed and could not fire while every boundary type spelled itself without a table. **`qsort` still does not bind**, and the compiler said in writing that it does: `int (*)(const void *, const void *)` has no Heroes spelling, and R4's `const` question is scheduled with the wart as step 0's provisional default. **R4's new diagnostic class is NOT opened**, provisionally and queued: measured, a wrong callback signature is already `ffi_parameter_type` at exit 1 on the `.hero` line, because clang's text carries panel 071's own ` to parameter of ` needle — so the ffi-pragmatist's *"a `qsort` binding exits 2"* is **falsified**, and what was really wrong was the note, which offered the struct repair under a function pointer | §4.19, §4.17, §1.11, §1.12 | 111 R4, 071, 062

2026-09-05 | **Panel 111 R9 lands with it, in one commit, because the permission is the door** (M-c-callbacks step 0). R9 says the guard cannot land before the permission (the functions it guards are the ones the permission admits) and the permission must not land before the guard (it is the door), so this is one step by the resolution's own words. **Where the guard goes**: `hero_thread_guard("<module>.<function>")` at the entry of every function whose address this program takes, in a program that declares an `extern` with a function-typed parameter — an over-approximation in the safe direction, since the emitter does not chase values through a local or a record field, and `func_ref` is where every address leaves. A program that hands C no function emits **zero** guards, measured, which is the compiler and all 45 corpus programs. **How the runtime knows**: a `_Thread_local bool`, zero on every thread the OS creates and claimed once from `hero_args_set` — panel 104's stack-guard placement, taken deliberately so the emitted `main` and the ABI stamp are untouched by a program that needs no guard. `_Thread_local` is not new platform surface: `runtime/parts/{array,drop}.c` already use it and CI compiles those on three legs (grep, run today). **Measured on panel 111's own first witness**, a `str` touched from 16 threads C made: **ten ASan runs of ten stop with the function's name at exit 134**, against `heap-use-after-free` or a double free in 9 of 10 before. The corruption is not repaired, it is **unreachable**. **HERO_RUNTIME_ABI 20 → 21** because a declaration was added, which is what forced the seed regeneration in this commit — and the stamp did exactly its job on the way, refusing the old seed against the new runtime. Fixpoint verified: the compiler rebuilt from the new seed by clang alone re-emits it byte for byte. **What is still open and is named rather than hidden**: a Heroes function that allocates nothing and only recurses never reaches a guarded point and still dies at exit 132 with an empty stderr on a foreign thread's small stack — `M-thread-stacks`' half, panel 107's, not this one's | §1.12, §4.20, Part 7.13 | 111 R9, 104, 107

2026-09-05 | **The seed-freshness check moves from tags to every push, and what moved it is a number that had expired in silence** (author decision, `/decide`, the recommended option taken). `.github/workflows/ci.yml`'s *The seed is what today's source emits* loses its `github.ref_type == 'tag'` gate and keeps `runner.os == 'Linux'`. **The gate was never about risk, it was about arithmetic**: `seed/README.md` priced the step at **15m41s**, measured 2026-08-19 when `measure` and `mutate` joined the port, and that is what put it on tags. Measured twice on this Mac 2026-09-05, the same command is **21.5 s** — the old figure predates the compiler getting fast, and it went on funding a decision it no longer funds while `main` sat red for a day with a seed two repairs behind, noticed by a golden case that happened to exist rather than by any instrument. Three places in `seed/README.md` carried the stale price and all three are corrected, **the dated one by appending the new measurement rather than rewriting it** (§14: a record is not rewritten). **Why not the net**, which is where the `/decide` item's own wording put the question: the net is run mid-change, and between an edit to `selfhost/` and its commit the seed is *deliberately* stale, so the net would be red all day — and an alarm that is always on is one people learn to ignore. CI asks after the commit, which is the only moment the question has a true answer. This is CLAUDE.md §3's own story once more: `seed/README.md` says in as many words that *"the rule of thumb needs no instrument to apply"*, and a rule with no executor is not performed | §1, §14 | 087 | seed/README.md · .github/workflows/ci.yml

2026-09-05 | **`ffi_callback_type` opens by author decision, over the coordinator's recommendation, and the measurement that argued against it is kept as the finding** (M-c-callbacks step 0, panel 111 R4). R4 owes the class *"so a wrong callback signature is the compiler's message and not clang's internal error at exit 2"*, and its ffi-pragmatist seat predicted a `qsort` binding would **exit 2** because no reader classifies an argument-position function-pointer mismatch. **Both were already false when the sitting sat**: clang writes `incompatible function pointer types passing 'X' to parameter of type 'Y'`, which carries panel 071's own ` to parameter of ` needle, so the message landed on the author's line at exit 1 before any code was minted. The seat's prediction is **falsified** and is scored so at this milestone's close. The coordinator recommended not opening the class, resting on `ffi_errors.hero`'s own written rule that *"a new diagnostic CLASS is a panel path which a second name for one question would be"*; **the author ruled the other way and the reason is the one the rule does not cover**: two mistakes can want the same repair sentence and **different searches**, and a reader chasing a callback should not have to sift every width and sign mismatch in the language out of `ffi_parameter_type`. **What landed**: the class in `emit/ffi_narrowed.hero`, returned before the width machinery because `what` and `consequence` are written for a value C converts and a function pointer is not one; a message naming the header's own callback type; and two notes, the second emitted **only where clang's text carries `const `** — §4.17's rule that the note is chosen by the type, applied to a note this repair first shipped unconditionally and which read as nonsense under `void (*)(void)`. Two cases, because the provoking case is a witness and not the class: an arity-and-type mismatch, and `qsort`, where both agree and the pointee's qualifier does not | §4.19, §4.17, §12 | 111 R4, 071

2026-09-05 | **The thread guard is measured on all three platforms and it fires on every one, and the binding that reaches it has two spellings and no portable one** (M-c-callbacks step 2; panel 111 R9, CLAUDE.md § Commands' three-platform rule). **The question worth fearing was whether the guard is silently FALSE somewhere**, since a guard that does not fire is worse than none: the program that should stop runs on. It is not. A `_Thread_local bool` starts at zero on a thread `CreateThread` made (Windows Server 2025, clang 22) and on one `pthread_create` made (glibc, clang 22), so `hero_thread_guard` aborts by name on both while the home thread passes in silence — the same result the Mac gave over ten ASan runs of ten. **Panel 111's seats were right to put `pthread_t` as a QUESTION rather than a premise** (CLAUDE.md §1): on glibc it is an `unsigned long` of 8 bytes, so the Mac's `@thread: ptr` does not compile there — and the refusal is `ffi_parameter_type` at exit 1 on the author's own line, with a `guess` fix *declare it `u64`* and a note that C's `long` is the platform's word rather than a width the header chose. **But a thread does bind on Linux**: `pthread_create(@thread: u64, …)` builds, runs, spawns, and the guard stops the worker from a real Heroes program at exit 134. So the finding is not *"it does not bind"* — it is **two spellings and no portable one**, which is `M-core-packages`' platform-typedef question arriving in a second place and which `M-isolated-threads` meets first. **Also discharged here**: the callback golden under `--sanitize` on Linux is exit 0 with the blessed output and no leak, which is § Commands' rule that an `extern` program's Linux leg runs where LeakSanitizer exists; `ffi_callback_type` fires on both platforms; and the guard is emitted **1** time in a callback program and **0** in one without, identical on all three. **What this record also holds is a rule this session broke**: § Commands says the three platforms are measured BEFORE the commit, and steps 0 and 1 were committed with one measured, because the Linux container was filed as needing to be brought up without checking whether Docker was already running — a failed search written as an impossibility, §1's named shape, costing two commits that happened to be right | §1.12, §4.19, Part 7.13 | 111 R9, 107 | docs/environment/linux/LINUX-MACHINE.md · docs/environment/windows/WINDOWS-MACHINE.md

2026-09-05 | **The callback permission reaches the document, at the bound the spec-warden predicted and with nothing to spare** (M-c-callbacks step 3; panel 111 R4). One paragraph replaces `Neither may be an absolute path.`: *A callback is a **parameter**, never a result; its parameters follow the same rule and `()` is `void`: `atexit(f: (function() -> ()))`.* **Two sentences and two jobs**, both named by R4: the spec-warden's **parameter-only restriction**, which the proposal's own longer wording never mentioned and whose colon was orphaned — it introduced a spelling and pointed at an example holding no function pointer — and the ergonomist's, which is *"the only thing that makes `atexit` writable from the document alone"*, because without it a reader knows a callback may be passed and not at which widths, nor that a `void` callback is spelled `(function() -> ())`. **Field position is not mentioned and costs zero**, the spec's field list being closed and already refusing it in writing. **Measured across four wordings rather than estimated once**: 3799 → **3830**, gross **+39** against a measured **−8**, net +31, headroom 266; the first draft read 3838 and was cut twice. **The removal was verified by running, not taken from the sitting**: an absolute header and an absolute `link` are both `error[machine_locked_path]` at exit 1 on the group head, and the message carries the entire rule plus three repairs — `CPATH`, `package`, and *`link` takes the library's name* — so the sentence is §1.4 redundancy the compiler pays back loudly, which is panel 089's shape and the only condition under which redundancy is repayable at all. **The payment was already in the record before the sentence was written**: panel 111's spec-warden registered *at M-isolated-threads close, `heroes measure spec/heroes-spec.md` reads ≤ 3830*, falsified if the callback's own RESULT width needs stating since *"a result may be wider than C's"* reverses for a function C calls back. This lands at **exactly** 3830 — the bound, not room under it — so any further callback text falsifies it, and the ledger row says so rather than leaving it to be found. `suite_spec.hero`'s `SPEC_TOKENS` and `LEDGER_ROWS` move to 3830 and 52 in the same commit, which is the whole reason that instrument exists | §1.6, §1.2, §1.4, §4.19 | 111 R4, 089, 012, 046

2026-09-05 | **Panel 112 refuses a `const` spelling and finds that the sitting was convened on 2.6% of its own subject** (`docs/panel/112-const-is-the-wrong-noun.md`; provisional, ratification queued). The brief carried panel 111 R4's *"without it 5 of 8 real callbacks do not bind"* as though `const` were the cause of all five. **The ffi seat measured `sqlite3.h` itself — 106 callback signatures, 153 unspellable parameter occurrences — instead of eight hand-picked ones**, and sorted them by cause: a pointer to a struct the header declares is **102 (66.7%)**, a pointer to a scalar 26, a pointer to pointer 18, and **a `const` pointee that is not `const char *` is 4 (2.6%)**. Cumulatively **17 of 106 bind today**, 18 with `const`, **82 with a pointer-to-header-record spelling**. `const` is the wrong noun. **Four seats refuse it on four independent grounds**: a new `Ty` case is **173 `non_exhaustive` errors across 49 files** before one arm gets correct behaviour, which Part 5's own test makes core rather than sugar; the cheapest spec wording is **+13** of 266 tokens of headroom and the full vocabulary **+49**; `selfhost/` binds **zero** callbacks across four extern groups, so Principle 0's compiler-need branch fails and no Part 11 effect was offered; and the seat given only the spec could not reach the question at all — **nothing turns a `[i64]` into a `ptr` and nothing reads through one**, verified against all four mentions of `ptr` in the document, so `qsort` is unwritable two layers below the qualifier. **Panel 083 R4's veto stands and is stronger than when it was written**: its stated reason was half a citation to CLAUDE.md §13 closing Part 7 *"until the fixpoint"*, and the fixpoint landed 2026-08-18, two days after that sitting — the premise expired in silence, §11's named shape, and what replaced it is `heroes check` printing 173 and `heroes measure` printing 3878. **Part 8 wart 19 lands instead, at zero spec tokens, with the ffi seat's ordering rather than the brief's**, plus a fifth cause nobody had named: an out-parameter **inside** a callback is a parse error, 26 occurrences in that one header. **Three shipped defects were found by seats, all three the coordinator's and all three the same shape — a claim written without being run**: a diagnostic citing a Part 8 wart that did not exist; a needle matching `const char *`, which `cstr` spells exactly, so a callback whose only mistake was `i64` for `int` was told *"this one cannot be written at all today"* while the one-character repair runs at exit 0; and `atexit(f: ptr)` handed `nullptr` at **exit 139**. The first two are repaired here with three tests over the exact strings. **The third is filed as defect 013 rather than repaired, and the reason is a measurement the sitting did not have**: the warden's `-Wpedantic` fires twice on `examples/sqlite/main.hero`, which passes a deliberate NULL callback and is correct, and `nullptr` does not inhabit a function type, so the example cannot be rewritten at the typed spelling. Both routes out cost something a measurement cannot choose between, so it goes to the author. **Two false sentences in design.md are corrected in place**, both about this sitting's own subject: §4.19's *"9 out of 9 fail"* (three bind since R4) and its *"how it is compiled is undecided"* for a shim (`static inline` in a group's header compiles with no build rule, and binds `sqlite3_exec` today in 28 lines). **What did not change**: the cast prohibition, re-earned by two seats running it — zero diagnostics, exit 0, an arbitrary permutation under this project's own fourteen flags | §1.2, §1.6, §1.7, §1.11, §4.17, §4.19, Part 5, Part 8 | 112, 083, 111 R4, 058, 060

2026-09-05 | **Every FFI diagnostic was blind on Windows whenever the source path was absolute, and one line of arithmetic is the whole cause** (defect 014, M-c-callbacks step 4). `selfhost/emit/ffi_site.hero`'s `location()` parsed clang's `<file>:<line>:<col>: <severity>: <message>` by taking the **first three colons** — and on Windows **the first colon is the drive letter's**, so the file name came out as `C`, the line number as the rest of the path, and the parse refused because that is not a number. `location()` is the gate **every** `ffi_*` classifier passes through, so width, sign, class, arity, format string, writable parameter and callback type all fell through to **exit 2 together** — the compiler blaming itself for the author's declaration, which is §4.19's own named failure — on the platform where an author is most likely to type an absolute path. **Measured on the real box**: the same callback mismatch is exit 1 on macOS and exit 2 on Windows under `C:/Users/…/note.hero`, and exit 1 under a relative path. **Nothing caught it because CI compiles relative paths**, so the Windows leg has never produced a line with a drive letter in it, and nine sittings have touched this function's callers with no Windows box in front of them. It surfaced only because the author started the machine for a different question. **The repair reads the location from the right**: the seam is the `": "` that ends the column and opens the severity, since a path may hold a colon and may not hold a colon followed by a space where clang puts one; inside that prefix the last two colons are the line and the column. `rest` is cut where it always was, so nothing downstream moves — the tests say so in as many words. Four tests over the exact strings, and then re-measured on the box: **exit 2 → exit 1** with the diagnostic on the author's own line. **The process fact**: this is what CLAUDE.md § Commands' three-platform rule buys, and the same session had already broken that rule once by filing the Linux container as unavailable without checking whether Docker was running | §4.19, §4.17 | 052, 071, 112 | docs/environment/windows/WINDOWS-MACHINE.md

2026-09-05 | **Defect 013 is repaired by changing the question, and the witness is the faulting PC rather than the faulting address** (M-c-callbacks step 5; the author asked for it repaired after panel 112 filed it as blocked). Both routes the sitting priced were refusals and both were blocked: `-Wpedantic` fires on `examples/sqlite/main.hero`, which passes a **deliberate** NULL callback and is correct, and letting `nullptr` inhabit a function type is a spec change that closes nothing. **So the question moved from *how is it prevented* to *how does the program say what happened*** — panel 104's own answer for the stack, reached in the same handler, on the line above. **The witness is `pc == 0`**, and it is measured unique rather than argued: a wild store to address 0, a wild read from address 0, a call through a null function pointer and a call through a garbage pointer give `si_addr == 0` in **three** of the four cases and `pc == 0` in **exactly one**. `si_addr` is the witness panel 104 already rejected once when a wild store was read as a stack overflow, and this is that lesson applied before the fact instead of after it. **The cost is nothing anybody pays**: zero spec tokens, zero compiler lines, no ABI move, and a correct program never faults so never reaches the handler — which is what neither blocked route could offer. **Measured firing on two platforms with one witness**, `qsort(base: buf, n: 4, size: 8, compar: nullptr)`: `panic: a null function pointer was called`, exit 134 on macOS and on glibc, with the three adjacent fault shapes verified untouched on both. **What the three platforms taught is the entry's real finding**: `atexit(f: ptr)` handed `nullptr` **segfaults on macOS, aborts with glibc's own assertion on Linux, and exits 0 on Windows** where the CRT checks — so NULL-tolerance is a property of the C **library on that machine**, not of the type and not even of the function, and no compiler on either side of the boundary can carry it. That is why a compile-time refusal was never available and why this repair is the one that was. **The Windows twin was written, withheld, and then landed the same evening** when the author turned the box back on: it was held out for two hours precisely because § Commands says an unrun platform fact is an inference, and shipping it unrun would have put a possible compile error on the Windows leg at the next TAG. It compiles there (seed from C alone, exit 0, zero errors) and it fires — but only against a witness built for the platform, because **the Windows CRT validates both `qsort` and `atexit`** and terminates before anything reaches address zero. What reaches it is a callback that does not go through the CRT, `static inline void run_callback(void (*f)(void)) { f(); }` in a header the group names, which is what any third-party library looks like. **A fourth platform behaviour on top of the three**, and the reason the witness had to differ per machine. One thing measured rather than assumed before it went in: `runtime/runtime.c` compiled on this Mac before and after is **byte-identical**, so the net running at the time still answered for the tree | §1.12, §4.20 | 104, 112, 111 R4 | docs/work/DONE.md (defect 013)

2026-09-05 | **CLAUDE.md §1 gains a fifth shape, and it is the one the other four were obeyed against** (author instruction, *"write down somewhere what you have learnt, so you do not make the same mistake again"*). Every rule in §1 is written about a **claim** — a number, an inference, a negative, a repair's adjacent shapes — and every one of them was **followed** on 2026-09-05 while the day's five real errors walked past. The reasoning was sound each time; **the list it reasoned over was short, and the list was mine**. That failure is invisible in a way a wrong number is not: a wrong number contradicts something, and a missing option contradicts nothing at all. The five, each caught by somebody else: a panel brief naming **three causes** where the real `sqlite3.h` has **five over 106 signatures**, with the sitting's own subject **fourth at 2.6%**; *"`const` buys 3 of the 8"* when it buys **2**, wrong in the convener's favour; an option list omitting the **adapter**, which the two languages closest to this architecture actually ship, objected to by the historian as a fault of the LIST rather than of the options; *"CI is green on all three platforms"* inferred from the word `success` when it was **one leg**, the matrix widening only on an `m-*` tag; and a defect filed **blocked** with two priced routes and a written recommendation to repair neither, when **a third route was in the same file three lines above** and took ten minutes once the author asked for it anyway. **Three obligations**: enumerate from the world rather than from what can be thought of, and the fix is usually one command; say **where the list came from** wherever it is handed on, because *"the eight I could think of"* and *"the 106 in the header"* are different objects a reader cannot tell apart; and treat a recommendation as a claim about the option **set**, which is §15's decision rule at its weak point — before recommending, ask what would have to be true for an unlisted route to exist, and if the answer is *somebody would have to look where I have not*, look there first | §1, §15 | 112 | CLAUDE.md §1

2026-09-05 | **M-c-callbacks closes, and scoring its eight predictions found two that no milestone could ever have scored** (M-c-callbacks close; `docs/journal/033-c-callbacks.md` § Predictions, scored). The eight named this close and every one was run rather than recalled. **Three hold**: panel 112's compiler-engineer on the wart costing nothing a language change would cost (`heroes measure` **3830**, `variant Ty` still **17** cases, both counted today); the spec-warden's *zero programs need editing* (**0** existing `.hero` files under `examples/` or `tests/golden/` modified across the whole milestone, the 7 modified files there being `.expected` snapshots); and the historian's *this is not on a compiler clock*, measured over four cases and **three** compilers — Apple clang 21.0.0, Debian clang 22.1.8, gcc 14.2.0 — where a `void *` to a `char ***` parameter is exit 0 and clean on all three and a `void *` comparator against `const void *` is exit 1 on all three. **One is false and one is right in a way that is worth reading twice**: the ffi seat's *exactly three compile* is **RIGHT** over all eight — `atexit`, `pthread_create`, `sqlite3_busy_handler` — while its **membership is wrong by one swap**, because `sqlite3_busy_handler` is on its own list of failures and binds at exit 0, and the fifth failure is `nftw`, which it did not name. Its mechanism is false too: every refusal is this compiler's own message at **exit 1** on the author's line, never clang's `error: incompatible function pointer types` at exit 2. **And that row was nearly scored on a failed search** — it was first written *unmeasured, `raylib.h` is not installed on this Mac*, on a `#include <raylib.h>` clang could not find. raylib **is** installed (`/opt/homebrew/include/raylib.h`, 6.0; `pkg-config --cflags raylib` names its directory), and the reason the include failed is exactly what `extern "raylib.h" package "raylib"` exists for. Measured with it, `SetTraceLogCallback` is `ffi_callback_type` at exit 1, on a cause the sitting's five-way breakdown does not name for this callback: `va_list` is **`char *`** on Darwin arm64, and a non-const `char *` has no Heroes spelling — all three candidate spellings were tried and all three are refused. CLAUDE.md §1's *a failed search written as an impossibility*, caught before the commit rather than by a seat. Both seats also predicted `qsort` at **exit 2**, clang's own text; it is **exit 1**, `error[ffi_callback_type]`, on the author's line, which is what the class was opened for. **And two could not be scored at a milestone at all, which is the finding**: panel 112's compiler-engineer veto-falsifier and its ffi seat's `(a)`-conditional both trigger on a **commit that may never exist** — *if a `const`/`c_int` kind ever lands* — so naming a milestone as their scoring point was a category error rather than a lapse. They are recorded **not triggered** and left standing in the sitting's own file for whoever lands one; they are **not** renewed with a new milestone name, which panel 046 R2 forbids, and they are not lapsed either, because nothing was spent on them. A prediction whose trigger is a date is owed to a milestone; a prediction whose trigger is a change belongs to the change. **A measured note nobody had written down, turned up by the historian's row**: gcc cannot read this project's flag list at all — 2 of the 14 in `selfhost/cli/flags.hero::flags()` are clang-only spellings (`-Wconditional-uninitialized`, `-Wshorten-64-to-32`) and a third is spelled `-Wdiscarded-qualifiers` there, so gcc 14 stops at `cc1: error` before it reads a line of C. Not a defect — clang is required by design (§7) — but the price of that decision, measured, for the next sitting that argues from what another C compiler would do | §1.11, §4.19, §12 | 046 R2, 111, 112

2026-09-06 | **The refcount is atomic, and the ABI stamp stays at 21 because the skew case was RUN rather than reasoned about** (M-isolated-threads step 3; panel 111 R5 and R6, both ratified 2026-09-05). design.md's second v1 invariant has said since day zero that a narrow, never-inlined refcount boundary is what makes this **one edit**, and the edit is one: `typedef _Atomic int64_t HeroRefcount` carries `str`, `[T]` and `{K: V}` together, so an ordinary `+= 1` on the field is sequentially consistent and correct instead of silently wrong, and only `str.c`, `array.c` and `map.c` ask for the weaker orders — relaxed on the way up, acquire-release on the way down, where the release half is the one that is easy to leave out. `alloc.c`'s two leak counters became `_Atomic` and stayed **process-wide**: that is R5, and it is the OPPOSITE of the `_Thread_local` pair step 2 had priced, because a thread-local gate reads the main thread's own balance and a worker's leak exits 0 in silence. **The cost, five alternating runs per arm on two copies differing only in these eight files, both caches cleared**: 36.76/35.57/35.08/35.47/35.70 against 37.19/36.17/36.01/36.74/36.01, **+0.71 s, +2.0%** and +2.2% without each arm's cold run, and the warm ranges do not overlap — the control's slowest (35.70) beats the atomic's fastest (36.01). Measured TWICE, because the first run described code that did not ship: the three `= 1` stores were relaxed afterwards, on the argument that a block one line old is private, and **that recovered nothing measurable**. They stay for what they tell a reader, not for a speed they do not buy. It reproduces panel 111's **+1.7%**, measured there over 20M incref pairs and here over 583 compiler tests. **Why the stamp did not move**: `seed/heroes.c` is generated C from before this change, and it is exactly the version skew the stamp exists to catch — so it was compiled against the new header instead of being argued about. Clean build, 583 tests green, `_Static_assert`s on the counter's width, alignment and lock-freedom holding on all three platforms. A stamp bump would have cost a full seed regeneration and bought nothing the cache key does not already cover. **What stays broken and is named at its own line**: `cow.c`'s `if (refcount == 1)` is a test and then a mutate; an atomic makes that read whole and does not make the pair single | §1.12, §4.20, Part 7.13 | 021, 111 R5, 111 R6

2026-09-06 | **design.md's *no data race is expressible* is corrected, and the sweep written to carry the correction found five objects no count in this project had ever seen** (M-isolated-threads step 3; panel 111 **R7**, ratified 2026-09-05 and never landed until today). R7 said the sentence is true of a program's values and false of the machinery under them: two threads that alias no Heroes value still meet on the reference count of anything they both hold. `spec:47`'s *"There are no mutable globals"* is named there as the spec-side twin and **stays as written** — it is true, and it is true about the program rather than about the runtime; the spec is also at 3830 of 4096 and the callback paragraph's prediction says any further text falsifies it. **The finding is the sweep's, not the sitting's.** `tests/harness/suite_runtime.hero` gains rule 3 — every object that survives between calls in `runtime/` is `_Thread_local`, `_Atomic`, or written down with the reason it may stay shared and the milestone that will take it — and it fails in BOTH directions, so an entry that names nothing is red too. Its first run measured **28 mutable objects, not 18**: panel 111 counted **file scope**, and **five live inside function bodies** — `f64.c:89`'s cached C locale and four `static int done = 0;` once-guards. Four are safe because of where they are called; the locale is not, and it is reached from BOTH float renderers, so ordinary Heroes printing an `f32` or an `f64` arrives there and two threads can both call `newlocale` and lose one handle — a leak neither instrument can see, since the gate counts this runtime's blocks and ASan has no leak detector on Darwin arm64. It is filed open. **This is CLAUDE.md §1's newest rule met in the wild**: every argument about the eighteen was sound, and nothing in a sound argument points at the row nobody wrote down | §1.12, §11, Part 7.13 | 021, 107, 111 R7

2026-09-06 | **The scratch the runtime keeps between calls becomes the thread's, and the thread that made it gives it back** (M-isolated-threads step 4). Ten objects are `_Thread_local` — `array.c`'s four comparison-scratch, `dir.c`'s three, `run.c`'s three including the argv buffer on §1.0's closure list — and `f64.c`'s cached C locale stops racing by compare-and-exchange rather than by going thread-local, because thread-local would trade one leaked locale per RACE for one per THREAD and nothing could give that one back. **The step exists because of a parenthesis its own tree had written down**: `array.c` said the queue must become `_Thread_local` and that *"the buffer then leaks one allocation per thread, which is why it is not that already"*. So `parts/alloc.c`, where that file's comment said the question belongs, records this thread's kept buffer in a key whose destructor frees it. **The pointer lives in the key and not in a `_Thread_local`, and that is panel 111's measurement**: on Darwin arm64 thread-local storage is already torn down when a destructor runs, so the variable that names the buffer reads 0 exactly where it is needed. **Both platform spellings were run before either was written in**: `pthread_key_create` on POSIX, `FlsAlloc` on Windows, which has no pthreads at all. C11's `<threads.h>` would have been one spelling for both and cannot be used — present on Windows and glibc, **absent from the macOS SDK**, measured the same day. The single slot is a falsifiable claim with a panic behind it rather than an assumption (CLAUDE.md §11). **Cost +0.20%, inside the noise**: 36.30/36.09/35.75 against 36.51/35.82/36.03, overlapping ranges, and the prediction was written before the runs. Two earlier runs are discarded because the coordinator loaded the machine while the clock was going — 937 s of wall against 33.7 s of CPU — which is the third time in one session that a measurement was spoiled by its own author's parallel work | §4.20, §1.12, Part 7.13 | 021, 111

2026-09-06 | **The instrument written to make one list honest caught its own author three times in three hours** (M-isolated-threads step 4; `tests/harness/suite_runtime.hero` § Rule 3). Rule 3 landed at step 3 and takes the list of shared mutable state from the tree on every run. Step 4 then made ten objects `_Thread_local`, and the same run went red **twice, in opposite directions**: ten allow-list lines that had just stopped matching anything, and **four new shared objects the repair itself had introduced** — a key, a slot and two once-guards, inside the very hook written to give a thread's buffer back. Neither was noticed while writing them. A third followed minutes later, from the third suite: a unit test written at step 3 asserted that `f64.c cached` was allow-listed, and step 4 removed that entry because the locale was repaired — so the witness was replaced by one drawn from the part of the list that is NOT waiting for a repair, with the reason written beside it. **None of the three would have survived a re-reading**, because the reasoning was correct each time and the list it reasoned over was short: CLAUDE.md §1's newest rule, met three times by the one check written to enforce it. Measured after step 4: 32 objects, 15 `_Thread_local`, 3 `_Atomic`, **14 still shared**, 6 of them inside function bodies | §1.12, §11, Part 7.13 | 107, 111

2026-09-06 | **Panel 113 was convened on copy-on-write and found the door somewhere else: a `[T]` crossed the FFI boundary inside a callback signature, at exit 0** (M-isolated-threads step 5; defect 014; `docs/panel/113-the-door-was-the-callback-not-the-copy.md`). `selfhost/check/ffi.hero`'s `crosses_the_boundary` is the one function that decides what a C header may spell. It refuses `.array | .fixed | .map | .fallible` and answered `.function_ty => return true` **without recursing into that function type's own parameters and result**, so the vocabulary rule was applied to the callback and never to what the callback carries. The bar was one forward declaration and no runtime include. **`spec:224` had already refused it** — *"a callback is a parameter, never a result; its parameters follow the same rule"* — so this was the compiler disagreeing with the specification (CLAUDE.md §12) and it cost no spec token to settle. The repair recurses, on the termination precedent at `check/table.hero:278-285`, and it **edits nothing that exists**: enumerated twice, by two seats, from the tree, there are **7 callback declarations inside `extern` groups and 0 of them name a container**. **And the sitting's real finding is the negative one**: both compiling seats, in separate checkouts and with the guard patched down, could not race `cow.c`'s `refcount == 1` — 8 threads and 240,000 mutations, ThreadSanitizer silent — while the same instrument named a hand-made race at `cow.c:80` in the same session. A callback parameter arrives borrowed and the emitted body increfs before it can store. Panel 111 measured the corruption correctly and attributed it to the wrong noun | §1.12, §4.19, Part 7.13 | 049, 053, 111, 113

2026-09-06 | **`selfhost/check/ffi.hero` is cut, and the seam that was NOT taken is the point** (M-isolated-threads step 5). Defect 014's repair pushed the file past CLAUDE.md §11's ~300 lines. The obvious cut is *the position*, which the file's own module doc calls a different question — and that doc also says the two Rust files were merged into one Heroes module **on purpose**, *"because the two questions read together"*. A line count is not a reason to undo a reasoned merge. The cut taken instead is the **file-wide sweep**, structurally a third thing: a pass over every position rather than a question about a type in hand, called from `checker.hero` rather than from the signature check. It is `selfhost/check/ffi_sweep.hero` now. **The compiler then confirmed the seam had been there all along**: after the move `checker.hero` used nothing whatsoever from `check/ffi`, and `error[unused_binding]` took the import out | §11, §4.19 | 061, 062, 113

2026-09-06 | **Panel 114 refuses a conditional-compilation form, and the mechanism the author asked for turned out to exist already** (author instruction, over the coordinator's twice-stated recommendation to leave the question scheduled; `docs/panel/114-the-question-was-not-which-platform.md`). Five seats, three vetoes, **and none of them is the budget** — every shape fits the 266-token headroom, so the refusal is §1.7 and Principle 0. **The ergonomist found the answer by reading the spec cold**: § FFI says *"A group names its header"* and does not say a SYSTEM header, so a program ships its own header with the `#ifdef` inside. Run on macOS and Windows the same hour, one `.hero` source, exit 0 on both, and `--emit-c` carries **0** platform words, so `cmp seed/heroes.c` survives. **Three measurements killed the language form and none is about the symbol**: under a body-scoped `$if` the `extern` group stays ungated, so Windows gives `error[ffi_missing_header]` — one of the three strings `shell.hero:450` SKIPS on — and ten skips of fifty-six never trip `suite_corpus.hero`'s half-the-corpus floor, so the author's own *skipped by nothing* criterion would read as met while it was not; `heroes mutate` walks the AST, so a parsed-but-unchecked arm is a mutation site whose every mutant counts SURVIVED, biasing Part 11 metric 3 per machine; and `tests/emission/`'s **200 blessed files** are what two independent implementations agreed on byte for byte, the second now archived, so one corpus program using the form makes them one platform's with no way back. **The symbol itself is vindicated and unused**: Turbo Pascal, Oracle PL/SQL and **V** — a C-emitting statically typed language — all spell it `$`, and `$` is free in this lexer; the one collision is that Nim, whose surface CLAUDE.md §6 copies, spells `toString` with it | §1.0, §1.3, §1.7, §4.19, §13 | 036, 049, 050, 097, 114

2026-09-06 | **The sitting was convened on a silence that was a ruling, and the brief carried two other false claims** (panel 114 § What the brief got wrong). CLAUDE.md §1 says to grep `DESIGN-LOG.md` and `docs/panel/` for the thing that is NOT there before convening on something the documents do not say. The convener did not, and *"Heroes has no conditional compilation"* is the artifact of three rulings — `DESIGN-LOG.md:407`, `:455`, `:485` with panel 097 — one of which `selfhost/cli/io.hero:22` states in the repository's own words: *"The route out is not a platform arm — this language has no `#if`, by design."* The other two: the brief told five judges that CI compares `seed/heroes.c` **on every leg**, and `ci.yml:604` reads `if: runner.os == 'Linux'` — one leg; and it told them panel 049's return condition had opened the door, when condition 6 unblocks the WORD `windows` and not the feature, the veto's ground being §1.0 and §1.3 which it does not touch. All three were caught by seats, two of them by the judge who wrote the veto being misquoted | §1.0, §1.3, §11 | 049, 114

2026-09-06 | **M-isolated-threads closes: three of panel 111's four corruption classes answered, and the fourth was never where the corruption came from** (close; `docs/journal/034-isolated-threads.md`). Class 1 at step 3, the refcount `_Atomic` at a measured **+2.0%** with non-overlapping arms; class 2 at step 4, ten scratch objects per-thread and the unweighed buffer given back by the thread that made it, at **+0.20%**; class 4 is `M-thread-stacks`', which the author moved to row 36 the same day **because this close made its defect reachable**. Class 3, copy-on-write, was the sitting's subject and could not be raced: two seats, separate checkouts, guard patched down, 8 threads and 240,000 mutations, ThreadSanitizer silent, with a hand-made race naming `cow.c:80` in the same session to prove the instrument live. **The reachable defect was one line in the checker** — `.function_ty => return true` without recursing — which let a `[T]` cross a callback signature at exit 0, against `spec:224`'s own words. **And nothing in the language was needed**: `runtime/parts/spawn.c` put the thread arm where every other platform arm already lives, threads arrive through `hero_os.h` at **zero spec tokens**, and the isolation is the FFI type rule rather than a promise. **Ten example programs**, 39 test blocks, **byte-identical output on macOS, Windows and Debian, and zero skipped anywhere** — the author's own acceptance criterion, measured on the string the harness skips on. Predictions scored: the spec-warden's ≤3830 **holds at the bound**, three of the ffi seat's hold, the compiler seat's blast radius is **falsified** while its mechanism was right, and two are named unscored rather than lapsed | §1.12, §4.19, §4.20, Part 7.13 | 107, 111, 113, 114

2026-09-06 | **A row enters the chain for the compiler nobody installs, and it half-reverses a refusal three days old** (author instruction — *"maybe it makes sense to add a step to the roadmap that says: let us make a compiler that runs inside a browser, so whoever wants to try the language can try it quickly on the site, compiling to wasm perhaps"*, and in the same minute the fallback, *"or if that cannot be done, a remote compiler, but with safeguards so it does not become a way to break into a machine or to burn resources forever"*; `docs/ROADMAP.md` § M-online-compiler, row 56, immediately before the gate for M-install-channels' reason). **The candidate already had a recorded refusal, and the record is what made the answer sharp**: `DESIGN-LOG.md:539` refused *a web playground* on 2026-09-03, and `docs/work/DONE.md:2415` files it among five candidates recorded as refused *"so the candidate is not proposed again as new"* — the table doing exactly the job its own note claimed for it. The reason given was *Part 2 and Part 9: wasm breaks the FFI premise*, and read forward, both cited passages (`design.md:590`, `design.md:2869`) and `design.md:520`'s *native compilation rather than wasm* are about **a Heroes program targeting the web**: wasm cannot call native C libraries, so under §1.11 such a program has nothing left to call. **The compiler is a different program** — it reads text and writes diagnostics and C, and asks for no C library on the visitor's behalf — so the argument does not reach it. The half that does reach it is whatever runs the **visitor's** program, and that half is the milestone's whole subject. **The measurement that makes it hard**: of **56** programs under `examples/`, **20** declare an `extern`, naming `hero_os.h` (10), `math.h` (3), `stdio.h` (2), `sqlite3.h` (2), `time.h`, `raylib.h`, `curl/curl.h` and `SDL3/SDL.h`, two linking `raylib` and two `sdl3`. §1.11 is why the interesting programs are exactly the dangerous ones: with no standard library, the ones that show what the language is *for* are the ones that open a window, a socket or a database — so a playground that refuses `extern` demonstrates a language that does not exist, and one that allows it on a public server is a remote shell, `extern "stdlib.h" { function system(cmd: cstr) -> i32 }` being one line the compiler is right to accept. The deliverable is therefore a decision about **what a stranger's program may name**, and only then an engine. **What was measured rather than reasoned about**: Apple clang 21.0.0 on this Mac has **no WebAssembly target compiled in** (`clang --target=wasm32 -c` answers *No available targets are compatible with triple "wasm32"*), and no `emcc`, `wasm-ld` or wasi-sdk is installed, so the browser route needs a **second toolchain** against M-install-channels' rule that every channel builds from the seed with the one clang line; `seed/heroes.c` is **733,838** lines; and `runtime/parts/run.c` reaches the toolchain through `execvp` and `CreateProcess`, neither of which a browser has, which is why that route can check and emit but not run. **Three things are named untested rather than impossible** (CLAUDE.md §1's failed-search rule): whether the seed compiles under a wasm toolchain, whether a wasm-hosted clang is a real option, and what the nearest languages actually ship — the last belonging to the historian at the sitting, not to the convener today. **No panel: scheduling changes no language** (the 2026-09-03 precedent), and the row's opening convenes one, because the engine, §10's second-binary question and the allow-list are all tool surface. `ROWS` 60 → 61; `REFUSED` gains `M-wasm-playground` and `M-browser-compiler`, the first pair refused for one shared reason — an id naming the engine asserts the answer the sitting exists to give | §1.11, §10, §13, Part 2, Part 7 item 15, Part 9, CLAUDE.md §4, §14 | —

2026-09-06 | **Panel 115: the floor is the thread that ran `main`, and the sitting refused the number it was convened to choose** (`docs/panel/115-the-floor-is-the-thread-that-ran-main.md`, full panel, provisional pending ratification). Convened during M-thread-stacks after step 1's per-thread guard made the second half of the subject measurable: a thread this runtime creates got **536,576 bytes on macOS, 8,388,608 on Linux and 67,108,864 on Windows** from one line of Heroes. The proposal was one number everywhere, 8 MiB, and **every seat that looked found the number indefensible** — glibc's default imported (spec-warden), an eightfold CUT on Windows from a reserve `selfhost/cli/flags.hero` links for a measured reason (compiler and ffi seats, historian), delivered by a Win32 call that sets the COMMIT and not the reserve without `STACK_SIZE_PARAM_IS_A_RESERVATION`, which is a defect libuv ships today and which `runtime/parts/spawn.c:168` would have walked into. **What the seats wanted was the surprise removed, and the surprise is not that platforms differ**: it is that one machine gives its own two threads **19 levels against 312**. Adopted, and smaller than the proposal: a thread the runtime starts never gets less stack than the thread that ran `main`, POSIX only, Windows untouched, **no number entering the project**, and the delivered size **verified after the fact** because the ffi seat measured that macOS and glibc refuse OPPOSITE things (a sub-minimum size clamps on one and is EINVAL on the other; a non-page-multiple is the reverse; neither refuses a terabyte) and on the EINVAL arm the attribute silently keeps its default — an implementation trusting the return code restores the exact defect it repairs. LLVM and Chromium reached this answer from the same Darwin measurement. Panel 107 does not reach here: its three refusals are link flags and the main thread, and `pthread_attr_setstacksize` on a runtime-created thread is a fourth mechanism **it could not have judged, because on 2026-09-04 the runtime created no threads**. Measured after landing: a worker that died silently at 20,000 levels answers at 60,000 and stops where `main` stops, naming the function; under `--sanitize`, aborting at 5,000 became 40,000 | CLAUDE.md §4, §12; design.md §1.12, §1.11, Part 7.13 | 115

2026-09-06 | **The document gains four words and gets shorter, and a `strip` is what shaped the sentence** (panel 115 Q2, M-thread-stacks step 3). `spec:159` gains `; so does recursion too deep.` at a measured **+7**, paid by `spec:105`'s `` `Point(3, 4)` does not exist. `` at a measured **−12**: **net −6**, 3830 → **3824**, headroom 266 → **272**. **The veto that shaped it was a command.** Every candidate promising that the abort NAMES THE FUNCTION — panel 107's own tabled shape and the ergonomist's first amendment included — is refused, because `runtime/parts/stack.c` names it only `if (who != NULL)` through `dladdr`, and `strip deep && ./deep` prints `panic: stack exhausted` with **no name**. Shipping stripped is ordinary, so the sentence would have been false on the day it landed, which is refusal #3 of panel 107 repeated one milestone later. **The removal is §1.4 redundancy the compiler pays back loudly** and was verified by running it: `Point(3, 4)` is `error[missing_label]` once per field with the whole rule in the message and a `certain` fix, so §1.2's round trip is never paid by a model — panel 089's shape. **Panel 107's queued payment is LAPSED rather than spent** (§1.6, a prediction is re-decided and never renewed): it named M-corpus-depth close, which passed on 2026-09-04, at a headroom panels 110 and 111 have overtaken. **And the ergonomist withdrew its own +22 wording**, on a better argument than the token count: this document never defines *abort*, it teaches the word by **nine instances**, so a tenth in the same list inherits the whole meaning free while a property stated beside the list asks a reader to trust a new KIND of claim — and its own wording named a resource, which is what invites the number nobody will give. The sentence carries no word about threads, on that seat's veto: the abort is a property of the language and a thread inherits it without being named | design.md §1.6, §1.4, §1.2; CLAUDE.md §12 | 115
2026-09-06 | **A knot is a property of the PARTITION, not of the grammar — and one of the two files the sitting ordered split had no knot at all** (M-declared-freer step 1, panel 109's condition 7, which is a seat's condition rather than tidying: the `owned <C function>` mark lands in both files and neither had a line of room). Measured with `tests/harness/suite_layout.hero`'s own `code_lines`: `selfhost/ir/lower.hero` **1511 → 174** and `selfhost/parse/decl.hero` **638 → 217**, both now under §11's own 300 and **out of `DECIDED` entirely**; five new modules, of which exactly one needs a row. **The two cuts are opposite kinds and that is the finding.** `ir/lower.hero` has a real knot — **25 functions, 990 code lines**, all reaching each other through `expr` — so the only cut available is the part that calls INWARDS only: the declaration walk stays behind, keeps the name, and the **21** modules calling `lower.lower(…)` did not move a character, while `ir/flatten.hero` carries the knot at **1104** with the map §11 owes. `parse/decl.hero`'s own module doc claimed the same thing — *"one module because the language leaves no choice"* — and it was **false, measured**: no cycle in that file's call graph at all, because the ring `file → extern_group → members → constant_tail` closes only while the shared tails live beside the dispatcher. Moved into `parse/tails.hero`, every edge runs one way — 217 + 246 + 235, no ceiling row for any of the three — and the answer had been printing itself in `module_cycle`'s own note the whole time: *"move what both modules need into a third one that neither uses"*. **That the first knot cannot be cut is a command and not a search** (§1: a failed search is not an impossibility): the one route out of a mutually recursive walk is to hand the recursion over as a function value, and a function type cannot carry `@` — `(function(@Box) -> ())` is `error[expected_type]: expected a type, found `@`` — which is exactly how this walk threads its builder. What left the knot left by a mechanical rule rather than a line count: `ir/questions.hero` (**149**) is everything with no `@b` parameter, `ir/emissions.hero` (**185**) everything that emits one fixed shape and never walks back. Panel 109's coordinator predicted **≤ 900** for `ir/lower.hero`: true of the file that kept the name, false of the piece the split produced, scored FALSE on the reading the seats meant. **The fixpoint holds on the new seed, byte for byte**, and the step also repaired a red `main` it did not cause: `e61fec3b` changed `selfhost/` without regenerating `seed/heroes.c`, so CI's Linux net ran a compiler built from a seed that still carried defect 015 and printed `i64(3)` where the fixture writes `int(3)` — measured here on both seeds, the two failing rows before and neither after.
2026-09-06 | **164 live source files pointed at a tree that moved eighteen days ago, and the instrument built to catch exactly that could not see any of them** (M-declared-freer step 1, found while writing four module docs for the split; option (a) of four, applied as the provisional default under CLAUDE.md §15, with the instrument half left open and measured). `crates/` appeared **179 times** in **145 files under `selfhost/`** and **19 under `tests/`**, always as a module doc's `Port of crates/heroes/src/…`; the tree became `archive/bootstrap-rs/` at M-bootstrap-archive and nothing followed it. **The wording was sampled before the rewrite and that is what made a mechanical sweep safe**: 118 of 128 sampled read *"Port of crates/"* and every variant of the rest (*"lives in"*, *"cases at"*) is a live pointer to a file rather than a dated claim about the Rust, so no sentence needed a judgement. `docs/` keeps its 46 — §14 forbids rewriting a dated record. **What the verification found is the part worth keeping**: of 165 distinct paths cited afterwards, 136 plain ones exist, 26 are brace or glob forms unresolvable by construction, and **3 did not resolve inside the archive either** — `heroes/src/diagnostics.rs` is `diagnostics/mod.rs`, `emit/inventory.rs` and `resolve/inventory.rs` are both `builtins.rs`, whoever wrote those docs having put the HEROES module's name inside the RUST path, which the old dead root had been hiding. **The seed does not move**, measured by re-emitting and `cmp`: an in-line substitution shifts no line, so no `#line` shifts. **And the instrument's blindness is now measured rather than assumed**: `tests/harness/suite_records.hero` reads `design.md` and `CLAUDE.md` only, and most of these citations carry no backticks, so it was green over 164 dead pointers including one in its own file. Extending its anchored rule to the compiler's comments would give **208 distinct paths in 294 occurrences with 8 misses, 3%** — against the **52%** panel 086 measured for the unanchored rule it refused — but 7 of those 8 are legitimate prose needing exemptions, which is a real price and is the author's to pay; the eighth was a `fixedbugs` golden calling a repaired twin *"still open"* thirteen days after the repair, and it is fixed here with the mirror shape re-measured rather than taken from the record: `error[ffi_field_type]`, exit 1.
2026-09-06 | **`owned <C function>` enters the language, and the formatter was deleting it within the hour** (M-declared-freer step 2; panel 109 ratified 2026-09-04, conditions 4, 5 and 6 of eight). The mark parses in both positions the sitting named — after a `cstr` result and after a parameter's type — and is a **contextual word, never a keyword**, which is measured rather than promised: `owned = 3` still binds, `function owned(n: i64)` still declares, both run at exit 0 on the compiler this step built. **The spec is the sitting's own wording Wt2**, one sentence after the callback rule, paid by R2a and carrying the third case's three words; every delta re-measured at this commit rather than carried — Wt2 **+71** (the sitting measured +70), R2a **−27** (−28), the three words **+3** exactly, **gross +74, net +47**, headroom **272 → 225**, and `docs/measurements/010` gains its row. **The reason two of three moved is worth more than the numbers**: a BPE count is not additive across an edit boundary, so a ledger that adds up a sitting's columns is doing arithmetic the instrument does not support — it re-runs `heroes measure` per delta instead. **AND CLAUDE.md §9'S LIST EARNED ITS KEEP INSIDE ONE HOUR.** With the parser done and the form reading correctly, `heroes fmt` printed `function strdup(s: cstr) -> cstr` — the mark **silently deleted**, which for this form is not a lost word but a **leak**: `fmt --in-place` would hand back a program whose C-allocated string is never freed, at exit 0 with nothing on either stream. `--dump-ast` dropped it too, which is the half that matters most, because `heroes fmt`'s own self-check compares two DUMPS and would have agreed with the formatter that was deleting it — the `as` defect exactly, caught this time before the commit because the contract says to look. Both printers now share one `owned_suffix`, so the two positions cannot learn the form apart, and the round trip is asserted over **six** shapes rather than the one that provoked it: both positions, both together, both unmarked, a non-`@` parameter carrying it, and `owned` as an ordinary name. **One structural finding, and it is this morning's again**: the marker had to live in `parse/members.hero` rather than beside `tag` and `partial` in `parse/tails.hero`, because `params` reads it and `tails` already uses `members` — a knot is a property of the partition, and the compiler's own `module_cycle` note is what says where the code goes. `ast.hero` 455 → **475** against a measured 466, the knot clause applying without argument since an AST is mutually recursive by construction; 2 lines of code and 13 of comment, which is the ratio §11 asks for.
2026-09-06 | **The citation check reads the whole tree, and there is no list of exemptions** (M-declared-freer step 2's companion; the open half of `docs/work/DECIDE.md`'s citation item, built the same day its sweep half landed). **What it read before**: `design.md` and `CLAUDE.md`, backticked tokens only, never the brace form — so it was green over **164 files** citing `crates/` eighteen days after that tree became `archive/bootstrap-rs/`, and one of the 164 was `tests/harness/suite_records.hero` itself. **What it reads now**: every living file, records exempt by §14, in prose and comments only; bare tokens as well as backticked; brace forms **expanded**, which is the shell's own semantics rather than a heuristic and which retires the limit panel 086 measured and recorded as permanent; and it resolves the way a READER resolves — from the root, through the house shorthand (`ir/lower.hero` is how this tree writes `selfhost/ir/lower.hero`), and from the citing file's directory or any above it. **THE DESIGN IS THAT THERE IS NO EXEMPTION LIST**, because a list is a second place where truth lives and this repository's record is a long account of second places going stale. Four shapes of legitimate path-like prose fall out by principle instead: a path inside a **string literal** is data and not a citation (`"docs/NOSUCH.md"` in a test is the fixture proving the compiler says *no such file*); what **git ignores** is not a dead citation and git is asked rather than guessed at; a **brace that does not close on the line** is half a promise and not checkable; and the evidence a sentence must carry is a **DATE**. That last one was measured rather than chosen: a resolving sibling was accepted until `runtime/parts/run.c` showed it silencing a real dead citation by coincidence — a date is checkable against the history, a neighbour is an accident. **From 91,864 path claims read to zero dead ones**, in fifty repairs that each turn a sentence into a dated sentence. **Four real defects surfaced while it was being built**: `.claude/agents/compiler-engineer.md` sent the panel's compiler seat to read an archived tree while `.claude/skills/panel/SKILL.md` says *"Never `crates/`"* in so many words — two instructions to one judge, contradicting, the wrong one in the file that defines it, and CLAUDE.md §4 tells this exact story about panel 087; `docs/work/DONE.md` is a record by §3's own words and was missing from `RECORD` for the ten days it has existed, so the widened check wanted 34 repairs inside it; **two sentences this session broke** in `f0d95197`, where a mechanical sweep was safe for pointers and not for prose that is ABOUT the move, found by grepping the signature — the archive path twice on one line — and a reminder that a sample says what is there and not what is missing; and `runtime/parts/run.c` claimed to ship `tests/golden/run/win-quote-round-trip.hero`, which `git log --all --diff-filter=A` says **zero** commits ever added, so the Windows quoting round trip is untested and its comment said otherwise from the day the function landed. **Cost**: the `records` suite 7 s → 17.7 s. **And three narrowings of `is_claim`, each paid for by a class of correct prose landing on the report**: a `.h` under an unknown root is a C library's header and not a path here; a `.hero` under an unknown root is this language's own module path being explained, which is vocabulary; `.rs` stays, because this repository writes no Rust any more — that last is panel 092's half, which the first draft dropped and a test caught.
2026-09-06 | **Your own call of a declared freer is a compile error, and it reads nothing but the callee's name** (M-declared-freer step 3; panel 109's condition 2 of eight, the one four seats wrote independently and the only one that turns a silent double release at exit 0 into a diagnostic). `owned sqlite3_free` hands the freeing to the compiler; a program that also calls `sqlite3_free` frees the string twice, and on both platforms that is **exit 0 with nothing on either stream**. `error[owned_freer_called]` now names the call, the mark's own line, and the two repairs — *delete this call and let the compiler free it, or delete the mark and free it yourself* — neither of which can be `certain`, because which one is right is a question about what the program MEANS. **It is a check and not a dataflow pass**: a function this program named with `owned` is one it handed to the compiler, so the callee's name is the whole question, and `selfhost/check/freer.hero` is 61 lines with `selfhost/check/walk.hero` growing by 8 (1670 → 1678 of a decided 1700). **Refusing EVERY call and not only the provably-owned one is the robust reading and is deliberate** (CLAUDE.md §4): a freer called on some other pointer is exactly the shape that double-frees the day the two pointers become one, nothing on the closure list needs it, and the narrow reading would have to prove which value reaches the call. **Both negative shapes are asserted rather than assumed**, because a refusal nobody can turn off is a ban: a program with no mark calls its freer freely (exit 0, measured), and a mark naming a DIFFERENT freer leaves this one alone (exit 0, measured). **Condition 5's implementation is NOT in this step and the reason is worth recording**: the `char **` carve-out lives in `selfhost/emit/ffi_mutable.hero`, which reads clang's own output and matches columns against the IR's function — and the mark does not reach the IR until the lowering lands, so that half rides step 4 rather than being forced here.
2026-09-06 | **A row enters the chain for the programs nobody writes, and every instrument here was pointing the other way** (author instruction — *"Consider adding a step to the roadmap, towards the end, called something like consolidation — you say when it makes sense, maybe after the packages. It should create as many valid Heroes programs as possible that nonetheless make the compiler crash: hunt for bugs, looking too at the bugs that were found in other similar languages and compilers. The difficult conditions are the interfacing with C and very deeply nested structures. Purely and simply with fuzzing."*; `docs/ROADMAP.md` § M-generated-programs, row 49, after M-panic-location). **The gap was measured before the row was written.** `heroes mutate` is the inverse instrument — its own module doc says it takes the corpus programs that check clean and makes *one plausible mistake per site*, counting how many the compiler catches — so everything this repository aims at the compiler is aimed at **wrong** programs, and nothing has ever aimed a machine at it with right ones. Of the **fourteen** defect entries in `docs/work/DONE.md`, every `found by` field names a person: writing a program (006, 007), a panel seat (004, 010, 013, both 014s, 015), the baseline net run before touching anything (002), a `fmt` sweep (003, 005), the Windows box (008), a program's first run (009), the post-M8a sweep (001). **Four of the fourteen are at the C boundary and one is depth** — 010, 013 and both 014s are FFI, and 007 is `heroes check` at exit **139** with nothing on either stream, on a valid file nesting deeper than the compiler's own recursive descent — which is why the author named those two conditions and not others; `selfhost/` holds **63** points where the compiler declares a case impossible (`hero_unreachable`/`unreachable()`, `emit/structural.hero` alone 12), and defect 006 is one of them reached by a program `heroes check` had just accepted. **Four choices, each put with a recommendation, each taken at the widest**: the name, refusing *consolidation* as an area and the runners-up `M-adversarial-corpus` and `M-compiler-survival` under §14 — the first annexes an area three milestones already share, the second states a promise a later milestone can falsify; the placing, after the four verdict milestones, after M-check-completeness whose promise is one of the oracles, and after M-panic-location because a crash that names its line is a triage of minutes instead of hours; **five oracles** rather than crashes alone (the compiler does not fall over · the answer is the one the generator computed, in the net's own three configurations `-O0`, `-O2`, `--sanitize` · `heroes fmt` re-prints byte for byte · what `check` accepts `build` compiles · no leak); and the harness rather than a new verb, so §10's stopping rule is untouched and the tool convenes nothing. **The method is named in the row so the milestone does not re-derive it**: generate from the TYPE and never from the text, so the program type-checks by construction and a refusal is itself a defect, and compute the answer while building, which is Csmith's checksum trick and the only thing that makes the exit-0-wrong-number class visible at all. **Heroes has no undefined behaviour**, so unlike YARPGen there is nothing to steer around — overflow, division by zero and an index out of range all abort by design — and what is left is a declared arm whose expected result IS the abort. **No panel: scheduling changes no language** (the 2026-09-03 precedent); a repair that reaches the language convenes one when it comes. `ROWS` 61 -> 62, the chain 57 -> 58, `REFUSED` gains two names — and the counting found a record fault, written here where it was found: `docs/work/DONE.md` numbers its defects 001-015 and uses **014 twice**, while 011 and 012 are recorded inside the entries that repaired them | §1.12, §10, Part 11, CLAUDE.md §1, §9, §14 | —
2026-09-06 | **Two vetoes in opposite directions, and the resolution is what both seats had already written down** (panel 116, soundness lane, convened by author instruction on the question panel 109 left open: where the `owned` release is BUILT). **The FFI seat vetoed the IR route** because `ptr` emits as `void *`, and the probe measured over panel 109's own list of freers is **10 of 10 accepted silently at `void *` against 6 of 10 rejected at `char *`**, each rejection naming the header's real type under a `-Werror` already in the flag list; `fclose((FILE *)strdup(…))` is **exit 139**. **The cast is the blinding agent** — `(void *)p` silences every check, `(char *)p` strips the `const`, which is the only thing that needs stripping, and keeps the pointee. design.md:2246-2249 names the same mechanism one level up. **The compiler seat vetoed the emitter route** because it implements `owned` in the checker and the backend and skips the lowering — a second elaborator inside the printer, against Part 5's *"keeps the emitter a printer"* — and measured that its only two arms are both refused elsewhere: `h_library_validated` is emitted **zero** times in a program that does not itself call it, so a generated release referencing it is `call to undeclared function`, **exit 2 on a legal program**, which `selfhost/emit/builtins.hero` records having already shipped once; and composing a `str?` in raw C is the arm **design.md:476-480 already refused** for `read_file`, in the same words. **Adopted: the release is built in the lowering, and the freer call keeps its pointee** — the two seats' conditions are compatible because they answer different questions, and the FFI seat wrote the exit itself. Costed with `suite_layout`'s own rule: adding the case names **exactly two** non-exhaustive sites and no others, the release builder is 76 lines in a new module, `flatten.hero` goes **1104 → 1084** with a three-site collapse, and **no `DECIDED` row moves**, which is panel 109's condition 7 honoured rather than argued. **Two findings worth more than the verdict.** The FFI seat enumerated its own limit from the header rather than from memory — `curl.h` writes `typedef void CURL;`, so `curl_easy_cleanup` genuinely is `void *` and no probe can catch a wrong freer there: **six of ten, not ten of ten**. And it re-measured the NULL guard rather than trusting panel 109: `fclose(NULL)` **survives** on this Mac and a guardless release exits 0 **even under ASan and UBSan**, so no local run can ever justify its absence. **AND THE COORDINATOR CONTAMINATED A SEAT'S BENCH.** The prebuilt compiler shipped inside the frozen snapshot was built from an uncommitted change — the checker half of step 4, written, measured at exit 2 and reverted from the tree but not from the binary. Measured after the sitting: that binary exits 0 where the committed compiler exits 1 with `bad_operand`. One supporting sentence in the compiler seat's verdict — *"the dump is already wrong today"* — was therefore the coordinator's own change and not the tree's; the argument survives the correction and the cost table, the exhaustiveness measurement and the undeclared-function experiment are all independent of it. `/panel` already says the working tree is frozen once the briefs go out **and that the rule binds the coordinator**, after panel 056; this is that failure one level worse, because a changed tree baked into a brief is where no judge can see it. The rule the sitting records: **the compiler a brief ships is built from the commit the snapshot names, and the brief says which commit that is.**
2026-09-06 | **A wrong freer is a compile error now, and the mechanism is the probe's parameter type** (M-declared-freer step 4a; panel 116's resolution as corrected at the implementation, ratified the same day). The probe this compiler emits per `extern` never runs — it exists so clang checks a name against its header — and **its parameter's TYPE is the whole question it asks**. Measured with `clang -std=gnu11 -Werror -Wall`: probes typed `void *` accept `free` **and** `fclose`, exit 0 with nothing said, because in C a `void *` converts to anything; typed `char *`, `free` passes and `fclose` is refused in the header's own words. Panel 116's FFI seat measured the same over ten real freers, **10 of 10 silent at `void *` against 6 of 10 refused at `char *`**, and ran `fclose((FILE *)strdup(…))` to **exit 139**. So a function some `owned` mark names is probed with `char *`, and `owned fclose` on a string that `strdup` returned is now `error[ffi_freer_type]` at exit 1 where it used to build clean and die at 139. **The honest limit, enumerated from the header rather than from memory**: `curl.h` writes `typedef void CURL;`, so `curl_easy_cleanup(CURL *)` genuinely is `void *` and no probe can catch a wrong freer there — six of ten, not ten of ten. **AND THE CARET IS ON THE MARK, NOT ON THE DECLARATION**, which is the shape next door and was wrong in the first cut: the first working version blamed `function fclose(f: ptr)`, a CORRECT declaration, and would have sent a reader to fix what is already right. `ffi_freer_type` points at `fclose` inside `owned fclose`, says the header takes `struct __sFILE *`, and names both repairs — the function that frees THIS string, or dropping the mark. **The correction to panel 116 is appended to the sitting rather than edited into it**: the coordinator's resolution said the freer CALL keeps its pointee where the seat's condition said the freer PROBE, and the measurement says the seat. A record is not rewritten, and the author's ratification must stay attached to the text they read. **One split, along a seam and not at a count**: `emit/extern_probe.hero` was at **297 of §11's 300** — three lines of room, so any real change crossed it — and the seam was already there, writing the probe versus reading clang's reply back. `argument_columns` and its test are `emit/probe_reply.hero` (**35**), the writer is **292**, *smaller than before the feature*, and **no `DECIDED` row is added or raised**.
2026-09-07 | **`owned` frees the memory, and the measurement is 12.8 MB** (M-declared-freer step 4; panels 109 and 116, both ratified). The release is built in the lowering as panel 116 ruled — a slot, a call, a branch and a call, Part 5's own vocabulary — and `selfhost/ir/owned_release.hero` is what builds it. **What it does, in the order that is the whole correctness of the shape**: the extern call yields the `cstr` the header declares; the pointer goes into a slot, because it is read three times and the call must not happen three times; the library's `validated` COPIES the bytes out **before** anything is freed, since reading after freeing is a use-after-free a plain build reports as nothing at all; the null test runs against `const nullptr`; and `cast cstr_to_ptr` reaches the freer by name. **Measured end to end on this Mac, same program, same printed answer, 200,000 strings**: with `owned free` the peak resident size is **5,357,568 bytes**, without the mark **18,202,624** — a difference of **12.8 MB**, which is 200,000 strings of about 58 bytes. **And the unmarked program is the one that could be written yesterday**: it leaks in silence at exit 0, and on Darwin arm64 nothing says so, because ASan's leak detector does not exist here (§7) and `hero_runtime_check_leaks()` counts Heroes allocations rather than C's. That is why §8's Linux `--sanitize` leg is the judge and why peak RSS is the honest local instrument. **The checker's half landed in the SAME commit and that was not a preference**: typing a marked `cstr` result as `str?` alone, written and measured on 2026-09-06, makes clang refuse the generated C at exit 2 — the compiler blaming itself for a `.hero` file. **All three call sites are hooked through one helper**, which is panel 116's second condition rather than tidiness: hooking the plain call alone leaves `x.strdup()` and `mod.strdup(…)` handing back a string nobody frees, in silence, on the two spellings nobody would have tested. **And the blessed emission proved the panel's own argument**: that golden's C grew by 262 lines the moment the release landed, because the release emits a REAL `.call(.heroes_fn(validated))` that the reachability walk can see — which is precisely what the refused route could not do, since a call existing only as a string in the emitter is invisible to a walk over IR instructions. `tests/golden/ir/owned-release.hero` pins the shape where `UPDATE_GOLDEN` is forbidden, so no later change can quietly drop a step.
2026-09-07 | **The `char **` carve-out lands, and the program panel 109 existed for compiles** (M-declared-freer step 4b; condition 5's second half — the spec has said it since step 2 and the compiler refused it until now, which is the worse of the two ways to be wrong). `@error: cstr owned sqlite3_free` against SQLite's `char **errmsg` was `error[ffi_writable_parameter]`, panel 058's rule, and that rule is right in general: lending a string's own bytes to a C that may write through them changes values this program never passed to C. **A MARKED `@cstr` is the one case where C writes a pointer the program will OWN**, so the rule steps aside there and only there. **Two places had to move, and the second was only visible by running it**: the diagnostic steps aside, and then the PROBE was wrong — it spelled the cell `const char **` where the header says `char **`, so the probe itself became the thing that would not compile (`discards qualifiers in nested pointer types`, exit 2). A marked cell drops the `const`, which is the same mechanism as step 4a's freer probe one level up: **the probe's spelling is what clang checks, and a wrong spelling is a wrong question**. **What this does NOT do, stated rather than left to be found**: the `@` cell now compiles and is not yet RELEASED — `ir/owned_release.hero` hooks the result position, and the cell needs its own release at the write-back. Condition 8 waits on that. **And the question moved to one home**: both the probe writer and the diagnostic reader need *is this parameter marked*, and they had a copy each for about ten minutes; it is `check/freer.hero`'s `owned_parameter` now, which is also what took `emit/extern_probe.hero` back under §11's 300 after the carve-out pushed it to 307.
2026-09-07 | **The `@` cell frees, and the program panel 109 was convened for runs** (M-declared-freer step 5; panel 109's conditions 4 and 8, the last two of eight). `@error: cstr owned sqlite3_free` against SQLite's `char **errmsg` declared cleanly since step 4b and freed nothing; it now type-checks, lowers, emits, runs and releases. **The shape is a HIDDEN cell, and that is the whole design**: the author's binding is a `str?` and C writes a `char *`, so they cannot be one cell — `ir/inout.hero` allocates a `cstr` of the compiler's own, null-initialises it, hands C its address, and records a `WriteBack` marked `owned` that `ir/owned_release.hero`'s `release_cells` pays after the call with the four steps the result position already used. **`release_cells` runs on every path and that is not tidiness**: the two marks are independent, so a cell release hung off the result mark's branch would be skipped for exactly `sqlite3_exec`, which returns an `i32`. **The blocker step 4b named was real**: `lower_args` and `with_receiver` gained a callee and a position, threaded from **nine** call sites — the note had priced six, and the four that construct a record, a case, `ok`/`fail` and a built-in were not on its list. **The emitter owed one line and only running it found which**: a `cstr` cell's address is `const char **`, and against `char **` that is `discards qualifiers in nested pointer types` under this compiler's own `-Werror`, so a marked `@cstr` is spelled `(char **)&cell` — step 4b's probe mechanism one level up, and it has to match the probe exactly or the two ask clang different questions. **Measured on this Mac over 50,000 refused statements, peak resident size, three runs each**: the binding that asks for the message and never frees it is 4,587,520 / 4,571,136 / 4,587,520 bytes, the marked one **2,981,888 three times** — 1.60 MB apart, and the marked arm is the one that also copies every message into a Heroes `str`. **Three `internal error`s at exit 2 were repaired on the way**, because a mark naming a freer no `extern` of this module declares, or one of the wrong arity, reached clang as generated C: `unknown_freer` and `freer_arity` are read from the declarations alone, which is panel 109's condition 1 and the half the probe structurally cannot ask — a name nobody declares has no probe. **And one claim this session wrote was falsified by running it**: *delete the null store and the run case goes red* is false — `run` **0 failed**, `ir` **1**, `emission` **3** — because the C local is indeterminate and held zero, which is panel 116's NULL-guard finding one level over, and the three comments that said otherwise now say what was measured.

2026-09-06 | **A row enters the chain for what a stopped program shows, and half of the promise turned out to already work** (author instruction — *"I would like to add a step that implements a debugger for the Heroes language, a first thinking over all the possibilities, and then tell me as well where you want to put it in the roadmap."* Three choices were put with a recommendation and the author took all three: the id `M-typed-inspection`, row **49** after M-panic-location, and opening the `assert` defect below in `DEFECTS.md` rather than carrying it only as a witness). **Measured before the row was written, with lldb in batch mode on this Mac**: a breakpoint on a `.hero` line resolves and is hit, `bt` names Heroes frames at `.hero:line`, a local carries the author's own spelling behind an index (`h3_base = 7`), and a record shows its fields — the line half was repaired at M-selfhost-port and works. Four things do not: `p p` is `error: use of undeclared identifier 'p'` because the C name is `h0_p` and lldb's parser is C++; a `[T]` and a `{K: V}` are an opaque `HeroArrayHeader *`; a `T?` prints **both** arms including a garbage `err` half under a hashed type name; and `frame variable` dumps **139** locals in one blessed emission (111 named, 28 temporaries) against **247** in `syn/expr.hero::compared`, because §7 hoists every local and `frame variable` has no name filter. **Two findings placed the row rather than the symptom list.** The promise has no instrument: `design.md:616` and `docs/ROADMAP.md:452` both say a golden runs lldb and asserts a breakpoint is hit, that golden is `archive/bootstrap-rs/heroes-cli/tests/golden.rs`, nothing has built that tree since M-bootstrap-archive on **2026-08-19**, CI installs lldb on the Linux leg for it and runs no cargo, and `tests/harness/` has no lldb suite — **eighteen days of a claim nobody executed, in a section whose own subject is a claim that had never been executed**. And design.md's consolation is false: Part 2 refuses typed inspection because *"printing rich values is still `print`'s job"*, while `print(p)` on a record is a compile error and `assert` shows both sides only for what `print` can print, so `assert [1, 2] == [1, 3]` prints the expression and **no sides**, silently, against `spec:202` — filed as defect **016**, cause `emit/abort.hero`'s `to_str_entry` gate, repair gated on M-reflection-verdict because a `to_str` derived over a record's fields is that sitting's own question. **The cheap route was RUN and not argued**: thirty lines of lldb Python read `len` from the array header, resolved the `elem` descriptor pointer to the symbol `hero_desc_str`, found the C type `HeroStr` and printed `"ada"` and `"grace"` — no compiler change, no runtime change, memory reads only — and `nm` shows a user type links as `_h_desc_Room_desc`, so the descriptor's own symbol IS the type name `HeroDesc` does not carry, which refuses a `name` field and an ABI bump before anybody proposes them. **No panel: scheduling changes no language** (the 2026-09-03 precedent); what the milestone's opening sitting must rule is the mechanism, §10's reading of a formatter the build emits and nobody types (Rust's `rust-lldb` is a wrapper script, which §10 refuses by name), and the Part 2 amendment. `ROWS` **62 -> 63** and `REFUSED` gains **three** names rather than the usual two, because the author was offered three candidates: `M-debugger` (one word, appropriates a topic, claims a closed milestone's half), `M-stopped-values` (annexes M-reflection-verdict) and `M-frame-values` (the compiler's word for the place, not the author's for the thing). **Re-checked 2026-09-07 by author instruction** (*"re-evaluate now, after other people's commits"*) after ten commits from a peer session closed M-declared-freer: rows 48 and 49 had not moved, one staleness this commit was going to repair had already been repaired by that session (§ Where we are's spec numbers), the line numbers had, and the defect was re-run on the new tree rather than carried.
2026-09-07 | **The author asked for annotations, and the record had already answered half of it in six words and the other half in a footnote** (author question, evening of 2026-09-06: *"The other possibility is adding annotations like Java's or Python's, though not meant as decorators: simply tags one can put on some parts of the code, which the compiler can use — so system tags, or other tags that could be defined by the user to do something. If this does not complicate things. Because I see some keywords that look exactly like decorators, such as `owned fclose`."*). **The observation is correct and it is the finding**: `owned fclose` IS an annotation, and the language has six — `owned`, `tag`, `partial`, `link`, `package`, `as` — every one a contextual word matched by its text in `selfhost/parse/` through one idiom, `cursor.slice_of(text, cursor.current_span(c)) == "<word>"`, none in `selfhost/keywords.hero`'s table of 21, each read in one position after the thing it modifies, each carrying a check (`owned` types the probe `char *` and refuses the program's own call of the freer, `partial` refuses `==` and the map key, `tag` is matched against the header), so none is inert. The shape was fixed before the first word landed — `design.md:2139-2144`, *"Do not use `@` … Reserve a keyword"* — and has been stated as a rule by three sittings separately and by none as a rule: 094 R3, 109, 114 R7. **User-defined tags are the half that complicates, and at exactly one point**: a tag the compiler does not know is either an error, and then the set is closed and not the user's, or ignored, and then a typo compiles clean — panel 114's measured Rust witness, `#[cfg(feature = "widnows")]`; for a user tag to do anything something must read it, and in Java that is run-time reflection, Part 6's Ruby row and M-reflection-verdict's own subject, while a Python decorator is a function wrapping a function and needs the closure M-closures-verdict rules on. Their one refusal on the record is `docs/panel/018-top-level-declaration-shape.md:97-100`, a rejected-along-the-way bullet this log's own row for that sitting does not name, which `docs/ROADMAP.md`'s row 41 already calls *uncitable*. **No language change and no sitting: scheduling changes no language** (the 2026-09-03 precedent). The question is homed in two places because the record had split it that same morning — a peer session moved the second-contextual-word item from M-declared-freer to M-core-packages in `docs/work/SCHEDULED.md` — so user-defined tags are M-reflection-verdict's question (iii), row 41, and the shape rule for compiler marks is M-core-packages' question (vii), row 44; joining them in one sitting was the first draft of this note and was withdrawn when the tree was re-read after ten peer commits, CLAUDE.md §15's rule about a scheduling fact read immediately before it is written. **Measured today**: `heroes measure` headroom **225**, unmoved since M-declared-freer step 2; `decorator` appears **three** times across design.md, `spec/`, `docs/panel/`, this log, `docs/work/`, `docs/ROADMAP.md` and CLAUDE.md — the prior's refusal in `spec/reserved-words.md:30` and FastAPI twice — and in no sitting; the ten peer commits `8f580c24..01a377ef` touch the question nowhere. **The honest argument for a general syntax goes on the brief rather than being left out**: CLAUDE.md §9's tooling bill paid once instead of once per word — true, and the cheap half, since the check each word carries is the expensive half and a general syntax does not pay it | design.md §1.3, §4.19, Part 6; CLAUDE.md §9, §14, §15 | —
2026-09-07 | **The version scheme is decided ahead of M-install-channels, and the number the compiler prints is a different object from the word `v1`** (author question, morning of 2026-09-07: *"let us work out how to handle releases and what number to start from: GitHub releases first and then the channels? and how do we number them? are we at 0.1, 0.9, 1.0, 1.1?"*; six choices put with a recommendation and all six taken; the rule's home is CLAUDE.md §14, *Release tags*). **Measured before a word was written, at `9b4a3494`**: `heroes --version` prints `heroes 0.0.1` and the number has never moved (`archive/bootstrap-rs/Cargo.toml:6` is already `0.0.1`); it is the first line of **213 of 213** blessed files under `tests/emission/`, which `tests/harness/suite_emission.hero:254` compares byte for byte, **and of the six `.expected` under `tests/golden/emit/`**, which the grep had printed and nobody read to the end of a 249-line output, so the net found them (6 red of 1609) after the plan had counted 213; eight more uses sit in the emission suite's own fixtures, six as the literal and two through the constant that held it, and the compiler found the two; the 37 tags are all `m-*`; `gh release list` is empty; the repository is private; the chain has 59 rows, 37 closed, M-discard-refusal open. **The confusion the question named is real and has a cause**: the project's `v1` (design.md §1.0, self-hosting, reached 2026-08-18 and printed as a status heading in `README.md`) is a milestone's name, while the world's `1.0` is a compatibility promise. Rust's announcement of 2015-05-15 and Nim's of 2019-09-23 both made that promise on the day, and Zig stays 0.x to avoid making it (all three read on the web this morning). Nobody has written that promise for Heroes; `docs/ROADMAP.md`'s gate entry owes it, and the surface still moves: `as` on 2026-09-02, `owned` on 2026-09-04, four verdict milestones ahead. **Decided**: semantic `X.Y.Z` with annotated tags `vX.Y.Z`, a third namespace beside `m-*` and `site-v*`; while `X` is 0, `Y` moves when `spec/heroes-spec.md` differs between the two release commits and `Z` when it does not, a `git diff` rather than a judgement; the promise of 0.x is one sentence (*before 1.0.0 the language may change between minor versions; a patch version changes no sentence of the spec*) and `1.0.0` stays M-publication-gate's, written with its compatibility paragraph; the number moves only in the tagged commit, with no `-dev` suffix, because each move regenerates the seed one generation further and rewrites the first line of 219 stored files; a release is the author's act on a clean `main` and never a milestone's; the GitHub Release carries the tag's source archive and notes and **no binary**, so the refusal at `DESIGN-LOG.md:539` stands; the notes are the tag's message, the milestone tags between two releases and the spec's diff between them, and there is no CHANGELOG file; the CI is the instrument, a `v*` tag running all three legs, asserting `--version` against the tag on each and a moved spec against `Y`, then creating the release. **The first release is `v0.1.0`, now, not at row 57**: 0.1 because the compiler compiles itself and the surface still moves, not 0.9 because 21 milestones stand before the gate, not 1.0 because the promise is unwritten; and now rather than in twenty milestones because a release procedure first run at the gate is first run at the worst moment. M-install-channels keeps the channels and loses the version question, which `docs/work/SCHEDULED.md` had said nothing else owned. **Conservative alternative, recorded so the author can still choose it** (CLAUDE.md §4): leave `0.0.1` and decide at row 57, at the cost of a first release rehearsed never. The author's part is the question and the choice to open it now; the analysis is the assistant's.

2026-09-07 | **The four work lists get one shape, and the rule that four documents stated gets an executor** (author instruction: the lists are to be more synthetic and regular — only the items with their state and their description, no surrounding prose; no log of closed items in `DECIDE.md`, since they are already in `DONE.md`; and one loud line where the items begin, so a glance says whether anything is there). One line of three fields per item, an optional body indented four spaces opening with `**Origin:**` and its date, the item region fenced between two lines of asterisks — one only in `DONE.md`, which is appended to forever. The first field is what the file's own instrument reads: the milestone in `SCHEDULED.md`, `panel NNN` in `DECIDE.md`, the number in `DEFECTS.md`. **Measured before the change**: `DECIDE.md` was 3466 bytes of which zero were items, three paragraphs narrating items it no longer held; `DEFECTS.md` had one open defect and 2753 bytes about five repaired ones, all five already in the record; `SCHEDULED.md`'s 41 items averaged **2092 characters on one line** and reached 5068, which is 96% of the file — the surrounding prose was the item, not the preamble. After: 176 characters per item line, and no item's content lost, since the words moved into the body rather than out of the file. **The executor is `records/lists` in `tests/harness/suite_records.hero`** — no `- [x]` in a live list, no `- [ ]` in the record, nothing outside the banners, the banner's count equal to the items counted — made to fail on all four shapes by hand before the commit. What the reshaping itself caught: seven dead-path citations that had passed only because a date shared their line, which re-wrapping moved off it (`records/citations` reads one line at a time), and `docs/ROADMAP.md`'s `LEARN.md` count, **319 against a measured 323**, which had survived the commit that corrected the cell beside it. **Three more files followed the same evening, each asked for separately.** `docs/work/DONE.md` keeps every one of its 413 entries untouched and gains a tightened preamble and ONE banner, since it is appended to forever. `docs/ROADMAP.md` loses **fifty lines of preamble that were the history of its own three reorganisations**, standing in front of its first line of status: the five rules those passes established stay, in five lines, and § Where we are moves from line 57 to 32. And `docs/learn/LEARN.md`'s **323** items take the shape with one rule fewer, because a ticked question stays there (`/learn`'s rule: the list is also its own record), so the check reads it with `ticked_allowed` and its banner counts the open questions; those 323 were re-laid-out by a script that reassembled each item from its parsed parts and demanded the original line back byte for byte, headers 545 → 370 characters. CLAUDE.md §3, `/decide` § 6, `/step` § 1 and `/panel`'s synthesis all state the shape; nothing else in the language, the spec or the compiler moved, so no sitting was convened (CLAUDE.md §4: the process is amended by author instruction).

2026-09-07 | **The operating contract stops being a diary: 1247 lines and 22689 tokens become 322 and 5127, the case law becomes a record, and the tree-local rules load only where they bind** (author instruction: *"study how to re-engineer CLAUDE.md to give it more order and priority, following the best practices, and to make sure every context takes in its rules and that there are no conflicting rules"*). Measured before: `CLAUDE.md` had grown 118 -> 555 -> 1139 -> 1247 lines in 35 days over 90 commits, against **3871** for `spec/heroes-spec.md`, which is the whole language, and it carried 62 emphasised amendment paragraphs. Every session loads all of it every turn, the five panel seats included. Four things followed. The 62 stories moved WHOLE into `docs/contract/case-law.md`, 69 dated entries, append-only and in `RECORD`, with the rule left in the contract citing its entry. Seven files under `.claude/rules/` carry what binds one part of the tree and load on a `paths:` match. **Twelve conflicts were measured, not guessed**, three of them live hazards: `/panel` still adopted the *conservative* resolution four days after the author struck that word; the milestone-close checklist pushed the branch, and with it the site, as a routine act; and `llm-ergonomist` carried a **3000**-token spec budget from 2026-08-04 against a ceiling of 4096 since 2026-08-10, over a spec measuring 3871, being the one seat forbidden to grep for the true number. And the hard stops got an executor: `.claude/settings.json` gains 12 deny rules and one ask, `.claude/hooks/guard_bash.py` refuses the forbidden git forms (16 of 16 blocked, 15 of 15 legitimate commands passed), `.claude/hooks/fmt_check.py` notices a non-canonical `.hero` at the moment of writing. **The section numbers §1 to §15 do NOT move, and that is a refusal**: they are cited **3280** times, **2599** inside records §14 forbids anybody to rewrite and **346** in commit bodies, and no instrument verified a section number, so a renumbering would have made every one wrong in silence. `spec/contract` now weighs the contract against `CONTRACT_CEILING` 5500 and `records/sections` resolves every live pointer into it. The target was the language's own 4096 and the honest result is 5127, written down rather than argued away.

2026-09-07 | **A path git IGNORES is not a dead citation, and the question goes to git's RULES rather than to the machine's disk** (author finding: the CI had been red for three runs and the assistant had not looked). `records/citations` forgave an ignored path by asking `git ls-files --others --ignored --directory`, which lists only the ignored paths that HAPPEN TO EXIST: `site/dist/`, `site/.cache/` and `.claude/worktrees/` exist on a machine that has built the site and made a worktree, so twelve live citations resolved there, while a fresh checkout has none of them and the same twelve read as dead. **The machine the check was written on was the only one that could not see the defect.** It is CLAUDE.md §11's own narrowing rule broken inside the instrument that enforces CLAUDE.md's citations. The repair is `git check-ignore`, which answers for a path that is not there, and the disk-based oracle is deleted rather than kept beside it. **It took three platforms and each found a different half**: Linux, in the container of the CI leg's architecture, cut twelve dead citations to five, all of them bare directory names, because `.gitignore`'s patterns are `dist/` and `.cache/` and a trailing slash there means *a directory only* while git cannot tell a path is a directory when the path is absent — so every token is asked twice, as written and as a directory. Windows then found `panic: a command line reached 256 arguments`, which is `runtime/parts/run.c`'s `HERO_RUN_MAX_ARGS`: two words per token makes 127 tokens the whole budget, so the call goes in batches of 100. Verified green on all three, on trees where the three ignored directories do not exist.

2026-09-07 | **An `assert` says which sides it can show, and the sitting's own sixth option is a walk to the differing leaf rather than a rendering** (panel 117, convened by author instruction *"when in doubt, panelize"* on open defect 016; `provisional — author ratification pending`). The spec promised both sides with no exception while the compiler printed neither for `[T]`, `{K: V}`, `T?`, a variant or a record, and **the measurement that reframed the question is that no mechanism can keep that promise for `{K: V}` at all**: two `==`-equal maps iterate in different orders (`{"a":1,"b":2,"c":3}` gives `b a c`, `{"a":9,"a":1,"b":2,"c":3}` gives `b c a`), and sorting is refused because `runtime/parts/sort.c` picks a comparison by descriptor pointer identity over twelve scalar descriptors, so a map with record keys has no order. So it was never silence-versus-promise; it was narrowing honestly versus shipping a promise false by construction. **Two options are refused with compiled evidence.** A sixth `HeroDesc` member: the FFI seat added it, and with the ABI left at 21 the seed compiles at exit 0 with **zero warnings** while 109 generated descriptors carry `render = NULL`, then the first aggregate `assert` dies `SEGV on unknown address 0x0 (pc 0x0)` with no type and no line, which is strictly worse than the defect — and the brief's claim that this costs "an ABI bump" was wrong, because panel 027 had already measured `_Static_assert(HERO_RUNTIME_ABI == N)` **blind** to a struct field. A per-type renderer: vetoed by the compiler seat because `selfhost/emit/structural.hero` is 293 code lines against §11's 300 and a renderer owes a third clause, *equal values render equal*, that the map measurement shows is false — while the FFI seat compiled that same route and approved it, so **two vetoing seats disagree and the record says so rather than smoothing it**. **The sixth option came from the historian reading Zig's source**: `std.testing.expectEqual` never renders an aggregate, it recurses to the differing leaf and prints two scalars, and its doc comment says the point out loud. It answers every objection by construction — no iteration order, so no map problem; `eq`'s own walk, so panel 076's depth answer already applies; no rendering, so no third clause; no ABI; and nothing that can go quiet, which is D's `-checkaction=context` failure mode the historian sourced. **What landed now**: the spec sentence, by reference to `print`'s set and with its restoration condition, 3871 → **3903**, net +32 against a re-measured −16 removal; and the aggregate row in `tests/harness/suite_special.hero`, which pinned `left:  4` for a scalar and **had no aggregate case at all**, so the watcher was blind exactly where the defect was. **The author overrode the spec-warden's cheaper wording on the record** (*"better robust than cheap"*), and that seat's objection stands unwithdrawn at +29 against its condition. The mechanism is scheduled at M-reflection-verdict, unrun, with three things owed before it lands.

2026-09-07 | **Panel 117 is ratified, all four parts, and the ratified route is the sitting's own sixth option** (author instruction, *"I ratify"*, the same day it was convened). The narrowed `assert` sentence stands as landed at **3903** tokens rather than the spec-warden's cheaper 3876 — the author had already taken the robust wording over that seat's condition (*"better robust than cheap"*), and the seat's objection stays on the record unwithdrawn, which is what a ratified override looks like in this project. The aggregate row in `tests/harness/suite_special.hero` becomes a ratified tripwire: it pins today's behaviour so the day a mechanism lands it goes red. **The leaf walk is now the ratified route and not a preference** — a walk to the first differing leaf that names the field instead of rendering the value, found in Zig's `std.testing.expectEqual` by the historian seat reading source rather than documentation — so a future sitting proposing another mechanism is overturning a decision and owes the argument for it. And the two refusals are ratified refusals: a sixth `HeroDesc` member, with the FFI seat's compiled `SEGV on unknown address 0x0` behind it, and a per-type renderer *for now*, whose return condition is the compiler seat's own measurement that two `==`-equal maps iterate in different orders. `docs/work/DECIDE.md` is back to zero open items, and this time the emptiness is a ratification rather than the tidiness CLAUDE.md warns about.

2026-09-07 | **The site becomes a language site: every program in `examples/` is a page on it, the nav answers a visitor's questions, and the copy loses half its words** (author instruction, morning of 2026-09-07: the site should work on a sceptical programmer who lands on it, *show me the code* being the mantra, with the examples reported ON the site and never linked out to GitHub, a hash so a changed example can be noticed, and copy that stops writing a lot to say little; then, mid-session, *seven nav items, and merge self-hosted with the panel and the Zen, and the author with the thanks*; then, on reading the first draft, *be careful about using numbers that go out of date straight away, where there are orders of magnitude use those, or greater-than and less-than*). **178 pages where there were 46.** The examples browser is generated from `examples/` at build time, so no program is copied into `site/` and nothing there can go stale: 66 examples, 54 programs and 12 gallery files, 118 `.hero` files, 18,622 lines, every count computed from the tree rather than typed. The highlighter reads the keyword table out of `selfhost/keywords.hero` and the type words out of `spec/heroes-spec.md`, so the site holds no copy of the language, and its round-trip assertion passes over **630** `.hero` files and **95,478** lines. **The site now enforces its oldest claim**: `site/README.md` had promised since it existed that every code block is a slice of a repository file and then said, in its own words, *"Nothing enforces it"*, having once credited a `check.py` that does not exist; `site/src/lib/figures.ts` is that instrument, wired into all 46 hand-written wrappers, and a drifted block is a failed build rather than a wrong page (522 blocks checked in `dist/`, 0 differences, and the check was made to fire by planting one character). **Four claims were wrong rather than stale and are repaired**: the fixpoint page's evidence was the hash `c7c0b384…` against a measured `63a786ae…`, the specification was described as 237 lines against 248, the docs landing said *twelve* chapters in one paragraph and *Thirteen* in another over a list of thirteen, and the home promised `heroes run` works *"from any directory"*, which is false because it looks for the runtime in `./runtime` relative to the working directory. **The rule that came out of the third instruction** is `site/CLAUDE.md` § A number on the page is generated, or it is a threshold: generated beats exact, otherwise a threshold in the direction the number moves, and a hash of a build is not evidence a page can keep. Measured for the copy: home 1,979 words to 758 with the average sentence from 22.3 to 14.8, `why.html` 3,066 to 1,634, the build log 4,846 to 1,517, and the nav's list 517px to 394px in English and 540 to 426 in Italian, one row of chrome above 800px and two below, never three, measured in Chrome at twelve widths in both editions. The author's part is the three instructions and the decision to merge rather than to hub; the measurements and the two defects the highlighter had are the assistant's | site/README.md · site/CLAUDE.md · site/src/lib/ · docs/work/DONE.md

2026-09-08 | **The repository opens, out of the chain's order, and the milestone that does it is a new id at row 38** (`M-open-repository`; author instruction, *"let us make a plan to make the repo public today"*, and then *"put it in as the roadmap's next step and slot it in where it goes, renumbering what follows"*). It is the mirror of 2026-09-03, when the author reversed the launch order and published the site with the repository shut, and `site/README.md` § Launch order named the price at the time: the pages that promise something reachable say plainly it is not reachable yet. That price is paid here. **A new id and not a step of M-publication-gate**, on the precedent M-thesis-harness and M-install-channels set: the gate is the outward act of a finished thing, and being readable is delivered with or without the thesis measured, the compatibility paragraph written or a channel installed, none of which this milestone touches. `M-discard-refusal` goes back to `scheduled` at row 39, which is honest because not a line of it had landed — its own entry states in the present tense that `_ = risky(0 - 1)` compiles and exits 0. **What the author asked for that GitHub cannot do**: forking cannot be disabled on a public repository (the policy covers private organisation repositories only) and there is no setting that closes pull requests, so the policy is declared in `CONTRIBUTING.md` and a pull-request template, `main` is protected instead, and the refusal states what Apache-2.0 still grants rather than implying it was withdrawn | author instruction, and a measurement that changed the shape of the answer rather than its size | CLAUDE.md §14, § Hard stops · §12 | — (no sitting: opening a repository is none of §4's five triggers) |
2026-09-08 | **The vendored-licence re-check ran against the upstreams and found the record wrong, in a direction nobody predicted** (M-open-repository; the gate has owed this since 2026-08-11 and `vendor/tokenizers/README.md` said so about itself). `openai/tiktoken` is MIT, `Copyright (c) 2022 OpenAI, Shantanu Jain`, as recorded. `anthropics/anthropic-tokenizer-typescript` — archived, last pushed 2024-03-04, whose `claude.json` is the table vendored here at matching size and sha — **states two licences in the same commit**: `LICENSE` carries the MIT permission text under `Copyright 2023 Anthropic, PBC.`, and `package.json` at 0.0.4 declares `"license": "Apache-2.0"`. `NOTICE` said MIT for both. Both are named now and **each upstream text is vendored beside the table it covers**, because a permissive licence asks that its text travel with the copy, which naming it does not do. **The disagreement is recorded rather than resolved**: resolving it means deciding on an archived third party's behalf which of its own two documents it meant, and nothing here needs that decided, since both are compatible with this repository's Apache-2.0 and the obligations of each are attribution plus text | a re-check that confirms is worth little; this one is the reason the rule says *against the upstream rather than against this project's recollection* | CLAUDE.md §12 | — |
2026-09-08 | **A number that ages leaves the README and the picture inside it, and what replaces it is a threshold rather than a fresher number** (author instruction, *"do not put exact numbers in the README, otherwise I have to update it a thousand times"*). This is `site/CLAUDE.md` § A number on the page is generated, or it is a threshold — the author's own rule of 2026-09-07 — reaching the one document the site's instrument cannot see, and it arrived with its own evidence: **six** claims in `README.md` that the tree contradicted, including a spec budget four hundred tokens stale, a milestone in flight that had closed eight days earlier, and a footer saying the domain held a *work in progress* page six days after the site went live. **The drawing had the same disease under an explicit carve-out**: `docs/assets/fixpoint-*.svg` kept three measured numbers by author decision 2026-08-31, granted on the condition that a reader could re-run them from the file comment, and nobody did; two of the three had moved, and the comment's own words were *"a graphic nobody re-measures should not carry the digit that moves"*. Rule 2 of `docs/assets/README.md` is reversed in place, with the decision it overturns kept | a threshold survives the value moving in the direction it moves; a fresh number only resets the clock | CLAUDE.md §1, §11 · site/CLAUDE.md | — |
2026-09-08 | **The chain is renumbered for the fifth time, and the citations inside the living file become names instead of new numbers** (M-open-repository; the reorders of 2026-08-25, 2026-08-26 and 2026-09-03 precede it). The insert at row 38 moves rows 38 to 59 into 39 to 60, scripted as pairs with every anchor asserted to match exactly once and dry-run before applied, and `tests/harness/suite_records.hero`'s `ROWS` moves 63 to 64 by hand. **The recommendation first put to the author was wrong and was withdrawn on a measurement**: it argued against inserting, on the ground that 127 `row NN` citations sat in append-only records, which conflated panel rows and spec-budget ledger rows with chain rows. The live chain citations at 38 or above are about a dozen in the records and thirteen in `docs/ROADMAP.md`. The thirteen are converted to the milestone's **name**, on 2026-09-03's own finding that *a reorder moves a number and never a name*, and **two of the thirteen were already pointing at the wrong row before this milestone existed**, `M-core-packages` cited at 39 while it stood at 44. The append-only citations are not rewritten: the past-tense ones stay true as they are, and this line is where the reorder is recorded | the fifth reorder is the one that should stop the sixth from costing anything | CLAUDE.md §14 · `.claude/rules/records.md` | — |
2026-09-08 | **The site counts its visitors, and the promise that said it could not is rewritten rather than kept** (author instruction, *"I would like to check the site's visits"*, then the author's choice between the two coverages once the free plan's limit was measured). Cloudflare Web Analytics is enabled on the `heroes-lang-site` Pages project, which injects the beacon itself **at the next deploy and not on the switch**; it stores nothing in the visitor's browser, no cookie, no `localStorage` and no fingerprinting, which is why no consent text is added and no privacy page is written. **The free plan forced the choice**: the zone's server-side analytics give requests, bandwidth, unique visitors and countries, while page views, visits and filtering by path or referrer host start at Pro, so which pages are read and where a reader arrives from could be had from the beacon alone. **The price was a claim, not a page**: `site/README.md` promised *"No JavaScript and no external assets, on any page, still"*, and three comments inside `site/public/style.css`, a file served publicly, repeated it; two of the three were already false before this decision existed, left from before the Astro build step and before the nav became a component. No page promises a reader anything about tracking, verified over all 180 pages of both editions, so the repair is internal text. **What the beacon cannot see is recorded beside it**: any request without a browser, `curl https://heroes-lang.org/spec.md` included, which is the gesture the home page itself offers under the buttons, so the zone stays the measure for those and § The visitor count says so | a promise that outlives the thing it described is a defect exactly as a stale number is, and here it had been one twice over before the beacon made it three | CLAUDE.md §10, §12 · site/README.md § Deployment | — (no sitting: a visitor count is none of §4's five triggers) |
2026-09-08 | **The word nobody had to invent: `_ =` stops swallowing a failure, and the valve four seats argued about turned out to be a dead C variable** (`docs/panel/118-the-missing-word-was-a-dead-variable.md`, provisional; M-discard-refusal, whose rule the author had already ruled `3a` on 2026-09-03). The census, re-measured from the tree rather than from the record: **299** discard sites, **60** of which throw away a `T?` and stop compiling the day this lands — `selfhost/` 53, `tests/harness/` 6, `examples/` 1 — with **36 of the 60 inside `test` blocks** and, of the 24 in production, **10 already inside a fallible function** where a `?` is the repair and improves them. **The sitting split two against two on whether the language needed a new form for a deliberate drop, and both sides were right inside their own branch of Principle 0**: the llm-ergonomist ran the experiment on itself and, blocked at `_ = declare(name)`, wrote `.default(0)` — a sentinel with no meaning, a worse program than the one it had been refused — while the compiler seat ran the layout instrument and found `selfhost/check/builtins.hero` at **374 of a DECIDED 374**, so the file where every built-in's type rule lives cannot take a new built-in at all, and `fallible_operation` cannot host a `()`-returning op because it returns a slot id and `()` has no slot (panel 017 C). **Then the premise both sides shared turned out to be false, and it fell to a different gate.** *The existing vocabulary reaches all 60 sites* was measured with `heroes check`; run through `heroes build`, `_ = f().is_err()` emits `warning: variable 't6' set but not used` at **every** payload type, and `tests/harness/suite_warnings.hero:24` says of itself *"THERE IS NO EXEMPTION"* — the FFI seat priced the corpus consequence at **162 passed / 0 failed becoming 161 / 1**, and the compiler seat withdrew that half of its own verdict. **The warning was never the language's, it was the emitter's**: `selfhost/emit/unread.hero:38-46` keeps a destination on every `.binary` because *"Index/Cast/Unary/Binary can ABORT and their emitters print the check WITH the assignment"*, which is sound for arithmetic, for `/` and `%`, for the shifts and for `< <= > >=` **on a float**, and false for `eq` and `ne` on a scalar. Sent back as a question rather than settled by symmetry, the seat returned the enumeration the code believes from `selfhost/emit/operator.hero:61-216` and corrected the convener three times — the orderings abort only on `f64`, so the safe set is per (operator, operand type); `&&` and `||` are not `ir.BinaryOp` cases at all because they lower to branches; and **`.eq` on an aggregate prints a runtime call, two of which carry checks** (`hero_array_require` at `runtime/parts/array.c:238-239`, and a `partial` record's deliberate `hero_panic` at `selfhost/emit/structural.hero:53-56`), so aggregates are excluded. The relaxation costs **~14 lines** in `selfhost/emit/unread.hero`, which at **183** code lines against `CEILING` 300 has no DECIDED entry and is the one file in that neighbourhood with room, leaves `selfhost/emit/inst.hero` at exactly **350 of 350** because `c` and `f` are already in scope so its line is edited and not added, and **moves no golden's bytes**, established three ways. **Three positions close, not one**, and the two new ones were found by running: a `_` **parameter** of fallible type (`function swallow(_: i64?)` with `swallow(risky(0 - 1))` checks and runs at exit 0, the error gone) and `_ = x` where the type is a type parameter, each costing **zero** migration sites — the tree holds no real `_` parameter and none of its 17 generic declarations contains a discard, `selfhost/library_source.hero` included. **No fix is `certain`, and that is measured rather than argued**: the same line's three repairs print **1**, **9** and a panic, so `?` is not meaning-preserving, `.is_err()` preserves the bug, and `.must()` aborts — the FFI seat measured `sqlite3_close` answering `SQLITE_BUSY` becoming exit 134 with the program's own message lost. **The third half of the proposal was in nobody's owed list**: `selfhost/value_errors.hero:131` tags `discarded_value`'s `_ = ` fix `certain`, and `heroes check --apply --in-place` was run and writes the exact form this rule outlaws, with `grep -l "_ = "` over all 7 `.fixed` goldens returning nothing, so no instrument watches that path. The spec clause spends **+49** of a real **132** — not the 193 `heroes measure` prints, which is defect 017, filed today — naming all three positions where the warden's own cheapest true sentence would have named one for +15, and the author's standing instruction of 2026-09-08 is that in doubt robustness beats token economy. **The departure is recorded as deliberate**: Hare 0.26.0, released 2026-02-13, introduced `_ = os::remove(path)` as *the* explicit way to ignore an error, *"so that the fact that you're ignoring the error here is more explicit"*, and this rule makes that same expression refuse; Zig ships the rule being adopted and has not retreated; and across seventeen languages searched **no language was found that banned the silent drop with no valve at all**, a negative that rests on that list.
2026-09-08 | **A discarded temporary says its silence is deliberate, and the narrower repair is thrown away for closing half the class** (M-discard-refusal step 2, panel 118 R5 as amended; the sibling half of this class is 2026-08-16's `fixedbugs-a-discarded-binding-warns-in-c`). `_ = f().is_err()` is what a program writes to drop a failure on purpose, and it is the line the next step's rule makes necessary; measured with the shipped compiler it emits `warning: variable 't6' set but not used` **at every payload type**, against a `warnings` suite that says of itself *"THERE IS NO EXEMPTION"*. The cause is not the language: `emit/unread.may_lose_its_destination` refuses to drop a `.binary`'s destination and the reason is sound, because Index, Cast, Unary and Binary can abort and their emitters print the check WITH the assignment, so the declaration and the assignment both stand while nothing reads the answer. **Two repairs were built and the first was thrown away after it worked.** It narrowed that row — an `==` or `!=` on a scalar prints an operator and nothing else, so it may drop its destination — and it measured clean: zero warnings, four dead assignments and four declarations gone from the emitted C, byte-identical on a second emission, `warnings` 162/0, `emission` 427/0, `determinism` 130/0, 589 compiler tests. **Then the shapes next to it were attacked and it closed two of four**: `_ = a == b` on a `str` and on an array still warned, before and after, because their assignment is a runtime CALL rather than an operator. What ships declares the temporary `__attribute__((unused))`, the idiom the slot half already writes, and closes all four; it keeps every abort check and every runtime call, and rests on **no premise about what another file prints**, where the narrow repair needed a per-(operator, operand type) enumeration read out of `emit/operator.hero:61-216` plus a test in that file to notice when the enumeration died. The aggregate case is why the narrow repair is wrong rather than merely smaller: `hero_array_eq` calls `hero_array_require` (`runtime/parts/array.c:238-239`) and a partial record's generated `_eq` is a deliberate `hero_panic` (`emit/structural.hero:53-56`) whose own comment says *"'should never' is exactly the premise that goes in the loud direction"* — dropping the assignment deletes both tripwires on exactly the lines nobody reads the answer of. **The convener asked the compiler seat the wrong question** — *may a `.binary` lose its destination* rather than *how does a discarded temporary avoid the warning* — and the right answer had been in the tree since 2026-08-16, which is CL-057's rule about the option set turned on the person who wrote the brief. `tests/golden/run/fixedbugs-a-discarded-temporary-warns-in-c.hero` draws **5** warnings from the seed-built compiler and **0** from this one, and `warnings` moves 162 → 163 because the case joins the bar it exists for.
2026-09-08 | **`_ =` stops swallowing a failure, in two positions and not three, and the third is stated instead because refusing it closes nothing** (M-discard-refusal step 3; `docs/panel/118`, whose adopted resolution this amends twice, on measurements taken after it was written). The witness the milestone was filed on is closed: `_ = risky(0 - 1)` compiled and exited 0 this morning and is now `error[discarded_failure]`, whose message names only the answers reachable at that site — no `?` inside `main`, because offering it sends the reader to `try_in_infallible`, and no `.default(v)` on a `()?`, because there is no `()` to write and `.default(())` is a parse error on §4.19's own ground. **No fix is `certain`, and the reason is measured rather than argued**: the same line's three repairs print **1**, **9** and a panic, so `?` is not meaning-preserving, `.is_err()` preserves the bug, and `.must()` ends the program. **THE SECOND POSITION WAS FOUND BY RUNNING**: `function swallow(_: i64?)` with `swallow(risky(0 - 1))` checked, ran and exited 0 with the error gone, and closing it cost **zero** migration sites, because the tree holds no real `_` parameter. **THE THIRD POSITION CAME OUT OF THE RESOLUTION, and that is the amendment**: the sitting had adopted refusing `_ = x` where the type is a type parameter, on the ground that it costs zero sites; then `function drop<A>(_: A)` was run and swallowed a failure **with no discard statement anywhere**, so the refusal relocates the hole rather than closing it, and the blind reader — sent back with that fact — showed the relocation goes one step further: `_ = [x]` is legal, means the same, and works on a KNOWN fallible, so `_ = [make_dir(path: p)]` defeats the rule itself. A refusal that manufactures the idiom that defeats it is worse than a refusal that is missing. **So the spec states the boundary**, +62 measured at the landing edit (`SPEC_TOKENS` 3903 → 3965, ledger row 56), and the boundary is owed with or without generics: `_ = args_checked()` drops a whole `[str?]` in one line. **The third half landed with the rule, in the same commit**: `discarded_value`'s `_ = ` fix is WITHDRAWN on a fallible rather than downgraded, because a `guess` that cannot compile is still wrong, and `heroes check --apply --in-place` was run beforehand and wrote exactly the form the rule now refuses. **63 sites repaired**, and the count corrected itself twice: 60 from the strip census, plus **2 inside test-program string literals** the strip could not see — found by the compiler's own suite going red — plus **1 in the golden corpus**. The repairs are **6** `?`, **14** `_ = e.is_err()` and **36** `assert !e.is_err()`; the last was `.must()` in the first draft and changed on a measurement, because `.must()` aborts and an abort inside `heroes test` kills the whole run instead of failing one case. **THREE DEFECTS THE INSTRUMENTS FOUND AND THE AUTHOR OF THE RULE DID NOT.** `tests/golden/check/type-parameter-needs-context` caught a **cascade**: the refusal fired on a line already carrying `cannot_infer` and printed the type as `??`, because a `T?` with a poisoned payload IS `.fallible` — which falsified a comment written an hour earlier claiming poisoning "falls out by itself", the exact shape `.claude/rules/module-shape.md` describes. `annotations` caught the rule **masking** an FFI diagnostic: `ffi_freer_type` is an emitter-stage refusal, so a checker error stops the pipeline before the probe that case exists for ever runs. And the compiler's own tests caught a substring helper that **aborts** on non-ASCII text: ten copies of that helper exist in this compiler, every one safe by an unwritten premise that the text it searches is emitted C, and a diagnostic message carries `—` and `…`. **The fixpoint holds**: the seed regenerated, `clang -I runtime seed/heroes.c runtime/runtime.c` builds in **2.78 s**, and that compiler re-emits its own seed byte for byte. Full net **1619 passed, 2 failed**, both the emission store's, blessed with `UPDATE_EMISSION=1` and their diff read: the `#line`s move by two, the tag read adds temporaries, and one of them is `__attribute__((unused)) bool t15;` — the two halves of this milestone meeting in one line of blessed C.
2026-09-08 | **A milestone is tagged only over a clean list, and the instrument that told a session how much spec it could spend was 60 tokens too generous** (author instruction, *"we could add a rule saying a step can be tagged complete if it leaves zero decide and zero defect"*; and defect 017, filed and closed the same day). **The rule is the author's and one half of it was declined with its reason.** Zero open defects is taken whole: a defect is something BROKEN and nothing depends on it staying open. Zero open decisions is taken with one exception, **named rather than the rule weakened**, because the literal form blocks every milestone that held a sitting: § 4 says a panel never blocks, so each one leaves a `panel NNN` item, and `records/verdicts` catches an unratified sitting **only while that item exists** — so the rule would have deadlocked the very mechanism built after panels 069-079 sat unratified for eight days. A ratification pending is waiting on the author, not broken. **The executor is `records/tagged`** and it reads the newest `m-*` tag's OWN COMMIT rather than the working tree, which is what makes the rule livable: a defect filed at 10am and repaired at 4pm is ordinary work, and only a tag placed over one is the mistake, so the check is silent through a milestone and speaks once at its close. A rule with no executor is § 3's own story about `DECIDE.md` (CL-044), which is why it was not left as prose. **Defect 017 is the same day's other half, and it is about the instrument this project trusts most.** `heroes measure` printed `CEILING - highest` and knew nothing of `FFI_FLOOR`, so the headroom a session reads BEFORE amending the spec said **193** where `tests/harness/suite_spec.hero` goes red at 132 — two instruments in one repository reporting different room for one document, and the looser is the one reached for first. Found by panel 118's spec-warden about its own instrument. **The repair's own first version was wrong in the direction the defect is about**: it printed *"71 usable"*, computed `CEILING - FFI_FLOOR - highest`, where the check's `>=` makes the largest green addition **70**. Caught by comparing the printed number against the check's actual condition rather than against the intention, and what shipped states BOTH SIDES OF THE COMPARISON — *"what is measured against the ceiling is 4025 and the check goes red at 4096"* — leaving no arithmetic for anybody to get wrong. **The class had a second witness and the defect's own entry demanded it**: `measure` shouted BREACH at `CLAUDE.md` for every token above 4096 while the harness holds the contract to `CONTRACT_CEILING` 5500, so the tool invited the one repair that is always wrong here, cutting a rule to make a number smaller. Each file is now judged by the ceiling that governs it, and `spec/spendable` asserts the tool's printed sums are the harness's computed ones, with a stub-compiler test that makes it fire. **That test caught two of its author's own mistakes in a row**: extending the check to the contract broke the stub that answered only the spec, and the contract's sentence carries an apostrophe, which inside `echo '…'` closes the quote and eats the rest of the line. `records` 14 checks → 15, `spec` 9 → 10, the net's own tests 121 → 122, and the seed regenerated with the fixpoint re-verified byte for byte.
2026-09-08 | **A wrong address gets a page and a 404, where the site answered 200 with its own home** (author instruction, *"fix all of it"*, on a defect found by running the post-publish list that `site/README.md` § Deployment prescribes). **With no top-level `404.html` in the output, Cloudflare Pages assumes a single-page application** and matches every unknown path to `/` at status 200, which is Pages' own documented behaviour: measured on the live site and on the two deployments before it, so the defect is older than the visitor count rather than arriving with it, and the list's own line saying *a missing page answers 404* had been false for as long as the site has been up. A page per edition now, through the same layout and fragment shape as every other page, with four links out. **Two mechanisms rather than one, because the file is both a handler and an address**: `heroes:nested-404` in `astro.config.mjs` renames `dist/it/404/index.html` to `dist/it/404.html`, the name Pages walks up the tree for, and throws rather than passing in silence if Astro ever writes it elsewhere; and both pages carry `noindex, follow` from a new `BaseLayout` prop while the sitemap filters them out, since `/404/` and `/it/404/` would otherwise answer 200 like any page. **The instrument caught the one thing prose would not have**: `tests/harness/suite_records.hero` went red on the Italian wrapper, and its own comment had predicted that, having left the 23 Italian `.astro` wrappers unexempted on the 2026-09-04 measurement that not one of them fired and promising the entry the day one did, with a measurement beside it. `it/404.astro` is the first, of 26 wrappers today, because a not-found page owes a whole sentence of description where every other page owes a title | a list of things to verify is worth nothing until somebody runs it, and this one had been carrying a false line since the site went up | CLAUDE.md §12 · site/README.md § Deployment | — (no sitting: a not-found page is none of §4's five triggers) |
2026-09-08 | **Closures are refused on cost alone, inline blocks are left deliberately unplaced, and the rule that would warn the reader is the one that does not fit** (M-closures-verdict step 1; panel 119, five seats, provisional pending ratification). **The count the sitting was owed, and design.md's own sentence about it, are both settled by measurement.** Part 8 wart 1 says the one-line helpers *"exist mostly to be passed around"* and Part 7 item 1 says closures *"will delete the handful … that currently exist only to be passed around"*; under that second, narrower wording the whole tree holds **21 declarations, 66 lines, every one in an example program**, four of the seven files being programs whose subject IS the higher-order function, and the compiler — 190 modules, 55,361 lines — holds **zero** counting every use and **one** counting production uses only. It passes a function as a value at **4 production sites**, none of them capturing, with 13 of its 17 value sites being `test` doubles. The instrument is `.func_ref` in the IR, validated **78 against 78 with zero disagreement** over the 150 modules that build standalone, and the first census was thrown away for pooling names across files and reporting 217, most of them locals called `at`, `after` and `env`. **Three vetoes and four compiled corruption classes.** `hero_desc_func.size` is 8 and a closure over one `i64` and one `str` is 32: the cast at a real `sqlite3_busy_handler` is **clang exit 0 with zero diagnostics under all fourteen flags and exit 139 at run time**; the array route is a heap-buffer-overflow with 24 of 32 bytes silently truncated; `hero_copy_func` is a bare assignment with `hero_drop_nothing`, so a captured `str` is use-after-free; and `hero_eq_func` reads 8 of 32, so either field order answers EQUAL about a case it should not. The compiler seat made the compiler count the surface rather than reading it: **282 exhaustive-match arms in 74 of 190 modules**, five DECIDED file ceilings breached, and **no legal home for the parser** — a separate module gives `error[module_cycle]`. **The reader-facing cost is the `@` cell and it is unrefusable.** A `total: i64 @ 0` captured by copy freezes at creation; a write to it is refusable and a read is not, because whether the frozen value was wanted is undecidable — and **the only draft of the clause that states this costs +118 against 71 free tokens**, which is the sitting's title. Two seats reached that class from inputs that do not overlap, the ergonomist holding only the spec. **The historian's null result came back not null**: four languages under this one's exact constraints refuse capture in writing — Zig (#229, 2020), Odin, C3, and Oberon since 1988, which self-hosts — and the single most decision-relevant fact is about the CHEAP option, `ziglang/zig#1717`, the capture-free anonymous function, **accepted in 2020 and rejected in 2023**. It also **corrected the panel's framing**: the historical hazard is capture by REFERENCE, which this language cannot express (Go 1.22 and C# 5.0 both shipped breaking changes for it, Java 8 forbade mutable capture outright), and no language that shipped capture by copy has reversed it — so the Part 6 row's falsifier deliberately rests on compiler need and a Part 11 effect rather than on copying being dangerous. And **no study in any language measures how much code closures deleted**: the literature counts sites converted and lambdas written, so this sitting's 21 is the only measured number in the room. **What the sitting found that was not on its agenda**: the spec writes `map`'s signature and no other while naming `filter`, `fold`, `find`, `any` and `all`, the four missing ones cost **+63** and therefore compete for the same 71 tokens as the closure clause, `find`'s `-> A?` being the one no reader can derive; both live statements of where a function type may cross the FFI are wrong, the real answer being **four positions refused and one admitted**; and two defects, 018 and 019, filed rather than repaired — `<` naming two of the ten types it accepts, and the compiler's own `guess` fix offering `fit_<width>(x)`, a family it shipped and withdrew. **One repair belongs to no option and is measured free**: `-Werror=cast-function-type-strict` catches the exit-139 cast where `-fsanitize=function` does not, and the seed, the runtime, the thread example and the SQLite binding all compile clean with it | `docs/measurements/020-four-sites-in-fifty-five-thousand-lines.md`; `heroes measure` 3965 of 4096, 71 free; timings `/usr/bin/time -p`, seed 3.09 s, `heroes check selfhost/main.hero` 13.17 real against 12.96 user | design.md §1.0, §1.1, §1.2, §1.6, §1.7, §1.11, §1.12, §4.13, §4.19, Part 5, Part 6, Part 7 items 1 and 12, Part 8 wart 1; CLAUDE.md §4, §12, §13 | 119 (013's condition discharged and sharpened; 039's unplaced shape reused; 043 and 017 for `fit_`)
2026-09-08 | **A signature cannot say which half `filter` keeps, so the spec buys prose and refuses the types** (M-closures-verdict step 2; panel 120, four seats, provisional pending ratification; **opened by author instruction mid-step** — *"as I told you, it must be opened right away then"* — on a finding panel 119 had produced hours earlier). **The ballot was replaced entire, and the sentence that did it is the historian's**: `function filter<A>(xs: [A], f: (function(A) -> bool)) -> [A]` is **byte-identical for keep and for reject**, so the four candidate drafts at +86, +69 and +63 bought the LOUD class — arity, return type — and bought **nothing** for the silent class the sitting was convened about. What landed is **+30 of prose**: *no anonymous functions* joins the enumeration of absences already at `spec:110-111`, the `Built-ins:` sentence gains *`filter`, keeping what the function accepts* and *`find`, the first it accepts, or an error*, and `spec:160` names `not_found` beside the one failure code the document already had. Spec **3965 → 3995**, **40** free. **Four ways the option set was mis-measured, each found by a seat, which is § RUN IT's rule that a list is a measurement too**: the spec states **two** of the seven types and not one (`range` at `:145`); `fold`'s order was **already written** at `:116-117`, found independently by two seats and refused by both as a second payment for one fact; every draft appended prose seventy lines above the place a reader meets the names, where the document's own habit is to carry the fact beside the name; and the fourth is the polarity sentence above. **THE REMOVAL WAS PRICED AND NOT TAKEN, and this is the sitting's disagreement**: the spec-warden measured `spec:108-109`'s mandatory-named-argument bullet at **−35** in isolation and in place — the rule lives entirely inside `error[needs_label]`, whose fix is `certain` and machine-applicable, panel 089's shape — which would have left the spec **five tokens smaller than it started**. The llm-ergonomist overturned it on evidence a token count cannot see: that bullet is **the conversion function from a listed signature to a written call**, and `write_file(path: str, text: str)` is a built-in every program uses whose call the spec never shows (`grep -c "write_file(path:"` is 1, the signature alone); the rule governs how a writer **names** parameters, so a reader who never learns it ships `relabel_d(a: str, b: str)`, which compiles, runs correctly and is permanently worse at every call site; the `certain` fix **entrenches** that by repairing the call site after the signature exists; and it is the only sentence saying where labels **stop**, without which the safe generalisation is *label everything* and `len(xs: rs)` fails. Its own falsifier went unmet — *"I cannot name a clause that needs 75 tokens and cannot fit in 40"* — and nobody named one. **THE HARNESS CAUGHT TWO DRAFTS BEFORE THE SUITE DID, and the second failure is worse than the first**: `tests/harness/spec_text.hero:107-136` harvests every **space-free backticked fragment** inside the `Built-ins:` sentence as an offered built-in name, so a type written `` `bool` `` there turns `spec/offered` red — and `filter` and `find` **drop out of the harness's coverage** when they sit inside a multi-word span, a check that stops checking without saying so. The landed wording harvests **byte-identically** to the previous one, nine names with `range` last. **The strongest precedent in the sitting is for the cheapest line**: Wirth's Oberon report declares its own test — silence is for what is *"derivable from stated rules of the language"* — and then obeys it in seventeen pages, spelling out that a procedure-typed actual parameter *"cannot be a predefined procedure"*, which is the same fact as `spec:116`; Heroes has no grammar production for a lambda, so the absence is not derivable, and where Go demotes fourteen refusal headings to a FAQ, `spec:3` says *"This document is the whole language"* and leaves nowhere to demote to. **And R1 is what panel 119's refusal owes the spec**: that sitting refused closures the same day, its Part 6 row lives in design.md, and design.md is not what a reader gets. **Measured and run, not read**: the compiler seat proved the embedded library live by **mutation**, changing `find`'s code to `PROOF_LIVE` and rebuilding until the live compiler said so where the seed said `not_found`; `filter` keeps (`1, 2` from `[1, 2, 5, 7]`), `find` gives the first **in array order** (`4` from `[5,4,3,2,1]`), and the self-hosted compiler **never calls any of the six** — zero call sites across 190 modules, it writes loops. **Two defects filed rather than repaired**: 020, the library's `extern` constants readable from every program while its `extern` functions are walled, one match arm with no guard; and 021, a diagnostic showing a reader `#0` from six lines of ordinary generic code, in a file that states in writing that this cannot happen — and 021 fires on the exact misreading of `fold`'s order this sitting's sentence exists to prevent. **A live condition of the author's own decision is already met and is reported rather than acted on**: `vendor/tokenizers/README.md`'s 2026-08-26 ruling says to re-read it *"if the headroom ever falls below the spread"*, and the spread is 78 against 40 free | `heroes measure spec/heroes-spec.md` 3995, printing *"is 4055"*; ledger row 3995; `SPEC_TOKENS` and `LEDGER_ROWS` repinned; spec suite 10 of 10, records 15 of 15 | design.md §1.0, §1.2, §1.4, §1.6, Part 5; CLAUDE.md §3, §4, §8, §11, §12 | 120 (081's harvest lesson inherited; 089's redundancy shape priced and refused; 119's finding is its origin)
2026-09-08 | **Four diagnostics that told a reader something untrue, all found by one sitting and repaired in one step, plus the design.md the closures verdict owed** (M-closures-verdict step 3; defects 018 to 021 filed and closed the same day). **The class none of the three suites was watching: a wrong MESSAGE is not a wrong answer.** Every one of these exited 1 with a diagnostic, which is what the goldens check — and every one of them misinformed inside it. **018**: `selfhost/check/ops.hero` accepts `.int_ty | .float_ty`, eight integer widths and both floats, and handed *"`i64` or `f64`"* to `bad_operand` at three sites, so a reader holding a `u8` was told the language compares only two types and converted a value that needed no conversion; the sibling `mixed_arithmetic` had been saying the true thing all along, and fires only when the two sides DIFFER, which is why the wrong text was reached exactly by two sides of one non-ordered type — the shape panel 119's ergonomist had predicted as its third task's modal failure in every arm. **019**: the `guess` fix offered `fit_<width>(x)`, a family that **shipped** at M-sized-integers step 7 and was withdrawn for breaking panel 017's naming rule (`design.md:923`), so following the compiler's own advice produced `unknown_name`. **020**: `print(HERO_OS_OK)` printed `0` from a plain program — panel 033 R5 had walled the library's extern FUNCTIONS and `resolve/names.hero` grouped `.constant_decl` with kinds it assumed cannot be extern; the repair splits the arm, asks the declaration in hand (a constant is extern exactly when it has a header where an ordinary one has a body) **and guards `record_decl` too with no witness today**, closing the class rather than the reachable case. **021**: six lines with an ordinary generic identity function gave *"found `[#0]`"*, an index into the checker's own tables, while `check/render.hero`'s module doc claimed the case was unreachable from a user program — **and that dead premise had a test pinning `#0`**, which is how it stayed comfortable for as long as it did. **THE REPAIR OF 020 INTRODUCED A WART AND IT IS RECORDED RATHER THAN LEFT**: a constant now reaches a message that read *"an extern is never CALLABLE across a module boundary"*, and a constant is read rather than called; it says **reachable** now, the word the neighbouring module doc had used all along, and the three golden lines pinning the old one were **hand-edited, never regenerated** — `UPDATE_GOLDEN=1` does not exist. **A gap is filed rather than tapped shut**: nothing in this project watches the TEXT of a `guess` fix. `.expected` compares `check --brief`'s stderr, which carries no `fix (…)` line, and `suite_fixes` tests only `certain` fixes through `.fixed` — so 019 could ship a withdrawn name indefinitely, and the four cases written today pin every message and not one fix title. The recommendation, its own step: assert that every backticked identifier in a fix title is a name the language has. **And design.md received panel 119's verdict.** Part 7 item 1 leaves for a **Part 6 row** refused on cost alone, its falsifier resting on compiler need and a compiled representation rather than on capture-by-copy being dangerous — the historian having shown history says the opposite; item 12 leaves for the ***unplaced*** paragraph in `comptime`'s shape, with three joint return conditions, because one seat vetoed the drafted form and then named a form it would approve, so neither Part 6's permanence nor Part 7's *"lose only on simplicity"* fits. **Both numbers are kept struck rather than reused**, since Part 7's numbers are cited across the record. The 2026-08-04 paragraph that priced closures at *"~150 lines and ~60 spec tokens"* with *"no refcount interaction"* is **left standing with its correction beside it** — 282 match arms in 74 of 190 modules, +118 and +141 on two independent drafts, and `check/counted.hero:91` answering `false` for `.function_ty` — because it is what the project believed and the correction is worth more next to it than in place of it. And step 15's *"Closures, being off the closure list, wait for the fixpoint"* keeps its sentence with four words added: they waited, they were judged, they are refused | four `tests/golden/check/fixedbugs-*` cases with hand-written `#~` annotations; verified against a compiler rebuilt from `selfhost/` in 44.89 s (`/usr/bin/time -p`) rather than the seed binary; suites check 95 of 95, annotations 131 of 131, the compiler's own 591 | design.md §1.7, §4.12, §4.17, §4.19, Part 5, Part 6, Part 7 items 1 and 12; CLAUDE.md §8, §9, §11, §12 | 119, 120 (033 R5's rule widened to the kind it had missed; 017's naming rule is why `fit_` went)
2026-09-08 | **A `certain` fix repairs the defect the diagnostic names, and a fix that leaves it standing is a `guess`, however well it compiles** (`/decide`, on the compiler seat's finding at panel 118). §4.17 and panel 071 answer only the inverse case, a fix that CHANGES meaning; this is the case where meaning is preserved and so is the bug, and `.is_err()` on a discarded fallible is its witness. Read at all **23** sites in `selfhost/` that can ship a `certain` fix (53 `Fix(` sites, 22 unconditional, one conditional, one forwarder), none violates it, and the two nearest the line were already guarded by construction. **No instrument enforces it and none can** — that sitting recorded a prediction whose instrument could not exist — so it binds the writer and the reviewer, with CI's weaker half, that an applied `certain` fix must compile, as the only mechanical part | `heroes check --apply` edits programs on the compiler's word, so the one repair that must never be certifiable is the one that silences the diagnostic and keeps the defect | §4.17 | 118, 071 |
2026-09-08 | **The spec's `.must()` silence is bought and homed; the worked-diagnostic exhibit is handed to the instrument that can price it** (`/decide`, on the llm-ergonomist's appetite at panel 118). Measured: the spec writes `abort` **8 times on 7 lines** and defines it nowhere, and says *ends the program* exactly **once** in the whole document, on `exit`'s parenthetical, while three diagnostics in `selfhost/value_errors.hero` tell a reader in plain words that `.must()` ends the program. **The option set was wider than the ballot (CL-057)**: a `.must()`-local clause was the only route written down, and one definition of *abort* covering all eight sites had never been priced, so M-interpolation-verdict prices both against the 40 free tokens. The exhibit is **not deferred by a date**, which is the mechanism §1.6's record shows failing: it goes to M-thesis-harness, because *a reader who has seen zero diagnostics cannot predict one* is a falsifiable claim about metric 2, and Principle 0 admits a form on a measured Part 11 effect | a document that is the whole language describes the repair a model reaches for under pressure in three words that never say a program stops | §1.6, §2, Part 11 | 118, 120 |
2026-09-08 | **Panel 118 RATIFIED in full** (author instruction, *"all the decisions as you suggested, all a"*), and what was ratified is the resolution AS LANDED rather than as adopted: R1 closes two positions after the third was withdrawn on a run program, R5 is the `__attribute__((unused))` declaration and not the emitter relaxation that was priced and thrown away for closing two shapes of four, and R6 cost +62 rather than +49 because the clause carries the outermost-type boundary. The `+15` clause and the `ignore` built-in are declined | the DECIDE item had described the sitting in the words of its ballot rather than of its own § Corrections, and ratifying a description the record contradicts settles nothing | §1.1, §4.17 | 118 |
2026-09-08 | **Panel 119 RATIFIED in full** (author instruction, same words), so the first Part 7 item this project ever judged is refused: closures with capture leave Part 7 for a Part 6 row refused **on cost alone**, whose falsifier deliberately does not rest on capture being hazardous, and inline blocks are examined and deliberately **unplaced** rather than deferred with a date. The dated-deferral alternative was declined with both objections in front of the decision | a refusal resting on a false premise is one the next sitting overturns for the wrong reason, and §1.6's own record shows the dated condition is the mechanism that fails | Part 6, Part 7 items 1 and 12, §1.7 | 119 |
2026-09-08 | **Panel 120 RATIFIED in full** (author instruction, same words), R5 included, so the mandatory-named-argument bullet stays and the **−35** removal is refused rather than merely unspent: it is not available to the next sitting that goes shopping for a removal, and its falsifier stays live and unmet. The spec stands at 3995 with 40 free | that bullet is the conversion function from a listed signature to a written call, and the rule governs the WRITER, so a reader who never meets it writes a program that compiles, runs correctly and is permanently worse at every call site | §1.6, §4.17 | 120 |
2026-09-08 | **`records/tagged` reads the origin field and not the substring `panel `, so a sitting's FINDING no longer passes as its ratification** (`/decide`, found by reading the executor while zeroing the list; author instruction to repair it at once). It binds only on a tag whose own tree carried the rule, decided by asking `git show <tag>:tests/harness/suite_records.hero` for a marker rather than by a constant naming the grandfathered tag, and the executor unreadable at a tag is a reported failure rather than either answer | CLAUDE.md § Verification always said *not a `panel NNN` ratification* and the check asked for a substring, so the contract and its executor disagreed in the one direction that is invisible: the check passed. `m-closures-verdict` was placed over three findings and one session settled all three | — | 118, 119, 120 |

2026-09-08 | **The pressure interpolation would relieve is in the emitter and the printers, not in the diagnostics, and the cheapest hole rule leaves half the corpus where it is** (M-interpolation-verdict step 1; `docs/measurements/021-what-would-stand-inside-a-hole.md`). The instrument is `heroes parse --dump-ast`, whose fully parenthesized re-print settles precedence and line breaks, and it reads **308 of 308** files because `parse` resolves no `use` — the coverage `docs/measurements/020-four-sites-in-fifty-five-thousand-lines.md` could not have, since `--dump-ir` speaks for 150. Validated against a second instrument that knows nothing of parentheses, the token stream: **2985** against **2982**, three files, one shape in all three, and the AST is right because a string literal can be the receiver of a UFCS call (`selfhost/print/fmt.hero:89` builds padding with `" ".repeat(indent.to_u64().must())`, so the operand is the call). **1461** chains hold **2536** holes, of which only **273** need a rendering at all. The ladder: a bare name admits 56.9% of holes and rewrites 48.7% of chains, a postfix run 99.7% and 99.5%, and the whole distance is **seven** holes, six arithmetic and one `???`. **What the record got wrong is the sentence that read the 945 lines**: the nine diagnostic modules hold 286 holes, 11.3%, against 1241, 48.9%, in `selfhost/emit`, `selfhost/print` and `selfhost/ir`, and the densest corpus is `tests/harness/` at 4.96 chains per 100 lines, which those counts never read | the item's only available warrant is the thesis, so a comprehension argument now has to be made about the compiler's own code generator rather than about its error messages, and the place the cheap rule serves best (88.1% bare names) is the 11.3% | §1.2, §1.6, §2, Part 7 item 7, Part 11 | 006, 008 |

2026-09-08 | **The narrow hole rule costs MORE spec tokens than the wide one, and no draft of the form fits the 40 free** (M-interpolation-verdict step 2; `docs/measurements/022-the-narrow-rule-costs-more-than-the-wide-one.md`). With the opening sentence, the example and the escape rule held identical so that only the rule sentence moves: any expression **+58**, only a bare name **+59**, a name and its `.field` steps **+64**, any postfix run **+67**. The enumeration is what costs, because *"Any expression may stand in a hole"* is four words and naming the admitted forms is a list. So `docs/ROADMAP.md` § M-interpolation-verdict's *"a bare name is the cheapest rule to write and to lex"* is right about the lexer and wrong about §1.2, which prices the document: the rule 021 measured at 100% coverage is also the cheapest of the four to write down, and the one covering 48.7% of chains costs a token more. **The tightest honest clause is +46 and gets there by stating no rule at all**, leaving it to its example, which is the silence panel 120 paid +30 of prose to close in the same week. **The credit side of panel 006's trade is −3**: `print` writing one value instead of a list returns three tokens against a +46 floor, and 174 call sites in `examples/` alone. **The two scheduled silences are affordable and the form is not**: one definition of *abort* at its first occurrence covers all eight sites for **+12**, against **+29** for the local clause written at those same eight, and the printed float's round-trip sentence is **+9**, the two together **+21** with no interaction and 19 left | the sitting arrives with a priced option set instead of an appetite, and the one number that reverses the entry's own framing was invisible until the example was held constant across the four drafts | §1.2, §1.6, §2, §4.17, Part 7 item 7 | 006, 008, 012, 046, 120 |

2026-09-08 | **The brace spelling would break 24 literals and every one of them loudly, and all 24 are in the one program whose subject is interpolation** (M-interpolation-verdict step 2; `docs/measurements/022-the-narrow-rule-costs-more-than-the-wide-one.md`). Read from `heroes lex --dump-tokens` over `selfhost/`, `examples/` and `tests/harness/`: **17,707** string literals, **312** holding a brace (1.76%) in **52** files, **173** of those carrying an unpaired brace a doubling rule would rewrite, and **24** shaped exactly like a hole. Every one of the 24 is in `examples/template/main.hero`, which implements `{key}` substitution by hand. **They fail at compile time rather than quietly**: the names in them are `name`, `role`, `nobody` and `anything`, which are map KEYS, and the file declares only `render`, `render_or`, `needed`, `sample_values` and `main` and holds no binding, parameter or loop binder of any of the four, while the language has no mutable globals and forbids shadowing, so no other route into scope exists | §1.12 asks whether a migration is loud or quiet, and here three rules of the language decide it: the answer is 312 literals to review, 24 to repair, and nothing that keeps compiling while meaning something else | §1.12, Part 7 item 7 | 008 |

2026-09-08 | **Panel 121 adopts string interpolation and refuses the spelling it was asked to adopt: a brace may not be active inside an ordinary string literal** (M-interpolation-verdict, five seats; `docs/panel/121-the-brace-was-already-taken.md`). The author decided the form ENTERS and raised the ceiling to fund it, so the sitting was asked the SHAPE, and two seats vetoed on grounds the ballot had not contemplated. **The unanimous half: 211 brace-bearing literals in 39 of `selfhost/`'s 190 modules, nearly all diagnostic text naming a map type, stop parsing under an ungated brace, so the compiler stops compiling itself** — a Principle 0 regression and not a price; the historian could source **no** brace-hole language that makes a bare `{` active in an unprefixed literal, and the spec-warden found the two-stage bootstrap hazard, that the old seed reads `{{` as two braces so doubling `selfhost/` before the compiler understands doubling corrupts every diagnostic that passes through it. Gating erases all of it: a plain literal is unchanged, the 312-literal migration goes to zero, and the lone-brace silence closes. **Adopted: any expression in a hole, with WG21 P3412R3's scanner rule stated in the spec** (*"scan for the first `}` … skipping over nested parenthesis, curly brace and square bracket pairs as well as nested string and character literals"*), because the ergonomist's locality veto rests on a literal spanning lines and **the compiler refuses that** — `error[unterminated_string]: strings are single-line` — while the spec never says so, the third instance this milestone of a silence the compiler already fills. **One node carrying the whole literal's span, desugared in LOWERING**: the parser route is self-defeating because `print/fmt.hero:1266` prints a literal by span and a `.binary` node structurally, so `heroes fmt` would erase the form from the project's own tree, and the cheap payload-on-`.str_lit` route would leave 59 walks treating an interpolated string as a childless leaf with no compile error. Costed: **4113** tokens, ceiling **4253** rather than the 4224 confirmed, because 87 free was nine more than the spread and not one | a sitting asked only about the shape found that the shape it was given could not compile the compiler, and the axis that decided it, whether the literal is gated, was in nobody's ballot | §1.2, §1.6, §1.7, §1.12, §2, §4.19, Part 5, Part 7 item 7 | 006, 008, 012, 024, 046, 119, 121 |

2026-09-08 | **Two defects filed out of panel 121, and one is memory corruption at exit 0** (`docs/work/DEFECTS.md` 022 and 023, both confirmed by the coordinator on the seats' reproducers). **022**: `return ("a" + n.to_str()).cstr()` builds with zero diagnostics under all fourteen flags and hands C a pointer into freed memory — `strlen` prints **0** where 17 is the answer, three runs of three, and `--sanitize` says `heap-use-after-free`, READ of 18, `freed by hero_release_block alloc.c:151` — while the same line with a literal is sound, because `HERO_STR_STATIC` gives a literal static storage and an immortal refcount. `spec:253` promises `.cstr()` lends a string *"for that call"*; returned, the `cstr` outlives its owner slot. **023**: `"count " + n` gives `mixed_arithmetic` whose `fix (guess)` offers `to_f64(x)` and `to_i64(x)`, and following it reproduces the identical diagnostic with `str` and `f64` while `n.to_str()`, the repair that works, is never named. Both predate the sitting; 022 blocks the tag, because robustness is rank 3 in CLAUDE.md § Precedence and a milestone is tagged only over a clean list | the seat that found 022 did not veto over it, and the coordinator ran the warden's steelman expecting to confirm a mistake and found the compiler advising a loop out of it | §1.12, §4.17 | 121 |

2026-09-09 | **The spec has never been inside its own ceiling, because the binding instrument was OpenAI's** (author's finding, author's key, author's question; `docs/measurements/023-the-instrument-was-not-the-readers.md`). `heroes measure` takes the larger of two VENDORED tokenisers, and the larger is `cl100k_base`, which is **OpenAI's** and which Anthropic's own documentation says undercounts Claude tokens by 15-20% on typical text and much more on code. Measured against `POST /v1/messages/count_tokens`, the request structure accounted for at **6** tokens by two independent probes: the spec is **4128** on Haiku 4.5 and Opus 4.6, which is **32 over** the hard 4096, and **5094** on Opus 5 and Sonnet 5, which is **998 over**. The two pairs agree exactly inside themselves, so this is two tokeniser GENERATIONS and not noise, and the gap between them is **966** tokens against the 78-token `spread` this project has used as its error bar all along. `CLAUDE.md` is **7190** against a `CONTRACT_CEILING` of 5500. So the 40 free tokens never existed, every ceiling number computed on 2026-09-08 is void as arithmetic — the 4224 confirmed, the 4237 recommended, panel 121's 4253 — and the clause deltas were understated by the same factor: the `f"…"` clause is **+151** where 022 read +118 and the `\{e}` clause **+126** where it read +99, so the ranking survives and the cheaper spelling is still cheaper, by 25 rather than 19 | §1.6 already said the reader's real tokeniser was unpublished and that the spread therefore applied to the ceiling; the sentence was right and the spread it warned about was an order of magnitude short, which is why the coordinator spent an evening computing a ceiling to the token on a number that was 27.5% low | §1.2, §1.6, §2 | 012, 024, 030, 086, 121 |

2026-09-09 | **The ceiling rises to 6144 TOKENS, the unit stays tokens, and the number must name its tokeniser** (author decision 2026-09-09, arrived at through a premise the author corrected himself: *"ho confuso bytes con token"*). The arc is worth the record because the correction is the finding. The author opened at **6 KB**, for two reasons that stand whatever the unit: it is easier to state as a claim on the site, and it stops binding the language to choices that are too cheap now that it is in the home stretch. Measured, 6 KB of BYTES was a **cut of 6718** on a 12,862-byte document, more than half of it, so the coordinator priced the byte route at **16 KB** and recommended it; the author then corrected the premise rather than the number, and the unit stayed. **6144 tokens against today's real 5094 leaves 1050, or 989 net of the 60-token FFI floor and the excluded bound**, and the interpolation clause at +126 real tokens is 12.7% of that. **Tokens are the right unit and bytes were only ever attractive because the token number was unreliable**: §1.6's argument is what the MODEL pays, and the cure for an untrustworthy count is to name the instrument, not to change the question. So the ceiling carries its tokeniser and its date, or it repeats 023 the next time a vendor ships a generation. **And the budget now has a FLOOR from the world, which nobody here had looked for**: Anthropic's minimum cacheable prefix is 512 tokens on Opus 5 and the Fable family, 1024 on Opus 4.8 and Sonnet 5, 2048 on Opus 4.7, and **4096 on Opus 4.6, Opus 4.5 and Haiku 4.5**, below which a prompt silently does not cache at all. The spec IS the prompt, a cache read costs about a tenth of base input, and the spec measures **4128** on Haiku 4.5, **32 tokens above that floor**. So 4096 was never a ceiling on that side; it was the line under which this project's own prompt stops being cacheable, and had the ceiling been honoured on the real instrument it would have cost that | the spec lives in a BAND, above 4096 to stay cacheable and below 6144 to stay comprehensible, and both ends are now measured or sourced rather than chosen; the byte figures are kept in 023 as the priced route not taken | §1.1, §1.2, §1.6 | 012, 024, 121 |

2026-09-09 | **The spelling is the `f` prefix after all, on the author's own ground: it is the standard one** (author decision 2026-09-09, *"preferisco l'f-string alla Python, è più standard"*). This ratifies `docs/panel/121-the-brace-was-already-taken.md` R3 as the sitting adopted it and closes the two rival spellings the day produced: the author's own `\{e}`, which the coordinator had recommended at **-25** real tokens, and the compiler-engineer's `\(e)`. **What it buys is procedural and it is worth more than the tokens**: neither rival adds an escape, so `docs/panel/008-escape-sequences.md` R3 is not triggered and no second sitting is owed. Re-measured on the real instrument (023): the `f"…"` clause is **+151** against the `\{e}` spelling's **+126**, and against the 6144 ceiling's 989 free that difference is 2.5% of the budget, where on 2026-09-08's false arithmetic it had been the whole argument. **And the author's reason is the ergonomist's own axis**: standard means the shape a reader has met most, which is the only warrant this item has, and it is why the coordinator had already declined Swift's `\(e)` in favour of braces inside a gated literal | three spellings were live at once and each was cheapest on a different scale — tokens, escapes, familiarity — so the record says which scale decided, and it was the one the thesis rests on rather than the one the budget measures | §1.1, §1.6, §4.17, Part 7 item 7 | 008, 121 |

2026-09-09 | **Panel 122: the lend rule is a rule of POSITION and never of type, and the defect that opened the milestone was two defects** (M-cstr-lifetime, five seats, convened by the author in one word after the coordinator put a fork resting on its own bad measurement; `docs/panel/122-the-lend-was-two-defects.md`). Three candidates went out and the resolution is a fourth. **Two seats vetoed the type reading from opposite directions and the same reading answers both**: as a rule about the TYPE `cstr` it cannot tell a lend from a pointer C owns, so it refuses `c = getenv(name: name.cstr())` — compiled and run by the warden — and `owned`, an `@` cell holding a `cstr`, and `-> cstr owned free`, all of which ship. Worded on the POSITION of the `.cstr()` expression, none of them is visible to it, and the engineer built that version: **0 files of 638** move, the compiler's own **591** tests pass, `heroes check` costs **+1.3%** and an early exit makes it free. **The extern discriminator is dropped**: the ergonomist vetoed it because `cio.say(...)` and `cio.printf(...)` are the same line whose legality is decided in another file, the historian found **no language that uses which KIND of function is called as a lifetime criterion**, and the engineer measured that it refuses **three sound sites** in the wrapper §4.19 prescribes. **Two clauses the ballot lacked, both measured free**: no non-extern function may answer `cstr` (every `-> cstr` in the tree is already inside a group), which closes a two-hop laundering a position rule alone blesses; and no `cstr` record field outside a group (none exists), which forces rewriting `selfhost/emit/gate.hero:172`, whose note currently PRESCRIBES the shape the clause refuses | the coordinator was wrong four times in this milestone and a seat caught each: 67 of 67 was 66, two of the four re-reads were not externs (one is a NEGATIVE fixture), the blast radius undercounted sites threefold, and the recommended rule had the two-hop hole | §1.7, §1.12, §4.19, §4.20, Part 5 | 061, 109, 121, 122 |

2026-09-09 | **What actually keeps a lend alive, and why no position rule can close the other half** (panel 122, measured from the emitted C by both compiling seats). One synthetic owner slot **per expression site**, released at function exit **or when its site re-executes, whichever comes first**: inside a loop the emitted C carries `t17 = h4_own4; h4_own4 = t10; hero_str_decref(t17);` in the block, so iteration *n+1* frees iteration *n*'s bytes. Function exit is an upper bound, not the guarantee, which falsifies the escape-only candidate's whole premise. **So a lend that is an argument of an extern call, where the C side RETAINS the pointer, is a use-after-free that every candidate blesses**: a `stash_put` that keeps what it is handed prints `0` where 14 is right, and SQLite's `sqlite3_bind_text(..., destructor: nullptr)` — SQLITE_STATIC, *the string is static, keep it* — gives `matched rows: 0` where 1 is right, freed at a `main.c` line with no `.hero` position at all. The discriminator is the C function's fifth ARGUMENT and both spellings are `const char *` in the header, so nothing about a Heroes position separates them. Filed as defect **024** and 022 rescoped to the escaping half; the answer is the keyword §4.19 has reserved since it was written, *"a borrowed pointer you must not touch"*, case 2 of three, of which `owned` is case 1 and landed at panel 109 | `examples/ledger/db/sqlite.hero:305-309` ships that shape and its own comment says why it is safe — *"a caller must step before it drops the string, and every caller here does, in the next line… the one place a correct-looking rearrangement would be a use-after-free"* — so the project had already written the rule down for a human, which is the definition of a rule no instrument performs | §1.12, §4.19 | 109, 122 |

2026-09-09 | **The floor was not a floor, and the vendored half was the half that never changed** (panel 123, four seats; `docs/panel/123-the-vendored-half-that-never-changed.md`). Two corrections to the coordinator's own record and one finding that reframes the instrument. **The band is withdrawn**: the coordinator told the author the spec lives between 4096 and 6144 with both ends measured or sourced, and the spec-warden refused it because a minimum cacheable prefix is a property of a request PREFIX rather than of a file — `CLAUDE.md` alone measures **5547** on Haiku 4.5, clearing 4096 by 1451 before a spec token loads, while §1 has the spec read inside a session that has already loaded the contract — because a floor makes a removal a violation and turns a forcing function into a band to centre, because a vendor price list for three legacy models expires silently and upward-invisibly, and because the margin is **31** content tokens, inside the 78-token spread the project was calling its error bar. **The offset is model-dependent**, probed twice over six ids: 6, 7 and **11**, so the older pair reads 4127 and the generation gap is **967**; and Opus 4.7 counts the same content as Opus 5 while wrapping it differently, which makes *newest* and *largest* independent axes and kills max-over-a-set as a gate. **And vendoring pinned the wrong half**: `expected_hash` content-addresses the ranks while `pat_str`, the regex that decides where text splits, lives in library code and was edited twice under the fixed name `cl100k_base` with no proof of output equivalence | the seat that would have verified the cache minimums was refused by this session's own guard, added hours earlier by author instruction — `/v1/models` answered *"the key is for count_tokens and nothing else"* — so those four numbers enter the record as a question and the fence worked against the coordinator's own panel | §1.1, §1.2, §1.6, Part 11 | 024, 069, 122, 123 |

2026-09-09 | **The budget's gate never touches the network, and the record carries the instrument's name** (panel 123 R1 to R10, provisional). Precedent is unanimous and Go states the reason in its own words: `go.mod`'s `toolchain` line exists *"for reproducibility"*, pinned data beside the identity of the tool that produced it, with `GOPROXY=off` for the offline mode — so a recorded count carrying `claude-opus-5 @ 2026-09-09` is that line. Bazel, pip and Nix converge: the network is admissible only where the answer is pre-recorded, `--refresh` being `nix-prefetch` and `pip-compile --generate-hashes` and Bazel's before-you-ship sha256; and `size-limit`, which pins an absolute threshold and records no compressor version, is the failure this project lived. **Adopted**: `SPEC_TOKENS` stays the offline vendored equality that answers *did the spec move*, one **pinned model id** binds the ceiling and moves only by author decision, `--refresh` PRINTS the constants for a human to paste with zero new externs because `cli/process.hero` already runs a named program and already reads the environment, staleness **withholds** the verdict rather than computing one, and the **2000 soft line is retired** as this change's named removal, which makes the payment rule unconditional. The 38 ledger rows are renamed and annotated on SPEC's Fair Use model and Wikimedia's *Legacy Pageviews*, never restated: read as deltas they survived re-measurement, read as levels they are void | design.md:3080-3086, Part 11 metric 1, is the ROOT CAUSE and was not on the coordinator's list of places to fix: it carries the vendored maximum as a rule and calls the vendor's undercount claim *"unsourced and contradicted by measurement"*, which is the document overriding a correct claim by comparing two instruments neither of which is a Claude tokeniser | §1.2, §1.6, Part 11 | 012, 024, 030, 086, 123 |

2026-09-09 | **`heroes measure --refresh` lands, and the key is in neither argv nor any file** (panel 123 R4 and R5, implemented; author's standing instruction of the same day, *the key counts tokens and does nothing else*). The mechanism is `--variable %ANTHROPIC_API_KEY` with `--expand-header`, which tells **curl** to read the environment itself and substitute the value inside the header it sends: the secret crosses from the environment into the socket without standing in this process's argument list, where `ps` shows it to every user on the machine, or in a config file, where the default umask makes it world-readable. **There is deliberately no fallback to `-H "x-api-key: …"`**, which works on every curl ever shipped and leaks: what this needs instead is curl **8.3.0** (2023-09-13), and an older one refuses the option BY NAME, so the failure is loud and its own message says so. Zero new externs, as R4 asked: `cli/process.hero` already runs a named program and already reads the environment, and `date +%Y-%m-%d` supplies the day this compiler has no clock for — read BEFORE any request is spent, because a count without its model and its date is not a measurement and refusing early costs nothing. The offset is measured in the same invocation from **two** probes, never one: one probe cannot tell a wrapper of 6 from a tokeniser that reads `x` as two tokens, so a difference other than 4 between `x` and `x x x x x` is a refusal rather than a rounding. **Run at landing, and it reproduced the hand-measured record exactly**: 5128, `claude-opus-5`, 2026-09-09, digest `6022a8bc2f16ca0e`, with the wrong file refused at exit 2 and a missing key refused at exit 2 | the fence the author asked for lives in `.claude/hooks/guard_bash.py` and cannot see inside a compiled binary, so what stands in for it here is that the endpoint is a CONSTANT one `grep` away and no other URL exists in this tree | §1.6, §10, Part 11 | 016, 123 |

2026-09-09 | **The remedy was missing, not the refusal: every candidate refusal was refused, and §4.19 gains a fourth case** (panel 124, five seats, four rounds for the ffi seat, provisional; `docs/panel/124-the-remedy-was-missing-not-the-refusal.md`). Three candidate rules went out for defect 024 and each came back with a veto; a fourth was the coordinator's and the seat whose sentence had suggested it vetoed that too, on measurement. **The header is why no declaration-site mark can work**: `sqlite3.h:4888` says the FIFTH ARGUMENT *"controls or indicates the lifetime of the object referenced by the third parameter"*, and its three options ARE §4.19's three reserved cases on the same parameter of the same declaration, chosen at runtime; `man 3 CURLOPT_POSTFIELDS` repeats it inside one `curl_easy_setopt`. So such a mark is either a false refusal or an empty comment, and **0 of 71 `cstr` parameters are decidable from a header** because they are all `const char *` and `const` promises no write, not no retention. Value-dependent retention **appears to be unprecedented**, sourced as an absence over thirteen systems, and what **Vala** shipped for this exact function is **two declarations for one C symbol**. The coordinator's rule — a lend living to the frame's exit rather than to its site's re-execution — fixed both use-after-frees it aimed at and was refused at **1.44 MB against 81.9 MB** at a million trips, O(1) against O(n), with a residue that is the function `examples/ledger/db/sqlite.hero:305-309` already ships. **What is adopted is a capability and not a rule**: bytes Heroes holds that C may read, a fourth case §4.19 does not name because all three of its cases are about a pointer C made. It is expressible today with `malloc` and a `ptr` parameter, both residues close on the shipping compiler, and holding costs **+1.1%** over today because the author releases inside the loop — so the 57x was the price of the COMPILER deciding, not of holding. It needs a word because the cheap spelling gives up four guarantees, one of them a segfault: `atoi(s: ptr)` with `nullptr` is **exit 139 with no message** where `cstr` gives a named panic | the ffi seat corrected its own first report before answering, CL-057: *"A's only remedy"* was an inference from an incomplete option set, it had tried `strdup` which answers `cstr` and had not tried `malloc` which answers `ptr` — and that omission was the whole difference between a class that cannot close and one that can | §1.12, §4.19, §4.20, §1.5, §1.6, §1.7, §13 | 109, 122, 123, 124 |

2026-09-09 | **Four sittings ratified in one line, and two of the questions had already answered themselves** (author decision 2026-09-09, `1a 2a 3a 4a`, meaning all four as recommended; `/decide`). Panels **121**, **122**, **123** and **124**, taking `docs/work/DECIDE.md` from four open items to **zero**. **What the skill's verify-before-asking step caught**: panel 121's R7 ceiling of 4253 is **void as arithmetic**, computed on `cl100k_base` which is OpenAI's, so there was nothing there to ratify; and the interpolation spelling had already been settled the same day by the author in favour of the `f` prefix, so it was ticked rather than put to them twice. **What it unblocked, verified rather than assumed**: panel 123's spec-warden held a live veto on any commit adding a token to `spec/heroes-spec.md` before §1.6 stated a ceiling WITH its pinned model id and date, and `design.md:256` and `:277` now state 6144 as `claude-opus-5`'s count through `POST /v1/messages/count_tokens` with the day's reading, **so the f-string clause is no longer blocked by the budget instrument**. **What each ratification costs, said plainly**: 121 overrides the warden's Principle 0 veto, since no reader has been put in front of either form and metric 2 has never run; 122's R5 agrees that a milestone closed over a defect still open, which is why `M-cstr-lifetime` carries no tag; 123 deletes a rule of §1.6, the 2000-token soft line, on the measurement that it was never once slack on the real instrument, which makes the payment rule unconditional and therefore stricter; and 124 takes a capability over four refused rules, entering on robustness at rank 3 with Principle 0 unmet and **leaving defect 024 open by its own design** | the conservative route was on the table in three of the four and was taken in none, which is what CL-040 asks the synthesis to record so the author can choose it, and the author chose robust each time | §1.6, §1.12, §2, §4.19, §4.20 | 008, 012, 024, 030, 109, 121, 122, 123, 124 |

2026-09-09 | **The guard that read freed memory, and the three clauses that replaced it** (panel 125, soundness lane, two seats, provisional; `docs/panel/125-the-guard-that-read-freed-memory.md`; M-held-bytes). The coordinator built §4.19's fourth case in the runtime first and measured a double release caught by LUCK: without ASan the panic fired only because `malloc` had left a zeroed magic word readable in a freed block, and under ASan it was `heap-use-after-free` in the guard itself. Both seats went further. The compiler seat ran the magic check against the inputs its own message named and found it **undefined on three of four** — a C literal, a C `malloc` and a stack buffer are `global-`, `heap-` and `stack-buffer-overflow` — because a magic word must dereference before it can validate, so it cannot be the discriminator for any foreign pointer; and the comment claimed three refusals where the code had two. The ffi seat found the same hole from the other side: `strchr` answers `cstr`, the same type the lease answers, so releasing an interior pointer type-checks, and with bytes that happen to spell the magic it is **`free()` of an interior pointer, exit 133, empty stderr**. **The resolution is the compiler seat's route with the ffi seat's standard**: `end_lease` takes the CELL and nulls it, the checker admits it only on a cell whose initialiser is the lease call, refuses every other write to that cell, and — the clause the coordinator added by running the seat's own table, where `g = h` then a release then a read of `g` through C was a use-after-free two clauses could not see — refuses the cell's name anywhere but as an argument of a call. Zero new types (a new `Ty` case is **175 errors across 51 files**, re-measured), zero new IR instructions, corpus cost **zero**. **The registry the ffi seat approved is refused on a measurement**: `hero_grow_kept`'s one-caller assertion FIRES on a second buffer, and widening it is Part 7.13's second allocation site; what it would have bought, a pointer laundered through C, is named as defect 024's residue rather than hidden. **The spelling is `lease`/`end_lease`**: the coordinator's `held`/`release` cost **29 errors in 11 files** by the reservation mechanism itself, and the two that landed cost zero by the same mechanism. **And the ABI stamp the coordinator moved made CLAUDE.md § Commands' first line red in the working tree**, caught by both seats: 224 lines in 222 files carry it, the seed is regenerated by a compiler built BEFORE the header moves, and all of it lands in one commit. `seed/README.md` promised a test that asserts the two numbers agree, and no such test exists anywhere but in its prose | the spec clause that states the capability alone measured **+71** real tokens and the one that landed is **+103**, because the two clauses that make a second release impossible cost 32 tokens and the coordinator had priced the cheap one first | §1.12, §4.19, §4.20, Part 7.13 | 109, 112, 116, 121, 122, 124, 125 |

2026-09-09 | **`M-interpolation-verdict` closes with the verdict and not with the form, as it was named to, and the form takes its own id** (panel 121 R10; author decision 2026-09-09 on the spelling; the ratification of 121 the same day). The ruling: string interpolation ENTERS, as `f"line {n}: {word}"` — the brace active only behind an `f`, a hole admitting any expression, one AST node carrying the whole literal's span and desugared in lowering, `{{` writing one brace. **Three counts made the ruling and two of them corrected the coordinator**: 021 found the bare-name rule would cover 56.9% of the holes real programs write; 022 found the narrow rule costs MORE spec tokens than the wide one and refuses one hole in 2536 the spec promises to accept anywhere; and the brace itself was already taken by 211 literals in 39 of the compiler's own modules, so the gate is what saved a two-stage bootstrap. **Every ceiling number the sitting computed is void**, 023: the instrument was OpenAI's tokeniser and the spec was 998 over a line it was told it was 40 under, so R7's 4253 is arithmetic on the wrong scale and the real ceiling is 6144 with its instrument named. The two spec sentences R6 accepted — one definition of *abort* covering eight sites, and the printed float's round-trip guarantee — land WITH the clause and are re-homed to `M-interpolated-strings`; the `???`-in-a-hole question is answered by R1 and enters the record. **What the implementation inherits as constraints, measured today**: a new AST node touches **85 sites in 27 files** that enumerate `.str_lit`; `grammar_expr.hero` is at 1002 of a decided 1002 and a helper module that calls `parse_expr` closes a `use` cycle (R5), so the parser's hook stays in the knot and the DECIDED number rises by exactly its lines with the reason written; `check/walk.hero` is at 1700 of 1700, `ir/flatten.hero` at 1102 of 1110, `print/fmt.hero` at 1136 of 1150. And the clause's +151 was priced on the document before the lend sentence landed, so it is re-measured before it is written | the coordinator put three routes to the author for the parser seam and the one they chose, a module of its own, cannot hold the hook that reads the holes, because R5 had already measured that it closes a cycle; the author is told so rather than surprised by it | §1.6, §4.3, §4.15, Part 7 item 7 | 008, 021, 121, 123 |

2026-09-09 | **String interpolation lands: three tokens, one node, a desugar in the lowering, and four decided ceilings moved by exactly what the knots took** (M-interpolated-strings step 1; panel 121 R1 to R5 as ratified). `f"line {n}: {word}"` lexes as `str_piece ident str_piece`: the pieces are tokens whose text says which piece they are, and the hole's own tokens stand between them lexed as anywhere else — which is what makes R1's skipping scan FREE, because `state.emit` already counts brackets and a `}` closes the hole only when the depth is back to what it was when the hole opened. `{{` is one brace, and a hole whose expression begins with `{` needs a space after the opener for the same reason Python's does. The parser makes ONE node, `interp { pieces, holes }`, with the holes as children (R4); the checker types each hole in the walk and hands the type to `check/interp.hero`, which refuses what `to_str` cannot render with `to_str`'s own words — so a map index, which answers `V?`, is refused until `.must()`; the lowering hands the hole values to `ir/interp.hero`, which chains `to_str` calls and `add`s and lets a `str` hole through untouched; every printer re-prints the literal verbatim, and `fmt` is a fixpoint on it. **The blast radius was the sitting's argument for the node and it was paid**: 68 exhaustive matches in 22 files gained an arm, seven of them real and the rest a literal group joined. **Four DECIDED rows moved by measured lines** — `grammar_expr.hero` 1002 to 1041, which confirms the compiler seat's registered prediction; `check/walk.hero` 1700 to 1708; `ir/flatten.hero` 1110 to 1114; `ast.hero` 475 to 484 — each because a helper that calls back into the knot is a `use` cycle (R5), and each with the lines named. The three helpers that needed no call back are modules of their own. **The spec grew +138 real tokens** for the clause with R1's scan stated, one definition of *abort* covering eight sites, and the printed float's round-trip; the staleness gate fired on the old digest first, as designed | the coordinator's two test programs were both wrong and the compiler was right both times — `ok` is a reserved name, and `m["k"]` in a hole is an `i64?` that the new diagnostic sent to `.must()` — and the second lexer test was wrong for Python's reason, `{{1: 2}` at a hole's edge being text | §1.6, §4.3, §4.15, Part 7 item 7 | 008, 121, 123 |
2026-09-09 | **Panel 125 ratified as adopted, and the spec's word for the forgotten lease made to say what runs** (author decision 2026-09-09, `1a 2a 3a`, `/decide`). The lease's soundness stands as the sitting resolved it: the cell is the authority and is nulled on release, a lease's name is an argument of a call and nothing else, `end_lease` takes only a lease cell, no registry (Part 7.13), spelling `lease`/`end_lease`; the conservative registry was declined. The fourth point came from the site panel the same evening: `spec:246` said *a lease nobody ends is named at exit*, and the runtime prints `panic: 1 lease(s) never ended`, a COUNT, when `main` returns and nothing through `exit()`, both run. Naming the lease would take the refused registry, so the wording moved to *counted when `main` returns*: **+4** real tokens, 5369 to 5373 on `claude-opus-5`, digest `5a9b288604a587fa`, 711 free net of the FFI floor. The example `13-lease.hero` and every site page already said the measured sentence. `m-cstr-lifetime` was first left untagged (`3a`) and then tagged at the author's word minutes later, on the commit that carries this verdict rather than on the close: a tag is the newest by creatordate and `records/tagged` reads its commit, and the close (`11bcfac2`) still held defect 024 open, so the tag stands where the list that milestone left open is clean and its message says so | a spec sentence the runtime falsifies is the one thing §12's "spec beats compiler" cannot be allowed to protect, since the compiler here is right by the sitting's own R5; the fix is the word, priced, not a mechanism the panel refused | §4.19, §1.6; CLAUDE.md §12, § Verification | 124, 125 |
2026-09-10 | **The forgotten lease aborts rather than being counted, and the exit path gets the golden that nothing had** (author decision 2026-09-10, `a`, `/decide`; panel 125's fourth point read once more by the site panel's languages seat). `spec:246` said *a lease nobody ends is counted when `main` returns*, which was true of the message and silent about the program dying; *abort* is the document's own verb, defined at `spec:71` and used for nine other runtime stops, and the lease dies the same way at exit 134. The clause reads *aborts when `main` returns, saying how many*: **+4** vendored (4206 to 4210) and **+5** real (5373 to 5378 on `claude-opus-5`), digest `527e1b762302f9ca`, 706 free net of the FFI floor, ledger row 62, seed regenerated with the fixpoint silent. The cheaper *aborts when `main` returns* was priced at +0 and refused: it drops the count, and a reader told only that it aborts looks for a name the runtime cannot give | **the payment was a prediction and it turned out to name a hole**: four run goldens named `lease(`, none of them named `exit(`, so the sentence both the specification and the site state about the `exit(code:)` bypass rested on one scratch run. `tests/golden/run/lease-open-through-exit.hero` pins the absence of the accusation, so a release that registers the gate with `atexit` turns a case red deliberately rather than absorbing a diff | §4.19, §1.6; CLAUDE.md §12, §9 | 124, 125 |
2026-09-10 | **The CI matrix widens to every push and the milestone tag stops starting a run of its own** (author instruction 2026-09-10: the complete build on all three platforms, once, and no second workflow). The narrowing to Linux between tags rested on a runner minute costing money, and that expired when the repository went public: measured on the account 2026-09-08, the gross is discounted in full on all three legs, macOS included. So `matrix.include` is the three legs unless a `workflow_dispatch` narrows it, the five steps that ran only at a tag run on every push, and `push.tags` drops `m-*` and keeps `v*`. What the widening buys is that a Windows or macOS divergence is found on the commit that causes it; what it costs is nothing, and what it removes is four full runs of one commit's work every time a session closes four milestones. The mutation score stays gated on a release tag or a request, at 20 to 30 minutes a leg for a table that came out byte-identical on all three | the trade this file described for three weeks, the third platform read at the tag rather than at the commit, was a price in dollars and the dollars are gone; the comments that explained it are corrected in place with their date rather than deleted, because they were true when they were written | CLAUDE.md §14, § Verification; `.claude/rules/platforms.md` | — |
2026-09-10 | **The golden that paid for the lease clause could not exist, and the CI's Linux leg said so within the hour** (`docs/measurements/025`). `tests/golden/run/` runs its third configuration as `run … --sanitize`, and a program that leaves a lease open through `exit(code:)` leaks by construction: LeakSanitizer reports 29 bytes from `hero_str_held` and the case is red. The case, its expectation and its blessed emission trace are removed, and the payment for the spec's +4 becomes that report — the Linux leg naming the allocation site while the runtime's own count stays silent, which is panel 125 (iii) exactly as the sitting wrote it, measured today instead of predicted | **the session's own probe was the defect, not the tool**: it ran `build --sanitize` and the binary, where the lease cell is a live local of `main` at `exit` so LSan calls the block reachable, read the silence, and wrote into the case's comment that no instrument on any platform accuses the program. Three C programs probed afterwards report their leaks in that same container, through `exit(0)` included, so LSan was working and the command was wrong. CLAUDE.md § RUN IT's third shape names this: a negative claim rests on the searcher's vocabulary, and the suite's own command was one line away in the file being written | CLAUDE.md § RUN IT, §9; §4.19 | 125 |
2026-09-10 | **The chain is judged against a written production-readiness criterion for the first time, four rows enter, and six of the ten things a production user needs turn out to be owned already** (**author decision**, out of the session that asked whether the chain was missing steps for the language to be usable for **production code that is not mission critical** at 1.0; every candidate was put with a recommendation and every one was accepted). **The finding that framed it is a measurement**: design.md defines v1 as *the language is finished for v1 when it can compile itself* (`:114`, reached 2026-08-18) and defines production readiness **nowhere** — a sweep of all 3,612 lines found no section on distribution, versioning, stability or a 1.0 promise, and no occurrence of semver, deprecation, backward compatibility, release cadence, LTS or an installer; the only `1.0` in the file is Go's (`:3202`). So the 19 scheduled rows had each been scheduled on their own reason and none had ever been measured against a standard. **The criterion is a list and therefore a measurement** (CLAUDE.md § RUN IT): ten rows, from what Go 1, Nim 1.0 and Rust 1.0 each shipped — a compatibility document, *code that compiles under 1.0 compiles under any 1.x*, and *stability without stagnation* — read against what this tree already reaches for. It lives in `docs/ROADMAP.md` § What production-ready means, by author decision, and not in design.md: the criterion is not the language. **Four rows enter, 63 -> 67**: `M-cleanup-verdict` (47), the ruling on a release bound to a scope — `defer`, `RAII`, `scope guard`, `destructor` and `finally` return **nothing about a construct** over design.md, `spec/` and all 125 sittings, while `owned <C function>` (2026-09-07) and `lease`/`end_lease` (2026-09-09) put a release obligation on every path and `?` makes an early return implicit, so the refusal is uncitable exactly as M-reflection-verdict's was; `M-arm-platform` (49), the fourth real machine, since the legs are Linux x86-64, Darwin arm64 and Windows x86-64 and **arm64 Linux has never compiled this compiler** — it is not the cross-compilation `DESIGN-LOG.md:539` refused, whose own ground *the three platforms are measured on real machines by rule* argues for a fourth machine, and its registered prediction is that `char` being unsigned on the ARM ABI and signed on x86-64 either finds a divergence in the corpus's 20 `extern` programs or shows the FFI's width rules stronger than three legs could; `M-deployable-binary` (63), what the machine that RUNS a program needs — measured `libSystem` alone on this Mac, **unrun** on the other two — with the `-O0` default of `heroes build` against `run`'s `-O2` as its one decision; and `M-compatibility-promise` (66), which gives M-publication-gate's paragraph the instrument the precedent says a promise needs, the corpus at a release tag recompiled by today's compiler, and writes the sentence §10 forces: a program cannot declare its language version, so the promise is one-directional. **Two positions were choices and both are recorded** (CL-040): M-arm-platform before M-core-packages, the robust place, where the cheap one was before M-install-channels at the price of every package binding owing a re-measurement; and M-compatibility-promise before the gate rather than inside it, because the gate is a checklist of outward acts and this is a suite. **M-core-packages was not a new row and its step order was**: the author asked for a step before M-web-framework delivering mini packages, JSON, an HTTP client and a small HTTP server in Go's shape, and that step already stood there — what the question exposed is that the web stack sat at the **end** (`net` 10, client 11, server 12, behind `os`, `time`, `math`, the byte buffer and `crypto`), so it now runs first, at no dependency cost, measured: the 100-line loopback server of 2026-09-03 needed none of the byte buffer, `htons` or `sockaddr_in`. **One dependency was corrected while the order was written**, `io`/`bufio` ahead of the server, because the package table says the server is Heroes alone over `net`, `io`, `strings` while the 2026-09-03 program read its request through `fdopen` and `fgets` directly. **The author's wrapper question became the sitting's question (ix)** rather than a decision taken here, with its settled half stated so it is not re-argued — panel 033 R5 makes a wrapper **mandatory**, since an `extern` is never callable across a module boundary — and its precedent verified rather than recalled: php.net's own manual calls `mysqli` and `PDO_MySQL` *lightweight wrappers on top of a C client library*, and PHP proposed to repair the result from inside at `wiki.php.net/rfc/consistent_function_names`, whose observable cost is `strpos(haystack, needle)` against `in_array(needle, haystack)`; that following each C library's conventions **caused** it is commentary and goes to the historian as a hypothesis (CL-018). Question **(viii)** joined it: a group's header is the authority per **module** and not per group — `selfhost/cli/process.hero:49-53` declares three `hero_os.h` functions under `extern "stdlib.h"` and compiles, because a TU includes every group's header — which is a refusal the spec promises per group and the compiler makes per module, and not a defect, since a wrong header alone is refused by `emit/ffi_declared.hero:100-137`. **CL-036's walk gained the two things that COLOUR a program**, by author instruction, and it was paid for before it was written: four days after `f"…"` landed the site's highlighter knew the form and cited `selfhost/lex_interp.hero`, while the TextMate grammar had no `f"…"` rule, admitted four escapes where `spec:76` gives five so a legal `\r` was painted `invalid.illegal`, and knew none of the six contextual marks — and **nothing judged either file**. Filed as items at M-vscode-extension rather than as a defect, by author decision, because the list would go from 0 to 1 and block the next tag while the repair touches no compiler line. **And all 42 items of `docs/work/SCHEDULED.md` were verified, merged where two were one, and ordered by the chain**, on the same instruction and under the constraint *verify, never repair*: **none had been done**, five rested on a sentence now false (the spec headroom voided by its own ledger, 247/151 hoisted locals that do not reproduce at ~119/122, a CLAUDE.md §8 citation that does not exist, a design.md claim about an lldb golden that was never made, and a thread guard that isolation had already fixed), and most of the rest carried pointers that had drifted, because design.md grew 150 to 290 lines and CLAUDE.md's §§7-11 became one-line pointers. **A line-number citation is what `records/citations` cannot judge** — it reads a path claim — so those had passed every run; three pairs merged, 42 items became 51 with the twelve this session added, and the file has an order for the first time | CLAUDE.md §1, §3, §10, §12, §14, § RUN IT, § Verification; design.md §1.11, §1.12, Part 6, Part 7.13, Part 8 warts 5, 8, 11, Part 11 | — (033 R5, 036, 056, 114 R6-R7, 119, 121, 124, 125 are what the new rows inherit)
2026-09-10 | **The section nods become links, and the tracklist that is their key is generated rather than kept by hand** (out of the session that asked whether a nod could play the song it names). Playing it in the page was refused on the rule the visitor-count row established: nothing the site serves writes to the visitor's browser, which is why no consent text and no privacy page exist, and an official embed writes state on its own origin the moment it loads, hidden or not, and cannot be deferred to a click without JavaScript. What the site was actually missing was the KEY, not the player: twelve gold subtitles said nothing at all to a reader who does not know Bowie, against `site/CLAUDE.md` § The reader is never assumed to be an expert. So every nod links to its own row on `/about/thanks/#tracklist`, each row names the record and links to where the song can be heard, and `src/lib/nods.ts` generates the rows from the nods the fragments actually spend, because the hand-kept ledger it replaces said twenty titles against a markup carrying thirteen and pinned one of them to a section carrying no nod. Two exceptions, both from the seats: the home page's nod stays inert, `Run it` being the highest-intent heading on the site, and the nod inside `docs/errors.html`'s chapter title is withdrawn. The five site seats also found three defects that predate this work, all three repaired here: no fragment arrival on this site was visible under the sticky nav, `/project/#panel` included; the dead `ol.tracks` counter measured 3.59:1 in light and 3.44:1 in dark against a 4.5:1 floor; and that chapter nod was reached by no `.nod` selector at all, so it rendered as 2.5rem serif and contradicted the register's own rule that a chapter carries none. | — | site panel, no docs/panel record
2026-09-10 | **The site says what the language is bad at, on the page that says what it is good at** (**author decision**, on the languages seat's stated yes-condition, open for two rounds of the site panel). Every other page earns trust by admitting something: the specification page binds its own count to a test, the errors page prints the rows the compiler catches least, the generics chapter says the anonymous functions were refused and what the refusal cost. What was missing was a place where that happens before a reader has committed to a chapter, and the material existed only scattered through chapters 8, 11 and 14. Four costs now sit on `/why/` between the language rules and the common questions, each one run in the session that wrote it: there is no standard library and the built-ins are one line of the specification; a thread takes an `i64` and gives one back, and a list inside a callback's type is `error[ffi_type]` before the program runs, with a note naming what to hand C instead; past the C boundary clang checks the declarations against the header and nothing checks which pointer comes back or how long a library keeps one; and some mistakes no check catches, which is why the errors page carries a measurement rather than a promise. | — | site panel, no docs/panel record
2026-09-10 | **The sitemap says when each page last changed, and the date is read from git rather than from the clock; `robots.txt` states the site's position on the crawlers that feed models instead of leaving it to `Allow: /`** (out of the session that asked why what a search engine shows does not match the site's new form). **The premise was half wrong, and the correction is the finding**: the SEO tags were written the same day as the redesign, on the author's own instruction, and the live site serves them, so nothing on the site described the old one. What was missing was any signal to come back and look. `<lastmod>` was absent on all **184** advertised addresses, and after a redesign that took 46 pages to 186, retired five addresses per edition and moved a sixth, the one thing a sitemap is uniquely able to say was the one thing it did not say. **Build time was refused as the source**: a date stamped at deploy claims 184 simultaneous edits on every push, which is a field carrying no information, and a date that is sometimes wrong is worse than one that is absent, because the absent one can still be added. The source is the last commit touching the page's OWN sources, its prose fragment and its wrapper, and never the shell, since `site/src/layouts/BaseLayout.astro`, the nav and the footer touch every page and folding them in would move all 184 dates together, which is the build-time stamp wearing a different hat. Measured after: **15 distinct dates over 184 addresses**, three of them verified by hand against `git log`. **The trap is silent and cost a workflow line**: `actions/checkout` is shallow by default and `git log` in a one-commit clone answers one date for every path, so `fetch-depth: 0` is now on the checkout step in `.github/workflows/deploy-site.yml`. **And the first audit passed on the exact run it existed to catch**, which is the sharper half: measured in a `--depth 1` clone, the refusal fired, `@astrojs/sitemap` **swallowed every throw from `serialize`**, all 184 entries were dropped, and the build wrote a sitemap holding **zero URLs and exited 0**; the audit was reading an array the config filled while building, a side channel, and an empty array cleared its own test. Both moved: the history check to `astro:build:start`, where a throw is not caught, and the audit onto the built `sitemap-index.xml` and its chunks, the files that ship. `site/CLAUDE.md` § No dates on the page is **not** relaxed, and the rule was read rather than stretched: it governs what a reader is shown, a sitemap is not a page, and no date became visible anywhere (**author decision**, put with three routes and the machine-only one chosen; the routes that would have put `dateModified` in the structured data and a date beside a search result were refused) | a sitemap that cannot say what moved leaves a redesign to be found by chance, and a check that cannot see the failure it exists for is worse than none, because it is also a reason not to look | — | —
2026-09-10 | **A microcontroller enters the chain as a verdict and not as a target, RISC-V first, and the two facts a 32-bit build refuses were measured before the row was written** (**author decision**, out of the session that asked whether supporting a microcontroller such as the ESP32, or a program under an RTOS, would be worth a step, since the language compiles without a garbage collector; three recommendations put, three accepted; no board yet, the author is ordering one). **What the record said was nothing**, measured: zero hits for microcontroller, RTOS, bare metal, freestanding, newlib or ILP32 across design.md, `spec/`, `docs/` and the site in that sense, so the question went to the compiler and the runtime first (`docs/measurements/026-the-two-facts-a-32-bit-target-refuses.md`). **What came back**: the runtime asks the C library for **73** functions, fifteen of them `pthread`, eight for processes, seven for signals and the stack guard; under `--target=riscv32-unknown-elf` and `arm-none-eabi` it refuses at `runtime/heroes_runtime.h:122`, a 64-bit atomic that must be lock-free because a string literal's block is read-only, which on a device is the flash, so the header's own argument grows there rather than shrinks; the emitted C of the smallest gallery program is 189 lines with `main(argc, argv)` and includes `<math.h>`; `runtime/parts/stack.c` delivers `spec:167` through a POSIX branch and a Windows branch, and FreeRTOS has neither; a hello at `-O2` is 89,160 bytes stripped on this Mac and links libSystem alone. No Espressif toolchain is installed, Homebrew's QEMU 11.1.1 knows no `esp32` machine and Apple clang 21 has no RISC-V backend, so newlib, GCC and the device itself are **unrun**, and the row says so. **Three decisions**: the row enters now, at 64 after M-deployable-binary, whose question this is with a different answer, and before M-install-channels; it is `M-microcontroller-verdict`, a ruling on `M-cleanup-verdict`'s precedent so that it may close with a refusal, because `DESIGN-LOG.md:539` refused cross-compilation on the ground that platforms are measured on real machines, and a board is one while the 5,666,664-byte compiler cannot run on it, which is the first of the seven questions the sitting is handed; and the RISC-V chips first (ESP32-C3, C6), because upstream clang and GCC carry the target and Apple clang already type-checks for it, while Xtensa lives only in Espressif's fork, with ESP-IDF over FreeRTOS rather than bare metal, because Part 2's *not a systems language* is met to the letter when the registers are Espressif's C and every `str`, `[T]` and `{K: V}` needs `malloc`. Not Principle 0: a platform is not a form, and the thesis effect on firmware is unrun; what fits is §1.11, a language with no standard library has nothing to port. The first step is a measuring session on ESP-IDF's toolchain and not the sitting | the conservative route, a note and no row, was on the table and declined (CL-040); the `records/lists` check caught the banner of `docs/work/SCHEDULED.md` at 51 against 52 items before the commit, as it exists to; and § Where we are's count of open scheduled items said **43** against a measured 51 before this row joined, corrected to 52 with its instrument named | design.md §1.11, §1.12, Part 2; `spec:167`, `spec:228`; CLAUDE.md §4, §14, § RUN IT | 049, 114 as precedent; no sitting convened, since scheduling changes no language
2026-09-10 | **A hard stop was resting on a file that never did anything, and the file is gone with the rule repointed at what actually publishes** (**author instruction**, given after the session reported the file and declined to delete it alone). `site/public/CNAME` held `heroes-lang.org` and was a GitHub Pages artifact on a site that ships to **Cloudflare Pages by Direct Upload**, which does not read one; `DESIGN-LOG.md`'s 2026-08-30 entry had already measured that GitHub Pages was never in play, and re-measured today, no workflow under `.github/` mentions `deploy-pages`, `upload-pages-artifact`, `configure-pages` or a `gh-pages` branch. Astro copies `public/` verbatim, so the file was **served publicly at `/CNAME`** while governing nothing. **What made this more than tidying is that four LIVE documents cited it as the reason a push is asked for**: § Hard stops, the step skill's close checklist, `docs/ROADMAP.md` and `docs/contract/case-law.md` CL-042. Deleting it alone would have left four citations resolving to nothing, which is the failure `records/citations` exists to catch, so the citations moved first. **The replacement says strictly more than the sentence it replaces**: `.github/workflows/deploy-site.yml` deploys on any push to `main` touching `site/`, `examples/`, `spec/heroes-spec.md`, `selfhost/keywords.hero` or the workflow itself, so a push that touches only `examples/` publishes the site as well, and a session reading the old sentence would have thought that safe. **CL-042 was corrected underneath and not over**, per §14: its conclusion was right and only its reason was wrong, so the false sentence stands with today's date beside it. **The contract's own budget was the binding constraint on the wording**: `heroes measure CLAUDE.md` read **12** tokens of headroom against the 5500 ceiling before and **9** after, and the first draft of the replacement spent 7 of the 12, so it was rewritten shorter; the named removal for the three it does spend is the CNAME sentence itself | a rule whose stated reason is false teaches the wrong thing even while it is obeyed, and this one understated its own scope: the artifact it named could be deleted with no effect, while the mechanism it did not name publishes on four paths it never mentioned | — | —
2026-09-10 | **The measuring session the microcontroller row asked for ran the same evening, and behind the first refusal there are two files, one symbol and a program that compiles** (out of the author's *installed everything Espressif*, which had installed the installer and not ESP-IDF: `eim install` was run from the session, non-interactive, and chose ESP-IDF **v6.1** by its own default with QEMU excluded; `docs/measurements/027-behind-the-first-refusal-two-files-and-one-symbol.md`). With `runtime/heroes_runtime.h:122`'s lock-free assertion neutralised in a scratch copy and the three headers newlib lacks (`dlfcn.h`, `sys/mman.h`, `ucontext.h`) stubbed empty, **all 27 remaining errors are in `runtime/parts/stack.c`**, the stack guard's POSIX surface (`sigaction` with `SA_SIGINFO`, `si_addr`, `ucontext_t`, `SA_ONSTACK`, `mmap`/`mprotect`/`munmap`, `dladdr`), plus one line at `runtime/parts/spawn.c:128`, `pthread_getattr_np`; nothing else in 6047 lines asks the device for what it lacks. The emitted C of `00-first` compiles under GCC 15.2 with **0 errors** and 6 `-Wunknown-pragmas` for the emitter's clang-only diagnostic block, and under Espressif's clang **21.1.3**, above the floor of 18, with 0 errors, which answers the row's question 4 both ways. Linked against newlib with `--gc-sections`, as ESP-IDF links (`tools/cmakev2/project.cmake:399` of the v6.1 tree), the hello is undefined on **one symbol**, `__atomic_load_8`, and ESP-IDF's own answer to it is a single global spinlock in a critical section (`components/esp_libc/src/stdatomic.c:20-22`), which sharpens question 2: the header's stated reason, a write to a read-only page, is not what that lock does, so what the refusal costs and what it protects are two sentences. The RISC-V object needs **83** outside symbols, 15 of them libgcc's because ESP32-C3 has no FPU and no 64-bit registers; the hello with its runtime and newlib is **42,351** bytes at `-O2`, read through a labelled probe stub | the two stubs and the neutralised assertion are the scratchpad's and the file says so at each use; nothing ran on a board or under QEMU; the link was `nosys` and not `esp_libc` under FreeRTOS, so ESP-IDF's own `pthread_*`, `_exit`, `sysconf` and `mkdir` show as undefined only where gc-sections is off | design.md §1.12, Part 2; `spec:167`; CLAUDE.md § RUN IT | 114 as precedent; no sitting convened
2026-09-11 | **The specification takes the shape of a Report: thirteen numbered sections in reference order, one home per rule, and the numbers as the citation anchors** (**author decision** 2026-09-10, out of the session in which the author read the document, found it disorganised, and allowed up to a tenth more tokens to buy it a form; panel 126, five seats over two evenings, provisional pending ratification; M-anchored-spec). **The measurement behind the impression, taken before a line was written**: 74 commits on `spec/heroes-spec.md` and not one heading ever added, removed, renamed or moved, so the eleven sections were v0's and every rule since had landed where it fell. `range` stood in two sections and `to_<type>` in two, `_` was explained in two halves, ten of the places a program aborts were spread over six sections, and *abort* was defined inside § Types. **The re-shaping is three movements, each measured alone**: the move, thirteen numbers and two new homes, **+50** vendored; twenty merges, **−59**, which are the row's named removals; and eight additions priced one at a time, **+209**, in all 5378 to **5598** real on `claude-opus-5`, digest `319c66850e1586bb`, 486 free net of the FFI floor. **The homage the author asked for is the order itself**: the historian verified the Report form from Naur's 1960 Algol Report, whose index says all references are given through section numbers and whose 1963 revision kept every one, through Wirth's Pascal, Modula-2 and Oberon reports to Scheme's, and found the vocabulary before the types in every one of them; the draft had frozen the reverse, so Literals and Types were swapped on the evening before the numbers set. **Two seats objected.** The spec-warden objects to the two worked examples as additions nothing pays for, and the ledger's row 63 says so in those words rather than naming metric 2, which does not exist; the compiler seat's six probes repaired the new `anchors` check before it landed, and on its first run that check found three modules citing a `§ Values` the specification has never had. **What the author's own check found**: six blind readers, four inventorying one text each and two reading both unlabelled, 1166 rules in all, and nothing of the current text absent from the new one from either side (`docs/measurements/028`). | **the ergonomist's experiment falsified its own first prediction and that is what it was for**: it predicted that a positional `fail("a", "b")` would fail under the text with no example, and measured after the run, both the positional and the named form are accepted, while a user function with two `str` parameters refuses the positional one and `range(1, 4)` is refused where `xs.slice(1, 3)` is not. The compiler's own built-ins skip the check that reads a signature and the seven written in Heroes obey it, which § 9 states without exception: filed as **defect 025** rather than repaired, and the milestone is therefore not tagged | §1.2, §1.3, §1.6, §4.17, Part 11; CLAUDE.md §4, §9, §12, §14 | 012, 035, 046, 087, 120, 122, 123, 126 |
2026-09-11 | **The site allows a little JavaScript, and the rule that replaces *no JavaScript of its own* is that JavaScript is never the only way** (**author instruction** 2026-09-10, given for the specification page, whose 265-line block a reader could not select whole inside a frame thirty lines tall). One page asks for one script: about thirty lines, vanilla, generated beside the block it serves, which copy the specification to the clipboard on a click and, where the clipboard is refused, select the block and say which key to press. **Without scripts the same block selects itself whole on one click** (`html:not(.js)` and `user-select: all`), and the file is still a link and a `curl` line away, so the script makes one thing easier and is the only way to nothing. It sets no cookie, touches no storage, fingerprints nobody, loads nothing external and sends nothing anywhere, which keeps the sharper rule of the day before, *nothing the site serves writes to the visitor's browser*, true word for word and leaves the site with no consent text and no privacy page. **The rule has an executor**: `site/src/lib/scripts.ts` reads every HTML file the build writes and refuses a script that is not declared by name in its own list, that carries a `src`, or that mentions a cookie or a store; it was seen red once on a page with `document.cookie` injected before it was seen green. The copied bytes were compared with the file by `cmp`: zero differences. | the rule is written so that the no-script path is a path and not an apology, and a build can check it, because the thing that rots is a promise no instrument reads | CLAUDE.md § Hard stops; `site/README.md` § Style guide | 126 |
2026-09-11 | **Panel 126 ratified as adopted, and the two worked examples stay on a decision rather than on a measurement** (author's word 2026-09-11, `ratifica 126`). The specification's thirteen numbered sections in reference order stand with Literals before Types, each rule with one home, the numbers as the citation anchors, and all eight additions including the Failure and containers examples the spec-warden had objected to. **The conservative route was declined**: the reorder and the twenty merges alone, at 5358 real tokens against today's 5598, twenty under the document as it stood before any of this. **What the ratification settles is the payment question and nothing else**, because that is what the sitting split over: the examples are about 150 real tokens paid by nothing, they stand on the author's decision of 2026-09-10 and on this word, and `docs/measurements/010` row 63 says exactly that, naming no instrument that does not exist. The warden's counterfactual prediction, that the text without them refreshes to 5412 ± 12, is **not renewed** (panel 046 R2): with the examples kept it has no milestone to be scored at and stands as an observation. | **a ratification that leaves an objection standing is worth more than one that resolves it by argument**: the seat asked for a record that tells the truth about what pays rather than for the tokens back, and that is the form the ledger row took; the milestone stays untagged, because defect 025 is open and § Verification tags only over a clean list | §1.6; CLAUDE.md §4, § Verification | 046, 126 |
2026-09-11 | **The same-typed-argument rule reaches the two built-ins that escaped it, both halves of it, and the classic argument inversion drops from 367 survivors to 75** (**author instruction** 2026-09-11, one word, the evening panel 126's ergonomist filed defect 025; panel 127, five seats, provisional pending ratification; M-labelled-builtins). `spec § 9` has said since v0 that two parameters of one type make the labels mandatory, and `fail("a", "b")` was accepted while a user function's two `str` parameters were refused, because the check reads a label from a DECLARATION's span and a runtime built-in has none. **The hole was worse than filed**: the label was not optional, it was unverified, so `fail(nonsense: …, garbage: …)` was accepted and ignored and `xs.slice(to: 3, from: 1)` panicked, using the values in the order written while the labels said otherwise. **And the obvious repair would have killed nothing**: the mutation operator moves label and value together, so after the missing-label half every swap is a labelled call in the wrong order, and the order half is what kills it. Both halves landed, in a module of their own with the names as literals in `inventory.hero`, 86 code lines, the emitted C byte-identical and the ABI stamp untouched. **1022 call sites** relabelled, the seven in the embedded library text and the ten in the compiler's own test programs by hand. Measured before and after on the instrument that existed: **1441 of 1910 killed and 367 survivors, to 1733 and 75, with the 159 swapped `fail(` at zero**. The document cost **one** real token, +8 for § 6's shape line and example and −7 for § 11's duplicate. | **the sentence that admitted the hole cost thirty-two tokens and the repair that closed it cost one**, and the cheap wording was refused because it was FALSE: *the built-ins the compiler provides are the one exception* is wrong about `range` and `write_file`, which the rule already refuses, so the warden vetoed it on CLAUDE.md §12 and the ergonomist vetoed it for withdrawing the locality guarantee from a class of calls to save two words in one of them | §1.2, §4.9, §4.11; CLAUDE.md §12, §9 | 120, 122, 126, 127 |
2026-09-11 | **A certain fix that renamed a label kept the swap it named, and the certainty now asks the call in hand** (panel 127's compiler seat, which applied the old fix and ran the result). `copy_text(to: "b", from: "a")` was refused `wrong_label` with a fix marked `certain`; applying it renamed the label, the program compiled and printed the other answer. `.claude/rules/diagnostics-and-goldens.md` calls that a `guess` however well it compiles, and the deciding fact is a fact about the value rather than a premise about the world: does the label the author wrote name ANOTHER position of this callee? If it does the call is a swap wearing labels and the fix is a `guess`; if it names nothing, as `z` does in `Point(x: 1, z: 2)`, the position decides which field it is and the rename is the repair. Two helpers, beside the record they read. | the rule that a fix which leaves the defect standing is a guess was written on 2026-09-08 and had no executor: the one certain fix that broke it was found by a seat applying it rather than by a reader, which is what `/panel`'s *prototype it far enough that the number is real* is for | CLAUDE.md §8; `.claude/rules/diagnostics-and-goldens.md` | 071, 118, 127 |
2026-09-11 | **The ceiling was written in two places and only one moved, so every suite was green and the CI went red on all three legs** (found by the CI, 2026-09-11, minutes after the push that carried panel 127's repair). `tests/harness/suite_layout.hero` keeps the DECIDED table and, sixty lines below it, a test asserting what the lookup returns for the file nearest the ceiling: the table said **1746** and the assert said **1708**. **CL-054 is the rule and this is its witness**: the net was run after the edit and the net's own tests were not, and they are the third suite, the one that goes red when an instrument's pinned number stops matching what the instrument reads. The compiler seat of panel 127 had even named the line, `suite_layout.hero:432`, in the cost it priced. The repair is the number plus the reason beside it, because the number stays in two places on purpose: one is the ceiling and the other is the proof that the lookup reads it. | **three identical red legs are the right behaviour and the cheapest place to find this**, which is the whole argument for the matrix widening of 2026-09-10: the leg that would once have run only at a tag caught a stale pin within two minutes of the push, and the milestone it belongs to was untagged anyway | CLAUDE.md § Verification; `.claude/rules/verification.md` | 127 |
2026-09-11 | **A call through a function value is positional, and now the language says it and refuses a label that claimed otherwise; the inversion it still admits is measured at 50% and left open on purpose** (**author instruction** 2026-09-11, *convoca il panel per risolvere il difetto 026*; panel 128, five seats, provisional pending ratification; M-positional-values). **Three of the four routes were vetoed, each by a different seat on soundness**: the document's positional sentence as balloted, because `f(nonsense: "a", rubbish: "b")` printed `a/b` at exit 0 and the sentence was therefore false (CLAUDE.md §12); refusing a function type whose parameters share a type, because it forecloses §4.19's ladder at three steps and deletes a comparator that compiles today; and names inside the type, because Part 5's test makes it core, eleven modules, two of them at their decided ceiling, and it inverts a shipped `certain` fix. What landed is the route nobody had listed, built by the vetoing seat itself in 25 diff lines: `error[label_on_function_value]`, a note that says why, and a certain fix that removes the label and leaves the value where it is. **With it the vetoed sentence became true**, so § 3's row now carries it at +25 real tokens, paid by nothing, and ledger row 65 says so rather than pretending. The compiler's own note stopped prescribing the hole in silence: it says to call the comparison BY POSITION and names the roles, `less(candidate, best)`. | **the sitting's deepest finding is that the defect does not ship where the defect said**: it ships inside `fold`, where a program's callback is called positionally with no name in the chain, printing `cba` for `abc` at exit 0, and `fold`'s two distinct letters mean no route considered reaches it. Five golden sites had implied coverage and the record now says they do not | §1.7, §4.9, §4.12, §4.19, Part 5; CLAUDE.md §4, §12, § Precedence | 126, 127, 128 |
2026-09-11 | **The three sittings of the two nights are ratified as adopted, and what each conservative route would have been stays on the record beside its verdict** (**author decision** 2026-09-11, `ratifico i panel`, after `ratifica 126` earlier the same day). Panel 126: the specification keeps its thirteen numbered sections with Literals before Types, and the two worked examples stay on the author's decision rather than on a measurement, which the ledger's row 63 says in its own sentences. Panel 127: both halves of the same-typed-argument rule keep reaching `fail` and `slice`, and the 1022 call sites that grew eleven characters each stay as they are, carried by the instrument reading 1733 of 1910 killed where it read 1441. Panel 128: the small half stands and **the conservative route is not built now**, so defect 026 stays open and narrowed to the inversion, priced at 50% unchanged, with the route scheduled at M-check-completeness under the measurement that lifts its own veto. `docs/work/DECIDE.md` is empty. | **the three ratifications leave five objections and three vetoes standing on the record rather than resolved by preference**, which is the point of writing a sitting down: the warden's arithmetic about what pays for what, the ergonomist's finding that names on peers buy a label and not a check, the ffi seat's three measurements where the swap reaches memory, and the compiler seat's price of eleven modules are all still there to be read by whoever builds the scheduled route | §1.6, §4.9, §4.12; CLAUDE.md §4, §12, § Precedence | 126, 127, 128 |

2026-09-11 · **M-labelled-types, panel 129, ratified as adopted the same day.** A
function type carries the parameter names of the positions that share a type with
another, and no others; the names are part of the type's identity; a declared
function used as a value carries its own names on the same condition; a call
through such a value is checked exactly as a call to a declaration is. Defect 026
is closed. `heroes mutate --operator swap-args` reads 1 of 6 killed before and 6
of 6 after on four programs of calls through function values, and **identical to
the mutant** over `examples/`, which holds no instance of the shape — the record
says the second number first. The document takes the spec-warden's wording rather
than the one drafted, at **+39 real, digest `0d61cc71040884f6`, ledger row 66,
paid by nothing**; the drafted pair cost +54 and one reading of it was false
against a running program. The cost in the repository's own instrument is **432
lines** against the compiler seat's condition of about 150, and that number went
to the author before the ratification rather than into a footnote.

2026-09-11 · **Three of panel 129's five seats sent the work back, and the record
keeps what each found.** The tree did not compile itself when the sitting opened —
a new function shadowed a local of its name and a failed build had been read as a
passing one, so twenty minutes of measurement had been taken with a stale binary
(CL-054, with the coordinator in the chair). The line count had been `grep`'s
rather than `code_lines`'s, which `.claude/rules/module-shape.md` already tells
every session to ask. A parameter a declaration calls `_` now contributes no name
and fits whatever the expected type calls that position, because ignoring an
argument is what a C callback does for a living. A name used at two positions of
one type is `repeated_parameter_name`, because names that do not distinguish
leave the two exactly as confusable as no names at all. And a name is written at
the confusable positions and nowhere else, in the type and at the call alike,
which is one rule where the document had left a reader three readings.

2026-09-11 · **The author answered panel 129's second question with neither of the
answers the sitting recommended.** The `fold` role inversion — a generic
callback's two parameters read the other way round, printing `cba` for `abc` at
exit 0 — was classified by the sitting as the price of design.md §4.9's criterion
rather than a defect against it. Asked to rule, the author said to build the route
that closes it: names on any function type, an expected type carrying none
accepting a value whose type does, and the generic path comparing the names it is
given. It is **M-named-callbacks**, chain row 49, with panel 129's compiler seat's
objection attached whole — that route introduces variance into a language that has
none, and it does not close the class, it lets a careful author close it one
function at a time.

2026-09-11 · **M-named-callbacks, panel 130, on the route panel 129 recommended
against and the author asked for anyway.** A function type may name any
parameter; where two share a type it must; and **the names of a function handed
to a callback type that names its parameters must agree with them**, checked in
`check/generics.hero`'s `bind`, the one path that never compares whole interned
ids. `fold`'s callback is `(function(acc: B, item: A) -> B)` now, and a program
that read its two the other way round — printing `cba` where it meant `abc`, at
exit 0, on every compiler this project had built — is `error[callback_name]`.
**Transposing the seven callbacks this repository hands to `fold` is caught 7 of
7.** The document is 5662 real, **−1**, digest `6e82771d0dbabc31`, ledger row 67,
paid by § 3's two extra arities at −20 measured alone.

2026-09-11 · **A rule was measured the way a metric is, and the number changed
the milestone.** The first version fired only where the callee reused the type's
OWN word for another position, which is panel 127's `elsewhere` test applied to a
value. Panel 130's compiler seat measured it against the seven transposed
callbacks: **2 of 7**, because five of them use their authors' vocabulary — `a`
and `b`, `so_far` and `one`, `total` and `i`. The author was given both numbers
and took agreement, which catches 7 of 7 at the price of a permanent naming
mandate on `fold`'s callers and six programs renamed once each. **It is a mandate
and not a detector**, and design.md, the diagnostic's module doc and the golden
all say so rather than letting a green suite imply a class is closed.

2026-09-11 · **Half of what panel 129 priced was unnecessary, and a measurement
said so before a line was written.** That sitting costed the route at four parts
and objected to two: a declared function naming its parameters always, and a
widening relation to carry it. Both rest on the premise that a role inversion is
invisible at any pair; it is not. With different types the swap changes the type
and `bind` already refuses it, and when they collapse to one type they share a
type, so the names already existed. What was missing was that `fold`'s own type
could not SAY what it wanted.

2026-09-11 · **Two silences the sitting names rather than closes.** A callback
whose own two parameters are DIFFERENT types puts no names in its type, so the
mandate does not bind it: `tally(item: str, acc: i64)` has its names fully
inverted against `fold`'s and prints `123` at exit 0. And a user's own generic
higher-order function whose callback type names nothing is unchecked. The
spec-warden found the first against the built compiler and refused to write the
unscoped sentence that would have denied it; design.md carries the narrowing.
