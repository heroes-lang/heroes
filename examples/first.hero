## First
#
# The smallest program worth compiling — the M5a acceptance input.
# Its hand-written C target is tools/spike/01-first.c; in M5a the emitter's
# output is compared against that file by eye.

main = function: ()
    print((2 + 3) * 4)
