7
!panic: 1 more handle(s) given back than were taken — a call marked `consumes` ran with no `acquires` behind it. Two things do this and the count cannot tell them apart, worst first: the same handle given back TWICE, which is a double release; or an `extern` marked `borrows` whose C function in fact hands ownership over, which makes the mark wrong
