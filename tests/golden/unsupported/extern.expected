unsupported[extern]: an `extern` function is not emitted yet
  at tests/golden/unsupported/extern.hero:9:1
    |
  9 | extern function labs(x: int) -> int     #~ extern
    | ^
  note: no change to this file will fix this
  note: the backend emits every type in the language, `if`, `while`, `for`, `match`, functions, and the built-ins `keys`, `len`, `print`, `push`, `slice`, `to_str` — no other built-in is emitted yet
