tests/golden/check/jump-and-unit.hero:15:11: error[no_value]: this `match` produces no value — every branch jumps, so there is nothing to bind
tests/golden/check/jump-and-unit.hero:21:5: error[bound_unit]: `done` would hold `()`, which is no value — keep the call and drop the binding
tests/golden/check/jump-and-unit.hero:23:5: error[discarded_value]: this line's value has type `[i64]` and nothing receives it — every value here is a copy, so `xs` is unchanged: write `xs @ …` to keep the result, or `_ = …` to discard it on purpose
