# UNVERIFIED — pending debrief
# Guards the Python/JS habit of a trailing comma. Heroes has one spelling per
# program (§4.15), so `f(a, b,)` and `f(a, b)` cannot both be it.
main = function: ()
    print(1, 2,)
