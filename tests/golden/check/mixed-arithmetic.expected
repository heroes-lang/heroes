tests/golden/check/mixed-arithmetic.hero:8:12: error[mixed_arithmetic]: `+` takes two of one type — two integers of the SAME width, or two floats of the same width — and found `i64` and `f64`
tests/golden/check/mixed-arithmetic.hero:11:12: error[mixed_arithmetic]: `<` takes two of one type — two integers of the SAME width, or two floats of the same width — and found `i64` and `f64`
tests/golden/check/mixed-arithmetic.hero:14:12: error[mixed_arithmetic]: `+` takes two of one type — two integers of the SAME width, or two floats of the same width — and found `str` and `i64`
