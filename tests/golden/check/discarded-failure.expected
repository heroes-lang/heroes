tests/golden/check/discarded-failure.hero:29:5: error[discarded_failure]: `_ =` would drop the error in this `i64?`, and that is the one discard the type system cannot see through — `.must()` ends the program here, `.default(v)` falls back, `.is_err()` drops the failure on purpose — `?` would need this function to return a `T?` itself
tests/golden/check/discarded-failure.hero:30:5: error[discarded_failure]: `_ =` would drop the error in this `()?`, and that is the one discard the type system cannot see through — `.must()` ends the program here, `.is_err()` drops the failure on purpose — `?` would need this function to return a `T?` itself
tests/golden/check/discarded-failure.hero:35:5: error[discarded_failure]: `_ =` would drop the error in this `i64?`, and that is the one discard the type system cannot see through — `?` sends the failure to your caller, `.must()` ends the program here, `.default(v)` falls back, `.is_err()` drops the failure on purpose
tests/golden/check/discarded-failure.hero:41:18: error[discarded_failure]: a `_` parameter written `i64?` drops its error where nothing can answer it — `_` binds nothing, so the body cannot reach the value at all
tests/golden/check/discarded-failure.hero:48:5: error[discarded_value]: this line's value has type `i64?` and nothing receives it, and `_ = …` will not do either: a `T?` may not be discarded
