tests/golden/check/end-lease-takes-only-a-lease.hero:11:5: error[end_lease_of_no_lease]: `end_lease` frees what `.lease()` made, and it takes the cell that holds it: `end_lease(@x)` where `x: cstr @ s.lease()` — this argument is not such a cell
tests/golden/check/end-lease-takes-only-a-lease.hero:13:5: error[end_lease_of_no_lease]: `end_lease` frees what `.lease()` made, and it takes the cell that holds it: `end_lease(@x)` where `x: cstr @ s.lease()` — this argument is not such a cell
