tests/golden/check/unread-mark.hero:17:48: error[unread_mark]: `acquires` on the result of `malloc` is a word nothing reads: `ptr` reaches no handle, so no rule can say which call hands the value over or takes it back, and the word could be false without anything noticing
tests/golden/check/unread-mark.hero:18:22: error[unread_mark]: `consumes` on `p` is a word nothing reads: `ptr` reaches no handle, so no rule can say which call hands the value over or takes it back, and the word could be false without anything noticing
tests/golden/check/unread-mark.hero:21:27: error[unread_mark]: `borrows` on the result of `tmpfile` is a word nothing reads: `ptr` reaches no handle, so no rule can say which call hands the value over or takes it back, and the word could be false without anything noticing
tests/golden/check/unread-mark.hero:22:64: error[unread_mark]: `acquires` on `stream` is a word nothing reads: `ptr` reaches no handle, so no rule can say which call hands the value over or takes it back, and the word could be false without anything noticing
tests/golden/check/unread-mark.hero:23:30: error[unread_mark]: `borrows` on `stream` is a word nothing reads: `ptr` reaches no handle, so no rule can say which call hands the value over or takes it back, and the word could be false without anything noticing
tests/golden/check/unread-mark.hero:26:24: error[unread_mark]: `consumes` on `s` is a word nothing reads: `cstr` reaches no handle, so no rule can say which call hands the value over or takes it back, and the word could be false without anything noticing
tests/golden/check/unread-mark.hero:29:24: error[unread_mark]: `consumes` on `fd` is a word nothing reads: `i32` reaches no handle, so no rule can say which call hands the value over or takes it back, and the word could be false without anything noticing
