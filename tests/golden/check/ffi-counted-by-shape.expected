tests/golden/check/ffi-counted-by-shape.hero:19:46: error[counted_by_shape]: `counted_by` on `p` of `not_a_pointer` names no extent — `counted_by` says how far C reads or writes through a `ptr`, and `p` is not one
tests/golden/check/ffi-counted-by-shape.hero:20:48: error[counted_by_shape]: `counted_by` on `p` of `no_such_sibling` names no extent — `m` names no other parameter of `no_such_sibling` — the extent is a sibling in the same signature, the one C reads the count from
tests/golden/check/ffi-counted-by-shape.hero:21:47: error[counted_by_shape]: `counted_by` on `p` of `not_an_integer` names no extent — `n` is not an integer C takes by value, so it cannot say how far C reads — an extent is `i32`, `u64`, or another width the header spells
tests/golden/check/ffi-counted-by-shape.hero:22:46: error[counted_by_shape]: `counted_by` on `p` of `out_parameter` names no extent — `n` is not an integer C takes by value, so it cannot say how far C reads — an extent is `i32`, `u64`, or another width the header spells
tests/golden/check/ffi-counted-by-shape.hero:27:24: error[field_lend_uncounted]: `.ptr()` lends a field's address to C with an extent this call states, and `p` of `slot_sum` does not say which argument the extent is — so nothing can check the number against the field
