tests/golden/check/ffi-type.hero:15:31: error[ffi_type]: `[i64]` cannot cross the FFI boundary, and it is an `extern`'s result — a C header can declare `i64`, `f64`, `bool`, `str`, `ptr` and `cstr`, and nothing else (§4.19)
tests/golden/check/ffi-type.hero:16:23: error[ffi_type]: `Point` cannot cross the FFI boundary, and it is an `extern`'s parameter — a C header can declare `i64`, `f64`, `bool`, `str`, `ptr` and `cstr`, and nothing else (§4.19)
