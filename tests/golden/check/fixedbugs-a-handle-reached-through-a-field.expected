tests/golden/check/fixedbugs-a-handle-reached-through-a-field.hero:45:14: error[unmarked_handle_producer]: `pair_make` hands back a `Slot` inside `Pair.s` and does not say whether the program owes it — `slot_close` takes one back, so this binding has said how a `Slot` ends and not how it begins
tests/golden/check/fixedbugs-a-handle-reached-through-a-field.hero:46:14: error[unmarked_handle_producer]: `outer_make` hands back a `Slot` inside `Outer.p.s` and does not say whether the program owes it — `slot_close` takes one back, so this binding has said how a `Slot` ends and not how it begins
tests/golden/check/fixedbugs-a-handle-reached-through-a-field.hero:47:14: error[unmarked_handle_producer]: `four_make` hands back a `Slot` inside `Four.a` and does not say whether the program owes it — `slot_close` takes one back, so this binding has said how a `Slot` ends and not how it begins
tests/golden/check/fixedbugs-a-handle-reached-through-a-field.hero:48:32: error[unmarked_handle_producer]: `pair_out` hands back a `Slot` inside `Pair.s` and does not say whether the program owes it — `slot_close` takes one back, so this binding has said how a `Slot` ends and not how it begins
