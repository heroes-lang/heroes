# Bulk regression case (assistant-written): a type name that resolves to
# nothing, and one that resolves to something that is not a type.
record Point
    x: int

function dist(p: Poimt) -> int
    return p.x

function use_it(f: dist) -> int
    print(f)
    return 0
