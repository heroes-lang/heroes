tests/golden/check/fixedbugs-a-lend-c-keeps-past-the-frame.hero:34:13: error[lend_kept]: `.ptr()` lends bytes to C for this call and no longer, and `p` of `keep` is not declared to let them go — a C parameter without `lent` is assumed to keep what it is handed, and bytes kept past the call are read after the program has changed or freed them
tests/golden/check/fixedbugs-a-lend-c-keeps-past-the-frame.hero:38:19: error[lend_kept]: `.cstr()` lends bytes to C for this call and no longer, and `s` of `keep_label` is not declared to let them go — a C parameter without `lent` is assumed to keep what it is handed, and bytes kept past the call are read after the program has changed or freed them
