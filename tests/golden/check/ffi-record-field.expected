tests/golden/check/ffi-record-field.hero:16:15: error[ffi_field_type]: `str` cannot be a field of a `record` in an `extern` group — the header owns the layout, and it can hold a number, `bool`, `ptr`, `cstr`, or another record of the group (§4.19)
tests/golden/check/ffi-record-field.hero:17:13: error[ffi_field_type]: `[i64]` cannot be a field of a `record` in an `extern` group — the header owns the layout, and it can hold a number, `bool`, `ptr`, `cstr`, or another record of the group (§4.19)
tests/golden/check/ffi-record-field.hero:18:17: error[ffi_field_type]: `{str: i64}` cannot be a field of a `record` in an `extern` group — the header owns the layout, and it can hold a number, `bool`, `ptr`, `cstr`, or another record of the group (§4.19)
tests/golden/check/ffi-record-field.hero:19:16: error[ffi_field_type]: `i64?` cannot be a field of a `record` in an `extern` group — the header owns the layout, and it can hold a number, `bool`, `ptr`, `cstr`, or another record of the group (§4.19)
tests/golden/check/ffi-record-field.hero:20:18: error[ffi_field_type]: `()` cannot be a field of a `record` in an `extern` group — the header owns the layout, and it can hold a number, `bool`, `ptr`, `cstr`, or another record of the group (§4.19)
tests/golden/check/ffi-record-field.hero:21:16: error[ffi_field_type]: `Local` cannot be a field of a `record` in an `extern` group — the header owns the layout, and it can hold a number, `bool`, `ptr`, `cstr`, or another record of the group (§4.19)
