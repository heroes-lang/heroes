tests/golden/check/fixedbugs-the-librarys-extern-constants-are-not-in-scope.hero:31:11: error[extern_across_modules]: `HERO_OS_OK` is the library's own `extern` — an extern is never reachable across a module boundary, and the library is a module
tests/golden/check/fixedbugs-the-librarys-extern-constants-are-not-in-scope.hero:32:11: error[extern_across_modules]: `HERO_OS_NOT_FOUND` is the library's own `extern` — an extern is never reachable across a module boundary, and the library is a module
tests/golden/check/fixedbugs-the-librarys-extern-constants-are-not-in-scope.hero:33:11: error[extern_across_modules]: `HERO_STR_OK` is the library's own `extern` — an extern is never reachable across a module boundary, and the library is a module
