tests/golden/check/fixedbugs-a-lend-that-leaves-its-frame.hero:27:27: error[cstr_out_of_heroes]: `built` answers `cstr`, and a `cstr` is C's pointer rather than this language's string (§4.19) — only a function inside an `extern` group may answer one
tests/golden/check/fixedbugs-a-lend-that-leaves-its-frame.hero:28:13: error[cstr_escapes]: `.cstr()` lends this string to C for the length of one call (§4.19), and a lend may stand only as an argument of a call — here it stands somewhere the pointer outlives the bytes it points into
