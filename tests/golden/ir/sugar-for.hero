# Part 5, row 2: `for x in xs` becomes a `while` with an index. Four blocks,
# and the fourth is the point: `continue` lands on the step, so the increment
# happens on that path too.
function total_of(xs: [i64]) -> i64
    sum: i64 @ 0

    for x in xs
        sum @ sum + x

    return sum
