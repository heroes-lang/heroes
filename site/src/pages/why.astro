---
import BaseLayout from '../layouts/BaseLayout.astro';
import { page } from '../lib/figures.ts';
import content from '../html/why.html?raw';
---
<BaseLayout url="/why/" title={'Why Heroes: the thesis, and the founding constraint'} description={'Heroes started from a refusal: no library of its own for the world. Everything comes from C through one FFI that clang verifies. And every plausible LLM mistake is a compile error.'} current="why">
<Fragment set:html={page(content, 'site/src/html/why.html')} />
</BaseLayout>
