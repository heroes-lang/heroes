---
import BaseLayout from '../layouts/BaseLayout.astro';
import { page } from '../lib/figures.ts';
import content from '../html/why.html?raw';
---
<BaseLayout url="/why/" title={"Why Heroes: the design choices"} description={"Why Heroes uses explicit syntax, checks common model mistakes and relies on C libraries. The results measured so far and what remains to be tested."} current="why">
<Fragment set:html={page(content, 'site/src/html/why.html')} />
</BaseLayout>
