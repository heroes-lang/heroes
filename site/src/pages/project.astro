---
import BaseLayout from '../layouts/BaseLayout.astro';
import { page } from '../lib/figures.ts';
import content from '../html/project.html?raw';
---
<BaseLayout url="/project/" title={'The project: the fixpoint, the panel, and the Zen'} description={'The Heroes compiler is written in Heroes and reproduces its own C byte for byte. How a change to the language is decided by five judges, and the twenty lines the binary prints.'} current="project">
<Fragment set:html={page(content, 'site/src/html/project.html')} />
</BaseLayout>
