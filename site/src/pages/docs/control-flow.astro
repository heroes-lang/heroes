---
import BaseLayout from '../../layouts/BaseLayout.astro';
import content from '../../html/docs/control-flow.html?raw';
---
<BaseLayout url="/docs/control-flow/" title={'Control flow, Heroes documentation'} description={'Two loops and one conditional. if takes only bool because there is no truthiness, for walks a sequence, while repeats on a condition, and a range is an ordinary function call.'} current="docs">
<Fragment set:html={content} />
</BaseLayout>
