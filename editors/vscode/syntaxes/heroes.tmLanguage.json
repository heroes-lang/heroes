{
  "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
  "name": "Heroes",
  "scopeName": "source.hero",
  "fileTypes": ["hero"],
  "patterns": [
    { "include": "#section" },
    { "include": "#comment" },
    { "include": "#foreign" },
    { "include": "#keyword" },
    { "include": "#builtin-type" },
    { "include": "#hole" },
    { "include": "#string" },
    { "include": "#char" },
    { "include": "#number" },
    { "include": "#declaration" },
    { "include": "#operator" }
  ],
  "repository": {
    "section": {
      "comment": "`##` opens a section heading (spec: comment text is markdown)",
      "match": "^\\s*##.*$",
      "name": "markup.heading.hero"
    },
    "comment": {
      "comment": "A comment directly above a declaration is its documentation",
      "match": "#.*$",
      "name": "comment.line.number-sign.hero"
    },
    "foreign": {
      "comment": "spec/reserved-words.md — keywords imported from another language. Marked INVALID on purpose: the editor shows the mistake before the compiler does. `use` was in this list and is a Heroes keyword, so every module line in every file showed as an error; it now sits in #keyword, where the spec puts it (`a module is named with use`). The list is otherwise the spec's own table, word for word.",
      "match": "\\b(struct|enum|union|class|fn|func|def|let|var|const|elif|switch|case|null|nil|None|try|catch|throw|raise|import|include)\\b",
      "name": "invalid.illegal.foreign-keyword.hero"
    },
    "keyword": {
      "comment": "spec/reserved-words.md's closing paragraph, and selfhost/keywords.hero::keyword. `true` and `false` were filed under #builtin-type, which is where a value is not: both tables call them keywords.",
      "match": "\\b(constant|function|record|variant|match|if|else|for|while|in|break|continue|return|test|assert|extern|nullptr|use|true|false|fail)\\b",
      "name": "keyword.control.hero"
    },
    "builtin-type": {
      "comment": "spec/heroes-spec.md § Types, the whole table. This read `int|f64|bool|str|ptr|cstr` and so coloured a word the language does not have while leaving every sized integer plain: `i64` is what a program actually writes, and it was the one type never highlighted. `[T]`, `{K: V}` and `T?` are shapes rather than words and are built out of these.",
      "match": "\\b(i8|i16|i32|i64|u8|u16|u32|u64|f32|f64|bool|str|ptr|cstr)\\b",
      "name": "support.type.hero"
    },
    "hole": {
      "comment": "`???` is a valid expression, not an error (§4.16)",
      "match": "\\?\\?\\?",
      "name": "markup.italic.hole.hero"
    },
    "string": {
      "begin": "\"",
      "end": "\"",
      "name": "string.quoted.double.hero",
      "patterns": [
        {
          "comment": "spec § 2: five in a string, `\\n` `\\t` `\\r` `\\\\` `\\\"`. This read `[nt\\\\\"]` and said `exactly these four` until 2026-09-13, so a legal `\\r` fell through to the rule below and a CORRECT program was painted `invalid.illegal`. Measured the day it was fixed: `print(\"a\\rb\")` runs at exit 0. `spec/heroes-spec.md:39` and `selfhost/escape.hero`'s `escape_text` are the two sides the `spec/colouring` check now holds together.",
          "match": "\\\\[ntr\\\\\"]",
          "name": "constant.character.escape.hero"
        },
        {
          "comment": "the backslash is reserved — anything else is a compile error",
          "match": "\\\\.",
          "name": "invalid.illegal.unknown-escape.hero"
        }
      ]
    },
    "char": {
      "comment": "one ASCII character or one escape; its value is an int (§4.3). The escape class carried `[nt\\\\']` until 2026-09-13 and so painted `'\\r'` as `invalid.illegal.char-literal.hero` by falling through to the third pattern — the same missing `r` as in the string rule above, in the second of the two places it had to be written by hand.",
      "patterns": [
        { "match": "'\\\\[ntr\\\\']'", "name": "constant.character.hero" },
        { "match": "'[\\x20-\\x7E]'", "name": "constant.character.hero" },
        { "match": "'[^']*'", "name": "invalid.illegal.char-literal.hero" }
      ]
    },
    "number": {
      "patterns": [
        { "match": "\\b[0-9]+\\.[0-9]+\\b", "name": "constant.numeric.float.hero" },
        { "match": "\\b[0-9]+\\b", "name": "constant.numeric.integer.hero" }
      ]
    },
    "declaration": {
      "comment": "`function dist2(…)` / `record Point` — the kind opens the line, the name follows (panel 018)",
      "match": "^\\s*(?:extern\\s+)?(?:constant|function|record|variant)\\s+([A-Za-z_][A-Za-z0-9_]*)",
      "captures": { "1": { "name": "entity.name.function.hero" } }
    },
    "operator": {
      "patterns": [
        { "match": "->|=>|==|!=|<=|>=|&&|\\|\\||::", "name": "keyword.operator.hero" },
        { "match": "@", "name": "keyword.operator.mutable.hero" },
        { "match": "[-+*/%<>=!?|.]", "name": "keyword.operator.hero" }
      ]
    }
  }
}
