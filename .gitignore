# Rust
/target

# heroes build cache (generated C, runtime.o, binaries — all derived)
/build

# bootstrap stages (derived)
stage[0-9]
*.o

# author's raw prediction records — personal, never published (CLAUDE.md rule 3)
/docs/journal/private/

# macOS
.DS_Store
